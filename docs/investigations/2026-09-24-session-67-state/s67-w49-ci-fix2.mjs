export const meta = {
  name: 's67-w49-ci-fix2',
  description: 'Second CI round for PR #178 (W4-9): after the hermeticity fix the curl_cffi guard now catches Shopify catalog fetches in CI that the patch.object stub stops locally - find the module-identity mechanism in the CI order, make the stub hold regardless, verify. One Opus 5.5 fixer then a verifier; the orchestrator commits.',
  phases: [
    { title: 'Fix', detail: 'reproduce the CI order locally, fix the stub binding, prove it', model: 'claude-opus-5-5' },
    { title: 'Verify', detail: 'independent check', model: 'claude-opus-5-5' },
  ],
}
const MODEL = 'claude-opus-5-5'
const MYSP = 'C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/75660a55-e14a-4de1-80dc-a537f2af315e/scratchpad'
const VENV = 'C:/Users/SynAckITPC/Documents/AI/.venv-qaren/Scripts/python.exe'
const DIR = 'C:/Users/SynAckITPC/Documents/AI/sc-w4-9'

const COMMON = `
ENVIRONMENT AND HARD RULES (violating any fails the task):
- Windows 11. Backend FastAPI, Python 3.12. ALWAYS run backend tests with the pinned venv ${VENV} (the exact CI pins); PYTHONIOENCODING=utf-8; -q -p no:cacheprovider -p no:randomly --timeout=120; sets add -m "not (live_unit or live_db or integration)". Lint: ${VENV} -m ruff check --select E9,F63,F7,F82 --no-cache; py_compile.
- Backend files are CRLF in the working copy: use the Edit tool; git diff --stat must show only intended lines.
- NEVER git commit/push/checkout/stash/reset/rebase/clean in any existing worktree; you write files only; the orchestrator commits. NEVER 'git checkout -- <file>'; byte-snapshot before every mutation, restore from it, sha256-verify. Work ONLY in ${DIR} plus your scratchpad; other agents work in the other sc-* worktrees concurrently - never touch them. Never run a recursive delete. Never install packages. Never live_db/live_unit/integration, never LIVE=1, never any Railway MCP tool, never print an env value.
- Run EVERY pytest invocation with a process-wide egress guard loaded as a plugin BEFORE conftest (-p <module> from your scratchpad on PYTHONPATH) that blocks non-loopback socket connect/connect_ex/getaddrinfo AND curl_cffi (requests.Session.request, requests.AsyncSession.request, curl.Curl.perform, curl.AsyncCurl.add_handle) - the verifier of round 1 wrote one at scratchpad/advw49/advnetguard.py; reuse or copy it. Never import app.* before conftest.
- Report honestly; paste real summary lines; final text is raw structured data. Your report MUST carry the final sha256 of every file you wrote.`

const TASK = `PR #178 (unit W4-9, worktree ${DIR}, CLEAN at b2463c82 = the unit + round-1 hermeticity commit dddd03b3 rebased onto main d575c7f5; git status --short must be empty - confirm FIRST) failed CI backend-tests AGAIN (job log: ${MYSP}/ci_log_178_107502495446.txt; the round-1 log for comparison: ${MYSP}/ci_log_178_107459482414.txt; the round-1 fixer/verifier reports: ${MYSP}/harvest_pr_ci_fix.json).
WHAT CI SHOWS NOW: the same two nodes, tests/test_w49_extraction_catch_redaction.py::test_r2e_quota_outage_on_the_phone_path_leaks_no_org_id[sync|sse], fail at the socket_guard teardown, but the Serper leak is GONE (the _serper_unconfigured fixture holds) and the offenders are now six curl_cffi records per node: ('curl_cffi', 'en-bh.ajmal.com'/'alhajisbahrain.com'/'bh.asgharali.com') x2 - i.e. price_service._fetch_shopify_catalog RAN in CI (its log line '[PRICE] Shopify catalog fetch failed for <host>: W4-9 socket guard: curl_cffi GET ... blocked' appears at price_service.py:14987) even though the quota_outage fixture does patch.object(price_service, '_fetch_shopify_catalog', AsyncMock(return_value=None)). The noon stub (patch.object(scs, 'fetch_noon_price')) DID hold - no noon record. Locally the file passes alone and in the round-1 reproduction order (15/15; 279 passed with the neighbours), so the stub works here and not in the single-process CI order.
ORCHESTRATOR'S GREP (verify, do not assume): tests/test_platform_router.py:321-328 does 'for mod in [m for m in sys.modules if 'price_service' in m]: del sys.modules[mod]' and asserts none remain (an import-cycle proof), so the NEXT import of app.services.price_service builds a FRESH module object; app/services/structured_comparison_service.py:1196-1247 binds 'fetch_shopify_price' BY NAME at its own import ('from app.services.price_service import (... fetch_shopify_price ...)'), so scs keeps the OLD function whose __globals__ is the OLD module dict, while the test's 'from app.services import price_service' (line 437) yields the NEW module - patch.object on the new module never reaches the old function; the noon stub holds because it patches scs itself. In the CI order test_platform_router.py runs before ours; locally it does not. Confirm this exact mechanism first (run tests/test_platform_router.py then ours in one process), then fall back to the wider hypotheses only if it does not reproduce.
WIDER HYPOTHESES: the module object the stub patches is not the one whose code runs. Candidates: (a) an earlier test in the CI order re-imports price_service as a FRESH module (sys.modules pop/del + import, or patch.dict(sys.modules)), after which structured_comparison_service (or wherever fetch_shopify_price is bound) keeps function objects whose __globals__ is the OLD module dict, so patching app.services.price_service (the new object) does nothing for them - or the reverse; (b) an importlib.reload(price_service) in one of tests/test_backend_cleanup.py, test_cache_service_bounded_transport.py, test_fan_out_budget_env.py, test_limiter_endpoint_key.py, test_m18_openai_tpm_sizing.py, test_nasser_adapter.py, test_observability.py, test_platform_router.py, test_supplement_branch_genuine.py combined with a name-binding elsewhere; (c) the call site resolution: price_service.py:15375 'catalog = await _fetch_shopify_catalog(domain)' inside fetch_shopify_price - check how scs imports fetch_shopify_price (by name at import time?) and which module dict that function closes over after a reload/re-import. The CI file order is in the log (grep '^tests/test_' after stripping the 29-char timestamp prefix); the file before ours is tests/test_visible_text_currency_rung.py and the whole suite ran ~583 files before it.
YOUR TASK: (1) REPRODUCE locally: first try the cheap targeted prefixes (each reload/re-import candidate file above + ours in one process), then if none reproduces, bisect the CI prefix (halves of the ordered file list before ours, always ending with our file) until the two nodes fail with the curl_cffi records; record the minimal offending file and the exact mechanism (which module object, which binding). (2) FIX inside tests/test_w49_extraction_catch_redaction.py ONLY (nothing under app/): make the Shopify stub hold regardless of module identity - e.g. resolve the live module via sys.modules['app.services.price_service'] AND patch the function object that fetch_shopify_price actually calls (its __globals__), or stub one level up (fetch_shopify_price itself in every module that binds it), or stub the curl_cffi call inside _fetch_shopify_catalog; choose the one that is robust to BOTH reload and fresh-import, and explain why. Keep the guard's assertion untouched. (3) PROVE: the reproduction order fails before the fix and passes after; the file alone 15/15; the round-1 repro order (tests/test_hotfix_shopping_query_clean.py first) still green; the neighbours (tests/test_text_error_envelope_no_raw_exception.py tests/test_m13_26_image_error_envelope.py tests/test_text_routes_error_mapping.py tests/test_http_400_cap_cut_mapping.py tests/test_prescoring_showable_guard.py) green; mutation: removing the new binding fix re-fails the reproduction; ruff + py_compile. (4) If the offender is a pre-existing test's module re-import that also endangers OTHER files, name it in follow_up (issue #183 already covers the Serper reload leak; #184 the curl_cffi guard gap). (5) pr_text = a 'CI hermeticity note, round 2' paragraph for the PR body. Leave only the test file modified (uncommitted); report git status --short verbatim.`

const FIX_SCHEMA = { type: 'object', required: ['unit', 'reproduction', 'root_cause', 'files_changed', 'test_evidence', 'mutation_proof', 'lint_clean', 'pr_text', 'follow_up', 'git_status_final', 'file_sha256'], properties: {
  unit: { type: 'string' }, reproduction: { type: 'string', description: 'the minimal local order that reproduces the CI failure, with the summary lines before and after the fix' }, root_cause: { type: 'string' }, files_changed: { type: 'array', items: { type: 'string' } }, test_evidence: { type: 'string' },
  mutation_proof: { type: 'string' }, lint_clean: { type: 'boolean' }, pr_text: { type: 'string' }, follow_up: { type: 'array', items: { type: 'string' } }, git_status_final: { type: 'string' }, file_sha256: { type: 'array', items: { type: 'string' } } } }
const VERIFY_SCHEMA = { type: 'object', required: ['unit', 'verdict', 'defects', 'summary', 'worktree_left_byte_identical'], properties: {
  unit: { type: 'string' }, verdict: { type: 'string', enum: ['SOUND', 'DEFECTIVE'] },
  defects: { type: 'array', items: { type: 'object', required: ['severity', 'location', 'claim', 'reproduced'], properties: { severity: { type: 'string', enum: ['blocking', 'major', 'minor'] }, location: { type: 'string' }, claim: { type: 'string' }, reproduced: { type: 'boolean' } } } },
  summary: { type: 'string' }, worktree_left_byte_identical: { type: 'boolean' } } }

log('w49-ci-fix2: start')
const fix = await agent(`You are fixing the second CI failure of PR #178 (unit W4-9).\n${TASK}\n${COMMON}`, { label: 'fix:W4-9-r2', phase: 'Fix', model: MODEL, effort: 'high', schema: FIX_SCHEMA })
if (!fix) return { fix: null, verify: null, final_verdict: 'fixer died' }
const verify = await agent(`You are an ADVERSARIAL verifier for the round-2 CI fix of PR #178 (unit W4-9) in worktree ${DIR} (base commit b2463c82; the fixer's UNCOMMITTED edits are exactly git diff HEAD). The fixer reported:\n${JSON.stringify(fix, null, 1)}\nThe task it was given:\n${TASK}\nAssume it is wrong until checked: read the diff (nothing under app/ may have changed; the guard assertion must be intact); re-run the fixer's reproduction order yourself and confirm it FAILED at HEAD-without-the-fix (use a byte snapshot of the test file, restore the pre-fix bytes from git show HEAD:<path> into your scratchpad copy - never git checkout in the worktree) and PASSES with the fix; re-run the file alone, the round-1 order and the neighbours; judge whether the chosen binding fix is robust to both importlib.reload and a fresh re-import (demonstrate with a scratch script if in doubt). Leave the worktree byte-identical to the fixer's state. reproduced:true only if you ran it. Do not fix anything. If sound, say SOUND.\n${COMMON}`, { label: 'verify:W4-9-r2', phase: 'Verify', model: MODEL, effort: 'high', schema: VERIFY_SCHEMA })
log('w49-ci-fix2: ' + (verify ? verify.verdict : 'verifier died'))
return { fix, verify, final_verdict: verify ? verify.verdict : 'verifier died' }
