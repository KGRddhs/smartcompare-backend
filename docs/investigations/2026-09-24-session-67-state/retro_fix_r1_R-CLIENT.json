{
 "unit": "R-CLIENT / W1-4d retro fix, fixer pass on the adversary's review of the green (wip 26b6b100 above base 1c6f6796). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-client was clean at start: git status --short was empty and HEAD was 26b6b100. The major cross-session defect (ADV-A1) is fixed: a refresh can now hand its rotated pair to a logout only if it was sent under the session that logout ends. All three minor findings are pinned, and nine of the ten surviving mutants are dealt with (seven killed by new pins, two shown equivalent). Nothing was committed.",
 "files_changed": [
  "SmartCompareApp/src/services/authService.ts",
  "SmartCompareApp/__tests__/authService.logoutRefreshFirst.w1-4d.test.ts"
 ],
 "all_unit_tests_pass": true,
 "test_evidence": "All runs use jest 29.7.0 by path from SmartCompareApp (node node_modules/jest/bin/jest.js --ci).\n\nRed first:\n- I added 12 new tests to w1-4d before changing the source. At wip 26b6b100 the file ran 2 failed / 36 passed of 38.\n- ADV-A1 was red. Received: 'Bearer <VALID_AT2 of user A>'; expected: 'Bearer <VALID_B>'.\n- The release pin was red because the test-only accessor did not exist yet.\n- The other 10 new tests pass at the wip. Their job is to kill the surviving mutants N03, N07, N08, N12, N18 and N19, which they now do (see mutation_checks).\n\nAfter the fix:\n- The three unit files (w1-4d, w1-4, a3) pass 68/68: 38 + 10 + 20.\n- The seven auth suites (those three plus api.refreshInterceptor, api.refreshMutex, api.refreshMutex.branches and authService.m18) pass 103/103 under --detectOpenHandles, with no open-handle report.\n- tsc 5.9.3 --noEmit: exit 0.\n- eslint 9.39.4 on all five unit files: 0 errors and 21 warnings. The count is unchanged from the green, and every warning is on a pre-existing line; authService.ts:321 'e' unused is the base's `catch (e)`.\n\nFull jest suite, run ONCE at the end with --ci --maxWorkers=25% (never -u):\n- 290 suites: 287 passed, 3 skipped, 0 failed.\n- 2880 tests: 2854 passed, 13 skipped, 13 todo, 0 failed.\n- Snapshots: 44/44.\n- Against the orchestrator's baseline (281 / 2752 / 44 at ed75dc70) and the red's base (290 / 2865 / 44 at 1c6f6796): +15 tests. That is the green's +3 plus this pass's +12, and nothing is lost.\n- No suite fails, so there is nothing to explain. The green's single failure, HistoryScreen.mobileJank.m21 MB-flows-08, passed in this run. That file mocks both services/api and services/authService, so it cannot involve this unit's files. The green already showed it fails identically with the base sources; it looks like a date- or order-dependent flake.\n- Jest printed 'A worker process has failed to exit gracefully' once. The green saw the same, and --detectOpenHandles over the seven auth suites is clean, so the leaking suite is elsewhere.\n- JSON output: scratchpad/rclient_fix/full.json. Log: scratchpad/rclient_fix/full.log.",
 "lint_clean": true,
 "mutation_checks": [
  "Method: the driver is scratchpad/rclient_fix/mutate.js with the mutants in scratchpad/rclient_fix/mutants.json. Each mutant was applied to the fixed-source byte snapshot (scratchpad/rclient_fix/snap/authService.ts, sha d18190037893...). The same 7 auth suites the adversary used were run: 103 tests. The file was then restored from the snapshot and sha256-verified; every restore printed d1819003. git was never used to restore.",
  "F01, the ADV-A1 mutant (fill every registered handoff regardless of epoch identity): KILLED by the ADV-A1 pin.",
  "F02 (handoff keyed to the post-bump epoch, so it never matches): KILLED (7), including 'sends the NEW pair' and the storage-never-written pin.",
  "N05 (handoff never released): KILLED by the new release pin, which asserts __pendingLogoutHandoffCount() is 1 while preparing and 0 after logout and after the hung refresh lands.",
  "N07 (success flag ignored on the fill): KILLED by the new 'a 200 refresh with success:false is never handed off' pin.",
  "N19 (`!inflight` instead of `!refreshStillInFlight`): KILLED by the new pin 'costs exactly one more bounded refresh, and the logout carries the pair it produced'. The wire order is refresh, refresh, logout, and the POST carries AT2/RT2.",
  "N03 (clearTimeout removed from settlesWithinLogoutBound's finally): KILLED (2) by the new 'no timer is pending' pins (jest.getTimerCount() === 0, one for the in-flight path and one for the expired path).",
  "N08 (`<=` to `<` at the exp boundary): KILLED by the new pin 'exp equal to now counts as expired', with Date.now pinned. A +1 s control proves the boundary is real.",
  "N12 (base64url length%4==1 guard removed): KILLED. Without the guard, an aligned expired payload plus one trailing char decodes to valid JSON and is treated as expired. The pin sees no refresh; a positive control shows the aligned payload alone does refresh.",
  "N18 (Number.isFinite dropped): KILLED. The adversary's claim that 'JSON cannot yield a non-finite number' is wrong, and I measured it: JSON.parse('{\"exp\":-1e999}').exp === -Infinity, which the pin asserts as its premise. Without isFinite, -Infinity*1000 <= now is true and the token counts as expired.",
  "Green-matrix mutants re-run on the fixed code: G02 (unbounded wait) KILLED (3); G03 (no epoch bump) KILLED (3, including a3's 'logout mid-flight is not undone'); G04 (refreshSession never fills) KILLED (7); G08 (no clear before the POST) KILLED (4); G09 (two logout POSTs) KILLED (32); G12 (clearSession removed from finally) KILLED (2).",
  "SURVIVED, equivalent: N04 (`logoutHandoffs.clear()` instead of `delete(handoff)`). Every refresh goes through the api.ts singleton, and refreshSession's only caller is performRefresh (grep). So two concurrent logouts always wait on the same in-flight promise, and the earlier one resumes and reads its handoff first. A later logout can never finish while an earlier one is still preparing, so the difference is unreachable.",
  "SURVIVED, equivalent: N06 (no `|| refreshToken` fallback). The fallback value is the refresh token refreshSession read from SecureStore. Without it, pair.refresh is undefined and logout reads the same SecureStore key, which still holds that value because the epoch-guarded refresh wrote nothing. The wire bytes are identical, which the new 'handed-off session without a refresh_token' pin shows (Bearer AT2, body {refresh_token: RT1}). The only divergence is a SecureStore read that throws inside logout: {} versus {refresh_token: RT1}.",
  "SURVIVED, equivalent by the same singleton argument: F03 (`h.epoch <= epochAtRequest` instead of `===`). A refresh with a later epoch cannot start while an earlier one is in flight, because of the singleton, and after the in-flight wait each logout reads its handoff within the same microtask chain. So no later-epoch refresh can land on an unread handoff. The direction that matters for ADV-A1, an older refresh filling a newer logout, is excluded by both `<=` and `===`."
 ],
 "comm_gate": "This is a client-only unit, so the gate is the full jest suite run once on HEAD, which contains the comm-gate file set.\n\n- HEAD (wip 26b6b100 plus this pass): 290 suites, 2880 tests, 44 snapshots. FAILED set = NONE.\n- Base (the red's run at 1c6f6796): FAILED set = NONE.\n- The green's own comm set: 68 suites / 712 tests, 0 failed.\n- comm -13 of base FAILED against HEAD FAILED is EMPTY.\n\nNo scratch worktree was created. HEAD's only diff from base is this unit's files, and the base run already exists. No backend gate was run (ruff, py_compile, pytest), because no app/ or .py file is in scope; R-AUTH owns the backend. The orchestrator re-runs the suite after the rebase onto d70dd876.",
 "flag_identity": "UNFLAGGED, the same as the green and the M13-W4/M23 client precedent. React Native has no per-call env flag, and the change is OTA-gated: phones on 97b5f15 are unaffected until the next eas update.\n\nThis pass only narrows the green: the handoff fill gains one identity condition. On every path with no in-flight refresh, nothing is registered when a refresh lands, so nothing changes. That covers the unexpired JWT, the six expired-UNKNOWN pins, the no-token case and the whole pre-existing w1-4 suite, and all stay green.\n\nOn the in-flight path from the same session (the refresh was sent before this logout's bump, with no intervening clearSession or logout), epochAtRequest equals the handoff's epoch. The fill is therefore identical to the green's: 'sends the NEW pair' and the storage-never-written pin are unchanged and green.\n\nThe only paths whose behaviour changes are the ones where the refresh belongs to an earlier session generation (ADV-A1). There the logout now sends the stored pair of the session it is ending, which is base behaviour.\n\nThe one new export, __pendingLogoutHandoffCount(), is test-only: read-only and never called from src.",
 "measurements_run": [
  "Start: git status --short was empty and HEAD was 26b6b100. Byte snapshots of all five wip files are in scratchpad/rclient_fix/wip_snap, with shas equal to the green's recorded ones (api cb703d00, authService d465d683, w1-4d d5625a64, w1-4 4197173c, a3 77bd160b).",
  "Re-derived ADV-A1 from code: logoutHandoff was a module-global filled by any epoch-guarded refresh while any logout prepared. clearSession() (line ~605) and logout both bump sessionEpoch and login() does not, so A's refresh (epochAtRequest e0) cannot equal the pre-bump epoch of B's logout, which is at least e3 after logout #1's bump and its two clearSessions. Keying on the pre-bump epoch is therefore sufficient.",
  "Red run of the 12 new w1-4d tests at wip: 2 failed / 36 passed (ADV-A1 plus the release pin).",
  "After the fix: 3 unit files 68/68; 7 auth suites 103/103 with --detectOpenHandles clean.",
  "Mutation matrix: 18 mutants. 15 killed, 3 equivalent survivors (N04, N06, F03) with reasons given.",
  "tsc 5.9.3 --noEmit exit 0; eslint 9.39.4 0 errors (21 pre-existing warnings, each located by line).",
  "Full jest once, --maxWorkers=25%: 290 / 2880 / 44, 0 failed.",
  "grep: refreshSession() has exactly one caller, api.ts performRefresh (line 121). This is the basis of the equivalence argument.",
  "CRLF check: authService.ts still has CRLF line endings; git diff --stat shows only +39/-10 in authService.ts and +270 in the w1-4d test.",
  "No git write of any kind, no scratch worktree, no network (the zero-network guard is autouse in w1-4d), no package install. The node_modules junction was untouched (@babel/core present)."
 ],
 "pr_text": "fix(mobile/auth): W1-4d \u2014 logout presents a fresh session to /auth/logout (retro R-CLIENT, OTA-gated)\n\nDEFECT (retro adversary on #140, reproduced)\nUpstream revocation is skipped silently whenever logout carries an expired or stale access token:\n- The server's get_current_user 401s the Bearer before logout_user runs.\n- /auth/logout is on the 401 interceptor's skip-refresh list.\n- logout() neither waited for a refresh already in flight nor refreshed an expired token.\nIn the in-flight race, the server finishes rotating RT1 to RT2 while the phone's P-A3 epoch guard discards RT2, so RT2 stays live upstream and nothing revokes it. Measured through the real api.ts and authService.ts: the wire Bearer was 'AT1-expired'.\n\nFIX (client only: SmartCompareApp/src/services/{api,authService}.ts)\n- api.ts: ONE new read-only export, getInFlightRefresh(): Promise<RefreshResult> | null. It never starts a refresh.\n- authService.logout():\n  1. Registers a handoff keyed to the session generation it ends (the epoch before its bump), then bumps the P-A3 epoch FIRST. A refresh that lands after the bump neither writes storage nor fires onSessionRefreshed (today's guard). It hands its rotated pair to the waiting logout IN MEMORY, and only when the epoch it was sent under equals that logout's epoch. A refresh from an EARLIER session can therefore never supply the pair, for example one hung across logout, then login(B), then logout (the second adversary's ADV-A1: B's logout sent A's rotated pair and left B unrevoked). Each logout removes its own handoff in finally.\n  2. Waits for an in-flight refresh, raced against a 5 s bound (LOGOUT_REFRESH_WAIT_MS). A black-holed refresh cannot hold the tap, and the request itself is never cut short (P-A3).\n  3. If the access token is a JWT whose `exp` has passed (exp <= now), starts ONE refresh through getOrStartRefresh() under the same bound. The payload is decoded without verification and with no atob/Buffer. Malformed tokens are expired-UNKNOWN and are sent as today: bad base64url (including length 1 mod 4), non-JSON, and a missing or non-finite exp. The refresh is skipped when the in-flight wait timed out, because it would coalesce onto the hung promise. After an in-flight refresh that FAILED within the bound, an expired token still gets its one refresh (pinned).\n  4. Clears the local session, then sends exactly ONE POST with the freshest pair held. No retry loop. clearSession also stays in finally.\n  Why the clear comes before the POST: the request interceptor stamps the STORED token over an explicit Authorization header whenever one is stored, so clearing is what lets an in-memory pair reach the wire.\n  Every preparation step swallows its own errors, so at worst the stored pair is sent, exactly as before.\n- A pre-logout refresh that returns 401 clears state through the existing dead-session path, and then no logout POST is sent (at most one; ruling 4).\n- The logout JSDoc no longer claims \"Supabase revokes a session by its REFRESH token\". That claim is false on the valid-token path (measured by the retro).\n- New test-only export __pendingLogoutHandoffCount(). It is read-only and never called from src; it exists to pin that the handoff is released.\n\nTESTS\n- New suite __tests__/authService.logoutRefreshFirst.w1-4d.test.ts, 38 tests. It runs the real api.ts and authService.ts with a stub adapter on the real axios instance, behind an autouse zero-network guard. Pins added on top of the red:\n  - Fable ruling pins: the black-holed refresh (fake timers); the storage-never-written pin.\n  - Second-adversary pins: ADV-A1 (a refresh hung across logout, login(B), logout still posts B's pair); handoff release; success:false never handed off; a handed-off session without refresh_token sends AT2 with the stored RT; an in-flight transient failure plus an expired token costs one more refresh; no timer left pending (two cases); exp == now is expired, with a +1 s control; a length-1-mod-4 payload is malformed, with an aligned control; a non-finite exp is unknown.\n- __tests__/authService.logoutRevoke.w1-4.test.ts:\n  - 'sends the SAME stored value' now drives login() and refreshSession() instead of repeating a literal key;\n  - the SecureStore-throws spy is restored in finally;\n  - new pin: a failure inside the preparation never costs the POST.\n- __tests__/authService.bootOptimistic.a3.test.ts:528: the ONLY edit resolves the pending boot-refresh POST while logout is pending. Its assertions are unchanged and green.\n- Mutation: the green's 22/22 killed. In this pass, 15 of 18 were killed, including the ADV-A1 identity mutant, handoff release, the success guard, the in-flight-then-expired refresh, timer hygiene and the exp/base64url/isFinite edges. The 3 survivors are equivalent, because all refreshes go through the api.ts singleton: release via clear() versus delete(own), `<=` versus `===` on the epoch, and the spent-RT fallback, whose value equals what storage holds.\n- Full jest suite: 290 suites / 2880 tests / 44 snapshots, 0 failed. Base was 290 / 2865 / 44; the +15 are this unit's added tests.\n- tsc: clean. eslint: 0 errors.\n\nFLAG\nNone. The client change is unflagged, following the M13-W4 and M23 precedent, and it is OTA-gated: phones on 97b5f15 are unaffected until the next `eas update --branch preview`. On the no-refresh, unexpired or opaque-token path, the wire bytes are identical to before (pinned). The only difference is ordering: local state is cleared before the POST instead of after.\n\nCLAUDE.md FLAG-ROW CORRECTION (ENABLE_LOGOUT_UPSTREAM_REVOCATION row, \"SESSION 65 wave W1 flag\" block). Apply at merge:\n- Replace \"INERT until the client sends it\" and \"the next `eas update` is the real activation lever, not the env var\" with:\n  \"ENABLE_LOGOUT_UPSTREAM_REVOCATION is NOT inert until the OTA once the backend calls admin.sign_out(access_token, \"local\") (group R-AUTH). On the valid-access-token path Supabase revokes by the JWT's session, and the refresh-token VALUE never reaches Supabase (retro adversary, measured on the pinned supabase-auth 2.31.0). The client half (PR #140 + W1-4d) matters only for the expired and in-flight paths.\"\n- Also correct \"on the expired path revocation happens via that rotation plus the sign_out on the NEW session\". That path cannot be reached through the route today: get_current_user 401s an expired Bearer before logout_user runs.\n\nBACKEND HALF\nThe backend lands in R-AUTH, not here: admin.sign_out(access_token, \"local\") regardless of the refresh token, the run_db offload of the upstream leg, and the expired-Bearer server path that also covers builds already on phones.\n\nACTIVATION GATE\nThe next `eas update --branch preview`. The server-side effect still needs R-AUTH plus ENABLE_LOGOUT_UPSTREAM_REVOCATION=true. After the OTA, check on-device with a test account: log in, idle past 1 h (or log out during a cold boot refresh), log out, then POST /api/v1/auth/refresh with the old refresh token. It must return 401 once R-AUTH is live.\n\nHONEST LIMITS\n- If the refresh really is black-holed (it takes longer than 5 s), logout sends the pair it has. That is an expired Bearer on the expired path, which the server still 401s. The rotated RT2 that lands later is discarded by the epoch guard and stays live upstream until R-AUTH's server-side expired-Bearer path exists.\n- A later logout may still WAIT up to 5 s on an earlier session's hung refresh. It never uses that refresh's pair.\n- Double-tap logout: only the first tap's handoff can match the in-flight refresh. A second tap sends the stored pair or, once the first has cleared storage, sends nothing. This is the same as base.\n- Expiry is strict (exp <= now), with no leeway. A token that expires in transit can still 401.\n- A 401 on the pre-logout refresh means no logout POST is sent.\n- A transient in-flight failure followed by an expired token costs one more bounded refresh attempt. This is now pinned.\n- Pre-existing and out of scope: refreshSession's 401 branch calls clearSession() with no epoch check, so an earlier session's hung refresh that finally 401s would clear a later session. This exists on base too.\n- In the multi-worker full run, jest printed 'A worker process has failed to exit gracefully' once. --detectOpenHandles over the seven auth suites is clean.\n\nFOLLOW-UPS\n- R-AUTH: admin.sign_out(access_token, \"local\") under the flag with a run_db offload; an optional expired-Bearer server path for pre-OTA builds.\n- Consider a small exp leeway (for example 30 s) in isJwtExpired.\n- Consider epoch-guarding refreshSession's 401 clearSession (the pre-existing cross-session clear).\n- The header of the existing w1-4 test file still repeats the \"revokes by REFRESH token\" premise. It is prose only.\n- HistoryScreen.mobileJank.m21 MB-flows-08 failed in the green's full run and passed in this one. That file mocks both changed modules, so it is flaky; worth its own check.\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)",
 "deviations_from_spec": [
  "Handoff identity: the adversary proposed either recording the pre-bump epoch or keying the handoff to the awaited promise. I did the first. Each logout registers {epoch: pre-bump sessionEpoch, pair} in a module Set, and refreshSession fills only the handoffs whose epoch equals its epochAtRequest. I used a Set rather than the single slot so that a double-tap cannot overwrite the first tap's handoff: with a single slot plus the epoch check, the first logout would lose the pair it is entitled to. Each logout removes its own entry in finally.",
  "authService.ts gained one test-only export, __pendingLogoutHandoffCount(), named in the api.ts `__resetRefreshMutex` style. Release is not observable on the wire by design, so without an accessor the residual-risk claim 'released in finally' (mutant N05) could not be pinned. The ruled 'ONE new export' limit applies to api.ts, which is untouched in this pass (sha cb703d00).",
  "Dispute, partial: the adversary lists N18 as trivially equivalent because 'JSON cannot yield a non-finite number'. Measured: JSON.parse('{\"exp\":-1e999}').exp === -Infinity. Without isFinite that token counts as expired, so N18 is a real mutant and is now killed by a pin.",
  "Dispute, partial: N12 is not equivalent. Removing the length-1-mod-4 guard lets a valid expired payload plus one junk char decode as expired, which triggers a refresh on a malformed token. It is now killed by a pin with a positive control.",
  "Dispute: N06 (the fallback to the spent refresh token) is behaviour-equivalent on the wire. The fallback equals the SecureStore value logout would read anyway, because the epoch-guarded refresh wrote nothing. I kept the code unchanged and pinned the observable result (Bearer AT2, body {refresh_token: RT1}), not the mutant. Sending RT1 alongside a valid AT2 is harmless, because R-AUTH revokes by the access token.",
  "N04 (release clear() versus delete(own)) and the new F03 (`<=` versus `===` on the epoch) are equivalent in every reachable schedule, because refreshSession's only caller is the api.ts singleton. They are documented rather than pinned.",
  "Pins were added only to the w1-4d file; w1-4 and a3 are byte-unchanged from the green. The Python gate items (pytest venv, ruff, py_compile, socket guard) do not apply to this client-only unit. I used the jest equivalents: the autouse zero-network guard in w1-4d, tsc and eslint."
 ],
 "git_status_final": " M SmartCompareApp/__tests__/authService.logoutRefreshFirst.w1-4d.test.ts\n M SmartCompareApp/src/services/authService.ts\n\nFinal sha256 of every file in the unit. The two I wrote in this pass are marked; the other three are byte-unchanged from the wip.\n- d18190037893cbd6a34443bc8e1b296948b0089fff75ded6cb4fd1f670999081  SmartCompareApp/src/services/authService.ts (WRITTEN)\n- 4eec0d94b9264ec382e9bfe8e3ce3c2b91da4eb009a46443cceab4c9ca0e31ee  SmartCompareApp/__tests__/authService.logoutRefreshFirst.w1-4d.test.ts (WRITTEN)\n- cb703d00ce1fbfd5c978938a7e9c7f998897afa6864816abbd21874c070b7e74  SmartCompareApp/src/services/api.ts (unchanged)\n- 4197173c39a2b750f1d22413e424a0d4ee6839c7c6d86bba96362975cb20193a  SmartCompareApp/__tests__/authService.logoutRevoke.w1-4.test.ts (unchanged)\n- 77bd160bdbcc0851fbc809b882813c7eb595c3205975914924eab57ae1ca42e9  SmartCompareApp/__tests__/authService.bootOptimistic.a3.test.ts (unchanged)\n\nHEAD is still 26b6b100. The wip-state byte snapshots are in scratchpad/rclient_fix/wip_snap/ and the fixed-state snapshots in scratchpad/rclient_fix/snap/.",
 "residual_risk": "- The fix reaches phones only with the next OTA, and server-side revocation still needs R-AUTH plus the flag.\n- A later logout can still WAIT up to 5 s on an earlier session's hung refresh. It never uses that refresh's pair.\n- A refresh slower than 5 s still leaves an expired Bearer on the wire and RT2 live upstream until R-AUTH's expired-Bearer path exists.\n- Pre-existing and out of scope: refreshSession's 401 catch calls clearSession() with no epoch check. An earlier session's hung refresh that finally 401s can therefore clear a later user's session, on base as well as HEAD.\n- refreshSession captures epochAtRequest AFTER its SecureStore read. This is a theoretical window, and only if a single SecureStore read outlasted a whole logout plus login; I left it unchanged.\n- Double-tap logout: only the first tap gets the handed-off pair.\n- Three mutants survive as equivalent (N04, N06, F03), argued from the api.ts singleton. If a second direct caller of refreshSession() is ever added, re-check N04 and F03.\n- 'A worker process has failed to exit gracefully' appeared once in the multi-worker full run. The seven auth suites are clean under --detectOpenHandles, so the source is another suite.\n- There is no RN device walkthrough; the harness drives the real axios instance with a stub adapter only."
}