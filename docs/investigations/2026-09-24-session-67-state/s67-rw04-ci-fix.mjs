export const meta = {
  name: 's67-rw04-ci-fix',
  description: 'CI-only failures of PR #196 (R-W04): four harness tests and one retro test fail after tests/test_platform_router.py deletes price_service from sys.modules (issue #185 class). Test-only fix that resolves the live module at call time, proven in the CI order; one Opus 5.5 fixer then a verifier; the orchestrator commits.',
  phases: [
    { title: 'Fix', detail: 'reproduce the CI order, fix the binding in the test files only, prove it', model: 'claude-opus-5-5' },
    { title: 'Verify', detail: 'independent check', model: 'claude-opus-5-5' },
  ],
}
const MODEL = 'claude-opus-5-5'
const MYSP = 'C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/75660a55-e14a-4de1-80dc-a537f2af315e/scratchpad'
const VENV = 'C:/Users/SynAckITPC/Documents/AI/.venv-qaren/Scripts/python.exe'
const DIR = 'C:/Users/SynAckITPC/Documents/AI/sc-r-w04'

const COMMON = `
ENVIRONMENT AND HARD RULES (violating any fails the task):
- Windows 11. Backend FastAPI, Python 3.12. ALWAYS run backend tests with the pinned venv ${VENV} (the exact CI pins); PYTHONIOENCODING=utf-8; -q -p no:cacheprovider -p no:randomly --timeout=120; sets add -m "not (live_unit or live_db or integration)". This unit's runs use the process-wide guard plugin: PYTHONPATH=${DIR}/.qa-retro/netguard and -p rw04_netguard, plus dead loopback proxies (HTTP_PROXY=http://127.0.0.1:9 etc.), exactly as ${MYSP}/rw04g/units.sh does. Lint: ${VENV} -m ruff check --select E9,F63,F7,F82 --no-cache; py_compile.
- Backend files are CRLF in the working copy (the two test files may be LF - keep whatever each file has): use the Edit tool; git diff --stat must show only intended lines.
- NEVER git commit/push/checkout/stash/reset/rebase/clean in any existing worktree; you write files only; the orchestrator commits. NEVER 'git checkout -- <file>'; byte-snapshot before every mutation, restore from it, sha256-verify. Work ONLY in ${DIR} plus your scratchpad. Never run a recursive delete. Never install packages. Never live_db/live_unit/integration, never LIVE=1, never the network, never any Railway MCP tool, never print an env value. NEVER run _proof/sweep2.py; the _proof corpus is read-only. Never import app.* before tests/conftest.py has run. A copy of .env may live only inside a detached scratch worktree you remove afterwards.
- Report honestly; paste real summary lines; final text is raw structured data. Your report MUST carry the final sha256 of every file you wrote.`

const TASK = `PR #196 (unit R-W04, worktree ${DIR}, CLEAN at 9d620327 = the squashed retro commit rebased onto main ab9442ae; git status --short must be empty - confirm FIRST) failed CI backend-tests (ubuntu, the FULL free-tier suite in ONE process, alphabetical order): 5 failed / 18820 passed. Job log: ${MYSP}/ci_log_196_$(ls).txt - find it with: ls ${MYSP}/ci_log_196_*.txt. The 5 nodes: tests/test_verify_flag_byte_identity.py::TestW04HarnessFlagsOnAndCompare::{test_flags_forces_the_flag_off_per_call, test_flags_on_forces_true_per_call_and_restores, test_flags_on_restores_an_unset_flag_to_unset, test_flags_on_vs_off_compare_surfaces_the_flag_fork} (the probe recorder saw 0 of 8 calls: '--flags-on must force ENABLE_W04_HARNESS_PROBE=true on all 8 calls: []') and tests/test_retro_w0_4.py::test_flag_on_extraction_is_identical_over_every_fixture_page ('assert (0 == 0 and set() == {true}' - its BeautifulSoup.__init__ spy recorded nothing).
ORCHESTRATOR'S REPRODUCTION (issue #185 class): running tests/test_platform_router.py BEFORE the two files in ONE process reproduces the FOUR harness failures locally (4 failed / 430 passed); the retro node did NOT fail in that order, so its trigger is another earlier file in the CI order (or a combination). MECHANISM for the four: tests/test_platform_router.py::test_does_not_import_price_service deletes every price_service entry from sys.modules (an import-cycle proof) and never restores it, so the next import builds a FRESH module object; tests/test_verify_flag_byte_identity.py binds 'from app.services import price_service as ps' at COLLECTION time (line ~168) and patches/probes through 'ps', while scripts/verify_flag_byte_identity.py imports 'from app.services.price_service import extract_price_from_html' INSIDE main() at CALL time (line ~258) - the harness therefore runs the fresh module's function and the test's probe on the stale 'ps' never fires. Locally, alone, both are the same object.
YOUR TASK: (1) REPRODUCE: the four with the platform_router prefix (as above); for the fifth node, bisect the CI prefix (the file order is in the log after stripping the 29-char timestamp prefix; ~600 files before tests/test_retro_w0_4.py) starting with the cheap candidates (test_platform_router.py, the importlib.reload files: test_backend_cleanup, test_cache_service_bounded_transport, test_fan_out_budget_env, test_limiter_endpoint_key, test_m18_openai_tpm_sizing, test_nasser_adapter, test_observability, test_supplement_branch_genuine, test_hotfix_shopping_query_clean) until it fails; record the minimal order and the exact mechanism. (2) FIX in the TWO TEST FILES ONLY (nothing under app/ or scripts/): resolve the live module at CALL time (e.g. a helper that returns sys.modules['app.services.price_service'] via importlib.import_module inside each test / fixture, and patch or probe on that object - or patch every reachable module dict the way tests/test_w49_extraction_catch_redaction.py::_price_service_globals does on main), and make the retro node's spy target whatever the live extraction actually calls; keep every assertion's meaning. (3) PROVE: each reproduction order fails before the fix and passes after; each file alone passes (test_retro_w0_4.py 88, test_verify_flag_byte_identity.py 15 + test_price_parse_offload.py 9 = 112 in both flag states); a mutation that reverts the binding fix re-fails the reproduction; ruff + py_compile. (4) pr_text = a 'CI hermeticity note' paragraph for the PR body (it must name #185 and say the app/scripts bytes did not change). Leave only the two test files modified (uncommitted); report git status --short verbatim and the final sha256 of every file you wrote.`

const FIX_SCHEMA = { type: 'object', required: ['unit', 'reproduction', 'root_cause', 'files_changed', 'test_evidence', 'mutation_proof', 'lint_clean', 'pr_text', 'git_status_final', 'file_sha256'], properties: {
  unit: { type: 'string' }, reproduction: { type: 'string' }, root_cause: { type: 'string' }, files_changed: { type: 'array', items: { type: 'string' } }, test_evidence: { type: 'string' },
  mutation_proof: { type: 'string' }, lint_clean: { type: 'boolean' }, pr_text: { type: 'string' }, git_status_final: { type: 'string' }, file_sha256: { type: 'array', items: { type: 'string' } } } }
const VERIFY_SCHEMA = { type: 'object', required: ['unit', 'verdict', 'defects', 'summary', 'worktree_left_byte_identical'], properties: {
  unit: { type: 'string' }, verdict: { type: 'string', enum: ['SOUND', 'DEFECTIVE'] },
  defects: { type: 'array', items: { type: 'object', required: ['severity', 'location', 'claim', 'reproduced'], properties: { severity: { type: 'string', enum: ['blocking', 'major', 'minor'] }, location: { type: 'string' }, claim: { type: 'string' }, reproduced: { type: 'boolean' } } } },
  summary: { type: 'string' }, worktree_left_byte_identical: { type: 'boolean' } } }

log('rw04-ci-fix: start')
const fix = await agent(`You are fixing the CI-only failures of PR #196 (unit R-W04).\n${TASK}\n${COMMON}`, { label: 'fix:R-W04-ci', phase: 'Fix', model: MODEL, effort: 'high', schema: FIX_SCHEMA })
if (!fix) return { fix: null, verify: null, final_verdict: 'fixer died' }
const verify = await agent(`You are an ADVERSARIAL verifier for the CI fix of PR #196 (unit R-W04) in worktree ${DIR} (base commit 9d620327; the fixer's UNCOMMITTED edits are exactly git diff HEAD). The fixer reported:\n${JSON.stringify(fix, null, 1)}\nThe task it was given:\n${TASK}\nAssume it is wrong until checked: read the diff (only the two test files may have changed; every assertion must keep its meaning - no weakened pin); re-run each reproduction order yourself at the pre-fix bytes (restore them from git show HEAD:<path> into a scratch copy, never git checkout in the worktree) and at the fixed bytes; re-run each file alone in both flag states; judge whether the binding fix is robust to both importlib.reload and a fresh re-import. Leave the worktree byte-identical to the fixer's state. reproduced:true only if you ran it. Do not fix anything. If sound, say SOUND.\n${COMMON}`, { label: 'verify:R-W04-ci', phase: 'Verify', model: MODEL, effort: 'high', schema: VERIFY_SCHEMA })
log('rw04-ci-fix: ' + (verify ? verify.verdict : 'verifier died'))
return { fix, verify, final_verdict: verify ? verify.verdict : 'verifier died' }
