# Next-session prompt — the engineering lane (written at the session-67 close, 2026-09-24 ~19:30 local)

Paste the block below as the first message of a fresh Claude Code session opened in `C:\Users\SynAckITPC\Documents\AI\smartcompare`. Everything it names exists on `main` at or after the merge of PR #200.

---

Load the `synack-build-orchestrator` skill and use it exactly as in sessions 66 and 67: you (Fable) orchestrate, gate, review and commit; Opus 5.5 agents (`model: 'claude-opus-5-5'`, effort high) do every red / green / adversary / fix / polish round as Workflow-tool scripts; ultracode is on. Run 3–4 workflows at a time, never more; prefer speed, but never cut a test, a gate or a review.

Read, in this order, before doing anything: (1) the memory `project-myez-session67-close-2026-09-24.md` — its "NEXT SESSION — START HERE" block and the production/migration paragraphs; (2) `docs/investigations/2026-09-24-session-67-state.md` and its folder `docs/investigations/2026-09-24-session-67-state/` (`NEXT_SESSION_PROMPT.md` = this file, `APPLY_PACK_AHMED.md`, `DECISIONS_AHMED.md`, the harvested reports, rulings and scripts); (3) CLAUDE.md's "Active runtime (SESSION 67 …)" block and its "SESSION 67 flags" block. Then tell me the state in ten lines and start.

State you can rely on (measured, 2026-09-24): main is at or past `3b0ea5d7` (PR #199) and PR #200 (docs) — `git fetch origin main` first. Production runs current main on Railway (`web`, project `empowering-enthusiasm`; the trial-expiry outage of 09-21 is over; the GitHub push trigger works; `/health` 200 with `loop_lag_max_60s_ms` visible). Phones run EAS group `561d2cba-f374-40e8-866b-3bfe6c7c9c3b` from `ab9442ae`. Flags ON in prod beyond the historical ones: `ENABLE_BRIGHTDATA_BUDGET_GATE` (web + price-warmer), `ENABLE_LOGOUT_UPSTREAM_REVOCATION` (web), `ENABLE_CONSENT_PERSIST` (web). Migrations 037, 038, 040, 041 are APPLIED and proven; 035/036 are not; 039 is reserved. The Supabase project had been paused and was restored; it answers. Do NOT flip any further flag and do NOT touch Railway or Supabase unless I ask — the canary order is in CLAUDE.md and every remaining flip is mine.

Process, binding: every unit is spec → (adversarial spec review for W4) → red tests → my gate rulings → green → adversary → fix → re-adversary → your diff review → ONE squashed commit → rebase onto origin/main → the unit's test set plus every file that pins the functions it touches, run in CI (alphabetical) order → ruff `E9,F63,F7,F82` + py_compile → push → PR via the REST helper (`gh` is unusable on this box: copy `docs/investigations/2026-09-24-session-67-state/pr_rest.py` and `ci_logs.py` to your scratchpad; the token comes from `git credential fill` and is never printed) → `pr_rest.py watch <n> 90` (merges on 6/6) → verify. Workflow scripts are pure ASCII/LF written with the Write tool (heredocs mangle backslashes; backticks inside a double-quoted bash string execute). Backend tests run with the pinned venv `C:/Users/SynAckITPC/Documents/AI/.venv-qaren/Scripts/python.exe`, `-p no:cacheprovider -p no:randomly`, `-m "not (live_unit or live_db or integration)"`, always under a process-wide network guard, never importing `app.*` before conftest. New worktrees use absolute paths (`git worktree add C:/Users/SynAckITPC/Documents/AI/sc-<unit> -b <branch> origin/main`); a client unit needs a `node_modules` junction into the clone and is never removed recursively (unlink the junction first with PowerShell `[System.IO.Directory]::Delete(path, $false)`); before any `eas update`, `npm ls --depth=0` in the clone must be clean. No agent brief may read Railway variables, print an env value, or copy `.env` beyond a gate that needs it; sweep the scratchpad for `.env` after every workflow. Byte-snapshot before every mutation and never `git checkout --` a file carrying uncommitted work; a killed adversary can leave a mutant, so re-run a unit's own files before trusting its tree.

The lane, in order (each its own unit, its own PR):
1. **R-W04 follow-ups W0-4e, W0-4f, W0-4g** — sources: the follow-ups section of `docs/investigations/2026-09-24-session-67-state/prbody_R-W04.md`, the r1 adversary's test-side notes in `retro_adv_R-W04_r1.json`, and CLAUDE.md's W0-4 row (the 3 MB cap that also bounds `_lazy_bh_pdp_backfill`'s search-page scan; `extract_from_url`'s three inline soups; the five test-hardening notes). Behaviour rides the existing `ENABLE_PRICE_PARSE_OFFLOAD` flag; flag OFF must stay byte-identical (corpus base → head → base2, record by record, plus `--flags-on` / `--compare`).
2. **The test-hermeticity unit** — issues #183 (`test_hotfix_shopping_query_clean.py` reloads `serper_service` with a fake key and never restores it), #185 (`test_platform_router.py` deletes `price_service` from `sys.modules` and orphans by-name bindings), #186 (W4-3's file before W4-4's breaks three flag-ON nodes), and **netguard 04c with curl_cffi** (#184: every autouse socket guard misses native libcurl — patch `curl_cffi.requests.Session.request`, `AsyncSession.request`, `Curl.perform`, `AsyncCurl.add_handle`; the W4-9 quota-outage tests fetched three Shopify stores and noon for real, in CI). Test infrastructure, no flag; prove each by reproducing the CI-order failure first.
3. **#198** — `app/services/auth_service.py:243` logs expected client 401s (invalid refresh token, bad credentials) at ERROR with `str(e)` and ships them to Sentry as error issues; per-context WARNING with exception TYPE only, caplog pins, canary = no new Sentry issue on an invalid-refresh probe. Small; unflagged by ruling (log hygiene, no legitimate reader).
4. **The remaining W4 product-truth specs 6a, 7, 8, 11, 12, 13, 14** — the unit list is the W4 section of `docs/investigations/2026-09-06-full-review.md` as refined by the rulings in `docs/investigations/2026-09-24-session-66-close-state.md`; each spec must carry its base SHA, be adversarially reviewed BEFORE its red phase (ten of ten specs in session 65d had 5–13 refuted claims), then red → gate → green → adversary. 6b waits on my answer to #101 (the recommendation in `DECISIONS_AHMED.md` is A, renormalize). Every price-path unit is flagged, default OFF, byte-identical OFF.
5. Then: skill Step 2 tooling (extend `.githooks/pre-commit`), the config-audit fixes as a docs/config PR, the Step 6 structured review at the milestone end, the end-of-session redaction re-run.

Write the session's state into the repo (`docs/investigations/<date>-session-68-state.md`, CLAUDE.md flag rows + Active-runtime block, `docs/CONTEXT_SESSION_LOG.md`) before any long break and before ~900k context; keep the memory resume block current. Report state and what remains before you start each unit.

---
