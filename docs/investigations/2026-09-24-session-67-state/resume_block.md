## RESUME AFTER COMPACTION (written 2026-09-24 ~12:55 local, session 67, Claude session id 75660a55-e14a-4de1-80dc-a537f2af315e)

**Process (Ahmed's standing instruction this session):** the `synack-build-orchestrator` skill — Fable orchestrates, gates and commits; Opus 5.5 agents (`model: 'claude-opus-5-5'`) run every red/green/adversary/fix/polish round as Workflow-tool scripts; ultracode is on; run 3–4 workflows at a time, prefer speed but never cut a test, a gate or a review; report state + remaining work to Ahmed. `gh` is unusable on this box (TLS) — every GitHub action goes through `scratchpad/pr_rest.py` (create/status/watch/merge; token from `git credential fill`, never printed) and `scratchpad/ci_logs.py <pr>` (failing job log).

**Paths:** scratchpad = `C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/75660a55-e14a-4de1-80dc-a537f2af315e/scratchpad` (also reachable as `C:/Users/SYNACK~1/AppData/...`). Pinned venv = `C:/Users/SynAckITPC/Documents/AI/.venv-qaren/Scripts/python.exe`. Worktrees still registered: `sc-r-w04` (branch `retro/w0-4-parse-offload`, PR #196), `sc-docs-67` (branch `docs/session-67-state`, the docs PR in preparation), `sc-r-client` (merged; keeps a node_modules JUNCTION — retire only with PowerShell `(Get-Item <junction> -Force).Delete()` after `LinkType -eq Junction`, then `git worktree remove`), plus the long-lived ones (smartcompare main checkout, sc-w0-load `_proof` corpus, sc-w0-base, sc-llm-baseurl, sc-w3-*, sc-w4-1, sc-w4-10, sc-scraper-proof). `sc-w4-9` is an empty unregistered dir held by a handle — `rmdir` it at the very end.

**State at compaction — main `ab9442ae`, 15 PRs merged this session** (#182 docs, #181, #180, #177 W4-3, #179 W4-4, #178 W4-9, #187 R-MAIN, #190 R-CLIENT, #188 R-BREAKER, #189 R-MIG, #191 R-W0, #192 W4-2, #193 R-W18, #194 R-AUTH, #195 R-METER); #36/#44 closed as superseded; issues #183–#186 filed. Every flag default OFF, nothing flipped, Railway never read.

**The ONE open item: PR #196 R-W04** (`retro/w0-4-parse-offload` at `9d620327` = ONE squashed commit on `ab9442ae`; adversary r0 + r1 SOUND; local guarded gates all green) is **CI-RED on 5 test-only nodes of the #185 class** (4 in `tests/test_verify_flag_byte_identity.py::TestW04HarnessFlagsOnAndCompare` — the test binds `price_service` at collection while the harness imports it at call time and `tests/test_platform_router.py` deletes it from `sys.modules` in between; 1 in `tests/test_retro_w0_4.py::test_flag_on_extraction_is_identical_over_every_fixture_page`, trigger not yet isolated). **Workflow `wf_538efd7b-a25`** (`scratchpad/s67-rw04-ci-fix.mjs`: fixer `fix:R-W04-ci` then verifier `verify:R-W04-ci`, test-only, must reproduce in the CI order) was RUNNING at compaction.

**Next steps, in order:**
1. `python scratchpad/harvest.py wf_538efd7b-a25 scratchpad/harvest_rw04_ci.json` — read `verify:R-W04-ci` (`verdict`, `defects`) and `fix:R-W04-ci` (`root_cause`, `files_changed`, `pr_text`, `file_sha256`). If the notification never arrived, the journal at `~/.claude/projects/C--Users-SynAckITPC-Documents-AI/75660a55-e14a-4de1-80dc-a537f2af315e/subagents/workflows/wf_538efd7b-a25/journal.jsonl` is the truth. If DEFECTIVE: relaunch `Workflow({scriptPath: scratchpad/s67-rw04-ci-fix.mjs})` with the verifier's defects added to the TASK text.
2. If SOUND: in `sc-r-w04`, `git diff` the two test files (only `tests/test_verify_flag_byte_identity.py` and `tests/test_retro_w0_4.py` may change; no app/ or scripts/ bytes), run the CI-order reproduction yourself (`tests/test_platform_router.py tests/test_retro_w0_4.py tests/test_verify_flag_byte_identity.py` in one process with `PYTHONPATH=sc-r-w04/.qa-retro/netguard`, `-p rw04_netguard`, both flag states), commit as a SECOND commit (`test(price): R-W04 resolve the live price_service module at call time in the harness and retro tests (CI order, #185 class)` + Co-Authored-By trailer), `git push --force-with-lease origin retro/w0-4-parse-offload`, append the fixer's `pr_text` to PR #196's body (PATCH `/pulls/196` via `pr_rest.api`), then `python scratchpad/pr_rest.py watch 196 90` (merges on green; if `backend-tests` fails again, `ci_logs.py 196`).
3. Docs PR: edit `scratchpad/rw04_facts.json` replacing the tokens `__PR__` → `196`, `__SHA__` → the #196 merge sha (= new main), `__TIME__` → the close time; then `python scratchpad/docs_patch_s67.py C:/Users/SynAckITPC/Documents/AI/sc-docs-67` (idempotent; dry-run verified on scratch copies — it patches CLAUDE.md [W1-4 row, W0-4 row, activation step 2, the CORRECTION paragraph, the consolidated-review sentence, inserts the SESSION 67 flags block before `## Tests` and the `## Active runtime (SESSION 67 …)` block before the SESSION 66 one], `docs/CONTEXT_SESSION_LOG.md` [SESSION 67 entry + the cleanup_expired_ratings correction] and writes `docs/investigations/2026-09-24-session-67-state.md` from `scratchpad/session67_state_draft.md`). Then refresh the state folder `docs/investigations/2026-09-24-session-67-state/` with the final scratchpad artefacts (`retro_*.json`, `retro_rulings_*.txt`, `rulings_s67.md`, `harvest_*.json`, `prbody_*.md`, `msg_*.txt`, `title_*.txt`, `s67-*.mjs`, `gen_*.py`, `heads_extra.json`, `docs_patch_s67.py`, `rw04_facts.json`, `harvest.py`, `pr_rest.py`, `ci_logs.py`, `green_launch_args.json`), secret-scan it (`grep -rlE "sk-[A-Za-z0-9_-]{20,}|eyJ[A-Za-z0-9_-]{30,}|AKIA[0-9A-Z]{16}"` must be empty), `git add -A docs/ CLAUDE.md`, commit (`docs(context): SESSION 67 state — the retro-fix wave + every open PR merged, CLAUDE.md SESSION 67 flags + Active-runtime blocks, session-log entry, W0-4/W1-4/migration corrections, state folder`), push, `pr_rest.py create docs/session-67-state "<title>" <bodyfile>` (body = §0 of the state doc + the merged table), `watch`.
4. Close: retire `sc-r-w04` and `sc-docs-67` after their merges (`git worktree remove` — no junctions there), `rmdir sc-w4-9`, `find scratchpad -name .env` (must be empty), `git worktree prune`; write the final memory (`project-myez-session67-close-2026-09-24.md`, update `MEMORY.md`); give Ahmed the close report: 16 PRs merged, main sha, the four issues, Ahmed's list (rotate the eight keys + ADMIN_API_KEY; Bright Data gate; OTA from main ≥ final sha incl. R-CLIENT's logout; migrations 038 → 037/040 any time (040 census first) → 039; EXPO_TOKEN; Supabase redirect entry; W3-7 icons + eas build; legal review; #101 and the W4-2 "(converted from USD)" product calls; railway login), and what is next (R-W04 follow-ups W0-4e/f/g, netguard 04c with curl_cffi #184, the #183/#185/#186 hermeticity unit, remaining W4 specs 6a/7/8/11/12/13/14, Step 2 tooling, config-audit fixes, Step 6 review, redaction re-run).

**Rules that bit this session (keep):** every unit's CI-order set must include every file that PINS the functions it touches (grep the symbols across tests/), run in alphabetical order; retro/route units are rebased BEFORE their last adversary; killed adversaries leave mutants — re-run unit files first, reports carry final sha256 per file; workflow scripts are ASCII/LF written with the Write tool (Git Bash heredocs mangle backslash-letter sequences; text-mode writes CRLF; `re.sub` treats a backslash-n replacement as a newline; backticks inside a template literal end it; a bash `-c` with an apostrophe-rich heredoc can fail to parse — put long text in a file first); agents copy `.env` into scratch dirs — sweep after every workflow; never `cmd /c rmdir` from Git Bash for a junction.
