{
 "unit": "R-BREAKER FIX phase (W1-3 OpenAI preflight breaker, ENABLE_LLM_PREFLIGHT_BREAKER, PR #147 retro). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-breaker, branch retro/w1-3-breaker, HEAD 7a7dfc24 (wip on base 1c6f6796). I confirmed the worktree was clean at start: `git status --short` was empty, and api_budget_service.py sha c26a60ba matched the green's recorded sha. Of the adversary's five defects, #1 and #2 are fixed with tests only, #5 has a minimal source fix plus pins, and #3 and #4 are confirmed real but out of scope, so they go in the PR's follow-ups.",
 "files_changed": [
  "C:/Users/SynAckITPC/Documents/AI/sc-r-breaker/app/services/api_budget_service.py: +7 lines, a final `except BaseException:` arm in guarded_llm_create that releases the half-open probe slot and re-raises. Final sha256 f84384eb8b8888097353dc2c2fb2db52358977eb512affe1246c516ce65b3028. CRLF on all 1318/1318 lines.",
  "C:/Users/SynAckITPC/Documents/AI/sc-r-breaker/tests/test_retro_w1_3.py: +183 lines appended (8 new nodes, 5 test functions plus helpers). Existing bytes untouched. Final sha256 6911d2ba5e7b07f4e40c041fea79cf2713067e177e867ae6df7880688a8c9142. CRLF on all 1099/1099 lines.",
  "UNCHANGED, for reference: tests/test_openai_breaker.py sha256 bb58e2d0f71790b2bd3de6fbf5c8e75b686a689e3d2603f8dacb33b23ff122c9; app/services/structured_comparison_service.py sha256 2a93e607390bdcd374a7dcaf74a05d97ada714a51058ef681ae1500efe0f0ed1.",
  "Scratch only, not in the repo (scratchpad/rbrk_fix/): mutate_fix.py, append_text.py, rbrk_netguard.py (a copy of the adversary's process-wide guard), unit_flag_{unset,true,false}.log, mlog_*.txt, mut_results.json, comm_head.log, comm_head_net.tsv, and byte snapshots api_budget_service.py.fix-snapshot, test_retro_w1_3.py.fix-snapshot and *.wip-base."
 ],
 "all_unit_tests_pass": true,
 "test_evidence": "Pinned venv .venv-qaren (Python 3.12, pytest 9.1.1). Every run used PYTHONIOENCODING=utf-8, -q -p no:cacheprovider -p no:randomly --timeout=120 -m \"not (live_unit or live_db or integration)\", and the process-wide non-loopback netguard plugin loaded with `-p rbrk_netguard` before conftest.\n(1) Unit files tests/test_retro_w1_3.py + tests/test_openai_breaker.py, run once per ambient flag state:\n- ENABLE_LLM_PREFLIGHT_BREAKER unset: 79 passed.\n- =true: 79 passed.\n- =false: 79 passed.\nThat is 71 pre-existing nodes plus 8 new ones. Each test also sets or clears the flag itself, so flag-ON and flag-OFF paths both run in every state.\n(2) The new pins alone (-k test_fix_): 8 passed, 0 netguard attempts.\n(3) Netguard during the unit runs: 61 blocked lookups (neutralized.supabase.invalid), all from the pre-existing test_openai_breaker.py tests (PO-RECORDED-MEASURED-04c). test_retro_w1_3.py made 0.\n(4) Comm-gate HEAD: see comm_gate.",
 "lint_clean": true,
 "mutation_checks": [
  "Method: each mutation was applied to the CRLF byte snapshot of api_budget_service.py (pre-mutation sha f84384eb asserted) by scratchpad/rbrk_fix/mutate_fix.py. The unit files were run under netguard, then the file was restored from the byte snapshot and its sha256 re-verified. restored_sha_ok was true on all 9; the final sha is f84384eb. No git command was used.",
  "N5 (the adversary's surviving mutant; a cancelled probe records SUCCESS): KILLED, 2 failed. Both are test_fix_cancelled_probe_is_a_release_not_a_verdict [outer-wall] and [task-cancel]. It survived 71/71 before this phase.",
  "N6 (a cancelled probe records FAILURE): KILLED, 4 failed. The two new cancel pins plus test_cancelled_half_open_probe_releases_the_slot and test_green_release_discards_denied_checks...",
  "MB (literal W1-3a: no record and no release): KILLED, 4 failed. The same four tests as N6.",
  "N10 (the cancel arm releases even on a non-probe admission): KILLED, 1 failed: test_green_cancel_on_closed_streak_admission_makes_no_release_call.",
  "N1 (the adversary's surviving mutant; `elif probe` -> `elif outcome_matters`): KILLED, 2 failed. Both are test_fix_closed_streak_non_transient_outcome_is_not_recorded, [400-context-length] and [non-sdk-ValueError]. It survived 71/71 before this phase.",
  "BA1 (the new BaseException arm made unreachable, i.e. the pre-fix code): KILLED, 2 failed. test_fix_teardown_base_exception_on_probe_releases_the_slot and test_fix_orphaned_probe_coroutine_closed_releases_the_slot. So both new teardown pins are RED against the green bytes (c26a60ba).",
  "BA2 (the BaseException arm releases even on a non-probe admission): KILLED, 1 failed: test_fix_closed_streak_non_transient_outcome_is_not_recorded[teardown-BaseException].",
  "BA3 (the BaseException arm on a probe records success): KILLED, 2 failed. The same two teardown/orphan pins.",
  "BA4 (the BaseException arm on a probe records failure): KILLED, 2 failed. The same two teardown/orphan pins.",
  "The flaky test_flag_off_{compare,streaming}_never_short_circuits nodes did not fail in any of the 9 mutation runs or the 3 unmutated runs of this phase."
 ],
 "comm_gate": "BASE: I reused the recorded base: the same 16-file module-reference set (.qa-retro/w1_3_commset_files.txt) at origin/main 41ec7d5f. Its backend is byte-identical to 1c6f6796 per the red phase's empty diff. That run was 322 passed / 0 failed (.qa-retro/w1_3_base_failed.txt is empty). This phase ran no new base worktree, and made no git write of any kind.\nHEAD: the 16 files plus tests/test_retro_w1_3.py, under the netguard plugin: 383 passed / 0 failed (322 + 61 nodes in the retro file). `comm -13` of sorted base vs head FAILED sets is EMPTY. Log: scratchpad/rbrk_fix/comm_head.log.\nNetguard census at HEAD: 97 blocked lookups (36 google.serper.dev, 61 neutralized.supabase.invalid), all from pre-existing tests in tests/test_openai_breaker.py. This matches the red phase's 36/61 exactly. tests/test_retro_w1_3.py made 0.",
 "flag_identity": "Flag: ENABLE_LLM_PREFLIGHT_BREAKER (default OFF, read per call). This phase adds no new flag.\n- The only source change is a new `except BaseException:` arm in the try block of guarded_llm_create. That block is reachable only after the flag-OFF early return `if not llm_preflight_breaker_enabled(): return await client.chat.completions.create(**kwargs)`, which is untouched. So with the flag OFF the new arm is unreachable.\n- The arm's only action is openai_release_half_open_probe(), which carries its own flag gate (pinned by test_green_release_helper_is_a_no_op_with_the_flag_off; the green's MI mutation kills it).\n- New flag-OFF pin: test_fix_flag_off_teardown_and_close_touch_nothing. On a poisoned (cooldown-expired OPEN) blob, a teardown BaseException is re-raised as the same object, and a closed in-flight dispatch (GeneratorExit) finishes closing. Both make 0 Redis ops (get/set/incr/expire all 0), 0 is_circuit_closed / record_failure / record_success calls, and leave the store byte-equal.\n- The existing flag-OFF pins stay green in all three ambient flag states: test_pin_flag_off_* \u00d717, test_flag_off_never_reads_or_records_and_always_dispatches, and both M10 entry-read pins.\n- Flag ON, the new arm changes behaviour only for a dispatch that ends in a BaseException that is neither CancelledError nor Exception. Even then it acts only when probe=True. On a CLOSED-streak admission it makes 0 extra ops, pinned by the [teardown-BaseException] case (1 GET, 0 SET, streak unchanged; BA2 kills it).\n- structured_comparison_service.py is byte-unchanged (sha 2a93e607).",
 "measurements_run": [
  "Worktree clean at start: `git status --short` was empty at 7a7dfc24; the api_budget_service.py sha was c26a60ba, equal to the green's recorded sha.",
  "Re-derived each defect from the code: #1 and #2 from the test inventory plus the adversary's surviving N5/N1 runs (both re-killed here). #3 from is_circuit_closed: the `n <= 0` path returns True fail-open, and _openai_dispatch_admission then returns probe=True, so the release's unconditional SET '0' can reset a live probe's counter. #4 from the memo path: a stale half_open memo routes the dispatch through is_circuit_closed on a CLOSED blob, probe=True, and a 400 then calls record_success. #5 from the handler list: only CancelledError and Exception were caught; confirmed by BA1 turning the two new pins red.",
  "Unit files in 3 ambient flag states (unset/true/false): 79/79 passed each time.",
  "New pins alone: 8/8 passed, 0 netguard attempts.",
  "Mutation matrix, 9 mutations (N5, N6, MB, N10, N1, BA1-BA4): all killed, all restores sha-verified.",
  "Comm-gate HEAD: 383 passed / 0 failed; comm -13 against the recorded base is empty.",
  "Netguard census: 36 google.serper.dev + 61 neutralized.supabase.invalid, all from pre-existing test_openai_breaker.py tests; 0 from test_retro_w1_3.py.",
  "ruff --select E9,F63,F7,F82 --no-cache on the 3 files: clean. Extra F401,F841 on test_retro_w1_3.py: clean. py_compile: clean on all 3.",
  "CRLF: api_budget_service.py 1318/1318 lines; test_retro_w1_3.py 1099/1099.",
  "git diff --stat: api_budget_service.py +7, test_retro_w1_3.py +183, 0 deletions. Only intended lines changed."
 ],
 "pr_text": "## R-BREAKER: W1-3 OpenAI preflight breaker, cancellation and half-open probe fixes (flag-internal, default OFF)\n\n### Defects (retroactive adversary review of PR #147)\n- **W1-3a (BLOCKING).** `guarded_llm_create` recorded a breaker failure on every bare `asyncio.CancelledError`. One compare's outer wall cancels every guarded dispatch still in flight. That wall can be the Tier-2 4 s wall, the stream hard cap, a per-stage `wait_for`, a client disconnect or a shutdown. So a single compare could reach `CB_FAILURE_THRESHOLD` while OpenAI was healthy, and the breaker then refused every compare from every user for 600 s. Reproduced through the real `tier2_fill_non_negotiables`: one compare left the blob at `{'state':'open','failure_count':4}`.\n- **W1-3b.** An admitted half-open probe that ended in a non-transient outcome recorded nothing. That covers a 4xx other than 429 (context-length or content-policy 400, 401/403/404/409/422) and any non-SDK exception. The single probe INCR was spent and the blob stayed `half_open`. Every later dispatch was denied (INCR >= 2) until the 3600 s blob TTL expired. Meanwhile the read-only preflight admitted every compare, and each one paid the full scrape fan-out before failing at the LLM.\n- **Interaction, measured by the red phase.** A literal W1-3a fix (just drop the record) would turn a cancelled half-open probe into exactly the W1-3b wedge.\n\n### Fix (`app/services/api_budget_service.py`, all inside `ENABLE_LLM_PREFLIGHT_BREAKER`)\n- `_openai_dispatch_admission()` now returns `(admitted, outcome_matters, probe)`. `probe` is True only when admission went through `is_circuit_closed`'s recovery path.\n- **CancelledError arm:** records **nothing**. If this dispatch held the probe, it releases the slot. The cancellation is then re-raised unchanged.\n- **Non-transient exception on a probe:**\n  - A 4xx other than 429 (`_openai_server_answered`) records **success-for-health**: OpenAI answered, so the breaker closes.\n  - Anything else **releases the slot**.\n- **Any other BaseException on a probe** (KeyboardInterrupt, SystemExit, or GeneratorExit when an orphaned coroutine is closed) **releases the slot** and re-raises. This was added in the fix phase so that \"every admitted probe records a total outcome\" holds for every exception Python can raise inside the call.\n- **Transient outcomes are unchanged** and still record one failure each: 429, connection error, `APITimeoutError`, 5xx, and a `TimeoutError` raised inside the call. A 429 probe still re-opens the breaker for a fresh window.\n- **New `openai_release_half_open_probe()`.** It reads the flag itself on every call, and acts only on a blob that is still `half_open`. It **resets** the per-trip probe counter to `\"0\"` rather than decrementing it. The reset also discards the INCRs of checks denied while the probe was in flight; a DECR would leave the counter >= 1 and wedge the breaker again. Cost: one GET + one SET, only on that rare path.\n- `structured_comparison_service.py` is unchanged.\n\n### Tests\n- **Red file.** `tests/test_retro_w1_3.py` goes from 12 RED to green. The inverted MINOR-5 test in `tests/test_openai_breaker.py` is green.\n- **13 GREEN-phase pins** fix which total outcome each probe shape records:\n  - a non-SDK exception releases the slot; it is not a success;\n  - a 4xx records success-for-health;\n  - the release is a reset, not a DECR;\n  - a CLOSED-streak admission makes no release call;\n  - the release writes nothing unless the blob is `half_open`;\n  - the helper is a no-op with the flag OFF.\n- **8 FIX-phase pins** close the tests the adversary showed prove nothing:\n  - `test_fix_cancelled_probe_is_a_release_not_a_verdict[outer-wall|task-cancel]`: right after the cancel, the blob is still `half_open`, `failure_count` is unchanged and the probe key reads `\"0\"`. This kills N5 (a cancel recorded as SUCCESS), which had survived 71/71.\n  - `test_fix_closed_streak_non_transient_outcome_is_not_recorded[400|ValueError|teardown]`: on a CLOSED breaker with a standing streak, each of these makes exactly 1 GET and 0 SETs and leaves the streak untouched. This kills N1 (`elif probe` -> `elif outcome_matters`), which had survived 71/71.\n  - `test_fix_teardown_base_exception_on_probe_releases_the_slot`, `test_fix_orphaned_probe_coroutine_closed_releases_the_slot` and `test_fix_flag_off_teardown_and_close_touch_nothing`: pins for the new BaseException arm.\n- **Mutation matrices.**\n  - Green phase: all 14 mutations killed, including the reviewer's M6, M10 and M14.\n  - Fix phase: 9 mutations killed (N5, N6, MB, N10, N1, and BA1-BA4 on the new arm). Every restore was sha256-verified.\n- **Comm gate.** The 16-file module-reference set plus the new file, run at HEAD under a netguard plugin: 383 passed / 0 failed. `comm -13` against the base (322 passed / 0 failed) is empty.\n\n### Flag\n| Flag | Default | Change |\n|---|---|---|\n| `ENABLE_LLM_PREFLIGHT_BREAKER` | OFF (per call) | No new flag. The flag-OFF path is byte-identical: the early return is untouched, and every new arm sits after it. Pinned: M14 kills 40 nodes, and a new flag-OFF teardown/close pin makes 0 Redis ops. |\n\n### Activation gate (unchanged order; this PR satisfies items 1-3 of the reviewer's gate)\n1. W1-3a: one compare's wall cancelling 3 or more dispatches cannot open the breaker. Pinned. A cancelled probe is a release, never a verdict. Pinned.\n2. W1-3b: a non-transient, cancelled or torn-down half-open probe cannot wedge deny-all behind an admitting preflight. Pinned.\n3. W1-3d: the 4xx-never-trips pin and the no-read-with-flag-OFF entry pin. Both are in the red file.\n4. **Still owed before any flip:** a measured count (from staging `stage_timings` or a flag-ON shadow log) of guarded dispatches per compare under current Bright Data latency.\n5. **Still owed before any flip:** `ENABLE_BRIGHTDATA_BUDGET_GATE=true` on `web` and `price-warmer`. This flag is not a spend control.\n\n### Honest limits\n- **The breaker no longer sees a slow-but-alive OpenAI stall shorter than the SDK's own deadline.** The client timeout is `httpx.Timeout(120, connect=30)` with `OPENAI_MAX_RETRIES` retries. The compare walls (4 s Tier-2, 30 s hard cap) cancel long before that, and a cancellation no longer counts. Only 429, 5xx, connection errors and full SDK timeouts trip the breaker. An in-guard `deadline` kwarg would restore stall detection. Not done here.\n- **A 4xx on a probe counts as success-for-health.** A dead key (401) on the probe closes the breaker, and compares then fail at the LLM exactly as they do with the flag OFF. The breaker is a transport-health signal, not a key-validity check.\n- **A SIGKILLed or OOM-killed probe still orphans the slot** until the 3600 s blob TTL, because no Python code runs there. An in-process teardown or a closed orphan coroutine now releases the slot.\n- The release path adds one GET + one SET. That happens only with the flag ON, and only on a half-open probe with a non-verdict outcome.\n\n### Follow-ups (named, not addressed)\n- **W1-3c:** cross-worker streak decay. A success seen by another worker never resets the shared `failure_count`, so 3 non-consecutive failures still trip the breaker. Reproduced by the reviewer as P3.\n- **Stale-memo probe path (fix-phase adversary P6, W1-3c territory).** A worker whose 60 s memo still says `half_open` routes a dispatch through `is_circuit_closed` on a blob that is already CLOSED with a streak. That makes `probe=True`, so a 400 records success and wipes the streak (measured 2 -> 0), where the ordinary CLOSED path leaves it at 2. Flag ON only. It is defensible under \"the server answered\", but inconsistent.\n- **Fail-open probe over-admission (fix-phase adversary P5).** `_openai_dispatch_admission` returns `probe=True` for a fail-open admission (`_redis_incr` returns 0 during a Redis blip). If that dispatch is then cancelled, its release SETs the counter to `\"0\"` while the real probe is still in flight, and one extra concurrent probe is admitted. The over-admission is bounded, happens only with the flag ON, and needs a Redis INCR failure during a live probe. The fix needs `is_circuit_closed` (shared with the firecrawl/scrapedo/serper breakers) to report whether it actually took the probe. It belongs with the stale-probe window below.\n- Per-compare contextvar dedupe (at most one recorded failure per compare).\n- A stale-probe window in `is_circuit_closed` (re-admit a probe after about 2x the request timeout). It closes the SIGKILL/OOM orphaned-probe case.\n- Optional: the preflight denies while a half-open probe is outstanding (a plain GET of the probe counter).\n- An in-guard `deadline` kwarg to restore genuine-stall detection (see Honest limits).\n- **PO-RECORDED-MEASURED-04c:** the existing compare tests in `tests/test_openai_breaker.py` are not zero-network. Measured across phases: 36 google.serper.dev and 61 neutralized.supabase.invalid getaddrinfo attempts from 6 existing tests (red phase and fix-phase HEAD); 30/61 in the green phase. Their flag-OFF `bd_search_web > 0` assertions flake with DNS and ordering state. Fix: in that file's autouse fixture, reset `serper_service.SERPER_API_KEY`, stub `get_admin_supabase_client`, and add a socket guard.\n- Reviewer minor: the corpus SHA from `scripts/verify_flag_byte_identity.py` proves nothing for this unit, because that harness never reaches `guarded_llm_create`. Flag-OFF identity rests on the pins above.\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)",
 "deviations_from_spec": [
  "Defect 5 (a teardown BaseException leaves a probe unreleased) was FIXED, although the adversary only recorded it. It is real: BA1 (the pre-fix code) turns both new pins red. It is a direct gap in the ruled requirement that every admitted probe records a total outcome. The fix is a final `except BaseException: if probe: release; raise` arm, not the `finally` the adversary suggested. A `finally` would also run on the success and Exception paths, which already record a verdict, so it would need extra state to avoid a double record. The arm is 7 lines, flag-ON only, and probe-only (BA2 pins that). It also covers GeneratorExit from a closed orphan coroutine. SIGKILL/OOM stay out of scope per ruling (4).",
  "Defect 3 (a fail-open admission with probe=True resets a live probe's counter) was NOT fixed. Re-derived from the code: the `n <= 0` path of is_circuit_closed returns True, and _openai_dispatch_admission then returns probe=True. So the finding is real, not disputed. But it is not a ruled requirement; the adversary calls it bounded over-admission of one extra probe, not a wedge. It is also stale-probe-window territory, which ruling (4) puts out of scope. A correct fix needs is_circuit_closed, which the firecrawl/scrapedo/serper breakers share, to report whether it took the probe. Recorded in pr_text follow-ups.",
  "Defect 4 (a stale half_open memo routes a CLOSED-streak dispatch through the probe path, so a 400 wipes the streak) was NOT fixed. Re-derived from the memo path; the finding is real. It is W1-3c cross-worker territory, which ruling (4) puts out of scope, and it is flag-ON only and makes the breaker less sensitive, not wedged. Named in pr_text follow-ups as the adversary asked.",
  "tests_that_prove_nothing rows 1 and 2 are folded into real pins: N5 is killed by test_fix_cancelled_probe_is_a_release_not_a_verdict \u00d72, and N1 by test_fix_closed_streak_non_transient_outcome_is_not_recorded[400|ValueError]. Row 3 (equivalent mutants N2, N8, N3) gets no pin, deliberately. As the adversary states, each is unobservable. A 429 is always a RateLimitError, and the transient branch catches it first. Only non-429 4xx errors ever reach _openai_server_answered. A stale OPEN-expired or HALF_OPEN memo routes the next dispatch through is_circuit_closed anyway. No test can distinguish these mutants without asserting on internals.",
  "No base comm-gate worktree was created in this phase. I reused the recorded base: the same 16-file set at 41ec7d5f, byte-identical backend to 1c6f6796, 322 passed / 0 failed. With a 0-failure base, comm -13 reduces to the HEAD failure set, which is empty. `git worktree list` shows no scratch worktree of mine."
 ],
 "git_status_final": " M app/services/api_budget_service.py\n M tests/test_retro_w1_3.py",
 "residual_risk": "- **All flag-ON only.** The flag is OFF in prod, and the flag-OFF path is pinned byte-identical. The new BaseException arm is unreachable with the flag OFF.\n- **Two confirmed flag-ON imperfections are deliberately left in follow-ups:**\n  - (a) A fail-open admission (Redis INCR blip) that is then cancelled can reset a live probe's counter, admitting one extra concurrent probe. This is bounded over-admission, not a wedge.\n  - (b) A stale 60 s half_open memo lets a 400 on a CLOSED-streak dispatch record success and wipe the streak.\n- **Carried over from the green phase:**\n  - Genuine slow-but-alive stalls shorter than the SDK deadline are no longer detected, because cancellations are not recorded.\n  - A 401 probe closes the breaker (same outcome as flag OFF).\n  - SIGKILL/OOM still orphans the probe slot until the 3600 s TTL.\n  - W1-3c streak non-decay is unaddressed.\n- The pre-existing test_openai_breaker.py compare tests still attempt DNS (36 + 61 blocked under netguard, PO-RECORDED-MEASURED-04c). A future CI red on test_flag_off_{compare,streaming}_never_short_circuits is most likely that known flake; it did not flake in any of this phase's 12 runs.\n- Nothing was committed; the orchestrator commits on top of wip 7a7dfc24."
}