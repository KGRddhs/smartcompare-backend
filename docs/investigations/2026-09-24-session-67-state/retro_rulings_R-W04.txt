FABLE RED GATE RULINGS R-W04 (2026-09-24, session 67; binding for the green and its adversary)

RED GATE: PASSED. 44 reds are right-reason (the red agent re-reproduced every defect and proved satisfiability with a prototype green on a scratch origin/main worktree: 111 tests green, only the 2 test_page_scraping baseline nodes red in the site files, corpus OFF/ON --compare equal with 0 differing records). The prototype design is the ACCEPTED design; the green re-derives it in the retro worktree (base 1c6f6796) - the prototype patch and file copies are in sc-r-w04/.qa-retro/proto_s67/ and may be read, never copied blindly.
Rulings on the red agent's spec_disagreements (each ACCEPTED as measured unless stated):
(1) W0-4c scope is SIX sites: pharmacy (_try_pharmacy_urls), url_extraction_service.extract_with_ai, fetch_bolo_price, fetch_boutiqaat_price, fetch_iherb_price AND StructuredComparisonService._fetch_page_price (scs:3108). The allowlist stays EMPTY; the sixth site routes through _extract_price_from_html_maybe_offloaded like the others, flag OFF byte-identical (its pin binds). Do not allowlist it.
(2) The pool is entered through contextvars.copy_context().run (asyncio.to_thread semantics); a bare loop.run_in_executor is a defect - the 9x2 ContextVar pins bind.
(3) Semaphore contract = SUBMISSION DEPTH: acquire the per-event-loop asyncio.Semaphore(PRICE_PARSE_MAX_WORKERS) BEFORE submitting to the 'price-parse' executor; never bind a module-level asyncio.Semaphore to the first loop (create lazily per loop, keyed by the running loop, as the prototype does). The cancel test stays a pool property.
(4) Harness: --flags-on and --compare live in scripts/verify_flag_byte_identity.py with tests in tests/test_verify_flag_byte_identity.py; with no --flags-on the payload keeps EXACTLY today's 6 keys and the OVERALL formula (every recorded base digest must stay reproducible); --flags-on restores the prior env value including 'unset' after the sweep.
(5) The 3 MB cap applies to every curl_fetch_html caller under the flag, INCLUDING structured_comparison_service._lazy_bh_pdp_backfill's regex scan of a curled search page - pin flag OFF only (whole page scanned) and DISCLOSE the flag-ON consequence (an href past 3,000,000 chars is no longer found) in pr_text as a stated limit + follow-up W0-4e (exempt or raise the cap for the backfill caller). Do not widen scope.
(6) PRICE_PARSE_MAX_WORKERS parsing: int(strip(value)) clamped to 1..16; unparsable, non-finite ('inf'/'nan') and empty fall back to 4 (the session-65d non-finite rule); -3 -> 1; 99 -> 16; ' 3 ' -> 3.
(7) The render-leg cap bounds ONLY the parse input; the provider-attempt html_kb telemetry keeps reporting the raw vendor body in both flag states (pinned).
(8) AST guard: an offload call is any call named to_thread, run_in_executor, or whose name contains 'offload' or 'parse_pool'; the transitive test allowlists url_extraction_service.extract_from_url (its extract_amazon_data / extract_noon_data / extract_generic_data soups are OUT of scope -> follow-up W0-4f in pr_text).
(9) GATES for the green, all through the process-wide guard plugin (.qa-retro/netguard/rw04_netguard.py, loaded with -p BEFORE conftest; never import app.* first): (a) the three unit files (test_retro_w0_4.py, test_verify_flag_byte_identity.py, test_price_parse_offload.py) green with the flag unset AND =true; (b) every pin mutation-checked from byte snapshots (sha-verified restores) incl. the reviewer's M6 (lxml-when-ON) and 'unflagged cap' mutants and the prototype variants (no copy_context -> 9 ContextVar pins red; no semaphore -> the submission-depth test red); (c) comm HEAD over the recorded 289-file set (.qa-retro/comm_set.txt), same runner and deselects; any hung node re-run in isolation before it counts; comm -13 against .qa-retro/base_failed_ids.txt EMPTY; (d) corpus gate over the READ-ONLY root C:/Users/SynAckITPC/Documents/AI/sc-w0-load/_proof: flag OFF must print OVERALL 9504e5a94a218969764c8cbfdb43243da7bfcd8e0950bb5fb9148b9a9258ce99 and results sha a1b3460c...; flag ON via --flags-on with --compare against the OFF results must report equal=True and 0 differing records; record both digests and the wall times in pr_text; never run _proof/sweep2.py; (e) ruff E9,F63,F7,F82 + py_compile on every edited module; CRLF hygiene.
(10) pr_text must carry: the flag row (ENABLE_PRICE_PARSE_OFFLOAD default OFF, effect ON per site, PRICE_PARSE_MAX_WORKERS knob), the activation order with the bounded pool + queue-depth/loop_lag_ms watch, the measured loop-gap residual (35-143 ms single large page, 171-534 ms six at once, vs 1089 ms inline), the corpus facts (11 records over 3,000,000 chars, none moved), the CLAUDE.md corrections for the docs PR (delete the '50 ms heartbeat ticked 0 times' claim and the false 'fetch_iherb_price already parses inside a run_in_executor lambda' sentence; activation step (2) pairing; the residual), follow-ups W0-4e/W0-4f, and the honest limit that the corpus harness cannot observe the cap (it calls extract_price_from_html directly).
(11) Report the final sha256 of every file written. Never git checkout/stash/reset; the orchestrator commits.
