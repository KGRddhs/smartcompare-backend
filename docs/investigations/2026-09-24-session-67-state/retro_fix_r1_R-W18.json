{
 "unit": "R-W18 FIXER: the W1-8 adapter-drop visibility retro fix (W1-8b/c/d), second adversary's defects and prove-nothing rows. Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-w18, branch retro/w1-8-adapter-drop. The worktree was confirmed clean at start on wip e1d7be86 (above base 1c6f6796). All changes are uncommitted on top of the wip commit. UNFLAGGED, logging only, no return value changes.",
 "files_changed": [
  "C:/Users/SynAckITPC/Documents/AI/sc-r-w18/app/services/structured_comparison_service.py (final sha256 ddbf92ee103071b929c9cafd029fe55a3777cbc6374895c680ecae8e4895b50d; start b2b9bf065808e27e6b67d5664557351cd22bf285b0c3b237a0164ea879786506; CRLF preserved)",
  "C:/Users/SynAckITPC/Documents/AI/sc-r-w18/tests/test_retro_w1_8.py (final sha256 0ff7a0817cd974e320bdf7572bd0a3fc1431a4635d4f08560828f7e4ccb3a6c4; start 3fb44ae7b4c10ab73409d6a05420962c58f020a292cd4c81553b70debfadc1d9; LF kept, as at start)",
  "C:/Users/SynAckITPC/Documents/AI/sc-r-w18/tests/test_adapter_drop_visibility.py (final sha256 ca9f4b6d63ef609b5859012ada7e1b533d2c5675b7aec40c16aa8022fb75d134; start c15e4a7a747c926e1350b8f0d986c7e5f52fd6a804eb8b971bad397c7d12bbb1; CRLF preserved)"
 ],
 "all_unit_tests_pass": true,
 "test_evidence": "Unit files: tests/test_retro_w1_8.py and tests/test_adapter_drop_visibility.py give 47 passed, 0 failed at the final shas. Run on the pinned venv with -q -p no:cacheprovider -p no:randomly --timeout=120 -m \"not (live_unit or live_db or integration)\", plus a process-wide netguard plugin (scratchpad/rw18_fix/rw18fix_netguard.py). The netguard log recorded 0 attempts.\n\nRED BEFORE THE FIX: 9 new nodes failed for the defect reasons.\n- Real-stack curl transport failure: 0 fanout lines, expected 2.\n- Real firecrawl and scrapedo services: 0 lines, expected 1 each.\n- Status test \u00d72 and curl no-html: 0 lines.\n- Pending test: '[FANOUT] wave=curl TIMEOUT pending=4 elapsed=1.02' with 3 of 4 finished, which reproduces defect 2.\n- `_count_finished` missing.\n- `_safe_exc('via https://user:pa/ss2@proxy...')` came back unchanged, which reproduces defect 4.\n\nAFTER: 47/47. Every changed function's return value is pinned by a fixture:\n- `_curl_scraper`: None on no-html, miss and error; the exact hit dict.\n- `_firecrawl_scraper` / `_scrapedo_scraper`: None on status 0, 429, 403 and 200-empty; the provider-attempt outcome 'timeout' is unchanged.\n- The `_count_finished` wrapper returns and raises exactly what the scraper does, never counts a cancel, and gives an identical fan_out best / failed_count / cancelled_count through the real fan_out.\n- `_get_price`: still returns 244.99 page_scrape_jsonld and still falls through to Tier 2.\n- `_timeout_none`: None or the value on every branch.\n\nLogs: scratchpad/rw18_fix/green_run1.txt, flag_*.txt, red_run.txt.",
 "lint_clean": true,
 "mutation_checks": [
  "Method: the adversary's driver was adapted as scratchpad/rw18_fix/mutate.py, working only on a scratch copy (scratchpad/rw18_fix/mutroot). The copy held app/, data/, pyproject.toml, .env (copied, never printed), the conftest files and the two unit files. Its scs and census shas were verified equal to the worktree before running. Each mutant: byte snapshot, apply, run both unit files, restore from the snapshot bytes, sha256-verify. The final check printed 'FINAL SHA OK scs ddbf92ee census ca9f4b6d'. The unmutated control passed 47/47. git was never used. Log: scratchpad/rw18_fix/mut_log.txt; results: muts_fix_results.json.",
  "The adversary's full list was re-run at HEAD: all of M01-M08, M11-M13, M15-M18, M20-M30, M32-M33 and N01-N03, N05, N06, N08-N12 were KILLED. The formerly surviving N01 (timeout*0.5), N02 (ceiling*0.5), N03 (margin 0.9), N05 ('woo' hard-coded), N06 (jsonapi relabelled sitemap), N08 (inline sitemap walrus removed) and N11 (summary elapsed 0.0) are all now KILLED.",
  "Six anchors no longer matched after the regex and pending rewrite (BAD-ANCHOR) and were re-expressed; all are KILLED: R-N04 (drop (?i)), R-N07 (fragment kept), R-M09 (userinfo kept), R-M10 (query and fragment kept), R-M14 (first '@' instead of last). M19 (pending wrong) is covered by F09 (pending = wave size) and F13 (unwrapped scrapers).",
  "Fixer F-series, all KILLED: F01/F02 curl FETCH-FAIL removed / moved to DEBUG; F03/F04 firecrawl / scrapedo FETCH-FAIL removed; F05 firecrawl FETCH-FAIL only on status 0; F06 scrapedo logs a 200-empty miss; F07 curl logs any falsy result ({} miss); F08 curl no-html branch returns {}; F09 pending = wave size; F10 wrapper does not count a raise; F11 wrapper counts a cancellation (BaseException); F12 wrapper swallows a raise; F13 call site passes unwrapped scrapers; F14 ambiguous '@' keeps the tail; F15 unbounded scheme quantifier (quadratic; killed by the perf pin via pytest-timeout); F16 curl FETCH-FAIL carries {url}; F17 wrapper returns None instead of the result.",
  "Total: 60 mutants executed, 60 KILLED, 0 SURVIVED. The 6 BAD-ANCHOR entries are accounted for by re-expressed mutants."
 ],
 "comm_gate": "Set: the recorded 152-file module-reference set (.qa-retro/R-W18_comm_set.txt). Flags: the exact pinned flags plus the netguard plugin rw18fix_netguard, which is the adversary's rw18adv_netguard: Python socket guard, loopback accept-and-close proxy sink, platform and openai.resources warm-up.\n\nHEAD at scs ddbf92ee and census ca9f4b6d: 2 failed, 3963 passed, 2 skipped, 29 deselected, 17 warnings in 306s. BASE (recorded by the red phase at 1c6f6796, reused as instructed): 2 failed, 3963 passed.\n\nThe failed-id sets are identical, and comm -13 base head (branch-only-NEW) is EMPTY. The two failures are:\n- tests/test_page_scraping.py::TestFetchPagePriceJsonLD::test_jsonld_nested_offers_picks_lowest\n- tests/test_page_scraping.py::TestFetchPagePriceOpenGraph::test_og_meta_extraction\n\nBoth are the base's 2, listed in tests/.pre_impl_failures.txt. The netguard log recorded 0 attempts.\n\nThe warning count is 17 against the recorded 16. The extra instance is the GC-timed RuntimeWarning \"coroutine 'save_specs' was never awaited\" from tests/test_spec_spine_d2.py (reported via unittest.mock/argparse). No warning mentions structured_comparison_service or `_count_finished`.\n\nNo scratch worktree was created. The recorded base was reused, and git worktree list shows no rw18 entry.\n\nLogs: scratchpad/rw18_fix/comm_head.txt, head_failed.txt, base_failed.txt, comm_netlog.txt.",
 "flag_identity": "UNFLAGGED per the rulings: the drops are silent, have no legitimate reader, and the change only adds logging.\n\nAdditivity and identity proof:\n\n(1) Every new statement is a logger.info call, the pure helper `_safe_exc` / `_safe_exc_url`, a local counter, or the `_count_finished` wrapper.\n\n(2) The three new FETCH-FAIL lines sit inside existing branches that already return None; the return statements are untouched. `_curl_scraper` gains `if page_price is None: log; return None` ahead of the existing `if not page_price ...: return None`, so the result is identical. Pinned by F07/F08 plus the miss pin ({\"_got_html\": True}, {}, amount None all stay silent and return None).\n\n(3) The one structural change in the cascade is `scrapers=[_count_finished(_s, _wave_finished) for _s in _scrapers]`. The wrapper awaits the scraper and returns its result object or re-raises its exception; cancellation (a BaseException) passes through uncounted.\n- Pinned by test_w18b_counting_wrapper_is_return_and_raise_identical: result identity, raise propagation, cancel not counted, and the real fan_out giving best identity with failed_count 1 and cancelled 0.\n- F10/F11/F12/F13/F17 killed.\n- Proven equal through the real `_get_price` and fan_out (all scrapers ran; Tier-2 fall-through unchanged).\n\n(4) `_safe_exc` only formats log text.\n\n(5) The unit files pass 47/47 in six states: default, ENABLE_GENUINE_PRICE_PRIORITY, ENABLE_NOT_A_PDP_FILTER, ENABLE_PRICE_PARSE_OFFLOAD, ENABLE_CONVERTED_PROVENANCE_STAMP (each alone), and all four on. None of these is defined in .env; I checked by name only.\n\n(6) Comm gate over 152 files: branch-only-NEW is empty and pass counts are identical (3963/3963).\n\n(7) Cancellation is still never caught in `_timeout_none` (M30 killed).\n\nscripts/verify_flag_byte_identity.py was not run: it exercises only extract_price_from_html, which this unit does not touch.",
 "measurements_run": [
  "Reproduced defect 1 before fixing, through the real stack. test_w18b_real_curl_transport_failure_reaches_one_fetch_fail_line (only curl_cffi.requests.get stubbed to raise a curl Timeout) showed 2 service-layer WARNINGs and 0 `[ADAPTER DROP] fanout:` lines. The firecrawl/scrapedo twins (only httpx.AsyncClient.post/get raising ConnectTimeout) each showed 0 lines. After the fix: one FETCH-FAIL line per scraper, domain only.",
  "Reproduced defect 2 through the real _get_price and the real fan_out, with 3 instant misses, 1 hang and FAN_OUT_BUDGET_SECONDS=1.0: before '[FANOUT] wave=curl TIMEOUT pending=4 elapsed=1.02'; after pending=1.",
  "Reproduced defect 4: _safe_exc('via https://user:pa/ss2@proxy.example:22225/x y') came back unchanged before the fix. After: 'via https://proxy.example:22225/x y'. The '?' and '#' in-password cases and the '@'-in-query case are now redacted, and socks5 userinfo is stripped.",
  "Defect 3, read from source rather than executed: adapter_timeout(d) = min(d, adapter_inner_ceiling()), with adapter_inner_ceiling() = max(1.0, min(10.0, PRICE_RACE_TIMEOUT or 15.0) - 1.0) = 9.0 by default. The ruled threshold is therefore 8.5 s. unbxd_service.py:58 and noon_service.py:102 use adapter_timeout(8.0) = 8.0 s, below 8.5, so they cannot produce SLOW-MISS for a single-hop swallowed inner timeout. woo/salla/occ/magento_gql/rest_json/brightdata clamp to 9.0 s and are covered. The adversary's 8.06 s measurement is consistent with this.",
  "Unit runs: 47/47 at the final shas in each of six flag states (logs scratchpad/rw18_fix/flag_*.txt); the red run had 9 failing, 38 passing.",
  "Mutation matrix: 60 executed, 60 KILLED, unmutated control passing, final restore sha OK.",
  "Comm gate HEAD over 152 files: 2 failed / 3963 passed, identical to base; branch-only-NEW empty; netguard 0 attempts.",
  "ruff check --select E9,F63,F7,F82 --no-cache: 'All checks passed!' on all three files. py_compile: OK on all three.",
  "git diff --stat: scs +77/-? lines (logging, the helper, the wrapper), the census file +64, the retro test file +433/-? lines. Only intended hunks."
 ],
 "pr_text": "fix(price): make adapter and fan-out drops visible and bounded (R-W18, W1-8 retro); logging only, unflagged\n\nDEFECTS (from the retro adversary review of PR #143, reproduced as probes P1-P5, then from the R-W18 green adversary):\n- W1-8b: fan-out fetch failures were invisible at INFO. The retro review's premise was that `_curl_scraper`, `_firecrawl_scraper` and `_scrapedo_scraper` catch their fetch exception at DEBUG. That premise is only half right. Every fetch layer swallows transport errors before the scraper sees them:\n  - `curl_fetch_html_same_site` logs a WARNING and returns None.\n  - firecrawl and scrapedo return `(None, 0)` or `(None, <status>)`.\n  So the scraper `except` branch only ever sees a bug. The green adversary drove the real stack, stubbing only `curl_cffi.requests.get` or `httpx.AsyncClient.post/get`, and got zero `[ADAPTER DROP] fanout:` lines for a wave where every fetch failed. `failed_count` stays 0 for these failures and nothing reads it.\n- W1-8c: the three consume `except asyncio.TimeoutError` branches (sitemap, jsonapi, the `_new_adapter_specs` loop) are silent. `_timeout_none` cannot see an inner timeout the adapter swallowed. It also logs a TimeoutError the adapter raised 10 ms in as `TIMEOUT after 10.0s`.\n- W1-8d: the ERROR drop line writes `str(exc)` verbatim. Probe P4 produced a 200,136-character record with a proxy password and a raw newline.\n- Wave TIMEOUT line: `pending=` reported the wave size, not the count still pending (reproduced: `pending=4` with 3 of 4 finished).\n\nFIX (app/services/structured_comparison_service.py only; no return value changes):\n- Per-scraper fetch failure, the PRODUCTION shape. One INFO line per failed scraper:\n  - `[ADAPTER DROP] fanout:curl:<retailer_domain>: FETCH-FAIL no-html` when fetch_page_price returns None, meaning no HTML was obtained: a transport error, a wall, a non-2xx response or a blocked URL. `{\"_got_html\": True}` is a MISS and stays silent.\n  - `[ADAPTER DROP] fanout:<firecrawl|scrapedo>:<retailer_domain>: FETCH-FAIL status=<n>` when the service returns no HTML with a non-200 status. 0 means transport timeout or error, 429/503 mean throttle, 403 means wall. A 200 without usable HTML is a miss and stays silent.\n- Per-scraper escaped exception (a bug above the transport): `[ADAPTER DROP] fanout:<kind>:<retailer_domain>: ERROR <Type>: <_safe_exc text>`, replacing the DEBUG `{url} raised` lines. Both token families carry the retailer label as the scraper receives it, never the scraped URL.\n- Per-wave summary after fan_out returns: `[FANOUT] wave=<curl|render> completed=N failed=N cancelled=N elapsed=S`, where completed = wave size - failed - cancelled and elapsed is the measured wave wall time.\n- Wave budget TimeoutError: `[FANOUT] wave=<kind> TIMEOUT pending=N elapsed=S`. N is exact: the call site wraps each scraper in `_count_finished`, which counts a return or a raise and never counts a cancellation. The wrapper returns or raises exactly what the scraper does, so fan_out's best, alternates and counts are identical.\n- Consume branches: `[ADAPTER DROP] <sitemap|jsonapi|woo|salla|occ|...>: CONSUME-BOUND after <bound>s`, with the bound actually waited under, captured with a walrus at the existing `_consume_bound()` call.\n- `_timeout_none` is timed with time.monotonic():\n  - `TIMEOUT after <elapsed>s` only when elapsed >= timeout - 0.05; any earlier TimeoutError is `ERROR TimeoutError (adapter-raised)`.\n  - `SLOW-MISS after <elapsed>s` when the adapter returns None at or after adapter_inner_ceiling() - 0.5.\n  - Fast misses and every hit stay silent. Cancellation is still never caught and never logged.\n- New local `_safe_exc(exc)`: it rewrites every `<scheme>://<token>` (any scheme, so http, https, socks5 and so on, with a bounded scheme match so it stays linear).\n  - URL userinfo is stripped up to the LAST '@' in the token, so a '@', '/' or ':' inside an unencoded password cannot leak.\n  - The query string and fragment are dropped; host and path are kept.\n  - When the text before that '@' holds a '?' or '#', the token is replaced by `[redacted]`. That '@' could sit in a query or in a password, and the two cannot be told apart.\n  - Every line break (including U+2028/U+2029 and \\x0b) becomes one space. The text is scrubbed before it is truncated to 200 characters.\n  - It is applied to the `_timeout_none` ERROR line and to the fanout ERROR lines. The W1-1 Sentry helper was measured unfit: it keeps `api_key=` and never strips userinfo.\n\nTHE PRODUCTION SIGNAL. `failed_count` keeps its semantics and stays 0 for real fetch failures. The production failure signal is the per-scraper `[ADAPTER DROP] fanout:` lines: FETCH-FAIL for a fetch that got no page, ERROR for an escaped bug. The W5 STEP-0 canary must count `[ADAPTER DROP] fanout:` lines (bucketed by FETCH-FAIL status / no-html vs ERROR) plus `CONSUME-BOUND`, `SLOW-MISS` and `TIMEOUT` lines, not failed=. The service-layer WARNING lines (`[PRICE] curl_fetch_html_same_site failed for <url>`, `[FIRECRAWL] Timeout`, `[SCRAPEDO] Timeout`) still fire unchanged and still carry the full URL (untouched, out of scope).\n\nFLAG ROW: none. The fix is unflagged because the reviewer found silent drops with no legitimate reader, and the change only adds log lines. Every changed function's return value is pinned by fixture-driven assertions. The call-site wrapper is pinned return- and raise-identical through the real fan_out.\n\nACTIVATION GATE: live on deploy; nothing to flip. Before reading W5 canaries off these lines, watch INFO volume during the first slow-source episode. The expected volume is:\n- one FETCH-FAIL line per curl URL that got no HTML (404 dead slugs included), plus one per failed render call;\n- at most one line per dropped adapter;\n- up to 2 [FANOUT] lines per product.\nVolume is UNMEASURED. price-warmer runs stale 2026-09-02 code and emits none of this until redeployed.\n\nEVIDENCE:\n- Unit: tests/test_retro_w1_8.py plus tests/test_adapter_drop_visibility.py give 47/47 on the pinned venv with an autouse zero-network guard plus a process-wide netguard plugin. The result is the same 47/47 under ENABLE_GENUINE_PRICE_PRIORITY, ENABLE_NOT_A_PDP_FILTER, ENABLE_PRICE_PARSE_OFFLOAD, ENABLE_CONVERTED_PROVENANCE_STAMP (each alone) and all four on.\n- Real-stack pins: a curl_cffi Timeout through the real curl_fetch_html_same_site, fetch_page_price, _curl_scraper and fan_out gives one FETCH-FAIL line per scraper. httpx ConnectTimeout through the real firecrawl/scrapedo services gives `FETCH-FAIL status=0`. The wave TIMEOUT through the real _get_price and fan_out gives `pending=1` for 3 finished and 1 hung.\n- Mutation matrix: 60/60 KILLED from sha-verified byte snapshots in a scratch copy, with an unmutated control passing. It covers the adversary's M/N list re-run at HEAD, with re-expressed anchors where the regex moved, plus the fixer's F01-F17.\n- Comm gate: the 152-file module-reference set under the netguard plugin gives 2 failed / 3963 passed, identical to base 1c6f6796. branch-only-NEW is empty, and both failures are in tests/.pre_impl_failures.txt.\n- Lint: ruff E9,F63,F7,F82 and py_compile are clean.\n\nHONEST LIMITS:\n- SLOW-MISS keys on adapter_inner_ceiling() - 0.5, as ruled. adapter_inner_ceiling() defaults to 9.0 s, so the threshold is 8.5 s. unbxd and noon clamp their inner fetch to adapter_timeout(8.0) = 8.0 s, so their single-hop swallowed inner timeout returns None at about 8.0 s and stays SILENT. Adapters at the 9.0 s clamp are covered (woo, salla, occ, magento_gql, rest_json, brightdata). Covering unbxd/noon needs a re-ruling of the threshold (for example, min(ceiling, the adapter's own clamp) - 0.5).\n- `FETCH-FAIL no-html` for curl is inferred from fetch_page_price returning None. Two other paths also return None and would be mislabelled:\n  - a content-safety drop inside fetch_page_price, which logs its own `[content_safety]` line first;\n  - with ENABLE_NOT_A_PDP_FILTER on (default OFF), a fetched page classified not-a-PDP.\n- A render `FETCH-FAIL status=404/410` means the provider or upstream returned no page. The line reports the status, and the reader decides whether it counts as load.\n- `_safe_exc` covers URLs of the form `<scheme>://`. A bare `api_key=...` outside a URL, and percent-encoded credentials, are not rewritten (the enclosing URL's query is dropped, which covers the common case). A fanout ERROR line keeps any host and path inside the exception's own text.\n- `<retailer_domain>` is the harvest label (it can be a subdomain); no eTLD+1 is computed.\n- CONSUME-BOUND names the family, not which sources in it were still pending (the cancelled per-source tasks stay unlogged by design).\n\nFOLLOW-UPS, not in this unit:\n- Measure log volume on the first canary.\n- Re-rule the SLOW-MISS threshold for the 8.0 s clamp adapters (unbxd, noon).\n- Scrub the service-layer WARNING lines, which log full URLs (price_service / firecrawl_service / scrapedo_service).\n- The PR #143 body's byte-identity citation is environment-dependent. Future claims should cite a same-env parent-vs-head pair only.\n- Redeploy price-warmer to pick this up.\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)",
 "deviations_from_spec": [
  "DEFECT 1 (major), FIXED BY EXTENDING THE RULED TOKEN SET \u2014 needs orchestrator/Fable confirmation. The adversary asked for a re-ruling and offered two options: move the line to the service layer (out of this unit's file scope), or have pr_text stop naming the fanout lines as the production signal. Ruling 4 binds pr_text to say the `[ADAPTER DROP] fanout:` lines ARE the production signal, and that is only true if they fire on real failures. So inside scs I added `[ADAPTER DROP] fanout:curl:<domain>: FETCH-FAIL no-html` (fetch_page_price returned None) and `[ADAPTER DROP] fanout:<firecrawl|scrapedo>:<domain>: FETCH-FAIL status=<n>` (no HTML with a non-200 status; a 200-empty stays a silent miss). FETCH-FAIL is a new token beside ruling 3's `ERROR <Type>`, which stays reserved for escaped exceptions (bugs). This is logging only, and every return is unchanged. If the ruling is 'pr_text only', revert hunks B/C/D (the three FETCH-FAIL lines), the four FETCH-FAIL tests and the miss-pin edit, then rewrite the PRODUCTION SIGNAL paragraph to point at the service WARNING lines.",
  "test_pin_scraper_misses_are_not_drops was redefined. A curl miss is now {\"_got_html\": True}, {} or an amount-less dict (all silent). fetch_page_price -> None is now a FETCH-FAIL (new test_w18b_curl_no_html_is_one_fetch_fail_line). The old pin treated 'no HTML' as a miss, which was exactly defect 1's blind spot.",
  "DEFECT 2 (minor), FIXED as the adversary proposed: a module-level `_count_finished(scraper, box)` wrapper at the fan_out call site makes `pending=` the true count of scrapers that neither returned nor raised. The green's claim that this was 'not reachable without changing price_service' was wrong.",
  "DEFECT 3 (minor), NOT FIXED IN CODE, by design. Ruling 1 fixes the SLOW-MISS threshold at adapter_inner_ceiling() - 0.5, and I did not override a binding ruling. Measurement from source: the threshold is 8.5 s at the default ceiling of 9.0 s, while unbxd_service.py:58 and noon_service.py:102 use adapter_timeout(8.0) = 8.0 s, so their single-hop swallowed timeouts stay silent. pr_text is corrected: the ceiling is 9.0 s and 8.5 s is the threshold, unbxd/noon are named as not covered, and a re-ruling is suggested (min(ceiling, the adapter's own clamp) - 0.5). test_w18c_slow_miss_threshold_is_ceiling_minus_half_a_second pins the ruled threshold exactly.",
  "DEFECT 4 (minor), FIXED and broadened slightly beyond the ruled http(s) scope. _safe_exc now rewrites every `<scheme>://` token, with the scheme quantifier bounded to {0,15} so the scrub stays linear (the F15 unbounded mutant times out). Userinfo is stripped up to the LAST '@' in the whole token, so '/', '?', '#' or '@' inside a password no longer leak. If the text before that '@' holds '?' or '#', the whole token becomes `[redacted]`, because a query '@' and a password '?' cannot be told apart. A bare `api_key=` outside a URL remains out of scope.",
  "Prove-nothing rows: the raise-stub tests (W1-8b \u00d73, P1-inverted, the fanout _safe_exc \u00d73) are KEPT as deliberate pins of the escaped-exception (bug) branch format, and their docstrings now say so. Production visibility is pinned by the new real-stack tests. The surviving mutants now each have a test, and every one is KILLED:\n- N01: a raise at 0.85 of a 1.0 s wrap.\n- N02/N03: a PRICE_RACE_TIMEOUT=3.0 ceiling of 2.0, with a 1.25 s miss silent and a 1.6 s miss logging SLOW-MISS.\n- N04: an upper-case URL.\n- N05: a second new-adapter family (salla) added to the consume-bound test.\n- N06: the census now checks each label's family against its lambda's callee (_sitemap_fetch_coro->sitemap, fetch_nasser_price->jsonapi, _na_fn->{_na_key}).\n- N07: a fragment-only secret.\n- N08: a new inline-sitemap-branch test.\n- N11: a measured summary elapsed of at least 0.25 s.",
  "The comm gate reused the red phase's recorded BASE, as instructed; no scratch worktree was created. HEAD shows 17 warnings against the base's 16. The extra instance is an unrelated GC-timed 'save_specs never awaited' RuntimeWarning from test_spec_spine_d2."
 ],
 "git_status_final": " M app/services/structured_comparison_service.py\n M tests/test_adapter_drop_visibility.py\n M tests/test_retro_w1_8.py",
 "residual_risk": "1. The FETCH-FAIL token is a fixer-level extension of ruling 3. It needs orchestrator/Fable confirmation before merge; the revert path is in deviations.\n\n2. INFO volume is unmeasured. The worst case is one FETCH-FAIL line per curl URL that got no HTML (404 dead slugs included) plus one per failed render call, per-source adapter drops, up to 2 [FANOUT] lines, and up to about 10 CONSUME-BOUND lines per product.\n\n3. `FETCH-FAIL no-html` can mislabel two paths that also return None:\n   - a content-safety drop inside fetch_page_price (it logs its own line first);\n   - a not-a-PDP verdict when ENABLE_NOT_A_PDP_FILTER is on (default OFF).\n\n4. SLOW-MISS cannot see unbxd/noon single-hop swallowed timeouts (their clamp is 8.0 s, the threshold 8.5 s). Fixing it needs a re-ruling.\n\n5. The service-layer WARNING lines in price_service, firecrawl_service and scrapedo_service still log full URLs. That is out of this unit's file scope.\n\n6. `_safe_exc` does not scrub non-URL `key=value` secrets or percent-encoded credentials. A `[redacted]` token loses host information in the ambiguous '@' case.\n\n7. The timing-based pins use margins of 0.25 s or more (1.25 against the 1.5 s threshold, 0.85 against the 0.95 s wrap margin). A heavily loaded box could flake them, although they passed in all six flag-state runs and the 60-mutant matrix while a comm run was going on concurrently.\n\n8. The wip commit e1d7be86 still carries the pre-fix code. The orchestrator must commit these uncommitted changes on top, and rebase onto origin/main d70dd876 after SOUND. The worktree was not rebased."
}