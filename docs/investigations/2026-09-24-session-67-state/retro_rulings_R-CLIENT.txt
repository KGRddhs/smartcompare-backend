(1) logout() must never block on a black-holed refresh: race the in-flight refresh against a 5 s bound (`Promise.race`), then proceed with the freshest pair available; add one fake-timer pin that a refresh which never settles still lets logout send its POST after the bound and clear state. (2) P-A3 semantics are preserved by construction: logout captures the in-flight refresh's RESULT directly from the awaited promise (the new pair) and bumps the session epoch BEFORE awaiting, so a refresh landing after that bump neither writes storage nor fires onSessionRefreshed (today's epoch guard) but still resolves for the awaiting logout. Therefore __tests__/authService.bootOptimistic.a3.test.ts:528 may be edited ONLY to resolve the pending boot-refresh POST while logout is pending (it must not deadlock); its assertions (storage ends cleared, onSessionRefreshed NOT called, no resurrection) must stay green unchanged. If the design cannot satisfy that, STOP and report instead of weakening a3. (3) The expired-token check decodes the JWT payload without verification; malformed/unknown tokens send the stored pair as today (the red's six pins). (4) A pre-logout refresh that returns 401 clears state; at most one logout POST is acceptable (the red's reading of ruling 3). (5) The rewritten 'sends the SAME stored value' node (drives refreshSession) stands. (6) pr_text carries the CLAUDE.md row correction from .qa-retro/pr_text_draft.txt and states that the backend admin.sign_out half lands in R-AUTH. (7) Run the full jest suite once at the end (--maxWorkers=25%) and compare with the base full-suite run the red recorded (290 suites / 2865 tests / 44 snapshots).