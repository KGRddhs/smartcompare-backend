# Fable rulings, session 67 (2026-09-24) - binding for the polish/commit of each retro group

## R-METER (adversary r1 SOUND, 4 minors)
- minor 1 (url_routes LLM_UNAVAILABLE -> 503 via the text route's mapping): ACCEPTED as built. 503 is the text route's contract; ruling 3's "400" was the contradiction, not the code. PR body states 503.
- minor 2 (url_extraction_service `if "error" in comparison` catches a SUCCESSFUL verdict whose JSON carries `error: null`): FIX in the polish: key on TRUTHINESS (`if comparison.get("error"):`) - both the str(e) branch and W4-9's constant are non-empty strings, so it still composes; add two pins: valid verdict with `error: None` -> 200 success (base behaviour), non-empty error -> 503 LLM_UNAVAILABLE.
- minor 3 (stale pr_text): rewrite pr_text at commit time from the r1/r2 adversary facts (comm gate ran clean; 54 mutations / 47 killed / 7 benign survivors; the 8 pins d43f9bdb added).
- minor 4 (envelope ON + metering OFF still logs success + writes history): ACCEPTED, pinned, documented - the no-bill half belongs to ENABLE_PAID_ROUTE_METERING by design.
## R-BREAKER (adversary r0 SOUND, 5 minors, 3 prove-nothing) - fix round runs automatically
- pin the cancelled-probe outcome (release, not success); pin the Exception arm as probe-only; the BaseException (KeyboardInterrupt/SystemExit) wedge: ACCEPT as a stated limit unless the fixer's arm is <= 5 lines and pinned; the fail-open-admission double-probe and stale-memo streak wipe: stated limits + follow-ups (W1-3c), not this unit.
## R-MIG (adversary r0 DEFECTIVE: major = 040's proname filter unpinned; N2 PERFORM / N3 identity_arguments mutants survive) - fix round runs automatically
- the fixer must pin the executed statement shape (EXECUTE + pg_get_function_identity_arguments + the proname predicate) with source-level tests that fail on N1/N2/N3/N4; the two docs lines (w3-remainder-state:126, CONTEXT_SESSION_LOG:68) go into pr_text's docs-corrections list (docs PR), not edited here; the 037 header rule keyed on policy names: state the limit (qual/with_check printed, not judged) in the header + pr_text.
## R-MAIN (green resume -> adversary r0 DEFECTIVE on tree-dirty + the 59-slot mutant in b519f9de -> fix adopted the green's edits) - r1 running
- the orchestrator commits the green's two uncommitted files after r1 reports; b519f9de's 59-slot ring is a session-66 mutant (lesson: the close re-check skipped the two PARTIAL groups).
## Script gap (mine): in green mode the adversary prompt referenced a non-existent retro_green_<KEY>.json and assumed a clean tree. Mitigation: retro_green_R-MAIN.json written from the journal; a poller writes retro_green_R-AUTH.json + wip-commits sc-r-auth's green output the moment green:R-AUTH lands.

## R-MAIN round 2 (adversary r1 DEFECTIVE: 1 blocking + 4 minors + 3 prove-nothing) - Fable rulings, binding
- BLOCKING (test-order pin): tests/test_retro_w1_1.py must never hold a module-level reference to sentry_service._before_send (or any sentry_service attribute) - resolve through the module object at call time (import the module, read the attribute inside the test) so a reload by tests/test_observability.py cannot break identity; prove it by running tests/test_observability.py then tests/test_retro_w1_1.py in ONE process (that is CI's order) before and after.
- minor (59-slot ring in b519f9de): CLOSED by the orchestrator's wip commit of the working tree (main.py fd091ace..., test file 7639c7b3...).
- minor (C5: 'the try wraps only the decode' unpinned): ADD the pin (mutant C5 must go red) - the claim stays in pr_text only if pinned.
- minor (SlowAPIASGIMiddleware re-sends http.response.start before every body chunk on slowapi 0.1.10): ACCEPTED as a stated limit + pin that every route the default limiter can reach today is single-chunk (/health, /, /favicon.ico) and record follow-up W1-9d (router routes are never default-limited; a multi-chunk non-exempt route would break) in pr_text and the CLAUDE.md corrections.
- minor (.env copy in the fixer's scratch base_tree): DELETED by the orchestrator; rule for every agent: never copy .env into a git-archive extraction, only into a detached worktree that is removed afterwards.
- prove-nothing rows: pin or drop each claim (decorated-route exemption half is vacuous on the pinned slowapi -> rewrite the test to pin what IS true and say so in pr_text; the recorder-recomputes claim -> pin it or delete the sentence).
## R-W18 (adversary r0 DEFECTIVE: the per-scraper ERROR drop lines can never fire because every fetch layer swallows transport exceptions) - ruling for the fix round (running) and any r2
- The drop line must be emitted where the failure is OBSERVABLE: in the layer that catches the transport exception (curl_fetch_html_same_site's warning, firecrawl_service.scrape_page_with_status's catch, the scrapedo equivalent) format that existing log as the ruled `[ADAPTER DROP] fanout:<kind>:<domain>: ERROR <Type>` line (registrable domain only, _safe_exc on the message), OR have the scraper treat a None fetch as a drop with reason NONE - logging only, no return-value change, both pinned through the REAL stack (a stub that raises inside the fetch layer). The pr_text canary claim must match what actually fires.
- minor pending=<n>: log the still-pending count (len(scrapers) - completed), not the wave size.
- minor SLOW-MISS threshold vs the 8.0 s inner clamp of unbxd/noon: a ruling gap - use `min(adapter_inner_ceiling(), <adapter's own clamp>) - 0.5` where the adapter exposes its clamp, else disclose per adapter in pr_text.
- minor _safe_exc userinfo regex stops at / ? #: widen the userinfo class to `[^\s@]*@` inside the URL match and pin the three shapes; socks5:// and bare api_key= stay out of scope (stated).
## R-CLIENT (adversary r0 DEFECTIVE: the logout handoff slot accepts ANY epoch-guarded refresh from ANY session) - ruling for the fix round (queued) and any r2
- The handoff must be keyed to the session it ends: capture the session identity (the stored refresh token or user id) when logout starts and accept a refresh result into the slot only when the refresh began under the SAME identity/epoch; a refresh that lands after a later login is ignored (and never revoked). Pin the cross-session scenario the adversary built (A's refresh hangs > 5 s, A logs out, B logs in, A's refresh lands) - B's session must survive.
- minors: pin the second-bounded-refresh-after-transient-failure claim or drop it from pr_text; pin the handoff edges (identity check, slot release, fallback to the spent token, success:false) - the four surviving mutants N04-N07 must go red.
