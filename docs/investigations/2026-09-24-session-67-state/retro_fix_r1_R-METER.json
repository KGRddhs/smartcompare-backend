{
 "unit": "R-METER round 4 (retro/w2-1-metering on the rebased tree b8b2c983 = origin/main db1ed3a4 + 3 wip commits): W2-1b camera unsuccessful result, W2-1c /url/compare failed-verdict leak, W2-1d anon refunds",
 "files_changed": [
  "app/api/url_routes.py",
  "tests/test_retro_w2_1.py"
 ],
 "all_unit_tests_pass": true,
 "test_evidence": "Start state: git status --short was empty at HEAD b8b2c983 (confirmed first). Unit files tests/test_retro_w2_1.py + tests/test_paid_route_metering.py on the pinned venv (.venv-qaren, CI pins), PYTHONIOENCODING=utf-8, -q -p no:cacheprovider -p no:randomly --timeout=120 -m \"not (live_unit or live_db or integration)\", process-wide non-loopback socket guard loaded as a pytest plugin (-p sockguard, PYTHONPATH=scratchpad/rmeter_r4/guard/sockguard.py, sha256 815df568789d89afee57db60125a6afba3dd23f105ac4e9dd1fe4273929dd814, byte-identical to the round-3 guard) so it is active before conftest and before any app.* import. Six ambient flag states, each 132 passed / EXIT=0: none; ENABLE_PAID_ROUTE_METERING; ENABLE_CAMERA_FAILURE_ENVELOPE; ENABLE_ANON_USAGE_GATE; METER+ENVELOPE; all three (logs scratchpad/rmeter_r4/unit_{none,M,E,A,ME,MEA}.txt). The two nodes the orchestrator had red on the rebased tree (test_pin_url_compare_extraction_failure_400_detail_unchanged, test_pin_url_compare_extraction_failure_envelope_unchanged) pass UNCHANGED (their bodies were not edited). Guard hits in the unit runs: only getaddrinfo('neutralized.supabase.invalid') from two pre-existing test_paid_route_metering nodes (test_camera_success_consumes_once_and_never_refunds, test_url_compare_saves_history_with_input_type_url), blocked, same as round 3; no real network. Label check against BASE db1ed3a4 (HEAD test file run in the detached base worktree): 67 failed / 44 passed; RED->FAILED 27, BASE PIN->PASSED 40, RECORD/BASE PIN->PASSED 2, RECORD->PASSED 2, GREEN PIN->FAILED 40; MISLABELLED: [] (log scratchpad/rmeter_r4/base_classify.txt). In particular the two <2-products pins PASS at origin/main db1ed3a4, i.e. HEAD's code-less /url/compare body is byte-identical to main.",
 "lint_clean": true,
 "mutation_checks": [
  "Driver scratchpad/rmeter_r4/driver4.py: in-place in sc-r-meter, each mutated file byte-snapshotted first, restored from the snapshot in a finally, sha256-verified after every mutant (url_routes.py f27ef446..., url_extraction_service.py d3e378fc... confirmed after the run); flags unset; results in scratchpad/rmeter_r4/mutations.jsonl.",
  "r4_codeless_through_mapping (url_routes: `if result.get(\"code\"):` -> `if True:`, i.e. the round-3 shape: the code-less <2-products exit goes through W4-9's allowlist) -> KILLED, 2 failed: test_pin_url_compare_extraction_failure_400_detail_unchanged, test_pin_url_compare_extraction_failure_envelope_unchanged (the exact two nodes the orchestrator saw red).",
  "r4_drop_coded_branch (`if result.get(\"code\"):` -> `if False:`, the coded LLM_UNAVAILABLE failure takes the bare 400) -> KILLED, 5 failed: test_url_compare_failed_verdict_wire_shape[POST/GET], test_green_url_compare_full_verdict_with_nonempty_error_is_503[POST/GET], test_pin_url_compare_surfaced_failure_is_returned_not_billed.",
  "r4_coded_return_dropped (`return surfaced` -> `pass`, a returned CONTENT_UNAVAILABLE body falls through to the 400) -> KILLED, 1 failed: test_pin_url_compare_surfaced_failure_is_returned_not_billed.",
  "r4_codeless_detail_default_changed (bare `detail=result.get(\"error\", ...)` -> structured `{code: BAD_REQUEST, error: ...}`) -> SURVIVED: wire-equivalent by construction, the error handler lifts that dict to the identical envelope {success:false, error, code:BAD_REQUEST}; the full-envelope pin compares it and it matches. Benign equivalent.",
  "a3_url_pop_falsy_error (adversary survivor, in-place pop of a falsy `error` before the truthiness check) -> KILLED, 6 failed: test_pin_compare_from_urls_falsy_error_verdict_delivered_as_base[error_none/error_empty], test_pin_url_compare_falsy_error_verdict_is_200_delivery[POST/GET x error_none/error_empty].",
  "a3_url_falsy_to_none (adversary survivor) -> KILLED, 3 failed (the error_empty rows of the same two pins).",
  "a3_url_clear_when_falsy (adversary survivor) -> KILLED, 6 failed (same six rows).",
  "a3_url_fabricate_winner0_when_falsy (adversary survivor) -> KILLED, 6 failed (same six rows).",
  "r4_success_inplace_add_key (new: unconditional in-place `comparison[\"probe_inplace\"] = 1` before the check, the success-path blind spot of the REAL_VERDICT[0] pins) -> KILLED, 10 failed incl. test_pin_compare_from_urls_success_is_byte_identical and test_pin_url_compare_success_route_body_flag_off[POST/GET].",
  "CONTROL (proves the kills come from this round's pins): the same five UX mutants run with the HEAD (pre-round-4) test file swapped in from a byte snapshot and restored (sha 1d14c619... verified): a3_url_pop_falsy_error / a3_url_falsy_to_none / a3_url_clear_when_falsy / a3_url_fabricate_winner0_when_falsy all SURVIVED 132/132 (reproduces the adversary), and r4_success_inplace_add_key was caught only by the unrelated sparse-verdict pin (1 failed), not by the two success pins."
 ],
 "comm_gate": "HEAD comm gate RUN (closes the recorded green gap) over the 17-file set recorded in .qa-retro/comm_set.txt (its test_*.py files; scratchpad/rmeter_r4/comm17.txt), flags unset, socket-guard plugin, same pytest flags as the unit runs. BASE = a detached scratch worktree of origin/main db1ed3a4 (per the round-4 ruling, not 1c6f6796) at scratchpad/rmeter_r4/basewt with .env copied in only there. Result: BASE db1ed3a4 = 4 failed, 393 passed, 4 deselected; HEAD (b8b2c983 + this round's uncommitted edits) = 4 failed, 393 passed, 4 deselected; failed ids IDENTICAL (diff empty): tests/test_camera_vision.py::TestIdentifyProductsMocked::{test_empty_product_fields_normalized, test_malformed_response_returns_error, test_successful_identification} (#89) and tests/test_rate_limiting_complete.py::TestUrlDetectSsrf::test_detect_allows_valid_url (needs DNS; the guard blocks it on both sides). No new failure. All guard hits were blocked lookups (neutralized.supabase.invalid, example.com, amazon.ae, 169.254.169.254, api.openai.com) and they come from the same tests on both sides. The base worktree was removed afterwards: its .env copy and a temporary test-file copy were deleted as single files, then `git worktree remove` (exit 0). `git worktree list` no longer shows it, and a `find -name '.env*'` over scratchpad/rmeter_r4 returns nothing.",
 "flag_identity": "UNFLAGGED url_routes change (composition fix): a code-less non-success now runs main's exact statement `raise HTTPException(status_code=400, detail=result.get(\"error\", \"Comparison failed\"))`, the same bytes as db1ed3a4 lines 115-118 (the diff against db1ed3a4 shows that raise as context, not as a change). So the <2-products body is byte-identical to origin/main in every flag state. Proof: test_pin_url_compare_extraction_failure_envelope_unchanged checks the full envelope minus request_id, {success:false, error:'Could not extract both products', code:'BAD_REQUEST'}, and test_pin_url_compare_extraction_failure_400_detail_unchanged checks the 400. Both PASS at base db1ed3a4 and at HEAD in all six flag states, and mutant r4_codeless_through_mapping turns both red. Only a CODED failure goes through text_routes._surface_comparison_failure. compare_from_urls produces exactly one coded failure: the W2-1c LLM_UNAVAILABLE result, which gives 503 {code, error} (pinned on POST/GET). The success/delivery path is untouched: the `if not result.get(\"success\")` guard is unchanged. W4-9's allowlist and text_routes were NOT edited. Flagged halves (ENABLE_PAID_ROUTE_METERING and ENABLE_CAMERA_FAILURE_ENVELOPE, both default OFF, read per call) were not touched this round. The six-state unit runs and the 0-mislabelled base classification still hold: the BASE PINs pass at db1ed3a4 and at HEAD with every flag unset.",
 "measurements_run": [
  "git status --short at start: empty at b8b2c983 (rebased: db1ed3a4 + c0120aa1/28b7dd74/b8b2c983)",
  "Read text_routes._surface_comparison_failure on the rebased tree: the W4-9 codeless allowlist _CODELESS_SAFE_MESSAGES = {_DEFAULT_FAILURE_MESSAGE, PRODUCT_PARSE_FAILURE_MESSAGE}; any other codeless message is redacted to 400 {INTERNAL_ERROR, INTERNAL_ERROR_FRIENDLY_MESSAGE}. This confirms defect 1 from the code",
  "W4-9 composition measured on the rebased tree: extraction_service.py:37 COMPARISON_GENERATION_ERROR = 'verdict generation unavailable' (non-empty), and generate_comparison's catch at :2559 returns {'winner_index': 0, 'error': COMPARISON_GENERATION_ERROR}. The get_client_raises tests drive that REAL catch and get 503 LLM_UNAVAILABLE with the constant message, no leak, and a refund under metering. This closes adversary item 3 as MEASURED",
  "unit files x 6 ambient flag states: 132 passed each",
  "9 mutants in the worktree plus a 5-mutant control against the HEAD test file (see mutation_checks)",
  "ruff --select E9,F63,F7,F82 --no-cache on the 5 unit files: All checks passed. Default-config ruff hits in url_routes.py/test_retro_w2_1.py (F401 HttpUrl, W293, B008, C408) are all on pre-existing lines, none on this round's diff",
  "py_compile of the 5 unit files: ok",
  "git diff --check: clean. CRLF preserved; git diff --stat HEAD shows only the intended lines (url_routes.py 26 lines, test_retro_w2_1.py 41 lines)",
  "comm gate 17 files: base db1ed3a4 vs HEAD, identical 4 failed / 393 passed",
  "label classification of test_retro_w2_1.py against base db1ed3a4: 111 nodes, 0 mislabelled"
 ],
 "pr_text": "## R-METER: W2-1 retro fixes (camera unsuccessful result, /url/compare verdict leak, anon refunds)\n\n### Defects fixed (from the W2-1 adversary review, each reproduced red-first)\n1. **W2-1b, camera: a failed comparison was billed as a delivery.** `compare_from_text` usually *returns* its failures instead of raising them. It returns `success: False` with TIMEOUT / INSUFFICIENT_DATA / LLM_UNAVAILABLE / CONTENT_UNAVAILABLE, or a code-less generic `error: str(e)`. `/image/identify` never read `success`. With `ENABLE_PAID_ROUTE_METERING` ON, a failed comparison kept the credit, bumped the lifetime counter, wrote a history row and logged a success. With every flag OFF, the generic branch also sent `str(e)` to the client; we measured `Incorrect API key provided: sk-proj-...` in the body (M13-26 class).\n2. **W2-1c, /url/compare: a live leak on the unflagged path.** When the verdict LLM failed, `generate_comparison` returned `{\"winner_index\": 0, \"error\": <text>}`. `compare_from_urls` served that as a 200 `success: true` with a made-up `winner_index: 0` and the error text. The shipped client took it to the Results screen as a real verdict.\n3. **W2-1d, anon.** `refund_anon_comparison_credit` had no callers, so every anonymous camera attempt that delivered nothing still burned the anon credit. `/url/compare` has no anonymous gate at all.\n\n### Fix\n- **Camera, UNFLAGGED.** When an unsuccessful result has no `code`, its `error` value becomes the constant `\"comparison unavailable\"`. No other key changes in any flag state. The scrub replaces an existing `error`; it never adds one (pinned).\n- **Camera, under `ENABLE_PAID_ROUTE_METERING`.** An unsuccessful result is non-delivery exit 7. It:\n  - refunds the authed credit (`usage_refund.image.comparison_unsuccessful`) and the anon credit\n  - skips `record_lifetime_comparison` and the history write\n  - logs `log_search(success=False, error_message=<code, or the constant>)`, never the free-text error\n- **Camera, new flag `ENABLE_CAMERA_FAILURE_ENVELOPE` (default OFF).** An unsuccessful result returns exit 6's existing `action: \"comparison_failed\"` envelope, which the client already falls back from. The envelope carries:\n  - the result's own `code`, or INTERNAL_ERROR when it has none\n  - the result's friendly `error`, or the constant for a code-less result\n  - `layer`, when present\n\n  Flag OFF keeps today's `action: \"comparison\"` body for the pre-OTA client (97b5f15).\n- **URL service, UNFLAGGED.** After the unpack, `compare_from_urls` checks `comparison.get(\"error\")`, i.e. whether the value is truthy, not merely whether the key is present.\n  - A truthy `error` returns `{success: False, code: \"LLM_UNAVAILABLE\", error: LLM_UNAVAILABLE_FRIENDLY_MESSAGE}`, never the error text or a winner.\n  - A verdict with a falsy `error` (null or \"\") is delivered exactly as before.\n  - `extraction_service` is not edited.\n- **URL route: composition with W4-9, UNFLAGGED.** Main now carries W4-9's codeless-message allowlist in `text_routes._surface_comparison_failure`. Only `Comparison failed` and the parse-failure message pass; any other code-less message is redacted to 400 INTERNAL_ERROR. So `/url/compare` splits its non-success exits:\n  - **A CODED failure** goes through the text route's mapping, which is what `/text/compare` uses. The only one `compare_from_urls` produces is the W2-1c LLM_UNAVAILABLE result, and it is sent as **HTTP 503** with `code` at the top level of the envelope. A returned CONTENT_UNAVAILABLE body is served as a non-delivery: refunded, no lifetime bump, no history. The route never produces that today, but it is pinned.\n  - **A CODE-LESS failure** (the <2-products exit) keeps main's exact statement, `raise HTTPException(status_code=400, detail=result.get(\"error\", \"Comparison failed\"))`. Its body stays byte-identical to origin/main: `400 {success:false, error:\"Could not extract both products\", code:\"BAD_REQUEST\"}`. It is pinned, and the pins pass at db1ed3a4.\n  - W4-9's allowlist is NOT widened, and `text_routes` is not edited.\n  - Mutation-checked both ways: routing the code-less exit through the mapping turns the two <2-products pins red, and dropping the coded branch turns the 503 and CONTENT_UNAVAILABLE pins red.\n- **Anon, under `ENABLE_PAID_ROUTE_METERING`.** `refund_anon_comparison_credit(fp, consumed_keys)` fires at most once per request on the six numbered camera exits plus exit 7. It returns the gate's own consumed_keys. It is armed only when the anon gate actually debited and metering is ON. Metering OFF keeps today's anon accounting.\n\n### Decided\n- **A failed URL verdict returns 503. This is DECIDED, not open.** The shared text-route mapping is the contract, and it sends LLM_UNAVAILABLE as 503 because a server-side outage is not a client error (W1-3). It is pinned on POST and GET.\n\n  On the shipped client (97b5f15), `parseApiError` turns the 503 into `TIMEOUT`, so the user sees the friendly `home.errors.timeout` copy and never the backend string.\n- **The W2-1c check tests truthiness, not presence** (Fable round 3). Two pins cover it:\n  - A full verdict carrying `error: null` or `error: \"\"` is DELIVERED as a 200 success, byte-identical to base.\n  - A full verdict carrying a NON-EMPTY `error` is 503 LLM_UNAVAILABLE with the constant message.\n- **The \"delivered as base\" pins build their expectation from a deep copy taken BEFORE the call** (round 4). They never compare against the verdict object the stub hands the code, and the stub's default verdict is itself a fresh deep copy. This makes an in-place edit of the verdict visible. The four in-place mutants that survived round 3 (pop / clear / null-out of a falsy `error`, a fabricated winner) are now killed, and so is an unconditional in-place key add on the success path. Run against the pre-round-4 test file, the same mutants survive; this was measured.\n- **W4-9 composition was measured, not assumed.** On main, `generate_comparison`'s catch stores the non-empty constant `COMPARISON_GENERATION_ERROR`, so the truthiness check fires. The tests that drive the REAL catch return 503 LLM_UNAVAILABLE with the constant message, no leak, and one refund under metering.\n- **Envelope ON with metering OFF keeps today's accounting. This is ACCEPTED as designed** and pinned by `test_pin_camera_envelope_on_meter_off_keeps_todays_accounting`.\n\n### Flag row (for CLAUDE.md)\n`ENABLE_CAMERA_FAILURE_ENVELOPE` defaults to OFF and is read per call in `image_routes.camera_failure_envelope_enabled` (strip/lower; true/1/yes/on).\n- **ON:** a `success: False` camera comparison returns `{success: false, action: \"comparison_failed\", error, code, request_id, products, vision_cost, message[, layer]}` instead of `action: \"comparison\"`.\n- **OFF:** today's body.\n- **Flip it TOGETHER with `ENABLE_PAID_ROUTE_METERING`.** That deploy state is pinned on the ledger: the reserved credit is refunded once, the anon credit is refunded once with the gate's keys, there is no lifetime bump, no history row, no anon metering, `log_search(success=False)`, and the full envelope body.\n\n### Activation gate\n- The unflagged halves go live on deploy: the camera `str(e)` scrub, the URL failed-verdict fix and the coded-failure URL mapping. The <2-products body does not change.\n- Flip `ENABLE_CAMERA_FAILURE_ENVELOPE` and `ENABLE_PAID_ROUTE_METERING` together, after checking that the shipped client renders `comparison_failed` for a TIMEOUT.\n- The adversary's other metering preconditions still stand: the size_or_count 500 window, the strict-auth coupling on /url/compare, and the 429 note on the admin probe.\n\n### Records (pinned, not built)\n- **/url/compare has NO anonymous gate.** With ENABLE_ANON_USAGE_GATE and metering both ON, a caller with a valid fingerprint and no auth gets 200 with zero anon checks. This is a follow-up for issue #128.\n- **The anon credit on /image/identify is debited BEFORE image validation.** The five pre-validation 400s and the moderation-exception re-raise do not refund it. These are follow-ups for #128.\n- **The refund latches need no pin** (ruling 5). The anon refund's `not device_fp` guard is redundant and is kept as a defensive check.\n\n### Verification (on the rebased tree, base = origin/main db1ed3a4)\n- **Unit files** (`tests/test_retro_w2_1.py` + `tests/test_paid_route_metering.py`): 132 passed in each of six ambient flag states (none; METER; ENVELOPE; ANON_GATE; METER+ENVELOPE; all three), on the pinned CI venv, with a process-wide non-loopback socket guard.\n- **Test labels** were checked against db1ed3a4 with 0 mislabelled across 111 nodes: 27 RED fail, 44 BASE PIN / RECORD pass, 40 GREEN PIN fail.\n- **Round-4 mutants: 9, with 8 killed.**\n  - Both url_routes branches are killed (see Fix).\n  - All four round-3 in-place survivors and the success-path in-place mutant are killed.\n  - The one survivor turns the code-less detail into a structured `{code: BAD_REQUEST, error}`, which the error handler lifts to the identical envelope. It is wire-equivalent.\n- **Earlier rounds:** 75 mutants, 67 killed. The 7 survivors are benign: two refund latches, the redundant device_fp guard, a log variant equivalent under the scrub, a server-side-only log, and two str-only truthiness variants.\n- **Comm gate** over the 17 test files that reach these modules, run sequentially with the socket guard: base db1ed3a4 and HEAD both gave 4 failed and 393 passed, with identical failed ids (the three #89 `TestIdentifyProductsMocked` nodes, plus `test_detect_allows_valid_url`, which needs DNS and fails under the guard on both sides). No new failures.\n\n### Follow-ups (adversary minors not addressed here)\n- size_or_count numeric -> AttributeError -> 500 with the credit kept (round-0 defect 4).\n- Unflagged `Depends(get_optional_user)` on /url/compare and its strict-auth coupling (defect 5).\n- The shared 429 bucket on the admin prices probe (defect 6).\n- The /text/parse bare-dict mock, M4 (defect 7).\n- The authed refunds drop `consumed_keys` (defect 8).\n- CLAUDE.md drift, plus the camera category and camera memory items (defect 9).\n- The /url/compare anon gate and the anon refunds on the validation 400s and moderation exception, for #128.\n- Add the new flag row to CLAUDE.md.\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)",
 "deviations_from_spec": [
  "Left unchanged on purpose: per ruling 1 the two <2-products pins had to go green UNCHANGED, so their bodies AND docstrings were not edited. As a result the docstring of test_pin_url_compare_extraction_failure_envelope_unchanged still says 'identity through the shared mapping', which is now stale: the code-less exit no longer goes through the mapping. Its assertion is correct and passes at db1ed3a4 and HEAD. The orchestrator can refresh that one docstring line when squashing.",
  "Beyond the literal ruling 2, _stub_url's default verdict is now copy.deepcopy(REAL_VERDICT) instead of the module constant. This keeps an in-place mutant from corrupting REAL_VERDICT for later tests. It is test-only and does not change any assertion's meaning; the success pins also take their own deep copy or literal before the call, as ruled.",
  "One mutant, r4_codeless_detail_default_changed, SURVIVED and is not fixed. It is wire-equivalent: the structured {code: BAD_REQUEST, error} detail produces an envelope byte-identical to the bare-string 400, and the full-envelope pin compares it.",
  "Edits were applied with small Python scripts that read and write bytes and preserve CRLF, not with the Edit tool (the session's bypass-mode instruction prefers Bash). git diff --check is clean, and the diff shows only the intended lines.",
  "Adversary item 3 (W4-9 interaction) is not a defect. It is closed as MEASURED on the rebased tree and stated that way in pr_text: COMPARISON_GENERATION_ERROR is non-empty, and the real-catch tests return 503 LLM_UNAVAILABLE."
 ],
 "git_status_final": "git status --short (verbatim):\n M app/api/url_routes.py\n M tests/test_retro_w2_1.py\n\nUncommitted, on top of HEAD b8b2c983; nothing committed. The base scratch worktree is removed. Final sha256 of every file written:\n- app/api/url_routes.py: f27ef446b79d88beef00bfe53561bcece115d6c94d60bdb17a511e0ec4493381\n- tests/test_retro_w2_1.py: 1d14c619fb67fdefd44d574a414122bacef13372353656bb4e70ba18b4df46e8\n\nUnchanged unit files (verified identical to HEAD):\n- app/services/url_extraction_service.py: d3e378fc2626e372d52f430ae3a8337f290e75373c41bbb733eff8ebe738f08e\n- app/api/image_routes.py: b2aed119acdfdb052d05d088917e7e415dbaab5d76a66e4bdc8b454f7ccea2f2\n- tests/test_paid_route_metering.py: 9e7fd283e1a23531dc8535517123b9668251482a308ae9c09498ed1c740433c0\n\nPre-edit snapshots (HEAD bytes):\n- url_routes.py: 1dddb57ab51224afd53b475cb1a852ca43b44a62c5d396623f1a2b97abacde72\n- test_retro_w2_1.py: 33294c05f0003fbe23892e1d96d70debe521f76adb6a8a9fa6e825ba0f2e8b0a\n\nScratch files are under scratchpad/rmeter_r4 (driver4.py, fix_url.py, edit_tests.py, unit.sh, comm.sh, logs, guard/sockguard.py); none of them is a .env.",
 "residual_risk": "- **Status-code choice on /url/compare.** The code-less exit relies on the fact that compare_from_urls has exactly one code-less failure producer, the '<2 products' constant. A future code-less producer carrying free text would go out verbatim as a bare 400. That is main's behaviour today, not a regression, but it bypasses W4-9's redaction floor, unlike /text/compare. A follow-up could redact code-less URL failures once the '<2 products' message is added to an allowlist. That needs a ruling because it touches text_routes.\n- **Label docstrings still say base 1c6f6796.** The classification was re-measured against db1ed3a4 with 0 mislabelled, so the labels hold; only the SHA in the prose is stale.\n- **The #89 camera_vision failures and the DNS-dependent test_detect_allows_valid_url** remain red on both base and head, and they are not in scope.\n- **No adversary has seen these exact bytes yet.** Per the session rule, nothing merges before an adversary pass on this round."
}