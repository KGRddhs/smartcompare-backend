{
 "unit": "R-W0 FIXER (W0-1b + W0-2b retro-fix of PR #135, adversary round 2) in worktree C:/Users/SynAckITPC/Documents/AI/sc-r-w0, branch retro/w0-1-w0-2. HEAD da16bada (wip over base 1c6f6796). The worktree was confirmed clean at start (git status --short was empty). All edits are uncommitted on top of the wip commit.",
 "files_changed": [
  "app/utils/url_validator.py",
  "tests/test_retro_w0_1.py",
  "tests/test_retro_w0_2.py"
 ],
 "all_unit_tests_pass": true,
 "test_evidence": "Every run used the pinned venv through the guarded runner .qa-retro/run_pytest.py with -q -p no:cacheprovider -p no:randomly --timeout=120 -m \"not (live_unit or live_db or integration)\" and PYTHONIOENCODING=utf-8. The wrapper script is scratchpad/rw0_fix/run.sh.\n\nUnit set (scratchpad/rw0_fix/unit_set.txt): tests/test_retro_w0_1.py, test_retro_w0_2.py, test_url_validator_offloop.py, test_url_validator_offloop_hops.py, test_supabase_client_reuse.py, test_ssrf_redirect_hardening.py.\n\nRe-deriving the defects before the fix:\n- The 12 new pins were run against the unfixed wip bytes (new_pins_prefix.txt): \"1 failed, 11 passed\". The only red was test_pin_zombie_empty_answer_keeps_the_negative, with \"an EMPTY zombie answer dropped the timeout negative: memo=None\". That reproduces defect 1 for the right reason.\n- Defect 2 was reproduced with a temporary probe test (created, run, then deleted; probe_d2.txt). Output: \"PROBE False False False None\". Call 1 was slow and public, call 2 was private and wrote memo False, and call 1's zombie then popped that private negative.\n\nAfter the fix:\n- Flags unset: \"71 passed, 21 warnings in 46.71s\" (59 existing + 12 new). Net guard log: 0 lines.\n- Both flags set in the process env: \"1 failed, 70 passed\". The failure is tests/test_ssrf_redirect_hardening.py::test_fetch_page_follows_valid_redirect, which the green already showed fails the same way at BASE bytes with the flag in the env. Its 3 guarded lookups match the green's and the base's net logs exactly (same sorted-unique sha256, 530d4399...).\n- ENABLE_OFFLOOP_DNS_RESOLVE only: the same 1 failed / 70 passed, same test.\n- ENABLE_SUPABASE_CLIENT_REUSE only: 71 passed, net log 0.\n\nLint: ruff --select E9,F63,F7,F82 --no-cache passes on the touched files and repo-wide. F401,F841 on the touched files passes. py_compile passes on all 6 unit files.",
 "lint_clean": true,
 "mutation_checks": [
  "Driver: scratchpad/rw0_fix/mut.py. Before each mutant it asserts the file equals the byte snapshot (scratchpad/rw0_fix/snap_fix/*.orig). It applies an exact byte replacement using the file's own EOL, runs the full 71-node unit set through the guarded runner with both flags popped, restores from the snapshot, and asserts the sha256. After all 19 mutants the working files match snap_fix/sha.txt exactly (SHAS_MATCH_SNAPSHOT). Net guard logs for every mutant run total 0 lines. Log: scratchpad/rw0_fix/mutations.jsonl. Result: 17 of 19 KILLED; the 2 survivors are equivalent mutants, explained below.",
  "D1 (the defect-1 fix reverted to `if not _addr_infos_allowed(addr_infos, hostname)`) -> KILLED by test_pin_zombie_empty_answer_keeps_the_negative.",
  "N1 (_TIMEOUT_MEMO_RUN_FRACTION = 0.5) -> KILLED by test_pin_run_fraction_is_three_quarters_measured_at_the_deadline[0.60T] and [just-under-0.75T]. It survived the green.",
  "N2 (0.95) -> KILLED by [0.85T] and [exactly-0.75T]. It survived the green.",
  "N15 ('>=' changed to '>') -> KILLED by [exactly-0.75T]. It survived the green.",
  "N3 (ran measured at the except branch as time.monotonic() - start) -> KILLED by [0.60T] and [just-under-0.75T]: the split clock makes the except branch read 10 T late. It survived the green.",
  "N6 (fut.cancelled() guard removed) -> KILLED by test_pin_callback_treats_a_cancelled_resolve_as_a_normal_outcome. That test uses caplog with a positive control; concurrent.futures.CancelledError is an Exception subclass on 3.12, measured. It survived the green.",
  "N5 (EAI_NODATA dropped from the confirmed set) -> KILLED by test_pin_each_confirmed_code_is_memoised_on_its_own[EAI_NODATA] on Windows. EAI_NODATA is re-pointed to a distinct value at call time. It survived the green.",
  "N14 (EAI_NONAME dropped) -> KILLED by test_pin_each_confirmed_code_is_memoised_on_its_own[EAI_NONAME] and test_pin_platform_without_eai_nodata_still_memoises_noname. It survived the green.",
  "N13 (the sync validator's pre-existing private-address memo write removed, 1st occurrence) -> KILLED by test_pin_sync_validator_memoises_a_private_address_under_the_flag. It survived the green.",
  "N10 (max_keepalive_connections=0) -> KILLED by test_pin_shared_transport_keeps_http11_connections_alive (5 sequential requests must use 1 server-side connection) and test_pin_shared_transport_pool_limits_are_the_base_values. It survived the green.",
  "N11 (max_connections=40) -> KILLED by test_pin_shared_transport_pool_limits_are_the_base_values. It survived the green.",
  "N7 (the callback's early `entry[1] is not False` check removed) -> SURVIVED, and it is EQUIVALENT BY CONSTRUCTION. The pop site re-checks `current[1] is False` under _DNS_MEMO_LOCK before popping, so the mutant can only skip an early exit; it can never pop a non-False entry, even if a True were planted. The early check only saves an _addr_infos_allowed call.",
  "N9 (explicit http1=True added) -> SURVIVED, EQUIVALENT: http1=True is httpx.Client's default. test_red_shared_transport_is_built_http1_only already asserts pool._http1 is True.",
  "Re-runs of earlier kills to confirm nothing regressed: H2BACK (http2=True) -> KILLED (6 red, including the new keep-alive pin). C1 (no done-callback) -> KILLED (2). C5 (callback attached with the flag OFF) -> KILLED by test_pin_flag_off_direct_async_call_never_drops_a_memo_entry. G1 (async gaierror gate removed) -> KILLED (3). M13 (flag-OFF get_auth_client on _build_client) -> KILLED (2). M14 (get_user_supabase_client always on shared options) -> KILLED (2)."
 ],
 "comm_gate": "HEAD comm gate over the recorded 35-file set. Batch A is .qa-retro/comm_set_A.txt (34 files) and batch B is tests/test_conftest_env_safety.py with --deselect tests/test_conftest_env_safety.py::test_app_main_still_imports_under_the_sentinels. Both ran through the SAME guarded runner .qa-retro/run_pytest.py with both flags unset.\n- Batch A: \"3 failed, 809 passed, 21 deselected, 21 warnings in 64.16s (0:01:04)\". The 3 failures are the example.com SSRF nodes: test_security_hardening.py::TestSSRFProtection::test_valid_external_url_passes, ::test_valid_http_url_passes and TestSecurityIntegration::test_ssrf_protection_integrated.\n- Batch B: \"24 passed, 3 deselected, 2 warnings in 17.79s\".\n- comm -13 against .qa-retro/base_failed_ids.txt (the 4 recorded base failures; the 5th accepted id is the deselected node) is EMPTY. No new failure.\n- comm -23 shows only test_conftest_env_safety.py::test_live_opt_in_restores_the_environment_end_to_end. It was an IO-load timeout at base and passes at head, as in the green's run.\n- Net guard logs:\n  - Batch A: 106 lines. Its sorted-unique set is byte-identical (hash d8531c15...) to the green's recorded head log. Against the base log it differs only by a line fragment split by a concurrent append.\n  - Batch B: 1 line, identical to the green's head B log. No non-loopback connection left the box, and every hostname was masked in my output.\n- The base side is the red phase's recorded run (comm_base_A2/B2, detached scratch worktree of 1c6f6796). I created NO scratch worktree.\n- Outputs: scratchpad/rw0_fix/comm_head_A.txt, comm_head_B.txt, head_failed.txt, base_failed.txt.",
 "flag_identity": "Both flags stay default OFF. The fixer changed only code that runs under ENABLE_OFFLOOP_DNS_RESOLVE ON.\n\nThe one code change is inside _clear_negative_if_resolved_public (the done-callback). It is attached only `if memoise` (the per-call flag read) and is re-invoked only inside the flag-ON `if memoise:` memo-write branch, so flag OFF never executes it.\n- Pinned by test_pin_flag_off_direct_async_call_never_drops_a_memo_entry; mutation C5, which attaches the callback with the flag OFF, is KILLED.\n- The other flag-OFF pins are still green in the 71-node run: test_pin_flag_off_gaierror_path_is_base[*], test_pin_flag_off_slow_success_is_base, test_pin_flag_off_never_touches_pool_or_memo and test_flag_off_sync_path_unchanged.\n- The flag-OFF sync validator and the helper are byte-unchanged by this fixer: the diff touches only the callback's docstring and 2 lines of its body.\n\nENABLE_SUPABASE_CLIENT_REUSE: database_service.py is byte-identical to the wip commit (sha256 01663fe9... unchanged). The fixer added pins only.\n- The OFF construction pins test_pin_flag_off_anon_client_is_the_bare_base_construction, test_pin_flag_off_user_client_is_the_bare_base_construction, test_pin_flag_off_after_on_rolls_anon_and_user_clients_back and test_flag_off_construction_unchanged were re-run as mutations M13 and M14; each is KILLED with 2 red.\n\nNew pins: all flag-ON except the cancelled-callback pin, which drives the helper directly.\n- They add no production behaviour.\n- The run-fraction pin monkeypatches uv.time (the module reference only, restored by monkeypatch). The asyncio loop's own clock is untouched.\n- The EAI_NODATA pins monkeypatch socket.EAI_NODATA via monkeypatch, restored after each test.",
 "measurements_run": [
  "Re-derivation of defect 1: the new pin ran red on the unfixed wip bytes (1 failed / 11 passed). Memo after an empty-list zombie: None, where it should be (t, False).",
  "Re-derivation of defect 2: a temporary probe on the wip bytes (flag ON, 0.3 s bound, 0.8 s slow public call 1, private call 2) printed 'PROBE False False False None'. Both calls fail closed, call 2 wrote a False memo, and call 1's zombie popped it. The probe file was deleted after the run.",
  "concurrent.futures.CancelledError MRO on the pinned venv (3.12): (CancelledError, Error, Exception, BaseException, object). So the cancelled() guard is observable only as a DEBUG 'done-callback failed' record, and that record is now pinned.",
  "Unit set after the fix: 71 passed with flags unset (46.71s, net log 0). 70 passed / 1 pre-existing env-dependent failure with DNS ON, and the same with both ON. 71 passed with REUSE ON only.",
  "Mutation matrix: 19 mutants, 17 KILLED, 2 equivalent survivors (N7, N9). Every restore was sha256-verified.",
  "HEAD comm gate: A 3 failed / 809 passed / 21 deselected (64.16s); B 24 passed / 3 deselected (17.79s). comm -13 is EMPTY.",
  "ruff E9,F63,F7,F82 (touched files and repo-wide) plus F401/F841 (touched files): clean. py_compile: OK."
 ],
 "pr_text": "R-W0 retro-fix of PR #135 (W0-1b + W0-2b): the off-loop DNS memo stops blacklisting healthy hosts, and the shared Supabase transport becomes HTTP/1.1\n\nBoth flags stay default OFF: ENABLE_OFFLOOP_DNS_RESOLVE and ENABLE_SUPABASE_CLIENT_REUSE. Nothing is flipped by this PR, and with the flags OFF behaviour is byte-identical (pins listed below).\n\n## Defect 1 (major, W0-2b): the shared HTTP/2 transport couples every Supabase call\nUnder ENABLE_SUPABASE_CLIENT_REUSE, every Supabase client shared ONE httpx.Client(http2=True). On a single h2 connection, httpcore 1.0.9 couples all in-flight calls:\n- A hung PostgREST stream's read timeout tears the connection down and fails every unrelated request on it.\n- Even with NO hung call, about 4% of concurrent requests failed. httpcore allocates h2 stream ids outside its write lock (`_sync/http2.py:134` get_next_available_stream_id vs `:144` _send_request_headers), so out-of-order stream ids reach the server, which must kill the connection (RFC 9113 5.1.1).\n- The same server with an http2=False client lost 0 of 3,989 requests.\n\n**Fix:** the shared transport is built with `http2=False`: an HTTP/1.1 keep-alive pool with one request per connection. The CR-PERFORMANCE-03 win (one SSLContext/certifi setup) is kept. The read timeout (the SUPABASE_POSTGREST_TIMEOUT_SECONDS knob, default 8 s) and connect timeout (3.0 s) are unchanged. The pool limits stay 100 / 20.\n\n**Pins (tests/test_retro_w0_2.py):**\n- pool._http2 is False.\n- ALPN against an h2-capable loopback TLS server negotiates HTTP/1.1.\n- A hung call stays bounded at knob + 0.5 s under 4 threads of traffic, and no unrelated request fails.\n- No-hung-call concurrency: 0 failures.\n- A fast request issued during a hung call succeeds.\n- max_connections >= the run_db width of 40.\n- The pool limits are exactly the base 100 / 20.\n- Sequential calls reuse ONE keep-alive TLS connection.\n\n## Defect 5 (minor): flag-OFF anon and user construction was unpinned\nMutations M13 and M14 survived the original unit file. New pins cover flag-OFF get_auth_client() and get_user_supabase_client() (exact ((URL, ANON), {}) call, options None, postgrest 120/120, gotrue 5.0, no shared transport built) and an ON -> OFF rollback. M13 and M14 each turn 2 pins red.\n\n## Defect 2 (major, W0-1b): the 60 s negative memo blacklisted healthy hosts\nThree rules close it, all under the per-call flag read:\n1. **EAI_NONAME-only memo.** A gaierror is memoised ONLY when exc.errno is in {socket.EAI_NONAME, socket.EAI_NODATA}, read from the platform constants at call time. EAI_AGAIN, EAI_FAIL, a bare gaierror with errno None, and every other code fail closed for THAT call and write nothing. This applies to both the sync and the async validator.\n2. **0.75 x timeout rule.** `_resolve` records time.monotonic() when a pool worker starts it. A timeout is memoised only when the job started AND had run at least 0.75 x DNS_RESOLVE_TIMEOUT_SECONDS by the caller's DEADLINE. The deadline is precomputed at submit, so a lagging event loop cannot inflate the run time. A job that never started, or started just before the deadline, writes nothing. A resolve that runs the full bound is still memoised (the P0's black-hole win).\n3. **Done-callback pop.** A flag-ON resolve is submitted to the dedicated pool directly (submit + wrap_future, which is what run_in_executor does internally) and gets a done-callback. When a zombie resolve later returns a NON-EMPTY, ALLOWED public answer, any False memo for that host is popped, so the next call re-resolves.\n   - It NEVER writes True. test_positive_result_is_not_memoized is the DNS-rebinding contract.\n   - A private or EMPTY zombie answer keeps the negative.\n   - A late re-check right after the memo write covers the race where the zombie returns between the deadline and that write.\n\n**Pins (tests/test_retro_w0_1.py), all using platform constants:**\n- Transient-gaierror reds (EAI_AGAIN, EAI_FAIL, errno None; async and sync).\n- A confirmed NXDOMAIN or private address is still memoised, now pinned on the SYNC path too.\n- Each confirmed code is memoised on its own. EAI_NODATA is re-pointed to a distinct value, so the pin works on Windows, where EAI_NODATA == EAI_NONAME == 11001.\n- A platform without EAI_NODATA still memoises EAI_NONAME.\n- A full-bound timeout is still memoised.\n- Start-near-deadline, slow-ok and late-start-healthy reds.\n- The exact 0.75 fraction, pinned with a per-thread split clock: exactly 0.75 T memoises, just under 0.75 T, 0.60 T and a loop that runs the except branch 10 T late do not, and 0.85 T does.\n- The zombie clears but never writes True; a private zombie keeps the negative; an empty zombie keeps the negative; the late re-check holds.\n- A cancelled resolve is a normal outcome, not a logged callback failure.\n- Flag-OFF direct async never touches the memo.\n\n**Windows vs Linux errno:** EAI_NONAME is -2 on Linux (CI, prod) and 11001 on Windows. Four existing stubs that hard-coded gaierror(-2) now raise socket.EAI_NONAME: tests/test_url_validator_offloop.py:72 and :333, tests/test_url_validator_offloop_hops.py:82 and :410. That is the only edit to existing tests.\n\n## Adversary round 2 (fixer)\n- **Fixed, minor: an empty zombie answer dropped a genuine timeout negative.** `_addr_infos_allowed([])` is vacuously True (a pre-existing gap). The callback now requires a non-empty answer before it drops anything. The change is 2 lines inside the flag-ON-only callback. The new pin was red before the fix and green after; reverting the fix (mutant D1) turns it red.\n- **Recorded, NOT changed: the callback pops ANY False memo for the host,** including a private-address negative that a concurrent call wrote. Reproduced: both calls still return False, and the next call re-resolves and blocks a private answer again, exactly as flag OFF does, so this is not a security regression. It follows ruling 3 (\"pop any False memo\") literally. The cost is that a rebinding-style host can keep clearing its own negative, so the memo saves less for it. Tagging memo entries by reason would change the memo shape and is left to the orchestrator.\n- **The 7 prove-nothing rows are folded in:**\n  - N1, N2, N15 and N3 are killed by the split-clock pin.\n  - N6 is killed by the cancelled-callback pin.\n  - N5 and N14 are killed by the per-code pins.\n  - N10 and N11 are killed by the keep-alive and base-limit pins.\n  - N13 is killed by the sync private-address pin.\n  - N7 and N9 are equivalent mutants: the pop site re-checks under the lock, and http1=True is httpx's default.\n\n## Flag rows (no new flags)\n- **ENABLE_OFFLOOP_DNS_RESOLVE:** memo semantics are confirmed-NXDOMAIN or private-address negatives, plus timeouts whose resolve ran at least 0.75 of the bound by the deadline. A zombie that later resolves to a non-empty public answer drops its negative.\n- **ENABLE_SUPABASE_CLIENT_REUSE:** the shared transport is an HTTP/1.1 keep-alive pool (100/20).\n\nThe CLAUDE.md W0 flag rows still describe `http2=True` and the started-Event memo rule, and should be updated at merge time.\n\n## Flag-OFF byte-identity\nPins, each re-run as a mutation and killed:\n- test_pin_flag_off_gaierror_path_is_base\n- test_pin_flag_off_slow_success_is_base\n- test_pin_flag_off_never_touches_pool_or_memo\n- test_pin_flag_off_direct_async_call_never_drops_a_memo_entry (C5)\n- test_flag_off_sync_path_unchanged\n- test_pin_flag_off_anon_client_is_the_bare_base_construction (M13)\n- test_pin_flag_off_user_client_is_the_bare_base_construction (M14)\n- test_pin_flag_off_after_on_rolls_anon_and_user_clients_back\n- test_flag_off_construction_unchanged\n\n## Gates\n- Unit set: 71 passed with the flags unset.\n- Fixer mutation matrix: 19 mutants, 17 killed, 2 equivalent. Each was restored from a byte snapshot with a sha256 check.\n- Comm gate over the recorded 35-file set with the guarded runner: batch A 3 failed / 809 passed / 21 deselected, batch B 24 passed / 3 deselected. comm -13 against the recorded base failures is EMPTY; the 3 failures are the network-dependent example.com SSRF nodes that fail identically at base.\n- ruff (E9, F63, F7, F82) and py_compile are clean.\n\n## Activation gate\n- **ENABLE_OFFLOOP_DNS_RESOLVE** may be canaried as W0 activation step 1 once this lands. Until W0-1c ships, confirm the /url/* limiter caps per-client concurrency at 16 or fewer.\n- **ENABLE_SUPABASE_CLIENT_REUSE** is flippable after this lands, subject to the non-finite-knob follow-up and the W3-2 ordering.\n\n## Honest limits\n- HTTP/1.1 opens one TLS connection per CONCURRENTLY busy request, up to 100, with 20 kept alive. Keep-alive reuse is pinned on loopback; handshake cost against real Supabase was NOT measured.\n- The late-start healthy-host shape is closed by both the 0.75 gate and the callback. Each has its own killing pin.\n- A resolve that runs 0.75-1.0 of the bound and then fails with EAI_AGAIN stays memoised for 60 s, by design.\n- The callback pop can also remove a concurrent private-address negative (see above).\n- tests/test_ssrf_redirect_hardening.py::test_fetch_page_follows_valid_redirect fails whenever ENABLE_OFFLOOP_DNS_RESOLVE is set in the process env, at base and head alike.\n\n## Follow-ups (out of scope under the binding rulings)\n1. **W0-1c single-flight (reviewer defect 3):** 16 concurrent validations of one black-holed host fill the 16-worker pool. Needs in-flight coalescing per hostname.\n2. **Non-finite SUPABASE_POSTGREST_TIMEOUT_SECONDS (reviewer defect 4):** inf, nan and 1e309 make every request on the transport raise. Add a math.isfinite check and correct the CLAUDE.md sweep line.\n3. **Optional:** reason-tagged memo entries, so the zombie callback drops only timeout negatives (round-2 defect 2).\n4. **Pre-existing validator gaps**, identical with the flag on or off: 100.64/10, multicast, and an empty addr list are allowed.\n5. Update the CLAUDE.md W0 flag rows at merge.\n\n## Red-phase harness incident (for Ahmed)\nThe red phase's first two base comm-gate attempts used a runner that imported app.* BEFORE tests/conftest.py ran neutralize_credentials(). load_dotenv(override=True) at import then built the module-level OpenAI and Upstash clients with the REAL .env credentials, and those runs had no process-wide network guard. Real OpenAI, Serper or Upstash calls cannot be ruled out. The keys are already on Ahmed's rotation list. Every result in this PR comes from the corrected runner (third-party pre-import only, process-wide non-loopback guard).\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)",
 "deviations_from_spec": [
  "Round-2 defect 2 (the callback pops ANY False memo, including a concurrent private-address negative) is NOT fixed. I re-derived it from code and reproduced it: the probe printed 'PROBE False False False None'. Ruling 3 says to 'pop any False memo', and the adversary itself classes this as not a defect against the rulings and not a security regression: the next call re-resolves and blocks a private answer again, as flag OFF does. A fix would need reason-tagged memo entries, which changes the memo shape beyond the scoped design. Left to the orchestrator and listed as follow-up 3 in pr_text.",
  "Round-2 defect 1 is fixed minimally inside the callback (`if not addr_infos or not _addr_infos_allowed(...)`). The pre-existing validator-level empty-list gap (an empty getaddrinfo answer validates True in both validators, flag on or off) is untouched; it is out of scope per the reviewer.",
  "Two tests_that_prove_nothing rows are explained rather than pinned, because they are equivalent mutants. N7: the pop site re-checks `current[1] is False` under the lock. N9: http1=True is httpx's default and is already asserted by test_red_shared_transport_is_built_http1_only.",
  "N11: a new pin fixes the pool limits at exactly the base 100/20. This is stricter than ruling 4's '>= 40' and matches the PR body's 'max_connections stays 100'. If the orchestrator wants 40 to be allowed, drop the max_connections assertion in test_pin_shared_transport_pool_limits_are_the_base_values. The keep-alive assertion there, and the separate behavioural keep-alive pin, still kill N10.",
  "The run-fraction pin monkeypatches the module reference uv.time with a deterministic per-thread clock (_SplitClock), so 0.75 T is hit exactly. The event loop's own clock and the real wait_for timeout are untouched. The alternative, real-time queueing, would have needed ~0.2 s margins and could not have pinned '>=' against '>'.",
  "The N6 pin asserts on the absence of a DEBUG 'done-callback failed' record, with a positive control. That record is the guard's only observable effect, because the callback swallows exceptions by design.",
  "Edits used the Edit tool. url_validator.py stays pure CRLF (537 CRLF, 0 bare LF). The two retro test files stay pure LF, as committed in the wip.",
  "A temporary probe file, tests/test_zz_rw0fix_probe.py, was created to reproduce defect 2 and deleted right after its run. It is not in git status."
 ],
 "git_status_final": " M app/utils/url_validator.py\n M tests/test_retro_w0_1.py\n M tests/test_retro_w0_2.py",
 "residual_risk": "- **Callback pops a private negative:** the zombie callback can still pop a concurrent PRIVATE-address negative (round-2 defect 2). This follows the ruling and is not a security regression, but a rebinding-style host can erode its own memo.\n- **Empty-answer gap in the validators:** a pre-existing gap outside the callback, the same with the flag on or off.\n- **Carried from the green:**\n  - HTTP/1.1 handshake cost against real Supabase is unmeasured. Keep-alive reuse is now pinned, on loopback only.\n  - W0-1c single-flight is still open.\n  - The non-finite postgrest knob is still open.\n  - The CLAUDE.md W0 rows are stale until merge.\n  - test_ssrf_redirect_hardening::test_fetch_page_follows_valid_redirect is env-dependent.\n- **Windows EAI_NODATA pin:** on Windows the per-code pin relies on re-pointing socket.EAI_NODATA. Real Linux constants (-2/-5) are exercised only in CI.\n- **Scratch artifacts** (outside the worktree) are in scratchpad/rw0_fix/: run.sh, mut.py, mutations.jsonl, snap_wip/ and snap_fix/ byte snapshots, the unit/comm outputs and net logs.\n- **Final sha256 of the files I wrote:**\n  - app/utils/url_validator.py 595f6e58b97b888fca47ad5f94b146d108424b9072be50cdb037e0dffbda056d\n  - tests/test_retro_w0_1.py 7c320d454976de0f14500970dabf4c06f33f01422b09d91e08dd85fde1385d45\n  - tests/test_retro_w0_2.py 0024f6d40fc8f3778006e86ddf9e96d311d8443a07c225650dfc790b565b6051\n- **Unchanged files, for reference:** app/services/database_service.py 01663fe905759260b00c855f4cc0f6368363aaa22d7739f5016c5b3a1b7f8f98 and app/services/auth_service.py 318b9e133b696c1771145930a86a8276f736a3f8c067009f93c51287bda9d17f."
}