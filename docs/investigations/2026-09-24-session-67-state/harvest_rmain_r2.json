{
 "fix:R-MAIN-r2": {
  "unit": "R-MAIN fix round 2 (W1-1b, W1-1c, W1-9c, W1-7b/W1-10b), worktree C:/Users/SynAckITPC/Documents/AI/sc-r-main, branch retro/w1-1-w1-7-w1-9-main, on top of wip 967d7fba (base 1c6f6796)",
  "files_changed": [
   "tests/test_retro_w1_1.py (uncommitted on top of 967d7fba). Final sha256 00d9121924aa32c1d5065863c95813c4753fb8f35ecbdd142635c097f1ab7aff. The module-level `from app.services.sentry_service import _before_send` is gone. `_sentry_module()` / `_before_send()` now resolve through the module object at call time; `_captured_init_kwargs` and the identity assert (`kwargs['before_send'] is _sentry_module()._before_send`) use them. New C5 pin: TestW11cMountPins::test_serving_error_on_authenticated_basic_request_is_not_a_401, parametrized over ValueError and UnicodeError.",
   "tests/test_retro_w1_9.py (uncommitted). Final sha256 bc1870624c478adcfc77dd94c67e3f0861c4916f586dc38a61322a9cd346f178. test_decorated_route_is_exempt_from_the_default_and_keeps_its_contract is rewritten and renamed test_router_decorated_route_is_unreachable_by_the_default_and_keeps_its_contract. New class TestW19cStatedLimitSingleChunk with 3 tests (10 nodes) plus the helpers _asgi_messages, _default_limiter_reachable_paths and _messages_into_slowapi.",
   "tests/test_retro_w1_7_w1_10.py (uncommitted). Final sha256 4e3894c3b8ed5dadb4cbba98bff5f5a8ea8fb1f0160987482ce528e2056179e4. New pin TestW110bWindowedMax::test_windowed_max_is_computed_by_the_recorder_not_by_health.",
   "UNCHANGED vs 967d7fba (sha256 for reference): app/main.py fd091ace0d01b4b88cd92a2952790b94a35d0dbc2c10608532a3d00651153008; app/services/sentry_service.py 50ea048c63bb11292378d45ac78387ca272f25a6184ecd06dbd87a80e257b469; tests/test_health_loop_lag.py 7ae7323373dcbe2ac9683531d80fc9905a7c9122fde58017e0cc3492b017de08 (byte-identical to base, per ruling 5). No product file was edited this round.",
   "Scratch only, under scratchpad/rmain_fix2/: run.sh, run_in.sh, mutate.py, rmain_netguard.py, gen_specs.py, patch_w19.py, specs2.json, mutation_matrix.jsonl, mut_*.txt, snap/ and snap_pre_mut/ byte snapshots, start_shas.txt, pre_mutation_shas.txt, order_before/after.txt, unit_*.txt, comm_*.txt, base/head_failed.txt, ci_order_files.txt, comm_files_head.txt. For the base comm run I copied .env into the existing base_tree (scratchpad/rmain_fix/base_tree, a git-archive extraction of 1c6f6796) and deleted it straight afterwards; I confirmed it is absent."
  ],
  "all_unit_tests_pass": true,
  "test_evidence": "Every run used the pinned venv (.venv-qaren: fastapi 0.141.1 / starlette 1.6.0 / slowapi 0.1.10) with the process-wide rmain_netguard plugin, PYTHONIOENCODING=utf-8, -q -p no:cacheprovider -p no:randomly --timeout=120, and -m \"not (live_unit or live_db or integration)\".\n\n(1) BLOCKING test-order pin, CI alphabetical order, one process: `tests/test_observability.py tests/test_retro_w1_1.py`.\n- BEFORE the fix (start bytes 535918a7...): 1 failed / 60 passed. The failure is TestW11bTransactionHookIsWired::test_other_init_options_are_unchanged, at line 217 (order_before.txt).\n- AFTER: 63 passed / 0 failed (order_after.txt). That is the 61 prior nodes plus the 2 new C5 nodes.\n\n(2) The 9-file unit set plus test_observability.py plus test_backend_cleanup.py (which reloads app.main), sorted alphabetically in one process: 243 passed / 0 failed (unit_ci_order_off.txt).\n\n(3) The 9-file unit set plus test_observability.py, ENABLE_DEFAULT_RATE_LIMITS unset: 225 passed / 0 failed (unit_off.txt).\n\n(4) Same set with ENABLE_DEFAULT_RATE_LIMITS=true process-wide: 223 passed / 2 failed. Both failures are expected, because each asserts the flag-OFF state of the app imported at module load:\n- test_retro_w1_9::TestW19cFlagOffIdentity::test_imported_app_has_no_rate_limit_middleware\n- test_m13_01::test_default_rate_limits_gated_off_by_default (this one also fails at base 1c6f6796 with the flag ON; recorded last round)\nThe flag-ON contract and stated-limit tests use monkeypatch plus a fresh exec, and they pass in every state.\n\n(5) The three edited files alone: 88 passed / 0 failed.\n\nNetguard: 2 blocked getaddrinfo attempts in every run, both in the pre-existing test_m13_01 lockout tests, which still pass. The new and edited files made zero attempts.",
  "lint_clean": true,
  "mutation_checks": [
   "Method: byte-snapshot every touched file first (snap_pre_mut/ plus pre_mutation_shas.txt), apply one single-occurrence mutation, run the sorted 11-file CI-order set in a guarded run, restore from the snapshot bytes, then sha256-verify. restored_sha_ok=true for all 9 mutants. `sha256sum -c pre_mutation_shas.txt` passes on all 6 files afterwards (main.py, sentry_service.py, feedback_routes.py and the 3 test files).",
   "C5_serving_inside_try: partition, compare_digest and super().__call__ moved back inside the try with `except (ValueError, UnicodeError): pass`. KILLED, 2 failed, both by the new test_serving_error_on_authenticated_basic_request_is_not_a_401[ValueError, UnicodeError]. This mutant survived 188/188 last round.",
   "W_snap_ringmax: loop_lag_snapshot returns max(_loop_lag_ring, default=0.0). KILLED, 1 failed, by the new test_windowed_max_is_computed_by_the_recorder_not_by_health. This mutant survived 188/188 last round.",
   "FB_decorator_removed: @limiter.limit('30/minute') dropped from POST /api/v1/feedback. KILLED, 1 failed, by the rewritten test_router_decorated_route_is_unreachable_by_the_default_and_keeps_its_contract. The decorated-contract half is load-bearing, and no default limit steps in because the route is unreachable.",
   "ST_new_undecorated_stream_route: an undecorated app-level 2-chunk StreamingResponse route added to main.py. KILLED, 2 failed: test_on_railway_the_default_limiter_reaches_only_three_app_level_routes and test_off_railway_only_the_docs_routes_are_added.",
   "ST_health_multichunk: /health returns a 2-chunk StreamingResponse. KILLED, 4 failed: the new single-chunk pin [/health] plus 3 existing flag-ON /health contract tests. This confirms the stated limit is real on our own app with the flag ON.",
   "R1_revert_to_SlowAPIMiddleware (regression re-check): KILLED, 11 failed.",
   "C1_except_narrow to (UnicodeDecodeError,) (regression re-check): KILLED, 4 failed.",
   "W_ring59, maxlen=LOOP_LAG_WINDOW_SLOTS - 1 (regression re-check): KILLED, 1 failed (test_window_is_exactly_sixty_ticks).",
   "S1_unwire_tx_hook, before_send_transaction removed (regression re-check): KILLED, 13 failed.",
   "Test-order defect as a mutant: the start-bytes test file (module-level `from app.services.sentry_service import _before_send`) gives 1 failed in the CI-order pair. The fixed file gives 0 failed."
  ],
  "comm_gate": "PASS. branch-only-NEW is empty and base-only is empty: the FAILED sets are identical.\n\n- Base: a git-archive extraction of 1c6f6796 (scratchpad/rmain_fix/base_tree). It is byte-faithful: main.py, conftest.py, sentry_service.py and test_observability.py match `git show 1c6f6796:` after CRLF normalisation. The head's .env was copied in for the run and removed afterwards. It ran the green's 61-file comm set (which includes test_observability.py and test_backend_cleanup.py): 9 failed / 1255 passed / 1 skipped / 8 deselected.\n- Head: the worktree ran the same 61 files plus the unit's own tests/test_retro_w1_1.py, test_retro_w1_7_w1_10.py and test_retro_w1_9.py (64 files, sorted = CI order, one process): 9 failed / 1343 passed / 1 skipped / 8 deselected.\n- The 9 failures are the same netguard DNS casualties as last round: test_auth_interceptor x2, test_camera_vision x3, test_rate_limiting_complete x1, test_security_hardening x3. The base failed set is byte-identical to the prior round's base_failed.txt.\n\nNo git write was made. I used no scratch worktree; the existing archive extraction served as the base.",
  "flag_identity": "This round changes NO product file: app/main.py and app/services/sentry_service.py are byte-identical to wip 967d7fba (sha256 unchanged), so every flag-identity property established last round still holds on the same bytes.\n\nW1-9c (ENABLE_DEFAULT_RATE_LIMITS, default OFF) changes only the flag-ON branch.\n- Flag OFF registers no middleware. TestW19cFlagOffIdentity pins this on the imported app, on a fresh exec, and for each falsy value ('', 0, false, off, no).\n- Mutant R2 (unconditional registration) was killed last round by 8 nodes.\n- The flag-OFF comm gate matches base exactly.\n\nW1-1b and W1-1c are UNFLAGGED security fixes, per the ruling.\n- W1-1b is additive: a new before_send_transaction hook on a shared _scrub_request_region helper. before_send behaviour is unchanged; TestBeforeSendUnchanged, test_sentry_service, test_admin_key_and_sentry_scrub and test_sentry_503_suppression are all green.\n- W1-1c only turns a former 500 into a 401 for a non-ASCII or non-base64 Basic payload. The new C5 pin now proves the other direction: a serving-time error on an AUTHENTICATED Basic request still surfaces as a 500, not a 401. The valid-credential and X-Admin-Key paths are pinned unchanged.\n\nW1-10b adds keys only. loop_lag_max_60s_ms is new; loop_lag_ms, loop_lag_max_ms, status and message are unchanged, and tests/test_health_loop_lag.py is byte-identical to base and green. The new recorder pin proves /health and loop_lag_snapshot() never touch the ring, so /health stays a pure dict read.",
  "measurements_run": [
   "Start state: `git status --short` was empty and HEAD was 967d7fba, so the worktree was clean. Start shas are in rmain_fix2/start_shas.txt and match the prior round's reported shas for main.py and sentry_service.py.",
   "Reproduced the blocking defect before fixing it. `pytest tests/test_observability.py tests/test_retro_w1_1.py` in one process: 1 failed / 60 passed.",
   "Grepped every test file for reload / sys.modules manipulation of the modules this unit binds. Only test_observability.py (sentry_service) and test_backend_cleanup.py (app.main) do it. I added test_backend_cleanup.py to the CI-order proof run (243 passed).",
   "Measured the default-limiter reach on the flag-ON fresh-exec app. With RAILWAY_ENVIRONMENT set (production config: docs, ReDoc and OpenAPI disabled by M13-21) it is exactly {/health, /, /favicon.ico}. With RAILWAY_ENVIRONMENT unset (local dev and this suite) the four FastAPI docs routes are added: /docs, /docs/oauth2-redirect, /redoc, /openapi.json.",
   "Measured the message framing. At the OUTERMOST send every route shows start, body, body, because the BaseHTTPMiddleware layers (ErrorHandler, SecurityHeaders, RequestID) re-frame with a trailing empty body. That is the wrong layer to measure chunking. Registration order puts SlowAPIASGIMiddleware between ErrorHandler (outside) and CORS (inside, pure ASGI). With a recorder spliced directly under it, all 7 reachable routes deliver exactly [http.response.start, http.response.body] with no more_body, and the whole app sends exactly one start with 200/204. favicon.png is 1466 bytes, one FileResponse chunk.",
   "Measured on a scratch app: with the pinned slowapi 0.1.10, SlowAPIASGIMiddleware emits http.response.start at least twice for a 2-chunk StreamingResponse on a default-limited route. This reproduces the adversary's minor.",
   "Mutation matrix: 9 mutants, all killed, every restore sha-verified (see mutation_checks).",
   "Unit set with the flag unset: 225 passed. With ENABLE_DEFAULT_RATE_LIMITS=true: 223 passed + 2 expected flag-OFF-state failures. CI-order set including test_observability and test_backend_cleanup: 243 passed.",
   "Comm gate, base 1c6f6796 vs head: identical failed sets. The head set adds the unit's 3 retro files.",
   "ruff --select E9,F63,F7,F82 --no-cache on the 5 unit files: All checks passed. ruff F401,F811,F841 on the 3 test files: All checks passed. compile() on the 5 files: ok.",
   "`file` reports CRLF line terminators on all 3 edited test files, and `git diff --stat` shows only them (+284/-7). The two scratch worktrees in `git worktree list` (rw04g/main_wt, w42fix/w42-base) belong to other agents; I created none."
  ],
  "pr_text": "retro(R-MAIN): W1-1b Sentry transaction scrub, W1-1c /admin Basic 500 -> 401, W1-9c async-aware default limiter, W1-10b windowed loop-lag max\n\nW1-1b (security, unflagged): init_sentry now passes before_send_transaction. It runs the same request-region scrub as before_send, through one shared helper (_scrub_request_region), so the two hooks cannot drift. The scrub covers the X-Admin-Key, Authorization and Cookie headers, PII in the URL and in the raw query_string, and the request data and cookies. Before this, every sampled performance transaction for an authenticated admin request sent X-Admin-Key to Sentry verbatim, because the SDK's own header filter does not list it. Trace ids, spans and contexts are left alone, and 503 transactions are NOT dropped.\n\nW1-1c (security, unflagged): the Basic branch of the /admin/* static mount now catches (ValueError, UnicodeError). b64decode(str) raises a plain ValueError on a raw byte >= 0x80, and binascii.Error is a subclass of ValueError, so the old clause missed that case and the mount returned 500. `Basic \\xff\\xfe` now returns 401 with WWW-Authenticate. The try wraps only the decode, so an error while serving an authenticated request still surfaces as a 500 and is not turned into a 401. This is now pinned: test_serving_error_on_authenticated_basic_request_is_not_a_401 kills the mutant that moves partition, compare_digest and super().__call__ back inside the try.\n\nW1-9c (inside ENABLE_DEFAULT_RATE_LIMITS, default OFF): with the flag on, the app registers slowapi's SlowAPIASGIMiddleware instead of SlowAPIMiddleware. On the pinned slowapi 0.1.10, SlowAPIMiddleware cannot await our async handler, so its 429s shipped a bare {\"error\": ...} body. With the ASGI variant, the default-limited routes 429 with the envelope, code RATE_LIMITED and Retry-After. With the flag OFF the app is byte-identical: no middleware, pinned by a fresh exec.\n\nTwo measured limits on the pinned stack (fastapi 0.141.1 / slowapi 0.1.10):\n(a) slowapi's _find_route_handler cannot see through fastapi's _IncludedRouter entries, so router routes are NEVER default-limited, decorated or not. The default reaches only app-level routes. On Railway that means /health, / and /favicon.ico. Locally, where docs are enabled, the four FastAPI docs routes are added.\n(b) SlowAPIASGIMiddleware re-sends the held http.response.start before every body message, so a multi-chunk response on a default-limited route breaks with the flag ON.\n\nLimit (b) is accepted as a stated limit because every route the default can reach today answers in one chunk. This is pinned by TestW19cStatedLimitSingleChunk:\n- The reachable set is pinned for both configs.\n- Each reachable route delivers exactly one start and one body directly under the middleware.\n- A scratch-app measurement pin records the double start on the pinned slowapi.\n\nThe old test \"decorated route is exempt from the default\" was vacuous, because POST /api/v1/feedback is unreachable, not exempt. It is rewritten to pin what is true: the route is unresolvable by the default limiter, and its own 30/minute decorator contract still holds with the flag ON.\n\nCLAUDE.md correction: the ENABLE_DEFAULT_RATE_LIMITS row says the flag rate-limits \"the 21 previously-undecorated routes\". That is false on the pinned stack. Router-included routes are never default-limited; only the app-level /health, / and /favicon.ico are (plus the docs routes where docs are enabled). Flipping the flag therefore does not rate-limit /app/version, /usage/status, /auth/me or the referral routes.\n\nFollow-up W1-9d (not in this PR):\n- If router routes must carry the blanket default, it needs a handler lookup that descends into _IncludedRouter. That would rate-limit every client at 10/min per path, so it needs a verified ENABLE_PROXY_AWARE_RATELIMIT first.\n- Before any multi-chunk response (StreamingResponse, or FileResponse over 64 KiB) becomes reachable by the default limiter with the flag ON, fix the slowapi start re-send (upgrade or local middleware) or exempt that route.\n- The stated-limit pins fail if the reachable set grows.\n\nW1-10b (additive key): /health and loop_lag_snapshot() gain loop_lag_max_60s_ms, the max over a 60-slot ring of per-tick lag samples floored at 0. Before this, loop_lag_max_ms was a high-water mark since process start that never decays, so a new stall after an old one was invisible. The existing keys are unchanged. The recorder recomputes the windowed max once per tick, so /health stays a dict read that never walks the ring. This is pinned by test_windowed_max_is_computed_by_the_recorder_not_by_health, which kills a snapshot that computes max(ring).\n\nW1-7b: tests/test_retro_w1_7_w1_10.py makes the loop-lag pins non-vacuous:\n- A synthetic tick is read back through GET /health without the heartbeat overwriting it.\n- The heartbeat records now - previous and advances previous.\n- The shutdown handler is registered.\ntests/test_health_loop_lag.py is intentionally untouched; its vacuous nodes are backstopped by these mutation-verified pins.\n\nFix round 2 (review of round 1):\n- BLOCKING test-order defect fixed. tests/test_retro_w1_1.py no longer holds a module-level reference to any sentry_service attribute; everything resolves through the module object at call time. tests/test_observability.py, which sorts first in CI, importlib.reload()s sentry_service, and that had broken an identity assert. Proven in one process in CI order: test_observability.py then test_retro_w1_1.py went from 1 failed to 0 failed. The full unit set plus test_observability.py and test_backend_cleanup.py passes 243/243 in alphabetical order.\n- Added pins for C5, the recorder-computed windowed max and the single-chunk stated limit, and rewrote the vacuous router-route test.\n- Mutation matrix: 9 of 9 killed, including the two round-1 survivors (C5 and the snapshot that computes max(ring)).\n- Comm gate: 61 files plus the unit's 3 retro files show the same failure set as base 1c6f6796 (9 netguard DNS casualties, all present at base).\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)",
  "deviations_from_spec": [
   "Ruling (3) says to pin that the only routes the default limiter can reach are /health, / and /favicon.ico. Measured: that holds only with RAILWAY_ENVIRONMENT set, i.e. the production config where M13-21 disables docs. With it unset (local dev and this test suite), /docs, /docs/oauth2-redirect, /redoc and /openapi.json are also app-level routes with an .endpoint, so the default reaches them too. I pinned both configs (exactly 3 on Railway, 3 plus the 4 docs routes off Railway) and included all 7 in the single-chunk pin; all 7 measured single-chunk under the middleware. The pr_text says so.",
   "The single-chunk pin measures directly under SlowAPIASGIMiddleware, via a recorder spliced into the built middleware stack, not at the app's outermost send. At the outermost send every route shows start, body, body, because the outer BaseHTTPMiddleware layers add a trailing empty body. That is framing from layers outside slowapi, not chunking slowapi has to handle, so the outermost layer would give a false failure. The pin also asserts the outermost send carries exactly one start with 200/204.",
   "The rewritten router-route test is renamed test_router_decorated_route_is_unreachable_by_the_default_and_keeps_its_contract, because the old name asserted exemption-by-decorator, which is false on the pinned stack. Anyone tracking the old node id should use the new one.",
   "Ruling (1) is satisfied, and I found one more reload site the adversary did not name: tests/test_backend_cleanup.py importlib.reload()s app.main, and it sorts before test_retro_w1_7_w1_10.py and test_retro_w1_9.py. I did not need to change anything for it; both files keep working after that reload because they read app_main.* at call time and the reload reuses the same module dict. It is covered by the 243-node CI-order run that includes it.",
   "No defect was disputed; all three were re-derived from code and are real. No product file changed: the fixes were all test pins or test restructuring, as the rulings scope them."
  ],
  "git_status_final": " M tests/test_retro_w1_1.py\n M tests/test_retro_w1_7_w1_10.py\n M tests/test_retro_w1_9.py",
  "residual_risk": "- The SlowAPIASGIMiddleware multi-chunk limit is still live code behaviour on the pinned slowapi 0.1.10. It is safe only because every route the default limiter can reach answers in one chunk. The pins fail if that set grows or a reachable route starts streaming, but a FileResponse over 64 KiB on an app-level undecorated route would also break with the flag ON; the 7 routes measured today (favicon 1466 B) are nowhere near that. Tracked as follow-up W1-9d.\n- The flag-ON path is still exercised only through fresh-exec private modules, not a real restart.\n- The default limit on the docs routes matters only off Railway.\n- The C5 pin injects a failure through the parent StaticFiles.__call__ (the class in the mount's __mro__[1]). If the mount stops subclassing StaticFiles, the pin needs re-pointing.\n- The e2e Sentry child-process tests (TestW11bEndToEnd) passed in every run here but are the slowest nodes (up to 900 s timeout budget).\n- The CLAUDE.md correction is only in pr_text; the CLAUDE.md row itself is not edited in this PR (out of scope).\n- The orchestrator must commit the 3 test files. Nothing was committed or pushed."
 },
 "adversary:R-MAIN-r2": {
  "unit": "R-MAIN re-review round 2 (W1-1b, W1-1c, W1-9c, W1-7b/W1-10b). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-main, branch retro/w1-1-w1-7-w1-9-main, wip 967d7fba on base 1c6f6796, plus the fix-round-2 uncommitted edits to 3 test files",
  "verdict": "SOUND",
  "defects": [
   {
    "severity": "minor",
    "location": "app/main.py:143-146 (the W1-9c comment block above `if _default_rate_limits_enabled():`)",
    "claim": "The comment says the blanket default reaches ONLY /health, / and /favicon.ico. That is true only when RAILWAY_ENVIRONMENT is set. Off Railway, meaning local dev and the test suite, the four FastAPI docs routes are also reachable. The fixer measured this, pinned it (test_off_railway_only_the_docs_routes_are_added) and states it correctly in pr_text. Only this code comment still gives the narrower set.",
    "why_it_matters": "Documentation only; there is no behaviour or test impact. The next editor who reads the comment instead of the pin may assume docs routes are exempt locally. It can be fixed in one line at merge time, and it does not block.",
    "reproduced": true
   }
  ],
  "tests_that_prove_nothing": [],
  "worktree_left_byte_identical": true,
  "summary": "Verdict: SOUND. Every item from round 1 is closed, and the fix is proven by mutation and by a full test run in the order CI uses.\n\n**Start state (as the brief expects for round 2):** HEAD is 967d7fba. `git status --short` shows only the fixer's 3 uncommitted test files (M tests/test_retro_w1_1.py, test_retro_w1_7_w1_10.py, test_retro_w1_9.py). `git diff HEAD` is +284/-7, exactly those files.\n\n**sha256 of each file at the start:**\n- app/main.py fd091ace0d01b4b88cd92a2952790b94a35d0dbc2c10608532a3d00651153008\n- app/services/sentry_service.py 50ea048c63bb11292378d45ac78387ca272f25a6184ecd06dbd87a80e257b469\n- tests/test_retro_w1_1.py 00d9121924aa32c1d5065863c95813c4753fb8f35ecbdd142635c097f1ab7aff\n- tests/test_retro_w1_9.py bc1870624c478adcfc77dd94c67e3f0861c4916f586dc38a61322a9cd346f178\n- tests/test_retro_w1_7_w1_10.py 4e3894c3b8ed5dadb4cbba98bff5f5a8ea8fb1f0160987482ce528e2056179e4\n- tests/test_health_loop_lag.py 7ae7323373dcbe2ac9683531d80fc9905a7c9122fde58017e0cc3492b017de08 (byte-identical to base, per ruling 5)\n- app/api/feedback_routes.py was a mutation target only; it is not in the diff.\n\nAll of these match the fix report. Every one was sha-verified unchanged at the end, and `git status` and `git diff --stat HEAD` match the start exactly.\n\n**Unit re-run** (pinned venv, process-wide netguard, 9 unit files):\n- Flag unset: 201 passed. That is round 1's 188 plus the 13 new nodes. No mutant is on disk.\n- ENABLE_DEFAULT_RATE_LIMITS=true: 199 passed / 2 failed. The two are the known flag-OFF-at-import nodes, failing by design.\n\n**CI-order pair** (tests/test_observability.py then tests/test_retro_w1_1.py, one process):\n- Current bytes: 63 passed / 0 failed.\n- With HEAD's old test file swapped in (mutant ORD): 1 failed, test_other_init_options_are_unchanged. The blocking defect is reproduced before the fix and closed after it.\n\n**Mutation matrix:** 33 mutants, 33 killed. Each was applied from a byte snapshot with no git involved, and every restore was sha256-verified. They cover:\n- **All 9 of the fixer's mutants**, re-run: C5, W_snap_ringmax, FB_decorator_removed, ST_health_multichunk, R1, C1, W_ring59 and S1 (all killed), plus the ORD test-order mutant.\n- **New W1-1c mutants:** C5b (serving code inside the try, broad `except`), and C1b (the base's `except (binascii.Error, UnicodeDecodeError)` clause).\n- **New W1-10b mutants:** a list-copy ring max inside the snapshot, ring sizes 61 and 59, windowed max replaced by the high-water mark, windowed max replaced by the last value, an unfloored ring, a zero snapshot, and a literal /health.\n- **Heartbeat mutants:** HB_B (records the interval), HB_C (no advance) and HB_D (shutdown unregistered).\n- **W1-9c mutants:** R2 (unconditional registration), a multi-chunk favicon, and a new single-chunk app-level route. The reachable-set pins catch that last one.\n- **Sentry mutants:**\n  - S2 no-op.\n  - S3: the transaction hook replaced by `_before_send`. This is killed by the 503 pin.\n  - S4: `before_send` wrapped in a lambda. The call-time identity assert still catches it.\n  - S6, S8, S9 and S10.\n  - S12: the transaction hook drops 503s.\n  - S13: the trace_id is clobbered.\n\nNo new test was found that survives removal of its fix.\n\n**Real-function probes** (under tests.conftest and the netguard; 17 passed; 0 blocked network attempts):\n- **Flag-OFF identity:** base main.py (read with `git show 1c6f6796`) and head main.py were exec'd with the flag unset, '', 'false' and '0'. User middleware, route table and exception handlers are identical. With 'true', the only difference is SlowAPIMiddleware changing to SlowAPIASGIMiddleware.\n- **/health:** the only new key is loop_lag_max_60s_ms. Every existing key and value is unchanged.\n- **Router routes with the flag on:** /api/v1/app/version, /usage/status and /legal/privacy give _find_route_handler None, and 13 calls produce zero 429s. This confirms the pr_text CLAUDE.md correction.\n- **/admin mount, 11 malformed Basic payloads:** \\xff\\xfe, \\xc3\\xa9, empty, ':' only, invalid base64, lowercase `basic` with \\x80, invalid UTF-8 after base64, no colon, wrong password, '=====', and a single character. Every one returns 401 with WWW-Authenticate.\n- **/admin mount, correct password that contains a colon:** returns 200.\n\n**Comm gate:**\n- Base: the fixer's git-archive tree of 1c6f6796. All 1093 app/tests/migrations blobs match per `git hash-object`. `.env` was copied in only for each run and deleted afterwards; its absence is confirmed.\n- 61-file comm set: base 9 failed / 1255 passed; head (61 files plus the 3 retro files) 9 failed / 1343 passed. Head-only failures: none. Base-only failures: none. The 9 are the known netguard DNS casualties.\n- **Full `pytest tests/`** in CI's alphabetical order, with the committed baseline deselected as CI does: base 5 failed / 17416 passed; head 5 failed / 17508 passed. The failed sets are identical, with no head-only and no base-only failures.\n  - The 5 failures are 4 netguard SSRF/DNS casualties plus test_shopify_discovery_l13::test_fetch_uses_catalog_and_matches. That test is order-dependent and fails the same way at base; it passes alone on both trees.\n  - The retro files made 0 network attempts.\n  - Head has 4 fewer skips than base. One is the base archive not being a git checkout (test_sqlfluff_config: 'git not available'). The other 3 were not identified, but head runs more tests, not fewer, so they cannot hide a head failure.\n\n**Hygiene:**\n- ruff E9/F63/F7/F82 is clean on all 5 files, and ruff F401/F811/F841 is clean on the 3 test files. compile() passes on all 5.\n- The 3 edited test files are fully CRLF, with 0 LF-only lines. The diff is line-scoped, not whole-file.\n\n**Rulings check:**\n1. No sentry_service attribute is bound at module level; everything is resolved at call time.\n2. The C5 pin exists and kills both C5 variants.\n3. The stated-limit single-chunk pins and the reachable-set pins are there (Railway set and off-Railway set), and W1-9d plus the CLAUDE.md correction are in pr_text.\n4. The router-route test is rewritten to pin the unreachability that actually exists, and the recorder-recompute claim is pinned.\n5. The report carries the final shas.\n\nThe one remaining nit is the main.py comment's route set, listed above as minor.\n\n**Scratch:** my files are all under scratchpad/rmain_adv2/ (mutate.py, mutation_matrix.jsonl, probe_test_adv2.py, probe_results.jsonl, comm_*/full_* outputs). I made no git writes. I created no worktree; the worktree count is 40 in `git worktree list` at the end. I created no stash."
 }
}