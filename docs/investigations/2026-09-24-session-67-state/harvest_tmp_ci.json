{
 "fix:R-W04-ci": {
  "unit": "R-W04 (PR #196): CI-only failures, 4 harness nodes plus 1 retro node. Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-w04 at 9d620327. `git status --short` was empty before any write.",
  "reproduction": "FOUR HARNESS NODES, REPRODUCED. Command: `pytest tests/test_platform_router.py tests/test_retro_w0_4.py tests/test_verify_flag_byte_identity.py`, with the unit's netguard plugin, dead proxies and flag OFF. Before the fix: `4 failed, 430 passed, 3 skipped in 59.26s`. The four failures are the exact nodes CI named (TestW04HarnessFlagsOnAndCompare::test_flags_forces_the_flag_off_per_call, test_flags_on_forces_true_per_call_and_restores, test_flags_on_restores_an_unset_flag_to_unset, test_flags_on_vs_off_compare_surfaces_the_flag_fork).\n\nFIFTH NODE: THE BRIEF MIS-ATTRIBUTED IT. In the CI log, tests/test_retro_w0_4.py::test_flag_on_extraction_is_identical_over_every_fixture_page did not fail an assertion. It failed with `E   Failed: Timeout (>60.0s) from pytest-timeout`, stuck in price_service `_contains_token -> re.search`. The message `assert (0 == 0 and set() == {'true'}` belongs to the harness node test_flags_on_restores_an_unset_flag_to_unset (`rc == 0 and set(seen) == {\"true\"}`).\n- CI runs `--timeout=60 --cov=app`, not 120.\n- The node's captured log reached its last WARNING (the EGP rows, 40 of 40) at 09:40:44, about 57 s in. The run was cut at 60 s just short of the end of the fixture list.\n\nLocal measurements:\n- Alone, with `--cov=app`: 46.72 s, 50.13 s, 52.36 s and 49.97 s.\n- Alone, with `--cov=app --timeout=30`: reproduces the timeout (`+++ Timeout +++`; pytest-timeout's thread method kills the process on Windows).\n\nBisection result: the failure does not depend on test order. It only depends on how much time the test needs. I profiled only this node with cProfile, in four runs:\n- alone: 125,840,318 calls\n- after CI prefix files 1-222: 125,796,249 calls\n- after CI prefix files 223-443 (this half includes test_platform_router.py at position 366): 125,641,651 calls\n- after the full 443-file CI prefix: 125,647,942 calls\n\nThe node does the same amount of work in every order. Profiled wall time was actually lowest after the full prefix (102.8 s, against 139.9 s alone).\n\nOne unprofiled run of the full prefix plus this node, with coverage and `--timeout=900`, took 120.94 s. The profiles show no extra work, so I attribute that to box noise; the cause was not measured. The profiles do show `_io.open` taking 11-24 s for 162 fixture reads, which points to this box's real-time scanner. A heap-ballast experiment (4,042,033 GC-tracked objects) left the node at 49.97 s, so GC is not the cause.\n\nThis node does not fail after test_platform_router.py. It also has no stale-spy problem. Its spy patches the `bs4.BeautifulSoup.__init__` class, and no test removes `bs4` from sys.modules or reloads it (grep over tests/). So the old or fresh price_service module both build soups through the same patched class.\n\nPrefix order used: the first 443 files of the CI order, taken from the log with the timestamp prefix stripped. test_retro_w0_4.py is file 444 of 627, not about 600. test_platform_router.py is at 366 and test_verify_flag_byte_identity.py at 600.\n\nLocal-only noise, unrelated to this unit: the full-prefix runs on this box also showed 5 failures that all passed in CI. They are test_openai_breaker x2, test_price_latency_budget x1 and test_rate_limiting_complete x2, and come from timing and the dead proxies.",
  "root_cause": "(1) The four harness nodes had a stale module binding (issue #185 class):\n- tests/test_platform_router.py::test_does_not_import_price_service (lines 321-328) deletes every price_service entry from sys.modules and never restores them. The next import therefore builds a FRESH module object.\n- tests/test_verify_flag_byte_identity.py bound `from app.services import price_service as ps` at collection time (line 168). `_install_env_spy` then patched `ps.extract_price_from_html` on that old module.\n- scripts/verify_flag_byte_identity.py imports `from app.services.price_service import extract_price_from_html` inside the sweep at call time (line 258). After the deletion that import resolves to the fresh module, so the harness ran the real extractor and the spy recorded 0 of 8 calls.\n- When the file runs alone, both names point to the same object, which is why local runs passed.\n\n(2) The retro node timed out; the cause is test cost, not order:\n- It runs about 1,300 real `extract_price_from_html` calls, roughly 125M Python calls. That count was measured identical across orders.\n- CI runs the whole free tier under `--cov=app` with `--timeout=60`. Coverage tracing brings the node to 47-52 s on the dev box, and the CI runner went past 60 s.\n- Its spy target, `bs4.BeautifulSoup.__init__`, is already the class the live extraction uses, so the spy needed no change.",
  "files_changed": [
   "tests/test_verify_flag_byte_identity.py",
   "tests/test_retro_w0_4.py"
  ],
  "test_evidence": "AFTER THE FIX:\n- Harness reproduction order (platform_router, retro, verify), flag OFF: `434 passed, 3 skipped, 1 warning in 105.99s`.\n- Same order, platform_router then verify only, flag ON: `277 passed, 3 skipped, 1 warning in 43.61s`.\n- Retro node alone with `--cov=app --timeout=30` (the CLI budget that timed it out before): `1 passed` (70.77s call). The marker overrides the CLI timeout.\n- Unit file set via units.sh (test_retro_w0_4 + test_verify_flag_byte_identity + test_price_parse_offload), flag OFF: `185 passed in 104.46s`.\n- Same set, flag ON: `185 passed in 100.78s`.\n- Each file alone: test_retro_w0_4.py `157 passed in 101.18s`; test_verify_flag_byte_identity.py `19 passed, 1 warning in 27.17s`. test_price_parse_offload.py was not run alone; its 9 is inferred as 185 - 157 - 19.\n\nThe counts are 157 + 19 + 9 = 185, not the brief's 88 + 15 + 9 = 112. Neither edit adds or removes a test, so the brief's figures are stale for this HEAD.\n\n`rw04 netguard: 0 blocked network attempts` on every unit run. The CI-prefix runs show 42-43 blocked attempts; the guard fails closed on those attempts.\n\nLINT: `ruff check --select E9,F63,F7,F82 --no-cache` on both files: `All checks passed!`. `py_compile` passed on both. Neither file is on .github/black-clean-paths.txt.",
  "mutation_proof": "Before the mutation, the fixed file was snapshotted (sha256 4396a3dcace25d6016617392d85c4c50a4cea3ff4dd2de691be60fefd4f9b591) to scratchpad r196/snap_fixed.\n\nThe mutant brought back the collection-time binding. It added `_MUTANT_COLLECTION_PS = _live_price_service()` at module level and patched `_MUTANT_COLLECTION_PS` instead of `_live_price_service()`. The reproduction (test_platform_router.py then test_verify_flag_byte_identity.py) failed again: `4 failed, 273 passed, 3 skipped`, the same four TestW04HarnessFlagsOnAndCompare nodes.\n\nThe file was restored with cp from the snapshot, not git checkout, and the sha256 was verified back to 4396a3dc. After that, one PEP8 blank line was added after the helper (final sha below), and every after-fix run listed above used the final bytes.\n\nThe retro marker's before/after is the same kind of proof. On the base bytes (no marker), `--cov=app --timeout=30` timed out; with the marker it passed.",
  "lint_clean": true,
  "pr_text": "CI hermeticity note (#185 class). The first CI run of this PR failed backend-tests (5 failed / 18,820 passed). These are test-harness problems in the one-process suite; no product defect was involved, and the app/ and scripts/ bytes did not change (`git diff -- app scripts` is empty). There were two separate causes.\n\n(1) Four TestW04HarnessFlagsOnAndCompare nodes. tests/test_platform_router.py::test_does_not_import_price_service deletes every price_service entry from sys.modules and never restores them. tests/test_verify_flag_byte_identity.py had bound `price_service as ps` at collection time and put its env spy on that old object. scripts/verify_flag_byte_identity.py imports extract_price_from_html inside the call, so after the deletion it ran the fresh module and the spy saw 0 of 8 calls. The test now resolves the live module at patch time (`importlib.import_module`, the object main() will read). Reproduction: platform_router placed before the harness file gives 4 failed; with the fix it gives 434 passed. A mutant that restores the collection-time binding fails the same 4 again.\n\n(2) tests/test_retro_w0_4.py::test_flag_on_extraction_is_identical_over_every_fixture_page. This was a timeout, not an assertion failure. It runs about 1,300 real extractions under `--cov=app`: 47-52 s alone on the dev box, and past the 60 s per-test budget on the CI runner. cProfile shows the same about 125M calls alone and after every CI-order prefix tested, so the cost does not depend on test order. It now carries `@pytest.mark.timeout(600)`, following the precedent in tests/test_retro_w1_1.py, and every assertion is unchanged.\n\nUnit files: 185 passed with ENABLE_PRICE_PARSE_OFFLOAD both unset and true.",
  "git_status_final": " M tests/test_retro_w0_4.py\n M tests/test_verify_flag_byte_identity.py",
  "file_sha256": [
   "811b9f989843e4cf6a6edda8a89f7c5074811efaa09745ecf9b12667b6e95799  C:/Users/SynAckITPC/Documents/AI/sc-r-w04/tests/test_verify_flag_byte_identity.py (CRLF kept)",
   "3d0cd2424c6fa23a0e797c46da1af014f42911f56d12ab0bbe77af11cfb39463  C:/Users/SynAckITPC/Documents/AI/sc-r-w04/tests/test_retro_w0_4.py (CRLF kept)",
   "scratch only, not in the repo: plugins r196/plug/heapballast.py and r196/plug/nodeprof.py, plus runner scripts r196/run.sh, runA.sh, ci_prefix.sh, ci_prefix_prof.sh and run_ballast.sh, all under C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/75660a55-e14a-4de1-80dc-a537f2af315e/scratchpad/r196/"
  ]
 }
}