(1) The logout contract under ENABLE_LOGOUT_UPSTREAM_REVOCATION becomes: for a VALID bearer, call `client.auth.admin.sign_out(access_token, "local")` on the admin client, offloaded via run_db (or asyncio.to_thread, whichever verify_token uses), regardless of any refresh token; the refresh-token VALUE is never sent upstream (it never reached Supabase). Rewrite the existing tests/test_auth_refresh_and_revocation.py nodes 3/4/5 to this contract (node 5 becomes: flag ON with no refresh token still makes exactly one admin sign_out by access token; nodes 3/4 pin the scope=local + the second-device survival through the admin call); the previous set_session-then-sign_out ordering pins are retired with a comment naming this ruling. (2) EXPIRED bearer path (the W1-4 client report's server alternative, in this group because it is the same route): when the flag is ON and the body carries a refresh_token, /auth/logout ACCEPTS a presented-but-expired Bearer: parse the JWT payload WITHOUT verification only to pick the route branch, blacklist the presented access token as today, then `client.auth.set_session(access_token, refresh_token)` (the SDK's expired branch rotates the pair upstream) followed by `client.auth.sign_out({"scope": "local"})`, all offloaded; a pair Supabase rejects (fake gotrue 400 on /token) still returns 200 (local clear done) and logs ONE warning with the exception TYPE only; a request with an expired bearer and NO refresh token keeps today's 401. Pins: red test 2 from the client report (expired Bearer + refresh_token body + flag ON reaches logout_user and produces POST /token then POST /logout?scope=local on the fake); flag OFF: today's exact 401 for an expired bearer; the token values never appear in any log line (thread-ident pin: the upstream leg runs off the event-loop thread). (3) W1-4b classifier is by TYPE: httpx.TransportError, supabase_auth AuthRetryableError, AuthApiError with status 429 or >= 500, AND AuthUnknownError whose original_error is an httpx.HTTPStatusError with status 429 or >= 500 (the HTML 429/500 gateway pages) -> 503 REFRESH_UPSTREAM_UNAVAILABLE; every other shape stays 401 (all the red's non-transient pins). (4) W1-4c: build the anon client's options LOCALLY in get_auth_client with SyncClientOptions(auto_refresh_token=False, persist_session=False, ...) via the constructor (SyncClientOptions.replace() cannot set False -- measured); do not change build_supabase_client_options (shared with database_service); pin: no supabase_auth Timer thread survives refresh/login/logout on either branch; pin: the set_session + sign_out expired path still works with persist_session=False. (5) W1-9b: arm the lockout atomically (SET NX EX + INCR, or a MULTI pipeline -- both exist on upstash-redis 1.7.0) and treat ANY non-positive TTL on a positive count (-2, -1, 0) as 're-arm the window now': the 429 then carries retry_after_seconds = the window and Retry-After; keep the red's TTL -2/0 nodes. (6) Out of scope here, record in pr_text: SlowAPIMiddleware (R-MAIN), logout 422 on non-JSON bodies, the W1-4 client half (R-CLIENT).