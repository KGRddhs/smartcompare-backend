# SESSION 68 state (2026-09-26, started ~04:20 local) — the engineering lane; read this FIRST before resuming

One session, Fable orchestrating and Opus 5.5 agents running every red / green / adversary / fix / spec / spec-review round as Workflow-tool scripts under the `synack-build-orchestrator` skill (Ahmed pasted the session-67 kickoff prompt verbatim). This file is a CHECKPOINT written mid-session (first docs PR of the session); every fact was verified from disk, the pinned venv or the GitHub REST API when written. The session-67 close doc (`2026-09-24-session-67-state.md`) is superseded for state by this one; its process facts still hold.

## 0. The one-paragraph state

Base at start: main `61585c58` (the session-67 close), production on Railway `web` at current main (`/health` 200 with `loop_lag_max_60s_ms`), phones on EAS group `561d2cba` from `ab9442ae`, flags ON in prod beyond the historical set = `ENABLE_BRIGHTDATA_BUDGET_GATE` (web + price-warmer), `ENABLE_LOGOUT_UPSTREAM_REVOCATION`, `ENABLE_CONSENT_PERSIST`; migrations 037/038/040/041 applied, 035/036 not, 039 reserved. **Nothing was flipped and Railway/Supabase were not touched this session** (two read-only HTTP probes of the public `/health` and `/api/v1/auth/refresh` only). The box is healthy (python/node spawn < 0.2 s). The R-W04 network-guard plugin had died with its worktree; it was REBUILT in the scratchpad (`state/scripts/qaren_netguard.py`: socket + curl_cffi `Session.request` / `AsyncSession.request` / `Curl.perform` / `AsyncCurl.add_handle`, loopback allowed) and verified before any test ran. **Merged so far: PR #202 (#198, main `ac887e2d`).** In flight at this checkpoint: the hermeticity unit (green → adversary), W0-4e/f/g (fix round 3 after a design correction), and the first two W4 reds (W4-13, W4-12). The seven remaining W4 specs are MEASURED and ADVERSARIALLY REVIEWED with Fable rulings written for each (§4).

## 1. Units and their state

| unit | worktree / branch | state | evidence |
|---|---|---|---|
| **#198** auth log hygiene | `sc-auth-198` / `fix/198-auth-401-log-level` (retired) | **MERGED PR #202 → `ac887e2d`**, deployed, canary 401 body identical on the fresh process; follow-up **#203** filed (the password-recovery re-wrap drops `.status`) | `state/unit-specs/AUTH_198_SPEC.md`, `RULINGS_198.md`, `state/reports/{red,green,fix2}_auth198.json`, `pr_text_198.md` |
| **Hermeticity** #183 #184 #185 #186 + netguard 04c | `sc-hermetic` / `test/hermeticity-183-184-185-186` | red DONE and GATED (14 rulings); green → adversary → fix pipeline RUNNING | `state/unit-specs/HERMETICITY_SPEC.md`, `RULINGS_HERMETICITY.md`, `state/reports/red_hermetic.json`, `attempting_nodes*.txt` |
| **W0-4e/f/g** (R-W04 follow-ups, existing flag `ENABLE_PRICE_PARSE_OFFLOAD`) | `sc-w0-4efg` / `retro/w0-4efg-followups` | red gated (R1-R10); pipeline ended SOUND on bytes but with an ESCALATED design fact (§3); fix round 3 (R11-R13) RUNNING | `state/unit-specs/W0_4EFG_SPEC.md`, `RULINGS_W04EFG.md`, `state/reports/{red,green}_w04efg.json` |
| **W4-13** measurement truth | `sc-w4-13` / `feature/s68-w4-13-measurement-truth` (from `ac887e2d`) | red RUNNING | `state/w4-specs/W4_13_*` |
| **W4-12** display contract | `sc-w4-12` / `feature/s68-w4-12-display-contract` (from `ac887e2d`) | red RUNNING | `state/w4-specs/W4_12_*` |
| W4-8, W4-6a, W4-11, W4-7, W4-14 | not yet cut | specs + reviews + rulings ready; batch order 8+6a → 11+7 → 14 (client, needs a `node_modules` junction) | `state/w4-specs/` |
| W4-6b | — | blocked on Ahmed's #101 answer (recommendation A, renormalize) | `DECISIONS_AHMED.md` (session 67) |

## 2. The measured facts this session produced (each from an agent report the orchestrator re-derived or from a direct probe)

- **Test egress (#184), the whole free tier under the curl_cffi-aware guard, CI order, 17 chunks:** 18,922 nodes run = 18,830 passed / 4 failed / 0 errors; the 4 failures are hidden LIVE DNS dependencies in SSRF "valid URL" tests (`tests/test_security_hardening.py` ×3, `tests/test_rate_limiting_complete.py::TestUrlDetectSsrf::test_detect_allows_valid_url`); **681 blocked network attempts from 196 nodes in 52 files** (329 getaddrinfo, 215 create_connection, 136 curl_cffi `Session.request`, 0 bare perform): 69 nodes hit api.openai.com, 106 hit the `.invalid` credential sentinels (95 exclusively), 28 made curl_cffi retailer fetches (noon, Shopify products.json on three BH stores, algolia, unbxd, footlocker, sharafdg), 8 SSRF literal probes, 6 example.com. Session 66's 376 / 111 over a 321-file subset was the same class, under-measured.
- **#186 root cause (measured, the issue's guess was wrong):** pytest 9.1.1's `monkeypatch.setattr` on an INSTANCE (`real_service = _ss.get_scoring_service()`) snapshots the BOUND method and `undo()` pins it into the singleton's `__dict__`, shadowing the class — W4-4's class-level spy is then never called. Any `monkeypatch.setattr(<singleton instance>, ...)` in the repo has the same hazard. Fix: a private `ScoringService()` in `_run_sync`.
- **#183 / #185 reproduced in CI order** (`serper_service.SERPER_API_KEY` left as `test-key`; `app.services.price_service` identity replaced by the `del sys.modules` cycle proof — the by-name `fetch_shopify_price.__globals__` is the orphaned dict). Reload audit over the 13 files that mention `importlib.reload`: 7 call it; #183 leaks a VALUE; five more leak module IDENTITY only (test_backend_cleanup/app.main, test_fan_out_budget_env, test_m18_openai_tpm_sizing, test_observability, test_supplement_branch_genuine) → follow-up issue, not this unit.
- **W0-4e (the GIL fact):** offloading the backfill's `re.findall` to a thread does NOT free the event loop — CPython's regex engine holds the GIL for the whole call (0.200 s offloaded vs 0.202 s inline on a 40 MB page; 0.147 vs 0.153 s on 30 MB). The spec's assumption was refuted by the r1 adversary and confirmed by the fixer's probe; ruling R11 replaced the cosmetic offload with a chunked, YIELDING scan on the loop (1,000,000-char slices, 65,536-char overlap, dedupe by absolute match start, `sleep(0)` per slice; result-identical to the single `findall` for any match shorter than the overlap).
- **W0-4 corpus facts:** the recorded OFF digests (OVERALL `9504e5a9…`, results `a1b3460c…`) reproduce ONLY with R-W04's FOUR-flag `--flags` list (`ENABLE_OFFLOOP_DNS_RESOLVE,ENABLE_SUPABASE_CLIENT_REUSE,ENABLE_UPSTASH_BOUNDED_TRANSPORT,ENABLE_PRICE_PARSE_OFFLOAD`); with `ENABLE_PRICE_PARSE_OFFLOAD` alone the OVERALL is `994b623e…` over the SAME results array (the digest hashes the flag list). ON `--compare` OFF: equal, 0 of 1656.
- **#198 SDK facts (supabase-auth 2.31.0):** `str(AuthApiError)` is the message only, never the code; `UserDoesntExist` and `AuthUnknownError` carry no `.status`; sentry-sdk 2.68.1 auto-enables `LoggingIntegration` with `event_level=ERROR` (WARNING = breadcrumb); with a `%`-template every generic-branch ERROR shared one Sentry `logentry.message` (measured with a loopback DSN), hence the pre-formatted lines.
- **W4 specs (§4):** every one of the seven specs written with full measurement had 5–13 refuted claims in adversarial review — the session-65d rule ("never let a spec reach a red phase unreviewed") held again.

## 3. Fable rulings that changed a design mid-flight (binding; full text in `state/unit-specs/` and `state/w4-specs/`)

- **#198 R11/R12** (after a SOUND pipeline): server-side 4xx (Kong "Invalid API key", gotrue `not_admin`, "is disabled", `AuthSessionMissingError`) stay ERROR via a carve-out checked BEFORE the status rule; both log lines pre-formatted (no record args) so Sentry groups per context + type. One more fix + re-adversary round, SOUND.
- **W0-4e R11** (after a SOUND-on-bytes pipeline): the pool offload of the regex scan is cosmetic (GIL) → chunked yielding scan on the loop; **R12** the `/health.price_parse_pool` key persists after an ON→OFF flip until restart; `queued` is transiently non-zero on semaphore hand-offs and only a value persisting across polls means orphaned parses; **R1** (red gate) `waiting`/`peak_waiting` count only jobs that BLOCKED on the semaphore.
- **Hermeticity R5–R7**: the four SSRF DNS tests get a test-side resolver stub (never `allow_network`); `getaddrinfo` is allowed for IP literals (no DNS); the report classifies targets `sentinel` / `ip_literal` / `egress` and the ratchet fails CI only on a NEW `egress`-class node; block-and-count, not fail, in this unit (the 196-node cleanup is the follow-up campaign).

## 4. The W4 lane — seven measured specs, reviewed, ruled (`state/w4-specs/`)

Each `W4_<n>_UNIT_SPEC.md` carries the measurer's spec (base `61585c58`), the appended `# ADVERSARIAL SPEC REVIEW (2026-09-26, session 68)`, and a sibling `W4_<n>_FABLE_RULINGS.md` that overrides both. Headline rulings: **6a** V un-stamps `value` only when both products have a price and `_spec_missing` is equal across the pair; T never un-collapses presence-credit ties (fashion/other); G + G' + the zero-margin rule ride T; the craft-tie claim (c) is unfixable by these flags → W4-6c for Ahmed. **7** three own flags; Part A = one-sided None coupled to `ENABLE_MISSING_DIM_RENORM` (activation: W4-6b tie-break → RENORM → Part A); Part B counts identity-matched rows and never reads High beside a pending price; all four wrong-key readers routed; the price-path key drift is W4-7d. **8** Half B is FLAGGED (`ENABLE_BLOCKLIST_PRECISION_V2`; the 22 natural intent phrasings stay blocked); Half A = recall over precision with a pharmacy veto that maps to `supplements`; the 360-row corpus is committed as a fixture. **11** `ENABLE_VERDICT_PROMPT_TRUTH` yes; TRUTH :77 rewritten so scores fix the winner and facts justify it; the count check fails LOUD at import; the two unfenced third-party prompts are folded in. **12** `ENABLE_SINGLE_VERDICT_MARGIN` = the pre-nudge calibrated gap; the rendered `review_praise` leak is folded in; the 23 collateral flag-OFF pins in W4-4/W4-3/home_routes get a `delenv` of the new flag. **13** `ENABLE_SEARCH_LOG_TRUTH` kept; migration **042** backfills the 13 probe strings except probe-#1 rows ≥ 10 s; a dedicated `SEARCH_LOG_SYNTHETIC_TOKEN` with the empty-guard and the header added to Sentry's scrub; `/text/quick` folded in. **14** client half unflagged (EN byte-identical); the Arabic directive carries the copy-policy bans; the Arabic-aware guard audit is W4-14c and a hard precondition for the flag; Part D → W4-8c.

## 5. Process facts (binding for the rest of the session)

- Every unit ran as: spec → (adversarial spec review for W4) → red → Fable gate rulings → green → adversary → fix → re-adversary → Fable diff review → ONE squashed commit → rebase → `ship_checks.py` (sha verify, ruff, py_compile, CI-order set under the guard) → push → `pr_rest.py create` → `pr_rest.py watch <n> 90`. Scripts: `state/scripts/s68-red-*.mjs`, `s68-green.mjs` (generic green → adversary → bounded fix rounds), `s68-fixround.mjs` (targeted fix + re-adversary after a SOUND verdict), `s68-w4-specs.mjs`, `s68-w4-red.mjs`; agent rules `s68-common.txt`.
- A pipeline that ends SOUND is NOT the end: the orchestrator reads the minors, and where a minor is a design consequence of a ruling (twice this session) it is ruled and fixed, never accepted as a stated limit.
- The guard plugin loads with `-p qaren_netguard` BEFORE conftest; once the hermeticity unit lands, its conftest guard replaces it and the plugin must not be stacked on top (two guards under-count).
- `git worktree remove` can deregister a worktree and fail to delete its directory when a shell holds it (sc-auth-198 today): verify 0 reparse points, then delete when free; never recursively delete a directory holding a junction.
- The clock: wall-clock times in the agents' reports are the box's local time; the `pr_rest.py` log is the authoritative timestamp for merges.

## 6. Ahmed's items touched by this session

Confirm the #198 canary in Sentry (no new issue for the ~06:50 local invalid-refresh probe against `ac887e2d`; a wrong-API-key probe is NOT part of the canary — it pages by design). Answer #101 (W4-6b). New product calls recorded in the W4 rulings: W4-6c (a non-numeric merit discriminator for fashion/other), W4-7 Part B's price-leg cap beside pending prices, W4-8's recall-over-precision default and the blocklist flag, W4-12's gpt-4o spend on an unrenderable verdict sentence and the backfill of 17 stored leaking reasons, W4-13's PO-RECORDED-MEASURED-17 anon_id, W4-14's `category_switched` disclosure and the native review of the 51 Arabic labels before the OTA. Migration 042 (W4-13) will be an APPLY_PACK row when merged.

## 7. Next (in order)

Hermeticity SOUND → diff review → commit → PR; W0-4e/f/g fix round 3 SOUND → same; W4-13 and W4-12 reds → gates → greens; then W4-8 + W4-6a, W4-11 + W4-7, W4-14; file the follow-up issues (reload identity leaks, W4-7d, W4-12b–f, W4-8b/c, W4-13b/c, W4-14b–d); skill Step 2 tooling, the config-audit docs PR, the Step 6 review, the redaction re-run; the session-68 close doc + CLAUDE.md flag rows (every new flag default OFF) + `docs/CONTEXT_SESSION_LOG.md`.
