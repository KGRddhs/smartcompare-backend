# mobile CRITIC

recommend_gap_round: True

## gaps
- MONETIZATION / PURCHASE SURFACE NEVER ENUMERATED (plausible P1). No lane enumerated how a user becomes premium. Verified myself: `PaywallScreen.tsx:236-243` `onStartTrial` fires an Alert 'Coming soon' ('Subscriptions arrive after launch'), `:374` is the primary CTA and `:406` 'Restore' calls the same `onComingSoon` stub; `grep -rn 'purchase|StoreKit|RevenueCat|InAppPurchase|billing' src/screens/PaywallScreen.tsx` returns only a copy string; `isPremium` is server-derived only (`useComparisonCounter.ts:69` `status?.tier === 'premium'`). Meanwhile the same screen advertises concrete plans ('0.9 BHD/mo' :290, '2.9 BHD' :303) and a trial->billing timeline ('In 3 days' :359, 'Gentle reminder before billing' :354). So: no user can ever leave the free tier, and a free user at the cap has no forward path. This is an App Store completeness/2.1 and metadata risk of the same class as the eight gates in §1.5, and it appears in the report ONLY as a rationale for downgrading MB-FLOWS-STATE-03 to P2 — never as a finding, never in the §1.5 table, never in a unit.
- PUSH-NOTIFICATION RECEIVE/TAP SURFACE NEVER ENUMERATED (plausible P1/P2, live flag). The flows lane probed '5 deep-link shapes' — all client-authored. The BACKEND-emitted set was never enumerated: `push_service.py:80` `qaren://profile/referrals` (Loop-2), `scripts/cron_expire_bonuses.py:125` same, `reengagement_service.py:326/:345/:366` `qaren://comparison/{id}?banner=insight|retrospective` and `qaren://cohort/divergence`. The client linking config (`App.tsx:283-297`) registers ONLY `c/:share_token`, `q/:share_token`, `r/:code` plus a `redeem?code=` rewrite — none of those four paths — and I found ZERO notification listeners in the whole client (`grep -rn 'Notifications\.' src App.tsx` yields only `requestPermissionsAsync`/`getPermissionsAsync`/`getExpoPushTokenAsync`; no `addNotificationResponseReceivedListener`, no `setNotificationHandler`). `ENABLE_REFERRAL_SYSTEM=true` in prod (baseline/railway-web-vars.txt), so the referral push half is live while its tap target does not exist. This is the same defect class as MB-FLOWS-STATE-01/-04 (dead route target) and was missed because the enumeration started from the client.
- CONSENT CAPTURE (ToS clickwrap + 13+ age gate) NEVER CHECKED. CLAUDE.md's Pre-launch section names 'ToS clickwrap + 13+ gate' as code-side blockers, and the brief told this lane to reconcile prior items. I grepped: `acceptTerms|terms_accepted|clickwrap|agreeTo` = 0 hits in `src`/`App.tsx`; no age/birthdate gate anywhere. §1.5's blocker table has 8 rows and neither cell appears; MB-FLOWS-STATE-08 (P2) covers only the ABSENCE OF LINKS before sign-in, not acceptance capture. Account deletion (the other CLAUDE.md code-side blocker) IS wired (`EditProfileScreen.tsx:135` `api.delete('/api/v1/auth/account')`) — but that was never stated either, so the reader cannot tell 'checked and fine' from 'not checked'.
- NATIVE-STRING LOCALIZATION CELL MISSED, AND IT RIDES THE ONE-BUILD CONSTRAINT. `app.json` has no `locales` key and no `CFBundleLocalizations`; the camera/photos permission prompts are English-only literals (`app.json:95` cameraPermission, `:101` photosPermission) for an Arabic-first GCC product, and the notification plugin block (`:110-115`) carries no localized copy. §1.6 claims to cover 'the Arabic user's experience, end to end' (rendering side) and §6 disclaims the native side generally, but this is an `app.json` change — i.e. exactly the U14 class the report itself argues must all land in ONE `eas build`. Omitting it forces a second production build.
- THE LEGACY 6-STEP `OnboardingScreen.tsx` WAS EXCLUDED ON A PREMISE THE LAUNCH PLAN BREAKS. §6 excludes it as 'unreachable at CANARY_NEW_ONBOARDING_PERCENT=100' (verified: `features.ts:30`). But CLAUDE.md's own soft-launch procedure says to drop that constant to 10 and ramp 10->50->100 before App Store launch — making a 413-line, unreviewed flow the signup path for ~90% of new users at exactly the moment the store gates matter. (I checked its enums are still backend-valid — `PRIORITY_OPTIONS`/`BUDGET_OPTIONS` at `OnboardingScreen.tsx:46-51` are subsets of `auth_routes.py:135-143` — so this is a coverage gap, not a known defect; nobody swept its screens, analytics, RTL or error paths.)
- NO OFFLINE / CONNECTIVITY-STATE CELL IN ANY LANE. There is no `NetInfo` dependency and no offline state anywhere in the client (grep over `src`, `App.tsx`, `package.json` = 0). The failure-classification work (M21 W2) classifies errors AFTER a request fails; nothing enumerates cold-start-with-no-network, a compare submitted offline, or the boot refresh under airplane mode — which is the same code path as §0.3's fleet-logout risk. Not listed in §6's not-covered either.
- GOOGLE PLAY STORE-SIDE GATES ABSENT. §1.5 enumerates Apple gates only (ASC labels, iOS privacy manifest, applinks). The Play Data Safety declaration, the Play 'Teen' rating questionnaire and the Android permission declaration (the same `android.permissions` RECORD_AUDIO row U14 removes) are never enumerated, although `assetlinks.json` and the Android bundle are treated as first-class elsewhere in the report.
- THE TWO-LEVER AXIS IS UNRESOLVED FOR 119 OF 142 ROWS. §6 admits 'the two-lever gap was not re-derived per finding' — only the reconcile and two-lever lanes diffed against `97b5f15`. Since §0.2 forbids calling any mobile finding 'fixed for users', every P2/P3 row is left without the one tag ('on phones too?' / 'main-only?') that decides whether it needs the OTA or a code unit. That is a whole deliverable axis pushed onto the reader, not a sampling gap.

## unverified_claims
- §1.6 item 8's headline impact number is labelled MEASURED but is probe-contaminated and I falsified its detail today. Report: 'MEASURED against the real error corpus, ~1,500 of 3,254 failures land on strings like "We couldn't finish this comparison in time. Try again."'. My read-only Supabase SELECT (2026-09-07): `search_logs` total 16,284; failures 3,316; only ELEVEN distinct `error_message` values, of which the quoted timeout copy is 209 (English prose in aggregate is 3,094, dominated by 'Still gathering prices' 1,288 and "We don't compare this category" 721). Decisively: 3,039 of 3,316 failures (91.7%) come from 13 query strings — 'Carrier 1.5T AC vs LG 1.5T AC' x225, 'glock 19 vs iPhone' x112, 'glock 19 vs ar-15' x76, a prompt-injection probe x75, 'asdf vs qwer' x42. The campaign's own accepted correction (PO-RECORDED-MEASURED-01, Fable AGREE: 'every number that came from search_logs must be re-stated on the organic subset') is never applied anywhere in this report; the honest organic failure count is roughly 277, not ~1,500, so MB-I18N-RTL-05's user-reach evidence is ~5x overstated.
- 'Biggest verified size win: the lucide barrel' (§1.4 heading) is self-contradicting: two lines later the same section says the saving is 'arithmetic on the measured attribution ... not a measured second build'. The attribution inputs check out (I confirmed 34 barrel import sites, 1,704 icon files on disk, no `metro.config.js`, and `lucide-react-native@1.14.0` exports map = ['.', './icons'] exactly as claimed) — but 'verified' is the wrong word for the delta, and U11's exit gate is the only thing that would make it true.
- '242/245 jest suites green' (§1.1b) versus §6's '245 suites, 2,320 passed ... 3 skipped suites' — skipped is not failing, and the report never reconciles the two phrasings. It compounds the acknowledged §3 problem that two coverage percentages (76.09% vs 84.26%) disagree, so no number in the tests-truth lane can be quoted as a floor.
- 'Phones run 97b5f15' underpins the whole §1.1(a) column and every 'not fixed for users' statement, and §3 concedes it comes from the handoff, not `eas update:list`, AND that the earlier cert-pin OTA has no recorded group id. If that OTA never fired, devices are older than the bundle actually analysed and §1.1(a) describes a bundle nobody runs.
- The two silent first-launch risks in §1.2 are derived, never executed: (2) the Hermes install branch of `intl-pluralrules` (`src/i18n/index.ts:7`) is explicitly 'device-unverified' because jest takes the Node branch, and (3) the 'order-dead' Sentry alarm is inferred from Babel require-hoisting plus a Proxy that throws synchronously. Both are asserted as ship risks with no runtime reproduction and no red test in U8 that would fail today on a real release bundle.
- The §1.5 npm-audit row ('42 rows / 1 critical + 19 high', 'only 3 reach the device JS bundle') is never reconciled against CLAUDE.md's recorded measurement of 43 rows / 21 high (2026-09-01), and the 'reaches the device bundle' split is a dependency-graph reading, not a check against the exported bundle or source map — which the lane had in hand from its three `expo export` runs.

## wrong_or_stale_claims
- '3 are already fixed on the other session's unmerged `feature/m23-mobile-w1`' (report line 10, repeated in §3's coordination bullet) is WRONG — it is 4. FABLE_REVIEW_NOTES records MB-FLOWS-STATE-06 as 'AGREE P1 -> OTHER-SESSION (B9 `7863bd3`, landed 2026-09-07 during this session)', and the report's own preamble makes those notes the status of record. I verified: `git cat-file -t 7863bd3` = commit, `git branch -a --contains 7863bd3` = `feature/m23-mobile-w1`, message 'fix(mobile): B9 — persist the Step-17 notifications answer instead of dropping it'. §5's verdict table applies the OTHER-SESSION verdict to MB-FLOWS-STATE-05, MB-RECONCILE-03/-05, MB-STARTUP-BUNDLE-02 and MB-TESTS-TRUTH-04 but silently omits it for MB-FLOWS-STATE-06.
- U9 ('The onboarding Step-17 answer is recorded') IS A REDUNDANT UNIT — both of its red tests are already green on that branch. `git show 7863bd3` fixes BOTH halves the unit names: the stale closure (`handleNotificationsDone` builds `const merged = {...data, notifications_enabled: granted}` and calls an explicit `advance(nextData)`) AND the transport (`buildPreferencesPayload` now sets `payload.notifications_enabled` guarded on `typeof === 'boolean'` so an explicit 'Not now' is sent). Scheduling U9 would duplicate landed work and collide on `OnboardingFlow.tsx`/`onboardingDraft.ts` — the exact cross-session collision the campaign is trying to avoid.
- §5 downgrades MB-FLOWS-STATE-03 to P2 with the rationale 'nothing is purchasable — the trial CTA and Restore are the same "Coming soon" stub — so it is a dead control on a placeholder screen, not a submission blocker'. The rationale is true and verified (`PaywallScreen.tsx:236-243`, `:406`) but it is used to shrink a finding while the larger fact it establishes — the app has no purchase path at all — is never filed as a finding, never entered in the §1.5 gate table and never assigned to a unit or to Ahmed. A downgrade swallowed the bigger item.
- The §1 arithmetic no longer holds and never matched the unit table: '20 distinct P1s: 3 other-session + 1 product call + 4 Ahmed-only, leaving 12 engineering units'. With the correct 4 other-session rows it is 11, and the §2 table actually proposes 14 units (U1-U14) spanning P1s plus P2/P3 packs — three different counts of 'the work' that the report never reconciles.
- VERIFICATION NOTE (not a defect): I re-opened 16 cited `file:line` pairs at HEAD and every one held — `url_extraction_service.py:575/:583` (the un-unpacked tuple), `extraction_service.py:2543/:2547` (both return paths tuples), `image_routes.py:89`, `feedback_routes.py:37-39` vs `FeedbackCard.tsx:18-22`, `App.tsx:173-177/:212-227/:283-297`, `api.ts:22/:43/:45/:47/:86-95`, `authService.ts:287-289`, `auth_routes.py:537-543`, `auth_service.py:49-53`, `useComparisonCounter.ts:69-82`, `referral_service.py:50`, `app.json:21-41/:71-74`, `fonts.ts:11-17/:40-44` (fontFamily.ar zero consumers), `ci.yml:270` (`eslint "src/**/*"` excludes App.tsx), plus the git measurements (72 files/+5,329-605; empty app.json/eas.json diff; two-line package.json delta; `d24d6e5` NOT an ancestor of `97b5f15`). The evidence base is sound; the defects above are of omission and of failure to apply the orchestrator's own notes, not of fabricated citations.