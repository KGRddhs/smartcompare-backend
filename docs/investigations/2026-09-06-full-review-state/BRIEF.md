# FULL REVIEW CAMPAIGN 2026-09-05 (internal workflow ids m22-*; NOT the other session's feature/m22-price-truth-cluster) — shared agent brief. Durable copy: docs/investigations/2026-09-06-full-review-state/ (baseline/ lives next to it)

You are one lane of a multi-workflow review of **SmartCompare / Qaren** (FastAPI backend + Expo RN app).
Repo (read-only for you): `C:\Users\SynAckITPC\Documents\AI\sc-scraper-proof` — **HEAD `76ace90` == `origin/main`**, clean tree.
Prod: `https://web-production-58776.up.railway.app` (Railway service `web`, 1 replica, `uvicorn app.main:app` — 1 worker, no `--proxy-headers`). `/health` 200 at review time. Deployed SHA = `76ace90`.

## HARD RULES (violating any = your output is discarded)
1. **READ-ONLY.** Do not edit, commit, stash, checkout, reset, or create worktrees in the repo. Do not write to prod Supabase/Redis/Railway. Do not call OpenAI/Serper/Firecrawl/Scrape.do. Do not run any load test or send more than a handful of requests to prod. Reading prod (`GET /health`, cached `GET` reads, Supabase `SELECT`, Railway MCP read tools, `railway variables`) is allowed.
2. **Evidence at HEAD `76ace90`.** Every finding cites `file:line` you actually opened at this SHA. Reproduce through the runtime function/module the orchestrator calls, not a lookalike. If you cannot reproduce, say so and rate the claim's confidence.
3. **Do not re-find what is already known.** Dedupe against: `docs/investigations/2026-09-01-m18-verified.json` (175 rows, M18, base `593ec1e`), `docs/investigations/2026-09-01-m18-product-app-load-review.md`, `docs/investigations/2026-08-31-m13-full-review.md` (M13, 131 findings), `docs/investigations/2026-09-02-mobile-checkup-findings.md` (17 verified + leads, at `79a4594` = PRE-M21 — reconcile, don't repeat), and the 33 open GitHub issues (list below). A known finding may be REPORTED ONLY as `status: still-open|regressed|fixed` with fresh evidence; new findings must be genuinely new.
4. **Coverage-driven, not hypothesis-driven.** ENUMERATE the surface (every route / every screen / every flag / every prompt / every category) then check each cell. List what you did NOT cover.
5. **Never trust the IDE/LSP diagnostics or CLAUDE.md status claims** — ground truth is code at HEAD, `npx tsc --noEmit` exit code, jest, pytest output you ran.
6. **Local pip env is OFF-LOCK** (fastapi 0.115.0 vs lock 0.141.1; starlette 0.38.6 vs 1.6.0; openai 2.21.0 vs 3.3.1; pydantic 2.7.0 vs 2.13.4; httpx 0.27.0 vs 0.28.1; supabase 2.28.0 vs 2.31.0). A local pytest result certifies nothing about CI/prod on framework-introspection or SDK-shape questions; say "local, off-lock" on any such claim. Free-tier pytest = `python -m pytest <files> -m "not (live_unit or live_db or integration)" --timeout=60 -q` (conftest strips credentials; `LIVE=1` restores). NEVER run the whole suite (~15,900 nodes) in a finder — targeted files only. Known-red baseline (11 nodes) = `tests/.pre_impl_failures.txt`; CI is green ONLY because it deselects those.
7. **Windows:** pass `encoding='utf-8'` on every file read/subprocess; never `print()` non-ASCII — write to a UTF-8 file and read it back.
8. Return **structured output only** via the schema you were given. No prose preamble.

## STATE THAT DECIDES YOUR METHOD
- **OpenAI is DEAD**: 1-token probe at review time → `429 insufficient_quota / credit_balance_exhausted`. Serper account balance is negative (-50). **No live compare is possible.** Product-output lanes score RECORDED outputs + fixtures + prompt/rubric code paths, and must say so per claim.
- **Recorded outputs available:** Supabase `comparisons` = 27 rows, ALL `created_at ≤ 2026-06-21` (mid-June engine; 0 rows since 2026-08-01). `search_logs` = 16,283 rows through 2026-09-02 (columns include `success`, latency, error text — a MEASURED outcome series; recent rows are `success=False` because of the OpenAI outage). Read-only Supabase access: python `supabase.create_client(os.environ["SUPABASE_URL"], os.environ["SUPABASE_SERVICE_KEY"])` after `load_dotenv(".env", override=True)` in the repo root. SELECT only.
- **Offline corpora / fixtures:** `tests/fixtures/*` (54 entries incl. `comparison_baseline_d2*.json` recorded payloads, `*_flag_off_golden.json`), root `full_response.json`, `tmp_result*.json`, `session23_test_response.json`, `_proof/` (414 cached PDP pages; `python _proof/sweep2.py curl_cffi 6 BHD` is a zero-network price sweep, ~100s). The deterministic judge in `scripts/eval_runner.py` is importable and scores a payload offline.
- **Prod flag state (Railway `web`, non-secret knobs)** is captured in `baseline/railway-web-vars.txt` next to this brief; code defaults are in `CLAUDE.md` → *Environment Variables → Feature Flags*. When the two differ, PROD WINS for "what ships today"; report both.
- **Measured prod (Railway MCP, last 24h):** CPU avg 0.0016 vCPU, RSS 0.1356 GB flat, network ~0, 16 HTTP requests sampled (all 2xx, p50 8ms / p95 22ms) — i.e. effectively zero traffic. Latest deployment `e6ebe368…` SUCCESS at `76ace90`. Railway retains logs per deployment; older deployments are REMOVED.
- **Mobile at HEAD:** `npx tsc --noEmit` exit **0** (measured at review time; `baseline/tsc.txt`). Jest/eslint/npm-audit baselines land in `baseline/` (read them if present; otherwise measure yourself — `node_modules` IS installed, 693 entries).
- **Two-lever gap:** phones run the `preview` channel OTA published from `97b5f15` (2026-09-02). Since then **6 commits / 38 files / +2,275 −506 under `SmartCompareApp/`** are on main but NOT on any phone (`git diff --stat 97b5f15..76ace90 -- SmartCompareApp/src SmartCompareApp/App.tsx`). "What users see" = `97b5f15`'s bundle; "what main says" = `76ace90`. Always state which.
- **Code delta since the last full review (M18 base `593ec1e` → `76ace90`):** 59 commits, 190 files, +23,586 −1,227. Biggest source deltas: `scoring_service.py` +600, `structured_comparison_service.py` +419/−97, `fact_check_service.py` +379, `HistoryScreen.tsx` +373/−272, `api.ts` +322/−67, `onboardingDraft.ts` +302 (new), `response_builder.py` +294, `usage_service.py` +293/−52, `price_service.py` +137, `extraction_service.py` +136, `home_routes.py` +117, `text_routes.py` +73, `migrations/036_home_savings_aggregate.sql` (new, UNAPPLIED, like 033/034/035), `.github/workflows/ci.yml`, `app/utils/prompt_sanitizer.py` (new), `app/services/text_sanitize.py` (new), `app/services/model_config.py` (new). PRs #122–#132 (M20 W1/W2, fact-check, mobile, load+scoring, M21 W1–W4, docs).

## WHAT PRIOR REVIEWS ALREADY ESTABLISHED (build on, don't repeat)
- M13 (full code, `daa1a42`): 0 P0 / 17 P1 (all fixed in 4 waves) / 74 P2 / 40 P3. Themes: fixed-once-never-propagated; docs-as-wrong-truth; currency-is-a-label; blocking work on the single loop; green-that-means-nothing.
- M18 (`593ec1e`): 175 confirmed (0 code P0, 28 P1 — 27 closed, `#114` held by design). LOAD ANSWER: ~10 compares/min DEPLOYMENT-WIDE because every slowapi limit is one bucket on the shared Railway edge IP; single event loop; OpenAI Tier-1 TPM; 40-thread executor; Upstash command budget. **Ordering rule: the mis-keyed limiter currently PROTECTS the loop — offloads must be ACTIVE before the limiter key changes.** All concurrency numbers MODELLED; Upstash RTT from Railway NEVER measured.
- M18 product-output: winner card could praise the loser (fixed #99/#110), `_PRICE_TRUST_SET` gaps (fixed #104), spec magnitude summing (#100, dark flag), MISSING_SCORE=50 (#101, dark flag, product call pending on win_margin 0.0), fact-check ceremonial (#106–#109 dark flags + honest-absence dark), Arabic requests get ENGLISH verdict text (PO-verdict-text-02 — check whether anything changed), prompts push false confidence.
- Mobile checkup (`79a4594`, pre-M21): P1s still believed live — camera `/image/identify` unmetered for authed users (`image_routes.py`), SSE progress dead in shipped client (fake 4.5s checklist vs ~30s backend), boot blocks splash on a 120s token refresh (`App.tsx`), compare loader no cancel/watchdog; leads — lucide barrel ≈1.27 MB/25% of an 8.2 MB bundle, `POST /url/compare` unmetered+no history, force-update wire dead, dead animations, `from_history` param never read. Findings against `HistoryScreen.tsx`/`ResultsScreen.tsx`/`ProfileScreen.tsx`/`api.ts`/`authService.ts`/`HomeScreen.tsx` are M21-SUSPECT — re-verify at HEAD.
- Two live breakages fixed 2026-09-02: all mobile analytics/feedback 422'd silently pre-M21-W1; referral landing read a phantom `response_data` column. Pre-2026-09-02 event/feedback/referral volumes are NOT trustworthy.
- Config landmines FIXED: `web SERPER_LIFETIME_LIMIT=0`, warmer OFF + off push-trigger. `#89` NOT done (11 nodes deselected). Migrations 033/034/035/036 written, NOT applied. M13-29 RLS migration never landed.

## OPEN GITHUB ISSUES (33) — cite `#N` when your finding matches one
#128 wire refund_anon_comparison_credit before ENABLE_ANON_USAGE_GATE · #114 limiter key anchor (held by design) · #96 live-suite empty OPENAI secret · #94 registry wrong 4 rows · #93 sephora curl re-test · #89 fix 11 baseline nodes · #81 /health probes · #80 cron failure visibility · #79 pharmacy hostname match (security) · #78 nasser variant guard · #77 shopify pagination · #76 wire virginmegastore/Alshaya/Algolia · #75 sitemap channel activation · #74 bound behavior-profile recompute · #73 pooled httpx + bound GCC endpoint · #72 rate limits keyed on user + shared storage · #71 Redis/Supabase off loop · #70 curl pool + fan-out cap · #69 url/quick failures via D2 helper · #68 fire-and-forget failures → Sentry · #67 warmer stale method set · #66 admin cost meters wrong · #65 behavior profile reads phantom columns (personalization DEAD) · #64 SSE analytics/quota on success flag · #63 /price-kpi auth+meter · #61 Firecrawl cap/half-open/lossy breaker · #58 model ids config · #57 L2 promote freshness · #56 category into _get_price · #55 admin flush keys · #54 Tier-3 clobber · #53 nogenuine sentinel · #52 iHerb currency tautology · #51 converted_usd stamp.

## OUTPUT CONTRACT (all lanes)
Each finding: `id` (LANE-NN), `title` (≤120 chars, states the DEFECT not the topic), `sev` (P0 = live user harm/security/spend leak now; P1 = real defect reachable by users or blocking a launch step; P2 = measurable quality/cost issue; P3 = nit/latent), `file`, `line` (int at HEAD), `evidence` (what you ran/read, quoted ≤300 chars), `repro` (command or steps; or "static" with why), `impact` (who/what, quantified where possible), `fix` (concrete, one paragraph, names the flag if it must ship dark), `test_first` (the red test that would pin it), `measured_or_modelled`, `dedupe` (`new` | `known:<M18/M13/mobile/#issue id>` + status), `confidence` (0-1), `m21_touched` (bool: file changed in 593ec1e..76ace90).
Also return `coverage` (what you enumerated and checked, with counts) and `not_covered` (what you skipped and why).
