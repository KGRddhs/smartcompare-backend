# Fable review gate — M22 P0/P1 set (2026-09-06), base 76ace90

Each verdict below is mine, re-derived against code I opened (not the agents' prose). "AGREE" = severity and mechanism confirmed by my own read.

| id | agent sev | Fable verdict | my evidence / correction |
|---|---|---|---|
| LS-REQUEST-PATH-BLOCKING-01 | P0 | **AGREE P0 (live, unauthenticated DoS lever + per-compare loop tax)** | `app/utils/url_validator.py:30` `socket.getaddrinfo(hostname, None)` inside a sync `def`; no async variant exists (`grep async def validate_external_url` = 0). Called inline from `async def` at `url_routes.py:91/112/148/175/197/216` (no auth dep, `@limiter.limit("10/minute")` on the shared edge-IP bucket), per redirect hop at `url_extraction_service.py:101/111`, and on the PRICE PATH at `price_service.py:14126` + `:14147` inside `async def curl_fetch_html_same_site` (the curl GET is `to_thread`'d at `:14135`, the resolve is not) and `shopify_pdp_service.py:284` (`_hop_is_allowed`, sync, from async `_fetch_once`). glibc does not negative-cache, so every unresolvable host costs the full resolver timeout on the loop. **Fix constraint I add:** `loop.getaddrinfo` uses the default executor = the M13-34 40-thread pool shared with the adapter fan-out; `asyncio.wait_for` cancels the await but not the libc call → zombie threads. Use a DEDICATED small resolver pool (or dnspython/aiodns with a real timeout), positive+negative result memo (60s), and keep the sync shim for the warmer/CLI. Unit must land BEFORE #114 (re-keying raises the per-caller ceiling on this route). |
| LS-REQUEST-PATH-BLOCKING-02 | P1 | **AGREE P1** | `auth_service.py:30/:37` build a fresh `create_client` per call; `:256-257` constructs OUTSIDE the `run_db` lambda so neither offload flag moves it; `database_service.py:52` `get_user_supabase_client` also per call (admin memoized at `:38-45`). The 355 ms figure is dev-box Windows (measured by finder+verifier); Railway Linux MODELLED 20-60 ms — still the largest per-request loop cost no flag removes. **Fix constraint:** never share one anon client with per-call `postgrest.auth(token)` (JWT bleed across concurrent requests) — use a keyed pool or a request-scoped wrapper over a shared httpx transport; pass `ClientOptions(postgrest_client_timeout=...)` in the same unit (see 04). |
| LS-REQUEST-PATH-BLOCKING-03 | P1 | **AGREE P1** | `cache_service.py:28` `Redis(url=…, token=…)` with library defaults. Verified on installed upstash-redis 1.6.0 (`Redis.__init__` signature `rest_retries=1, rest_retry_interval=3`; `http.py` `httpx.Client(timeout=None)` + `time.sleep(self._retry_interval)`); lock pins 1.7.0 (verifier read the pinned wheel: same). `ENABLE_ASYNC_REDIS_OFFLOAD` absent in prod → every command inline. Fix (bounded transport + retries=0) is safe: every helper is fail-open. Hard precondition for the offload flag too (a hung command parks an executor thread forever). |
| LS-REQUEST-PATH-BLOCKING-04 | P1 | **AGREE P1** | Zero `ClientOptions`/`postgrest_client_timeout` in `app/`; postgrest default 120 s (verifier: pinned postgrest 2.31.0 `DEFAULT_POSTGREST_CLIENT_TIMEOUT = 120`; runtime probe `Timeout(timeout=120)` vs GoTrue 5 s). 120 s > 30 s healthcheck, one worker. Caveat accepted: an 8 s bound can cut the LIVE unbounded `/home/savings` scan at scale → land with/after #116 or bound that route separately. |
| MB-RECONCILE-01 | P1 | **AGREE P1 (known still-open, checkup A#1)** | `image_routes.py` has zero `consume_comparison_credit`/`record_lifetime_comparison`; only gate `:89 if anon_usage_gate_enabled() and not user` (flag OFF in prod). Backend-only → same on phones and main. Latent today (11 camera rows lifetime) but a freemium bypass the moment scan is promoted. Land with MB-RECONCILE-07 as ONE metering unit (mirror `text_routes.py:193/247/278`). |
| MB-RECONCILE-03 | P1 | **AGREE P1** | `App.tsx:189 await initializeAuth()` → `authService.ts:442 await refreshSession()` → `api.post('/api/v1/auth/refresh')` with no per-call timeout → axios `timeout: 120000` (`api.ts:26`); render gate `:274`. Cached-user fallback `:454` reached only after the call settles. On phones and main. Fix: boot from cached user, background refresh, sessionEvents already provides the path back to Auth. |
| MB-RECONCILE-05 | P1 | **AGREE P1 (dead taps on two showcase surfaces)** | `HomeScreen.tsx:860` and `ProfileScreen.tsx:416` navigate with `{ from_history }`; route type `types.ts:590-594` has no such key; `grep from_history src/` = 2 writes, 0 reads; `ResultsScreen.tsx:129-130` discriminator false → empty state. tsc silent via `as any` (Home) and `navigation: any` (Profile). One-line fix each + a route-contract test. OTA-safe. |
| MB-RECONCILE-06 | P1 | **AGREE P1 — and it is the single highest-value action available** | `git merge-base --is-ancestor 97b5f15 d24d6e5` TRUE: the phone OTA predates ALL FOUR M21 waves incl. W1. So ON PHONES TODAY: every Results analytics batch and feedback submission still sends `metadata.query` as `comparison_id` (`97b5f15:ResultsScreen.tsx:331`) and 422s silently; the freemium gate is the M13-14 formula `used < lifetimeFree + bonusRemaining` which dead-ends EVERY free user at 3 lifetime while the backend grants daily 3 / monthly 10 beyond it. Verifier correction accepted: the referral phantom-column fix is server-side and IS live. **Consequence for reporting: no mobile finding may be called "fixed for users" until `eas update --branch preview` from ≥76ace90 lands** (Ahmed-gated; native-compatible per the two-lever lane, pending). Add the CI guard (published-channel commit vs main) as a unit. |
| MB-RECONCILE-07 | P2→P1 | **AGREE P1** | `url_routes.py:34-38` model has no `selected_category` (pydantic default extra=ignore drops the chip sent at `HomeScreen.tsx:433`); `:128 compare_urls` has no user dep, no consume/refund/record, no `save_comparison` (grep = 0). Data loss (URL compares never in History) + server-side freemium bypass + local counter drift (`HomeScreen.tsx:440 increment()`). |
| PO-PROMPTS-11 | P1 | **AGREE as STATUS P1 (known, gated)** | `extraction_service.py:614-615` (+ `:827`, `:832`) license training-data fill in the SHIPPED prefix; `ENABLE_SPECS_NO_FABRICATION` absent in prod; `:1517` stamps `N/A`; cached 7 d. No code change; ordered activation after Serper is healthy (with #101 and the never-empty-cons rule in the same window). |
| CR-DELTA-CORRECTNESS-07 | P3→P2 | **AGREE P2, flag as LIVE regression from M21 W3** | brand-dedup applied to `_display_product_names` but `compute_scores` (`scoring_service.py:1554-1557`) still builds raw `f"{brand} {name}"` for `dimension_winners`; `compute_tradeoff_pairs` matches winners against the DEDUPED names → `tradeoffs` collapses to `[]` and `key_tradeoff` blanks on the hard-cap deterministic verdict whenever the product name repeats the brand (fragrance house names do this constantly). Unflagged, live on every such compare since `eb331cb`. Unit: route `:1554` through `text_sanitize.dedup_brand_name` + pin `dimension_winners` keys == `winner_evidence` spelling. |
| CR-DELTA-CORRECTNESS-09 | P3→P2 | **AGREE P2 (canary note, no code)** | Half A (`text_routes.py:630` `complete_after_client_gone` refund) is UNFLAGGED and live; Half B (`ENABLE_PREVERDICT_DISCONNECT_ABORT`) dark → a pre-verdict disconnect refunds the credit while the full OpenAI/Serper tail still runs. This was the documented design (CLAUDE.md M18 CD-interactions-01) — keep, but the canary baseline for free-credit burn must be re-taken post-M21. |

## Cross-cutting conclusions so far (pending the remaining lanes)
1. **Load: the loop is blocked by things no flag removes** — DNS resolve (01), Supabase client construction (02), unbounded Upstash/postgrest timeouts (03/04). These four are the true W0 of the load plan and must precede any offload-flag canary AND #114.
2. **Mobile: the OTA gap is wider than every prior handoff recorded** (W1 missing too). Users today still hit the 422 analytics/feedback black hole and a wrong paywall. `eas update --branch preview` is the top Ahmed action.
3. **Metering has two unmetered paid doors** (camera, URL) — one unit.
4. **Product output:** the shipped specs prompt still fabricates by design until Serper returns; one LIVE regression (tradeoffs collapse) came from M21 W3's brand dedup.

---

# SESSION 65 (2026-09-07) — Fable verdicts on the REMAINING P0/P1 set, from my own code reads at `76ace90` (worktree `sc-review-s65` @ `67e9e26`, code byte-identical)

Legend: **AGREE** = mechanism + severity confirmed by my read; **DEDUPE→id** = same defect as another row, report once; **STATUS** = known item, fresh evidence only; **OTHER-SESSION** = already fixed on `feature/m23-mobile-w1` (unmerged, unreviewed) — do NOT file a unit, re-verify after their merge; **↓P2** = I downgrade to P2 on the brief's scale (P1 = reachable user defect or launch blocker).

## Load / scale
| id | verdict | my evidence / constraint |
|---|---|---|
| LS-RATELIMIT-KEY-01 | **AGREE P1** | `rate_limiter.py:125-129` builds `Limiter(key_func, storage_uri, default_limits)` with no `key_style`; installed slowapi defaults `key_style="url"` and sets `_endpoint_key = request["path"]`, so every parameterised route gets one bucket per concrete path. Fix `key_style="endpoint"` is independent of the #114 key_func change and must land WITH or BEFORE it (otherwise #114 re-keys per client but still per URL). |
| LS-RATELIMIT-KEY-02 | **AGREE P1**, DEDUPE→CR-SECURITY-06 | `text_routes.py:759-798` `GET /prices/{product}`: `@limiter.limit("20/minute")`, no user dep, no usage call, `await get_regional_prices(...)` = 6 `_get_price` fan-outs into the paid cascade; the per-path bucket above makes the limit per product string. One unit: optional-user + metering (or admin-gate) + route-keyed limit. |
| LS-CACHE-REDIS-01 / LS-CONCURRENCY-LIMITS-04 | DEDUPE→LS-REQUEST-PATH-BLOCKING-03 | Same `cache_service.py:28` defect (timeout=None + sleep(3) retry). One unit. |
| LS-CACHE-REDIS-02 | **↓P2** (unit stays, load W1) | Verified: zero singleflight symbols in `app/`; `_get_specs` (`scs.py:5225-5240`) goes cache→L2→resolve with no reservation. It is cost amplification under concurrency, not a user-visible defect; the brief's scale makes that P2. Keep as a W1 load unit (per-key asyncio Future, in-process). |
| LS-CACHE-REDIS-03 | **AGREE P1 (security)** | `auth_service.py:284-294` `logout_user`: `_revoke_token` (Redis, 1 h) then `get_auth_client().auth.sign_out()` on a FRESH anon client with no session — gotrue sends no revoke without a session, so the refresh token stays valid indefinitely. Fix needs the client to POST the refresh token to `/auth/logout` (OTA-safe client change + backend `set_session` + `sign_out(scope='global')`). |
| LS-CONCURRENCY-LIMITS-01 | **AGREE P1** | `def extract_price_from_html` (`price_service.py:12987`, sync) called bare at `:14090` inside `async def fetch_page_price` (`:14060`); only the curl GET is `to_thread`'d. Unit with CR-PERFORMANCE-01 (parse once, then offload the parse). Price path → flag-OFF byte-identity gate applies (offload does not change the value; soup reuse must be pinned by the golden fixtures). |
| LS-CONCURRENCY-LIMITS-02 | **↓P2 pending vote** | `scs.py:5865-5885` fires every direct-adapter gather at once; `executor.py` sizes 40 for "~18". Capacity/queueing under one cold fragrance compare, bounded by the per-adapter timeouts — quality/cost, not a reachable defect. Semaphore unit in load W1. |
| LS-FAILURE-MODES-COST-01 | **AGREE P1 (LIVE SPEND)** | `brightdata_service.py:60-95`: `_bd_budget_precheck()` returns True and `_bd_record()` is a no-op when `ENABLE_BRIGHTDATA_BUDGET_GATE` is unset; `baseline/railway-web-vars.txt` has `ENABLE_BRIGHTDATA_FALLBACK=true` + key + zone on BOTH web and price-warmer and no gate var, while `SERPER_LIFETIME_LIMIT=0` sends every search leg to Bright Data. Fix is a Railway env flip (`ENABLE_BRIGHTDATA_BUDGET_GATE=true` on both services) — Ahmed/ops, first item of the activation runbook, no code. Pairs with CR-CLAUDE-CONFIG-02 (docs say dormant). |
| LS-FAILURE-MODES-COST-02 | **AGREE P1 today** | `scs.py:3250-3266` `elif explicit_pair:` builds products deterministically and never calls `parse_product_query`; nothing checks OpenAI health before the Phase-1 gather, so with OpenAI at 429 every app compare spends the Bright Data / render cascade and then fails. Unit = OpenAI breaker (with -03), default-OFF flag. |
| LS-FAILURE-MODES-COST-03 | **AGREE P1** | `api_budget_service.py` `PROVIDER_CONFIGS` = firecrawl/scrapedo/brightdata/serper/youtube — no `openai`; no `is_circuit_closed("openai")` anywhere. Same unit as -02. |
| LS-FAILURE-MODES-COST-05 | **↓P2** | Verified: one `@app.on_event("startup")` (`main.py:168`), zero shutdown handlers, uvicorn started without `--timeout-graceful-shutdown` (`railway.json:7`, `Procfile:1`), refund is `fire_and_forget` in the SSE `finally` (`text_routes.py:685-689`). Real but latent: it burns a credit only for compares in flight at deploy time, at ~0 traffic. Unit: tracked background-task set + bounded drain + `--timeout-graceful-shutdown 20`. |
| LS-MEASURED-EVIDENCE-01 | **AGREE — P1 as EVIDENCE** | Upstash REST RTT from the prod container MEASURED 178 ms median (n=10), Supabase SELECT ~285 ms; both offload flags UNSET. Not a code defect; it is the load-model input M18 lacked. Feeds the runbook: offloads ON (after W0) before #114. |

## Code review
| id | verdict | my evidence / constraint |
|---|---|---|
| CR-CLAUDE-CONFIG-02 | **AGREE P1 (docs truth)** | `CLAUDE.md:328` says the Bright Data trio is "default OFF/unset — dormant"; prod has it ARMED on both services. Fix in this session's docs PR; keep with LS-FAILURE-MODES-COST-01. |
| CR-CLAUDE-CONFIG-11 | **AGREE P1 (propose-only)** | `.claude/settings.local.json` IS tracked; `permissions` has only `allow` (192 entries) incl. three `shutdown /s` variants, `PowerShell(Stop-Computer -Force)` and `Bash(git *)`. Per the orchestrator skill the config audit is propose-only: Ahmed deletes the six destructive entries and splits `git *`. |
| CR-DATA-MIGRATIONS-05 | **STATUS P1 (#65 still open)** | Fresh evidence accepted (42703 on two phantom columns; 0/18 users with a profile). Unit already tracked by #65. |
| CR-PERFORMANCE-01 | **AGREE P1** | Same site as LS-CONCURRENCY-LIMITS-01; one unit (soup once + offload). |
| CR-PERFORMANCE-03 | DEDUPE→LS-REQUEST-PATH-BLOCKING-02 | Same per-request `create_client` defect. |
| CR-SECURITY-01 | **AGREE P1** | `migrations/010:70` and `025:28` create `delete_user_cascade` SECURITY DEFINER with no REVOKE (Postgres grants EXECUTE to PUBLIC by default); only `036:94-95` shows the REVOKE/GRANT precedent. Migration 037: REVOKE from PUBLIC/anon/authenticated, GRANT to service_role, for `delete_user_cascade`, `increment_lifetime_comparisons`, `cleanup_expired_ratings`; plus a source test over every SECURITY DEFINER in `migrations/`. Not P0: the anon key is server-side only (verifier confirmed it is absent from the app bundle). |
| CR-SECURITY-02 | **AGREE P1 (live state ≠ migration)** | `010:12` enables RLS and `:58` defines `events_select` on `auth.uid() = user_id`, yet the anon key reads all 146 rows live. Repair belongs in the same migration 037 (`ENABLE`/`FORCE ROW LEVEL SECURITY`, drop stray policies) + a `live_db` matrix pin. |
| CR-SECURITY-03 | **AGREE P1** | `_before_send` (`sentry_service.py:128-190`) scrubs only `exception.values[].value`, breadcrumbs, request headers/url/query_string; `sentry_sdk.init` sets `send_default_pii=False` but leaves local-variable capture at its default (on), so frame `vars` (`token`, `expected`), `request.data`, `cookies`, `extra`, `contexts`, `logentry` all ship raw. |
| CR-SECURITY-04 | **AGREE P1** | `admin_routes.py:34-38` `hmac.compare_digest(x_admin_key, expected)` on `str` raises `TypeError` for any non-ASCII header; `ErrorHandlerMiddleware.dispatch` (`error_handler.py:134-162`) turns it into a 500 AND `sentry_sdk.capture_exception(exc)` — with -03 that ships `expected` = the production ADMIN_API_KEY to Sentry on an unauthenticated request. ONE security unit with -03: byte-safe compare + whole-event scrub. |
| CR-SECURITY-05 | DEDUPE→LS-REQUEST-PATH-BLOCKING-01 (P0) | Same `url_validator.py:30`. |
| CR-SECURITY-06 | DEDUPE→LS-RATELIMIT-KEY-02 | Same route. |

## Product output
| id | verdict | my evidence / constraint |
|---|---|---|
| PO-CATEGORIES-I18N-01 | **AGREE P1** | `extraction_service.py:1096-1097` maps `tablet`/`tablets` → electronics in `_CATEGORY_SYNONYMS`; the explicit-pair path (the app's only path) classifies deterministically (`scs.py:3258`) so every "X tablets vs Y tablets" pharmacy pair is scored as electronics. Flag the change (category drives price + scoring). |
| PO-CATEGORIES-I18N-03 | **AGREE P1** | `content_blocklist.json:53` still carries the bare Arabic `أفيون` while the EN side (`:42-45`) was tightened to multi-word forms in `9f6e498`. Arabic YSL Opium queries are hard-blocked. Data-file change, unflagged by nature; red test first. |
| PO-FACTCHECK-CONFIDENCE-01 | **AGREE P1** | `_score_reliability` (`scoring_service.py:1958-1976`) returns exactly 0.3 for any all-unverified fact_check (+0.1 on `price_verified`), so the reliability-fed dimension is a constant tie on warm traffic. Fix: return `None` when verified+likely+flagged == 0 — behind the #109 wiring flag. |
| PO-FACTCHECK-CONFIDENCE-02 | **AGREE P1** | `response_builder.py:1055` calls the module-level `compute_confidence(product_data)` (`scoring_service.py:1057`, no `shopping_count`/`cached`) while the orchestrator persists the method form (`:2651`) with both — the pill row users see is the worse-informed call. |
| PO-FACTCHECK-CONFIDENCE-03 / -04 | **STATUS P1** (#109 / #106) | Activation backlog, sequenced #107 → cache roll → #106 → #109. No new code beyond the one-line `specs_unverified` exclusion inside #109. |
| PO-PRICE-TRUTH-01 | **AGREE P1** | `detect_currency` (`price_service.py:3464-3474`) consults `CURRENCY_SYMBOLS` ({$,£,€,¥}) and the 11-code `CURRENCY_CODES` (`:546-550`), never `GCC_CURRENCY_SYMBOLS` (`:572`, "BD"→BHD); at `:9572` the shopping rung therefore parses `22.500 BD` as 22500 and stamps `currency=BHD, source_method=local_bhd` (`:9678-9682`). 1000× on the native Bahraini format. Price path → flagged, byte-identity OFF. |
| PO-PRICE-TRUTH-02 | **AGREE P1** | Same site: TRY/PLN/CHF/SEK/DKK/CAD absent from `CURRENCY_CODES` → `detected_cur=None` → `item_converted=False` (`:9591`) → raw number stamped BHD/local_bhd. Fix in `_shopping_foreign_currency_signal`: a well-formed non-target ISO code is FOREIGN even when unconvertible (pend). Same unit as -01. |
| PO-PRICE-TRUTH-03 | **STATUS P1** (M13-09) | `ENABLE_SHOPPING_STRICT_CURRENCY` absent from prod; flip AFTER -01/-02 land, own canary window (currency-label change). |
| PO-PRICE-TRUTH-04 | **AGREE P1** | `is_price_showable` reads `title = price.get("title")` (`:1694`) and feeds `_is_sample_or_decant_listing(product_name, title, amount)` (`:1722`); the html extractor stamps `name`, not `title`, so the sample/decant guard is disarmed for every page-scrape price; `identity = title or price.get("name")` exists only for the backstop (`:1747`). One line (`title or name`) — stricter only, but the price path rule still says flag + byte-identity OFF, then flip. |
| PO-PRICE-TRUTH-07 / -08 | **STATUS P1** (#52 / #51) | Fixes exist on the OTHER session's `feature/m22-price-truth-cluster` (`334d300` ENABLE_IHERB_PAGE_CURRENCY, `e748cbb` ENABLE_CONVERTED_PROVENANCE_STAMP), unmerged/ungated. Not my units; activation after their merge. |
| PO-RECORDED-MEASURED-01 | **AGREE P1 (measurement truth)** | 88.6% of the 13,237-row series is 13 probe strings; the honest product series is ~1,513 organic rows, 80% success, p50 22.0 s. Every number in the campaign brief that came from `search_logs` must be re-stated on the organic subset. Code unit (analytics filter) is P2; the finding's value is the corrected baseline. |
| PO-RECORDED-MEASURED-02 | **AGREE P1** | Stamp at `:9682` has no host/country predicate; 857 of 1,484 `local_bhd` rows carry a google.com search link. `local_bhd` is the strongest label in `_GENUINE_BH_SOURCE_METHODS` → the genuine-BH-share KPI is ~80% false. Flagged fix, same family as PT-01/02. |
| PO-RECORDED-MEASURED-03 | **AGREE P1 (verified today)** | `:9678` puts Serper's `link` (a `google.com/search?ibp=oshop` URL) in `price["url"]`; `_is_listing_url` returns True for it (I ran it) and `exact_gate_enabled()` is True by default with the flag absent from prod, so the display backstop (`:1744-1746`) pends every shopping-tier price as `non_pdp_url`. Product decision needed: resolve to the merchant PDP before selection, or keep the search link out of `url` (an absent url is NOT pended, `:1755`). |
| PO-RECORDED-MEASURED-04 | **AGREE P1** | `scs.py:3418` `compute_scores` runs on raw `product_data`; `build_comparison_response` (`:3557` → `response_builder.py:1522`) pends the price AFTER scoring. Unit: pre-scoring showable pass right after `reconcile_pair_fairness` (`:3373`), mirroring `apply_region_currency_guard` (`:3390`). |
| PO-RECORDED-MEASURED-05 | **AGREE P1** | `scs.py:3659` and `:4429` return/emit `str(e)`; `text_routes.py:146` raises it as `detail` when no `code` is present, and the error handler passes a plain-string detail through. Disclosure + UX. The other session's A11 fixes the CLIENT copy only; the backend envelope half is mine. |
| PO-RUBRIC-01 | **AGREE P1** | Per-signal `_<dim>_missing` stamping (`scoring_service.py:2222-2233`) marks the value dim missing when spec OR price is absent while the numeric still decides the winner; `compute_dimension_winners` then prints N/A for the decisive dim. |
| PO-RUBRIC-02 | **AGREE P1** | `:2116-2155` collapse ANY equal non-MISSING reliability/popularity/spec pair to MISSING on the in-code assumption "zero genuine ties in the corpus" — two identical, fully-verified products falsify it. Gate each collapse on evidence of sparsity. |
| PO-RUBRIC-03 | **AGREE P1** | `HIGHER_IS_BETTER_BY_CATEGORY`/`LOWER_...` for `fashion` and `other` are `set()` (`:262-286`), so `_score_specs` takes the presence branch (1.0) for every field → constant → collapsed → 35–40% of the score deleted for the largest bucket. |
| PO-RUBRIC-09 / -10 / -11 | **STATUS P1** (#101 / #100 / #102) | Fresh evidence accepted (no-data beats measured-bad 9/9; unit notation decides 18.7 pts; `fair_price` asserted from the sentinel because `breakdown.get(value_dim)` is never None — `apply_value_badges` `:2521-2524`, `response_builder.py:1706` defaults the badge). One small code change each INSIDE the existing flags + the activation order (#100's flag only after the order-symmetric tie-break). |
| PO-VERDICT-TRUTH-01 | **AGREE P1 — NEW, LIVE, old** | `url_extraction_service.py:575` `comparison = await generate_comparison(...)` then `comparison.get(...)` at `:583`; `generate_comparison` returns a `(dict, usage)` tuple on BOTH paths (`extraction_service.py:2543`, `:2547`) and every other caller unpacks (`scs.py:3440/7907`). `/url/compare` therefore 500s on every successful extraction. Fold the unpack into the URL metering unit (metering a dead route is pointless). |
| PO-VERDICT-TRUTH-02 | **AGREE P1** | `response_builder.py:1107-1108` default `overall` to 50 → `calibrate_score(50)=70` both → the collapse guard (`:1136-1146`) nudges the loser to 69 → a hard-cap PARTIAL crowns product_0 by input order. Return `{}` for `scoring_v2` when scores are absent (FE already falls back). |

## Mobile
| id | verdict | my evidence / constraint |
|---|---|---|
| MB-FLOWS-STATE-01 | **AGREE P1 (Ahmed: qaren.app)** | Re-measured 2026-09-07: `https://qaren.app/`, `/.well-known/apple-app-site-association`, `/c/testtoken` all **522**. `referral_service.py:50` hard-codes `APP_BASE_URL`, `:348` builds every share link on it; `app.json:57-66` verifies universal/app links against the same dead host. Every invite and every deep link is dead until the origin is attached — the standing "qaren.app DNS" blocker, now with a concrete consequence list. |
| MB-FLOWS-STATE-02 | **AGREE P1 (store-readiness)** | `App.tsx:283-297` linking config has no recovery path; `grep recovery|updateUser|setSession(` over `src/` (non-test) = 0 functional hits; `auth_service.py:539` `reset_password_email(email)` without `redirect_to`. Email/password users cannot complete a reset. Unit needs a Supabase redirect allow-list entry (Ahmed dashboard) + `qaren://reset-password` route + screen. |
| MB-FLOWS-STATE-04 | **AGREE P1** | `ReferralLandingScreen.tsx:118/:160`, `InviteeQuizScreen.tsx:275` reset to `'Main'`; the unauthenticated stack (`App.tsx:318-322`) registers only `Auth`. OTA-safe one-liner ×3 + router test. |
| MB-FLOWS-STATE-05 | **OTHER-SESSION (A18 `b73c47e`)** | Their branch navigates with `{ comparison_id }` at `HomeScreen.tsx:972` / `ProfileScreen.tsx:421`. Kill after their merge; re-verify the route-contract test exists. |
| MB-FLOWS-STATE-06 | **AGREE P1 → OTHER-SESSION (B9 `7863bd3`, landed 2026-09-07 during this session)** | `OnboardingFlow.tsx:393-396` `setField(...); handleNext();` — `handleNext` is a `useCallback` over `data` (`:290-308`) and calls `onComplete(data)` in the same tick, so the Step-17 answer is dropped; no payload builder writes `notifications_enabled` (only `types.ts:91` + Profile reads). Verify the `:476` "persists" handler is on a different path before the unit closes. |
| MB-NETWORK-CONTRACT-01 | **AGREE P1 (phones + main)** | `FeedbackCard.tsx:18-22` chips accurate/detailed/fast; `feedback_routes.py:37-39` allowlist lacks them; validator `:98-107` raises → 422 on every in-app feedback; identical in `97b5f15`. Backend allowlist extension + superset grep test (mirror of the events test); no OTA needed for the backend half. |
| MB-NETWORK-CONTRACT-02 | **AGREE P1** | `auth_routes.py:539-543` raises 401 on ANY `success:false`, and `_categorize_auth_error` (`auth_service.py:49-53`) folds network/timeout/DNS into that path; the client treats a refresh 401 as `sessionInvalid` → `clearSession` (`api.ts:87-93`). Overlaps the other session's REMAINING "B2 expired-token 401" — coordinate before filing. |
| MB-NETWORK-CONTRACT-03 | **AGREE P1** | Only `authService.ts:125` (register) sends `X-Device-Fingerprint`; social fetches `:572/:684` send `Content-Type` only; backend writes `device_fingerprint_hash` only in `/register` (`auth_routes.py:378-388`). |
| MB-NETWORK-CONTRACT-04 | **AGREE P1** | `deviceFingerprintHash` exists only at `ShareBottomSheet.tsx:43/118/159`; the single mount (`ResultsScreen.tsx:797-810`) never passes it → SAME_DEVICE control dead. OTA-safe. |
| MB-NETWORK-CONTRACT-07 | **AGREE P1** | `text_routes.py:276-291` fire-and-forgets the save and returns the pre-save dict; zero `comparison_id` assignments onto any response in `app/`. Backend unit: await the insert only. |
| MB-NETWORK-CONTRACT-14 | DEDUPE→MB-RECONCILE-01 + MB-RECONCILE-07 | One metering unit (+ PO-VERDICT-TRUTH-01 unpack). **Coordination:** the other session's REMAINING queue lists A1/A6 = the same unit; this session owns it per the user's instruction — their resume must skip A1/A6. |
| MB-RECONCILE-03 / MB-STARTUP-BUNDLE-02 | **OTHER-SESSION (A3 `e58f022`)** | `initializeAuth(onRefreshed)` boots from the cached session on their branch (`App.tsx` diff, `authService.ts:525-535`). Kill after merge. |
| MB-RECONCILE-15 | **P1 as PRODUCT CALL** | Verified: `openRatingSource` defined once, zero call sites, no `price.url` consumer. Not a code defect to fix blind — Ahmed decides the outbound-link affordance; OTA-safe once decided. |
| MB-SAFE-HYGIENE-01 / -02 | **STATUS P1 (App Store gates)** | Icons still template-identical; legal docs still DRAFT and served live. Ahmed. |
| MB-STARTUP-BUNDLE-01 | **AGREE P1 (measured) — unit owned by the OTHER session (B1)** | 1.21 MB / 24% of the bundle is the lucide barrel. Overlaps the other session's REMAINING "B1 lucide per-icon imports" — leave the unit to whichever session runs first; needs a before/after `expo export` measurement, OTA-safe. |
| MB-TESTS-TRUTH-04 | **OTHER-SESSION (A12 `4f470cc`)** | Their `HistoryScreen.tsx:720-722` refetches with `appliedQueryRef.current`. Kill after merge. |
| MB-TWO-LEVER-RELEASE-02 | **AGREE P1 (needs native build)** | `app.json:71-74` requests `RECORD_AUDIO`; zero audio use in `src/`. Config change + config test; ships only with the next `eas build`, not OTA. |

## Dedupe clusters for the consolidated report (report ONCE each)
1. **DNS on the loop (P0):** LS-REQUEST-PATH-BLOCKING-01 = CR-SECURITY-05.
2. **Per-request Supabase client:** LS-REQUEST-PATH-BLOCKING-02 = CR-PERFORMANCE-03.
3. **Upstash unbounded:** LS-REQUEST-PATH-BLOCKING-03 = LS-CACHE-REDIS-01 = LS-CONCURRENCY-LIMITS-04.
4. **/text/prices unauth fan-out:** LS-RATELIMIT-KEY-02 = CR-SECURITY-06.
5. **Metering doors:** MB-RECONCILE-01 = MB-NETWORK-CONTRACT-14(a); MB-RECONCILE-07 = MB-NETWORK-CONTRACT-14(b) + PO-VERDICT-TRUTH-01.
6. **Boot refresh:** MB-RECONCILE-03 = MB-STARTUP-BUNDLE-02 (other session, A3).
7. **from_history:** MB-RECONCILE-05 = MB-FLOWS-STATE-05 (other session, A18).
8. **Bright Data unbudgeted:** LS-FAILURE-MODES-COST-01 = CR-CLAUDE-CONFIG-02 (docs half).
9. **HTML parse on the loop:** LS-CONCURRENCY-LIMITS-01 + CR-PERFORMANCE-01 (one unit).
10. **Admin key + Sentry:** CR-SECURITY-03 + CR-SECURITY-04 (one unit).
11. **Shopping-rung currency labels:** PO-PRICE-TRUTH-01 + -02 + PO-RECORDED-MEASURED-02 (one flagged family), then PO-PRICE-TRUTH-03's flip.

## SESSION 65 — reconciliation with the lane-batched second votes (all 59 P0/P1 ids re-reproduced; 12 agents)
- **Withdrawn downgrades (vote evidence stronger than my modelled read):** LS-CONCURRENCY-LIMITS-02 stays **P1** — measured 62 submissions / 40 running / 21 queued against the real 40-worker pool from ONE cold fragrance compare (the executor docstring's "~18, two fan-outs" is wrong; the semaphore closes it; the red test must assert submitted-and-unfinished pool depth). LS-CACHE-REDIS-02 stays **P1** — n=5 concurrent cold calls -> 5 searches + 5 extractions on one key; sequential control 1/1; a per-key singleflight drops it to 1/1 (launch-capacity/cost defect on the brief's "blocking a launch step" clause).
- **Agreed downgrades:** LS-FAILURE-MODES-COST-05 -> **P2**, with a nuance that changes the runbook: with both offload flags OFF the refund runs INLINE and survives shutdown; the moment ENABLE_ASYNC_REDIS_OFFLOAD is ON it becomes a yielding await and IS lost -> **W1-6 (bounded drain + --timeout-graceful-shutdown) is a PRECONDITION for the Redis-offload canary**. PO-RUBRIC-11 -> P2 (mechanism holds under every flag state; badge claim). PO-RECORDED-MEASURED-01 -> P2 for the code half (admin-read dilution); the measurement remains the campaign baseline. PO-RECORDED-MEASURED-02 -> **P2**: 1,032 of the 1,484 `local_bhd` rows are already pended by the live non-PDP guard (make_pending_price emits no source_method, so they are in neither eval numerator nor denominator); ~15 genuinely wrong-country showable rows remain. **Ordering consequence:** W4-1 (host/country predicate) must land WITH or BEFORE W4-2 (stop pending the google link) — W4-2 alone would un-pend 857 rows and expose the label.
- **Everything else CONFIRMED at P1** (incl. all 18 mobile ids, all 6 rubric/fact-check ids, PO-VERDICT-TRUTH-01/-02 reproduced through the REAL generate_comparison and _build_partial_response, MB-NETWORK-CONTRACT-01 at the wire: 422 for each shipped chip / 200 for 'price').
- **Last product batch (2026-09-07):** PO-CATEGORIES-I18N-01 CONFIRMED P1 and WORSE than filed — category also gates price routing (`scs.py:5671-5677` `is_supplement = category=='supplements' or (category in ('other',None) and is_supplement_query(...))`), so "electronics" also kills the iHerb / Bahrain-pharmacy price branch for every tablets pair. PO-CATEGORIES-I18N-03 DOWNGRADED → **P2** (accepted): the blocklist lookaround fires ONLY on the bare-hamza spelling; the no-hamza form, the definite-article form and the market transliteration all pass, so the AR entry barely functions as a control and the user-reach premise fails. Fix stays in W4-8 as a P2 data change.
- **Mobile synthesis adjudications accepted (2026-09-07):** MB-RECONCILE-04 → **P2** — the watchdog EXISTS at HEAD (`api.ts:549/:793` pass `COMPARE_TIMEOUT_MS=35000`, `:230` arms the camera abort, `:616` the SSE watchdog); the unbounded case is the `97b5f15` bundle (= MB-RECONCILE-06) and the cancel half is the other session's A4 (on their branch). MB-I18N-RTL-03 (P2) is a **main-only REGRESSION from M21 W4** (RN mirrors physical left/right itself on all four platform paths; the `isRTL ? 'left' : 'right'` ternary double-flips 5 sites and the contract test requires the wrong form) → revert BEFORE the OTA (W3-11a). **Three OTA preconditions recorded:** W1-4 backend half (refresh 503, else one Supabase blip logs out the fleet through the new forced-logout listener), the textAlign revert, and if possible the Sentry-init ordering (W3-12). Their U3 two-flag metering design is folded into the single `ENABLE_PAID_ROUTE_METERING` unit; its 12-test usage-gate list is adopted verbatim.
