# product-output CRITIC

recommend_gap_round: True

## gaps
- /home/savings is never enumerated by any of the 7 lanes (0 hits for 'savings' in the report) yet it is a live, user-facing MONEY claim: the app calls it (SmartCompareApp/src/services/api.ts:1015) and app/api/home_routes.py:151-172 (_extract_winner_loser_prices) sums winner-vs-loser price['amount'] straight out of stored full_response rows with NO filter on estimated / unavailable / source_method / guard_rejected — only a currency=='BHD' equality test. Every defect the report itself files (PO-PRICE-TRUTH-01 22.500 BD -> 22500 BHD; PO-RECORDED-MEASURED-12 42.8% of the price cache is a GPT training-data guess; PO-RECORDED-MEASURED-04 scores computed on a price that is pended afterwards) lands unfiltered in the SavingsBanner headline. Plausible P1, never checked.
- /home/trending (app/api/home_routes.py:779, called at api.ts:1110, served from data/trending_curated.json with a 1h global per-region cache) is user-facing product output and is never mentioned in the report (0 hits for 'trending') — content freshness, region correctness and Arabic rendering of the curated list were never enumerated.
- The camera / vision compare path is deferred by EVERY lane and covered by nothing: po-verdict-truth ('the image/camera compare build path' not covered), po-categories-i18n ('the camera/vision category path ... only read, not swept'), po-prompts ('image_service.extract_image_via_gpt prompt body ... body not read'). It is a real shipped path (api.ts:233 -> app/api/image_routes.py:229) and app/api/image_routes.py:229 passes NO selected_category, so on the camera path category is decided solely by the deterministic classify_category_from_text at structured_comparison_service.py:3243 with _parser_path=False — i.e. the exact 'name detection outranks the chip' mechanism behind P1 PO-CATEGORIES-I18N-01, with no chip to override it at all. Plausible P1 that no lane owns.
- The cohort personalization layer is never swept, although ENABLE_COHORT_PERSONALIZATION=true is one of only a handful of flags ON in prod (baseline/railway-web-vars.txt). scoring_service.py:1299-1304 applies apply_cohort_adjustments (definition at :2744) whenever the user has NO explicit preferences — i.e. the default for most users — and it routes through the same _apply_capped_adjustments the lane's own PO-RUBRIC-07 measured breaching its cap by up to 43.65%. The report's rubric coverage claims 'all three personalization layers against their caps' (explicit/behavioral/session); the fourth, and the only one actually ON in prod, was not exercised.
- Four product-output endpoints exist at HEAD and are never enumerated: GET /text/prices/{product} (text_routes.py:759 — CLAUDE.md flags it as serving stale cache with no category), GET /text/price-kpi (:800), GET /text/parse (:895), plus /home/savings and /home/trending above. The report's surface census stops at compare / stream / quick / url / share / smart-pick / recent-decisions.
- Six flags that are SET in the prod snapshot the report claims to reason from are never named anywhere in it: ENABLE_PRICE_TITLE_PERSIST=true, ENABLE_FRAGRANCE_SIZE_RECONCILE_FIX=true, ENABLE_VARIANT_DESCRIPTOR_AXES=true, ENABLE_SERPER_FAIL_FAST=true, ENABLE_BH_GCC_CATALOG_SOURCES=true, ENABLE_COHORT_PERSONALIZATION=true (all 0 hits). Three of them govern exactly the surfaces the P1s are about — price identity (title round-trip, product_data_service.py:33-46,157-158,199-200), fragrance size stamping (PO-PRICE-TRUTH-06's size=None claim) and variant axes — so the prod flag state under which the price sweeps ran is not established.
- Five of six GCC regions are never exercised. po-price-truth explicitly did not run the SAR/AED/KWD/QAR/OMR legs, and po-recorded-measured reports metadata.region='bahrain' on 26/27 recorded rows, yet region is a first-class product input (_VALID_REGIONS at home_routes.py:137, GCC_REGIONS in extraction_service). No lane checked what a Saudi/UAE/Kuwait user's payload looks like — the region-currency guard is OFF in prod, so a non-Bahrain request's prices, labels and value dimension are entirely unmeasured.
- po-rubric's own explicitly recommended gap round was dropped from the synthesis: the lane said 'a gap round should re-score those bodies [the 27 recorded comparisons + tests/fixtures/comparison_baseline_d2*.json] to convert my modelled frequencies into corpus-measured ones'. Neither §3 nor §6 carries that recommendation, so every frequency in §0.1 and §1d (53.7% missing cells, 9.0%/21.5%/31.3% zero-margin, 84% constant badge) stays a statistic over a constructed grid with a known, cheap, offline path to a real one left untaken.
- Re-engagement push copy is never reviewed (0 hits for 'reengagement'/'push'). app/services/reengagement_service.py:316-365 generates three classes of user-facing product prose in BOTH English and Arabic (e.g. Arabic title/body pairs at :316-317, :336-337, :356-357) naming the user's winner. It is product output, it is the only generated-Arabic surface in the backend, and its existence qualifies the report's sweeping §1a claim that no Arabic generation path exists anywhere. Flag is OFF on web today, so P2-shaped, but it was never enumerated.
- Lane not_covered items that the synthesis's §6 silently dropped (so a later session cannot see them): (a) po-categories-i18n's 'the camera/vision category path and /url/compare's selected_category handling were only read, not swept'; (b) po-factcheck's '_calculate_freshness and the product-level result["data_freshness"] field are NOT traced' and trust_validation_service not exercised; (c) po-verdict-truth's '4 stored rows with fewer than 2 products / no overview were skipped by the rebuild harness' (so '23 recorded rows' is 23 of 27, never stated in §3); (d) po-categories-i18n's per-category spec-field FILL rates (only which fields are ASKED FOR was checked).
- Open issues in the brief's dedupe list that are product-output-relevant and never reconciled: #63 (/price-kpi auth+meter — 0 hits; the endpoint now carries Depends(verify_admin_key) at text_routes.py:807, i.e. its status appears to have changed and the lane leaned on this KPI without opening it), #67 (warmer stale genuine-method set — 0 hits, despite the lane measuring _GENUINE_BH_SOURCE_METHODS parity), #78 (nasser variant guard) and #79 (pharmacy hostname match) — both size/identity-truth issues on the supplements path the lane calls a launch category.
- U6's fix framing is under-scoped: the guard-key drift is real for extract_price_from_html, but price['title'] IS stamped by the adapter rungs (price_service.py:14706 _match_shopify_product, :14994 fetch_bolo_price, :15093 fetch_boutiqaat_price, :15304 _match_nasser_product, :15461/:15596 fetch_iherb_price), and eval_runner.py:554 reads `price.get('title') or price.get('name')` while is_price_showable reads only title. Neither the asymmetry nor the regression risk to those rungs is enumerated anywhere in the report.

## unverified_claims
- §1b's honest unit cost '$0.0140 per successful organic compare' is presented as the measured truth, but the lane's own PO-PROMPTS-07 says the Tier-3 gpt-4o synthesis never records its tokens and PO-RECORDED-MEASURED-07 says every failure row omits cost= — so the logged cost column is a floor, not a measurement, and the figure inherits an unquantified under-count. No caveat is attached where the number is quoted.
- §1b's retry table is internally inconsistent: '893 retries within 2 min -> 640 successes (50 %); 1,037 within 10 min -> 818 (64 %)' — 640/893 is 71.7% and 818/1,037 is 78.9%. Either the denominators are something else (distinct users? distinct queries?) or the percentages are wrong; as printed the 'the retry works' verdict rests on numbers that do not reconcile.
- The priced-cell denominator over the same 27 recorded payloads is quoted three different ways and never reconciled: '24 of 40 priced cells' (§1a / PO-RECORDED-MEASURED-03), '46 product sides' (§6 po-factcheck), and the verify note on PO-RECORDED-MEASURED-16 records '24/28 shown cells ... finder's 52-cell denominator does not reproduce — I get 46'. The headline 'the highest-volume price tier is structurally unshowable' is stated on the 40-cell basis with 14 cells unaccounted for.
- 'gpt-4o serves the prod verdict below the daily token threshold' (§1a) is asserted with no product-lane finding id behind it — it is a cross-lane borrow from the code review. The code half checks out (model_router_service.py:46-64, DAILY_4O_CAP 1_000_000 at :37, SWITCH_THRESHOLD 0.80), but the current Redis 4o counter was never read, so 'below the threshold' is inference, not measurement.
- The cap-hit series ('15.4 % terminate at the 30 s cap, 2.6 % exceed it', PO-RECORDED-MEASURED-13) is rendered in the measured column of §1b, while §3.6 states the partial rate is structurally unmeasurable because partials log success:true. Duration >= 30 s is measurable; attributing those rows to the hard cap rather than a client abort or a slow success is inference and is not labelled as such at the point of use.
- PO-PRICE-TRUTH-06 ('16 of 16 multi-variant captures ship with size=None') and the other price sweeps never state the flag state they ran under with respect to the prod-ON ENABLE_FRAGRANCE_SIZE_RECONCILE_FIX and ENABLE_VARIANT_DESCRIPTOR_AXES, both of which are size/variant flags set true on web. Flag-state provenance is missing for exactly the findings those flags could move.
- PO-PRICE-TRUTH-04's blindness claim never addresses ENABLE_PRICE_TITLE_PERSIST=true in prod, which round-trips a `title` key through the L2 cache (product_data_service.py:157-158, :199-200). I checked and it does not rescue the guard (the write stores price_data.get('title'), which extract_price_from_html never sets), but the report reaches its conclusion without ever testing the one prod flag named after the key in question.
- The 25 P0/P1 rows carrying 'orchestrator review notes' as their second vote of record — which is 23 of the 23 final P1s minus PO-PROMPTS-11 — are single-reviewer verdicts. The report declares this (§3.12), but every severity in §0 and §1a is then stated flatly; a gap round that re-votes those 25 independently is the cheapest remaining confidence purchase.

## wrong_or_stale_claims
- §0.3 and §1c: 'the local_bhd label — the strongest member of _GENUINE_BH_SOURCE_METHODS, pinned 1:1 to the genuine-BH-share KPI' and 'the KPI numerator is inflated ~2.5x ... [the usable_exact_genuine KPI] must be re-taken AFTER the local_bhd country predicate lands' is contradicted by code at HEAD. The KPI is a live HTTP run through GET /text/price-kpi (app/api/text_routes.py:800-856), which applies is_price_showable(enforce_correctness=True) at :838-846 and pends anything that fails; and scripts/eval_runner.py:531-541 (usable_exact_genuine_for_product) independently rejects any price whose url is_non_pdp_listing_url, rejects converted/estimated methods, and fail-closes without a truth entry. The 857 google.com search-link rows therefore cannot enter the KPI numerator at all — the ~2.5x figure is a property of the product_prices CACHE TABLE, not of the KPI, and the ordering rationale built on it in §1c does not follow.
- §0.2 and PO-CATEGORIES-I18N-01: 'the app's only compare path is explicit_pair' is overbroad and stale. api.ts:536-543 sends explicit product_a/product_b only when HomeScreen passes a pair (HomeScreen.tsx:316) — the same function sends q= for a string input — and the app additionally reaches the vision path (api.ts:233 -> image_routes.py:229 -> compare_from_text(vision_products=...)) and /url/compare. The finding's mechanism survives, but the exclusivity claim used to justify its severity does not, and the camera variant (no selected_category passed at all) is never mentioned.
- §1a flag table preamble: 'ENABLE_BUNDLE_C_SCORING=false and ENABLE_GENUINE_PRICE_PRIORITY=false are the only two set explicitly OFF'. The captured snapshot the sentence cites (baseline/railway-web-vars.txt) lists a third explicit OFF on web: ENABLE_REENGAGEMENT_PUSHES=false (and price-warmer adds ENABLE_PRICE_CACHE_WARMER=false).
- Spot-check result, for the record: I re-opened 12 cited pairs at HEAD and every one held — extraction_service.py:1096-1097 (tablet/tablets -> electronics), content_blocklist.json:53 (bare أفيون in illegal_drugs.ar), url_extraction_service.py:575/583 (comparison = await generate_comparison(...) then comparison.get(...), with generate_comparison returning `parsed, usage` at extraction_service.py:2543), response_builder.py:1055 (second compute_confidence call with no shopping_count/cached; module signature at scoring_service.py:1057-1060 vs the method at :2651-2655), response_builder.py:1107-1108 (raw_a/raw_b default 50 -> calibrate 70 + the loser-nudge block at :1129-1145), scoring_service.py:297 (MISSING_SCORE = 50), :2113-2120 (the B0-A reliability tie-collapse), fact_check_service.py:441-446 (median/deviation<=30 verdict), price_service.py:9572 (detect_currency on the shopping display string), model_config.py:200-205 (verdict_chain_max_attempts default 6), zero code sites for ENABLE_HYBRID_MODEL_ROUTING and ENABLE_TPM_AWARE_ROUTING under app/, and ConfidencePills.tsx + confidenceDetailsLines.ts byte-identical 97b5f15..HEAD while DimensionBars.tsx is not. No stale citation was found in the sample.