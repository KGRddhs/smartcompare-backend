# M22 MOBILE — full report, base `76ace90` (review 2026-09-05/06, continued and synthesised 2026-09-07)

Workflow `m22-mobile` (internal M-numbering; **not** the concurrent session's `feature/m23-mobile-w1`).
Base of record: **`76ace90` = `origin/main` at review time**, the deployed backend SHA, prod `/health` 200.
Code read in the SESSION-65 worktree `C:/Users/SynAckITPC/Documents/AI/sc-review-s65` (HEAD `67e9e26`), whose `app/`, `SmartCompareApp/`, `tests/`, `scripts/`, `migrations/` are byte-identical to `76ace90` (`git diff --stat 76ace90..67e9e26` over those paths is empty). Every `file:line` below is valid at `76ace90`.

**Provenance of every row.** 9 finder lanes filed **142 findings**; a verify pass ruled on **142/142**; a second-vote pass covered **13** rows before its agent was lost to the usage limit. The remaining **18 P1 rows** were re-derived **in this synthesis run, by me, against code I opened and commands I ran** — they are marked `synth-2nd` in the trails and the evidence I re-ran is quoted in §1 and §5. Where the orchestrator's review notes (`FABLE_REVIEW_NOTES.md`) disagree with an agent, **those notes are the severity of record**; that moves eight rows (§5). I made **one** further adjudication of my own, also in §5.

**Final ledger: 0 P0 · 23 P1 rows (20 distinct defects after dedupe) · 77 P2 · 42 P3 · 0 refuted.**
Of the 20 distinct P1s: **3 are already fixed on the other session's unmerged `feature/m23-mobile-w1`**, **1 is a product call for Ahmed**, **4 are Ahmed-only** (icons, legal, qaren.app DNS, the OTA itself), leaving **12 engineering units** in this report.

---

## 0 Stop the line

**0.1 — The URL compare tab is dead, on phones and on main, and has been for as long as the code has looked like this.**
`app/services/url_extraction_service.py:575` does `comparison = await generate_comparison(...)` and then reads `comparison.get("winner_index", 0)` at `:583`. `generate_comparison` returns a **`(dict, usage)` tuple on both of its return paths** — `extraction_service.py:2543` (success) and `:2547` (exception) — and every other caller in the codebase unpacks it (`structured_comparison_service.py:3440`, `:7907`). So the first successful extraction raises `AttributeError: 'tuple' object has no attribute 'get'` inside `compare_from_urls`, which `url_routes.py:151` (POST) and `:178` (GET) await. **I reproduced this by reading all four sites in this run.** The URL mode tab is visible on the Home screen of the shipped bundle, so this is a user-reachable dead feature, unflagged. It is filed by the product lane as `PO-VERDICT-TRUTH-01`; the mobile consequence is that `MB-RECONCILE-07` (that same route has no auth dependency, no metering, no history save and drops the client's `selected_category`) is metering a route that cannot succeed. **Fix the unpack in the same unit as the metering, or the metering is decoration.**

**0.2 — The bundle on phones predates every M21 wave, so nothing this campaign "fixed" for mobile is fixed for a user.**
I re-ran the git checks myself: `git merge-base --is-ancestor d24d6e5 97b5f15` **fails** — W1 is not in the published bundle — and `git diff --stat 97b5f15..76ace90 -- SmartCompareApp/` is **72 files, +5,329 −605**. On a phone today, `ResultsScreen` still sends `metadata.query` as `comparison_id`, so every analytics batch and every feedback submission is 422-rejected by the M13-29 UUID validators; there is no compare deadline of any kind (`grep -c COMPARE_TIMEOUT_MS` at `97b5f15` = **0**); and the paywall runs the M13-14 `used < lifetimeFree + bonusRemaining` formula instead of the backend axes. **`eas update --branch preview` from ≥`76ace90` is the highest-value action available to this project and it is Ahmed's, not engineering's.** Until it lands, no mobile finding in any m22 lane may be reported as "fixed for users".

**0.3 — Publishing that OTA arms a defect that is currently silent: one Supabase blip will log out the entire fleet.**
The OTA ships the new global forced-logout listener (`App.tsx:212-227` + `src/services/sessionEvents.ts`), and the two-lever lane cleared it on the grounds that `sessionInvalid` "is never set on network errors". That is true of *client-side* network errors only. `MB-NETWORK-CONTRACT-02`: `auth_routes.py:537-543` raises **401** whenever `refresh_session` returns `success:false`, and `auth_service.py:49-53` folds `network / connection / timeout / dns / econnrefused / socket hang up / enotfound / failed to fetch` into exactly that path. The client maps any 401 from `/auth/refresh` to `clearSession()` + `sessionInvalid` (`authService.ts:286-290`, `api.ts:86-95` — I read both in this run). The boot path runs a refresh on **every cold start**. So: Supabase hiccups → the server turns it into a 401 → every phone wipes both tokens → after the OTA, every phone also gets routed back to Login. **Land the backend half of `MB-NETWORK-CONTRACT-02` (a distinguishable 503 + a stable error code) before or with the OTA.** The backend half needs no OTA of its own.

**0.4 — Two paid doors are open and one of them writes history anyway.**
`app/api/image_routes.py` contains **zero** metering symbols (`grep -c "consume_comparison_credit|record_lifetime_comparison|refund_comparison_credit"` → 0, against 13 in `text_routes.py`), and its only gate is `:89 if anon_usage_gate_enabled() and not user:` — a flag that is absent from the Railway `web` vars, and which is `and not user` anyway, so an **authenticated** caller is never metered under any flag state. `url_routes.py` has neither a user dependency nor a counter. Latent today (11 lifetime camera rows, ~zero prod traffic, OpenAI at 429) — a real freemium bypass and a real spend leak the moment scan or URL is promoted.

---

## 1 The answers

### 1.1 Is the mobile app in shape today? Two answers.

**(a) The bundle ON PHONES — the `preview` OTA published 2026-09-02 from `97b5f15`. Verdict: not in shape, and the reason is a release action, not code.** MEASURED by me this run.

| what a user hits on a phone today | evidence at `97b5f15` | fixed on main? |
|---|---|---|
| Every Results analytics batch + every feedback POST 422s silently | `97b5f15:ResultsScreen.tsx:331` `const comparisonId = metadata?.query;` vs `76ace90:…:345` `result?.comparison_id ?? route?.params?.comparison_id` | yes, M21 W1 `d24d6e5` |
| Feedback also fails on the chip vocabulary, independently of the above | `FeedbackCard.tsx:18-22` chips `accurate/detailed/fast` vs `feedback_routes.py:37-39` allowlist — identical at both SHAs | **no — still broken on main** (`MB-NETWORK-CONTRACT-01`) |
| No compare deadline: text/URL ride the 120 s axios ceiling, camera has none at all | `git show 97b5f15:…/api.ts \| grep -c COMPARE_TIMEOUT_MS` = **0** | yes, M21 W2 `1df95b0` (`api.ts:43/45/47`, `:230`, `:549`, `:793`) |
| Free users paywalled by the M13-14 formula instead of the backend's daily/monthly axes | `97b5f15:useComparisonCounter.ts` `canCompare = isPremium \|\| used < lifetimeFree + bonusRemaining` | yes, `useComparisonCounter.ts:76-82` |
| No RTL sweep, no Arabic plural forms, no localized currency glyphs | `DirectionalIcon.tsx`, `intl-pluralrules`, `currencyDisplay.ts` are all NEW in the delta | yes, M21 W4 `9b34e95` |
| No failure-classification matrix, no onboarding draft/retry, no register email-confirmation state, no History jank fixes, no Android image downsample, no Sentry query-string scrub, no ISRG X1 backup pin, no legacy-token purge | `git diff --stat 97b5f15..76ace90 -- SmartCompareApp/` = 72 files / +5,329 −605 | yes (W1–W4) |
| URL compare 500s; camera and URL unmetered; `from_history` dead taps; boot blocks on a 120 s-ceiling refresh; feedback chips rejected; qaren.app 522 | unchanged between the two SHAs | **no — open on both** |

Everything **server-side** is current: prod is deployed at `76ace90`, so the referral phantom-column fix, the prompt fence and the M21 backend work are all live for phones already.

**(b) MAIN `76ace90`. Verdict: no P0, one dead feature (§0.1), and 20 distinct P1 defects — but the shape is "reachable defects", not "broken product".** `npx tsc --noEmit` exit 0 and 242/245 jest suites green were reproduced by the tests-truth lane at HEAD (`baseline/`), and both gates are structurally blind to most of what is in this report: `App.tsx`, `ResultsScreen.tsx` and `usageService.ts` are executed by **zero** tests, and the CI eslint glob excludes `App.tsx` entirely (`MB-TESTS-TRUTH-06`, `-01`, `-03`, `-11`).

### 1.2 What the pending `eas update --branch preview` changes; is it native-compatible; the first-launch risk list

**Native-compatible: YES.** I re-derived this in this run: `git diff 97b5f15..76ace90 -- SmartCompareApp/app.json SmartCompareApp/eas.json` is **empty** — no plugin, permission, SDK, `runtimeVersion` or `updates.url` change — and `expo` stays `~54.0.33`. `runtimeVersion.policy` is `appVersion` against `version` `1.0.0`, which is exactly what the installed preview builds run. `package.json` changes are **two lines only**: `axios ^1.13.4 → ^1.20.0` (lock 1.16.1 → 1.20.0) and a **new** `intl-pluralrules ^2.0.1` — both pure JS with no native side (`MB-TWO-LEVER-RELEASE-10`; this corrects M18 MB-two-lever-04's "zero dependency changes" premise while leaving its conclusion intact).

**What it ships to users:** the analytics/feedback `comparison_id` fix; the backend-derived paywall axes; the Arabic RTL/plural/currency sweep; forced routing back to Auth on a definitively-dead session; the register email-confirmation state; the onboarding completion draft + replay; bounded compare timeouts, an `AbortController` on `/image/identify` and an SSE watchdog; the History jank fixes and the Android `resizeMethod="resize"`; Sentry query-string/breadcrumb scrubbing; the ISRG Root X1 backup pin; the legacy plaintext-token purge. Propagation is **two launches** (background download, apply on next cold start); the only in-app accelerator is `Updates.reloadAsync()` on a language switch (`useLanguage.ts:23`).

**First-launch risk list — five RISKY-ON-OTA items, in the order they can bite:**

1. **Forced logout on an upstream blip** (§0.3). The listener is new in this OTA; the 401-for-everything refresh contract is old. This is the one interaction that can eject the whole fleet at once. Mitigation: ship the backend 503 half first (`MB-NETWORK-CONTRACT-02`, U4).
2. **`import 'intl-pluralrules'` at bundle-load time** (`src/i18n/index.ts:7`) assigns `Intl.PluralRules` inside a strict-mode module. Jest exercises the polyfill class but takes the **Node** branch for the install (Node 24 has `selectRange`), so the **Hermes install path is device-unverified**. Worse, the polyfill's own guard probes `en/es/ru/zh` and never `ar` (`MB-I18N-RTL-14(e)`) — the one locale the app needs. Verify on device by reading an Arabic count at 0/1/2/3/11 on Home and History.
3. **`certificatePinning.ts`** ships a new pin plus a release-only "running unpinned" Sentry alarm that fires from `api.ts:22` at module scope — and that alarm is **order-dead in its primary case** (`MB-TWO-LEVER-RELEASE-03` / `MB-STARTUP-BUNDLE-07`): Babel hoists every require above `initSentry()`, and the ssl library's `Proxy` throws **synchronously** when the native module is missing, so the capture happens before Sentry has a client.
4. **`sentry.ts`** registers three new SDK hooks in `Sentry.init`; combined with (3) and with **no sourcemap upload on either lever** (`MB-TWO-LEVER-RELEASE-05` — `eas.json` has no `env` block on any profile, no `sentry.properties`, no `.sentryclirc`, no upload script), every crash from this OTA arrives unsymbolicated. That is also why the open Google Sign-In failure is currently undiagnosable.
5. **`useComparisonCounter.ts:81`** now hard-depends on `status.remaining.{daily,monthly}` and **silently** falls back to the M13-14 hard-3 dead-end if those keys ever go missing. Safe at the deployed SHA (`usage_service.py` emits `remaining` on every branch) — but it is the third formula for this gate in three months and the degradation has no telemetry (`MB-TWO-LEVER-RELEASE-11`).

**Publish discipline:** publish from a shell with **no** `NODE_ENV` override (the babel console-strip is gated on `NODE_ENV/BABEL_ENV === 'production'`), record the update group id and SHA (M18 MB-two-lever-03's recording gap is still open, which is why "did the cert-pin OTA ever fire?" is unanswerable and devices may be even further behind than `97b5f15`), and do **not** publish to `--branch production` — no production build exists, so it would reach zero devices.

### 1.3 The reconciliation ledger — what M21 closed, what is still open

**Closed by M21, MAIN-ONLY (31 rows: 28 M18 + 3 checkup)** — `MB-RECONCILE-19`, commit-attributed and re-verified by `git log -S` per symbol:

| wave | closed |
|---|---|
| W1 `d24d6e5` | MB-contract-01 (the `metadata.query` 422 breakage), MB-contract-03 (share sheet reachable), MB-contract-05 (referral phantom `response_data` column — **backend, so this one IS live for users**) |
| W2 `1df95b0` | MB-perf-01/-03 + MB-contract-06 (bounded timeouts, AbortController, watchdog, transport decided before any request), MB-contract-02 (code/layer preserved on the SSE error event), MB-contract-09 (503→TIMEOUT only when nothing more specific), MB-flows-01 (`sessionInvalid`), MB-flows-02 (routing back to Auth), MB-flows-03 (email-confirmation state), MB-flows-05 (`failureClassification`) |
| W3 `7fb0b0d` | checkup A#13 (row-stagger cap), A#14 / MB-perf-07 (`HistoryRow` memo + `renderItem` useCallback), MB-perf-06 (`ResultsContent`/`ResultsAccordion` memo), MB-perf-08 (Android `resizeMethod`), MB-flows-04 (`onboardingDraft.ts`), MB-flows-08 (`openRecentDecision` by id) |
| W4 `9b34e95` | MB-i18n-rtl-01/-02 (plural polyfill + Arabic plural families), -03 (`DirectionalIcon` + wiring contract test), -04 (`TwoInputShell` RTL), -05 (logical alignments — **but see the regression below**), -08 (`home.savings.count` plurals), -07 **half** (ISO→Arabic glyph via `currencyDisplay.ts`) |
| elsewhere in the delta | MB-two-lever-02 (paywall axes), MB-contract-10 (phantom required `winner_index`/`win_margin` dropped from the type), MB-perf-02 (`abortRef` actually invoked on unmount) |

**CHANGED, not closed:** checkup A#11 (a *codeless* error still renders `error.message`); MB-contract-04 (new rows carry the reconciled winner, but pre-fix Supabase rows were never backfilled, so History can still name the opposite winner for them); MB-two-lever-02's *premise* (phones at `97b5f15` were never running a hardcoded `used < 3` — that M18 sentence was already stale when written).

**Stale claims killed for good** (do not re-file these):
- "Phones run a hardcoded `used < 3`" — **false**; `97b5f15` runs the lifetime+bonus formula, which is *more* permissive.
- "The referral landing still reads the phantom `response_data` column on devices" — **false**; that fix is server-side and is live at `76ace90`.
- "M21 W4's RTL sweep is regressed on phones" — **false**; the regression (`MB-I18N-RTL-03`) landed in `9b34e95`, which is *not* on any phone, so it is a main-only regression.
- "Reanimated leaves orphaned infinite animations after unmount" (M18 MB-perf-04's second clause) — **refuted** at reanimated 4.1.7, which registers `cancelAnimation` in an unmount effect.
- "The checkup's 8.2 MB is the iOS bundle" — it is the **Android** Hermes bytecode; iOS is 5.05 MB.

**Still open at HEAD and re-verified this campaign:** all 20 distinct P1s in §4 plus 17 reconciled P2/P3 items (`MB-RECONCILE-18`) — the 1.5 s splash floor, the `settle_complete` latch gap (dark-flag only), `CounterTicker`'s 60 Hz `runOnJS`, the dead `usageStatus` fetch on Results, the unconditional 1.2 s Results floor, the Login back arrow and "Email" pill no-ops, silent camera-capture failures, the per-focus network fan-out, the compare re-entrancy gap, the 429 envelope dropping `tier`/`remaining`, the hard 50-row History cap, `category_switched` never surfaced, the mixed Arabic digit policy, BHD's third decimal, and the two-consumer `arabicLineHeightMultiplier`.

### 1.4 The measured bundle picture, and the biggest verified size win

MEASURED with three real `npx expo export` runs at HEAD (node v24.11.1, expo ~54.0.33) plus a VLQ source-map attributor; the verifier re-ran its own export and reproduced every figure to within a rounding digit.

| | bytes | note |
|---|---|---|
| iOS Hermes bytecode (`.hbc`) | **5,299,992** (5.05 MB) | `--no-minify` = 5,279,756, i.e. minification is ~0 for hbc |
| Android Hermes bytecode | **8,285,250** (7.90 MB) | +56.3% from the identical module graph; this is the checkup's "8.2 MB" |
| iOS JS attributed (87.8% of the `--no-bytecode` build) | 5,060,869 | 3,842 modules in the map |
| **lucide-react-native** | **1,212,812 (23.96%)** | 1,710 modules, of which **1,703 are icons**; **68** icons are actually used across **34** barrel import sites (I re-counted this run: 34 sites, 1,704 icon files on disk) |
| react-native | 686,585 (13.57%) | |
| react-native-reanimated | 672,359 (13.29%) | |
| the app's own `src/` | 424,968 (8.40%) | |
| all `@sentry*` | 823,858 (16.28%) | of which **198,567 is provably dead browser code**: `@sentry-internal/replay` 132,946 + `feedback` 50,195 + `replay-canvas` 15,426, none registered by `initSentry` |
| `react-native-markdown-display` + `markdown-it` | 80,564 (1.52%) | sole consumer is `LegalScreen.tsx:18`, a modal nobody opens at boot |
| OTA asset payload (iOS) | 1,145,042 across 30 assets | **11 TTFs = 1,136,172 = 99.4%** |
| — Cairo | 753,428 (8 weights) | only 400/600/700 are registered → **469,608 B (41.0% of the asset payload) is never used** |
| — Geist | 382,744 (3 weights) | shipped **twice** in a store build: as JS assets *and* natively via the `app.json` expo-font plugin |

**Biggest verified size win: the lucide barrel, ~1.19 MB (≈24% of the JS bundle).** Two caveats that must ride the unit:
- The saving is **arithmetic on the measured attribution** (1,212,812 minus 68 icons × ~374 B mean), not a measured second build — HARD RULE 1 forbade editing the tree. The unit's exit gate is a before/after `expo export` byte diff.
- **Both obvious fixes do not resolve.** I checked the installed package this run: `lucide-react-native@1.14.0`'s `exports` map has exactly `"."` and `"./icons"` — no `./icons/*` wildcard, no `./dist/*` — and there is no `icons/` directory at package root. So `lucide-react-native/icons/<kebab>` cannot resolve and the `dist/esm/icons/*.mjs` deep path is exports-blocked. The unit needs a Metro `resolver.resolveRequest` shim or a babel transform to absolute paths.

Second-largest and cheapest: the **Sentry browser trio, 198,567 B**, removable by a Metro resolver stub with no call-site change (nothing registers replay or feedback). Third: the **five unused Cairo weights, 469,608 B** — the removal is a subpath-import change, and it is incomplete unless `fonts.ts:12-17`'s `useFonts` import from the package **root** is also switched, or the eight top-level `require()`s in the package barrel keep everything in the graph.

The multiplier behind all of it: there is **no `metro.config.js`** (confirmed this run: ENOENT), so Expo's `inlineRequires: false` default governs and every statically-imported module's factory runs eagerly at cold start — the 1,706 lucide factories, 5,849 `Object.defineProperty` calls and 1,703 `forwardRef` creations included (`MB-STARTUP-BUNDLE-04`, P3 only because its own fix needs a staged rollout).

### 1.5 App Store production blocker status

| gate | status at `76ace90` | evidence | lever |
|---|---|---|---|
| **Icons (ICN-0001)** | **OPEN, unchanged for 105 days** | I recomputed the three SHA-256s this run: `icon.png` `74c64047…`, and `splash-icon.png` == `adaptive-icon.png` == `5f4c0a73…` — byte-identical to each other and to the 2026-05-24 template record; `git diff 593ec1e..76ace90 -- SmartCompareApp/assets` empty | **needs-build**, Ahmed |
| **Legal docs** | **OPEN** | both `app/legal/{privacy_policy,terms_of_service}.md:7` still read `*DRAFT — This document is a template…*` (re-read this run); prod serves that text into the app today (`legal_routes.py` reads the file per request, `LegalScreen.tsx:33-34` fetches it) | backend deploy only, Ahmed/legal |
| **Privacy labels / manifest** | **OPEN, and the policy is materially incomplete** | `app.json:21-41` `privacyManifests` has `NSPrivacyTracking:false` + four `NSPrivacyAccessedAPITypes` and **no `NSPrivacyCollectedDataTypes`**, while the code collects 9–11 declarable types the policy's §2 never mentions — demographics (`onboardingDraft.ts:81-90`) and a persistent hashed device id (`deviceFingerprint.ts` → `X-Device-Fingerprint`) among them; §11 then promises not to share an age/gender §2 never admits collecting | manifest = **needs-build**; policy = backend deploy |
| **`npm audit --audit-level=high`** | **triaged, and the triage is the answer** | 42 rows / 1 critical + 19 high; **only 3 reach the device JS bundle** (nanoid via react-navigation, called at default size; markdown-it + linkify-it, fed only by our own `/legal` markdown, both `fixAvailable:false`). 17 of 20 are EAS/dev toolchain. The one direct high is `expo` itself and its fix is an SDK-54→57 major | `npm audit fix` (non-major) clears 9 rows incl. nanoid; do **not** chase `expo@57` for the audit |
| **RECORD_AUDIO** | **OPEN — new this review** | `app.json:71-74` requests `android.permission.RECORD_AUDIO` and the plugin defaults resolve a boilerplate `NSMicrophoneUsageDescription`, while a case-insensitive grep of `src` + `App.tsx` for audio/microphone returns **zero** runtime hits (I re-ran it). Triple re-add path: `app.json`, `expo-camera`'s `recordAudioAndroid=true` default, `expo-image-picker`'s microphone permission | **needs-build** — no OTA can remove it, so it must land **before** the first production build |
| **Google Sign-In** | **still failing on EAS preview; diagnosable in principle, not in practice** | the `[GOOGLE-DIAG]` instrumentation does survive release (the Sentry breadcrumb at `authService.ts:556` and `captureMessage` at `:602` are not `console.*`, so the babel strip leaves them) — but it is unreadable while (3) Sentry may not be initialised at the failure point and (4) no sourcemaps are uploaded. `signInWithApple` (73 lines) has **0% coverage** and its test is one of the 13 `it.todo` stubs | fix the Sentry ordering + sourcemap upload first, then read the dashboard |
| **Account recovery** | **OPEN** | there is no password-reset path at all: no recovery route in `App.tsx:283-297`'s linking config (re-read this run), zero functional `recovery`/`updateUser`/`setSession(` hits in `src/`, and `auth_service.py:539` sends the reset mail with **no** `redirect_to` (`MB-FLOWS-STATE-02`) | client OTA + backend + a Supabase dashboard allow-list entry (Ahmed) |
| **Universal / app links** | **OPEN** | `qaren.app` returns Cloudflare **522** for `/`, `/.well-known/apple-app-site-association`, `/.well-known/assetlinks.json` and `/c/<token>` (re-measured 2026-09-07). `referral_service.py:50` hard-codes `APP_BASE_URL` with no `getenv` despite its own comment, and `app.json` verifies against that dead host — so Android 12+ never offers the link to the app and iOS never activates `applinks:` | DNS/edge + a backend deploy, Ahmed |

TestFlight internal is unaffected by all of the above except Google Sign-In and the recovery path.

### 1.6 The Arabic user's experience, end to end (rendering side)

The catalogs are in better shape than the rendering. MEASURED: `en.json` and `ar.json` are **896 keys each**, key-set equal, placeholder parity 100%, zero untranslated Arabic prose (the 11 byte-identical values are symbols and brand tokens), and zero copy-contract violations *in the catalogs*. The M21 W4 `DirectionalIcon` sweep is **complete** — 15 of 19 directional glyph sites are wrapped and the 4 unwrapped ones (Telegram send ×2, a node-graph share glyph, a travel plane) correctly must not mirror. What fails is everything between the catalog and the glass:

1. **First launch is left-to-right.** `App.tsx:173-177` calls `I18nManager.allowRTL(true)` + `forceRTL(shouldBeRTL)` and **nothing else**, while RN 0.81.5 caches `isRTL` as a module-load constant (`I18nManager.js:15/:59`) — so an Arabic-locale install renders the entire first session, including all 17 onboarding steps and the first compare, as Arabic copy inside an LTR shell. The in-app switcher does the same call **and then reloads** (`useLanguage.ts:17-27`), which is the proof the boot path is missing a step. (`MB-I18N-RTL-01` = `MB-STARTUP-BUNDLE-09`, P2, known as M13-62.)
2. **Two contradictory `isRTL` values ship side by side** — `useLanguage().isRTL` (from `i18n.language`) versus `I18nManager.isRTL` (the boot constant) — so inside that first-launch window the UI is *half* mirrored: `useTypography` applies the Arabic line-height and `CohortBadge` slides from the RTL side while every chevron and spec column stays LTR (`MB-I18N-RTL-02`).
3. **M21 W4 introduced a textAlign regression, on main only.** RN mirrors physical `left`/`right` itself — verified in the app's own `node_modules` on all four platform paths (`RCTAttributedTextUtils.mm:195-201`, `RCTTextAttributes.mm:109-115`, `TextAttributeProps.java:294-297`, and Fabric's Yoga edge swap) — so `9b34e95`'s `I18nManager.isRTL ? 'left' : 'right'` **double-flips** 5 sites that were correct at `593ec1e`: the Results spec table's two value columns, the DimensionBars legend, the Profile priorities percent, and two character counters. **And the contract test `__tests__/rtl/textAlignLogical.contract.test.ts:52-53` requires the wrong form, so the bug is fenced in.** (`MB-I18N-RTL-03`.)
4. **Cairo is loaded, blocks the splash, and never renders a single character.** `fontFamily.ar` has **zero** consumers; `theme/index.ts`'s typography presets carry **no** `fontFamily` at all; the only two applied font literals in the whole app are `'Geist-Bold'`. Every Arabic user reads the app in the OS fallback face — and `useTypography`'s 1.7× line-height was calibrated for Cairo's metrics, so even the leading is tuned against a font that never appears. `useTypography` itself has **zero production importers** (test-only), so it does not run either. (`MB-I18N-RTL-04` = `MB-SAFE-HYGIENE-03`; `MB-SAFE-HYGIENE-04` = `MB-RENDER-PERF-A11Y-03`.)
5. **Numbers are four different systems on one screen.** 32 `ar.json` values use Arabic-Indic digits and 21 use Western (0 mixed within a value) — the same "70 comparisons" claim appears twice on the Paywall with different digits — while four `toLocaleString()` calls follow the **device** locale and `formatDate` hard-codes `ar-SA` (Arabic-Indic) three lines above a `formatTimeAgo` that returns ASCII. (`MB-I18N-RTL-06`.)
6. **BHD loses its third decimal.** History renders `toFixed(2)`, Home/Profile `toFixed(0)`, Results puts the code first and the amount second while History does the reverse. For a Bahrain price-comparison app, `12.345 BHD` shown as `12 د.ب` is a correctness claim the product cannot back. (`MB-I18N-RTL-07`; the verifier corrected one clause — Results' bare `toLocaleString()` does **not** round, it emits 3 fraction digits.)
7. **Relative time bypasses the catalog entirely**: hard-coded Arabic with one singular form serving counts 2–59, and a second implementation on Profile that returns bare `3h` / `5d` to Arabic users, plus untranslated `'N/A'` in History rows. (`MB-I18N-RTL-08`.)
8. **Raw English backend prose still reaches Arabic users** at two of three HomeScreen alert sites — and MEASURED against the real error corpus, ~1,500 of 3,254 failures land on strings like "We couldn't finish this comparison in time. Try again." — vocabulary the copy contract forbids, shipped verbatim inside an Arabic alert title. (`MB-I18N-RTL-05`.)
9. **The lint fence reads 0 and means nothing**: `i18next/no-literal-string` runs in `mode:'jsx-text-only'` over `src/screens` + `src/components` only, so it cannot see `Alert.alert`, `setError`, `accessibilityLabel` or any JS string — and `RegisterScreen.tsx:130/147/223` ship English sign-in failures while `LoginScreen` does the same thing correctly with `t()`. (`MB-I18N-RTL-09`.)
10. **Nothing anywhere applies bidi isolation.** `writingDirection()` has zero consumers and there are exactly two bidi control characters in the entire tree, both in `ar.json`. Since the backend returns English verdict prose to Arabic users (the product lane's PO-verdict-text-02), Latin runs inside Arabic sentences reorder unpredictably — and will continue to for brand names and hostnames even after the generation side is fixed. (`MB-I18N-RTL-14(a)`.)
11. **Choosing العربية hard-restarts the app with no warning** — at onboarding step 2 that drops the user back to step 1 (the flow holds all 17 steps' answers in component state and writes its first draft only on completion), and from Profile it kills the session mid-task. There is not even a catalog string to explain it. (`MB-I18N-RTL-11`, known as M13-61.)

Latent but load-bearing: the plural polyfill's guard never probes `ar` (§1.2 item 2), and the `referrals.bonus.expiresIn*` families carry only `_one`/`_other`, so the moment that UI is wired Arabic falls back to **English** for four of the six CLDR buckets — while both existing i18n fences stay green (`MB-I18N-RTL-10`).

---

## 2 Proposed units through the standing gates

Every unit below is **TDD red-first**: the named test must be shown failing at `76ace90` before any source change. Every unit carries the **module-reference comm gate** — build the test file set by grepping the test tree for references to *every module the unit touches* (never filename keywords), run it at base and at HEAD, and require `branch-only-NEW == []`. **No unit in this report touches the price extraction path**, so the `_proof` flag-OFF byte-identity gate does not apply to any of them; where a unit adds a flag, the equivalent gate is an explicit **flag-OFF no-op assertion** (the route behaves byte-identically to `76ace90` with the flag unset). **Fable reviews every unit before commit**, and no unit is a direct fix — each is a red test, then the smallest change that greens it, then the gate.

Client-side changes ship **unflagged by nature** (React Native has no per-call env flag) and reach phones **only via `eas update`**; the last column of each row says which lever the unit needs.

| unit | findings | files | flag | tests-first | gates | order reason |
|---|---|---|---|---|---|---|
| **U1 — Channel-freshness guard + publish runbook** (release; no lever of its own) | MB-RECONCILE-06, -19, MB-TWO-LEVER-RELEASE-05, -10 | `.github/workflows/ci.yml`, `docs/runbooks/qaren-canary-onboarding.md` | none | a CI job that resolves the `preview` channel's published commit and fails when it is more than one merge behind `origin/main` — **red today (6 commits / 72 files behind)** | comm gate n/a (CI-only); land non-blocking for one week, then blocking | **first.** It is the only thing that makes the two-lever gap self-reporting instead of needing a reconciliation pass; the runbook line (publish with no `NODE_ENV` override, record the group id + SHA, upload sourcemaps) is what makes the *next* OTA auditable |
| **U2 — Feedback allowlist + superset fence** (backend deploy — **fixes phones with no OTA**) | MB-NETWORK-CONTRACT-01 | `app/api/feedback_routes.py`, `tests/test_feedback_allowlist_superset.py` | none (additive, backwards-compatible) | (a) a backend test that greps `SmartCompareApp/src` for `mattered_most:` chip literals and asserts each is in `VALID_MATTERED_MOST` — red on `accurate`/`detailed`/`fast`; (b) a jest test asserting `submitFeedback`'s payload is one `FeedbackRequest` accepts | comm gate over the modules referencing `feedback_routes`/`feedback_service`; free-unit pytest | **before the OTA.** The only P1 a backend deploy alone fixes for phones today — the chips are identical in the shipped bundle. Mirrors the existing `tests/test_events_allowlist_superset.py` precedent |
| **U3 — `/url/compare`: make it work, then meter it; camera metering** (backend deploy) | PO-VERDICT-TRUTH-01, MB-RECONCILE-07, MB-RECONCILE-01, MB-NETWORK-CONTRACT-14 | `app/services/url_extraction_service.py`, `app/api/url_routes.py`, `app/api/image_routes.py`, `tests/test_url_routes_metering.py`, `tests/test_image_routes_metering.py` | `ENABLE_URL_USAGE_METER`, `ENABLE_IMAGE_USAGE_METER` — **both default OFF**, read per call via `os.getenv` (repo convention; the change turns 200s into 429s). The tuple unpack is **unflagged** — a pure defect fix with no behavioural fork | the full usage-gate list below | comm gate over every test referencing `url_routes`/`image_routes`/`url_extraction_service`/`usage_service`; **flag-OFF no-op assertion** on both routes; free-unit pytest | **before any camera or URL promotion, and before `ENABLE_ANON_USAGE_GATE`.** `#128` (wire `refund_anon_comparison_credit`) stays a hard precondition on that flag and this unit must not flip it. Metering a 500-ing route is pointless, so the unpack leads |
| **U4 — Refresh: distinguish "session dead" from "upstream down"** (backend deploy first, client half OTA) | MB-NETWORK-CONTRACT-02 | `app/api/auth_routes.py`, `app/services/auth_service.py`, then `SmartCompareApp/src/services/authService.ts` | none | (a) patch `refresh_session` to raise `ConnectionError` and assert `POST /auth/refresh` returns **503** with a stable `code`, not 401 — red today; (b) jest: reject with `{status:503, code:'REFRESH_UPSTREAM_UNAVAILABLE'}` and assert `clearSession` was **not** called | comm gate over `auth_routes`/`auth_service`; free-unit pytest + jest | **the backend half must land before or with the OTA** (§0.3). Coordinate with the other session's remaining "B2 expired-token 401" before filing so the two do not collide inside `_categorize_auth_error` |
| **U5 — `comparison_id` on the compare response** (backend deploy; user value only with the OTA) | MB-NETWORK-CONTRACT-07 | `app/api/text_routes.py` (3 handlers), `app/services/feedback_service.py` | `ENABLE_COMPARISON_ID_ECHO` default OFF, **or** the zero-latency variant: generate the row UUID in the route and pass it into `save_comparison` | a route test asserting the POST body carries a `comparison_id` that is a valid UUID and matches the persisted row — red today (no `comparison_id` assignment exists anywhere in `app/`) | comm gate over `text_routes`/`feedback_service`/`database_service`; free-unit pytest; the added await must not land on the single-loop hot path (prefer the UUID variant) | **after U3.** The shipped `ResultsScreen` reads `metadata.query`, so the echo alone changes nothing on phones; with the OTA it unblocks Loop-1 sharing at the one high-intent moment and anchors every feedback/event row |
| **U6 — Device fingerprint on social sign-in and the share sheet** (client OTA + backend deploy) | MB-NETWORK-CONTRACT-03, -04; precondition note for `#128` from MB-NETWORK-CONTRACT-09 | `SmartCompareApp/src/services/authService.ts`, `src/components/ShareBottomSheet.tsx`, `app/api/auth_routes.py`, `app/services/auth_service.py` | none | (a) jest: `signInWithGoogle` sends `X-Device-Fingerprint`; (b) pytest: social login writes `users.device_fingerprint_hash` and inherits `lifetime_comparisons_used`; (c) jest: a share tap posts a 64-hex `device_fingerprint_hash`; (d) pytest: `evaluate_invite` returns `SAME_DEVICE` when both sides share a hash — all four red today | comm gate over `auth_routes`/`auth_service`/`referral_service`/`abuse_detection_service`; jest + pytest | **while `ENABLE_REFERRAL_SYSTEM=true` in prod.** Both device-based referral defences are down at once, and each half is inert without the other, which is why the gap survived. Resolve the fingerprint *inside* `ShareBottomSheet` so no caller can forget it again |
| **U7 — Route contract: never navigate with a param no screen reads** (client, OTA-safe) | MB-FLOWS-STATE-04; re-verify MB-RECONCILE-05 = MB-FLOWS-STATE-05 after the other session merges | `src/screens/ReferralLandingScreen.tsx`, `src/screens/InviteeQuizScreen.tsx`, `src/types/types.ts`, `__tests__/navigation.resultsEntry.contract.test.ts` | none | (a) router-level: for the **unauthenticated** route-name set, `StackRouter.getStateForAction(deepLinkState, reset({routes:[{name:<target>}]}))` must not be `null` — red today on all three `reset→'Main'` sites; (b) a source-contract test asserting every `navigate('Results', …)` literal uses only keys in `RootStackParamList['Results']` — red today on the two `from_history` sites | comm gate over the screens + `types.ts`; jest; `tsc --noEmit` exit 0 **after** the `as any` casts are removed | **after `feature/m23-mobile-w1` merges** — their A18 fixes the `from_history` half; this unit owns the referral-exit half they do not, plus the contract test that would have caught both. Fixing `qaren.app` without this makes the referral dead-end *worse*, because it becomes the default path |
| **U8 — Boot cannot strand, and Sentry is armed before the graph runs** (client, OTA-safe) | MB-STARTUP-BUNDLE-03, -07 = MB-TWO-LEVER-RELEASE-03, -14(b)(c); re-verify MB-RECONCILE-03 = MB-STARTUP-BUNDLE-02 after the other session merges | `App.tsx`, `index.ts`, `src/services/sentry.ts`, `src/i18n/index.ts`, `src/config/featureBucket.ts`, `src/services/certificatePinning.ts` | none | (a) render `App` with `expo-localization.getLocales` throwing and assert the tree leaves the splash within a tick — red today (`isLoading` never clears); (b) a require-order test asserting `Sentry.init` ran before the `api.ts` module body — red today; (c) a test that a synchronously-throwing `initializeSslPinning` still produces a `captureMessage` | comm gate over `App.tsx`/`sentry`/`i18n`/`featureBucket`; jest; `tsc` exit 0 | **before the OTA if it can be.** (b) is what makes every other first-launch risk in §1.2 observable; without it a boot crash introduced by an OTA is invisible. The refresh-blocking half is the other session's A3 — do not re-file it |
| **U9 — The onboarding Step-17 answer is recorded** (client, OTA-safe) | MB-FLOWS-STATE-06 = MB-RECONCILE-17 | `src/screens/onboarding/OnboardingFlow.tsx`, `src/services/onboardingDraft.ts`, `src/screens/ProfileScreen.tsx` | none | (a) render the real `OnboardingFlow` at step 17, press `s17-not-now`, assert `onComplete.mock.calls[0][0].notifications_enabled === false` — red today (the key is absent in **both** branches, measured through the real component); (b) `buildPreferencesPayload({notifications_enabled:false})` contains the key — red today | comm gate over `OnboardingFlow`/`onboardingDraft`/`ProfileScreen`; jest | **before `ENABLE_REENGAGEMENT_PUSHES` is ever flipped.** Today the consequence is a wrong toggle state in Profile; with that flag on it becomes push sent to users who explicitly declined. Verify the `:476` "persists" handler is a different path before closing |
| **U10 — Arabic rendering pack** (client, OTA-safe) | MB-I18N-RTL-03, -05, -06, -07, -08, -09, -14(c)(d)(f), MB-RECONCILE-18(16) | `src/components/results/{ResultsAccordion,DimensionBars}.tsx`, `src/components/ProfileEditorialSections.tsx`, `src/screens/{ContactUs,InviteeQuiz,Register,History,Home}Screen.tsx`, `src/utils/formatDate.ts`, new `src/utils/{formatNumber,formatPrice}.ts`, `src/i18n/{en,ar}.json`, `eslint.config.js`, `__tests__/rtl/textAlignLogical.contract.test.ts` | none | (a) **invert the fence first**: rewrite `textAlignLogical.contract.test.ts:52-53` to require the bare physical value and forbid the `I18nManager.isRTL` ternary — red on all 5 regressed sites; (b) `formatPrice(12.345,'BHD')` === `'12.345 د.ب'` — red at every existing site; (c) an Arabic alert-body test asserting no ASCII letters on the two HomeScreen error paths; (d) a catalog fence asserting one digit system across `ar.json` — red today (32 vs 21) | comm gate over every touched screen/component + `formatDate`; jest; `tsc` exit 0; **plus the on-device Arabic walkthrough that has never happened** before this is called done | **(a) is a revert of a wrong fix and goes first** — the regression is main-only, so shipping the OTA without it would be the first time a user ever sees it. The rest is one pack because they are all "one number, one string, rendered four ways" |
| **U11 — Bundle diet** (client, OTA-safe, gated on a measured before/after export) | MB-STARTUP-BUNDLE-01 = MB-RECONCILE-09, -05, -06 = MB-RECONCILE-10, -08, -13 | new `metro.config.js`, `babel.config.js`, `src/theme/fonts.ts`, `app.json` (expo-font plugin list), the 34 lucide import sites | none | (a) a build-assertion test over the exported source map asserting fewer than 100 modules under `lucide-react-native/…/icons/` — red today at 1,703; cheaper red first: an eslint `no-restricted-imports` banning the bare barrel — red on all 34 sites; (b) no source path matching `@sentry-internal/(replay\|replay-canvas\|feedback)` — red today (4 modules); (c) the exported asset manifest contains only registered TTFs — red today at 11 | comm gate over `fonts.ts` + anything referencing lucide; jest; `tsc` exit 0; **exit gate = two `expo export` runs, byte-diffed**, plus a smoke that a thrown error still reaches the Sentry DSN | **after U8** — it changes module-evaluation order, which is exactly what U8's Sentry ordering protects. Coordinate with the other session's remaining B1; whichever session runs first owns it. Mind the exports-map trap in §1.4 |
| **U12 — Error copy the user can act on** (client OTA + one backend contract choice) | MB-NETWORK-CONTRACT-05, -06, MB-RECONCILE-18(12) | `src/screens/{Home,Profile,History,EditProfile,Results}Screen.tsx`, `src/utils/failureClassification.ts`, `src/i18n/{en,ar}.json`; backend `app/api/auth_routes.py` **or** new single-purpose preference routes | none | (a) feed a synthetic `429 {code:'RATE_LIMITED'}` and assert the alert body is `t('home.errors.rateLimited')` and never contains "Rate limit exceeded" — red today (zero `RATE_LIMITED` references exist in the client); (b) render `ProfileScreen` with `getPreferences → null`, tap the AI-sharing switch, assert no `priorities:[]` PUT is issued (or that the rendered error is not the raw Pydantic string) — red today | comm gate over the screens + `auth_routes`; jest + pytest | **before any traffic.** The 429 path is the failure mode a launch-day spike produces first, and today it tells text users their *products* are wrong, which generates more requests against the same bucket. (b) is a privacy control (`ai_sharing_enabled`) a user without a preferences row cannot exercise at all |
| **U13 — Password reset exists** (client OTA + backend deploy + Ahmed's Supabase dashboard) | MB-FLOWS-STATE-02 | `App.tsx` (linking config + unauth stack), new `src/screens/ResetPasswordScreen.tsx`, `app/services/auth_service.py` | none | (a) call the real `getStateFromPath('reset-password#access_token=…&type=recovery', linkingConfig)` and assert the state names a `ResetPassword` route — red today; (b) pytest asserting `request_password_reset` forwards a non-empty `redirect_to` | comm gate over `App.tsx`/`auth_service`; jest + pytest | **App Store gate**, blocked on Ahmed adding `qaren://reset-password` to the Supabase redirect allow-list. Until it ships, `ForgotPasswordScreen`'s success copy is a promise the product cannot keep — change that copy in the same OTA |
| **U14 — The native-only App Store bundle** (needs-build, ONE `eas build`) | MB-TWO-LEVER-RELEASE-02, -08, -09, MB-SAFE-HYGIENE-01 = MB-TWO-LEVER-RELEASE-12, MB-SAFE-HYGIENE-06 | `app.json`, `package.json`, `assets/{icon,splash-icon,adaptive-icon}.png`, new `docs/privacy-data-inventory.md` | none | (a) a config test asserting `android.permissions` excludes `RECORD_AUDIO` **and** both plugin entries set `microphonePermission:false` — red today; (b) the three PNG SHA-256s differ from the recorded template hashes and `splash != adaptive` — red on all four assertions; (c) `ios.privacyManifests.NSPrivacyCollectedDataTypes` is non-empty and equals the committed inventory — red today (key absent); (d) a `package.json` test that every runtime dep is imported or allowlisted with a justification — red on exactly one name (`react-native-gesture-handler`) | comm gate n/a (config/assets); `npx expo prebuild --clean` then assert `NSMicrophoneUsageDescription` absent from the Info.plist and `RECORD_AUDIO` absent from the manifest; `npx expo-doctor`; smoke the tab + modal navigation on a preview build | **all four are native-baked, so they land in ONE build, not four.** Sequence with the icon re-render so the first production `eas build` is also the last rehearsal; consider switching `runtimeVersion.policy` to `fingerprint` in the same build (§5) |

**U3's usage-gate tests-first list, in full.** Each must be RED at `76ace90`, and each asserts the **spend**, not only the status — a status-only assertion would pass a "fix" that returns 429 *after* the Vision call:

1. `POST /api/v1/url/compare` with two extractable URLs returns **200** with `success:true` — red today: the tuple unpack raises and the route fails.
2. Authenticated caller over the lifetime/daily/monthly cap on `POST /api/v1/image/identify` → **429 `USAGE_LIMIT`**, `consume_comparison_credit` awaited exactly once **and the Vision mock awaited 0 times**.
3. The same-shaped control on `POST /api/v1/text/compare` stays green (it is the reference bracket at `text_routes.py:193/:247/:278`).
4. `image/identify` identifying fewer than 2 products → `refund_comparison_credit` awaited exactly once.
5. `image/identify` vision-parse error path (`image_routes.py:146`) → refund once. *(Missing from the finder's fix; the second-vote agent caught it.)*
6. `image/identify` L4 moderation `CONTENT_UNAVAILABLE` path (`:175`) → refund once.
7. `image/identify` success → `record_lifetime_comparison` awaited exactly once, alongside the existing `save_comparison_and_track_cohort`.
8. Authenticated caller over the cap on `POST /api/v1/url/compare` → 429 `USAGE_LIMIT`, and `compare_from_urls` **not** called.
9. `url/compare` success → `save_comparison_and_track_cohort` scheduled with `input_type='url'`, so URL compares finally reach History.
10. `url/compare` with a posted `selected_category` → it reaches `compare_from_urls` (or the client send site at `HomeScreen.tsx:433` is deleted and a test pins that it is not sent).
11. **Anonymous** caller with both new flags unset → the route makes byte-identically the same calls as at `76ace90` (no consume, no refund, no record).
12. A guard test asserting `refund_anon_comparison_credit` has at least one caller before `ENABLE_ANON_USAGE_GATE` can be enabled (`#128`). This unit must not flip that flag, and `MB-NETWORK-CONTRACT-09` shows why: the client never sends `X-Device-Fingerprint` on the only gated route it calls, so flipping it today changes nothing observable.

**Not units — Ahmed / product decisions, listed so they are not mistaken for engineering work:** publish the OTA (§0.2); attach the `qaren.app` origin and serve the two well-known files with a real cert fingerprint (`MB-FLOWS-STATE-01`); the legal redraft and the ASC labels (`MB-SAFE-HYGIENE-02`, `MB-TWO-LEVER-RELEASE-07`); the icon re-render (feeds U14); **whether Results should have an outbound retailer link at all** (`MB-RECONCILE-15` — `openRatingSource` is dead code with zero call sites and `price.url` is rendered nowhere; `git` shows `f99807a` deleted three working `onPress` sites, so this is a *lost* affordance, not a scope decision, but the call is Ahmed's); and the History pagination, `category_switched` banner and Arabic digit-policy calls from `MB-RECONCILE-18`.

---

## 3 What could not be known

- **No device and no emulator.** Every wall-clock startup number, frame rate, VoiceOver/TalkBack behaviour, permission-dialog timing and "what an Arabic phone actually paints" claim here is derived from code plus library source, not observed. The on-device Arabic walkthrough CLAUDE.md has been promising since M21 still has not happened.
- **No live compare.** OpenAI was `429 insufficient_quota` and Serper's balance negative at review time, so the metering bypasses, the URL failure and the error-copy paths were reproduced through the runtime modules and the ASGI app, never end to end against real providers.
- **EAS channel state is taken on trust.** No EAS CLI or device access, so "phones run `97b5f15`" comes from the handoff, not from `eas update:list`. M18 MB-two-lever-03 flagged that the `270a240` cert-pin OTA has **no recorded group id** — if it never fired, devices are *further* behind than this report assumes.
- **The lucide saving is arithmetic, not a second measured build.** HARD RULE 1 forbade editing the tree, so no modified bundle was exported. The same applies to every other removal figure: they are node_modules/asset bytes and source-map attribution, not measured bundle deltas.
- **Android byte attribution was not run** (no `--dump-sourcemap` on the Android export); only the total 8,285,250 B and its asset manifest were measured.
- **Supabase auth configuration was never read** — Site URL, redirect allow-list, email templates, whether email confirmation is even required. That is the confidence cap on U13.
- **Google Sign-In's failure mode is still unknown.** The instrumentation ships and is safe, but it is unreadable until the Sentry init ordering (U8) and sourcemap upload are fixed.
- **`eslint mode:'all'` never ran** (flat-config resolution failed twice from the scratchpad), so "how many literal strings would a real fence report?" has no authoritative number — only the enumerated-surface sweep.
- **Hermes' `Intl.PluralRules` for `ar`** and **`android:supportsRtl` in the generated manifest** are both unverifiable without a build; if the latter is false, the RTL findings get worse, not better.
- **Two coverage numbers disagree** — 76.09% (finder, with an explicit `collectCoverageFrom`) versus 84.26% over 123 files (verifier, default run, with `App.tsx`/`ResultsScreen.tsx`/`usageService.ts` *absent* rather than at 0%). The qualitative claim (those three files are executed by no test) is solid; the headline percentage is not, and work on `MB-TESTS-TRUTH-11`'s floor must re-derive it before setting a threshold.
- **The second-vote agent died on 18 P1 rows.** I re-derived each myself in this run — the greps, the git ancestry checks, the SHA-256s, the `generate_comparison` tuple, the `api.ts` timeout constants — but that is one reviewer (me) instead of two, and it is recorded as `synth-2nd`, not as an independent vote.
- **Coordination risk, not a knowledge gap:** three P1s are already fixed on the other session's **unmerged, unreviewed** `feature/m23-mobile-w1`. This report assumes their merge; if it is abandoned, those three come back.

---

## 4 Findings tables


**Tally after both verification stages, the Fable review notes and this run's adjudications: P0 0 / P1 23 / P2 77 / P3 42 / refuted 0.** 142 findings filed, 142 first-verified, 13 second-voted before that agent died; the remaining 18 P0/P1 rows were re-derived in this synthesis run against code I opened myself and are marked `synth-2nd`. A row whose `dedupe` names a CLUSTER is the same defect another lane also filed - count it once.

| lane | P1 | P2 | P3 | filed |
|---|---|---|---|---|
| mb-reconcile | 6 | 10 | 3 | 19 |
| mb-network-contract | 6 | 6 | 4 | 16 |
| mb-flows-state | 5 | 9 | 3 | 17 |
| mb-startup-bundle | 2 | 9 | 3 | 14 |
| mb-i18n-rtl | 0 | 9 | 5 | 14 |
| mb-safe-hygiene | 2 | 3 | 9 | 14 |
| mb-render-perf-a11y | 0 | 11 | 4 | 15 |
| mb-tests-truth | 1 | 15 | 2 | 18 |
| mb-two-lever-release | 1 | 5 | 9 | 15 |

### P1 - 23 rows

| id | title | file:line | M/M | verdict trail | dedupe / cluster |
|---|---|---|---|---|---|
| MB-FLOWS-STATE-01 | Every referral share link points at qaren.app, which returns Cloudflare 522 for every path — the entire ENABLE_REFERRAL_SYSTEM loop ha... | `app/services/referral_service.py:348` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-FLOWS-STATE-02 | Password reset is unrecoverable: the app has no recovery deep link or set-new-password screen anywhere, and the reset email is sent wi... | `SmartCompareApp/App.tsx:283` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-FLOWS-STATE-04 | Both escape hatches on the referral landing/quiz screens dispatch reset->'Main', a route not registered while unauthenticated — the ro... | `SmartCompareApp/src/screens/ReferralLandingScreen.tsx:118` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-FLOWS-STATE-05 | Home 'Smart pick' and Profile 'Recent decisions' navigate to Results with a `from_history` param that ResultsScreen never reads — both... | `SmartCompareApp/src/screens/HomeScreen.tsx:860` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:OTHER-SESSION | CLUSTER-> MB-RECONCILE-05; known:mobile-checkup adjacent-observation (flows-triggers finder P1, ne... |
| MB-FLOWS-STATE-06 | The onboarding Step-17 notifications answer is dropped by a stale closure AND is absent from every persistence payload, so Profile the... | `SmartCompareApp/src/screens/onboarding/OnboardingFlow.tsx:393` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | known:mobile-checkup flows lead ('onboarding Step-17 notifications answ... |
| MB-NETWORK-CONTRACT-01 | Every in-app result feedback is 422-rejected and lost: FeedbackCard's chips (accurate/detailed/fast) are not in the backend's VALID_MA... | `SmartCompareApp/src/components/FeedbackCard.tsx:18` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-NETWORK-CONTRACT-02 | A transient Supabase outage force-logs-out every user: POST /auth/refresh returns 401 for its OWN upstream connection failure, and the... | `app/api/auth_routes.py:539` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-NETWORK-CONTRACT-03 | Google/Apple signups never carry X-Device-Fingerprint and the social backend path never writes users.device_fingerprint_hash — the fre... | `SmartCompareApp/src/services/authService.ts:570` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-NETWORK-CONTRACT-04 | The referral SAME_DEVICE abuse control can never fire: ShareBottomSheet's deviceFingerprintHash prop has no caller, so referral_invite... | `SmartCompareApp/src/components/ShareBottomSheet.tsx:159` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |
| MB-NETWORK-CONTRACT-07 | No compare response ever echoes the saved comparison_id, so the referral share sheet is unreachable at the one high-intent moment and ... | `app/api/text_routes.py:291` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | known:M18 MB-contract-03 (partial fix — History path only) — residual s... |
| MB-NETWORK-CONTRACT-14 | POST /image/identify is still completely unmetered for authenticated users, and POST /url/compare still has no auth dependency, no met... | `app/api/image_routes.py:89` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:DEDUPE | CLUSTER-> MB-RECONCILE-01 + MB-RECONCILE-07; known:mobile-checkup #1 and #6 — both still-open at HEAD, re-verified p... |
| MB-RECONCILE-01 | STILL OPEN: POST /image/identify runs a paid Vision+compare with no per-user metering for signed-in callers | `app/api/image_routes.py:65` | static | verify:CONFIRMED 2nd:CONFIRMED fable:AGREE | known:mobile-checkup-A#1 still-open (also M18 metering cluster) — uncha... |
| MB-RECONCILE-03 | STILL OPEN: boot awaits a network token refresh (no per-call timeout, 120s global ceiling) before clearing the splash gate; the cached... | `SmartCompareApp/App.tsx:189` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:OTHER-SESSION | known:mobile-checkup-A#3 still-open — re-verified against the M21 W2/W3... |
| MB-RECONCILE-05 | STILL OPEN: Home 'Smart pick' and Profile 'Recent decisions' navigate to Results with a from_history param no screen reads — both taps... | `SmartCompareApp/src/screens/HomeScreen.tsx:860` | static | verify:CONFIRMED 2nd:CONFIRMED fable:OTHER-SESSION | known:mobile-checkup-A adjacent-observation (unverified P1 lead) — now ... |
| MB-RECONCILE-06 | TWO-LEVER: the 97b5f15 phone OTA predates ALL FOUR M21 waves (incl. W1), so on devices every Results analytics/feedback batch still 42... | `SmartCompareApp/src/screens/ResultsScreen.tsx:345` | measured | verify:CONFIRMED 2nd:CONFIRMED fable:AGREE | known:M18 MB-two-lever-01/02 regressed-scope — the gap is LARGER than M... |
| MB-RECONCILE-07 | STILL OPEN: POST /url/compare has no user dependency, no metering and no history save, and silently discards the client's selected_cat... | `app/api/url_routes.py:128` | static | verify:UPGRADED 2nd:UPGRADED fable:UPGRADED | known:mobile-checkup-A#6 still-open — unchanged since 79a4594 |
| MB-RECONCILE-15 | STILL OPEN: the Results screen has no outbound retailer link anywhere — openRatingSource is dead code with zero call sites and price.u... | `SmartCompareApp/src/screens/ResultsScreen.tsx:500` | static | verify:CONFIRMED 2nd:UPGRADED fable:PRODUCT-CALL | known:mobile-checkup-B flows lead (never verified) — now confirmed stil... |
| MB-SAFE-HYGIENE-01 | App Store blocker ICN-0001 is UNCHANGED at HEAD: the three launcher PNGs still carry the exact SHA-256s recorded as Expo-template-iden... | `SmartCompareApp/app.json:7` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:STATUS | known:CLAUDE.md App-Store-blocker #1 / docs/plans/bundle-d-followups.md... |
| MB-SAFE-HYGIENE-02 | Both legal documents still open with a DRAFT disclaimer at HEAD and prod is serving that text to the app today | `app/legal/privacy_policy.md:7` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:STATUS | known:CLAUDE.md App-Store-blocker #2 — still-open (fresh prod evidence ... |
| MB-STARTUP-BUNDLE-01 | lucide barrel ships 1,703 icon modules for 68 used, AND runs all 1,706 of their factories on the cold-start critical path: 1,212,812 B... | `SmartCompareApp/App.tsx:18` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | known:mobile (unverified lead, '~1,682 icons = 1.27 MB / ~25% of an 8.2... |
| MB-STARTUP-BUNDLE-02 | Boot still blocks the splash on a token refresh with a 120 s ceiling and no per-call deadline - unchanged at HEAD after M21 touched au... | `SmartCompareApp/App.tsx:189` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:OTHER-SESSION | CLUSTER-> MB-RECONCILE-03; known:mobile #3 (P1, verified at 79a4594) - status STILL-OPEN at 76ace9... |
| MB-TESTS-TRUTH-04 | HistoryScreen's useFocusEffect freezes render-0's loadHistory, so refocusing refetches unfiltered while the search box still shows the... | `SmartCompareApp/src/screens/HistoryScreen.tsx:664` | static | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:OTHER-SESSION | new |
| MB-TWO-LEVER-RELEASE-02 | App requests RECORD_AUDIO and ships a boilerplate NSMicrophoneUsageDescription although no audio/microphone API is used anywhere in th... | `SmartCompareApp/app.json:73` | measured | verify:CONFIRMED 2nd:agent-died -> synth-2nd:CONFIRMED fable:AGREE | new |

### P2 - 77 rows

| id | title | file:line | M/M | verdict trail | dedupe / cluster |
|---|---|---|---|---|---|
| MB-FLOWS-STATE-03 | Paywall 'Terms' and 'Privacy' links fire a 'Coming soon' alert instead of opening the Legal screen that exists and works — dead contro... | `SmartCompareApp/src/screens/PaywallScreen.tsx:397` | measured | verify:DOWNGRADED | new |
| MB-FLOWS-STATE-07 | The OS push-permission prompt is fired at signup, 17 steps before Step 17 asks for it — burning the one-shot iOS prompt and making Ste... | `SmartCompareApp/App.tsx:246` | static | verify:CONFIRMED | new |
| MB-FLOWS-STATE-08 | No Terms or Privacy link exists anywhere before sign-in, and the Legal route is not even registered in the unauthenticated branch | `SmartCompareApp/src/screens/RegisterScreen.tsx:465` | static | verify:CONFIRMED | new |
| MB-FLOWS-STATE-09 | Login screen ships two dead controls: the back arrow (nothing to pop to) and the 'Email' social pill (its handler sets an already-empt... | `SmartCompareApp/src/screens/LoginScreen.tsx:307` | measured | verify:CONFIRMED | known:mobile-checkup flows lead ('Login back arrow + Email social pill ... |
| MB-FLOWS-STATE-10 | The logout confirmation shows irreversible account-DELETION copy in every language except English — the EN-only .replace() never match... | `SmartCompareApp/src/screens/ProfileScreen.tsx:305` | static | verify:CONFIRMED | known:mobile-checkup flows lead ('Arabic logout confirmation shows acco... |
| MB-FLOWS-STATE-11 | The Results screen has zero outbound purchase or source links — openRatingSource is defined and never called, and price.url is never r... | `SmartCompareApp/src/screens/ResultsScreen.tsx:500` | static | verify:CONFIRMED | CLUSTER-> MB-RECONCILE-15; known:mobile-checkup flows lead ('Results has no outbound purchase/sour... |
| MB-FLOWS-STATE-12 | History search is silently reverted on every screen refocus by a stale useFocusEffect closure, submit blanks the whole screen includin... | `SmartCompareApp/src/screens/HistoryScreen.tsx:664` | static | verify:CONFIRMED | CLUSTER-> MB-TESTS-TRUTH-04; known:mobile-checkup #12 (flagged 'M21 risk HIGH — RE-VERIFY FIRST; lik... |
| MB-FLOWS-STATE-13 | Camera shutter and gallery-pick failures are swallowed with an empty catch — a failed capture is an invisible dead tap on the primary ... | `SmartCompareApp/src/screens/ScanCameraScreen.tsx:190` | static | verify:CONFIRMED | known:mobile-checkup flows lead ('camera capture failures are silent') ... |
| MB-FLOWS-STATE-17 | Nothing survives an app kill mid-onboarding: the 17-step flow holds all answers in component state and writes its first draft only aft... | `SmartCompareApp/src/screens/onboarding/OnboardingFlow.tsx:206` | static | verify:CONFIRMED | new (M18 MB-flows-04 covered the POST-completion draft/retry only; this... |
| MB-I18N-RTL-01 | First launch on an Arabic-locale device runs the whole session LTR — boot calls forceRTL with no reload (M13-62, still open) | `SmartCompareApp/App.tsx:176` | static | verify:DOWNGRADED | new |
| MB-I18N-RTL-02 | Two contradictory definitions of isRTL ship side by side — useLanguage().isRTL (i18n.language) vs I18nManager.isRTL (boot constant) — ... | `SmartCompareApp/src/hooks/useLanguage.ts:8` | static | verify:DOWNGRADED | new |
| MB-I18N-RTL-03 | M21 W4's textAlign ternary double-flips: RN mirrors physical left/right itself, so 5 sites regressed vs 593ec1e and a contract test fo... | `SmartCompareApp/src/components/results/ResultsAccordion.tsx:888` | static | verify:DOWNGRADED | known:M21 W4 MB-i18n-rtl-05 + regressed |
| MB-I18N-RTL-04 | Cairo is loaded, blocks first paint, and is never applied to a single character — fontFamily.ar has zero consumers and the typography ... | `SmartCompareApp/src/theme/fonts.ts:26` | static | verify:DOWNGRADED | new |
| MB-I18N-RTL-05 | Raw English backend error prose is still shown to Arabic users at two of the three HomeScreen alert sites — M18's guard only covered t... | `SmartCompareApp/src/screens/HomeScreen.tsx:355` | measured | verify:DOWNGRADED | known:M18 MB-contract-02 + still-open (fix applied to 1 of 3 sites) |
| MB-I18N-RTL-07 | BHD's 3 decimals are lost on History (toFixed(2)) and Home/Profile (toFixed(0)), Results uses the device default, and the symbol swaps... | `SmartCompareApp/src/screens/HistoryScreen.tsx:715` | measured | verify:CONFIRMED | new |
| MB-I18N-RTL-08 | Relative-time copy bypasses the catalog entirely: hard-coded Arabic with one wrong plural form for every count, hard-coded English abb... | `SmartCompareApp/src/utils/formatDate.ts:28` | static | verify:CONFIRMED | new |
| MB-I18N-RTL-09 | The literal-string fence measures 0 violations while user-visible English still ships — mode:'jsx-text-only' cannot see attributes, Al... | `SmartCompareApp/eslint.config.js:52` | measured | verify:CONFIRMED | new |
| MB-I18N-RTL-11 | Choosing العربية hard-restarts the app with no warning — at onboarding step 2 it drops the user back to step 1, and from Profile it ki... | `SmartCompareApp/src/screens/onboarding/Step02Language.tsx:29` | static | verify:CONFIRMED | new |
| MB-NETWORK-CONTRACT-05 | RATE_LIMITED is unmapped everywhere on the client: a throttled text compare tells the user to "choose different products", and the URL... | `SmartCompareApp/src/screens/HomeScreen.tsx:470` | static | verify:CONFIRMED | new (adjacent to known:M18 MB-flows-11 which covered AUTH screens only) |
| MB-NETWORK-CONTRACT-06 | Profile privacy/notification toggles 422 for any user without a preferences row: buildNextPrefs sends priorities:[] against a min_leng... | `SmartCompareApp/src/screens/ProfileScreen.tsx:134` | measured | verify:CONFIRMED | new |
| MB-NETWORK-CONTRACT-08 | An expired access token silently downgrades every compare to anonymous, and the 401-refresh interceptor can NEVER re-drive it because ... | `app/api/auth_routes.py:306` | static | verify:CONFIRMED | known:mobile-checkup section B lead 'expired access token silently down... |
| MB-NETWORK-CONTRACT-09 | ENABLE_ANON_USAGE_GATE is a placebo: the only gated route the app reaches (/image/identify) is never sent X-Device-Fingerprint, and th... | `SmartCompareApp/src/services/api.ts:216` | static | verify:CONFIRMED | new (precondition for known:#128) |
| MB-NETWORK-CONTRACT-12 | Social-login raw fetches still have no timeout and no AbortController — a stalled sign-in hangs until the OS socket timeout (camera id... | `SmartCompareApp/src/services/authService.ts:570` | static | verify:CONFIRMED | known:mobile-checkup #8 — camera half FIXED (api.ts:229-307), social-lo... |
| MB-NETWORK-CONTRACT-13 | Force-update remains a dead wire: GET /api/v1/app/version is mounted and configurable but has zero client call sites, so APP_FORCE_UPD... | `app/api/version_routes.py:19` | static | verify:CONFIRMED | known:mobile-checkup #9 — still-open at HEAD |
| MB-RECONCILE-02 | Synthetic loading checklist hits all-done at 4.5s (STAGE_CYCLE_MS 900 x 5) while the shipped client never streams (ENABLE_EXPO_FETCH_S... | `SmartCompareApp/src/config/features.ts:52` | measured | verify:DOWNGRADED | known:mobile-checkup-A#2 still-open (also M18 MB-perf-01 which fixed th... |
| MB-RECONCILE-04 | Home compare loader has no cancel affordance (abortRef fires only on unmount); the "no client watchdog / unbounded 120s" half is alrea... | `SmartCompareApp/src/screens/HomeScreen.tsx:881` | static | verify:DOWNGRADED 2nd:CONFIRMED fable:SYNTH-DOWNGRADE | known:mobile-checkup-A#4 still-open (cancel+watchdog half); the camera-... |
| MB-RECONCILE-08 | STILL OPEN (M18 CD-uncovered-05 / mobile-checkup #9): force-update is a dead wire — GET /api/v1/app/version is served but has zero cli... | `app/api/version_routes.py:19` | static | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-TWO-LEVER-RELEASE-14; known:mobile-checkup-A#9 still-open — unchanged since 79a4594 |
| MB-RECONCILE-09 | The whole lucide-react-native barrel (1,703 icon modules, ~1.34 MB of the bundle's 10.06 MB source) ships for 65 used icons; no transf... | `SmartCompareApp/babel.config.js:50` | modelled | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-STARTUP-BUNDLE-01; known:mobile-checkup-B lead (P1 bundle, never verified) — now confirmed... |
| MB-RECONCILE-10 | STILL OPEN: the Cairo barrel import ships all 8 font weights; 5 are never referenced, adding ~470 KB (not 898 KB) of unused ttf to eve... | `SmartCompareApp/src/theme/fonts.ts:17` | measured | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-STARTUP-BUNDLE-06; known:mobile-checkup-B startup lead ('5 unused Cairo weights ~470 KB', ... |
| MB-RECONCILE-11 | STILL OPEN: get_optional_user cannot distinguish "no credential" from "rejected credential" — verify_token returns None for an expired... | `app/api/auth_routes.py:306` | static | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-NETWORK-CONTRACT-08; known:mobile-checkup-B lead (P1 network, never verified) — now confirme... |
| MB-RECONCILE-12 | History search submit blanks the screen to a bare spinner, and the empty-dep useFocusEffect refetches unfiltered on every tab refocus,... | `SmartCompareApp/src/screens/HistoryScreen.tsx:837` | static | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-TESTS-TRUTH-04; known:mobile-checkup-A#12 still-open — re-verified against the M21 W3 6... |
| MB-RECONCILE-13 | STILL OPEN: three Reanimated animations drive shared values that are never bound to render — the winner celebration, the onboarding gl... | `SmartCompareApp/src/components/hero/RevealBurst.tsx:104` | static | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-RENDER-PERF-A11Y-02; known:mobile-checkup-A#10 + mobile-checkup-B render lead (LoadingRings)... |
| MB-RECONCILE-14 | STILL OPEN after the W4 RTL sweep: the Arabic logout confirmation keeps "cannot be undone" copy, because the string surgery that strip... | `SmartCompareApp/src/screens/ProfileScreen.tsx:305` | static | verify:CONFIRMED 2nd:CONFIRMED | CLUSTER-> MB-FLOWS-STATE-10; known:mobile-checkup-B flows lead ('W4 was the RTL sweep — likely fixed... |
| MB-RECONCILE-17 | STILL OPEN: onboarding Step 17's notifications answer is collected into flow state but is in none of the three persistence buckets, so... | `SmartCompareApp/src/services/onboardingDraft.ts:94` | static | verify:CONFIRMED | CLUSTER-> MB-FLOWS-STATE-06; known:mobile-checkup-B flows lead ('onboarding Step-17 notifications an... |
| MB-RENDER-PERF-A11Y-02 | LoadingRings still reads SharedValue.value during render — rings freeze after 2.4s; and with OS Reduce Motion on, Reanimated 4.1.7 res... | `SmartCompareApp/src/components/hero/LoadingRings.tsx:134` | static | verify:CONFIRMED | known:M18/MB-perf-04 still-open (the render-read + freeze) — but the fi... |
| MB-RENDER-PERF-A11Y-04 | The brand emerald #10B981 measures 2.54:1 on white — it fails WCAG AA for normal text, for large text, and for UI components — and it ... | `SmartCompareApp/src/theme/index.ts:19` | measured | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-05 | 42 of 128 interactive elements ship with no accessibilityRole — including three icon-only Results back buttons that announce nothing a... | `SmartCompareApp/src/screens/ResultsScreen.tsx:629` | measured | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-06 | 36 tap targets are smaller than 44pt and 25 of them have no hitSlop — including every Results header button (36x36) and the Profile la... | `SmartCompareApp/src/screens/ResultsScreen.tsx:921` | measured | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-07 | Nested touchables hide the inner control from VoiceOver: the History row's delete button and the camera slot's remove button sit insid... | `SmartCompareApp/src/screens/HistoryScreen.tsx:628` | static | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-09 | The app's only virtualized list ships with zero tuning — no initialNumToRender, windowSize, removeClippedSubviews, maxToRenderPerBatch... | `SmartCompareApp/src/screens/HistoryScreen.tsx:891` | static | verify:CONFIRMED | known:M18/MB-perf-07 covered the row stagger and the per-keystroke row ... |
| MB-RENDER-PERF-A11Y-10 | Nothing in the app queries AccessibilityInfo, and four components' docstrings claim a useReducedMotion behaviour that does not exist —... | `SmartCompareApp/src/components/hero/LoadingRings.tsx:20` | static | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-11 | The 25-31s compare loading screen is silent to screen readers — no live region, no announcement — and StageChecklist labels each stage... | `SmartCompareApp/src/screens/LoadingScreenVariants.tsx:252` | static | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-12 | The Paywall ships as a transparentModal with no accessibilityViewIsModal — VoiceOver can walk straight past it into the Home screen be... | `SmartCompareApp/src/screens/PaywallScreen.tsx:250` | static | verify:CONFIRMED | new |
| MB-RENDER-PERF-A11Y-13 | ProductImage's downsampling fix is Android-only — iOS still decodes full-resolution og:image bitmaps into 64-160pt tiles, and expo-ima... | `SmartCompareApp/src/components/primitives/ProductImage.tsx:111` | static | verify:CONFIRMED | known:M18/MB-perf-08 PARTIALLY-FIXED — resizeMethod="resize" landed in ... |
| MB-RENDER-PERF-A11Y-14 | History's focus effect pins the first render's closure, so every tab return refetches unfiltered and blanks the screen — eslint flags ... | `SmartCompareApp/src/screens/HistoryScreen.tsx:664` | static | verify:CONFIRMED | CLUSTER-> MB-TESTS-TRUTH-04; known:mobile-checkup #12 STILL-OPEN — the checkup flagged it at pre-M21... |
| MB-SAFE-HYGIENE-03 | The whole Geist+Cairo type system is loaded at boot and never applied: `fontFamily` has zero importers and `typography` sets no fontFa... | `SmartCompareApp/src/theme/fonts.ts:19` | static | verify:CONFIRMED | CLUSTER-> MB-I18N-RTL-04; new |
| MB-SAFE-HYGIENE-04 | useTypography (the Arabic 1.7x line-height rule) has zero production importers — the AR readability adjustment in the design spec neve... | `SmartCompareApp/src/hooks/useTypography.ts:38` | static | verify:CONFIRMED | CLUSTER-> MB-RENDER-PERF-A11Y-03; new |
| MB-SAFE-HYGIENE-05 | ResultsScreen.tsx carries a fully dead legacy render path — 56 of the app's 80 unused-vars warnings, including 11 component imports an... | `SmartCompareApp/src/screens/ResultsScreen.tsx:94` | measured | verify:CONFIRMED | new (the M21 waves touched ResultsScreen.tsx but no prior review enumer... |
| MB-STARTUP-BUNDLE-03 | Four boot awaits sit OUTSIDE init()'s try/catch and setIsLoading(false) is not in a finally - one rejection strands the app on the spl... | `SmartCompareApp/App.tsx:168` | measured | verify:CONFIRMED | known:mobile ('Startup leads: init() has unguarded awaits before its tr... |
| MB-STARTUP-BUNDLE-05 | Sentry's browser Session Replay, Replay-Canvas and Feedback widget (198,567 B) are bundled and executed in the React Native app althou... | `SmartCompareApp/src/services/sentry.ts:227` | measured | verify:CONFIRMED | new |
| MB-STARTUP-BUNDLE-06 | Five Cairo font weights that are never registered ship in every build: 469,608 B = 41.0% of the entire OTA asset payload | `SmartCompareApp/src/theme/fonts.ts:40` | measured | verify:CONFIRMED | known:mobile ('5 unused Cairo font weights (~470 KB) ship in every buil... |
| MB-STARTUP-BUNDLE-07 | initSentry() runs after the whole import graph is evaluated - ESM import hoisting defeats the invariant the comment above it asserts, ... | `SmartCompareApp/App.tsx:10` | measured | verify:CONFIRMED | CLUSTER-> MB-TWO-LEVER-RELEASE-03; new |
| MB-STARTUP-BUNDLE-09 | The boot path calls I18nManager.forceRTL() with no reloadAsync, so an Arabic device's very first launch renders the whole app left-to-... | `SmartCompareApp/App.tsx:176` | static | verify:CONFIRMED | CLUSTER-> MB-I18N-RTL-01; new (M18 has MB-i18n-rtl-02/03/04/05/09 but none covers the boot-time f... |
| MB-STARTUP-BUNDLE-10 | Push-token registration re-runs its full network dance on every authed cold start and can raise the OS notification-permission dialog ... | `SmartCompareApp/src/services/pushTokenService.ts:22` | static | verify:CONFIRMED | new |
| MB-STARTUP-BUNDLE-11 | Home's focus effect fires a /health probe whose result is discarded and refires on every tab focus, alongside usage, referral and a on... | `SmartCompareApp/src/screens/HomeScreen.tsx:204` | static | verify:CONFIRMED | known:mobile ('first interactive paint fires 4 separate network calls i... |
| MB-STARTUP-BUNDLE-12 | 1.5 s JS splash floor with no expo-splash-screen hand-off: the package is not installed, so the native splash cannot be held over Herm... | `SmartCompareApp/src/screens/SplashScreen.tsx:33` | static | verify:CONFIRMED | known:mobile #5 (P2, verified at 79a4594) - status STILL-OPEN at 76ace9... |
| MB-STARTUP-BUNDLE-13 | All 20 screens are statically imported by App.tsx with inlineRequires off, so markdown-it + react-native-markdown-display (~80 KB, ~1.... | `SmartCompareApp/App.tsx:47` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-01 | ResultsScreen.tsx is executed by zero tests (0/309 statements): all 12 dedicated suites read it as TEXT via readFileSync | `SmartCompareApp/src/screens/ResultsScreen.tsx:1` | measured | verify:DOWNGRADED | CLUSTER-> MB-SAFE-HYGIENE-05; new |
| MB-TESTS-TRUTH-02 | `openRatingSource` has been dead code since 2026-05-30 — no results component renders a tappable rating source, so the whitelisted `so... | `SmartCompareApp/src/screens/ResultsScreen.tsx:500` | static | verify:DOWNGRADED | CLUSTER-> MB-RECONCILE-15; new |
| MB-TESTS-TRUTH-03 | usageService.ts is executed by zero tests (0/23 statements) — and the branch that actually fires in prod is :63, not the :62 legacy on... | `SmartCompareApp/src/services/usageService.ts:59` | measured | verify:DOWNGRADED | new |
| MB-TESTS-TRUTH-05 | The global react-i18next mock has no `ar` entry and a 16-key English dictionary, so no test ever renders an Arabic string | `SmartCompareApp/__mocks__/react-i18next.ts:1` | measured | verify:DOWNGRADED | new |
| MB-TESTS-TRUTH-06 | App.tsx is invisible to both mobile gates: 0/138 statements executed and excluded from the CI eslint glob | `SmartCompareApp/App.tsx:1` | measured | verify:DOWNGRADED | known:mobile — the 120s-splash-refresh P1 was raised pre-M21 against Ap... |
| MB-TESTS-TRUTH-07 | signInWithApple (73 lines) has 0% coverage and the test that would have covered it is one of the 13 it.todo stubs — an App Store launc... | `SmartCompareApp/src/services/authService.ts:639` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-08 | __tests__/setup.ts hard-assigns console.warn/console.error to a noop for all 245 suites, so React act()/key/unmounted-update warnings ... | `SmartCompareApp/__tests__/setup.ts:15` | static | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-09 | ts-jest runs transpile-only (isolatedModules) under a deprecated `globals` block, so jest type-checks nothing — a mock returning a wro... | `SmartCompareApp/jest.config.js:11` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-10 | Mock-vs-real drift: 3 runtime react-native symbols used by src are absent from the mock (Linking, Share, Keyboard), and reanimated's u... | `SmartCompareApp/__mocks__/react-native.ts:121` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-11 | frontend-tests has no coverage floor at all while backend-tests enforces --cov-fail-under=83 — mobile coverage can collapse to zero an... | `.github/workflows/ci.yml:264` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-12 | CI pins node 20 while the dev box runs node 24.11.1, and the repo has no engines field, no .nvmrc and no preinstall check — so neither... | `SmartCompareApp/package.json:64` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-13 | The 3 skipped suites are the ONLY render tests HomeScreen ever had, replaced by source-greps; the 13 todos name the exact untested lau... | `SmartCompareApp/__tests__/HomeScreen.currentDesign.contract.test.ts:20` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-14 | All 10 category fixtures are hand-written and drift from the real payload; a generated schema fixture is the right mechanism but 3 of ... | `SmartCompareApp/__tests__/fixtures/v2_response_electronics.json:1` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-15 | ResultsScreen.tsx carries 56 of the repo's 80 unused-symbol warnings including a duplicate formatPrice that has drifted behind the liv... | `SmartCompareApp/src/screens/ResultsScreen.tsx:408` | measured | verify:CONFIRMED | CLUSTER-> MB-SAFE-HYGIENE-05; new |
| MB-TESTS-TRUTH-16 | referralService runs 0 of its branches and 0 of its functions (17% statements), and api.ts executes only 45% of its exported functions... | `SmartCompareApp/src/services/referralService.ts:72` | measured | verify:CONFIRMED | new |
| MB-TWO-LEVER-RELEASE-01 | qaren.app origin is down (522) so every minted referral/share link and the AASA are unreachable; APP_BASE_URL is a hardcoded literal w... | `app/services/referral_service.py:50` | measured | verify:DOWNGRADED | CLUSTER-> MB-FLOWS-STATE-01; new |
| MB-TWO-LEVER-RELEASE-03 | initSentry() runs AFTER every imported module body (Babel hoists requires), so the new 'running unpinned' security alarm shipping in t... | `SmartCompareApp/App.tsx:10` | measured | verify:CONFIRMED | new |
| MB-TWO-LEVER-RELEASE-04 | ISRG Root YE — the actual root of the live certificate chain — is not pinned; the fleet survives only because Root YE is currently cro... | `SmartCompareApp/src/services/certificatePinning.ts:56` | measured | verify:CONFIRMED | new |
| MB-TWO-LEVER-RELEASE-05 | No sourcemap upload exists on either lever — every JS crash from the pending OTA (and from the installed builds) arrives in Sentry uns... | `SmartCompareApp/src/services/sentry.ts:15` | measured | verify:CONFIRMED | new |
| MB-TWO-LEVER-RELEASE-07 | Privacy policy's 'Information We Collect' omits the demographics and the persistent device identifier the app actually transmits — the... | `app/legal/privacy_policy.md:22` | static | verify:CONFIRMED | new |

### P3 - 42 rows

| id | title | file:line | M/M | verdict trail | dedupe / cluster |
|---|---|---|---|---|---|
| MB-FLOWS-STATE-14 | handleUrlCompare registers no abort handle, so the #118 unmount/logout cancellation covers only the text path | `SmartCompareApp/src/screens/HomeScreen.tsx:426` | static | verify:CONFIRMED | new |
| MB-FLOWS-STATE-15 | The Results error card always offers 'Back to history' even when the user arrived from Home, the smart-pick card, or the camera — and ... | `SmartCompareApp/src/screens/ResultsScreen.tsx:694` | static | verify:CONFIRMED | new |
| MB-FLOWS-STATE-16 | P3 sweep — nine smaller flow/state defects found while walking the 60 navigation edges and 16 declared route names | `SmartCompareApp/src/types/types.ts:617` | measured | verify:CONFIRMED | items 1-8 new; item 9 known:mobile-checkup #5 and #17 — both STILL-OPEN... |
| MB-I18N-RTL-06 | No Arabic numeral policy exists: 32 catalog strings use Arabic-Indic digits, 21 use Western, and four toLocaleString() calls follow th... | `SmartCompareApp/src/i18n/ar.json:1` | measured | verify:DOWNGRADED | new |
| MB-I18N-RTL-10 | Arabic plural completeness is unfenced — both i18n tests pass a 2-form Arabic family, and one live call site passes count as a formatt... | `SmartCompareApp/src/components/results/ResultsAccordion.tsx:664` | measured | verify:DOWNGRADED | known:M21 W4 MB-i18n-rtl-07/08 + still-open (fix covered 2 families, no... |
| MB-I18N-RTL-12 | switchLanguage guards on the stale I18nManager.isRTL, so a language toggle can no-op and leave one boot rendering English copy in an R... | `SmartCompareApp/src/hooks/useLanguage.ts:17` | static | verify:DOWNGRADED | new |
| MB-I18N-RTL-13 | The 'vs' pill and the profile badge use a left:'50%' + translateX centring hack that RN half-mirrors, putting them ~2x the offset off-... | `SmartCompareApp/src/components/HomeEditorialSections.tsx:596` | static | verify:CONFIRMED | new |
| MB-I18N-RTL-14 | P3 sweep — six latent i18n/RTL defects: dead bidi helper with no isolation anywhere, dead/empty catalog keys, an unmirrored shimmer (s... | `SmartCompareApp/src/utils/rtl.ts:13` | static | verify:CONFIRMED | new |
| MB-NETWORK-CONTRACT-10 | url_routes still raises a plain-string 400 instead of the D2 helper (#69) — but the impact is cosmetic today: compare_from_urls emits ... | `app/api/url_routes.py:158` | static | verify:DOWNGRADED | known:#69 (url/quick failures via D2 helper) — still-open; the dead cli... |
| MB-NETWORK-CONTRACT-11 | settle_complete does not set the SSE terminal latch — forward-compat hazard only: the flag is dark, complete always follows, and no sc... | `SmartCompareApp/src/services/api.ts:713` | static | verify:DOWNGRADED | known:mobile-checkup #7 — still-open at HEAD (fresh line api.ts:713) |
| MB-NETWORK-CONTRACT-15 | Neither compare handler has a re-entrancy guard: a double-tap fires two paid backend comparisons and leaks the first abort handle | `SmartCompareApp/src/screens/HomeScreen.tsx:279` | static | verify:DOWNGRADED | known:mobile-checkup section B flows lead ('compare handlers lack a re-... |
| MB-NETWORK-CONTRACT-16 | P3 sweep — eight smaller client/backend contract gaps found while walking the api call sites | `SmartCompareApp/src/services/api.ts:26` | static | verify:CONFIRMED | mixed — (a) known:M18 MB-perf-03 partial + mobile-checkup #3 still-open... |
| MB-RECONCILE-16 | App.tsx init()'s pre-try awaits + bare init() could strand the splash, but both named storage calls swallow internally | `SmartCompareApp/App.tsx:168` | static | verify:DOWNGRADED | known:mobile-checkup-B startup lead ('init() has unguarded awaits befor... |
| MB-RECONCILE-18 | P3 sweep — 17 lower-severity reconciled items still open at HEAD | `SmartCompareApp/src/screens/SplashScreen.tsx:33` | static | verify:CONFIRMED | known:mobile-checkup-A#5,#7,#15,#16,#17 + mobile-checkup-B leads + M18 ... |
| MB-RECONCILE-19 | Reconciliation ledger: 31 prior rows (28 M18 + 3 checkup) fixed or changed at HEAD by M21 W1-W4 — all MAIN-ONLY | `docs/investigations/2026-09-02-mobile-checkup-findings.md:1` | measured | verify:CONFIRMED | known:M18 MB-contract-01/02/03/04/05/06/09/10, MB-flows-01/02/03/04/05/... |
| MB-RENDER-PERF-A11Y-01 | After M21's memo, a winnerRevealed flip still re-renders ~7 unmemoized ResultsContent children (not ResultsAccordion, which IS memo'd)... | `SmartCompareApp/src/components/results/ResultsContent.tsx:432` | measured | verify:DOWNGRADED | known:M18/MB-perf-06 partially-fixed — the memo wrappers and useCallbac... |
| MB-RENDER-PERF-A11Y-03 | useTypography — the entire Arabic 1.7x line-height fix — has ZERO production consumers; all 54 text files import the static English-tu... | `SmartCompareApp/src/hooks/useTypography.ts:38` | static | verify:DOWNGRADED | new |
| MB-RENDER-PERF-A11Y-08 | HomeEditorialSections is unmemoized and takes three inline arrow props, so every Home mode-tab tap re-renders the editorial subtree (r... | `SmartCompareApp/src/screens/HomeScreen.tsx:858` | static | verify:DOWNGRADED | new |
| MB-RENDER-PERF-A11Y-15 | P3 sweep — 8 lower-severity render/a11y items, including two still-open dead animations and two false leads other lanes should not re-... | `SmartCompareApp/src/components/hero/RevealBurst.tsx:104` | static | verify:CONFIRMED | mixed status report: known:M18/MB-perf-05 still-open; known:checkup#10 ... |
| MB-SAFE-HYGIENE-06 | react-native-gesture-handler is a runtime dependency with zero imports, zero config references and zero peer requirement — the only tr... | `SmartCompareApp/package.json:50` | measured | verify:DOWNGRADED | new |
| MB-SAFE-HYGIENE-07 | npm audit reachability triage: only 3 of the 20 high/critical advisories reach the device JS bundle, and the one DIRECT high (expo) ne... | `SmartCompareApp/package.json:25` | measured | verify:DOWNGRADED | known:CLAUDE.md 'npm audit … MEASURED 2026-09-01: 43 vulnerabilities' —... |
| MB-SAFE-HYGIENE-08 | api.updateEmail is exported and called by nothing — the backend email-change route (which requires the current password) has no client... | `SmartCompareApp/src/services/api.ts:387` | static | verify:DOWNGRADED | new |
| MB-SAFE-HYGIENE-09 | 160 of 896 i18n keys (18%) are referenced nowhere in the app, in BOTH catalogs — including whole families orphaned when M13-64 deleted... | `SmartCompareApp/src/i18n/en.json:3` | measured | verify:CONFIRMED | new (M13-15 fixed the opposite direction — code->catalog gaps; nothing ... |
| MB-SAFE-HYGIENE-10 | Five exported service symbols have zero callers anywhere including tests — SAFE to delete with proof | `SmartCompareApp/src/services/api.ts:778` | static | verify:CONFIRMED | new |
| MB-SAFE-HYGIENE-11 | src/types/react-native-vector-icons.d.ts declares an ambient module for a package that is not installed and is imported nowhere | `SmartCompareApp/src/types/react-native-vector-icons.d.ts:1` | static | verify:CONFIRMED | new |
| MB-SAFE-HYGIENE-12 | assets/logo-wordmark.png is referenced by nothing, and the doc that clears it of the icon blocker claims the frontend consumes it via ... | `SmartCompareApp/assets/logo-wordmark.png:1` | measured | verify:CONFIRMED | new |
| MB-SAFE-HYGIENE-13 | A stale 42 MB dist/ export bundle sits in the working tree — correctly gitignored and untracked, but it is 20 MB of sourcemap from an ... | `SmartCompareApp/.gitignore:8` | measured | verify:CONFIRMED | new |
| MB-SAFE-HYGIENE-14 | KEEP list — eleven apparently-removable items that are load-bearing, each with the proof that stops a future cleanup deleting them (mo... | `SmartCompareApp/jest.config.js:17` | measured | verify:CONFIRMED | new |
| MB-STARTUP-BUNDLE-04 | No metro.config.js overrides Expo's inlineRequires:false default, so every statically-imported module's requires are hoisted and run e... | `SmartCompareApp/babel.config.js:58` | measured | verify:DOWNGRADED | new |
| MB-STARTUP-BUNDLE-08 | Every English user's splash is gated on loading three Arabic Cairo faces (283,820 B) that will never render, because useAppFonts ANDs ... | `SmartCompareApp/src/theme/fonts.ts:45` | measured | verify:DOWNGRADED | new |
| MB-STARTUP-BUNDLE-14 | P3 sweep: redundant triple i18n import, needlessly serialised boot awaits, unawaited pinning init, per-boot storage sweep, module-scop... | `SmartCompareApp/App.tsx:171` | static | verify:CONFIRMED | known:mobile for item (a) only ('a redundant 2nd i18n import', unverifi... |
| MB-TESTS-TRUTH-17 | The 44 snapshots protect atoms and design tokens well but cover no hot screen, and 4 decorative hero suites account for ~4,000 of the ... | `SmartCompareApp/__tests__/hero/__snapshots__/PeerLattice.test.tsx.snap:1` | measured | verify:CONFIRMED | new |
| MB-TESTS-TRUTH-18 | P3 sweep — 7 smaller test-truth defects (leaked worker handle, 6 remaining exhaustive-deps, 13 lazy require()s, FlatList mock landmine... | `SmartCompareApp/jest.config.js:57` | measured | verify:CONFIRMED | new |
| MB-TWO-LEVER-RELEASE-06 | CANARY_NEW_ONBOARDING_PERCENT=100 has no guard anywhere — nothing in CI, tests or eas.json stops `eas update --branch production` from... | `SmartCompareApp/src/config/features.ts:30` | static | verify:DOWNGRADED | known:M18 MB-two-lever-06 still-open |
| MB-TWO-LEVER-RELEASE-08 | iOS privacy manifest declares required-reason APIs but no NSPrivacyCollectedDataTypes at all, so the shipped manifest contradicts the ... | `SmartCompareApp/app.json:21` | static | verify:DOWNGRADED | new |
| MB-TWO-LEVER-RELEASE-09 | runtimeVersion is pinned to appVersion 1.0.0 with appVersionSource=remote and autoIncrement=true, so a future native change will NOT b... | `SmartCompareApp/app.json:144` | static | verify:CONFIRMED | new |
| MB-TWO-LEVER-RELEASE-10 | OTA verdict for 97b5f15..76ace90: native-compatible and safe to publish to --branch preview, but M18's 'zero dependency changes' premi... | `SmartCompareApp/package.json:23` | measured | verify:CONFIRMED | known:M18 MB-two-lever-04 premise-regressed, verdict still-holds |
| MB-TWO-LEVER-RELEASE-11 | The OTA re-derives the paywall gate a second time and now hard-depends on backend `remaining.{daily,monthly}`; the silent fallback whe... | `SmartCompareApp/src/hooks/useComparisonCounter.ts:81` | measured | verify:CONFIRMED | known:M18 MB-two-lever-02 fixed-then-re-derived |
| MB-TWO-LEVER-RELEASE-12 | App Store blocker 1 still open at 76ace90: icon, splash and adaptive-icon are byte-identical to the create-expo-app scaffolding, and s... | `SmartCompareApp/assets/icon.png:1` | measured | verify:CONFIRMED | CLUSTER-> MB-SAFE-HYGIENE-01; known:M18 MB-two-lever-08 still-open |
| MB-TWO-LEVER-RELEASE-13 | App Store blocker 2 still open at 76ace90: both live legal documents still open with the DRAFT-template banner, and they are served un... | `app/legal/privacy_policy.md:7` | measured | verify:CONFIRMED | CLUSTER-> MB-SAFE-HYGIENE-02; known:M18 MB-two-lever-09 still-open |
| MB-TWO-LEVER-RELEASE-14 | No client consumer of the existing /app/version force-update contract, and expo-updates is never checked at boot — a bad OTA can only ... | `SmartCompareApp/App.tsx:168` | measured | verify:CONFIRMED | known:mobile-checkup 'force-update wire dead' still-open |
| MB-TWO-LEVER-RELEASE-15 | P3 sweep: five smaller release-hygiene items verified at HEAD | `SmartCompareApp/src/services/api.ts:22` | measured | verify:CONFIRMED | new |


---

## 5 Refuted and downgraded ledger

**Nothing in this lane was refuted outright** — all 142 findings survived first verification. What moved is severity, and several load-bearing *sub-claims* inside otherwise-confirmed rows were falsified. Both are recorded here, because a wrong sub-claim inside a right finding is how the next session inherits a false belief.

**My one adjudication this run.** `MB-RECONCILE-04` was filed P1 ("Home compare loader has no cancel affordance **and** no watchdog / unbounded 120 s"). The first verifier downgraded it to P2 on the watchdog half; the second vote pushed it back to P1. I read the code and the first verifier is right: at `76ace90` `api.ts:549` (REST compare) and `:793` (URL compare) both pass `timeout: COMPARE_TIMEOUT_MS = 35000`, `:230` arms `IDENTIFY_TIMEOUT_MS` on an `AbortController`, and `:616` arms a 60 s SSE watchdog. There is no unbounded wait at main. The unbounded-camera case is a property of the **`97b5f15` bundle**, where `grep -c COMPARE_TIMEOUT_MS` returns 0 — i.e. it is `MB-RECONCILE-06`'s deployment gap, not a code defect at HEAD. The residual at main is a missing cancel button on a ≤35 s loader: quality and spend, **P2**. The second vote's supporting sentence ("REST path on the 120 s axios instance") is simply wrong about HEAD. Also noted: the finder's proposed extra 35 s HomeScreen watchdog would race the axios timeout and risk a double alert — ship the cancel control only.

**Sub-claims falsified inside confirmed findings** (kill these, keep the findings):

| inside | falsified sub-claim | what is true |
|---|---|---|
| MB-RECONCILE-06 | "the referral landing still reads the phantom `response_data` column on phones" and "the paywall dead-ends bonus holders" | the referral fix is backend and IS live at `76ace90`; `97b5f15`'s gate counts `bonusRemaining`, so it is *more* permissive, not a bonus dead-end. The core two-lever claim and P1 stand |
| MB-STARTUP-BUNDLE-01 | both proposed fixes (`lucide-react-native/icons/<kebab>` and the `dist/esm/icons/*.mjs` deep path) | neither resolves — the package `exports` map has only `"."` and `"./icons"`, and there is no root `icons/` dir. A Metro `resolveRequest` shim or a babel transform to absolute paths is required |
| MB-RENDER-PERF-A11Y-02 | "an orphaned infinite UI-thread animation continues after unmount" | refuted at reanimated 4.1.7 — `useSharedValue` registers `cancelAnimation` in an unmount effect. The render-read freeze and the reduce-motion invisibility stand |
| MB-SAFE-HYGIENE-09 | "160 orphan i18n keys, each family verified at zero occurrences" | an independent recount gives **142** (15.8%), and three named families are **live**: `home.camera.*` (19 src hits), `paywall.*` (30), `priorities.*` (19). Only 3/13/6 of their keys are orphaned |
| MB-TWO-LEVER-RELEASE-01 | "the AASA and assetlinks files do not exist to be served" | they **do** exist at `76ace90` under `landing/.well-known/`, nginx-wired with the real Team ID; what is missing is the real signing-cert fingerprint (and the origin). This is why the row is P2 and `MB-FLOWS-STATE-01` is the P1 lead |
| MB-TWO-LEVER-RELEASE-14 | "there is no server-side way to stop a bad bundle" | `app/api/version_routes.py:19` already serves `min_version` + `force_update`; the contract exists and has **no client consumer**, so the work is to consume it, not to invent one |
| MB-I18N-RTL-10 | "passing `count` as a string disables Arabic pluralisation on Results, live" | inert — `results.reviews.ratingWithCount` has no plural suffixes in either catalog, so string vs number changes nothing. The genuinely measured English fallback affects only the currently-unreferenced `referrals.bonus.expiresIn*` |
| MB-I18N-RTL-12 | "unrecoverable; only a reinstall fixes it" | self-heals — `App.tsx:174-177` re-runs `forceRTL` against a freshly-read constant on every boot, so the mismatch lasts at most one session |
| MB-I18N-RTL-13 | the mechanism was attributed to old-architecture `LayoutShadowNode.java` | `newArchEnabled: true`, so Fabric is the shipping path — but Fabric performs the same edge swap (`YogaLayoutableShadowNode.cpp:912-927`), and `transform` is still never mirrored, so the finding holds |
| MB-STARTUP-BUNDLE-04 | "all ~3,842 module factories execute at cold start", `measured` | 3,842 is the source-map count, not the boot closure, and react-native exposes many submodules through lazy getters; no cold-start ms was measured, so the impact is **modelled** |
| MB-SAFE-HYGIENE-05 | "45 of 80 unused-vars warnings are in ResultsScreen" | it is **56** — the finder under-counted its own case; the `--max-warnings` number in its test_first must be re-derived from 56 |
| MB-FLOWS-STATE-03 | "App Store Review 3.1.2 blocker" | nothing is purchasable — the trial CTA and Restore are the same "Coming soon" stub — so it is a dead control on a placeholder screen, not a submission blocker |
| MB-SAFE-HYGIENE-08 | "Apple's account-management requirement covers email change" | Apple requires in-app account **deletion**, which `EditProfileScreen` already carries. Missing email change is a missing feature, not a defect |

**Severity moves, row by row** (filed → final; `verify` = the first pass, `2nd` = the second vote, `Fable` = the orchestrator's notes, which win):


| id | filed | final | what changed, and on what evidence |
|---|---|---|---|
| MB-FLOWS-STATE-01 | P1 | P1 | Fable AGREE - re-measured 2026-09-07: qaren.app, the AASA and /c/<token> are all 522 |
| MB-FLOWS-STATE-02 | P1 | P1 | Fable AGREE - needs a Supabase redirect allow-list entry (Ahmed dashboard) |
| MB-FLOWS-STATE-03 | P1 | P2 | verify DOWNGRADED - Code claim exact: both legal links call the Alert stub while a working Legal route exists and Profile uses it. But the P1 rationale (App Store 3.1.2) does not hold at HEAD: nothing is purchasable — the trial CTA (:238-247) and Restore are the same 'Coming soo... |
| MB-FLOWS-STATE-04 | P1 | P1 | Fable AGREE - OTA-safe one-liner x3 + a router test |
| MB-FLOWS-STATE-05 | P1 | P1 | Fable OTHER-SESSION - = MB-RECONCILE-05 (A18 b73c47e) |
| MB-FLOWS-STATE-06 | P1 | P1 | Fable AGREE - verify the :476 'persists' handler is a different path before the unit closes |
| MB-I18N-RTL-01 | P1 | P2 | verify DOWNGRADED - Mechanism CONFIRMED: App.tsx:173-176 flips the native flag with no reload; the only reloadAsync in the app is useLanguage.ts:23; RN 0.81.5 caches isRTL as a module-load constant so forceRTL cannot change it in-session. But the finder's dedupe 'new' is FALSE —... |
| MB-I18N-RTL-02 | P1 | P2 | verify DOWNGRADED - Two definitions CONFIRMED at HEAD and the consumer split is as claimed (I found one MORE the finder missed: OnboardingFlow.tsx:424 `flipForRTL(isRTL)` uses the language-derived value while DirectionalIcon uses the boot constant). But the divergence has NO ind... |
| MB-I18N-RTL-03 | P1 | P2 | verify DOWNGRADED - The central technical claim is CONFIRMED — I read all four RN 0.81.5 platform paths and every one mirrors physical left/right, so the comment at ResultsAccordion.tsx:883 and the test's premise are false; 593ec1e had the correct bare literals. Two corrections:... |
| MB-I18N-RTL-04 | P1 | P2 | verify DOWNGRADED - Every factual claim reproduced: fontFamily.ar has zero consumers, the typography presets carry no fontFamily, useTypography only scales lineHeight, exactly two fontFamily literals exist app-wide, no defaultProps/setCustomText, and App.tsx:274 gates the splash... |
| MB-I18N-RTL-05 | P1 | P2 | verify DOWNGRADED - Both sites CONFIRMED verbatim at the cited lines, and the backend really does emit that English prose (I found the literals in structured_comparison_service.py). The corpus arithmetic checks out: 13237 - 9983 successes = 3254 failures, and the listed error_me... |
| MB-I18N-RTL-06 | P2 | P3 | verify DOWNGRADED - I re-ran the census independently and got the finder's numbers exactly (896 values, 32 Arabic-Indic, 21 Western, 0 mixed) plus the same-screen contradictions, the four unpinned toLocaleString calls and the formatDate ar-SA / formatTimeAgo split; the node ICU ... |
| MB-I18N-RTL-10 | P2 | P3 | verify DOWNGRADED - Every measurement reproduces through the real runtime, but the titled live defect is inert. `results.reviews.ratingWithCount` has NO plural suffixes in either catalog, so passing count as a string changes nothing: I measured both string and number counts and ... |
| MB-I18N-RTL-12 | P2 | P3 | verify DOWNGRADED - Every code claim reproduces, but the load-bearing impact — "unrecoverable, reinstalling is the only recovery" — is refuted. App.tsx:174-177 re-runs `forceRTL(shouldBeRTL)` on EVERY boot against a freshly-read constant, so the cold start after the trap correct... |
| MB-NETWORK-CONTRACT-01 | P1 | P1 | Fable AGREE - backend allowlist + superset grep test; the backend half needs no OTA |
| MB-NETWORK-CONTRACT-02 | P1 | P1 | Fable AGREE - overlaps the other session's remaining B2 expired-token 401 - coordinate before filing |
| MB-NETWORK-CONTRACT-03 | P1 | P1 | Fable AGREE |
| MB-NETWORK-CONTRACT-04 | P1 | P1 | Fable AGREE - OTA-safe |
| MB-NETWORK-CONTRACT-07 | P1 | P1 | Fable AGREE - backend unit: await the insert only |
| MB-NETWORK-CONTRACT-10 | P2 | P3 | verify DOWNGRADED - The route half reproduces (plain-string detail -> _is_structured_detail False -> code BAD_REQUEST), but the impact chain does not. compare_from_urls' only failure return is {success, error, details} with NO code key, and no CONTENT_UNAVAILABLE or TIMEOUT is p... |
| MB-NETWORK-CONTRACT-11 | P2 | P3 | verify DOWNGRADED - The line reproduces verbatim: case 'settle_complete' sets neither sawTerminal nor clearWatchdog. But the double-spend chain fails at HEAD three ways: (1) ENABLE_EXPO_FETCH_SSE_DEFAULT=false, so the shipped path is REST; (2) every backend yield site emits ('se... |
| MB-NETWORK-CONTRACT-14 | P1 | P1 | Fable DEDUPE - = MB-RECONCILE-01 + MB-RECONCILE-07; the other session must skip their A1/A6 |
| MB-NETWORK-CONTRACT-15 | P2 | P3 | verify DOWNGRADED - The literal code claim is true — no `if (loading) return;`, no in-flight ref, and :321 clobbers the abort handle. But the evidence omits a material guard: HomeScreen.tsx:644 passes `disabled={loading}` into TwoInputShell, whose CTA is `disabled={!canSubmit}` ... |
| MB-RECONCILE-01 | P1 | P1 | Fable AGREE - land with MB-RECONCILE-07 as ONE metering unit |
| MB-RECONCILE-02 | P1 | P2 | verify DOWNGRADED - Mechanism reproduced exactly; severity and framing overstated. Code is as claimed and every compare hits it. But nothing functionally breaks — the loader stays mounted, the result arrives — so this is perceived-latency/quality, i.e. P2 on the brief scale, not... |
| MB-RECONCILE-03 | P1 | P1 | Fable OTHER-SESSION - fixed on feature/m23-mobile-w1 A3 e58f022 (unmerged); kill after merge |
| MB-RECONCILE-04 | P1 | P2 | verify DOWNGRADED - Half true. Cancel: confirmed — HomeScreen.tsx:881 overlay wraps LoadingScreenVariants, which contains zero Pressable/Touchable/onPress; abortRef assigned :321, invoked only at :199 in the unmount effect. Watchdog: refuted at HEAD — the shipped path is ENABLE_... / Fable SYNTH-DOWNGRADE - the watchdog half is refuted at HEAD - api.ts:549 and :793 pass COMPARE_TIMEOUT_MS=35000 and :230 arms IDENTIFY_TIMEOUT_MS - and the unbounded-camera half is a property of the 97b5f15 bundle, i.e. MB-RECONCILE-06. Residual at main is a missing cancel affordance on a <=35 s loader. |
| MB-RECONCILE-05 | P1 | P1 | Fable OTHER-SESSION - fixed on feature/m23-mobile-w1 A18 b73c47e (unmerged); kill after merge |
| MB-RECONCILE-06 | P1 | P1 | Fable AGREE - the single highest-value action available; nothing mobile is 'fixed for users' until the OTA lands |
| MB-RECONCILE-07 | P2 | P1 | verify UPGRADED - All three sub-claims reproduced at 76ace90. url_routes.py has zero hits for get_optional_user/Depends/consume_comparison_credit/save_comparison/selected_category; the whole call path (compare_from_urls, structured_comparison_service) has none either. Reachabl... / 2nd UPGRADED - All three sub-claims reproduce at HEAD (76ace90 == working tree for both files; `git diff 76ace90..HEAD` on them = empty). Endpoint is registered unflagged (main.py:177) and reachable from the shippe... / Fable UPGRADED - P2 to P1: data loss + server-side freemium bypass; carries PO-VERDICT-TRUTH-01 |
| MB-RECONCILE-15 | P2 | P1 | 2nd UPGRADED - Fully reproduced at 76ace90 (mobile tree byte-identical to working HEAD 7dd04c1): one openRatingSource definition, zero call sites, the sole Linking.openURL in the results tree, zero price.url consum... / Fable PRODUCT-CALL - the outbound retailer link is Ahmed's decision, not a blind fix |
| MB-RECONCILE-16 | P2 | P3 | verify DOWNGRADED - Structure is exactly as claimed at HEAD (SmartCompareApp unchanged since 76ace90). But the repro is refuted: getSavedLanguage wraps AsyncStorage in try{}catch{} and getStableId wraps SecureStore/Crypto in try/catch with a randomUUID fallback — neither can rej... |
| MB-RENDER-PERF-A11Y-01 | P2 | P3 | verify DOWNGRADED - Core mechanism confirmed: memo at ResultsContent.tsx:112, winnerRevealed is a prop (:95,:127) with exactly one JSX consumer (:432), flipped by ResultsScreen.tsx setTimeout 800 (:372-384). But the evidence is materially wrong: 'ResultsAccordion ~901 LOC re-ren... |
| MB-RENDER-PERF-A11Y-03 | P2 | P3 | verify DOWNGRADED - Every stated fact reproduces: `grep -rl useTypography src __tests__ App.tsx` returns only src/hooks/useTypography.ts and __tests__/hooks/useTypography.test.ts (zero production consumers); 54 .tsx under src reference `typography`; theme/index.ts:69-86 pins lin... |
| MB-RENDER-PERF-A11Y-08 | P2 | P3 | verify DOWNGRADED - Code claims all reproduce; the impact does not support P2 and it is not new. M18 MB-perf-06 is titled 'Zero React.memo in the entire app' and was rated P3; M21 memoized Results and History and left Home — this is that row's residual, status still-open. Cost r... |
| MB-SAFE-HYGIENE-01 | P1 | P1 | Fable STATUS - App Store gate, Ahmed; needs-build |
| MB-SAFE-HYGIENE-02 | P1 | P1 | Fable STATUS - App Store gate, Ahmed/legal; backend deploy only |
| MB-SAFE-HYGIENE-06 | P2 | P3 | verify DOWNGRADED - The factual claim is fully CONFIRMED — I re-walked package-lock.json packages[*] for every dependencies/peerDependencies/optionalDependencies/devDependencies edge naming react-native-gesture-handler and got exactly one: `ROOT :: dependencies`. A second walk o... |
| MB-SAFE-HYGIENE-07 | P2 | P3 | verify DOWNGRADED - Every measured number reproduces exactly, but two corrections. (a) LINE: the only direct high is `"expo": "~54.0.33"` at package.json:25, not :31 (:31 is expo-crypto). (b) DEDUPE: this is not merely a CLAUDE.md claim — M13-27 (P2) already filed "npm audit is ... |
| MB-SAFE-HYGIENE-08 | P2 | P3 | verify DOWNGRADED - Code behaves exactly as claimed and the asymmetry is real, so the factual core is CONFIRMED at HEAD. Severity drops to P3: there is no user harm, no spend, no security exposure — a user simply cannot change their email, which is a missing feature, not a defec... |
| MB-STARTUP-BUNDLE-01 | P1 | P1 | Fable AGREE - measured 1.21 MB / 24%; overlaps the other session's remaining B1 |
| MB-STARTUP-BUNDLE-02 | P1 | P1 | Fable OTHER-SESSION - = MB-RECONCILE-03 (A3 e58f022) |
| MB-STARTUP-BUNDLE-04 | P2 | P3 | verify DOWNGRADED - Config facts all true and I reproduced the emitted evidence: `ls metro.config.*` ENOENT, ExpoMetroConfig.js:322 `inlineRequires: false`, babel.config.js adds only removeConsole + reanimated, and in my own export the App factory head is `var t=e(r(_d[0])),n=e(... |
| MB-STARTUP-BUNDLE-08 | P2 | P3 | verify DOWNGRADED - Code claim fully confirmed and arithmetic is exact: fonts.ts:38-45 ANDs both families unconditionally; App.tsx:150/:274 gate the splash on it; app.json:81-88 embeds only the 3 Geist TTFs natively, so Cairo is the runtime load. 94480+94688+94652 = 283,820 B an... |
| MB-TESTS-TRUTH-01 | P1 | P2 | verify DOWNGRADED - Reproduced exactly. Measured 0/309 statements, 0/174 branches, 0/55 functions. 15 grep hits, every one a readFileSync path string; zero imports. Count correction: 12 dedicated ResultsScreen* suites (7 in __tests__, 5 in __tests__/screens), 14 files total read... |
| MB-TESTS-TRUTH-02 | P1 | P2 | verify DOWNGRADED - Code claims all verified verbatim. But two corrections. (1) Dedupe is NOT new: the 2026-09-02 mobile checkup already lists it (line 155: "Results has no outbound purchase/source links at all (`openRatingSource` is dead code, `price.url` never used)") — known:... |
| MB-TESTS-TRUTH-03 | P1 | P2 | verify DOWNGRADED - Coverage claim reproduced (0/23 st, 0/6 br, 0/5 fn); 8 suites jest.mock it, the 9th (useComparisonCounter:28) imports but mocks at :30. The evidence is INVERTED, though: main.py:149 registers http_exception_handler globally and error_handler.py:74-100 rebuild... |
| MB-TESTS-TRUTH-04 | P1 | P1 | Fable OTHER-SESSION - fixed on feature/m23-mobile-w1 A12 4f470cc; kill after merge |
| MB-TESTS-TRUTH-05 | P1 | P2 | verify DOWNGRADED - Core claim confirmed: no `ar` key, `dict[key] \|\| key` for the other 880 keys, opts.defaultValue ignored, and the only suite that builds a real i18next over ar.json and calls changeLanguage('ar') is __tests__/i18n/plurals.test.ts (pure logic, 2 count keys). ... |
| MB-TESTS-TRUTH-06 | P1 | P2 | verify DOWNGRADED - Every claim reproduced exactly. App.tsx: 0/138 statements, 0/26 branches, 0/34 functions. The M22 eslint baseline lints 141 files, none of them App.tsx, because ci.yml:270 globs only `src/**`. All five App.*.test files readFileSync the source — App.navigation... |
| MB-TWO-LEVER-RELEASE-01 | P1 | P2 | verify DOWNGRADED - Core reproduces: referral_service.py:50 is a bare literal (zero getenv/environ in the file), no APP_BASE_URL in web vars, ENABLE_REFERRAL_SYSTEM=true, and I measured 522/522/523/404. But a load-bearing evidence claim is FALSE: landing/.well-known/apple-app-si... |
| MB-TWO-LEVER-RELEASE-02 | P1 | P1 | Fable AGREE - needs-build: ships only with the next eas build |
| MB-TWO-LEVER-RELEASE-06 | P2 | P3 | verify DOWNGRADED - Facts all reproduce — features.ts:30 = 100, the only assertion is a tautological 0..100 range check, hashBucket short-circuits at >=100, and `git diff 97b5f15..76ace90 -- SmartCompareApp/src/config/` is empty. But this is M18 MB-two-lever-06, which reviewed t... |
| MB-TWO-LEVER-RELEASE-08 | P2 | P3 | verify DOWNGRADED - Fact claim fully reproduced: ios.privacyManifests at app.json:21-41 contains only NSPrivacyTracking:false and four NSPrivacyAccessedAPITypes (CA92.1/C617.1/35F9.1/E174.1); no NSPrivacyCollectedDataTypes, no NSPrivacyTrackingDomains. Genuinely new (no prior re... |


---

## 6 Coverage statement

**What was enumerated and checked, with counts** (9 lanes; every count below is a lane's own exhaustive scan at `76ace90`, not a sample):

| lane | surface enumerated | checked |
|---|---|---|
| `mb-network-contract` | 86 mounted backend routes (walked via `tests/_route_introspection`), 44 client call sites, 13 Pydantic body models field-by-field, 6 value-enum allowlists, every response shape the client reads, the whole error-code map (16 emitted vs **3** the client branches on), the 401-refresh sequence, the SSE terminal latch, every retry path | 44/44 call sites matched to a mounted route (zero 404-by-path); per-call matrix of method × path × fields × timeout × abort × auth header × fingerprint |
| `mb-reconcile` | 82 prior items: mobile-checkup §A 17 verified + §B 15 leads, and 47 M18 mobile rows | 17/17, 15/15, 47/47 — result: 12 + 13 + 18 still open, 31 fixed/changed, all main-only |
| `mb-flows-state` | 3 navigators / 16 declared vs 13 registered route names, 37 screen modules, **60** navigation edges, 5 deep-link shapes probed through the app's own `@react-navigation/routers@7.5.5`, 6 state machines, the copy contract | every edge checked against both auth branches; 4 node probes + 1 RNTL render of the real OnboardingFlow |
| `mb-startup-bundle` | 17 startup cells (every module-scope side effect and every await to first interactive frame), 7 boot/first-paint network calls, **three real `expo export` runs** with a custom VLQ attributor | 3,842 modules attributed (87.8% of bytes), 30 assets, 11 TTFs |
| `mb-i18n-rtl` | 896 catalog keys × 2 (key-set, placeholders, empties, mixed script, digit census), 6 plural families at counts 0/1/2/3/11/100 through the real i18next 26.1.0, 19 directional glyph sites, 86 physical vs 20 logical style props, 6 textAlign ternaries, 6 translateX sites, 17 number/date sites, 7 modal surfaces | every one read; RN 0.81.5 platform source read on all four paths to falsify the textAlign premise |
| `mb-safe-hygiene` | 452 tracked files, 134 non-test src modules (full import graph incl. bare side-effect imports), **408** exported symbols, 55 dependencies, 9 assets (SHA-256 recomputed), 20 mocks, 17 snapshots, 245 test files, 896 i18n keys, 2 feature flags, 42 npm-audit rows | 134/134, 408/408, 55/55, 9/9, 20/20, 17/17, 245/245, 42/42 |
| `mb-render-perf-a11y` | 31,757 LOC; 4 memo boundaries and their 11 children; the 1 virtualized list; 41 `useSharedValue` sites matched to consumers; 5 `withRepeat(-1)`; **128** interactive elements audited for role/label/state/hitSlop; 36 sub-44pt targets; 16 contrast pairs computed; 6 modal surfaces; 23 haptic sites | every cell; child re-render counts measured through the real `ResultsContent` |
| `mb-tests-truth` | the full jest + eslint baseline re-run (245 suites, 2,320 passed, 152 warnings); 3 skipped suites and 13 skipped + 13 todo tests read individually; 21 mocks diffed against real symbol usage; 11 fixtures key-path-diffed against recorded backend payloads; per-file coverage for every hot file; all 5 CI jobs | 3/3, 13/13, 13/13, 21/21, 11/11 |
| `mb-two-lever-release` | the 72-file OTA delta classified file by file; `app.json` cell by cell (9 plugins read against their installed config plugins); `eas.json` 3 profiles; the 4 App-Store blockers re-measured; the live TLS chain pulled and all four SPKI hashes recomputed | 72/72 classified: **0 needs-native-build**, 5 risky-on-OTA, the rest OTA-safe |

**What was NOT covered, and why:**
- **No device, no emulator, no live compare** — the single largest gap; see §3. Every "the user sees X" statement is derived, not observed.
- **The two-lever gap was not re-derived per finding.** Every row is stated against main `76ace90`; only the reconcile and two-lever lanes diffed against `97b5f15`. For M21-touched files the phone behaviour is the *older, worse* version.
- **Admin routes** (20 of 86), `/docs`, `/redoc`, `/openapi.json` — no client call site.
- **Response body *semantics*** (is the amount the right currency, is the confidence honest) — the product-output lane owns those; this lane checked shape only.
- **The legacy 6-step `OnboardingScreen.tsx`** (unreachable at `CANARY_NEW_ONBOARDING_PERCENT=100`) and the per-step bodies of Steps 1–16 beyond the scripted sweeps.
- **Native side entirely**: `/ios` and `/android` are gitignored generated folders, so autolinking effects, `Podfile.lock` and the merged AndroidManifest were never read — which is why every `app.json` claim in U14 must be re-asserted after `expo prebuild --clean`.
- **Store-listing assets** (screenshots, ASC answers) are not in the repo.
- **Compare re-entrancy** was probed and abandoned: `__mocks__/react-native.ts:18` replaces `TouchableOpacity` with a plain `View` that carries `disabled` as an inert prop, so the harness structurally cannot measure a `disabled` gate. Reported as unresolved rather than as a finding.
- **`LoadingRings` commit cadence** was attempted and came back inconclusive (state updates outside `act()`), so the 60 Hz claim is modelled.
- **Whether legacy Supabase `comparisons` rows written before the `#99` winner overwrite were backfilled** — needs a SELECT that was scoped out; until then History can still name the opposite winner for pre-fix rows.
- **Backend lanes** (load/scale, code review, product output) — different workflows; only their intersections are cited here (`PO-VERDICT-TRUTH-01`, `#128`, `#114`).

---

## Appendix A — SAFE TO REMOVE

Nothing here is deleted without proof. Each row carries the reference proof, the verification command that must pass afterwards, and the lever. **Everything else in the app that looked removable is in Appendix B.**

| # | item | proof it is unreferenced | verify after removal | lever | finding |
|---|---|---|---|---|---|
| A1 | `SmartCompareApp/src/types/react-native-vector-icons.d.ts` (whole file) | ambient `declare module 'react-native-vector-icons/Ionicons'`; the package is in neither dependency block and is **not installed**; zero importers in the 134-module import graph; the only repo mentions are transitive `@expo/vector-icons` rows in the lock. It is a tripwire — an import would typecheck and then fail at runtime | `npx tsc --noEmit` exit 0 | OTA (type-only, no bundle change) | MB-SAFE-HYGIENE-11 |
| A2 | `api.ts:770-800` `compareTextPair` + its `CompareOptions` interface | word-boundary scan of all 408 exports against src + `App.tsx` + 245 tests + 20 mocks returns only the self-definition and one comment; the control `rtlFlip` correctly shows real consumers, so the scan is not a false-negative artifact. It is also a **second, divergent** `POST /text/compare` path beside the live one — the real cost is the ambush, not the bytes | `tsc` exit 0 + 242 suites green | OTA | MB-SAFE-HYGIENE-10 |
| A3 | `authService.ts:303 isLoggedIn` | same scan, zero references anywhere including tests | `tsc` + jest | OTA | MB-SAFE-HYGIENE-10 |
| A4 | `usageService.ts:76 formatUsageMessage` | same scan, zero references | `tsc` + jest | OTA | MB-SAFE-HYGIENE-10 |
| A5 | `pushTokenService.ts:99 PUSH_TOKEN_REGISTERED_KEY` (the re-export alias only — keep the file-local `REGISTERED_FLAG_KEY`) | same scan; the alias has no consumer. **Note:** the underlying flag is *never read or written either* (`MB-STARTUP-BUNDLE-10`), so the better move is to make it work rather than delete the alias in isolation | `tsc` + jest | OTA | MB-SAFE-HYGIENE-10 |
| A6 | The 56 unused bindings and ~10 dead helper definitions in `ResultsScreen.tsx` (11 component imports at `:72-101`, 20 lucide icons, 8 types, and `formatPrice:408`, `getScoreColor:418`, `getProductScores:562`, `SCORE_LABELS:577`, `priceDiff:586` — which *executes* every render — `getAllSpecKeys:603`, `isSpecDifferent:614`, `specsExpanded:155`, `showDiffsOnly:157`, `usageStatus:167`) | `baseline/eslint.json` attributes 56 of the app's 80 `no-unused-vars` warnings to this one file; the screen delegates to `ResultsContent` at `:751`; the duplicate `formatPrice` has drifted behind the live one | `tsc` exit 0 + the 6 ResultsScreen suites | OTA | MB-SAFE-HYGIENE-05, MB-TESTS-TRUTH-15 |
| A7 | **`openRatingSource:500` is the one exception inside A6 — do NOT delete it silently.** It is the subject of the product call in `MB-RECONCILE-15`, and `CLAUDE.md:143` documents it as live behaviour | zero call sites; the only `Linking.openURL` in the whole results tree; `git` shows `f99807a` deleted three working `onPress` sites | either wire it (with the CTA copy) or delete it **and** correct the CLAUDE.md sentence in the same commit | OTA | MB-RECONCILE-15 |
| A8 | `react-native-gesture-handler` (dependency, 5.9 MB in node_modules) | a full `package-lock.json` walk over every `dependencies`/`peerDependencies`/`optionalDependencies` edge naming it returns exactly one: `ROOT :: dependencies`. A second walk over every installed package.json returns **none**. `@react-navigation` v7 native-stack + bottom-tabs dropped the requirement. Zero imports, zero config references | `npx expo-doctor`; smoke tab + modal navigation on a preview build | **needs-build** (autolinking; no OTA can do this) | MB-SAFE-HYGIENE-06 |
| A9 | The 5 unregistered Cairo weights — 469,608 B, 41.0% of the OTA asset payload (200ExtraLight, 300Light, 500Medium, 800ExtraBold, 900Black) | measured from the export asset manifest; `fonts.ts:40-44` registers only 400/600/700. **This is not a file deletion**: `@expo-google-fonts/cairo`'s `index.js` is eight unconditional top-level `require()`s, so the removal is a subpath-import change — and it is **incomplete** unless `fonts.ts:12-17`'s `useFonts` import from the package **root** is also switched to `expo-font`'s, or the barrel keeps all eight in the graph | a build-assertion test that the exported manifest contains exactly the registered TTFs (red today at 11) | OTA | MB-STARTUP-BUNDLE-06 |
| A10 | The Sentry browser trio — `@sentry-internal/replay` 132,946 B + `feedback` 50,195 B + `replay-canvas` 15,426 B = **198,567 B** | `initSentry` (`sentry.ts:227-251`) passes six options and registers **no** replay or feedback integration; `@sentry/react-native`'s dist imports none of the three (its FeedbackWidget is a separate RN-native implementation), so a Metro `resolveRequest` stub is behaviourally inert | a build-assertion that no source path matches `@sentry-internal/(replay\|replay-canvas\|feedback)`; smoke that a thrown error still reaches the DSN | OTA | MB-STARTUP-BUNDLE-05 |
| A11 | ~1.19 MB of unused lucide icons (1,703 bundled, 68 used) | measured attribution + a 34-site / 68-icon scan I re-ran this run. **Not a file deletion** — a build-graph change, and the naive deep-import does not resolve (§1.4) | two `expo export` runs, byte-diffed; the existing `__mocks__/lucide-react-native.ts` jest shim must stay valid | OTA | MB-STARTUP-BUNDLE-01 |
| A12 | `SmartCompareApp/dist/` — 42 MB / 41 files of stale local export (5,256,028 B `.hbc` + 15,625,618 B map, built 2026-09-02 01:58 from an unrecorded commit) | `git check-ignore -v dist` → `.gitignore:8`; `git ls-files dist` → 0. Nothing references it; `npx expo export` regenerates it | none needed | local only (`rm -rf`) | MB-SAFE-HYGIENE-13 |

## Appendix B — KEEP or UNSURE (do not let a cleanup pass touch these)

**B1 — KEEP, with the proof that stops a future deletion** (`MB-SAFE-HYGIENE-14`):

| item | why it is load-bearing |
|---|---|
| all 20 `__mocks__/*.ts` | every one is referenced by a `jest.config.js` moduleNameMapper entry (`:17-52`) and all 19 mocked packages are still installed — zero stale mocks |
| all 17 `__snapshots__/*.snap` | every one has its owning `.test.tsx` present; the `ConcentricMotif` orphan CLAUDE.md records as removed is confirmed gone |
| `react-native-safe-area-context`, `react-native-screens` | peers of `@react-navigation/{native-stack,bottom-tabs,elements}` per the lock — zero imports is expected |
| `react-native-worklets` | peer of `react-native-reanimated` |
| `expo-dev-client` | required by `eas.json`'s `development.developmentClient: true` |
| `@types/jest`, `@types/node` | `tsconfig.json` `types: ["jest","node"]` |
| `@types/react`, `typescript` | consumed by tsc / ts-jest, never imported |
| `@testing-library/jest-native` | a **bare side-effect import** at `__tests__/setup.ts:6` — invisible to any `from`-anchored scanner, and it read as dead in the lane's own first pass |
| `intl-pluralrules` | same bare-import shape at `src/i18n/index.ts:7`, and `__tests__/i18n/plurals.test.ts:117-128` pins both the import order and its presence in package.json |
| `src/types/react-test-renderer.d.ts` | zero importers, but deleting it makes `npx tsc --noEmit` exit 2 with TS7016 (`@types/react-test-renderer` is deprecated upstream and two suites import the runtime package) |
| `src/theme/fonts.ts`'s `fontFamily` export | unused in production but **pinned as a contract** by `__tests__/theme/fonts.test.ts:4-14` — not deletable, only wireable (U10/U12 wire it) |
| `src/hooks/useTypography.ts` | test-only importer, but it is the **only implementation** of the Arabic 1.7× line-height spec rule — deleting it removes the rule and hides the gap. Wire, never delete |
| `assets/fonts/OFL.txt` | the SIL Open Font License text for the bundled Geist faces — a redistribution requirement |
| the 3 Geist TTFs | applied at exactly two sites today, but they are the design system's EN face and are also embedded natively by the `app.json` expo-font plugin. Resolve the **double-ship** (drop one of the two paths) rather than deleting the faces |

The two concrete traps in that list are `@testing-library/jest-native` and `intl-pluralrules`: a naive scanner (or `depcheck`'s default config) will propose both, plus the four peer-only natives, and break `npm ci`, the jest setup, or Arabic plural resolution.

**B2 — UNSURE, decide before touching:**

| item | the open question | note |
|---|---|---|
| the **142** orphan i18n keys (not 160 — recount in §5) | per family: "the surface was deleted" (delete from **both** catalogs in one commit — `i18n.test.ts` enforces set equality) vs "the surface is planned" (leave, allowlist it) | the unambiguous first batch is the M13-64 casualties: `home.bonus.*` and `referrals.bonus.*`, orphaned when 13 components were deleted and their strings were not. **Never bulk-delete**: the 160-key dynamic families (`results.spec.*`, `currency.*`, `onboarding.s4.gov_*`, `onboarding.s8.priority_*`, `onboarding.s9.*`, `onboarding.s13.stage_*`, `home.region.*`, `preferences.*`, `contact.category.*`, `referrals.quiz.brand.*`) are runtime-resolved and statically invisible. Seed a committed allowlist at today's count and ratchet down |
| `assets/logo-wordmark.png` (5,318 B) | keep until the ICN-0001 re-render is done | referenced by nothing (app.json included), **and** `docs/plans/bundle-d-followups.md:108-109` wrongly claims the frontend consumes it via `QarenLogo` — which renders an inline SVG and never requires a PNG. **Correct that doc line immediately**; it is the document Ahmed will read when he fixes the icon blocker, so it would send a re-export into a slot nothing reads |
| `api.ts:387 updateEmail` | is email change in scope for v1? | exported, called by nothing; the backend `/auth/email` route (which requires the current password) has **no client at all**, so its rate limit and password gate have never been exercised. Deleting the function erases the only evidence of the gap — so either wire it into `EditProfileScreen` beside the display-name field, or delete it **and** open an issue recording that the route is unexercised |
| `utils/rtl.ts:13 writingDirection` | listed as SAFE by the hygiene lane, but it is also the natural implementation site for the bidi isolation the app entirely lacks (`MB-I18N-RTL-14(a)`) | delete it only together with an explicit decision that no bidi isolation is wanted. Keep `rtlFlip` in either case — it has real consumers |
| the 3 skipped jest suites and 13 `it.todo` stubs | they are the **only** render tests HomeScreen ever had, replaced by source-greps, and the todos name the exact untested launch surfaces (RTL mirroring, Apple sign-in gating, list virtualization) | do not delete them to make the suite tidy; they are the inventory of what is unprotected (`MB-TESTS-TRUTH-13`) |
