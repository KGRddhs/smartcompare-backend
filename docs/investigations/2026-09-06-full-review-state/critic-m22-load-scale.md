# load-scale CRITIC

recommend_gap_round: True

## gaps
- ON-LOOP CPU AFTER THE FETCH IS NEVER ENUMERATED. The lane's loop-cost inventory covers HTML parse + SSL context + DB/Redis waits only. scoring_service.compute_scores (sync, app/services/scoring_service.py:1264, 3,819 lines) is called bare from async at structured_comparison_service.py:3418 and :4128; response_builder.build_comparison_response (app/services/response_builder.py:1295, 2,005 lines) and every fact_check_service entry (:204/:301/:358/:399, 608 lines) are sync and run on the loop for every compare. The strings 'scoring_service' and 'fact_check' appear ZERO times across all 96 findings. So 1a(b)'s per-compare loop budget (7.7 s parse + 0.4 s SSL + un-offloaded executes) and 1b row 2's '~7.8 compares/min from HTML parsing alone' rest on an incomplete term list — and scoring_service.py is +600 lines in the exact 593ec1e..76ace90 delta the brief flagged.
- /image/identify WAS NEVER TRACED (acknowledged in 6.2 item 7, but it is the skipped cell most likely to hold a P1). app/api/image_routes.py:105-130 accepts 4 images x 10 MB, awaits img.read() into memory, and openai_service.identify_products (async def, :139) base64-encodes each one inline on the loop at :197-208, materialising ~53 MB of data-URI strings per request behind a 10/minute shared bucket and (per the mobile checkup) no usage metering for authed users. 1b row 7's verdict 'Memory: not binding today' is derived from HTML-parse allocations only and never accounts for this path.
- INGRESS KNOBS BEYOND --limit-concurrency ARE UNENUMERATED. Only --limit-concurrency, --workers and --proxy-headers are considered. --timeout-keep-alive, h11 header/body read limits, request-body size ceilings and slowloris behaviour on the single worker are never listed as cells, although W1-1 is itself a start-command unit and the stage-4 metastability story is about unbounded accept-and-queue.
- NO DISPOSITION FOR 67 OF 96 ROWS. Section 2 proposes units citing 29 finding ids; the remaining 67 (55 P2 / 27 P3 minus the four P2s that ride a unit) appear only as table rows with no defer/backlog/drop ruling. M18's precedent was an explicit P2/P3 triage (146 triaged, 17 dropped as noise); this report never says what happens to its own tail.
- LOAD-ADJACENT OPEN ISSUES NOT RECONCILED (brief HARD RULE 3). #70 (curl pool + fan-out cap) is literally W1-2's subject and is never cited (0 mentions in the report); #64 (SSE analytics/quota keyed on the success flag) touches the same event_generator metering code as 1c stage 7 and W2-2 — 0 mentions; #63 (/price-kpi auth+meter) is the same unauthenticated-unmetered class as W0-6 — 0 mentions; #55, #67, #128 also 0; #61 appears once and only as the Firecrawl half of 0.4. Each should have carried a still-open/regressed/fixed status line.
- THE price-warmer SERVICE AND THE CRON SURFACES WERE NOT COVERED. Acknowledged in 6.2 item 5, but it is the only unattended service that SPENDS (SERPER_LIFETIME_LIMIT=2200 vs web's 0, Bright Data armed with no budget gate), and reengagement/bonus-expiry crons that share the same Upstash and Supabase were never modelled at all (which is also why #80 cron failure visibility went unreconciled).
- EXECUTOR SATURATION WITH THE OFFLOAD FLAGS ON WAS NEVER MEASURED (the ls-request-path-blocking finder's own not_covered item 7), yet 1a(b)'s '2-3 concurrent, 5-8/min' answer and W0-4/W1-2's 'canary together' ordering both depend on how the 40-thread pool behaves once DB and Redis work moves into it. Likewise the four things a second worker breaks are static reads, never exercised.
- NO END-TO-END COMPARE MEASUREMENT ANYWHERE, so the fan-out width (~18) used in the 7.7 s parse arithmetic, the ~36 warm / ~72 cold Upstash commands, and the 12-18 Bright Data requests per compare are all derived from primitives. Acknowledged, but it means every per-compare figure in sections 0-1 is a composition, not an observation.

## unverified_claims
- 1b row 5 labels the Upstash ceiling 'MEASURED RTT + MEASURED command counts' and states 'free tier (500K commands/mo) exhausts near ~150 DAU'. The Upstash plan was never read (6.2 item 3), the per-compare counts are derived not observed, and the ls-cache-redis finder's own crossover estimate was 250-350 DAU on commands alone. The ceiling is MODELLED on an assumed plan.
- 1b row 6 labels 'a slow query can hold the loop 120 s' MEASURED. The 120 s is the locked postgrest default (DEFAULT_POSTGREST_CLIENT_TIMEOUT) plus a runtime Timeout probe — STATIC. No query was ever observed holding the loop; verified at HEAD that app/ contains zero ClientOptions/postgrest_client_timeout occurrences, which is the static half only.
- 1b row 1 labels the shared-bucket ceiling MEASURED while its own caveat column says the bucket count is [1..N_peers] and modelled from 10 peers accumulated over two months of ~zero traffic. 'Deployment-wide' is a modelled property presented under a MEASURED label.
- The 178 ms Upstash RTT is an upper bound (urllib per call, no keep-alive, SDK path never timed — the report says so in 3.2), yet 1a(b) presents '~6.4 s warm / ~12.8 s cold of loop blockage relocated' as 'MEASURED counts x MEASURED RTT', and 1d's 'a cache HIT costs a full blocking round trip' inherits the same upper bound.
- 1c stage 3's client-side chain (api.ts:146-153, HomeScreen.tsx:394-399, ContactUsScreen.tsx:75, 'the shipped bundle shows the raw axios string Request failed with status code 429') is not backed by any verified finding in this lane — no SmartCompareApp file appears in the file field of any of the 96 rows — and is presented without attribution to the mobile lane.
- The concurrency-cliff percentages are repeated as bare MEASURED numbers in 1c stages 1-2 without the 3.1 item 5 confound (overlap derived from duration_ms, so exposure and outcome share a variable), and 1a(a) still carries 'overlap>=4 success 0-29%', a range whose lower end (the finder's 0%) explicitly did not reproduce in the verifier's recount.
- '78.3% of the request log is a synthetic harness' asserts authorship the evidence does not establish: the ls-measured-evidence finder recorded that it could not prove the mechanism behind the 10,364 duration_ms==0 rows and reported the population's signature, not its author.
- Bright Data's 'packaged 4,500/month allowance' is the code constant PROVIDER_CONFIGS['brightdata']['monthly_limit'] (app/services/api_budget_service.py:49-52, comment '~5,000/mo free, save 500 buffer'), not a verified account plan; the ~250-375-compare burn-down and the 12-18 requests/compare are both modelled.
- The 'four things a second worker breaks' in 1a(c) are called 'all verified', but only the memory:// limiter storage has its own finding row; the Shopify _next_slot_at doubling, the per-process observability under-count and the 40->80 thread arithmetic appear only inside other findings' evidence prose, never as ruled rows.
- Evidence-integrity disclosure dropped in synthesis: the ls-cache-redis finder recorded that its cold _get_price run made real HTTP GETs to retailer Shopify catalogs before it realised the cascade would reach the network. The report's method statement ('zero network') never carries this.

## wrong_or_stale_claims
- WRONG file:line AT HEAD. Section 0.1 and unit W0-1 cite the price-path DNS hops as price_service.py:14126,14147 'inside async def curl_fetch_html_same_site'. Verified at 76ace90: curl_fetch_html_same_site spans 13984-14059 and fetch_page_price starts at 14060, so 14126 is 'if ucp_price:' and 14147 is a blank line — neither resolves anything. The real validate_external_url call sites on that path are price_service.py:14016 and :14037. The defect is real; the citation (inherited verbatim from FABLE_REVIEW_NOTES row LS-REQUEST-PATH-BLOCKING-01) is wrong, and W0-1's call-site fix list would patch the wrong lines. (Independently verified: the only socket.getaddrinfo in app/ is url_validator.py:30, and validate_external_url's callers are url_routes.py:91/112/148/175/197/216, url_extraction_service.py:101/111, price_service.py:14016/14037, shopify_pdp_service.py:284, all confirmed present.)
- SEVERITY OF RECORD CONTRADICTED. Section 5.2 records LS-CONCURRENCY-LIMITS-02 and LS-CACHE-REDIS-02 as moved P1 -> P2 by 'Fable review notes', and W1-2/W1-5 repeat '(Fable ↓P2, unit stays)'. FABLE_REVIEW_NOTES.md, SESSION 65 section, explicitly WITHDRAWS both downgrades: 'LS-CONCURRENCY-LIMITS-02 stays P1 — measured 62 submissions / 40 running / 21 queued ... LS-CACHE-REDIS-02 stays P1 — n=5 concurrent cold calls -> 5 searches + 5 extractions on one key'. The report's own preamble names those notes the severity of record, so the ledger should read 1 P0 / 15 P1 / 53 P2, not 1 P0 / 13 P1 / 55 P2, and the two units must be re-placed accordingly.
- ORDERING CONTRADICTED BY THE SAME NOTES. FABLE_REVIEW_NOTES (SESSION 65, agreed-downgrades bullet) rules that with both offload flags OFF the streaming refund runs INLINE and survives shutdown, but the moment ENABLE_ASYNC_REDIS_OFFLOAD is ON it becomes a yielding await and IS lost — therefore 'bounded drain + --timeout-graceful-shutdown is a PRECONDITION for the Redis-offload canary'. The report puts that unit at W2-2, in the section titled 'only after the offloads are active and canaried', with the order reason 'Latent at ~zero traffic, which is exactly why it is W2 and not W0', and omits it from STEP 1 of the 1e ordering rule. As written the plan flips the flag that creates the loss before landing the fix.
- INTERNAL INCONSISTENCY WITH ITS OWN EVIDENCE: 3.1 item 6 and 6.2 item 8 both say the recorded series contains '2 distinct user_id values'; the underlying ls-measured-evidence lane states 3 distinct user_id values and 18 non-null user_id rows.
- CROSS-LANE NUMBERS UNRECONCILED. This lane's organic subset is 1,892 rows / p50 19,927 ms / 76% success with 78.3% of the series synthetic; the campaign's product lane (carried into CLAUDE.md) reports 88.6% probe strings and an organic series of ~1,513 rows / p50 22.0 s / 80% success. The headline throughput is 60/19.9 = 3.0 compares/min and becomes 2.7/min on the other subset. The report never names the discrepancy or says which filter is canonical.
- Section 0.1 says all seven /api/v1/url/* routes are protected only by 'the route's 10/minute limit'. At HEAD url_routes.py:190 and :210 (/detect POST+GET) carry @limiter.limit('20/minute') and /retailers (:46-47) carries no limiter at all — so the burst arithmetic ('ten such requests are ~110-135 s of frozen loop inside a 60 s window') understates the reachable rate on three of the seven.
- m21_touched is dropped from the synthesis entirely. It is present on all 96 rows in the state file (49 True / 47 False), and the brief called out the 593ec1e..76ace90 delta as the thing to look at; the report never uses it to separate long-standing defects from ones the M20/M21 waves introduced.