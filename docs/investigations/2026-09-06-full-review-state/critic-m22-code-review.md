# code-review CRITIC

recommend_gap_round: True

## gaps
- ROUTE-HANDLER BODY CORRECTNESS was never a cell of this lane, and it hides at least one live P1 that I reproduced myself: app/services/url_extraction_service.py:575 does `comparison = await generate_comparison(...)`, but app/services/extraction_service.py:2302 returns a TUPLE (`return parsed, usage` at :2543 and `return {...}, {...}` at :2547) - every other caller unpacks it (`comparison, usage = await generate_comparison(` at structured_comparison_service.py:3440 and :7907), so url_extraction's `comparison.get("winner_index", 0)` at :581 raises AttributeError and POST /api/v1/url/compare 500s on EVERY successful two-URL extraction. The lane opened url_routes.py and url_extraction_service.py (cited at :101/:111 for DNS and for 5 soup sites) but only for SSRF/DNS/parser cells, and the report's own LIVE-broken list in §the-three-plain-questions does not contain it. The same never-audited class covers /share/*, /referrals/*, /feedback + /events, /image/identify and all 20 /admin/* (§3 item 9 admits 54 of 68 routes were never driven, but files zero findings against any of them).
- THE SSE STREAMING PATH WAS NEVER EXECUTED. `compare_from_text_streaming` and `text_routes.event_generator` (the path the shipped client actually uses) were not driven end-to-end by any lane; per-event Redis/analytics writes, the `complete_after_client_gone` metering latch and `_tail_residual()` are un-instrumented (report §3 item 8, cr-performance not_covered names it as its own biggest gap). Every delta finding that could be streaming-only - e.g. the CR-DELTA-CORRECTNESS-07(a) tradeoffs collapse, the region-guard pre-scoring pass, ENABLE_FULL_STREAM_DEADLINE x ENABLE_PREVERDICT_DISCONNECT_ABORT - was reasoned or unit-reproduced on the sync path only.
- IDOR / OWNERSHIP was mandated by the cr-security lane prompt ("user-scoped reads through the ADMIN client instead of get_user_supabase_client") and the word IDOR appears ZERO times in the report and ZERO times in all 100 filed findings. The lane's coverage text says only that "every admin-client read of user data in history_routes was checked" - home_routes.py:215, :599, :822 and profile_routes.py:184, :305, :474 each fall back to `get_admin_supabase_client()` (RLS-bypassing) when `access_token` is falsy and were never enumerated, and neither was share_routes / referral_routes / feedback_routes client selection. 51 of 141 .execute() sites hit `users` and 22 hit `comparisons`; only the history subset got an ownership verdict.
- THE UNVALIDATED-FETCH CENSUS IS ONE-SIDED. The lane prompt asked for "every place a user-supplied URL is fetched WITHOUT [is_safe_url] (price_service page scrape of Serper links? url_extraction_service? image_service?)". The report enumerates only the sites that DO call the validator (url_routes.py:91,112,148,175,197,216; price_service.py:14126/:14147; shopify_pdp_service.py:284). `image_service` appears zero times in the lane coverage statement and zero times in the 100 findings, so the negative half - which fetchers of model/Serper-supplied URLs skip the guard entirely - was never established.
- TWO cr-data-migrations PROMPT CELLS WERE SKIPPED SILENTLY: `.text_search()` argument shape (CLAUDE.md documents a live trap - options={"type":"plain","config":"english"} must be positional-options, not kwargs, and `.limit()` must precede it) is mentioned only as one entry in the op histogram and never checked at any call site; and `count='exact'` usage cost in app code was never censused (the only count=exact occurrences are the reviewers' own probe headers). Neither appears in the lane's not_covered list, so they read as covered when they are not.
- AUTH-ROUTE BODIES ARE OWNED BY NOBODY IN THIS CAMPAIGN. cr-security's not_covered defers `auth_routes.py` register/login/refresh/social-login/password-reset internals to "the M13/M18 lanes" - lanes that do not exist in m22 - so token TTLs, refresh rotation, social-login nonce handling and the lockout path went unreviewed at 76ace90 while being cited as fixed in prior handoffs.
- NO SYSTEMATIC STATUS PASS OVER THE PRIOR BACKLOG. Only 20 of 100 rows carry a `known:` dedupe. The brief's dedupe sources include M13's 74 P2 + 40 P3 and M18's 175 rows (of which ~130 were left live per the campaign handoff); the report gives fresh status for roughly eight of them (M13-45/49/50/102, quality-bloat-06, #65, #89, #114). A reader cannot tell whether the remaining live M13/M18 P2/P3 rows are still open at 76ace90.
- ACKNOWLEDGED-BUT-UNCLOSED CELLS THAT STILL BOUND THE ANSWER: live index and RLS-policy state (PostgREST cannot reach pg_indexes/pg_policies, no DATABASE_URL, EXPLAIN disabled - so every index and policy claim is DDL-derived); no write-path RLS test (so the L2 cache-poisoning limb of CR-SECURITY-13 is reasoned only); dead code INSIDE function bodies, the 103 StructuredComparisonService methods and 298 price_service module constants; jest never run locally and the other 19 moduleNameMapper stubs never diffed; GitHub rulesets never checked; MCP tool-schema token cost unmeasured.

## unverified_claims
- CR-PERFORMANCE-05's page sizes are labelled `measured` in the P2 table but are MODELLED extrapolations: the live `comparisons` table holds 27 rows with max_rows_per_user = 10 (the verify note on CR-PERFORMANCE-11 states exactly that), so the "358 KB at the FE default limit=20" and "897 KB at 50" pages do not exist in the data that was measured - only the per-row average does. The same report also prints two different 'measured' averages for that one SELECT: `avg 17,936 B/row` in §1b and `avg=17955 B` in the §5 verify note.
- The P0's severity rests on dev-box (Windows, off-lock) resolver behaviour: the 3.0 s stub / 0 ticker advances and the 11.07 s black-holed-host number are Windows measurements, and Railway/Linux resolver timeouts, plus the deployment-wide reach through the shared-edge-IP limiter, are MODELLED. The report says so in §3 item 3 but §0.1 presents the finding as 'Measured, not argued'.
- CR-SECURITY-01's headline consequence ('one unauthenticated POST wipes any user') is inferred, not demonstrated - the destructive RPC was deliberately never fired; the evidence is a GET-only privilege probe reading SQLSTATE 25006 with a per-function verb. Residual doubt is stated as ~0.02 but the claim is not executed.
- §0.2's operational instruction ('treat ADMIN_API_KEY as possibly already disclosed and rotate it') rests on an unknowable: the report itself says nobody could check whether such a 500 has ever fired in prod. The exfiltration was reproduced locally through a real init_sentry()+TestClient pipeline, never against prod.
- CR-CLAUDE-CONFIG-02's spend limb is env-derived only. That ENABLE_BRIGHTDATA_FALLBACK + key + zone are set on both services is measured; that Bright Data 'is the live search path the moment Serper errors' and that it bills was never exercised (no call was made, vendors dead), and CLAUDE.md's own note says the mapper must be validated from the first-call `[brightdata] parsed OK` log - i.e. it has never parsed a real response.
- The entire §1d dead-code list is proof-of-no-reference, never proof-of-green-CI: no deletion was attempted and pytest was not run after any hypothetical removal (the lane says so). The same applies to §1c item 8's proposed fix - a two-entry jest `projects` matrix - which is untested because no lane ran jest at all.
- §1b's per-request millisecond figures (SSL context construction, create_client, extract_price_from_html) are dev-box Windows numbers on an off-lock pip env; only the COUNTS transfer to Railway. Upstash RTT from Railway is still unmeasured, the same gap M18 recorded, so every 'compares/min' consequence stays modelled.
- CR-DELTA-CORRECTNESS-04/06/07 and the home_routes rows were never driven through the real orchestrator (OpenAI 429, Serper -50); they are unit-reproduced against exported trees. The 27-fixture flag-OFF A/B is synthetic - authored by the lane, not drawn from the 27 recorded Supabase payloads, which the lane itself notes would not exercise the M20/M21 paths.

## wrong_or_stale_claims
- STALE AT THE CHECKOUT THE REPORT TELLS READERS TO USE. §0.3, CR-CLAUDE-CONFIG-02 and unit U16 assert that `CLAUDE.md:328` documents Bright Data as 'default OFF/unset - dormant' and that the report 're-verified this myself this run'. At the worktree HEAD (`e8991cf`, 'docs(session-65): ... CLAUDE.md truth fixes', on top of 67e9e26) that same line 328 now reads '**ARMED IN PROD on `web` AND `price-warmer` - flag=true + key + zone set, verified 2026-09-05/07 ... documented as dormant until the 2026-09-06 review corrected it (CR-CLAUDE-CONFIG-02)**'. The doc half of the finding is already fixed and the report never says so. Same for `ENABLE_TPM_AWARE_ROUTING` at CLAUDE.md:394 (now explicitly labelled PHANTOM), the ACTIVATION-ORDER step-(2) caveat (now carries the CR-COMPLEXITY-DEADCODE-04 note), and the migrations 033/034-applied correction - all landed in that commit, all still presented as open in §0/§2/Appendix B.
- SCOPE ERROR BEHIND THOSE CITATIONS: the report's header lists the byte-identical paths as `app/`, `SmartCompareApp/`, `tests/`, `scripts/`, `migrations/` and requirements - CLAUDE.md and `.claude/` are NOT in that list, and CLAUDE.md did change (git diff 76ace90..HEAD -- CLAUDE.md = +17/-4). Yet all 19 cr-claude-config findings cite CLAUDE.md line numbers with no statement of which revision they are at. (`.claude/settings.local.json` IS byte-identical between the two clones, 13,655 B both, so CR-CLAUDE-CONFIG-11 stands.)
- INTERNAL COUNT CONTRADICTION ON THE ADMIN SURFACE. §6 coverage reports 'Auth census: verify_admin_key 19' while §1a / CR-SECURITY-04 says the TypeError 500s 'all 22 admin-key-gated routes'. Ground truth at HEAD: `grep -rn "Depends(verify_admin_key)" app/` = 22 (19 in app/api/admin_routes.py + 3 in app/api/text_routes.py:807, :863, :898). The finding's 22 is right; the coverage statement's route-table census under-counts its own surface by the three /text/* admin routes.
- MEASUREMENT DENOMINATORS DISAGREE FOR CR-PERFORMANCE-01: §1b says '4 soups on 40/40 cached PDPs and 363 soups for 92 pages', the P1 table title says '4x per call on 89/92 cached PDPs'. Two different sample sizes are presented for one measurement with no note that they are two runs; 4x92=368 vs the reported 363 is only reconcilable via the 89/92 figure.
- DEDUPE LABELLING ERROR (brief rule 3): CR-COMPLEXITY-DEADCODE-05 carries dedupe `new` in both the state file and the P2 table while its own title and §1c item 1 identify it as M13-45 still-open-and-grown (1,933 -> 1,949 LOC). CR-DELTA-CORRECTNESS-01 is likewise filed `new` and then refuted on the ground that it is the shipped fix of M18 PO-fact-check-06 / #104. The prose is right and the ledger column is wrong.
- SPOT-CHECK RESULT (no error found, recorded for completeness): I re-opened 12 cited pairs at HEAD and all hold - url_validator.py:30 `socket.getaddrinfo(hostname, None)`; admin_routes.py:33 `Header(...)` + :37 `hmac.compare_digest` on str; text_routes.py:759 `@router.get("/prices/{product}")` with no auth dep; auth_service.py:256 `client = get_auth_client()` one line above the run_db lambda at :257; scoring_service.py:1554-57 the raw `f"{brand} {name}"` concat; price_service.py:10642 `BeautifulSoup(html,'html.parser')`; price_service.py:3177 `extract_domain` with substring `.replace("www.","")` and no lower(); migrations/010_enable_rls.sql:28 / :58 / :70; config.py:55 `settings = get_settings()`; jest.config.js:18 + the 143-line react-native stub with `Platform = { OS: 'ios' }`; ResultsContent.tsx:425 `comparison_quality === 'weird'`; sentry_sdk.init at sentry_service.py:215 with no `include_local_variables`; zero `TPM_AWARE` hits under app/ + scripts/; `cleanup_expired_ratings` in no migration and no code; only 036 carries a REVOKE. CR-SECURITY-12's allow-list claims also reproduce on this box (100.64.0.1 and 224.0.0.1 both pass is_private/is_loopback/is_link_local/is_reserved; 169.254.169.254 and 127.0.0.1 are blocked).