# Full review 2026-09-06 — product output · code · load/scale · mobile — base `76ace90`

**Consolidated 2026-09-07 (SESSION 65).** Orchestrator/reviewer: Fable. Workers: Opus 5 (four ultracode Workflows, internal ids `m22-*`; NOT the concurrent session's `feature/m22-price-truth-cluster` / `feature/m23-mobile-w1`). Every P0/P1 below was (1) filed by a coverage-driven finder, (2) confirmed by a refute-by-default verifier, (3) re-reproduced by a second, reproduction-lens vote, and (4) re-derived by me against the code — the four verdict trails are in `2026-09-06-full-review-verified.json`; my own reads are in `2026-09-06-full-review-state/FABLE_REVIEW_NOTES.md`. Nothing here is a fix: every item becomes a unit through the standing gates (§2).

**Ground truth used:** prod = Railway `web` at `76ace90`, flags in `2026-09-06-full-review-state/baseline/railway-web-vars.txt`; phones = the `preview` OTA published from `97b5f15`; OpenAI **429 credit_balance_exhausted**, Serper balance negative (so every search leg is a PAID Bright Data request); local pip OFF-LOCK (claims about framework/SDK shape were checked against the pinned wheels, not the local env). The measured `search_logs` series is **88.6 % probe traffic** (PO-RECORDED-MEASURED-01); the honest organic series is ~1,513 rows, **80 % success, p50 22.0 s** — every latency/failure number quoted before this review must be re-read on that subset.

**Counts.** 442 filed (product 104, code 100, load 96, mobile 142); after both verification stages and my dedupe: **1 P0**, and the P1 set below (per-workflow tallies in §4). 11 refuted at first verification, 1 downgraded at the second vote (§5). Dedupe collapsed 12 clusters (§4 header) — the same defect was independently found by up to three lanes, which is corroboration, not double counting.

---

## 0. Stop the line — true TODAY, in the order it hurts

1. **P0 — the event loop can be frozen by anyone with a hostname.** `app/utils/url_validator.py:30` runs a synchronous, untimeoutable `socket.getaddrinfo` ON the loop from every unauthenticated `/api/v1/url/*` handler (`url_routes.py:91/112/148/175/197/216`) and per candidate URL + redirect hop on the price path (`price_service.py:14126/14147`, `shopify_pdp_service.py:284`, `url_extraction_service.py:101/111`). Measured 11–12 s per black-holed host, no negative cache: one request with two such hosts parks the only worker; `/health` shares that loop. (LS-REQUEST-PATH-BLOCKING-01 = CR-SECURITY-05.) **Unit W0-1, lands BEFORE #114.**
2. **LIVE SPEND with nothing to show for it.** Bright Data is ARMED on `web` and `price-warmer` (flag + key + zone), `SERPER_LIFETIME_LIMIT=0` routes every search leg to it, and `ENABLE_BRIGHTDATA_BUDGET_GATE` is UNSET — so it is unbudgeted, unmetered, breaker-less (LS-FAILURE-MODES-COST-01; CLAUDE.md said "dormant" — corrected in this PR, CR-CLAUDE-CONFIG-02). The app's only compare shape (`explicit_pair`, `scs.py:3250`) never checks OpenAI health, so with OpenAI at 429 **every compare pays the Bright Data / render cascade and then fails** (LS-FAILURE-MODES-COST-02/03: no `openai` budget entry, no breaker). **Ahmed/ops today:** set `ENABLE_BRIGHTDATA_BUDGET_GATE=true` on both services (already-written path, 4,500/mo ceiling) — or unset `ENABLE_BRIGHTDATA_FALLBACK` until OpenAI credits return. Unit W1-3 adds the OpenAI breaker.
3. **Phones are four waves behind main.** The `preview` OTA `97b5f15` predates ALL FOUR M21 waves: on devices every Results analytics batch / feedback still 422s and every free user is paywalled at 3 lifetime compares (MB-RECONCILE-06). Nothing mobile in this report is "fixed for users" until `eas update --branch preview` from ≥ `76ace90` (native-compatible per the two-lever lane). **Top Ahmed action, unchanged.**
4. **Two secrets can leave the building on an unauthenticated request.** `verify_admin_key` raises `TypeError` on any non-ASCII `X-Admin-Key` (`admin_routes.py:37`), the error middleware 500s and ships the exception to Sentry, and `_before_send` scrubs 5 of 12 event regions — frame locals (`expected` = the production ADMIN_API_KEY, `token` = user JWTs), request body/cookies, extra, contexts, logentry go raw (CR-SECURITY-04 + -03). **Unit W1-1.**
5. **Supabase grants and RLS are not what the migrations say.** `delete_user_cascade` (+ `increment_lifetime_comparisons`, `cleanup_expired_ratings`) are SECURITY DEFINER with EXECUTE left to PUBLIC/anon (CR-SECURITY-01); `user_events` is world-readable to the anon key despite `010:58` (CR-SECURITY-02) — 146 rows, 7 real user UUIDs. P1 not P0 only because the anon key is server-side. **Unit W1-2 = migration 037 + live pins.** Logout never revokes the Supabase session (LS-CACHE-REDIS-03) and a Supabase blip force-logs-out every user at boot (MB-NETWORK-CONTRACT-02) — same auth unit family.
6. **Features that are dead while looking alive.** `/url/compare` 500s on every successful extraction — the callee has returned a tuple since 2026-03-13 and this caller never unpacked it (PO-VERDICT-TRUTH-01). 100 % of in-app result feedback is 422-rejected — the three chips are not in the backend allowlist, on phones AND main (MB-NETWORK-CONTRACT-01). Every referral share link and every universal/app link targets `qaren.app`, which answers **522** on every path (re-measured 2026-09-07; MB-FLOWS-STATE-01) — the standing DNS blocker now has a consequence list. Password reset cannot be completed by any email/password user (no recovery route, no `redirect_to`; MB-FLOWS-STATE-02). The Serper-shopping tier writes a Google search link into `price.url` and HEAD's own display backstop then pends it as `non_pdp_url` (PO-RECORDED-MEASURED-03) — the highest-volume price source is structurally unshowable.
7. **The loop is taxed by things no flag removes** (the true W0): per-request Supabase `create_client` outside the `run_db` lambda (LS-REQUEST-PATH-BLOCKING-02), Upstash client at `timeout=None` + `time.sleep(3)` retry (-03), postgrest 120 s default with zero `ClientOptions` (-04), HTML price parse on the loop with the same page souped four times (LS-CONCURRENCY-LIMITS-01 + CR-PERFORMANCE-01). Measured from the prod container: Upstash 178 ms, Supabase ~285 ms per round trip, both offload flags unset (LS-MEASURED-EVIDENCE-01). **Ordering rule stands:** W0 → offload flags ON → `key_style="endpoint"` (LS-RATELIMIT-KEY-01) with #114 → second worker.
8. **The product's numbers lie in specific, repeatable ways** (all LIVE at prod flags): a native Bahraini `22.500 BD` ships as 22,500 BHD, TRY/PLN/CHF ship as BHD (PO-PRICE-TRUTH-01/-02); 82 % of `local_bhd` rows are not on a Bahrain host (PO-RECORDED-MEASURED-02); scores are computed on a price the chokepoint then pends (PO-RECORDED-MEASURED-04); a timeout partial crowns product_0 with a fabricated 70/69 (PO-VERDICT-TRUTH-02); the decisive dimension is reported "N/A" (PO-RUBRIC-01); two equal, fully-verified products are reported as missing data (PO-RUBRIC-02); `fashion`/`other` have no numeric spec fields so 35–40 % of their score is deleted (PO-RUBRIC-03); pharmacy "tablets" are electronics (PO-CATEGORIES-I18N-01); Arabic YSL Opium is blocked (PO-CATEGORIES-I18N-03); raw Python exception text is the user-facing error (PO-RECORDED-MEASURED-05); the specs prompt still licenses training-data fill (PO-PROMPTS-11, gated until Serper is healthy); brand dedup from M21 W3 collapses `tradeoffs` to `[]` whenever a name repeats its brand (CR-DELTA-CORRECTNESS-07, LIVE regression).

**Ahmed-gated (unchanged + sharpened):** `eas update --branch preview` (now); OpenAI credits (re-score with `scripts/eval_runner.py` the moment they land — §3); qaren.app origin (share links, deep links, password-reset landing, AASA/assetlinks); Awin/CJ; the Bright Data gate flip (or fallback pause); **rotate `ADMIN_API_KEY`** (treat as possibly disclosed — nobody can check whether a non-ASCII `X-Admin-Key` 500 has already fired into Sentry; do it with unit W1-1); **rotate, then delete `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`** from both Railway services (zero code readers, a committed test forbids reading them) and drop the phantom `ENVIRONMENT`/`ENABLE_HYBRID_MODEL_ROUTING` in the same pass; apply migration 037 after 035/036 (verify schema after apply); the Supabase redirect allow-list entry for `qaren://reset-password`; App Store gates (icon bytes, legal DRAFT); the `.claude/settings.local.json` cleanup (CR-CLAUDE-CONFIG-11 — propose-only: delete the four shutdown/Stop-Computer entries and two Stop-Process entries, split `Bash(git *)`, add a `deny` block, untrack the file).

---

## 1. The answers (one per workflow synthesis; Fable-reviewed)

_(Filled from the four synthesis reports `2026-09-06-full-review-state/report-m22-*.md` once the continuation workflows complete; each answer is re-derived against the verified JSON, not copied.)_

### 1.1 Load / scale — "can it handle a huge load of people at the same time?"
### 1.2 Code — structured review (security / performance / complexity / dead code) + M20/M21 interaction bugs

Source: `report-m22-code-review.md` (7 lanes, 100 filed → **1 P0 · 10 P1 · 57 P2 · 29 P3 · 3 refuted**; the 4 P1s whose second vote died were re-voted in synthesis and all held). Fable-reviewed: I agree with every severity; two design disagreements are marked ✎.

**Security (critical = P0, high = P1).** critical: `CR-SECURITY-05` = the P0 DNS stall. high: `-01` anon `EXECUTE` on `delete_user_cascade`/`increment_lifetime_comparisons`/`cleanup_expired_ratings` (proven read-only via the per-function SQLSTATE 25006 verb; `cleanup_expired_ratings` exists in NO migration — resolve its real signature from `pg_proc` before revoking); `-02` `user_events` RLS not in effect live (146 rows / 7 UUIDs anon-readable while sibling tables are enforced); `-03` + `-04` the admin-key `TypeError` → 500 → Sentry event carrying `ADMIN_API_KEY` in frame locals (reproduced end-to-end through the real `init_sentry()` + capturing transport: **99 unredacted sentinel sites**; the scrub alone leaves 48 because `_scrub_dict` never descends the ASGI header list-of-lists, so `include_local_variables=False` is part of the fix); `-06` `/text/prices/{product}` unauthenticated paid fan-out (route introspection: `PRICES_DEPS=[]`). medium: `-07` the `comparisons` SELECT policy hands the anon key every `share_token` + owner `user_id`; `-08` `extract_specs_targeted` has no prompt fence at all; `-10` logout reports success while revoking nothing when Redis is down; `-11` a missing `X-Admin-Key` 422s naming the header (admin routes enumerable); `-12` the SSRF guard is TOCTOU (validated hostname re-resolved by the fetcher; CGNAT/multicast/arbitrary ports allowed); `-13` anon reads the whole L2 cache and both cohort PII views, and 012's `USING(true)` policies are an anon **write** path into a displayed price; `CR-COMPLEXITY-DEADCODE-11` a live `CLOUDFLARE_API_TOKEN` in both prod services with zero readers. low: `backend/` is a deployable shadow app with no middleware; five small route defects held by design behind #114.

**Performance (counts transfer; dev-box ms do not).** On the loop per request and removable by no flag: `CR-PERFORMANCE-01` four soups per page (87 % of the extractor's own runtime; 363 soups / 92 pages), `-03` 2–3 fresh Supabase clients per authed request (measured per route: `/comparisons/history` 3), `-02` 21–26 throwaway `httpx.AsyncClient`s per compare (P2 — the "45 % of wall" figure was a Windows artifact), `-10` `html.parser` everywhere while `lxml` is pinned and never imported (withdraw M13-50's "delete lxml"). Redundant work: Shopify catalog fetched twice per domain (no single-flight), four redundant Upstash GETs per compare, `record_success()` GETs and does nothing, `/comparisons/history` ships 358 KB of `full_response` for a 200-byte summary. Offload coverage narrower than the flags imply: 8 request-path reads bare under `ENABLE_SYNC_DB_OFFLOAD` (`/home/*`, `/profile/*`), and `#115`'s dispatchers bypassed by `review_service`/`usage_service`'s hottest reads under `ENABLE_ASYNC_REDIS_OFFLOAD` — a canary of either flag today measures the wrong thing. Scales badly: `/home/savings` flag-OFF scan (N × 17.9 KB), every analytics query silently truncated at PostgREST's 1,000-row cap (popular queries ranked from 6 % of `search_logs`), no retention on any append-only table.

**Complexity.** `_get_price` is 1,949 LOC / cc 320 / 24 swallow-and-continue handlers (M13-45, grown) — carve the three banner-delimited phases into module functions one byte-identical commit at a time, never rewrite. The two god modules are held apart by **98 function-local imports** (20 cycles below module level; every hoist is an `ImportError`) — extract a leaf `price_primitives.py` first. Host normalisation exists nine ways and one is wrong (`extract_domain` substring-replaces `www` and never lowercases, byte-duplicated in `review_service`). `build_comparison_response` has 22 kw-params and the PARTIAL path already silently omits one. The API-contract models (`app/models/*.py`, 190 statements) are imported by nothing — the structural reason wrong-shape mocks (`#65`, the phantom `response_data`, the re-engagement columns) are undetectable. All 245 jest suites run against a 143-line `react-native` stub with `Platform.OS='ios'` — every Android branch is untested.

**Dead code — proven safe-to-remove (no-reference, NOT green-CI; one commit per row through the module-reference comm gate):** the `cache_service.py:586-765` v1 block (a second rate limiter + a second budget gate + a name-colliding `get_price_cache_key`; retire `FREE_TIER_DAILY_LIMIT`/`MAX_MONTHLY_COST`/`CACHE_DURATION` and their Railway vars with it — they look like spend controls and are not); 25 orphaned `*_mismatch` axis predicates (~250 LOC in `price_service`); `judgeme_service.py` (+ flag row — licensing says never ship it); `verdict_builder.py`; three `database_service` user-CRUD functions; `quality_ranker.py`; five unreferenced Serper entry points; nine repo-root orphan scripts (a real hazard: bare `pytest` collects them and they `load_dotenv()` before the credential sanitiser — add `testpaths=["tests"]`); `app/config.py` (import-time `Settings()` trap); `backend/` (delete at minimum its `Procfile`/`railway.json`). KEEP: `refund_anon_comparison_credit` (wire it, #128), the sitemap helpers (no runner, not dead), `warmer_write_veto_async` (documented deferral — refuted as dead), the six repo-root JSON corpora (relocate), the `get_rate` cluster (M13-39 decision). UNSURE: `yotpo_service.py` (wire behind the existing dark flag or move to prototypes), the two schema modules (wire them; leaving them is the one wrong option).

**The three plain questions.** *Sound after M20/M21?* Directionally yes (a 27-fixture flag-OFF A/B through both trees differed only by the intended `#104` authority delta), mechanically not yet: the delta shipped **two unflagged live defects** and **four dark flags that carry a defect that manifests only on flip**, and the offload coverage is partial on exactly the hottest paths. `ENABLE_TPM_AWARE_ROUTING` is documented and has **zero code sites**. *LIVE vs DARK:* live = the DNS stall, the RLS/grant drift, the admin-key→Sentry chain, the unmetered paid door, Bright Data armed+uncapped, personalization dead for all 18 users (#65), `tradeoffs` collapse, `/home/savings` caching the degraded zero payload for 6 h, five runner-less cron scripts, a live-suite that has never executed, and a CI deselect list where 2 of 11 rows are green (honest count **9**). Dark-but-armed = `#100`'s spec-norm scores an unparseable value as the number 1 (winner flips on `'Lightweight'`), `#102`'s honest-absence branch is dead, `#108`'s rubric downgrades hyphenated units, `ENABLE_COMPARISON_QUALITY_V2` emits `'weak'` which the shipped client's denylist lets through (un-suppresses the reveal on thin evidence — needs an OTA BEFORE the flip), and the two review flags gate modules with no importers. *Interaction bugs, as M18 found two?* **Five, two live:** the brand-dedup split (`tradeoffs = []`), the `#116` TTL × degraded-payload cache (5 min → 6 h), the anon debit-before-validation ordering under the dark gate (#128), the quality-label × client denylist (two-lever), and the spec-norm presence-credit axis.

**Appendices (in the source report):** A — the Claude Code configuration audit plan (propose-only): delete the shutdown/Stop-Computer/Stop-Process grants, split `Bash(git *)`, add a `deny` block, untrack `settings.local.json`; ten dead doc pointers; three stale skills; `CLAUDE.md` is ~33 k tokens of always-on context with a ~24 k-token trim plan (generate `docs/FLAGS.md` instead of writing it). B — the flag three-way table: 5 flags ON in prod with zero `CLAUDE.md` mentions, 21 read in code and documented nowhere (incl. `ENABLE_BRIGHTDATA_BUDGET_GATE`), and the documented-but-false rows. C — the `#89` eleven-node table (9 red: 2 red-by-choice diagnostics, 3 one fixture bug, 2 price-path assertions, 2 TDD stubs never xfail-contained; 2 GREEN and permanently deselected; pytest accepts a `--deselect` for a nonexistent node silently).

✎ **Fable disagreements with the synthesis, recorded:** (1) its U1 ships the DNS resolver flag **default ON**; this plan keeps every W0 flag default OFF (byte-identity gate, one env flip = activation step 1). (2) its U6 memoises clients with no flag; this plan keeps `ENABLE_SUPABASE_CLIENT_REUSE` because client sharing carries a real concurrency risk (JWT bleed) that deserves a kill switch. (3) its U4 rides `ENABLE_ANON_USAGE_GATE` for `/text/prices`; this plan meters it under `ENABLE_PAID_ROUTE_METERING` with the camera/URL doors (W2-1) so the anon-gate flip stays independent of #128.
### 1.3 Mobile — on phones vs on main, the OTA, App Store status, the Arabic experience
### 1.4 Product output — is it any good today, what each dark flag changes, what must be re-scored live

---

## 2. Units through the standing gates — the wave plan

**Gates on every unit (from CLAUDE.md):** TDD red-first (the listed red tests fail for the right reason before any implementation); the **module-reference comm gate** (build the test set by grepping `tests/` for imports/references to every module the unit touches, run it at base and at head, `comm` the failures — never filename keywords); **flag-OFF byte-identity** wherever `price_service`/`structured_comparison_service`'s price path is touched (golden fixtures `tests/fixtures/*_flag_off_golden.json` + the `_proof/` zero-network sweep); Fable review of spec + tests BEFORE implementation and of the diff BEFORE commit; path-restricted commits; isolated worktree per wave; PRs, never pushes to main; re-read `origin/main` before every integration. Mobile client units are unflagged by nature and reach phones only via OTA — each says **OTA-safe** or **needs-build**.

**Ordering rationale.** W0 removes the loop taxes that no flag removes and is the precondition for BOTH the offload canaries and #114 (the mis-keyed limiter is what protects the single worker today). W1 closes the security/spend doors that are reachable now. W2 closes the two unmetered paid doors and the dead URL route in one unit (metering a 500 route is pointless). W3 is the mobile set that is NOT on the other session's branch. W4 is product output, ordered by user-visible harm, every price-path unit flagged. W5 is the activation runbook — env flips only, one canary window each.

### W0 — load foundations (backend; before any offload canary and before #114)
| unit | findings | files | flag | tests-first (red) | notes |
|---|---|---|---|---|---|
| **W0-1 off-loop DNS resolve** | LS-REQUEST-PATH-BLOCKING-01 (P0), CR-SECURITY-05 | `app/utils/url_validator.py`; call sites `app/api/url_routes.py` (6), `app/services/url_extraction_service.py:101/111`, `app/services/price_service.py:14126/14147`, `app/services/shopify_pdp_service.py:284` | `ENABLE_OFFLOOP_DNS_RESOLVE` (default OFF) | (a) loop-heartbeat: `socket.getaddrinfo` patched to sleep 3 s → async validator returns False within ≤2.5 s while a 10 ms heartbeat keeps ticking (RED: ticks==0); (b) positive+negative memo (60 s) hit on the second call; (c) resolver pool is a DEDICATED `ThreadPoolExecutor(max_workers=4)` not the shared 40-thread executor (assert pool identity + zombie bound); (d) flag OFF ⇒ sync path byte-identical (same results as today for stubbed hosts); (e) `url_routes` handlers use the async form under the flag. | `asyncio.wait_for` cancels the await, not libc — the dedicated pool bounds the zombie count; keep the sync shim for the warmer/CLI. Price-path touch ⇒ byte-identity gate. |
| **W0-2 Supabase client reuse + bounded timeouts** | LS-REQUEST-PATH-BLOCKING-02, -04, CR-PERFORMANCE-03 | `app/services/auth_service.py:30/37/256`, `app/services/database_service.py:44/52` (+ `home_routes.py:214`) | `ENABLE_SUPABASE_CLIENT_REUSE` (default OFF); `SUPABASE_POSTGREST_TIMEOUT_SECONDS` (unset = today's 120) | (a) `create_client` counter over two authed `GET /auth/me` + `GET /usage/status`: ≤1 per key kind under the flag (RED today: 2–3 per request); (b) constructed clients: `postgrest.session.timeout.read ≤ 10` and `storage` bound; (c) JWT non-bleed: two concurrent user-scoped calls carry their own `Authorization` (never a shared `postgrest.auth(token)` mutation); (d) flag OFF ⇒ counter and timeouts identical to today. | Spike first against the PINNED `supabase==2.31.0` wheel (local is off-lock): if `ClientOptions.httpx_client` exists, share ONE `httpx.Client` (one SSL context) across per-request clients; else a keyed pool. Bound `/home/savings` separately or land with #116 (an 8 s bound can cut its unbounded scan). |
| **W0-3 Upstash bounded transport** | LS-REQUEST-PATH-BLOCKING-03 (= LS-CACHE-REDIS-01 = LS-CONCURRENCY-LIMITS-04) | `app/services/cache_service.py:28/36` | `ENABLE_UPSTASH_BOUNDED_TRANSPORT` (default OFF) | (a) `redis_client._http._client.timeout.read ≤ 2.0` and `_retries == 0` under the flag (RED: `None`/1); (b) stalled transport (stub sleeps 5 s) ⇒ `_redis_get` returns None within ≤2.5 s, fail-open preserved; (c) `redis://` fallback gets `socket_timeout`; (d) flag OFF ⇒ library defaults untouched. | Hard precondition for `ENABLE_ASYNC_REDIS_OFFLOAD` too (a hung command parks an executor thread forever). Verified on the pinned 1.7.0 wheel: no timeout kwarg exists — replace the transport after construction. |
| **W0-4 parse once, off the loop** | LS-CONCURRENCY-LIMITS-01 + CR-PERFORMANCE-01 | `app/services/price_service.py:10642/13056/13092/13097/13112/14090/15425`, `structured_comparison_service.py:1643/1722` | `ENABLE_PRICE_PARSE_OFFLOAD` (default OFF) | (a) `BeautifulSoup.__init__` counter == 1 per `extract_price_from_html` call on a JSON-LD fixture (RED: 4); (b) heartbeat test: `fetch_page_price` with a 300 ms stub parse does not block a 50 ms ticker; (c) flag OFF ⇒ 4 soups and inline parse (byte-identical); (d) goldens + `_proof` sweep unchanged under both states. | Pure-sync parse; `to_thread` at the four sites. |

### W1 — security + spend doors (backend)
| unit | findings | files | flag | tests-first (red) | notes |
|---|---|---|---|---|---|
| **W1-1 admin key + Sentry scrub** | CR-SECURITY-04, CR-SECURITY-03 | `app/api/admin_routes.py:34-38`, `app/services/sentry_service.py:128-190` | none (security; behaviour only stricter) | (a) `verify_admin_key` over `['é','ك','\xff'-latin1,'','wrong']` ⇒ always `HTTPException(403)`, never `TypeError` (RED: TypeError ×3); (b) a fixture event with a JWT + admin key in all 12 regions ⇒ zero survivors after `_before_send` (RED: 12–17); (c) `include_local_variables=False` OR frames `vars` scrubbed. | Also make a MISSING header a 403 not a 422 (`Header(default="")`). |
| **W1-2 migration 037: grants + RLS truth** | CR-SECURITY-01, CR-SECURITY-02 | `migrations/037_security_definer_grants_and_rls.sql` (+ rollback), `tests/` | n/a (migration) | (a) source test: every `SECURITY DEFINER` in `migrations/` has a matching `REVOKE ALL … FROM PUBLIC` (RED: 3 lack it); (b) `live_db` matrix: anon `count=exact` per table matches the documented model (`user_events` anon must be 0; RED: 146). | Apply is an Ahmed/ops step (033–036 are also unapplied); the doc says so. |
| **W1-3 OpenAI breaker + budget entry** | LS-FAILURE-MODES-COST-02, -03 | `app/services/api_budget_service.py:33`, the 4 `AsyncOpenAI` construction sites, `scs.py:3250` preflight | `ENABLE_LLM_PREFLIGHT_BREAKER` (default OFF) | (a) three consecutive 429s ⇒ the 4th call short-circuits without dispatch (RED: dispatches); (b) explicit-pair compare with the breaker OPEN ⇒ returns the existing `LLM_UNAVAILABLE`-class envelope BEFORE the provider fan-out (counting stubs on `search_web`/`bd_search_web`/Firecrawl == 0; RED: >0); (c) flag OFF ⇒ byte-identical. | Pairs with the Bright Data gate flip (env, W5 step 0). |
| **W1-4 logout revokes upstream + refresh 503-vs-401** | LS-CACHE-REDIS-03, MB-NETWORK-CONTRACT-02 | `app/services/auth_service.py:284-294/226`, `app/api/auth_routes.py:539`, `SmartCompareApp/src/services/authService.ts` (logout posts the refresh token; boot treats 503 as "keep session") | `ENABLE_LOGOUT_GLOBAL_SIGNOUT` (backend, default OFF) | (a) POST `/auth/logout` with a refresh token ⇒ stub records `set_session(access, refresh)` then `sign_out(scope='global')` (RED: bare `sign_out` on a session-less client); (b) `refresh_session` raising `ConnectionError` ⇒ 503 `REFRESH_UPSTREAM_UNAVAILABLE`, not 401 (RED: 401); (c) client: a 503 on boot refresh keeps the cached session (jest). | **Coordinate:** the other session's REMAINING "B2 expired-token 401" touches the same route — one owner. Client half is OTA-safe. |
| **W1-5 route-keyed limiter** | LS-RATELIMIT-KEY-01 | `app/middleware/rate_limiter.py:125-129` | ships WITH #114 (same PR family), else `ENABLE_LIMITER_ENDPOINT_KEY` | 35 GETs to `/api/v1/share/<distinct token>` ⇒ a 429 appears (RED: 35×200). | Independent of the key_func change; both must be live before the second worker. |
| **W1-6 graceful shutdown drain** | LS-FAILURE-MODES-COST-05 (P2) | `app/main.py`, `app/utils/async_utils.py:54`, `railway.json`, `Procfile` | none (ops) | a tracked background-task set is awaited with a bounded `asyncio.wait` on shutdown; `--timeout-graceful-shutdown 20` present (RED: no shutdown handler). | Low priority at ~0 traffic; cheap. |

### W2 — the metering unit (backend; mirrors `text_routes.py:193/247/278`)
| unit | findings | files | flag | tests-first (red) | notes |
|---|---|---|---|---|---|
| **W2-1 camera + URL metering, URL unpack, history, category** | MB-RECONCILE-01, MB-RECONCILE-07, MB-NETWORK-CONTRACT-14, PO-VERDICT-TRUTH-01, LS-RATELIMIT-KEY-02 (= CR-SECURITY-06) | `app/api/image_routes.py:65-89`, `app/api/url_routes.py:34-38/128-163`, `app/services/url_extraction_service.py:575-583`, `app/api/text_routes.py:759-798` | `ENABLE_PAID_ROUTE_METERING` (default OFF; OFF = byte-identical) | (a) `POST /image/identify` authed at the lifetime cap ⇒ 429 `USAGE_LIMIT` + `consume_comparison_credit` called (RED: 200, 0 calls); refund on <2 products identified and on exception (SIX exits — the double-charge trap from the other session's notes: assert exactly one consume and at most one refund per request); (b) `POST /url/compare` with the three leaves stubbed ⇒ `success: True` (RED: `AttributeError: 'tuple' object has no attribute 'get'`); meters/refunds/records like text; `selected_category` accepted and threaded; `save_comparison_and_track_cohort(input_type='url')` fired; (c) `GET /text/prices/{product}`: optional-user + metering (or `verify_admin_key` if it has no product consumer) + a route-keyed limit (RED: 200 unauth, 25 distinct strings ⇒ 0×429). | **Coordinate:** the other session's REMAINING A1/A6 = this unit; per the user's instruction this session owns it — their resume must skip A1/A6. Fix the `generate_comparison` annotation to the tuple. |

### W3 — mobile units NOT on `feature/m23-mobile-w1` (client unflagged; OTA-safe unless marked)
| unit | findings | files | tests-first (red) | notes |
|---|---|---|---|---|
| **W3-1 feedback chips allowlist** (backend) | MB-NETWORK-CONTRACT-01 | `app/api/feedback_routes.py:37-39`, `tests/test_feedback_allowlist_superset.py` | backend grep test: every `mattered_most: [` literal in `SmartCompareApp/src` is in `VALID_MATTERED_MOST` (RED: accurate/detailed/fast missing). | Backend-only ⇒ fixes phones without an OTA. Product may later rename the chips; the superset test then guards it. |
| **W3-2 comparison_id echo** (backend) | MB-NETWORK-CONTRACT-07 | `app/api/text_routes.py:276-291` (+ GET/stream twins), `app/services/feedback_service.py:47-51` | route test: authed `POST /text/compare` body carries a UUID `comparison_id` equal to the row written (RED: absent). | Await only the insert; keep cohort/Loop-2 fire-and-forget. Unblocks the share sheet at the high-intent moment and attributes feedback/analytics rows. |
| **W3-3 device fingerprint on social + share** | MB-NETWORK-CONTRACT-03, -04 | `SmartCompareApp/src/services/authService.ts:570/682`, `src/screens/ResultsScreen.tsx:797-810`, `app/api/auth_routes.py:365-391` (extract `_apply_device_fingerprint`) | jest: social sign-in sends `X-Device-Fingerprint`; share payload carries a 64-hex `device_fingerprint_hash`; backend: social path writes `device_fingerprint_hash` (RED ×3). | OTA-safe (client) + backend. |
| **W3-4 unauth reset → 'Auth'** | MB-FLOWS-STATE-04 | `ReferralLandingScreen.tsx:118/160`, `InviteeQuizScreen.tsx:275` | router-level test with the unauth route-name set: reset to the chosen route is handled (RED: `'Main'` dropped). | OTA-safe. |
| **W3-5 Step-17 notifications answer** | MB-FLOWS-STATE-06 | `screens/onboarding/OnboardingFlow.tsx:290-308/393-396` + the persistence payload builders | RNTL: render at step 17, press `s17-not-now` ⇒ `onComplete` payload has `notifications_enabled: false` (RED: absent); persistence payload includes it. | OTA-safe; verify the `:476` handler path first. |
| **W3-6 password reset flow** | MB-FLOWS-STATE-02 | `App.tsx` linking + `ResetPasswordScreen` (unauth branch), `app/services/auth_service.py:539` (`redirect_to`) | `getStateFromPath('reset-password#access_token=x&type=recovery')` names the route (RED: none); backend passes `redirect_to`. | Needs the Supabase redirect allow-list entry (Ahmed) and, for universal links, the qaren.app origin; the `qaren://` scheme works without it. OTA-safe client. |
| **W3-7 RECORD_AUDIO removal** | MB-TWO-LEVER-RELEASE-02 | `SmartCompareApp/app.json:71-74` (+ `expo-camera`/`expo-image-picker` plugin opt-outs) | config test: no `RECORD_AUDIO`, plugins set `microphonePermission: false` / `recordAudioAndroid: false` (RED). | **needs-build** (native manifest). |
| **W3-8 force-update wire** | (mobile-checkup lead A9; two-lever lane) | client version check ↔ `/app/version` | jest: a `min_version` above the running version routes to the blocking update screen (RED). | In the other session's REMAINING queue too — one owner. |
| **W3-9 lucide per-icon imports** | MB-STARTUP-BUNDLE-01 | 34 barrel imports | eslint `no-restricted-imports` on the barrel (RED: 34) + a source-map assertion (<100 lucide icon modules; RED: 1,703). Measure before/after with `expo export`. | In the other session's REMAINING "B1" — one owner. OTA-safe. |
| **W3-10 outbound retailer link** | MB-RECONCILE-15 | `ResultsScreen.tsx:500` + results components | after the product decision: a `results-price-link-0` testID exists and `Linking.openURL` is called with the http(s) `price.url` (RED). | **Product call first (Ahmed).** |
| Killed for me by the other session (verify after their merge, do NOT re-file): MB-RECONCILE-03 / MB-STARTUP-BUNDLE-02 (A3), MB-RECONCILE-05 / MB-FLOWS-STATE-05 (A18), MB-TESTS-TRUTH-04 (A12), plus checkup A2/A4/A5/A7/A8/A10/A11/A15/A17/B5. |

### W4 — product output (backend; ranked by user-visible harm; every price-path unit flagged + byte-identity OFF)
| unit | findings | files | flag | tests-first (red) | notes |
|---|---|---|---|---|---|
| **W4-1 shopping-rung currency truth** | PO-PRICE-TRUTH-01, -02, PO-RECORDED-MEASURED-02 | `app/services/price_service.py:3464` (`detect_currency`), `:9457-9462` (`_shopping_foreign_currency_signal`), `:9572-9591`, `:9682` | `ENABLE_SHOPPING_CURRENCY_TRUTH` (default OFF) | parametrized through `extract_price_from_shopping(…, 'BHD')`: `'22.500 BD'→22.5`, `'8.750 KD'→10.7625`, `'250 SR'→25.075`, `'32,000 QR'→3305.6` (RED: 22500 / 8750 / 250 / 32000); `'TRY 1.299,00'` ⇒ pended (RED: 1299 BHD local_bhd); a google.com `link` ⇒ never `local_bhd` (RED: local_bhd); flag OFF ⇒ goldens byte-identical. | Then flip `ENABLE_SHOPPING_STRICT_CURRENCY` (PO-PRICE-TRUTH-03) in its OWN window. Expect the genuine-BH-share KPI to DROP — that is the metric becoming true. |
| **W4-2 shopping url is not a search link** | PO-RECORDED-MEASURED-03 | `price_service.py:9678`, `build_retailer_url` (`:260-275`) | `ENABLE_SHOPPING_DISCOVERY_URL_SPLIT` (default OFF) | a shopping candidate with a `google.com/search?ibp=oshop` link, title matched ⇒ `is_price_showable(..., enforce_correctness=True)` True with `url` unset and `discovery_url` set (RED: pended `non_pdp_url`). | **Product decision first:** (a) resolve the merchant PDP before selection, or (b) keep the search link out of `url`. The unit implements (b) unless Ahmed picks (a). |
| **W4-3 pre-scoring showable pass** | PO-RECORDED-MEASURED-04 | `structured_comparison_service.py:3373-3418` (+ streaming twin `:3985-4128`) | `ENABLE_PRESCORING_SHOWABLE_GUARD` (default OFF) | two products whose prices carry a non-PDP url ⇒ `scores.product_0.breakdown.value_score` is the MISSING sentinel AND `missing_data` lists it BEFORE `compute_scores` runs (RED: 100.0 from the raw price). | Mirror of `apply_region_currency_guard` (#113). |
| **W4-4 partial never fabricates scoring_v2** | PO-VERDICT-TRUTH-02 | `app/services/response_builder.py:1107-1146` | `ENABLE_HONEST_PARTIAL_SCORING` (default OFF) | `build_comparison_response(…, scoring_result={}, metadata={'partial': True})` ⇒ `scoring_v2 == {}` and no `factual_verdict` names a winner (RED: 70/69, winner_idx 0). | FE already falls back (`ResultsContent.tsx:528`, `ResultsScreen.tsx:746`). |
| **W4-5 sample/decant guard reads `name`** | PO-PRICE-TRUTH-04 | `price_service.py:1694` (+ `scs.py:1660`) | `ENABLE_SHOWABLE_NAME_IDENTITY` (default OFF) | `is_price_showable('Xerjoff Ilm Eau de Parfum', {amount: 94, source_method: 'converted_usd', name: "'Ilm Sample & Decants by Xerjoff"}, …, enforce_correctness=True)` is False (RED: True). | One line; flagged only because it is the price path. Flip immediately after. |
| **W4-6 rubric truth** | PO-RUBRIC-01, -02, -03 (+ the #100/#101/#102 in-flag one-liners: PO-RUBRIC-09/-10/-11) | `app/services/scoring_service.py:262-286/1789-1826/2113-2155/2222-2233/2496-2533`, `response_builder.py:1706` | `ENABLE_RUBRIC_TRUTH_2609` (default OFF) + the existing #100/#101/#102 flags | (a) 50-vs-52 BHD identical-spec pair: the decisive dim is never in `missing_data` (RED); (b) identical fully-verified pair: `missing_data` empty (RED: 4/6); (c) two different fashion products: `spec_raw` differs (RED: 1.0 == 1.0); (d) no-data never outscores measured-bad in 9 categories under both flag states (RED: 9/9); (e) `'5000 mAh' vs '5 Ah'` ⇒ `win_margin 0.0` with the same winner in both input orders (RED: 18.7 / order flip); (f) no `value_badge` when the value dim is missing (RED: `fair_price`). | Land the order-symmetric tie-break BEFORE flipping `ENABLE_SPEC_FIELD_NORM`. The #101 product call (win_margin 0.0 semantics) stays Ahmed's — options in §1.4. |
| **W4-7 fact-check honesty** | PO-FACTCHECK-CONFIDENCE-01, -02 (+ activation of -03/-04 = #109/#106) | `scoring_service.py:1958-1976`, `response_builder.py:1055` | inside `ENABLE_CONFIDENCE_FACTCHECK_WIRING` (#109) | `_score_reliability(all-unverified)` is None (RED: 0.3); builder legs == persisted legs for the same inputs (RED: 'weak' vs 'acceptable'). | Activation order per `scoring_service.py:982-989`: #107 → cache roll → #106 → #109. |
| **W4-8 category truth** | PO-CATEGORIES-I18N-01, -03 | `extraction_service.py:1096-1097`, `app/data/content_blocklist.json:53` | `ENABLE_CATEGORY_TOKEN_FIX` (default OFF) for the synonym map; blocklist is data (unflagged) | `classify_category_from_text('Panadol Extra 24 tablets vs Adol 500 tablets') != 'electronics'` (RED); `check_query_intent('عطر أفيون ايف سان لوران').allowed` (RED: blocked, `illegal_drugs`). | Arabic multi-word forms mirror the EN tightening in `9f6e498`. |
| **W4-9 error envelope, not `str(e)`** | PO-RECORDED-MEASURED-05 | `scs.py:3659/4429`, `text_routes.py:146` | none (only stricter) | POST `/text/compare` with the service returning `{'success': False, 'error': 'relation "comparisons" does not exist'}` ⇒ body never contains the string; `code: 'INTERNAL_ERROR'` + `request_id` present (RED). | The other session's A11 owns the CLIENT copy; this is the backend half. |
| **W4-10 tradeoffs dedup parity** | CR-DELTA-CORRECTNESS-07 (LIVE M21-W3 regression) | `scoring_service.py:1554-1557` (`compute_scores` internals) | none (regression fix; pin by test) | a pair whose names repeat the brand ⇒ `tradeoffs` non-empty and `dimension_winners` keys == `winner_evidence` spelling (RED: `[]`). | Route `:1554` through `text_sanitize.dedup_brand_name`. |
| **W4-11 specs prompt fence** | PO-PROMPTS-11 (status) | `extraction_service.py:614-616/827/832` — no code; activation | `ENABLE_SPECS_NO_FABRICATION` | prod-shape assertion: the SHIPPED prefix must not contain the training-data licence once the flag is on (RED today by design). | LAST in the activation order, after Serper restoration + D1. |

### W5 — activation runbook (env flips; one canary compare per step, never batch across steps; OpenAI credits required for any live canary)
0. **Now, no code:** `ENABLE_BRIGHTDATA_BUDGET_GATE=true` on `web` + `price-warmer` (or unset `ENABLE_BRIGHTDATA_FALLBACK` until OpenAI is back). Re-take the free-credit baseline post-M21 (CR-DELTA-CORRECTNESS-09).
1. After W0 merges: `ENABLE_OFFLOOP_DNS_RESOLVE` → `ENABLE_UPSTASH_BOUNDED_TRANSPORT` → `ENABLE_SUPABASE_CLIENT_REUSE` (+ the postgrest timeout) → `ENABLE_PRICE_PARSE_OFFLOAD`, each its own window, watching `/health` p95 and the stage timings.
2. Then the M18 offloads: `ENABLE_ASYNC_REDIS_OFFLOAD`, `ENABLE_SYNC_DB_OFFLOAD` (now with bounded transports beneath them), then `ENABLE_SERPER_BREAKER`, `ENABLE_FULL_STREAM_DEADLINE`, `ENABLE_PREVERDICT_DISCONNECT_ABORT`.
3. Only then #114 (limiter key) WITH `key_style="endpoint"`, and only after Railway's hop count is live-verified; then a second worker.
4. `ENABLE_PAID_ROUTE_METERING` (W2) after `#128` for the anon half.
5. Product: W4-5 → W4-1 → `ENABLE_SHOPPING_STRICT_CURRENCY` (own window) → the other session's `ENABLE_IHERB_PAGE_CURRENCY` / `ENABLE_CONVERTED_PROVENANCE_STAMP` after their merge → W4-2/3/4 → #107 → #106 → #109 (W4-7) → W4-6 (+ `ENABLE_SPEC_FIELD_NORM` after the tie-break) → `ENABLE_SPECS_NO_FABRICATION` LAST. The existing ACTIVATION-ORDER runbook in CLAUDE.md (M0/M5/M6/M7 flags) is unchanged and interleaves by its own rule (`ENABLE_VISIBLE_TEXT_CURRENCY` and `ENABLE_NORMALIZE_WORDS_EMPTY_FIX` keep their own windows).
6. Mobile: merge the other session's `feature/m23-mobile-w1` (after ITS review + gates) → W3 → ONE `eas update --branch preview` from the merged main → on-device AR walkthrough → `eas build` for W3-7.

**Parked, pending the user's explicit GO and OpenAI credits:** any live load test (the load synthesis carries the k6/locust outline against an ISOLATED Railway environment with its own Upstash + Supabase, staged 1/3/5/10/30, abort on p95 / 5xx / partial rate / spend).

---

## 3. What could not be known

- **No live compare was possible** (OpenAI 429, Serper negative). Every product-output verdict is on recorded payloads, fixtures, the `_proof/` corpus and the prompt/rubric code paths; the ones that can only be closed live are listed in §1.4 with the exact re-score command (`python scripts/eval_runner.py …` per the brief). Prompt changes are unflagged by nature; their canary is a fixed 24-query panel scored by the deterministic judge, and every prompt edit re-warms the OpenAI prompt cache.
- **Load numbers are MODELLED except:** Upstash RTT 178 ms and Supabase ~285 ms from the prod container; the 11–12 s DNS stall; the 245–416 ms `create_client` cost (dev box; Linux modelled 20–60 ms); the 270–530 ms/page parse (dev box). No load test was run (standing rule).
- **The measured request series is polluted** (88.6 % probes). Until `search_logs` carries a `source`/`is_synthetic` column, every KPI reader (`analytics_service.get_daily_stats`, `get_popular_queries`, admin dashboards) reports probe traffic as product truth.
- **Live Supabase state diverges from the migrations in at least two places** (grants, `user_events` RLS); 033–036 are unapplied; the M13-29 RLS migration never landed. Only Ahmed/ops can apply 037.
- **On-device behaviour** (captive-portal boot, Arabic RTL walkthrough, the RECORD_AUDIO prompt) was not exercised on hardware.
- **The other session's two branches are unreviewed and unmerged**; three of this review's mobile P1s are fixed there. Their merge order (price cluster first, then mobile) and their own review/gate phases are prerequisites for the W3/W5 mobile steps.

---

## 4. Findings tables (per severity; deduped; verdict trail = finder → verifier → second vote → Fable)

_(Generated from `2026-09-06-full-review-verified.json` by `2026-09-06-full-review-state/build_verified.py` after the continuation workflows complete — see `findings-tables.md` next to this file.)_

**Dedupe clusters (reported once, listed here for the audit trail):** DNS-on-loop {LS-REQUEST-PATH-BLOCKING-01, CR-SECURITY-05}; per-request Supabase client {LS-REQUEST-PATH-BLOCKING-02, CR-PERFORMANCE-03}; Upstash unbounded {LS-REQUEST-PATH-BLOCKING-03, LS-CACHE-REDIS-01, LS-CONCURRENCY-LIMITS-04}; /text/prices {LS-RATELIMIT-KEY-02, CR-SECURITY-06}; metering doors {MB-RECONCILE-01, MB-RECONCILE-07, MB-NETWORK-CONTRACT-14, PO-VERDICT-TRUTH-01}; boot refresh {MB-RECONCILE-03, MB-STARTUP-BUNDLE-02}; from_history {MB-RECONCILE-05, MB-FLOWS-STATE-05}; Bright Data {LS-FAILURE-MODES-COST-01, CR-CLAUDE-CONFIG-02}; parse-on-loop {LS-CONCURRENCY-LIMITS-01, CR-PERFORMANCE-01}; admin key + Sentry {CR-SECURITY-03, CR-SECURITY-04}; shopping currency {PO-PRICE-TRUTH-01, -02, PO-RECORDED-MEASURED-02}.

## 5. Refuted and downgraded ledger

_(Generated with §4.)_ Fable downgrades on the brief's scale: LS-CACHE-REDIS-02 (no singleflight) P1→P2, LS-CONCURRENCY-LIMITS-02 (prefetch fan-out) P1→P2, LS-FAILURE-MODES-COST-05 (deploy-time refund) P1→P2. Fable re-classifications: LS-MEASURED-EVIDENCE-01 and PO-RECORDED-MEASURED-01 are EVIDENCE rows (measurements), CR-CLAUDE-CONFIG-11 is PROPOSE-ONLY, MB-RECONCILE-15 is a PRODUCT CALL, and MB-RECONCILE-03/-05, MB-STARTUP-BUNDLE-02, MB-FLOWS-STATE-05, MB-TESTS-TRUTH-04 are OTHER-SESSION (fixed on `feature/m23-mobile-w1`, unmerged).

## 6. Coverage statement + cross-session coordination

30 finder lanes (product 7, code 7, load 7, mobile 9) enumerated every route (61 decorated limits, 19 admin-gated routes), every screen, every prod flag (54 `web` vars three-way against code and docs), every prompt, every category (9), every migration (037 pending) and the recorded corpora; per-lane `not_covered` statements are in the partial JSONs. Not covered by design: live compares, load tests, on-device runs, prod writes.

**The concurrent session** (`feature/m22-price-truth-cluster` = #51–#57 fixes at `7dd04c1`, `feature/m23-mobile-w1` = 13 of 24 mobile fixes at `1e7c894`, both pushed, UNREVIEWED, UNMERGED) overlaps this review as follows — trust artifact paths, not M-numbers:
- **Fixed there, killed here after their merge:** MB-RECONCILE-03/MB-STARTUP-BUNDLE-02 (A3), MB-RECONCILE-05/MB-FLOWS-STATE-05 (A18), MB-TESTS-TRUTH-04 (A12); PO-PRICE-TRUTH-07 (#52, `334d300`), PO-PRICE-TRUTH-08 (#51, `e748cbb`).
- **In their REMAINING queue AND in this plan — pick ONE owner before either resumes:** A1/A6 (camera + URL metering = W2-1, assigned to this session by the user), B1 (lucide = W3-9), A9 (force-update = W3-8), B2 (expired-token 401 ~ W1-4's refresh half).
- **Their rules this session kept:** never `checkout`/`stash`/`reset` in the shared clone `sc-scraper-proof`; isolated worktree (`sc-review-s65`, branch `docs/session-65-full-review`); path-restricted commits; PRs only; `origin/main` re-read before integrating; their untracked scratch untouched; the CLAUDE.md flag-table rows for their 7 price flags stay DEFERRED to their merge.
