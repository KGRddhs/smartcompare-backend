**SESSION 66 flags (2026-09-23; every row default OFF, read PER CALL via `os.getenv`, NOTHING flipped; merge order #164 W3-9 → #165 W3-4 → #166 W3-3 → #168 W3-15 → #170 W3-7 → #171 W3-6 → #169 W3-13 → W3-16 → W4-1 → …):**
- `ENABLE_SOCIAL_DEVICE_FINGERPRINT` (default OFF, W3-3, `app/api/auth_routes.py` social-login route, PR #166) — **Effect ON:** the `X-Device-Fingerprint` header a social sign-in (Google/Apple) carries is applied to the created/linked account exactly as the email register path applies it (the referral lifetime-cap and re-signup inheritance then cover social accounts too). A failed apply is SILENT and logged by exception TYPE only (never `str(exc)` — an auth-path SDK exception can carry a credential as its argument). **Flag OFF:** the route's insert and response are byte-identical (pinned). No client change: the header is already sent by phones on 97b5f15.
- `ENABLE_PASSWORD_RESET_DEEP_LINK` (default OFF, W3-6, `app/services/auth_service.py::password_reset_deep_link_enabled`, PR #171) — **Effect ON:** `request_password_reset` passes `{"redirect_to": PASSWORD_RESET_REDIRECT_URL or "qaren://reset-password"}` to `reset_password_email`, so the recovery email returns to the app, where the OTA'd client routes `reset-password` to `Auth > ResetPassword` and completes the reset through the new unauthenticated `POST /api/v1/auth/password-recovery` (5/min; access token = the credential; blacklist → upstream `get_user` → fail-closed AMR `recovery` gate → the same admin write `change_user_password` makes → token blacklisted; every failure a 400 with a top-level `code`, never 401). **Flag OFF:** today's exact one-positional `reset_password_email(email)` call (pinned). **HARD PRECONDITIONS:** (1) `qaren://reset-password` on the Supabase project's Redirect-URL allow-list (GoTrue silently falls back to the Site URL otherwise); (2) the W3-6 client OTA on phones; (3) read the first canary's `[auth] password recovery rejected: amr=` line — the AMR claim shape of a recovery session is unmeasured live. Optional override `PASSWORD_RESET_REDIRECT_URL`. A GoTrue 500/429 during `get_user` still reads as a dead link (same gap as W1-4's classifier; follow-up `PO-AUTH-RESET-01`).
- `ENABLE_CONSENT_PERSIST` (default OFF, W3-16, `app/services/consent_service.py::consent_persist_enabled`) — **Effect ON:** `register_user` and the new-account branch of `sign_in_with_social` add `terms_accepted_at` / `terms_version` / `age_attested_at` to the users insert, from the three optional request fields `terms_accepted` / `terms_version` / `age_attested` (`terms_version` recorded AS SENT — what the user saw — never coerced to the server's `TERMS_VERSION`). **Flag OFF:** the insert dicts are byte-identical (`{id,email,subscription_tier}` / `{id,email,auth_provider,subscription_tier}`, pinned). **HARD PRECONDITION:** migration 038 (`migrations/038_users_consent_capture.sql`, UNAPPLIED) applied first — without it the insert fails on an unknown column.
- `ENABLE_CONSENT_REQUIRED` (default OFF, W3-16, `consent_service.consent_required_enabled`, independent of PERSIST) — **Effect ON:** `/api/v1/auth/register` returns 400 `{success:false, code:TERMS_ACCEPTANCE_REQUIRED}` BEFORE any Supabase account is created, and `/social-login` returns the same 400 for a NEW account only (no users row yet; existing accounts are never gated), whenever the request lacks `terms_accepted=true` + `age_attested=true` + a non-empty `terms_version`. **PRECONDITIONS, in order:** (a) the W3-16 client OTA on phones (97b5f15 sends no consent, so every registration from it would be refused); (b) `scripts/bundle_d_prod_smoke.py` probes 5, 10 and 11 post bare `/register` and `/social-login` bodies (`:297-304`, `:414-445`) and turn red — add the three consent fields or announce them expected-red first; (c) flip PERSIST first (REQUIRED without PERSIST rejects but records nothing). **Canary:** the count of `TERMS_ACCEPTANCE_REQUIRED` 400s on the two routes; any non-zero rate means phones without the OTA. **Terms-version bump** (after the legal redraft): EIGHT anchors move together — `app/api/legal_routes.py:30` and `:47`, `app/legal/terms_of_service.md:5`, `app/legal/privacy_policy.md:5`, `landing/terms.html:184`, `landing/privacy.html:195`, `app/services/consent_service.py:31`, `SmartCompareApp/src/services/consent.ts:12` (B12 pins three of them; follow-up `PO-CONSENT-01` widens it).
- `ENABLE_SHOPPING_CURRENCY_TRUTH` (default OFF, W4-1, `app/services/price_service.py::shopping_currency_truth_enabled`, both doors: `extract_price_from_shopping` and the M13-10 stash `_seed_shortcircuit_candidates`) — **Effect ON:** (1) the Serper-shopping rung resolves the GCC display token / ISO residue `detect_currency` cannot see BEFORE the strict check and the parse ("22.500 BD" → BHD → 22.5, not 22,500); (2) a known non-target ISO code the effective rate table cannot convert ("TRY 1.299,00" with `ENABLE_EXTENDED_FALLBACK_RATES` off) PENDS instead of shipping the raw amount stamped with the ask currency; (3) `local_bhd` requires HOST evidence: on every ask a missing link, a listing link or any `google.com` host relabels to `converted_usd`; on a BHD ask only, so does a global-tier host, an other-country token in any subdomain label or any path segment, a bahrain-tier registry row that declares `locale_paths` on its bare host, or an off-registry host with no BH marker — amount/url/retailer unchanged. **Flag OFF:** byte-identical on both doors (pinned; the corpus byte-identity harness is blind to this tier by construction). **Composition:** runs BEFORE `ENABLE_SHOPPING_STRICT_CURRENCY`'s pend, deliberately. **Stated limits (pinned):** compound strings ("From 22.500 BD"), Latin Omani "RO", bare numerals, Google ccTLDs (`google.com.bh`, `google.com.sa`), out-of-vocabulary country tokens (`/pk/`). **Canary:** the `[SHOPPING_CURRENCY_TRUTH] relabel … reason=` and `pend unconvertible` INFO lines; watch the genuine-vs-Tier-3 share. Product call for Ahmed: whether the client shows a "(converted from USD)" caption on relabelled rows.
- (no flag) W3-13 — the `channel-freshness` CI job (`.github/workflows/ci.yml`) reads the `preview` EAS channel on every PR and fails when it is more than one first-parent commit behind `origin/main`; it SKIPS green until the `EXPO_TOKEN` repository secret exists and is NON-BLOCKING until 2026-10-07 (the FLIP PRECONDITION is in the job comment). The canary runbook now publishes to `preview`, never `--branch production` (zero devices), and carries the OTA ledger (section 9).
- (no flag, needs `eas build`) W3-7 — `RECORD_AUDIO` blocked on Android and no `NSMicrophoneUsageDescription` on iOS; `ios.privacyManifests.NSPrivacyCollectedDataTypes` declares the 12 collected types traced in `docs/privacy-data-inventory.md` (the source for the App Store nutrition labels); `react-native-gesture-handler` is an orphan pending `npm uninstall` (d2 goes red when it is removed until the `PENDING_REMOVAL` entry is deleted).
