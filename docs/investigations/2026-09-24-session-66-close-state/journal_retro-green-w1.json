{
 "green:R-METER": {
  "unit": "R-METER / W2-1 paid-route metering retro GREEN (W2-1b camera unsuccessful result, W2-1c /url/compare failed-verdict leak, W2-1d anon refunds + /url/compare anon-gap record). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-meter, base origin/main 1c6f6796.",
  "files_changed": [
   "C:/Users/SynAckITPC/Documents/AI/sc-r-meter/app/api/image_routes.py (+129/-0: ENABLE_CAMERA_FAILURE_ENVELOPE reader, CAMERA_UNSUCCESSFUL_CONSTANT_ERROR, _camera_failure_envelope, unsuccessful-result branch, anon refund helper + 7 call sites)",
   "C:/Users/SynAckITPC/Documents/AI/sc-r-meter/app/api/url_routes.py (+13/-5: non-success exit routed through text_routes._surface_comparison_failure)",
   "C:/Users/SynAckITPC/Documents/AI/sc-r-meter/app/services/url_extraction_service.py (+19: `if \"error\" in comparison` -> success False / LLM_UNAVAILABLE / LLM_UNAVAILABLE_FRIENDLY_MESSAGE)",
   "C:/Users/SynAckITPC/Documents/AI/sc-r-meter/tests/test_paid_route_metering.py (red-phase rewrite only, ruling 4; untouched in green)",
   "C:/Users/SynAckITPC/Documents/AI/sc-r-meter/tests/test_retro_w2_1.py (untracked; red file + 12 green-phase pin functions appended = 28 new nodes)"
  ],
  "all_unit_tests_pass": true,
  "test_evidence": "Pinned venv, `-q -p no:cacheprovider -p no:randomly --timeout=120 -m \"not (live_unit or live_db or integration)\"`: tests/test_retro_w2_1.py + tests/test_paid_route_metering.py gave 71 passed with the red file as written (all 27 former REDs now green, all 23 red-phase pins still green). After appending the green pins: 99 passed, 0 failed in 191.6 s. The file's autouse fixture clears ENABLE_PAID_ROUTE_METERING, ENABLE_CAMERA_FAILURE_ENVELOPE and ENABLE_ANON_USAGE_GATE before each node, and each node sets its own flag combination. The four camera flag states (meter x envelope) are covered by parametrized nodes; the ambient-environment run in each flag state was NOT separately executed. The zero-network guard fired 0 times.",
  "lint_clean": true,
  "mutation_checks": [
   "Method: scratchpad/rmeter_green/mutate.py. For each mutation it takes a byte snapshot, str-replaces an anchor that must occur exactly once (CRLF-aware), runs the named nodes, restores from the snapshot and asserts sha256. It never uses git. Results are in scratchpad/rmeter_green/mutations.json.",
   "KILLED restore_str_e_unflagged_scrub: the str(e) scrub is removed and the sk-proj probe reaches the body. The any-flag-state probe pin and the flags-OFF body pin both go red. This is the ruling-1 mutation.",
   "KILLED scrub_applies_to_coded_results_too",
   "KILLED drop_unsuccessful_authed_refund: usage_refund.image.comparison_unsuccessful is removed.",
   "KILLED metering_half_never_fires",
   "KILLED metering_half_ignores_flag: this proves the flags-OFF body and accounting pins are load-bearing.",
   "KILLED envelope_missing_in_meter_branch",
   "KILLED envelope_missing_in_meter_off_branch",
   "NOT CONFIRMED, run interrupted: the matrix took about 5 min per mutation on the loaded box, and I stopped it when final output was demanded. These 15 have no recorded verdict yet: envelope_ignores_flag, envelope_forwards_generic_error, flag_reader_no_strip_lower, anon_refund_drop_one_product_exit, anon_refund_drop_vision_exception_exit, anon_refund_drop_unsuccessful_exit, anon_refund_ignores_metering_flag, anon_refund_latch_removed_double_call, reviewer_M1_delete_moderation_exception_refund, reviewer_M3_disable_url_exception_refund, url_drop_error_check, url_forward_str_e, url_error_check_truthy_not_presence, url_route_blanket_400_restored, url_route_drop_failure_refund. Some may have finished and been written to mutations.json before the stop; re-run with `python mutate.py <names>`.",
   "Restore verified after the stop: the three app files were copied back from green_snap, and their sha256 matches the green snapshot (image_routes b2aed119..., url_routes 1dddb57a..., url_extraction_service 7b003ffd...).",
   "M2 (the refund latch) has no pin, by ruling 5. M4 (/text/parse mock) is out of scope."
  ],
  "comm_gate": "NOT RUN, and this is a gate gap. I built the file set by grepping tests/ for image_routes|url_routes|url_extraction_service|/image/identify|/url/compare|/api/v1/url/|compare_from_urls. That gives 17 test files plus the new tests/test_retro_w2_1.py (conftest and _env_safety dropped), recorded in scratchpad/rmeter_green/grep_set.txt. The detached base worktree at 1c6f6796 was created at scratchpad/rmeter_green/base_wt with .env copied in. The base and HEAD runs were queued to run sequentially after the mutation matrix (ruling 6 load-flake note), but the matrix did not finish in time. For reference, the red phase's comm gate over 16 of these files gave base 3 failed, HEAD 3 failed, the same three #89 TestIdentifyProductsMocked MagicMock nodes, and comm -13 EMPTY. The green HEAD has not been re-gated. The orchestrator must run base vs HEAD over grep_set.txt and then remove base_wt with `git worktree remove` (it has no node_modules junction).",
  "flag_identity": "ENABLE_CAMERA_FAILURE_ENVELOPE (new, default OFF, read per call with strip/lower, reader pinned over 11 rows). With it OFF the camera body for an unsuccessful result is today's body (result + camera metadata + action 'comparison'). Pinned by test_pin_camera_flags_off_unsuccessful_result_body_and_effects_unchanged (TIMEOUT, exact dict equality, history ['camera'], log [True], no metering) and by test_pin_camera_meter_on_envelope_off_unsuccessful_body_unchanged. ENABLE_PAID_ROUTE_METERING (existing), new code under it: the refund, the lifetime/history skip and the success=False log on the unsuccessful exit, plus the anon refunds on the six exits and the unsuccessful exit. With it OFF, test_pin_camera_anon_exits_do_not_refund_with_metering_off (six nodes) and the flags-OFF pins hold, and the metering_half_ignores_flag mutation is KILLED. UNFLAGGED changes: (a) the camera route's code-less unsuccessful `error` becomes 'comparison unavailable' in every flag state (ruling 1). test_pin_camera_flags_off_generic_failure_body_identical_except_error asserts key-for-key and key-order equality with only that one value relaxed. test_pin_camera_coded_failure_error_value_untouched_flags_off (4 codes) proves coded results are untouched. test_camera_str_e_probe_never_reaches_body_in_any_flag_state (4 flag states) proves the probe never reaches the body. (b) compare_from_urls returns success False / LLM_UNAVAILABLE when the verdict carries an `error` key. test_pin_compare_from_urls_success_is_byte_identical (full dict + key order), the sparse-defaults pin and the <2-products shape pin prove every success path is identical. (c) /url/compare's non-success exit goes through _surface_comparison_failure. The <2-products exit takes the bare-string 400 arm, which is identical by construction; test_pin_url_compare_extraction_failure_envelope_unchanged pins the full envelope minus request_id as {success False, error 'Could not extract both products', code BAD_REQUEST}. Base-side execution of that new pin was not done.",
  "measurements_run": [
   "git status --short at start: only ` M tests/test_paid_route_metering.py` and `?? tests/test_retro_w2_1.py` (.qa-retro/ is gitignored)",
   "Read the RED report and the W2-1 adversary entry in full (9 defects, tests_that_prove_nothing, activation_gate)",
   "Read image_routes.py, url_routes.py, compare_from_urls, generate_comparison's except-branch, compare_from_text's failure returns (the code-less ones are :3542 parser-failure and :3927 str(e)), text_routes._surface_comparison_failure (LLM_UNAVAILABLE maps to 503), and usage_service.refund_anon_comparison_credit / check_anon_usage_allowed",
   "Unit files 71 passed, then 99 passed after the green pins",
   "ruff --select E9,F63,F7,F82 --no-cache: All checks passed; py_compile exit 0 on all 5 files",
   "git diff --stat: image_routes +129, url_routes 18 (+13/-5), url_extraction_service +19, test_paid_route_metering 20 (red-phase edit); CRLF preserved (no whole-file diffs)",
   "Mutation matrix: 7 run, 7 killed, run interrupted; files restored and sha-verified",
   "Module-reference comm set recomputed (17 files + the new file); base scratch worktree created; comm runs NOT executed"
  ],
  "pr_text": "## R-METER: W2-1 retro fixes (camera unsuccessful result, /url/compare verdict leak, anon refunds)\n\n### Defects (from the W2-1 adversary review, all reproduced red-first)\n1. **W2-1b, camera: a failed comparison is billed as a delivery.** `compare_from_text` usually *returns* its failures (`success: False` with TIMEOUT / INSUFFICIENT_DATA / LLM_UNAVAILABLE / CONTENT_UNAVAILABLE, or a code-less generic `error: str(e)`). `/image/identify` never read `success`. With `ENABLE_PAID_ROUTE_METERING` ON it kept the credit, bumped the lifetime counter, wrote a history row and logged success. With every flag OFF it also sent the generic branch's `str(e)` to the client. We measured `Incorrect API key provided: sk-proj-...` in the body (M13-26 class).\n2. **W2-1c, /url/compare: live leak on the unflagged path.** When the verdict LLM failed, `generate_comparison` returned `{\"winner_index\": 0, \"error\": str(e)}`. `compare_from_urls` served that as a 200 `success: true` with a made-up `winner_index: 0` and the raw exception text.\n3. **W2-1d, anon.** `refund_anon_comparison_credit` had no callers, so every anonymous camera non-delivery burned the anon credit. `/url/compare` has no anonymous gate at all.\n\n### Fix\n- **Camera, UNFLAGGED:** when an unsuccessful result has no `code`, its `error` value becomes the constant `\"comparison unavailable\"`. No other key changes in any flag state.\n- **Camera, under `ENABLE_PAID_ROUTE_METERING`:** an unsuccessful result is non-delivery exit 7. It refunds the authed credit (`usage_refund.image.comparison_unsuccessful`) and the anon credit, skips `record_lifetime_comparison` and the history write, and logs `log_search(success=False)`.\n- **Camera, new flag `ENABLE_CAMERA_FAILURE_ENVELOPE` (default OFF):** an unsuccessful result returns exit 6's existing `action: \"comparison_failed\"` envelope, which the client already falls back from. It carries the result's own `code` (INTERNAL_ERROR when there is none), the result's friendly `error`, or the constant for a code-less result, plus `layer` when present. Flag OFF keeps today's `action: \"comparison\"` body for the pre-OTA client (97b5f15).\n- **URL, UNFLAGGED:** `compare_from_urls` checks whether the verdict has an `error` key, which composes with W4-9 whichever way it lands. If present, it returns `{success: False, code: \"LLM_UNAVAILABLE\", error: LLM_UNAVAILABLE_FRIENDLY_MESSAGE}` and never `str(e)` or a winner. `extraction_service` is not edited.\n- **URL route:** every non-success now goes through `text_routes._surface_comparison_failure`, the same mapping `/text/compare` uses. LLM_UNAVAILABLE surfaces with `code` at the top level of the envelope. **The mapping sends LLM_UNAVAILABLE as HTTP 503, not 400** (W1-3 in text_routes); see Deviations. The <2-products exit takes the mapping's bare-string 400 arm, which gives the same body as before (pinned).\n- **Anon, under `ENABLE_PAID_ROUTE_METERING`:** `refund_anon_comparison_credit(fp, consumed_keys)` fires at most once per request (latched) on the six numbered camera exits plus the unsuccessful-result exit. It is armed only when the anon gate actually debited and metering is ON. Metering OFF keeps today's anon accounting.\n\n### Flag row (for CLAUDE.md)\n`ENABLE_CAMERA_FAILURE_ENVELOPE` (default OFF, read per call in `image_routes.camera_failure_envelope_enabled`). ON: a `success: False` camera comparison returns `{success: false, action: \"comparison_failed\", code, error, request_id, products, vision_cost, message[, layer]}` instead of `action: \"comparison\"`. OFF: today's body. Envelope ON with metering OFF changes the body only: history and the success log are still written (pinned). The no-bill half belongs to `ENABLE_PAID_ROUTE_METERING`, so flip both together.\n\n### Activation gate\n- The unflagged halves (the camera str(e) scrub, the URL failed-verdict fix and the URL wire mapping) go live on deploy.\n- Flip `ENABLE_CAMERA_FAILURE_ENVELOPE` together with `ENABLE_PAID_ROUTE_METERING`, after checking that the shipped client renders `comparison_failed` for a TIMEOUT. It already does for exit 6.\n- The adversary's other metering preconditions still stand: the size_or_count 500 window, the strict-auth coupling on /url/compare, and the 429 note on the admin probe.\n\n### Records (pinned, not built)\n- **/url/compare has NO anonymous gate.** With ENABLE_ANON_USAGE_GATE and metering both ON, a valid fingerprint and no auth get 200 with zero anon checks. Follow-up for issue #128; the pin is expected to flip then.\n- **The anon credit on /image/identify is debited BEFORE image validation** (an empty upload still gives anon_checks == [fp]). The five pre-validation 400s and the moderation-exception re-raise do not refund the anon credit. These are follow-ups for issue #128, pinned by `test_record_camera_anon_followups_not_refunded`.\n- **The refund latch (adversary M2) needs no pin** (ruling 5). No exit calls the refund twice on one request.\n\n### Honest limits\n- The comm gate over the green HEAD was NOT completed in this run. The red phase's gate was clean, but it covered the red tree, not these changes.\n- 7 of 22 mutations were confirmed killed. The remaining 15 were interrupted, including reviewer M1 and M3. The M1/M3 pins passed at red and still pass.\n- 503 vs 400 for the failed URL verdict is a wire change from the previous (broken) 200. The pre-OTA client's handling of a 503 LLM_UNAVAILABLE on the URL path has not been verified on a device. The text path already sends the same shape.\n- Load flake: `test_security_regression::test_dual_shape_product_a_b_hits_sanitizer` timed out once under load in the red phase. Run the comm gate sequentially.\n\n### Follow-ups (adversary minors not addressed here)\n- size_or_count numeric -> AttributeError -> 500 with the credit kept (defect 4).\n- Unflagged `Depends(get_optional_user)` on /url/compare and its strict-auth coupling (defect 5).\n- The shared 429 bucket on the admin prices probe (defect 6).\n- The /text/parse bare-dict mock, M4 (defect 7).\n- The authed refunds drop `consumed_keys` (defect 8). The new anon refund does pass them.\n- CLAUDE.md drift, plus the camera category and camera memory items (defect 9).\n- The /url/compare anon gate and the anon refunds on the validation 400s and moderation exception, for #128.\n- Add the new flag row to CLAUDE.md.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "deviations_from_spec": [
   "Ruling 3 names both 'the SAME mapping the text route uses (_surface_comparison_failure)' and 'code LLM_UNAVAILABLE -> HTTP 400'. The two conflict, because that mapping sends LLM_UNAVAILABLE as 503 (text_routes W1-3). I used the mapping unchanged, so the status is 503 with {code: LLM_UNAVAILABLE, error: constant} at the top level. test_url_compare_failed_verdict_wire_shape pins status 503. Changing it to 400 would mean forking or editing the text route's mapping, which is out of scope.",
   "The URL failed-verdict message reuses structured_comparison_service.LLM_UNAVAILABLE_FRIENDLY_MESSAGE (lazy import) instead of a new constant. It follows the no-scary-copy rule.",
   "The camera failure envelope reuses exit 6's action 'comparison_failed' shape, because the shipped client already handles it. The spec did not name the envelope shape.",
   "Envelope ON with metering OFF still writes history and the success log. This is the literal reading of the ruling (no-bill half under ENABLE_PAID_ROUTE_METERING only). It is pinned and documented.",
   "The comm gate and the full mutation matrix are incomplete; see comm_gate and mutation_checks."
  ],
  "git_status_final": " M app/api/image_routes.py\n M app/api/url_routes.py\n M app/services/url_extraction_service.py\n M tests/test_paid_route_metering.py\n?? tests/test_retro_w2_1.py\n(last measured before the final restore; the restore wrote green bytes whose sha matches the green snapshot, so this is unchanged. The scratch base worktree at scratchpad/rmeter_green/base_wt is STILL REGISTERED and must be removed with `git worktree remove`.)",
  "residual_risk": "Four risks remain. (1) There is no green-HEAD comm gate yet. The change reaches /url/compare's error surface, and other test files (test_url_compare_category, test_security_regression, test_m13_26_image_error_envelope) could assert the old blanket-400 or old camera body; that is unverified. (2) 15 of 22 mutations are unconfirmed. (3) The 503 wire status for a failed URL verdict has not been checked on a device against the pre-OTA client. (4) The detached scratch worktree base_wt still exists and needs `git worktree remove` (no junction inside). Worktree app files are restored to green and sha-verified."
 },
 "green:R-BREAKER": {
  "unit": "R-BREAKER / W1-3 OpenAI preflight breaker (ENABLE_LLM_PREFLIGHT_BREAKER, PR #147): GREEN for W1-3a (BLOCKING), W1-3b, and the flag-OFF entry-read pin (M10). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-breaker, base 1c6f6796.",
  "files_changed": [
   "C:/Users/SynAckITPC/Documents/AI/sc-r-breaker/app/services/api_budget_service.py (GREEN change: +101/-36 across the unit incl. tests; source sha256 4b1824bf -> c26a60ba, CRLF 1311/1311 lines)",
   "C:/Users/SynAckITPC/Documents/AI/sc-r-breaker/tests/test_retro_w1_3.py (RED file from the red phase + 13 GREEN-phase pin nodes appended; CRLF 915-ish/all lines CRLF verified; sha256 c445e605)",
   "C:/Users/SynAckITPC/Documents/AI/sc-r-breaker/tests/test_openai_breaker.py (UNCHANGED in green; carries only the red phase's ruled MINOR-5 inversion, sha256 bb58e2d0 == red snapshot)"
  ],
  "all_unit_tests_pass": true,
  "test_evidence": "Pinned venv .venv-qaren (fastapi 0.141.1 / pytest 9.1.1 / openai 3.3.1), -q -p no:cacheprovider -p no:randomly --timeout=120 -m \"not (live_unit or live_db or integration)\".\n(1) Unit files, pre-pin: tests/test_retro_w1_3.py + tests/test_openai_breaker.py -> 58 passed (all 13 former REDs green: 3 wall-cancel tests, external task.cancel, cancelled half-open probe, 8 W1-3b wedge cases, and the inverted MINOR-5 test).\n(2) The new GREEN pins run alone: 11 passed, then the 2 added later -> 4/4 of the release-helper pins passed.\n(3) Final comm-gate HEAD run over the red's recorded 16-file set (.qa-retro/w1_3_commset_files.txt) plus tests/test_retro_w1_3.py, under the red's netguard plugin (scratchpad/rbreaker/netguard_plug.py): 375 passed, 0 failed (322 + 53 in the new file). Log: .qa-retro/w1_3_green_head_final.log.\nFlag states: every test sets or clears ENABLE_LLM_PREFLIGHT_BREAKER itself, and an autouse fixture delenv's it, so each file exercises both states per test: flag ON (the W1-3a/b and pin tests) and flag OFF (the test_pin_flag_off_* family, test_green_release_helper_is_a_no_op_with_the_flag_off, and the M10 entry pins). Ambient env state cannot leak in.",
  "lint_clean": true,
  "mutation_checks": [
   "All mutations were applied to CRLF byte snapshots (scratchpad/rbreaker_green/*.green|.base) by scripts mutate.py/mutate2.py, restored from the snapshot and sha256-verified after each (restored_sha_ok=true on every row; final api_budget_service sha c26a60ba == green snapshot; structured_comparison_service sha 2a93e607 == base). No git command was used to restore.",
   "MA revert W1-3a (record_failure on CancelledError): KILLED, 7 failed. 3 wall tests + external cancel + cancelled-probe + release_discards + closed-streak pin + inverted MINOR-5.",
   "MB literal W1-3a (no record AND no release on cancel): KILLED by test_cancelled_half_open_probe_releases_the_slot + test_green_release_discards_denied_checks... (this is the W1-3a x W1-3b wedge the red predicted).",
   "MC drop the whole W1-3b probe-outcome branch: KILLED, 16 failed (8 wedge cases + 2 non-SDK pins + 6 4xx pins).",
   "MD non-SDK probe exception records nothing: KILLED (wedge ValueError/KeyError + 2 non-SDK release pins).",
   "ME non-SDK probe exception recorded as success: KILLED by test_green_non_sdk_probe_exception_releases_the_slot_not_success[x2].",
   "MF 4xx probe releases instead of success-for-health: KILLED by test_green_4xx_probe_records_success_for_health[x6]. The red tests allow either option, so this pin encodes the choice made here.",
   "MG release is a DECR, not a reset: KILLED by test_green_release_discards_denied_checks_counted_while_probe_in_flight.",
   "MH probe flag always True (release also on a CLOSED-streak admission): KILLED by test_green_cancel_on_closed_streak_admission_makes_no_release_call.",
   "MI release helper's own flag gate removed: KILLED by test_green_release_helper_is_a_no_op_with_the_flag_off.",
   "MJ release ignores the blob state: it SURVIVED the first matrix; its only failure there was the known flake. A pin was added (test_green_release_writes_nothing_unless_the_blob_is_half_open[closed|open]) and a targeted re-run KILLED it, 2 failed.",
   "Reviewer M6 (_openai_failure_is_transient always True): KILLED, 18 failed, incl. test_pin_non_transient_4xx_never_trips_the_breaker.",
   "Reviewer M10 (preflight reads circuit:openai before the flag check, in structured_comparison_service): KILLED by both M10 pins (test_pin_flag_off_preflight_never_reads_the_openai_breaker, test_pin_flag_off_both_compare_entries_never_read_the_openai_breaker).",
   "Reviewer M14 (flag-OFF dispatch reads the breaker): KILLED, 40 failed, incl. every test_pin_flag_off_* node and test_flag_off_never_reads_or_records_and_always_dispatches.",
   "Noise disclosed: test_openai_breaker.py::test_flag_off_compare_never_short_circuits and ::test_flag_off_streaming_never_short_circuits failed intermittently in 6 mutation runs (MB, MD, ME, MJ, M6, M10). Every one of those mutations touched flag-ON code only, so those failures do not count toward any kill claimed above. This is the pre-existing unguarded-DNS flake the red recorded ('bd_search_web: 0'). They passed in every unmutated run: 58-pass, 362-pass and 375-pass.",
   "First-matrix harness defect, disclosed: passing multi-line anchors through Git-Bash argv mangled the \\n escapes. Nine mutations aborted on 'anchor count 0' and M6 produced a syntax error (rc 2). The source was restored and sha-verified each time. All ten were re-run correctly from a Python-defined spec (mutate2.py), and those are the results above."
  ],
  "comm_gate": "BASE: reused the red phase's recorded base run, 16 files at origin/main (41ec7d5f, backend byte-identical to 1c6f6796 per the red's empty `git diff --stat 1c6f6796 41ec7d5f -- app tests migrations requirements*.txt pyproject.toml`): 322 passed / 0 FAILED (.qa-retro/w1_3_base_failed.txt is empty). No new base worktree was created in this phase. The scratch worktrees in `git worktree list` (rauth_green, rmain_green_base, rmeter_green, w43-*) belong to other agents and were not touched.\nHEAD: the same 16 files plus tests/test_retro_w1_3.py, run under the netguard plugin: 375 passed / 0 failed. `comm -13 base head` over the sorted FAILED sets is EMPTY. An earlier HEAD run before the final pins: 362 passed / 0 failed, also empty.\nNetguard log (scratchpad/rbreaker_green/netattempts_final.tsv): 91 blocked getaddrinfo attempts, all from 6 existing tests in tests/test_openai_breaker.py (61 'neutralized.supabase.invalid', 30 google.serper.dev). ZERO attempts came from tests/test_retro_w1_3.py.",
  "flag_identity": "Flag: ENABLE_LLM_PREFLIGHT_BREAKER (default OFF, read per call by llm_preflight_breaker_enabled() via os.getenv).\n- The flag-OFF branch of guarded_llm_create is untouched: its first statement is still `if not llm_preflight_breaker_enabled(): return await client.chat.completions.create(**kwargs)`. Every changed statement sits after that return: the admission 3-tuple, the CancelledError arm, and the `elif probe` outcome branch. So flag OFF, none of the new code is reachable from the dispatch path.\n- The new helper openai_release_half_open_probe() carries its own per-call flag read (pinned: test_green_release_helper_is_a_no_op_with_the_flag_off makes 0 Redis ops on a half_open blob; mutation MI kills it).\n- _openai_server_answered is pure classification with no I/O, and it is only called inside the flag-ON branch.\n- Pins that prove flag-OFF identity (M14 kills 40 of them):\n  - test_pin_flag_off_wall_cancelled_dispatches_touch_no_breaker: 0 ops, 0 breaker calls, empty store, cancellation propagates.\n  - test_pin_flag_off_any_exception_on_a_half_open_blob_touches_nothing[14 shapes]: the same exception object is re-raised, 0 ops, the store is byte-equal, and the next call dispatches.\n  - test_pin_flag_off_tier2_wall_records_nothing.\n  - test_flag_off_never_reads_or_records_and_always_dispatches.\n- M10 entry-read pins (M10 kills them): with the flag OFF, _llm_preflight_short_circuits and both real compare entries read circuit:openai 0 times; a flag-ON control proves the counter can see the read.\n- No unflagged behaviour was added. structured_comparison_service.py is byte-unchanged (sha 2a93e607).",
  "measurements_run": [
   "Unit files at HEAD: 58 passed (the 13 former REDs are now green).",
   "GREEN pins: 11 passed, then 4/4 release-helper pins after the MJ pin was added.",
   "Comm gate HEAD under netguard, final: 375 passed / 0 failed; comm -13 against the recorded base (0 failed) is empty.",
   "Netguard attempt census at HEAD: 91 blocked lookups (61 supabase.invalid + 30 google.serper.dev), all from 6 pre-existing test_openai_breaker.py compare tests; 0 from the new file. The red measured 36/61, so the Serper count varies with ordering and state.",
   "Mutation matrix of 14 mutations: 10 implementation mutations + reviewer M6, M10, M14 + MJ re-run after its pin. All killed, all restores sha-verified.",
   "ruff --select E9,F63,F7,F82 --no-cache (plus F401,F841 on the new test file): clean. py_compile clean on all 3 files.",
   "CRLF check: api_budget_service.py has 1311/1311 CRLF lines; tests/test_retro_w1_3.py is all CRLF.",
   "Note: the box ran very slowly throughout; single pytest invocations took 8-13 minutes."
  ],
  "pr_text": "## R-BREAKER: W1-3 OpenAI preflight breaker, cancellation and half-open probe fixes (flag-internal, default OFF)\n\n### Defects (retroactive adversary review of PR #147)\n- **W1-3a (BLOCKING).** `guarded_llm_create` recorded a breaker failure on every bare `asyncio.CancelledError`. One compare's outer wall cancels every in-flight guarded dispatch it covers: the Tier-2 4 s wall, the stream hard cap, the per-stage `wait_for`s, a client disconnect or a shutdown. So a single compare could reach `CB_FAILURE_THRESHOLD` on a healthy OpenAI and refuse every compare from every user for 600 s. Reproduced through the real `tier2_fill_non_negotiables`: blob `{'state':'open','failure_count':4}` after one compare.\n- **W1-3b.** When an admitted half-open probe ended in a non-transient outcome, nothing was recorded. That covers a 4xx other than 429 (context-length or content-policy 400, 401/403/404/409/422) and any non-SDK exception. The single probe INCR was spent and the blob stayed `half_open`, so every later dispatch was denied (INCR >= 2) until the 3600 s blob TTL expired. Meanwhile the read-only preflight admitted every compare, and each one paid the full scrape fan-out before failing at the LLM.\n- **Interaction, measured by the red phase.** A literal W1-3a fix (just drop the record) would turn a cancelled half-open probe into exactly the W1-3b wedge.\n\n### Fix (`app/services/api_budget_service.py`, all inside `ENABLE_LLM_PREFLIGHT_BREAKER`)\n- `_openai_dispatch_admission()` now returns `(admitted, outcome_matters, probe)`. `probe` is True only when admission went through `is_circuit_closed`'s recovery path.\n- CancelledError arm: records **nothing**. If this dispatch held the probe, it releases the slot, then re-raises unchanged.\n- Non-transient exception on a probe:\n  - A 4xx other than 429 (`_openai_server_answered`) records **success-for-health**, because OpenAI answered and the breaker closes.\n  - Anything else **releases the slot**.\n- Transient outcomes are unchanged and still record one failure each: 429, connection error, `APITimeoutError`, 5xx, and a `TimeoutError` raised inside the call. A 429 probe still re-opens the breaker for a fresh window.\n- New `openai_release_half_open_probe()`. It has its own per-call flag read, and it acts only on a blob that is still `half_open`. It **resets** the per-trip probe counter to `\"0\"` rather than decrementing it, which also discards the INCRs of checks denied while the probe was in flight. A DECR would leave the counter >= 1 and re-wedge the breaker. Cost: one GET + one SET, only on that rare path.\n- `structured_comparison_service.py` is unchanged.\n\n### Tests\n- The red file `tests/test_retro_w1_3.py` goes from 12 RED to green. The inverted MINOR-5 test in `tests/test_openai_breaker.py` is green.\n- 13 GREEN pins were added. They pin which total outcome each probe shape records:\n  - non-SDK exception = release (not success);\n  - 4xx = success-for-health;\n  - reset rather than DECR;\n  - no release call on a CLOSED-streak admission;\n  - the release writes nothing unless the blob is half_open;\n  - the helper is a no-op with the flag OFF.\n- Mutation matrix: all 14 mutations killed, including the reviewer's M6, M10 and M14. The pins added for M6 and M10 are the red phase's `test_pin_non_transient_4xx_never_trips_the_breaker` and the two key-filtered flag-OFF entry-read pins.\n- Comm gate: 16-file module-reference set plus the new file, run at HEAD under a netguard plugin: 375 passed / 0 failed. `comm -13` against the base (0 failed) is empty.\n\n### Flag\n| Flag | Default | Change |\n|---|---|---|\n| `ENABLE_LLM_PREFLIGHT_BREAKER` | OFF (per call) | No new flag; flag-OFF path byte-identical (early return untouched; pinned, M14 kills 40 nodes) |\n\n### Activation gate (unchanged order; this PR satisfies items 1-3 of the reviewer's gate)\n1. W1-3a: one compare's wall cancelling 3+ dispatches cannot open the breaker. Pinned.\n2. W1-3b: a non-transient or cancelled half-open probe cannot wedge deny-all behind an admitting preflight. Pinned.\n3. W1-3d: the 4xx-never-trips pin and the no-read-with-flag-OFF entry pin. Both in the red file.\n4. **Still owed before any flip:** a measured count (staging `stage_timings` or a flag-ON shadow log) of guarded dispatches per compare under current Bright Data latency.\n5. **Still owed before any flip:** `ENABLE_BRIGHTDATA_BUDGET_GATE=true` on `web` and `price-warmer`. This flag is not a spend control.\n\n### Honest limits\n- **The breaker now does not see a slow-but-alive OpenAI stall shorter than the SDK's own deadline.** The client timeout is `httpx.Timeout(120, connect=30)` with `OPENAI_MAX_RETRIES` retries. The compare walls (4 s Tier-2, 30 s hard cap) cancel long before that, and a cancellation no longer counts. Only 429, 5xx, connection errors and full SDK timeouts trip the breaker. The reviewer's optional in-guard `deadline` kwarg (`asyncio.timeout` inside the guard, recording only its own TimeoutError) would restore stall detection. Not done here.\n- **4xx on a probe = success-for-health.** A dead key (401) on the probe closes the breaker, and compares then fail at the LLM exactly as with the flag OFF. The breaker is a transport-health signal, not a key-validity check.\n- A probe whose process is SIGKILLed or OOM-killed (no CancelledError) still orphans the slot until the 3600 s blob TTL. Out of scope, see follow-ups.\n- The release path adds one GET + one SET, flag ON only, and only on a half-open probe with a non-verdict outcome.\n\n### Follow-ups (named, not addressed)\n- **W1-3c:** cross-worker streak decay. A success seen by another worker or process never resets the shared `failure_count`, so 3 non-consecutive failures still trip the breaker. Reproduced by the reviewer as P3.\n- Per-compare contextvar dedupe (at most one recorded failure per compare).\n- Stale-probe window in `is_circuit_closed` (re-admit a probe after about 2x the request timeout). This closes the SIGKILL/OOM orphaned-probe case.\n- Optional: the preflight denies while a half-open probe is outstanding (plain GET of the probe counter).\n- In-guard `deadline` kwarg to restore genuine-stall detection (see limits).\n- **PO-RECORDED-MEASURED-04c:** the existing compare tests in `tests/test_openai_breaker.py` are not zero-network. Measured: 36 google.serper.dev and 61 neutralized.supabase.invalid getaddrinfo attempts from 6 existing tests (red phase); 30/61 again in this phase's HEAD run. Their flag-OFF `bd_search_web > 0` assertions flake with DNS and ordering state. They flaked in 6 of this phase's mutation runs, always on mutations that cannot reach the flag-OFF path. Fix: in that file's autouse fixture, reset `serper_service.SERPER_API_KEY`, stub `get_admin_supabase_client`, and add a socket guard.\n- Reviewer minor: `scripts/verify_flag_byte_identity.py`'s corpus SHA is vacuous for this unit, because it never reaches `guarded_llm_create`. Flag-OFF identity rests on the pins above.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "deviations_from_spec": [
   "The reviewer's proposed W1-3b fix records openai_record_failure (re-open) for non-transient non-4xx exceptions. Per the binding Fable ruling, a non-SDK exception (and a cancelled probe) RELEASES the slot instead. The ruling wins.",
   "For 4xx-other-than-429 on a probe, the ruling allows success-for-health OR release. This implementation chose success-for-health and pinned the choice (test_green_4xx_probe_records_success_for_health; mutation MF kills it).",
   "Release was implemented as a RESET of the per-trip probe counter to '0', not a DECR. A DECR would leave the INCRs of denied concurrent checks and re-wedge the breaker. Pinned by test_green_release_discards_denied_checks_counted_while_probe_in_flight (MG kills it).",
   "Added 13 GREEN-phase pin nodes to tests/test_retro_w1_3.py, beyond the red's 40, so every branch of the fix is mutation-killable. MJ survived the first matrix and got its own pin.",
   "Did not re-run a base worktree. Reused the red phase's recorded base (0 failed on the identical 16-file set; the backend at origin/main is byte-identical to 1c6f6796 per the red's diff). With a 0-failure base, comm -13 reduces to the HEAD failure set, which is empty.",
   "Not done, per ruling (4): W1-3c, per-compare contextvar dedupe, stale-probe window, orphaned probes, and the in-guard deadline kwarg. Per ruling (3), the existing compare tests' network hygiene was not rewritten; it is recorded as PO-RECORDED-MEASURED-04c."
  ],
  "git_status_final": " M app/services/api_budget_service.py\n M tests/test_openai_breaker.py\n?? tests/test_retro_w1_3.py",
  "residual_risk": "Flag ON, the breaker can no longer detect a slow-but-alive OpenAI stall shorter than the SDK deadline (120 s total x retries), because the compare walls cancel first and cancellations are deliberately not recorded. Only 429, 5xx, connection errors and full SDK timeouts trip it. This is the price of W1-3a; an in-guard deadline is the follow-up.\n\nRecording success-for-health on a probe 4xx means a dead key (401) closes the breaker, so the result is the same as flag OFF, not worse.\n\nA SIGKILLed or OOM-killed probe still orphans the half-open slot until the 3600 s blob TTL (no stale-probe window).\n\nW1-3c cross-worker streak non-decay is unaddressed, so the breaker trips more easily than 'consecutive' implies.\n\nAll of this is flag-ON only. The flag is OFF in prod and the flag-OFF path is pinned byte-identical.\n\nThe existing test_openai_breaker.py flag-OFF compare tests remain network-dependent and flaky (PO-RECORDED-MEASURED-04c). A future CI red on test_flag_off_{compare,streaming}_never_short_circuits is most likely that flake, not this change.\n\nNothing was committed; the orchestrator commits."
 },
 "adversary:R-METER": {
  "unit": "R-METER / W2-1 retro GREEN (W2-1b camera unsuccessful result, W2-1c /url/compare failed verdict, W2-1d anon refunds + /url/compare anon-gap record). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-meter, base origin/main 1c6f6796.",
  "verdict": "SOUND",
  "defects": [
   {
    "severity": "minor",
    "location": "app/api/url_routes.py:124 (routes the non-success exit through text_routes._surface_comparison_failure) + tests/test_retro_w2_1.py::test_url_compare_failed_verdict_wire_shape",
    "claim": "Ruling 3 is internally contradictory, and the implementer resolved it toward the mapping. The ruling names 'the SAME mapping the text route uses (_surface_comparison_failure)' and also 'code LLM_UNAVAILABLE -> HTTP 400'. That mapping sends LLM_UNAVAILABLE as 503 (text_routes.py:281-296). The code ships 503 with top-level {code: LLM_UNAVAILABLE, error: LLM_UNAVAILABLE_FRIENDLY_MESSAGE}, and a pin asserts status 503 on both verbs. Reproduced: my own probe (GET, metering ON, the real generate_comparison except-branch) returned 503 with code LLM_UNAVAILABLE, one refund, no history and no lifetime bump. The deviation is disclosed in deviations_from_spec.",
    "why_it_matters": "The orchestrator must pick 503 or 400 before merge; the code and pin currently encode 503. Client impact at 503 is benign on the current source. HomeScreen's URL path calls parseApiError, which keeps an explicit non-timeout code on a 503 and does not auto-retry, so the user gets the generic error alert. The shipped 97b5f15 client was not checked on a device.",
    "reproduced": true
   },
   {
    "severity": "minor",
    "location": "app/api/image_routes.py:246 (_refund_anon_credit passes anon_consumed_keys) + tests/test_retro_w2_1.py Ledger.anon_refund",
    "claim": "The PR body says 'The new anon refund does pass them' (consumed_keys), but no test pins it. The Ledger's anon_refund recorder throws away its consumed_keys argument. Mutation anon_keys_dropped (refund_anon_comparison_credit(device_fp, None)) SURVIVED: 99/99 green. The behaviour itself is correct: my probe test_probe_anon_refund_passes_gate_consumed_keys confirmed the gate's {daily, monthly} keys reach the refund.",
    "why_it_matters": "This is the M18 usage-symmetry class (adversary defect 8). A future edit that drops the keys would silently return the credit to the wrong day's window near UTC midnight, and every test would stay green.",
    "reproduced": true
   },
   {
    "severity": "minor",
    "location": "app/api/image_routes.py:53-73 (_camera_failure_envelope)",
    "claim": "Three documented properties of the new ENABLE_CAMERA_FAILURE_ENVELOPE body are unpinned. Each mutation SURVIVED with 99/99 green: (a) dropping the `layer` passthrough ('plus layer when present' in the PR body and flag row); (b) dropping `request_id`; (c) changing the code-less fallback `INTERNAL_ERROR` to another string (the test only checks for a non-empty code).",
    "why_it_matters": "The flag row in the PR body promises this exact envelope shape for the client, and the client branches on `layer` for CONTENT_UNAVAILABLE. A regression would go unnoticed until the flag is flipped.",
    "reproduced": true
   },
   {
    "severity": "minor",
    "location": "app/services/url_extraction_service.py:621 (`if \"error\" in comparison`)",
    "claim": "The ruling requires the W2-1c check to key on the PRESENCE of the error key so that it composes with W4-9, and it does: my probe test_probe_url_presence_semantics passed for error=None, '' and a constant. But mutation url_truthy_not_presence (`if comparison.get(\"error\"):`) SURVIVED: 99/99 green. No test drives a falsy error value.",
    "why_it_matters": "If W4-9 ever stores an empty or None marker, a truthiness regression would ship a fabricated winner_index 0 as success:true again (the live leak this unit closes), and CI would stay green.",
    "reproduced": true
   },
   {
    "severity": "minor",
    "location": "app/api/image_routes.py:483-487 (envelope ON, metering OFF branch)",
    "claim": "This is the literal reading of the ruling, and the implementer disclosed and pinned it. With ENABLE_CAMERA_FAILURE_ENVELOPE ON and metering OFF, a failed comparison returns action 'comparison_failed' while still writing log_search(success=True) and a history attempt. The body says failure and the analytics say success.",
    "why_it_matters": "The activation note must keep the 'flip both together' instruction. Flipping the envelope alone would count failed camera comparisons as successes in search_logs.",
    "reproduced": true
   }
  ],
  "tests_that_prove_nothing": [
   "Mutation anon_keys_dropped SURVIVED: tests/test_retro_w2_1.py's Ledger.anon_refund throws away consumed_keys, so no test pins the PR's claim that the anon refund passes the gate's consumed_keys. The behaviour is correct by probe, but unpinned.",
   "Mutation envelope_drops_layer SURVIVED: test_camera_failure_envelope_flag_returns_failure_not_comparison[*-CONTENT_UNAVAILABLE] and test_pin_camera_envelope_shape never assert that `layer` is carried.",
   "Mutation envelope_drops_request_id SURVIVED: no envelope test asserts request_id.",
   "Mutation envelope_code_fallback_changed SURVIVED: test_camera_failure_envelope_flag_returns_failure_not_comparison[*-generic_str_e] asserts only that `code` is a non-empty str, not INTERNAL_ERROR.",
   "Mutation url_truthy_not_presence SURVIVED: no test feeds compare_from_urls a verdict whose `error` value is falsy, so the ruled presence semantics are unpinned.",
   "Mutations anon_latch_removed and reviewer_M2_authed_latch SURVIVED as expected. No exit calls either refund twice, so neither latch is observable; ruling 5 accepts this for M2, and the anon latch is the same class.",
   "Mutation envelope_forwards_generic_error SURVIVED as expected (benign). The unflagged scrub has already replaced a code-less `error`, so the envelope's `if code else CONSTANT` is redundant defence, not a gap.",
   "Mutation anon_armed_regardless_of_consumed SURVIVED (benign). The gate never returns allowed with consumed False; its fail-open path also reports consumed True."
  ],
  "worktree_left_byte_identical": true,
  "summary": "**SOUND, with five minor findings.** All four fixes behave correctly in the real code: the unflagged camera str(e) scrub, the camera no-bill half under ENABLE_PAID_ROUTE_METERING behind the new default-OFF ENABLE_CAMERA_FAILURE_ENVELOPE, the unflagged /url/compare failed-verdict fix with the shared mapping, and the anon refunds on the six exits plus the unsuccessful-result exit. Every load-bearing half has a pin that goes red when its fix is removed.\n\n**How I tested.** I never touched the worktree. Mutants ran in scratch copies at C:/Users/SYNACK~1/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/a47353ce-a0e3-4273-b85d-f21cfa01e031/scratchpad/rmeter_adv/w0..w3, restored from sha-verified snapshots. Driver: rmeter_adv/driver.py; results: rmeter_adv/results.jsonl. All runs used the pinned venv with -p no:randomly --timeout=120 and the not-(live) marker.\n\n**Measured results.**\n- Control: tests/test_retro_w2_1.py + tests/test_paid_route_metering.py gave 99 passed.\n- ruff E9,F63,F7,F82: clean.\n- Mutation matrix: 36 mutations, 27 killed, 9 survived (listed in tests_that_prove_nothing).\n  - Killed: the ruling-1 restore of str(e), the scrub applied to coded results, dropping the authed refund, logging success=True, writing history, metering ignoring the flag or never firing, the envelope ignoring the flag or missing in either branch, the envelope dropping products, the reader without strip/lower, all seven anon-refund drops, the anon refund ignoring metering, reviewer M1 and M3, dropping the URL error check, forwarding str(e) on the URL path, changing the URL code, restoring the blanket 400, and dropping the URL failure refund.\n  - This settles all 15 of the implementer's unconfirmed mutations.\n- Base-side run of the new test file in the existing scratch base_wt at 1c6f6796: 30 passed, 48 failed. Every node documented as passing today passed at base, including test_pin_url_compare_extraction_failure_envelope_unchanged, which the implementer never ran at base. All 48 base failures are red nodes or green pins that need the new code.\n- Real-function probes, 9/9 passed:\n  - The real StructuredComparisonService generic except-branch, carrying an sk-proj probe, is scrubbed to 'comparison unavailable' in all four flag states. With flags OFF the key order is unchanged and only the one value changes.\n  - The anon refund carries the gate's consumed_keys.\n  - The URL check keys on presence for None, '' and a constant.\n  - GET /url/compare with metering ON returns 503 LLM_UNAVAILABLE with one refund and no history.\n\n**Comm-gate sample**, run sequentially per ruling 6. Files: test_m13_26_image_error_envelope, test_url_compare_category, test_m13_03_paid_work_gating, test_camera_vision, test_b2_strict_optional_auth, test_security_regression, test_paid_route_metering.\n- Base: 3 failed. These are the known #89 TestIdentifyProductsMocked MagicMock nodes.\n- First HEAD run: pytest-timeout killed the run at test_security_regression::TestInputValidation::test_normal_length_query_accepted. The stack shows a /text/compare real-OpenAI-client hang under load in a file with no network guard; text_routes is not in this diff. Re-run alone at HEAD it passed.\n- HEAD re-run with that node deselected: 9 failed. Three are the #89 nodes. Six are TestFrontendSecurityGuards, which read SmartCompareApp/ sources that I deliberately left out of my scratch copy; they passed at base and the diff does not touch SmartCompareApp. That the six would pass with SmartCompareApp present is an inference, not a measurement.\n- Net branch-only-NEW on the sample: empty. The full 18-file grep_set comm gate is still not run.\n\n**Worktree state.** The five files still match the green sha256 (image_routes b2aed119, url_routes 1dddb57a, url_extraction_service 7b003ffd, test_paid_route_metering 9e7fd283, test_retro_w2_1 fb387146). git status and diff --stat are unchanged. I deleted the .env copies from my scratch dirs, and the test file I placed temporarily in base_wt is gone.\n\n**Orchestrator actions.**\n- Choose 503 or 400 for ruling 3. The code and its pin currently say 503.\n- Optionally add pins for consumed_keys, layer, request_id, INTERNAL_ERROR, and a falsy `error` value.\n- Remove the implementer's detached scratch worktree scratchpad/rmeter_green/base_wt (still registered, no junction) with `git worktree remove`. I reused it but did not create it."
 },
 "green:R-CLIENT": {
  "unit": "R-CLIENT / W1-4d. This is the green phase of the retro fix to the W1-4 client half (PR #140). Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-client, HEAD 1c6f6796, sources unchanged at start (api.ts bb95d083, authService.ts 3610c491). logout() now waits, up to a bound, for a refresh that is already in flight. It refreshes an expired JWT once. It then sends exactly one POST /auth/logout carrying the freshest pair it holds.",
  "files_changed": [
   "SmartCompareApp/src/services/api.ts",
   "SmartCompareApp/src/services/authService.ts",
   "SmartCompareApp/__tests__/authService.logoutRefreshFirst.w1-4d.test.ts",
   "SmartCompareApp/__tests__/authService.logoutRevoke.w1-4.test.ts",
   "SmartCompareApp/__tests__/authService.bootOptimistic.a3.test.ts"
  ],
  "all_unit_tests_pass": true,
  "test_evidence": "Unit files, run with node node_modules/jest/bin/jest.js --ci (jest 29.7.0):\n- logoutRefreshFirst.w1-4d: 26/26. That is the red's 24 plus 2 pins I added for the Fable rulings.\n- logoutRevoke.w1-4: 10/10. That is the red's 9 plus 1 pin I added for a failure inside the refresh preparation.\n- bootOptimistic.a3: 20/20, after the one edit ruling (2) allows.\n- Together: 56/56. A --detectOpenHandles run of the three files reports no open handles.\n- Before the fix, the red recorded 8 failed / 16 passed in the new file.\n\nComm gate:\n- Base run by the red at 1c6f6796 over the 67 test files that import api or authService: 0 failed (667 passed / 13 pending / 5 todo).\n- HEAD run over the same 67 files plus the new file: 68 suites (65 passed, 3 skipped by design), 712 tests, 694 passed / 0 failed / 13 pending / 5 todo.\n- comm -13 of the two FAILED sets is EMPTY.\n\nFull suite, run once at the end with --maxWorkers=25%:\n- 290 suites (286 passed, 1 failed, 3 pending), 2868 tests (2841 passed, 1 failed, 13 pending, 13 todo), 44/44 snapshots.\n- The red's base was 290 / 2865 / 44. The +3 tests are exactly the 3 pins I added.\n- The one failure is HistoryScreen.mobileJank.m21.test.tsx › 'MB-flows-08 — hero marquee tap … NOT in the loaded history page' (Unable to find testID history-hero-card-gone-1).\n- That file jest.mock()s both services/api and services/authService, so it never loads either changed source.\n- It fails deterministically, twice on its own at HEAD. It also fails with BASE api.ts and authService.ts restored in place (sha bb95d083 / 3610c491), after which the green bytes were restored and sha-verified (cb703d00 / d465d683).\n- It is therefore not a regression from this unit. The likely cause is the calendar date: the clock rolled to 2026-09-24 between the red's full run and this one, and HistoryScreen groups rows by date. That cause is not proven.\n\nOther checks:\n- tsc 5.9.3 --noEmit: exit 0, run twice.\n- eslint 9.39.4 on all 5 changed files: 0 errors, 21 warnings, all on pre-existing lines.",
  "lint_clean": true,
  "mutation_checks": [
   "Driver scratchpad/rclient_green/mutate.js. Each mutant was applied to a byte snapshot, the four files (w1-4d, w1-4, a3, api.refreshInterceptor) were run, then the file was restored from the green snapshot and sha256-verified: api.ts cb703d00…, authService.ts d465d683…. git was never used for restores. Every restore verified.",
   "M01 getInFlightRefresh always returns null: KILLED (4 failed)",
   "M02 unbounded await of the in-flight refresh (bound removed): KILLED (1, the fake-timer black-hole pin)",
   "M03 no epoch bump at logout start: KILLED (2: a3 'logout mid-flight is not undone' plus the storage-never-written pin)",
   "M04 refreshSession does not stash the rotated pair into the logout handoff: KILLED (2: R1 'sends the NEW pair' plus the storage pin)",
   "M05 isJwtExpired always false: KILLED (4)",
   "M06 exp read as milliseconds: KILLED (3)",
   "M07 malformed / no-exp token counted as expired: KILLED (7: the six expired-UNKNOWN pins plus one more)",
   "M08 no clearSession before the POST (the interceptor then stamps the stale stored Bearer): KILLED (2)",
   "M09 logout POST sent twice: KILLED (16)",
   "M10 two refreshes on the expired path: KILLED (3)",
   "M11 expired-path refresh attempted behind a hung in-flight refresh: KILLED (1, the fake-timer pin)",
   "M12 clearSession removed from finally: KILLED (2)",
   "M13 token and refresh token written to console.log: KILLED (4)",
   "M14 handed-off pair ignored: KILLED (2)",
   "M15 an error inside the preparation drops the POST: SURVIVED on the first run. Added pin 'W1-4d: a failure inside the refresh-first preparation never costs the POST' to logoutRevoke.w1-4, re-ran: KILLED (1 of 71)",
   "Reviewer-named W1-4 mutants re-run on the new code: R_M1 body reverted to {}: KILLED (16); R_M2 keystore read guard dropped: KILLED (1); R_M4 clear only when a token exists: KILLED (2); R_M5 send refresh_token:null: KILLED (18). The reviewer's M3 (log the token) is covered by M13.",
   "Red prove-nothing key mutants: K1 logout reads another SecureStore key: KILLED (14); K2 refreshSession reads another key: KILLED (23). The rewritten 'sends the SAME stored value' node now kills K2.",
   "S1 '/auth/logout' removed from the interceptor skip list: KILLED (2: api.refreshInterceptor.test.ts plus the w1-4d source pin)",
   "Result: 22/22 mutants killed. The single survivor was fixed with a pin and re-killed."
  ],
  "comm_gate": "Client-only unit, so the gate is the jest equivalent of the backend one. File set: .qa-retro/comm_gate_files.txt, the 67 test files that reference services/api or services/authService, plus the new w1-4d file at HEAD.\n\n- BASE (the red's run at 1c6f6796, before any W1-4d edit): FAILED set = NONE.\n- HEAD (scratchpad/rclient_green/comm_head_summary.txt): 68 suites, 712 tests, FAILED set = NONE.\n- comm -13 (branch-only-NEW) = EMPTY.\n\nNo detached scratch worktree was created: HEAD equals the base SHA, so the base run was done in place by the red. No backend gate was run, because no app/ file is in scope (R-AUTH owns the backend). ruff and py_compile do not apply: no .py file was touched.",
  "flag_identity": "UNFLAGGED, the same as the M13-W4 and M23 client precedent. React Native has no per-call env flag, and the change is OTA-gated: phones on 97b5f15 are unaffected until the next eas update. The reviewer classes the current behaviour as a defect with no legitimate reader: a stale or expired Bearer revokes nothing, and today the in-flight race sends 'Bearer AT1-expired'.\n\nUnflagged-additivity proof: on every path with no refresh in flight and a token that is not a known-expired JWT, the wire is byte-identical to base. Same single POST /api/v1/auth/logout, same body ({refresh_token: stored} or {}), same Authorization value, same count. These pins hold it:\n- 'pin: an UNEXPIRED JWT is sent as-is with no refresh'.\n- The six 'expired-UNKNOWN -> no refresh attempt, one logout POST as today' pins: opaque string, 2 segments, non-base64url, non-JSON, no exp, non-numeric exp.\n- 'pin: no access token stored -> no refresh, no logout POST'.\n- The whole pre-existing logoutRevoke.w1-4 suite, all green: body shape, {} when nothing is stored, {} when the keystore throws, no call without a token, local clear on 5xx/timeout, token never logged.\n\nTwo ordering-only differences on that identity path, stated honestly:\n- The session epoch is bumped at the start of logout instead of at the final clear.\n- clearSession now also runs just before the POST. It still runs in finally too.\nNeither changes a byte on the wire. The early clear means local state is gone before the POST resolves, not after.",
  "measurements_run": [
   "git status --short FIRST: only 'M SmartCompareApp/__tests__/authService.logoutRevoke.w1-4.test.ts' and '?? SmartCompareApp/__tests__/authService.logoutRefreshFirst.w1-4d.test.ts' (the red's two files); .qa-retro/ is gitignored",
   "Base byte snapshots (scratchpad/rclient_green/base): api.ts bb95d083 and authService.ts 3610c491, both equal to the red's recorded shas",
   "Unit files after green: 53/53, then 56/56 with the added pins; each file also run alone (24→26, 9→10, 20)",
   "Mutation matrix (22 mutants), described in mutation_checks",
   "tsc 5.9.3 --noEmit exit 0, twice; eslint 9.39.4 on the 5 changed files: 0 errors",
   "Comm gate at HEAD: 68 suites / 712 tests / 0 failed (comm -13 empty against the red's base FAILED=NONE)",
   "Full jest suite once, --maxWorkers=25%: 290 suites / 2868 tests / 44 snapshots; 1 failure outside scope",
   "HistoryScreen.mobileJank.m21 checked three ways: fails alone at HEAD, fails alone again, fails with BASE api.ts and authService.ts restored in place; green bytes restored and sha-verified cb703d00/d465d683",
   "--detectOpenHandles over the 3 unit files: clean. Jest printed 'A worker process has failed to exit gracefully' once in the multi-worker comm and full runs. It does not reproduce in-band, and the red's base logs do not contain it; the suite that causes it was not identified",
   "No git write of any kind, no scratch worktree, no network, no package install; node_modules junction untouched"
  ],
  "pr_text": "fix(mobile/auth): W1-4d — logout presents a fresh session to /auth/logout (retro R-CLIENT, OTA-gated)\n\nDEFECT (retro adversary on #140, reproduced)\nUpstream revocation is skipped silently whenever logout carries an expired or stale access token:\n- The server's get_current_user 401s the Bearer before logout_user runs.\n- /auth/logout is on the 401 interceptor's skip-refresh list.\n- logout() neither waited for a refresh already in flight nor refreshed an expired token.\nIn the in-flight race, the server finishes rotating RT1 to RT2 while the phone's P-A3 epoch guard discards RT2, so RT2 stays live upstream and nothing revokes it. Measured through the real api.ts and authService.ts: the wire Bearer was 'AT1-expired'.\n\nFIX (client only: SmartCompareApp/src/services/{api,authService}.ts)\n- api.ts: ONE new read-only export, getInFlightRefresh(): Promise<RefreshResult> | null. It never starts a refresh.\n- authService.logout():\n  1. Bumps the P-A3 session epoch FIRST. A refresh that lands after that neither writes storage nor fires onSessionRefreshed (today's guard). Its rotated pair is handed to the waiting logout IN MEMORY, through a handoff slot that refreshSession's existing epoch-guard branch fills only while a logout is preparing.\n  2. Waits for an in-flight refresh, raced against a 5 s bound (LOGOUT_REFRESH_WAIT_MS). A black-holed refresh cannot hold the tap, and the refresh request itself is never cut short (P-A3).\n  3. If the access token is a JWT whose `exp` has passed, starts ONE refresh through getOrStartRefresh(), under the same bound. The payload is decoded without verification and with no atob/Buffer. Malformed tokens and tokens without a numeric exp are expired-UNKNOWN and are sent as today. The refresh is skipped when the in-flight wait timed out, because it would coalesce onto the hung one.\n  4. Clears the local session, then sends exactly ONE POST with the freshest pair it holds. No retry loop. clearSession also stays in finally.\n  Why the clear comes before the POST: the request interceptor stamps the STORED token over an explicit Authorization header whenever one is stored, so clearing is what lets an in-memory pair reach the wire. Every preparation step swallows its own errors, so at worst the stored pair is sent, exactly as before.\n- A pre-logout refresh that returns 401 clears state through the existing dead-session path, and then no logout POST is sent (at most one; ruling 4).\n- The logout JSDoc no longer claims \"Supabase revokes a session by its REFRESH token\". That claim is false on the valid-token path (measured by the retro).\n\nTESTS\n- New suite: __tests__/authService.logoutRefreshFirst.w1-4d.test.ts, 26 tests. It runs the real api.ts and authService.ts with a stub adapter on the real axios instance, behind an autouse zero-network guard. It adds two pins:\n  - a refresh that never settles lets logout POST after the bound and clear state (fake timers);\n  - a refresh landing after the epoch bump never writes the rotated pair to storage, yet its pair is the one the POST carries.\n- __tests__/authService.logoutRevoke.w1-4.test.ts:\n  - the 'sends the SAME stored value' test now drives login() and refreshSession() instead of repeating a literal key;\n  - the SecureStore-throws spy is restored in finally;\n  - new pin: a failure inside the preparation never costs the POST.\n- __tests__/authService.bootOptimistic.a3.test.ts:528: the ONLY edit resolves the pending boot-refresh POST while logout is pending, which is what stops the test deadlocking. Its assertions are unchanged and green: storage ends cleared, onSessionRefreshed is NOT called, no resurrection.\n- Mutation matrix: 22/22 killed, including every reviewer-named mutant and the two storage-key mutants.\n- Comm gate: 67 api/authService test files plus the new file, 0 failed, branch-only-NEW empty.\n- Full suite: 290 suites / 2868 tests / 44 snapshots. The one failure is HistoryScreen.mobileJank.m21 MB-flows-08. That file mocks both changed modules, and it fails identically with base sources, so the failure is not caused by this change (likely date-dependent; see follow-ups).\n- tsc: clean. eslint: 0 errors.\n\nFLAG\nNone. The client change is unflagged, following the M13-W4 and M23 precedent, and it is OTA-gated: phones on 97b5f15 are unaffected until the next `eas update --branch preview`. On the no-refresh, unexpired or opaque-token path, the wire bytes are identical to before (pinned). The only difference there is ordering: local state is cleared before the POST instead of after.\n\nCLAUDE.md FLAG-ROW CORRECTION (ENABLE_LOGOUT_UPSTREAM_REVOCATION row, \"SESSION 65 wave W1 flag\" block). Apply at merge:\n- Replace \"INERT until the client sends it\" and \"the next `eas update` is the real activation lever, not the env var\" with:\n  \"ENABLE_LOGOUT_UPSTREAM_REVOCATION is NOT inert until the OTA once the backend calls admin.sign_out(access_token, \"local\") (group R-AUTH). On the valid-access-token path Supabase revokes by the JWT's session, and the refresh-token VALUE never reaches Supabase (retro adversary, measured on the pinned supabase-auth 2.31.0). The client half (PR #140 + W1-4d) matters only for the expired and in-flight paths.\"\n- Also correct \"on the expired path revocation happens via that rotation plus the sign_out on the NEW session\". That path cannot be reached through the route today: get_current_user 401s an expired Bearer before logout_user runs.\n\nBACKEND HALF\nThe backend lands in R-AUTH, not here: admin.sign_out(access_token, \"local\") regardless of the refresh token, the run_db offload of the upstream leg, and the expired-Bearer server path that also covers builds already on phones.\n\nACTIVATION GATE\nThe next `eas update --branch preview`. The server-side effect still needs R-AUTH plus ENABLE_LOGOUT_UPSTREAM_REVOCATION=true. After the OTA, check on-device with a test account: log in, idle past 1 h (or log out during a cold boot refresh), log out, then POST /api/v1/auth/refresh with the old refresh token. It must return 401 once R-AUTH is live.\n\nHONEST LIMITS\n- If the refresh really is black-holed (it takes longer than 5 s), logout sends the pair it has. That is an expired Bearer on the expired path, which the server still 401s. The rotated RT2 that lands later is discarded by the epoch guard and stays live upstream until R-AUTH's server-side expired-Bearer path exists.\n- Expiry is strict (\"exp has passed\"), with no leeway. A token that expires in transit can still 401.\n- A 401 on the pre-logout refresh means no logout POST is sent. Nothing is left to revoke on the phone, and the server already refused that token.\n- A transient in-flight failure followed by an expired token costs one more bounded refresh attempt.\n- In the multi-worker comm and full runs, jest printed 'A worker process has failed to exit gracefully' once each. --detectOpenHandles over the three auth suites is clean, and the source suite was not identified.\n\nFOLLOW-UPS\n- R-AUTH: admin.sign_out(access_token, \"local\") under the flag with a run_db offload (reviewer defects 2 and 3); an optional expired-Bearer server path for pre-OTA builds (defect 1's server alternative).\n- Consider a small exp leeway (for example 30 s) in isJwtExpired.\n- The header of the existing w1-4 test file still repeats the \"revokes by REFRESH token\" premise. It is prose only and was left untouched.\n- HistoryScreen.mobileJank.m21 MB-flows-08 fails on base sources as of 2026-09-24 (likely date-dependent). Worth its own check.\n- Unaddressed reviewer minors from the backend half (R-AUTH / W1-4b / W1-4c): the substring 401-vs-503 classifier, the orphaned SDK auto-refresh timer, 422 on non-JSON logout bodies, and the untested scrub rules M7/M9.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "deviations_from_spec": [
   "The handoff mechanism departs from Fable ruling (2)'s wording 'captures the RESULT directly from the awaited promise'. RefreshResult carries only the access token, and the ruled api.ts scope is ONE export. So the rotated pair travels through an in-memory slot in authService (logoutHandoff). refreshSession's existing epoch-guard branch fills it only while a logout is preparing, and logout reads it after the bounded await. The invariants of ruling (2) all hold and are pinned: the epoch is bumped before awaiting, the late refresh writes nothing to storage and fires no listener, and logout still gets the new pair. The alternative was widening RefreshResult with the refresh token for every consumer (the interceptor and runBootRefresh), which I judged to be wider credential exposure.",
   "logout() now also calls clearSession() immediately BEFORE the POST, and still in finally. It has to: the api.ts request interceptor overwrites an explicit Authorization header with the STORED token whenever one is stored, so without the early clear an in-memory fresher pair can never reach the wire (mutant M08 proves the dependency). Wire bytes on the unchanged path are identical; local state is simply cleared earlier.",
   "After the in-flight wait times out, the expired-path refresh is skipped: getOrStartRefresh() would coalesce onto the same hung promise. Pinned by the fake-timer test and mutant M11.",
   "__tests__/authService.bootOptimistic.a3.test.ts was edited as ruling (2) permits: the pending POST is resolved while logout is pending, and one existing assertion was moved after `await loggingOut` with its content unchanged. All original assertions stay.",
   "I added three pins beyond the red: the fake-timer black-hole pin required by ruling 1, a storage-never-written pin for ruling 2, and a preparation-failure pin in the w1-4 file to kill the surviving mutant M15.",
   "Expiry check is strict 'exp has passed', as specified, with no clock-skew leeway (listed as a follow-up).",
   "The logout JSDoc sentence 'Supabase revokes a session by its REFRESH token' was corrected, because that function's JSDoc was rewritten anyway.",
   "The task template's Python items (pytest venv, ruff, py_compile, socket-guard fixture) do not apply to this client-only unit. I used the jest equivalents (zero-network guard in the new file, tsc, eslint)."
  ],
  "git_status_final": " M SmartCompareApp/__tests__/authService.bootOptimistic.a3.test.ts\n M SmartCompareApp/__tests__/authService.logoutRevoke.w1-4.test.ts\n M SmartCompareApp/src/services/api.ts\n M SmartCompareApp/src/services/authService.ts\n?? SmartCompareApp/__tests__/authService.logoutRefreshFirst.w1-4d.test.ts",
  "residual_risk": "- The fix only takes effect on phones after the next OTA. The server-side revocation still needs R-AUTH plus the flag.\n- A refresh that takes longer than the 5 s bound still ends with an expired Bearer on the wire and a live RT2 upstream. The epoch guard discards RT2 on the phone. Closing this needs R-AUTH's server path.\n- The in-memory handoff holds a rotated refresh token in a module variable until logout's finally. It is never logged or persisted, and only one logout owns the slot. With concurrent logouts, the last one wins the slot and the other falls back to storage.\n- Local state is now cleared before the POST resolves. Any screen that reads storage during a logout sees it cleared earlier than before. No such reader is known.\n- An unexplained 'worker failed to exit gracefully' notice appeared in the multi-worker jest runs. --detectOpenHandles over the auth suites is clean.\n- HistoryScreen.mobileJank.m21 MB-flows-08 is red on base and HEAD alike as of 2026-09-24. It is outside this unit, but CI will see it.\n- There is no RN device walkthrough; the jest harness drives the real axios instance only."
 },
 "green:R-MIG": {
  "unit": "R-MIG green: W1-2 migration 037 retro-fix. W1-2b adds migration 040 to revoke cleanup_expired_ratings. W1-2c adds the user_events BEFORE checks and a rollback that restores the recorded BEFORE value. W1-2d guards statement 4 with to_regprocedure, corrects the apply order and amends 036 to revoke anon. Worktree C:/Users/SynAckITPC/Documents/AI/sc-r-mig, base origin/main 1c6f6796.",
  "files_changed": [
   "migrations/036_home_savings_aggregate.sql",
   "migrations/037_security_definer_grants_and_rls.sql",
   "migrations/rollback/037_security_definer_grants_and_rls.sql",
   "migrations/040_revoke_cleanup_expired_ratings.sql (new)",
   "migrations/rollback/040_revoke_cleanup_expired_ratings.sql (new, comments only)",
   "tests/test_migration_037_security_definer_grants.py",
   "docs/investigations/2026-09-08-session-65-next-units.md",
   "tests/test_retro_w1_2c.py (red file; green added one rollback-shape pin and the strip_line_comments import)",
   "tests/test_retro_w1_2d.py (red file; green added the 036-anon pin required by ruling 1 and refreshed one docstring)",
   "tests/test_retro_w1_2b.py (red file, unchanged)",
   "tests/_retro_r_mig_sql.py (red helper, unchanged)"
  ],
  "all_unit_tests_pass": true,
  "test_evidence": "Pinned venv (.venv-qaren: fastapi 0.141.1, pytest 9.1.1), PYTHONIOENCODING=utf-8, -q -p no:cacheprovider -p no:randomly, -m \"not (live_unit or live_db or integration)\".\n\nUnit files (test_retro_w1_2b/2c/2d, test_migration_037_security_definer_grants, test_migration_index_predicate_immutability):\n- First run: 95 passed, 4 deselected (the live_db class). That was before the green-authored rollback-shape pin existed.\n- After that pin: 96 passed, 0 failed. This was the mutation harness's green baseline.\n- The red had 21 failures; all now pass.\n- The one existing node the ruling required to change (test_migration_037_header_states_the_apply_order_and_the_live_proof) now pins the new contract and passes.\n\nComm gate at HEAD: the red's recorded 25-file set plus the 3 retro files → 512 passed, 0 failed, 13 deselected, rc 0 (scratchpad rmig_green/comm_head_run.txt). A network guard plugin blocked 12 DNS attempts, all by existing tests (neutralized.supabase.invalid, api.openai.com), same as the red's run.\n\nThe base failed set is the red's recorded run: 474 passed / 0 failed at 1c6f6796 in a detached scratch worktree, .qa-retro/R-MIG_comm_base_failed.txt. It is empty. comm -13 of base and head failed sets is empty.\n\nNo flag exists, so there is only one flag state.\n\nLocal PostgreSQL 18.1 throwaway cluster (the red's scratchpad initdb, 127.0.0.1:55437, trust auth, Supabase-style roles and default privileges; stopped afterwards; output in scratchpad rmig_pg/green_scenarios.txt), repo files applied byte-for-byte:\n- S1, prod-today schema with no 035/036 plus an out-of-band cleanup_expired_ratings():\n  - 037 rc 0. Before the guard, the red measured rc 3 on this schema.\n  - delete_user_cascade and increment_lifetime_comparisons: {postgres=X,service_role=X}; anon gets 42501.\n  - resolve_referral_code keeps anon.\n  - 040 rc 0 (\"revoked EXECUTE on 1 signature\"). cleanup_expired_ratings anon_x=false and authd_x=false; anon call gets 42501.\n  - Re-running 037 and 040 gives rc 0 both times.\n- S2, 036 applied late (after 037): home_savings_aggregate anon_x=false from the amended 036 alone; re-running 037 gives rc 0 and it stays false.\n- S3, 036 then 037: rc 0; home_savings_aggregate has authenticated and service_role only.\n- S4, 040 on a database without the function: rc 0 (\"0 signatures\"). With two overloads, () and (p_days integer DEFAULT 7), 040 revokes both. rollback/040 is a comment-only no-op, rc 0.\n- S5, RLS-off branch:\n  - BEFORE relrowsecurity=false, anon count 4; after 037, true and 0.\n  - The unedited rollback raises (rc 3) and relrowsecurity stays true.\n  - The rollback edited to before=false gives relrowsecurity=false.\n- S6, RLS on plus an out-of-band USING(true) policy:\n  - Anon reads 4 before 037 and 4 after, so 037 does not close CR-SECURITY-02 here, as the new header rule says.\n  - The rollback edited to before=true leaves RLS on; anon DELETE RETURNING = 0 (the old rollback gave 1).\n  - Re-run is idempotent.\n\nsqlfluff (venv) lint migrations/: clean, exit 0, both before and after.",
  "lint_clean": true,
  "mutation_checks": [
   "Harness scratchpad rmig_green/mutate.py. For each of the 31 mutants: byte-snapshot the target, apply one mutation (asserted to change the bytes), run the 5 unit files in-process, restore the bytes and sha256-verify. The green HEAD baseline had 0 failures. All 31 were killed. Results are in rmig_green/mutants.json. A final sha check of all 11 touched files against the post-green snapshot: bad=0.",
   "b1 delete 040 -> 7 red (census pin + 040 tests + predicate param)",
   "b2 040 revoke narrowed to FROM PUBLIC -> test_040_revokes_... red",
   "b3 040 adds GRANT to service_role -> test_040_grants_nothing... red",
   "b4 040 top-level REVOKE instead of DO -> test_040_resolves_..._inside_a_do_block red",
   "b5 040 header drops has_function_privilege('anon' -> header test red",
   "b6 census test restores the 'does not exist' assert -> test_census_parser_pin_... red",
   "b7 037 header says NO SUCH FUNCTION EXISTS again -> red",
   "b8 next-units doc says DOES NOT EXIST again -> red",
   "c1 037 header drops the pg_policies query -> red",
   "c2 037 header drops the pg_class BEFORE query -> red",
   "c3 037 header drops the 'does NOT close CR-SECURITY-02' rule -> red",
   "c4 rollback back to top-level unconditional DISABLE -> 2 red",
   "c5 rollback defaults the BEFORE value to false -> green-authored rollback-shape pin red",
   "c6 rollback DISABLEs in the TRUE branch -> rollback-shape pin red",
   "c7 rollback drops the NULL->RAISE guard -> rollback-shape pin red",
   "c8 live pin says 'not that the policy is wrong' again -> red",
   "c9 live pin drops pg_policies -> red",
   "c10 rollback header drops the relrowsecurity warning -> red",
   "d1 statement 4 unguarded -> regprocedure-guard test red",
   "d2 037 header prescribes 035 -> 036 -> 037 again -> 2 red (incl. the updated existing apply-order node)",
   "d3 037 header drops the re-run-037 instruction -> red",
   "d4 036 revoke back to FROM PUBLIC only -> test_pin_036_revokes_home_savings_aggregate_from_anon red",
   "d5 next-units doc prescribes 035 -> 036 -> 037 again -> red",
   "d6 guard names the wrong signature (text) -> guard test red",
   "d7 037 header drops '035 is independent' -> red",
   "Reviewer M1 delete statement 4 entirely -> test_pin_statement_4_narrows_anon_but_never_authenticated red",
   "Reviewer M2 narrow statement-4 revoke to FROM PUBLIC -> same pin red",
   "Reviewer M3 drop service_role from resolve_referral_code grant -> test_pin_resolve_referral_code_regrant_names_service_role red",
   "Reviewer M4 remove BEGIN/COMMIT -> test_pin_037_is_exactly_one_transaction red",
   "Red M5 guard statement 1 too -> 4 red",
   "Reviewer M6 (later DROP+CREATE with no revoke) is covered by the red's order-aware census self-test pin, which passes (it detects the fixture)"
  ],
  "comm_gate": "Set: the 25 files the red recorded in .qa-retro/R-MIG_comm_set.txt (every tests/*.py that references migrations/, *.sql, rollback/ or docs/investigations/) plus the 3 new retro files.\n\n- HEAD (this worktree): 512 passed, 0 failed, 13 deselected, rc 0. The FAILED set is empty.\n- BASE: the red's recorded run at 1c6f6796 in a detached scratch worktree, 474 passed, 0 failed, with an empty FAILED set. I did not re-run it. The base tests and migrations are byte-identical to what the red measured, and this box took 11m50s for one 96-test run.\n\ncomm -13 base_failed head_failed = EMPTY: no branch-only new failures.\n\nI created no git worktree. The three detached scratch worktrees in `git worktree list` (rauth_green/basewt, rmain_green_base, rmeter_green/base_wt) belong to other agents and were not touched.",
  "flag_identity": "No ENABLE_* flag. The change is unapplied SQL, tests and docs only.\n\n- `git diff -- app/ scripts/ SmartCompareApp/ CLAUDE.md` is 0 lines, so no caller path can fork.\n- 037's executable SQL, comments stripped and split into statements outside dollar quotes: base has 11 statements, head has 10. The only difference is statement 4. The two top-level statements `REVOKE ALL ON FUNCTION public.home_savings_aggregate(uuid) FROM PUBLIC, anon` and `GRANT EXECUTE ... TO authenticated, service_role` now sit inside `DO $$ BEGIN IF to_regprocedure('public.home_savings_aggregate(uuid)') IS NOT NULL THEN <the same two statements> END IF; END $$`. Statements 1-3, 5 and BEGIN/COMMIT are byte-identical.\n- Pins that hold the unchanged paths:\n  - test_pin_only_unapplied_upstream_functions_are_guarded: statements 1-3 stay top-level. Red M5 kills it.\n  - test_pin_037_still_enables_rls_and_does_not_force_disable_or_touch_policies\n  - test_pin_statement_4_narrows_anon_but_never_authenticated\n  - test_pin_resolve_referral_code_regrant_names_service_role\n  - test_pin_037_is_exactly_one_transaction\n- 036's executable change is only `FROM PUBLIC` -> `FROM PUBLIC, anon`, pinned by test_pin_036_revokes_home_savings_aggregate_from_anon.\n- 040 is additive: revoke only, no GRANT, CREATE or DROP, pinned by test_040_grants_nothing_and_neither_creates_nor_drops_the_function. Revoke-only is safe because no repo caller exists (test_pin_no_repo_caller_of_cleanup_expired_ratings_so_revoke_only_is_safe).\n\nOn a real engine, 037 with 036 applied first gives the same ACLs as the red's base measurement for all four functions (S3).",
  "measurements_run": [
   "Read the RED report and the reviewer's W1-2 entry in full: 5 defects and 3 tests_that_prove_nothing",
   "Unit files on the pinned venv: 95 passed, then 96 passed once the rollback-shape pin was added; 0 failed",
   "Mutation harness: 31 mutants, all killed, every restore sha256-verified; final sha check bad=0",
   "Comm gate at HEAD over 28 files: 512 passed / 0 failed; base recorded 474/0; comm -13 empty",
   "ruff --select E9,F63,F7,F82 on all 5 touched/new .py files: clean. py_compile: clean",
   "sqlfluff (venv, repo .sqlfluff) lint migrations/: clean before and after",
   "Local PostgreSQL 18.1 throwaway cluster (the red's scratchpad initdb, loopback 55437) running scenarios S1-S6: forward, late 036, 036-first, 040 absent / overloads / re-run / rollback-040, and rollback/037 in the RLS-off and RLS-on+policy branches with unedited, false and true values plus re-run. Cluster stopped afterwards (pg_ctl stop, rc 0)",
   "Executable-statement diff of base vs head 037: only statement 4 differs (now wrapped in the guard)",
   "git diff of app/ scripts/ SmartCompareApp/ CLAUDE.md = 0 lines"
  ],
  "pr_text": "## R-MIG: fix the gaps in migration 037 (W1-2b/c/d), with a new migration 040 and a 036 amendment\n\n**Nothing here changes production when it merges.** Migrations 035, 036, 037 and 040 are all unapplied. This PR changes no app code and adds no flag.\n\n### Defects (retro adversary on PR #153, all reproduced)\n1. **W1-2b: `cleanup_expired_ratings` was dismissed as nonexistent, but it exists live.**\n   - 037's header, a test assert and the next-units doc all called it nonexistent because a repo grep found nothing.\n   - The function was created out of band. `docs/investigations/2026-09-06-full-review-verified.json` records an anon GET returning SQLSTATE 25006, meaning anon passed EXECUTE and the body reached a DELETE.\n   - 037 did nothing to it, so CR-SECURITY-01 would stay partly open after apply.\n2. **W1-2c: 037 assumed CR-SECURITY-02 means \"RLS is off\".**\n   - With RLS already on plus an out-of-band `USING (true)` policy, anon read 4 of 4 rows before 037 and 4 of 4 after.\n   - The old rollback then DISABLEd RLS unconditionally, which let anon DELETE rows. That is worse than the state before 037.\n   - The live pin's failure message also blamed \"RLS is off\" for what could be a policy leak.\n3. **W1-2d: 037 prescribed `035 -> 036 -> 037` as a hard prerequisite.**\n   - 037 never needed 035, and 035's own header says not to apply it.\n   - Statement 4 names 036's `home_savings_aggregate`, so on today's schema 037 fails with rc 3 and rolls back entirely. The account-destruction RPC fix was blocked by two unrelated feature migrations.\n\n### Fixes\n- **New `migrations/040_revoke_cleanup_expired_ratings.sql`.**\n  - An idempotent DO block that finds every `public.cleanup_expired_ratings` by NAME in `pg_proc` and runs `REVOKE ALL ... FROM PUBLIC, anon, authenticated` on each. The lookup is by name because the signature is unknown.\n  - Revoke only: no grant (no repo caller exists), no CREATE, no DROP.\n  - Its header carries the queries to run first: pg_proc, proacl, pg_get_functiondef, and the anon-executable SECURITY DEFINER census (`prosecdef AND has_function_privilege('anon', ...)`).\n  - Numbering: 038 is W3-16's and 039 stays reserved for M13-29.\n  - `rollback/040` contains comments only and deliberately refuses to re-grant.\n- **037 header.**\n  - Corrected the `cleanup_expired_ratings` note; it now points to 040.\n  - Added BEFORE checks for `user_events`: `pg_class relrowsecurity/relforcerowsecurity` and a `pg_policies` query filtered on `schemaname='public' AND tablename='user_events'`.\n  - Added the rule: RLS already on, or any policy other than `events_insert`/`events_select`, means 037 does NOT close CR-SECURITY-02.\n  - Removed the claim that \"apply 037 is sufficient\".\n- **037 statement 4** is now `DO $$ ... IF to_regprocedure('public.home_savings_aggregate(uuid)') IS NOT NULL THEN <same REVOKE/GRANT> END IF; ... $$`. Statements 1-3 stay unguarded on purpose, so a typo still rolls back the whole file loudly.\n- **Apply order, rewritten:** 037 any time; 036 whenever its flag is readied, then re-run 037; 035 is independent.\n- **036 (unapplied, amended per the red-gate ruling):** `REVOKE ... FROM PUBLIC, anon`. A late 036 now closes anon by itself; measured anon_x=false.\n- **rollback/037** no longer DISABLEs unconditionally. A DO block reads `before_relrowsecurity`, which ships as NULL and the operator sets to the value recorded before apply:\n  - NULL raises, and nothing changes;\n  - true leaves RLS on;\n  - only false DISABLEs.\n  The header carries a warning.\n- **Tests.**\n  - The census pin's absence assert is replaced with a pin that 040 names the function.\n  - The apply-order node now pins the new contract: 037 does not depend on 035 or 036, the to_regprocedure guard, re-run 037, and no `035 -> 036 -> 037`.\n  - The live `user_events` pin's message now names both causes and the `pg_policies` query.\n  - New pins: 036 revokes anon, and the rollback's NULL/true/false shape.\n- **Next-units doc:** corrected lines 38, 64 and the four apply-order lines (298, 327, 361, 403, as numbered before this PR).\n\n### Flag row\nNone. There is no runtime code: `git diff -- app/ scripts/` is empty. In 037's executable SQL, only statement 4 changes, and only by being wrapped in the guard.\n\n### Evidence\n- **Unit files:** 96 passed, 0 failed.\n- **Comm gate:** HEAD 512 passed / 0 failed over 28 files; base 474/0; branch-only new failures: none.\n- **Mutation:** 31 mutants, all killed, with sha-verified restores. This includes the reviewer's M1-M4, a guarded-statement-1 mutant, and 26 per-defect mutants.\n- **Lint:** ruff, py_compile and sqlfluff are clean.\n- **Local PostgreSQL 18.1** with Supabase-style roles and stand-in bodies:\n  - 037 applies with rc 0 without 036 (the red measured rc 3 before the guard).\n  - 040 gives rc 0 with the function absent, present, with two overloads, and on re-run; after it, anon gets 42501.\n  - The rollback behaves correctly in the RLS-off and the RLS-on-plus-policy branches.\n\n### Activation gate (Ahmed, in order)\n1. Before 037, run and keep the output of the `pg_class` and `pg_policies` checks from 037's header.\n2. Before 040, run 040's header queries.\n3. Apply 037, then 040, in either order, whenever you choose.\n4. After apply:\n   - proacl has no `anon=`, `authenticated=` or bare `=X` entry on `delete_user_cascade`/`increment_lifetime_comparisons`;\n   - the anon-key `rpc/delete_user_cascade` POST returns 42501;\n   - an anon `count=exact` on `user_events` returns 0;\n   - an anon GET on `rpc/cleanup_expired_ratings` never again returns 25006;\n   - the census lists only `resolve_referral_code`.\n5. If the BEFORE checks showed RLS already on or an extra policy, CR-SECURITY-02 stays OPEN. It needs a follow-up migration that drops those policies by name, written from that output.\n6. If 036 is applied after 037, re-run 037.\n\n### Honest limits\n- The real signature, body and ACL of `cleanup_expired_ratings` are unknown. The local test used a stand-in.\n- Where `service_role` has no explicit default grant, 040 also removes its EXECUTE. No repo caller exists.\n- The rollback needs the recorded BEFORE value. Without it there is no safe answer, and the shipped NULL refuses to run.\n- The engine was a local PostgreSQL emulation, not Supabase: no PostgREST and no schema cache.\n- No `live_db` pin for `cleanup_expired_ratings` was added (superseded by the revoke-only ruling).\n\n### CLAUDE.md corrections (separate docs PR; not edited here)\n- **:370** (`ENABLE_SYNC_DB_OFFLOAD` row): \"the behaviour-profile RPC (needs migration 037)\" -> \"needs a future migration (unnumbered)\".\n- **SESSION 65c ADDENDUM, #153 bullet:** \"apply order `035 -> 036 -> 037`\" -> \"037 applies any time (statement 4 is guarded by `to_regprocedure('public.home_savings_aggregate(uuid)')`); 036 whenever `ENABLE_HOME_SAVINGS_AGGREGATE` is readied, then re-run 037; 035 independent; run 037's `user_events` BEFORE checks first.\"\n- **SESSION 65 block, \"Corrections to the consolidated review itself\":** \"`cleanup_expired_ratings` ... does not exist anywhere in migrations/ or app/\" -> \"exists LIVE, out of band (anon GET -> SQLSTATE 25006, recorded in docs/investigations/2026-09-06-full-review-verified.json). It is in no migration, which is why a repo grep misses it. Migration 040 revokes it from PUBLIC, anon, authenticated.\"\n\n### Follow-ups (incl. the reviewer's unaddressed minors)\n- **W1-2e:** the main file's REVOKE-credit guard is still a set union that ignores file order. The order-aware census and the M1-M4/M6 pins live in `tests/test_retro_w1_2d.py`; fold them into `test_migration_037_security_definer_grants.py`.\n- A migration that drops extra `user_events` policies by name, if the BEFORE output shows any.\n- FORCE ROW LEVEL SECURITY is still undecided (it depends on the table owner).\n- The next-units doc still says \"033-036 are also unapplied\" at lines 58 and 142; 033 and 034 are applied. These lines were outside the cited scope.\n- rollback/037's claim that there is \"no PostgreSQL on the machine\" is now stale; there is a local 18.1.\n- The migration-number uniqueness check the reviewer asked for exists as `test_pin_migration_numeric_prefixes_are_unique`.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "deviations_from_spec": [
   "Timeouts were --timeout=600, not the prescribed 120. The box is badly overloaded: the first in-process run of 96 source-scan tests took 11m50s, and the red hit pytest-timeout at 120 on both sides for the same reason.",
   "The comm-gate base was not re-run. It reuses the red's recorded base run (474 passed / 0 failed at 1c6f6796, in a detached scratch worktree since removed), whose tests and migrations are byte-identical to today's base. HEAD was run fresh.",
   "Added migrations/rollback/040_revoke_cleanup_expired_ratings.sql. It holds comments only, refuses to re-grant, and follows the rollback-pair precedent. It was not named in the scope list.",
   "Added two pins inside red-authored files. In tests/test_retro_w1_2d.py: test_pin_036_revokes_home_savings_aggregate_from_anon, required by ruling 1. In tests/test_retro_w1_2c.py: test_pin_rollback_037_ships_a_null_before_value_and_disables_only_when_it_was_false, needed because no source test could see the rollback's branch logic. Also refreshed one red docstring that said 036 was outside scope.",
   "In test_migration_037_security_definer_grants.py, besides the two required nodes (the census pin and the apply-order pin) and the live user_events message, I corrected two prose-only spots that repeated the false claims: the enables-RLS docstring ('applying 037 is sufficient') and the live-pins WHEN TO RUN comment ('in order, after 035 and 036').",
   "036's explanatory comment sits after its GRANT so that the existing 036:94/036:95 line citations in 037, rollback/037 and the tests stay valid."
  ],
  "git_status_final": " M docs/investigations/2026-09-08-session-65-next-units.md\n M migrations/036_home_savings_aggregate.sql\n M migrations/037_security_definer_grants_and_rls.sql\n M migrations/rollback/037_security_definer_grants_and_rls.sql\n M tests/test_migration_037_security_definer_grants.py\n?? migrations/040_revoke_cleanup_expired_ratings.sql\n?? migrations/rollback/040_revoke_cleanup_expired_ratings.sql\n?? tests/_retro_r_mig_sql.py\n?? tests/test_retro_w1_2b.py\n?? tests/test_retro_w1_2c.py\n?? tests/test_retro_w1_2d.py",
  "residual_risk": "- **040 targets an unknown function.** The body, signature and ACL of `cleanup_expired_ratings` are unknown to the repo. Locally it was proven only on stand-ins (a no-argument function, and an overload taking `integer DEFAULT 7`). The header queries must be run first.\n- **040 can remove service_role's EXECUTE.** On a project without an explicit default grant to service_role, 040 removes its EXECUTE as well. This is harmless only while no caller exists, which a pin enforces.\n- **CR-SECURITY-02 can stay open.** If the live cause is a permissive policy, 037 does not close it. The header now says so, but closing it needs a follow-up migration written from the pg_policies output.\n- **The rollback depends on a recorded value.** It needs the relrowsecurity value recorded before apply. The shipped NULL raises by design, so an operator who did not record it has no automatic rollback.\n- **Emulation, not Supabase.** All engine evidence comes from a local PostgreSQL 18.1 emulating Supabase roles and default privileges. There was no PostgREST and no schema cache, and prod ACLs were never read.\n- **Base comm set reused.** The base comm set was the red's recorded run, not a fresh one.\n- **Unfixed items recorded as follow-ups:**\n  - the main test file's set-union REVOKE credit (the W1-2e order-aware logic lives only in `test_retro_w1_2d.py`);\n  - the next-units doc lines 58 and 142 (\"033-036 unapplied\");\n  - rollback/037's stale \"no PostgreSQL on the machine\" line;\n  - the three CLAUDE.md corrections, which go in a docs PR."
 }
}