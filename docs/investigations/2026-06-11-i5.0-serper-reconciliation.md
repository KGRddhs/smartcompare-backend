# I5.0 — Serper ceiling reconciliation: live-counter read attempt + finding

**Date:** 2026-06-11 · **Lane:** I5 "Yield & Wall" (Bundle B S2) · **G1 gap (a):** the I5.0 commit cited CLAUDE.md for the ceiling instead of reading the REAL balance — the docs-vs-reality drift class. This records the live-read attempt and what it found.

## Live-counter read attempt (control-calibrated)

Attempted to read `budget:serper:lifetime` (and `budget:firecrawl:lifetime`) from the lane worktree env, with the sandbox-DNS safety discipline:

- **Control:** `socket.gethostbyname("google.com")` → 142.250.202.142 (resolves).
- **Upstash host:** `socket.gethostbyname("faithful-eel-37884.upstash.io")` → **gaierror (does NOT resolve)**.
- Retried with `dangerouslyDisableSandbox` → same `ConnectError [Errno 11001] getaddrinfo failed`.

**Conclusion:** the Upstash host is **DNS-unresolvable from this box** (not a sandbox network-policy block — disabling the sandbox didn't change it; this box's DNS/firewall does not resolve the Upstash hostname). So the live counter **cannot be read from the lane worktree**. Per never-fabricate, NO value is recorded here — the live read must come from a reachable environment:
- **Preferred:** `GET /api/v1/admin/costs → serper_burn.used` (prod reads `budget:serper:lifetime` server-side; also surfaces `{limit, threshold, fraction, over_threshold}`).
- **Authoritative for the new key:** Ahmed's serper.dev dashboard for key `3d304e...`.

## Reconciliation finding (the part that does NOT need the live read)

The counter key is `budget:serper:lifetime` — **a single key, NOT scoped to the API key**. Per CLAUDE.md the Serper key was rotated to `3d304e...` 2026-06-10 after mid-baseline depletion, but the Redis counter was **not** reset on rotation. Therefore:

- The counter **plausibly contains pre-rotation burn** accumulated under the *previous* key(s).
- The "real remaining balance **for key 3d304e...**" is therefore **NOT derivable from the counter alone** — the counter conflates usage across key generations. Only the serper.dev dashboard reflects the new key's actual consumption.
- **Implication for the alert:** the 80%-burn tripwire fires on the SHARED counter, so it is conservative-correct for "stop burning credits" (it trips at aggregate 1760 regardless of which key burned them), but it is NOT a per-key balance. After a rotation, consider resetting `budget:serper:lifetime` so the counter tracks the live key — **a reset needs dispatcher GO** (it is not done here; the dispatcher will ask Ahmed for the dashboard number first).

## Ceiling config (unchanged, confirmed correct)

`api_budget_service.PROVIDER_CONFIGS["serper"]`: `monthly_limit=2200`, `warn_at=2000`, `is_lifetime=True` — a 2,500-credit serper.dev free account with a 300 buffer, matching CLAUDE.md "Serper 2200/lifetime". No code change to the ceiling; I5.0 added the 80% tripwire (1760) below the pre-existing 2000 warn. Runway at the trip: 440 credits ≈ 30–44 escalating cold queries (runbook).

## Carry-over (binding rule — nothing silently drops)

- Live `budget:serper:lifetime` read: **owner = dispatcher/Ahmed**, via `/admin/costs → serper_burn` or serper.dev dashboard (lane worktree cannot resolve the Upstash host).
- Counter reset post-rotation: deferred, **needs dispatcher GO** (do not reset unilaterally).
