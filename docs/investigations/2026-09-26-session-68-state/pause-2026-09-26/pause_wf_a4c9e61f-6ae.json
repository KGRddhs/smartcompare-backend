{
 "fix:W4-13-r2": {
  "unit": "W4-13 fix round 3 (Fable rulings R10-R13, after adversary r2 returned SOUND): search_logs measurement truth. Flags ENABLE_SEARCH_LOG_TRUTH and ENABLE_SEARCH_LOG_SYNTHETIC_MARKER, knob SEARCH_LOG_SYNTHETIC_TOKEN, migration 042. Worktree C:/Users/SynAckITPC/Documents/AI/sc-w4-13, HEAD ac887e2d, not rebased.",
  "files_changed": [
   "tests/test_w4_13_measurement_truth.py 102dc20262720f992dbd8f5c02c0a3bed89b84a7abdeb8b353f9773d86be76e7 (EDITED, R11: _C3_SITES += camera_exception, camera_exception_anon; new pins test_stream_terminal_without_a_success_key_keeps_heads_success_row, test_a_zero_top_level_total_cost_is_a_valid_cost, test_partial_truthy_but_not_true_is_not_a_partial[int_1|str_yes]; LF, as before)",
   "tests/test_w4_13_flag_off_ledger.py 9dba0689b8118e46313664cf3ce9c1a1d19b30efd666a8bdbcbf9af677c8c8bd (NEW, R10: 2 nodes)",
   "tests/fixtures/_gen_w4_13_flag_off_ledger.py ee030aedc7305249879e032b403361a40f73dcac6d1d5e30769875b29ac82b11 (NEW, R10: the generator; lazy app/helper imports)",
   "tests/fixtures/w4_13_flag_off_ledger.json 5b569af58a74afb546e7f28224a1fdfb5f77d86e095e722b02347e1657c61157 (NEW, R10: 42 records recorded at base ac887e2d; records_sha256 331d21a92a9baca96f3a584fe39439fee9608f46a130d0cc9043254a92d4089c)",
   "migrations/042_search_logs_is_synthetic.sql f56d3a29cf581001e330f62918dd5bc892d3a1a3a1d0a3666c9af5eee5ca5231 (EDITED header only, R12; LF preserved; SQL body byte-unchanged)",
   "tests/test_migration_042_search_logs_is_synthetic.py 92e2828525638cb6ad64fc9bc7f628c8e7b95e36bae60b912c36387b92d2cfbc (EDITED, R12: + test_042_header_states_read_committed_truth_and_the_pause_instruction)",
   ".qa-s68/ci_order_set.txt 63fbcd95b0e3f9f4338d45a3501682fb92082938d804ea38ec57b59ae36e6043 (EDITED, gitignored: + tests/test_w4_13_flag_off_ledger.py, 21 files, LC_ALL=C order)",
   "app/api/text_routes.py 82f224d218337aa745057692dbb885ad89b7e11718a38bbe13260096cf922af5 (left as found)",
   "app/api/image_routes.py b36ef3af52e0dcbdcf758e822ccb35e6ab30fb5f80e1b937d76049acb12ddc32 (left as found)",
   "app/api/url_routes.py e22e8fb3121cf91cf37a15a6cac5940dc981f2afcf83cfbae975b3226ee7f500 (left as found)",
   "app/services/analytics_service.py 106d7e11533b5f73356120c597088f3f2d67c4f257a7e545dcb727261b4a71d7 (left as found)",
   "app/services/database_service.py 26457ff414f9d92ab705ba2e11638721ac8f54b51f0480886dd5d083c0cfd0ab (left as found)",
   "app/services/sentry_service.py be168e6b209967674bbba5963833859660d6f4666b9f673f91c592f97021da8a (left as found)",
   "scripts/eval_runner.py 913b1415e0c8195343da3841808ef6925698e7dbdf731ebd05ee304d49844281 (left as found)",
   "tests/test_m18_preverdict_disconnect_refund.py f33e72009dc95d2874be382a3180f0d79f2cd12a65f7bc210ab473518694ea6c (left as found)",
   "migrations/rollback/042_search_logs_is_synthetic.sql 0f4845f24358bf52e4f2aabd3d9d67fd6c5eed3d520f7bd1f572306ae74379bc (left as found)",
   "scratch (not in repo): <scratchpad>/w413fix3/pr_text.md dd6f6f9ee8d91680ceb8bb51567aefc26f7c846cc8bd67ed55bbde342689a9df"
  ],
  "all_unit_tests_pass": true,
  "test_evidence": "Start check: all 12 files matched the ground-truth shas in the task, and git status showed 8 M + 4 ??. Pre-edit byte snapshots are in <scratchpad>/w413fix3/pre/ (start_shas.txt).\n\nUnit files (4): test_m18_preverdict_disconnect_refund.py, test_migration_042_search_logs_is_synthetic.py, test_w4_13_flag_off_ledger.py and test_w4_13_measurement_truth.py. Pinned venv, PYTHONPATH=<scratchpad>/netguard, -p qaren_netguard, -p no:randomly, not-live marker filter, one process per state:\n- unset: '[netguard] blocked 0 network attempt(s)' / '243 passed, 8 warnings in 193.38s'\n- ENABLE_SEARCH_LOG_TRUTH=true: 'blocked 0' / '243 passed, 8 warnings in 114.55s'\n- marker + token: 'blocked 0' / '243 passed, 8 warnings in 167.31s'\n- both + token: 'blocked 0' / '243 passed, 8 warnings in 163.76s'\n\n243 = the round-2 234 plus 9 new nodes:\n- 2 C3 camera nodes\n- 1 X7 pin\n- 1 X14 pin\n- 2 X15 pins\n- 1 R12 header pin\n- 2 ledger nodes\n\nThe committed ledger test alone at HEAD: 'blocked 0' / '2 passed'.\n\nLogs: <scratchpad>/w413fix3/unit.{none,truth,marker,both}.log",
  "lint_clean": true,
  "mutation_checks": [
   "Harness <scratchpad>/w413fix3/mutate3.py. For each mutant it takes a shutil.copyfile byte snapshot, applies a CRLF-aware edit that must match exactly once, runs the 4 unit files with flags + token unset under the netguard, restores from the snapshot and sha256-verifies (it aborts on any mismatch). All 7 restores were verified, and the post-run sha of every app/script file equals the start sha. Results are in mut3_out.json and mut3.log. 7/7 killed.",
   "X20 (image_routes camera success site `if _truth and comparison_unsuccessful` -> `if comparison_unsuccessful`): '1 failed, 242 passed', blocked 0. Red: tests/test_w4_13_flag_off_ledger.py::test_flag_off_ledger_equals_the_pre_unit_recording_record_by_record. Ruling R10 required this committed test to catch X20, and it does.",
   "X11b (the T7 camera-exception row block moved BEFORE _refund_reserved_credit/_refund_anon_credit): '2 failed, 241 passed'. Red: test_raising_row_builder_still_fires_the_refund[camera_exception] and [camera_exception_anon].",
   "X7 (T3 `complete_response.get(\"success\") is False` -> `not complete_response.get(\"success\")`): '1 failed, 242 passed'. Red: test_stream_terminal_without_a_success_key_keeps_heads_success_row.",
   "X14 (_log_cost_value `v < 0` -> `v <= 0`): '1 failed, 242 passed'. Red: test_a_zero_top_level_total_cost_is_a_valid_cost.",
   "X15 (_partial_log_note `md.get(\"partial\") is True` -> `md.get(\"partial\")`): '2 failed, 241 passed'. Red: test_partial_truthy_but_not_true_is_not_a_partial[int_1] and [str_yes].",
   "R12a (the whole pre-R12 042 file restored, old 'no row lands in between' guarantee): '1 failed, 242 passed'. Red: test_042_header_states_read_committed_truth_and_the_pause_instruction.",
   "R12b (only the old sentence '(no row lands in between)' re-inserted beside the new text): '1 failed, 242 passed'. Red: the same R12 header pin.",
   "X26 (T4 `_failure_log_message(error_payload)` -> raw .get('error')) is an accepted EQUIVALENT mutant per R11: the W4-9 SSE floor already mutates the same dict. The row floor is kept and pr_text says so. No pin was added.",
   "The earlier 39-mutant table (fixer r2 + adversary r1) was not re-run. No app, script or rollback file changed this round (shas identical to the start), so those kills still stand on these bytes."
  ],
  "comm_gate": "HEAD run of the recorded 119-file comm set (.qa-s68/comm_set.txt, LC_ALL=C sorted, 6 chunks), flags + token unset, CI deselects, -p qaren_netguard. The base run was not repeated, as R13 allows.\n\nFirst pass at --timeout=60:\n- chunk_01: 412 passed.\n- chunk_03: 362 passed.\n- chunk_04: 4 failed / 697 passed.\n- chunks 00, 02 and 05: killed by the pytest-timeout inside subprocess-spawning tests (test_conftest_env_safety) on the heavily loaded box.\n\nThose three chunks were re-run at --timeout=600:\n- chunk_00: 1 failed / 599 passed / 4 skipped, blocked 11.\n- chunk_02: 377 passed, blocked 33.\n- chunk_05: 668 passed, blocked 139.\n\nTotal: 5 failed / 3115 passed / 4 skipped.\n\nThe 4 chunk_04 failures are the guard-caused SSRF DNS nodes, all listed in .qa-s68/specs/W4_13_comm_base_failed.txt:\n- test_rate_limiting_complete.py::TestUrlDetectSsrf::test_detect_allows_valid_url\n- test_security_hardening.py::TestSSRFProtection::test_valid_external_url_passes\n- test_security_hardening.py::TestSSRFProtection::test_valid_http_url_passes\n- test_security_hardening.py::TestSecurityIntegration::test_ssrf_protection_integrated\n\n`comm -13` of that set against the base failed set is EMPTY.\n\nThe 5th failure is tests/test_conftest_env_safety.py::test_live_opt_in_restores_the_environment_end_to_end:\n- Its child pytest hit its own inner --timeout=45 while importing openai under load.\n- Re-run alone 3 times it passed every time: 'blocked 0' / '1 passed' at 107.59 s, 22.87 s and 69.26 s.\n- It touches no unit module.\n- It is timing-environmental, not a regression.\n\nCounts are consistent: 3115 = the round-2 3109 + the 7 new unit nodes in the set - that 1 timing node.\n\nVERDICT: PASS, no new failure attributable to the unit. Logs: <scratchpad>/w413fix3/comm_head/ (chunk_*.log, chunk_*.rerun.log), envsafety.{1,2,3}.log",
  "ci_order_run": "The CI-order set .qa-s68/ci_order_set.txt now has 21 files, with tests/test_w4_13_flag_off_ledger.py added in LC_ALL=C order. It ran in ONE process per state with the CI deselects and -p qaren_netguard. 0 failed in every state:\n- unset: '[netguard] blocked 17 network attempt(s)' / '701 passed, 210 warnings in 384.07s'\n- flag 1: 'blocked 46' / '701 passed in 366.99s'\n- flag 2 + token: 'blocked 17' / '701 passed in 401.64s'\n- both + token: 'blocked 46' / '701 passed in 558.01s'\n\n701 = the round-2 692 + the 9 new nodes. The blocked targets are the fail-open neutralized.supabase.invalid:443 inserts, the same as earlier rounds.\n\nPreserve set (34 files, <scratchpad>/w413/preserve34.txt, --timeout=300). 786 passed, 10 deselected in every state:\n- unset: blocked 26. The first attempt died on a Windows fatal 0x8007000e (out of memory) during import; the re-run gave 786 passed.\n- flag 1: blocked 58.\n- flag 2 + token: blocked 26.\n- both + token: blocked 55.\n\nLogs: <scratchpad>/w413fix3/ci.*.log, pres.*.log, pres.none.rerun.log",
  "byte_identity": "Not a price-path unit, so scripts/verify_flag_byte_identity.py is N/A. The flag-OFF identity is now the COMMITTED ledger (R10).\n\nThe fixture tests/fixtures/w4_13_flag_off_ledger.json was recorded at the pre-unit base ac887e2d:\n- It ran in a detached scratch worktree <scratchpad>/w413fix3_base, with the head helper test module and the generator copied in (cmp-identical), flags + token unset, through a pytest probe under the netguard.\n- Result: 'blocked 0' / '1 passed', records sha 331d21a92a9baca96f3a584fe39439fee9608f46a130d0cc9043254a92d4089c, 42 records.\n\nRe-derived, record by record, with 0 of 42 records differing each time:\n- base2 (a second base run): sha 331d21a9.\n- HEAD, the committed test: '2 passed', blocked 0.\n- current main 88cb78ae (a second detached scratch worktree): sha 331d21a9, 'blocked 0'.\n- The earlier adversary-r2 scratch head ledger.\n\nThe fixture's _meta names:\n- generator: tests/fixtures/_gen_w4_13_flag_off_ledger.py\n- recorded_at: ac887e2d (= 61585c58 + #202)\n- rule: 'flag OFF is byte-identical to the pre-unit kwargs at every log_search site'\n- scenarios: 42\n- records_sha256\n\nThe test asserts exactly 42 unique scenarios and compares record by record. The ON ledger stays scratch.",
  "pr_text": "## W4-13: search_logs records what happened, and the admin readers skip the probe traffic\n\nFindings: PO-RECORDED-MEASURED-01, -07, -10, -11; PO-CATEGORIES-I18N-13; LS-MEASURED-EVIDENCE-07, -08.\nPO-RECORDED-MEASURED-17 (anon_id) stays OPEN as its own row for Ahmed (Q5), because a fingerprint-derived id can be re-linked to users.device_fingerprint_hash.\n\n### Defects (measured at ac887e2d, which is 61585c58 plus the auth-only #202)\n- **The four admin readers aggregate every row.** 88.6 % of the recorded search_logs series is 13 probe strings. On the fixture, the daily avg_duration is 3,667 ms over 12 rows, where the organic truth is 22,000 ms over 2 rows. popular[0] is 'product1 vs product2'.\n- **Failure rows never carry a cost.** Four of the five failure sites pass no `cost`. camera.unsuccessful reads metadata.total_cost, which is always absent there, because the orchestrator puts total_cost at the TOP level.\n- **Delivered partials are not marked** in the log.\n- **The stream logs its success:False TERMINALS as successes**, with success True and cost 0. These are STREAM_TIMEOUT, INSUFFICIENT_DATA and the moderation refusal. This is consistent with the recorded \"0 of 721 stream failures\" (an inference: those rows were written by older code).\n- **Abandoned streams and mid-stream raises write no row.**\n- **/url/compare and POST /text/quick write no row at all.**\n- **Codeless failures and the camera exception log the service's str(e)** into search_logs.error_message.\n\n### Design\nThere are two per-call flags. Both default OFF and are read with `.strip().lower() in (true, 1, yes, on)`. Each has ONE definition, in database_service.\n\n**ENABLE_SEARCH_LOG_TRUTH (flag 1).** It has no precondition and is a live kill-switch.\n- T1: POST/GET failure rows carry `cost`. The value is the top-level total_cost, else metadata.total_cost, else 0.0. NaN, inf, negative, bool, str and an int too large for a float all give 0.0. A top-level total_cost of exactly 0.0 is a valid cost and never falls through to metadata.total_cost (pinned, ruling R11(c)).\n- T2: a delivered partial keeps success True and gets error_message 'partial:<stage>'. Only `metadata.partial is True` (W4-4's own stamp) counts; a truthy-but-not-True value such as 1 or 'yes' writes no note (pinned, R11(d)).\n- T3: a stream terminal whose payload has `success is False` logs a failure under the NEW label log_search.text_stream.terminal_failure. `is False` is deliberate: a terminal WITHOUT a `success` key keeps HEAD's success row (pinned, R11(b)). Billing is UNCHANGED.\n- T4: the stream error row carries the event's own text (already floored by W4-9) and its cost.\n- T5: an abandoned or raised stream writes ONE failure row, keyed on client_gone: 'client_gone_before_complete' or 'stream_incomplete'.\n- T6: the camera unsuccessful cost uses three rungs (R3):\n  1. top-level total_cost + vision;\n  2. else metadata.total_cost as-is (the route already added vision);\n  3. else vision alone.\n- T7: the camera exception row carries vision_cost. Its message is the constant 'camera_exception', never str(e) (post-green R9(b)). Flag OFF keeps today's str(e).\n- T8: /url/compare writes a success, failure or exception row on both verbs.\n  - The query keeps scheme + host + an explicit port + path. It drops the query string, the fragment and the userinfo (R4).\n  - user_id is the caller's id from get_optional_user, or None for an anonymous caller.\n  - There is no cost, because compare_from_urls exposes none.\n  - The '<2 products' failure logs its own route literal verbatim (R9(a)). Every other codeless message is floored.\n- T9: POST /text/quick writes rows (Q7/D1).\n- R1: under flag 1, every failure row's error_message passes the SAME W4-9 codeless floor as the response (`_is_codeless_safe_message`, no second allowlist).\n  - A coded result logs its message unchanged.\n  - A codeless message that is not allowlisted logs INTERNAL_ERROR_FRIENDLY_MESSAGE.\n  - Exception rows log constants: 'url_compare_exception', 'quick_compare_exception' and 'camera_exception'.\n  - On the stream error row (T4) the floor is defence in depth: the W4-9 SSE floor has already rewritten that same dict in place, so the row-floor mutant (X26) is an accepted EQUIVALENT mutant, not a coverage gap. The row floor stays.\n- C3: every NEW or changed failure row is built AFTER its refund fire_and_forget and before the raise. This is now pinned at every such site, including the camera exception row (T7), where a raising row builder still fires the reserved-credit refund (authed + metering) and the anon-credit refund (anon gate + metering + fingerprint) (R11(a)). The row builders are TOTAL over any input. That includes a products list holding non-dict items, a non-dict metadata, and an int total_cost too large for a float.\n- R2: under flag 1, the existing T1/T4/T6/T7 rows move AFTER their refund. This also REORDERS the fire_and_forget labels on those paths. There is no response, status or SSE change.\n- The url and quick delivery rows sit after their metering calls.\n\n**ENABLE_SEARCH_LOG_SYNTHETIC_MARKER (flag 2), with the SEARCH_LOG_SYNTHETIC_TOKEN knob.** HARD PRECONDITION: migration 042 is APPLIED.\n- log_search gains `is_synthetic=None`. The key is written only when it is not None AND flag 2 is ON.\n- Every site passes the caller's classification, including both /url/compare verbs. A request is synthetic when the X-Qaren-Synthetic header equals the token:\n  - both must be non-empty, checked before the compare (C2);\n  - the compare is compare_digest over bytes with surrogateescape;\n  - there is no broad except.\n- The four readers select `, is_synthetic` and drop rows where it `is True`; NULL and False rows are kept. The three dict readers report `synthetic_excluded`.\n- The shared Sentry request scrub redacts X-Qaren-Synthetic in both hooks (D4).\n- scripts/eval_runner.py sends the header only when the token is set.\n- The token is least-privilege: if it leaks, the holder can mark or hide only their OWN traffic. Rotation is to set a new value on Railway `web` and in the eval runner's env. No code changes.\n\n### Flag rows (CLAUDE.md, at merge)\n| name | default | effect ON | flag-OFF identity | knobs |\n|---|---|---|---|---|\n| ENABLE_SEARCH_LOG_TRUTH | OFF, read per call | T1-T9 + R1 + R9 as above; no client-visible byte changes | committed pin `tests/test_w4_13_flag_off_ledger.py` + `tests/fixtures/w4_13_flag_off_ledger.json`: 42 scenarios recorded at the pre-unit base, asserted record by record at HEAD | none |\n| ENABLE_SEARCH_LOG_SYNTHETIC_MARKER | OFF, read per call | the writer key, the reader filter and synthetic_excluded | the same committed ledger; pins 8, 10 and 16 | SEARCH_LOG_SYNTHETIC_TOKEN (secret, read per call, never logged; rotation = new value on web + eval env) |\n\n### APPLY_PACK row: 042\nApply 042 AFTER merge and BEFORE flag 2. To roll back, turn flag 2 OFF FIRST, then run rollback/042, which discards the classification.\n- The census is an OPERATOR instruction (R5, no DO block). The Supabase SQL editor wraps one pasted multi-statement script in one transaction, so paste the census, the body and the AFTER check into ONE run.\n- **One transaction is not one snapshot (R12).** At PostgreSQL's default READ COMMITTED isolation each statement takes its own snapshot, while now() is fixed at transaction start. A probe-string row committed between the census SELECT and the UPDATE (with created_at before that now()) is therefore TAGGED but NOT COUNTED, and AFTER may exceed BEFORE - EXCLUDED by exactly that many rows. Pause the eval runner (and any other probe harness) for the apply. Otherwise, accept a small positive difference and re-run the census afterwards.\n- Paste back three numbers: BEFORE = a + b, EXCLUDED = c, and AFTER = BEFORE - EXCLUDED (or slightly more, per the line above). Paste a_pull and c_pull beside them for the anchor.\n- Sanity anchors from the 2026-09-02 pull: a_pull = 11,724 and c_pull = 295, so the pull window tags 11,429. The organic series re-states to 1,808 rows / 82.08 % / p50 20,937 ms. The live buckets a and c also count pre-June rows and rows written since the pull, so a > 11,724 or c > 295 is expected, not an anomaly.\n- Residual ambiguity: the 295 excluded probe-#1 rows (>= 10 s) may still be probes, and probe-#1 rows under 10 s may be users.\n\n### Activation order\n1. **Flip flag 1 alone.** Publish the KPI series change the day it flips; both effects below are definition changes.\n   - Under T5/T3, abandoned streams and moderation/timeout terminals count as FAILURES in get_error_stats, so the stream failure rate rises from 0.\n   - Failure rows now carry a cost, so the cost KPI rises.\n2. **Apply 042**, using the paste-back above, with the eval runner paused.\n3. **Set SEARCH_LOG_SYNTHETIC_TOKEN** on web and in the eval env, then flip flag 2.\n4. **Run the one-minute canary.** The count of rows written after the flip must be > 0. log_search swallows insert errors, so silence means a missing column. Then check that the count of `is_synthetic IS NULL` rows after the flip is 0.\n\n### Gates\nAll runs used the pinned venv and the process-wide netguard plugin (`-p qaren_netguard`), because the hermeticity unit is not merged yet. The worktree HEAD is still ac887e2d (not rebased; main 88cb78ae touches no unit file).\n- **Unit files (4):** test_w4_13_measurement_truth.py, test_w4_13_flag_off_ledger.py, test_migration_042_search_logs_is_synthetic.py and test_m18_preverdict_disconnect_refund.py. 243 passed in all 4 flag states (unset, flag 1, flag 2 + token, both + token), blocked 0.\n- **CI-order set (21 files, the ledger test added; one process per state):** 701 passed, 0 failed, in all 4 states (unset, flag 1, flag 2 + token, both + token). blocked 17 / 46 / 17 / 46, all at the fail-open neutralized.supabase.invalid:443 inserts. 701 = the round-2 692 + the 9 new nodes.\n- **Preserve set (34 files):** 786 passed, 10 deselected, in all 4 states. blocked 26 / 58 / 26 / 55; these vary with timing, and every target is a fail-open Supabase insert or getaddrinfo api.openai.com. The first unset-state run died on a Windows out-of-memory fatal (0x8007000e) while importing; the re-run passed 786.\n- **Comm set (119 files, HEAD run, flags + token unset):** 4 failed / 3115 passed / 4 skipped once three chunks were re-run. The box's 60 s pytest-timeout killed chunks 00, 02 and 05 inside subprocess-spawning tests, so those three were re-run at --timeout=600. The 4 failures are the guard-caused SSRF DNS nodes, all in .qa-s68/specs/W4_13_comm_base_failed.txt. One more node failed only in the chunk-00 re-run: test_conftest_env_safety.py::test_live_opt_in_restores_the_environment_end_to_end. Its child pytest hit its own inner --timeout=45 while importing openai on the loaded box. Re-run alone it passed 3 of 3 (blocked 0). It touches no unit module. `comm -13` against the base failed set is therefore empty. 3115 = the round-2 3109 + the 7 new unit nodes in the set - that 1 timing node.\n- **Committed flag-OFF ledger (R10):** recorded at the pre-unit base ac887e2d in a detached scratch worktree (records sha 331d21a9, 42 records). It is re-derived identically, 0 of 42 records differing, at base2 (a second base run), at HEAD (the committed test, 2 passed), and at current main 88cb78ae. It equals the earlier scratch-gate ledger record by record.\n- **Mutation (fix round 3, byte snapshots, every restore sha-verified):** 7 of 7 killed.\n  - X20, the camera success-site `_truth and` guard dropped: 1 red, the committed ledger test.\n  - X11b, the T7 row moved before the camera refunds: 2 red (the camera_exception and camera_exception_anon C3 nodes).\n  - X7, T3's `is False` weakened to falsy: 1 red.\n  - X14, `v < 0` weakened to `v <= 0`: 1 red.\n  - X15, `partial is True` weakened to truthy: 2 red.\n  - The old 042 guarantee restored, both as the whole pre-R12 header and as the single sentence: 1 red each.\n  - X26 is an accepted equivalent (see R1 above).\n  - The earlier rounds' 39 mutants were killed on the round-2 bytes. This round changes no app file (all seven app/script shas are unchanged), so they were not re-run.\n- **Lint:** ruff E9,F63,F7,F82 clean and py_compile clean on the 12 changed or new .py files. sqlfluff 4.3.0 is clean on 042 + rollback.\n- **Corpus:** scripts/verify_flag_byte_identity.py is N/A. This is not a price-path unit, and that harness drives extract_price_from_html only.\n\n### Fix round 3 (Fable rulings R10-R13, after adversary round 2 returned SOUND)\n- **R10: the C7 call ledger is now a committed pin.** Before this round, the HARD flag-OFF identity rule was guarded at one site (the camera success-site cost selector) only by a scratch file. The pin is made of three files:\n  - `tests/fixtures/_gen_w4_13_flag_off_ledger.py` is the generator. Its app imports are lazy, and it is regenerated only through a pytest probe.\n  - `tests/fixtures/w4_13_flag_off_ledger.json` holds the 42 records. Its `_meta` names the generator, the recording commit and the rule.\n  - `tests/test_w4_13_flag_off_ledger.py` pins exactly 42 scenarios and compares record by record.\n- **R11: four predicates pinned.**\n  - (a) The camera exception row joins the C3 set: a raising row builder still fires both camera refunds.\n  - (b) A stream terminal without a `success` key keeps HEAD's success row under flag 1.\n  - (c) A top-level total_cost of 0.0 is logged as 0.0, with metadata 0.02 beside it.\n  - (d) metadata.partial of 1 or 'yes' writes no partial note.\n- **R12: the 042 header states READ COMMITTED truth.** The old \"no row lands in between\" guarantee is gone. The header pin asserts the corrected sentence and the pause instruction.\n- No app, script or rollback file changed this round.\n\n### Stated limits\n- **CD-wave-diffs-02.** Under flag 1, for success:False terminals the stream LOG says failure while the BILL says delivered. This lasts until W4-13b.\n- **The NULL window.** Rows from 2026-09-03 to the flag-2 flip are never classified, and the readers count them as organic permanently. Measure the window at activation.\n- **D2.** The sync CONTENT_UNAVAILABLE exits (prefilter and moderation_api) carry no total_cost, so T1 logs 0.0 for a PAID moderation_api run.\n- **URL rows carry cost 0.0.**\n- **The readers have no .range().** The PostgREST max-rows cap can silently truncate any aggregate. The flag-2 canary on /admin/stats/popular can pass or fail on truncation alone.\n- **The pre-existing str(e) leak.** The leak of str(e) into error_message on POST/GET codeless failures and on the camera exception stays byte-identical with flag 1 OFF (pinned). Only flag 1 closes it.\n- **Harness marker.** Only eval_runner marks its traffic.\n- **Camera cost sum.** The camera's rung-1 sum of two finite floats can in theory round to inf, for example 1e308 + 1e308. That is not client-visible: log_search swallows the insert error.\n- **042 apply-time race.** Under READ COMMITTED, a probe row committed mid-script can make AFTER exceed BEFORE - EXCLUDED. The header says so and says to pause the eval runner.\n\n### Follow-ups\n- W4-13b: the stream meters success:False terminals.\n- W4-13c: the synthetic header for bias_matrix_probe.py, bundle_d_prod_smoke.py and run_validation_matrix.py.\n- PO-RECORDED-MEASURED-01c: the readers have no .range() (the PostgREST cap).\n- D2: add total_cost to the moderation-refusal and CONTENT_UNAVAILABLE exits.\n\n### CLAUDE.md corrections for the docs PR\n- Add the two flag rows and the knob.\n- Add the 042 row to the Migrations line: apply AFTER merge and BEFORE flag 2, with the eval runner paused, and turn flag 2 OFF before rollback. 039 stays reserved.\n- Record that the analytics readers filter only under flag 2.\n- Record that search_logs.error_message may carry 'partial:<stage>' on success rows, and the constants 'url_compare_exception', 'quick_compare_exception' and 'camera_exception' under flag 1.\n- Record that the flag-OFF identity of every log_search site is pinned by the committed ledger `tests/fixtures/w4_13_flag_off_ledger.json`. Any change to a flag-OFF log_search call is a deliberate regeneration through its generator, stated in the PR.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "deviations_from_spec": [
   "R10 names the recording point '88cb78ae-equivalent'. The fixture was recorded at the worktree's pre-unit base ac887e2d (= 61585c58 + the auth-only #202), and _meta.recorded_at says so. Equivalence to main 88cb78ae was measured separately: a detached 88cb78ae worktree re-derived the same 42 records, sha 331d21a9, 0 differing. I did not rebase, per R13.",
   "The generator's docstring probe and the committed test load tests/test_w4_13_measurement_truth.py's helpers and autouse guards, so the ledger stays byte-identical to the scratch C7 gate: sha 331d21a9 equals the red, green, r2-fixer and adversary ledgers. Because of this, the committed ledger test depends on that helper module.",
   "R11(a) 'both camera refunds': the reserved-credit refund (authed + metering) and the anon-credit refund (anon gate + metering + fingerprint) cannot fire on one request, so the camera exception joins _C3_SITES as TWO parametrized sites, camera_exception and camera_exception_anon. The raising builder is image_routes._log_cost_value, which is patched only for those two sites; T7 builds its cost with it.",
   "R12 wording: the header adds '(with created_at before that now())' to the tagged-but-not-counted sentence, because only such a row is tagged. It also names 'any other probe harness' beside the eval runner, since the spec's watch item records an out-of-tree harness. The pause instruction and the 're-run the census afterwards' fallback are as ruled. The AFTER-check line now says 'must equal (a + b) - c ..., or exceed it by the late-committed probe-string rows'. No census column changed, so the five-column census pin is untouched.",
   "The earlier 39-mutant table was not re-run: no app, script or rollback file changed this round, and all seven app/script shas equal the start shas. The 7 R10/R11/R12 mutants were run and killed.",
   "Gate environment: this box was heavily loaded (other agents' workflows), so the comm HEAD run's first pass lost chunks 00, 02 and 05 to the 60 s pytest-timeout. Those chunks were re-run at --timeout=600. One test_conftest_env_safety node then failed on its child's inner --timeout=45 and passed 3/3 alone. The Preserve unset run hit a Windows out-of-memory fatal once and passed on re-run. Preserve used --timeout=300 in all states.",
   "The comm set stays the recorded 119 files, as R13 says. The new ledger test is not in it: it is covered by the 4-state unit runs and the 21-file CI-order runs."
  ],
  "git_status_final": " M app/api/image_routes.py\n M app/api/text_routes.py\n M app/api/url_routes.py\n M app/services/analytics_service.py\n M app/services/database_service.py\n M app/services/sentry_service.py\n M scripts/eval_runner.py\n M tests/test_m18_preverdict_disconnect_refund.py\n?? migrations/042_search_logs_is_synthetic.sql\n?? migrations/rollback/042_search_logs_is_synthetic.sql\n?? tests/fixtures/_gen_w4_13_flag_off_ledger.py\n?? tests/fixtures/w4_13_flag_off_ledger.json\n?? tests/test_migration_042_search_logs_is_synthetic.py\n?? tests/test_w4_13_flag_off_ledger.py\n?? tests/test_w4_13_measurement_truth.py",
  "residual_risk": "- **Ledger coupling.** The committed ledger drives its scenarios through tests/test_w4_13_measurement_truth.py's helpers and tests/test_retro_w2_1's camera stubs. An edit to those helpers (for example a changed fixture payload) moves the recorded records and reddens the ledger test. That is by design, but it makes such an edit a deliberate regeneration through the generator.\n- **What the ledger does not cover.** It pins flag-OFF identity only over the 42 recorded scenarios. A new flag-OFF log_search site that no scenario drives would still need its own pin; the existing AST test 11 and pin 16 cover site presence and kwargs.\n- **Box-load dependence of the gates.** One comm node failed on a child-process timeout, chunks timed out at 60 s, and the Preserve unset run hit an OOM fatal. Each was re-run clean: the env-safety node 3/3 alone and Preserve 786. CI should see none of these.\n- **Scratch netguard.** All gates ran with the scratch netguard, because the hermeticity unit is not merged.\n- **Unchanged from earlier rounds:**\n  - the log/bill disagreement for stream success:False terminals until W4-13b;\n  - the readers have no pagination (01c);\n  - the NULL window;\n  - the D2 moderation cost of 0.0;\n  - the camera float-sum-to-inf corner.\n- **Not yet adversarially reviewed.** These exact bytes have had no fresh adversary: the test file 102dc202..., the ledger test 9dba0689..., the generator ee030aed..., the fixture 5b569af5..., the migration f56d3a29... and the migration test 92e28285....",
  "scratch_cleanup": "- **Scratch worktrees.** I created two detached scratch worktrees, <scratchpad>/w413fix3_base (ac887e2d) and <scratchpad>/w413fix3_main (88cb78ae). Neither had a node_modules junction (checked). Both were removed with `git worktree remove --force`, and `git worktree list | grep -c w413` = 0.\n- **No .env copied.** `find <scratchpad> -name .env | wc -l` = 0.\n- **No git writes.** I ran no commit, checkout, stash, reset, rebase or clean.\n- **Mutation restores.** Every mutation was restored from a byte snapshot and sha-verified, and all app/script shas equal the start shas.\n- **Ignored cache.** py_compile left an ignored tests/fixtures/__pycache__/ (gitignored, not a change).\n- **Artefacts** are in <scratchpad>/w413fix3/:\n  - pre/ (the pre-edit byte snapshots) and start_shas.txt\n  - regen.sh and test_w413_regen.py (the recording probe)\n  - ledger_fixture_base.json, ledger_base2.txt, ledger_main88.txt\n  - mutate3.py, mut3.log, mut3_out.json, snaps/\n  - run4.sh, pt.sh, gates.sh, gates.log, rerun.sh, rerun.log\n  - unit/ci/pres logs, comm_head/, envsafety.{1,2,3}.log\n  - pr_text.md (sha dd6f6f9e...)"
 }
}