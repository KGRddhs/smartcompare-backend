# TEST-HERMETICITY UNIT SPEC — issues #183, #184 (netguard 04c with curl_cffi), #185, #186 (session 68, written by Fable 2026-09-26)

Base SHA: `61585c58` (= origin/main). Worktree `C:/Users/SynAckITPC/Documents/AI/sc-hermetic`, branch `test/hermeticity-183-184-185-186`.
Test infrastructure only: NO flag, NO change under `app/` (if a fix seems to need an app-side seam, STOP and report — the orchestrator rules). Read the four issue bodies first (the orchestrator pasted them into the prompt).
Reference implementation of the guard: `C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/b1819d0c-d9c5-466a-ba0d-b82774c624db/scratchpad/netguard/qaren_netguard.py` (verified: blocks `socket.getaddrinfo`, `curl_cffi.requests.get`, a bare `Curl.perform`; two repo test files pass under it). Existing per-file guards to stay compatible with: `tests/test_retro_w0_4.py::_zero_network`, `tests/test_w49_extraction_catch_redaction.py` (autouse guard + `_price_service_globals`).

## 0. Invariants (binding)
1. Every fix is in `tests/` (+ `pyproject.toml` marker registration, `scripts/netguard_ratchet.py`, `.github/workflows/ci.yml` ratchet step). `git diff --stat -- app/` is EMPTY at the end.
2. Reproduce first: each of #183, #185, #186 is reproduced at `61585c58` in the CI order that exposes it (verbatim summary lines in the report) before it is fixed.
3. The guard imports no `app.*` code and installs before any app import (conftest import time), so `tests/conftest.py`'s credential neutralisation still runs first.
4. The free tier's pass/fail set under the new guard equals the set without it, except the nodes this unit fixes and any exception LISTED in the report (a hidden live dependency that cannot be fixed test-side goes to the orchestrator's ruling, never silently into a baseline).
5. No test is made to FAIL merely for attempting the network in this unit: the attempt raises `NetworkBlocked` into the code under test (fail-open code takes its offline branch); the REPORT and the RATCHET make egress visible and stop new offenders. Making attempts a failure is the follow-up campaign over the measured offender list.

## A. Netguard 04c (#184) — `tests/_netguard.py` + `tests/conftest.py`
- Port the reference plugin into `tests/_netguard.py`: `NetworkBlocked(OSError)`; loopback allowlist (`localhost`, `127.0.0.0/8`, `::1`, `::`, `0.0.0.0`, `''`, `None`, `testserver`, AF_UNIX paths; `QAREN_NETGUARD_ALLOW` extra hosts); patches `socket.socket.connect` / `connect_ex`, `socket.getaddrinfo`, `socket.create_connection`, `curl_cffi.requests.Session.request`, `AsyncSession.request`, `curl_cffi.Curl.perform`, `curl_cffi.AsyncCurl.add_handle` (the loopback handoff via a ContextVar so a loopback `Session.request` may `perform`, a bare `perform` outside such a call is blocked).
- Install from `tests/conftest.py` (module import time or `pytest_configure`), guarded so a double import never double-patches.
- Per-test attribution: an autouse fixture (or `pytest_runtest_call` hooks) records, per node id, the attempts made during that test (kind + target); the terminal summary prints `[netguard] blocked N attempt(s) from M node(s)` and the node ids with their first target; `QAREN_NETGUARD_REPORT=<path>` writes `{"blocked": n, "nodes": {nodeid: [targets...]}}` at session end.
- Opt-out: marker `allow_network` (registered in `pyproject.toml` `[tool.pytest.ini_options].markers` with a one-line reason) lifts the guard for that test only (a ContextVar/flag the guard checks). Tests carrying any `LIVE_TIER_MARKERS` (`live_unit`, `live_db`, `integration`) or `bench`/`live_prod` are treated as `allow_network`. When `tests._env_safety.live_mode_enabled()` is true (LIVE=1) the guard is not installed at all (the live tiers are the point of that run) — record it in the summary line.
- Ratchet: `scripts/netguard_ratchet.py <report.json> [baseline=tests/.network_attempt_baseline.txt]` exits 0 when the report's attempting node ids are a subset of the baseline (prints the shrink, if any), 1 on any NEW node id (prints them), 2 on a missing/unreadable report. The baseline is the measured list from §G (one node id per line, sorted, `#` comments allowed, generated — never hand-edited — by the same script with `--write`). CI `backend-tests`: set `QAREN_NETGUARD_REPORT` on the "Run unit tests" step and add a step "Network-attempt ratchet" running the script (it must run even when the test step fails: `if: always()` is WRONG here because a failed test step already fails the job — use a plain following step).

## B. #183 — `tests/test_hotfix_shopping_query_clean.py`
`TestSearchProductPricesIntegration` (three tests) does `monkeypatch.setenv("SERPER_API_KEY", "test-key")` + `importlib.reload(serper_service)` and leaves `serper_service.SERPER_API_KEY == "test-key"` for the rest of the process. Fix: snapshot `vars(serper_service)` (the values of every module global the reload rebinds — compute the changed-key set) before the reload and restore them in the fixture's teardown; OR replace the reload with `monkeypatch.setattr(serper_service, "SERPER_API_KEY", "test-key")` if the tests only need that global (measure which). Then AUDIT every `importlib.reload(` in `tests/` (13 files at base: test_backend_cleanup, test_cache_service_bounded_transport, test_fan_out_budget_env, test_hotfix_shopping_query_clean, test_limiter_endpoint_key, test_m18_openai_tpm_sizing, test_nasser_adapter, test_observability, test_password_reset_deep_link, test_platform_router, test_retro_w1_1, test_supplement_branch_genuine, test_w49_extraction_catch_redaction): table `file :: fixture/test :: module reloaded :: globals changed after teardown (measured by diffing vars() before/after the test) :: verdict (leaks / restores / harmless)`; fix every leaking one the same way.

## C. #185 — `tests/test_platform_router.py::test_does_not_import_price_service`
Runs the import-cycle proof in a SUBPROCESS: `[sys.executable, "-c", "import sys, app.services.platform_router as m; assert not any('price_service' in k for k in sys.modules), sorted(k for k in sys.modules if 'price_service' in k)"]` with `cwd` = repo root, `env` = the current (neutralised) `os.environ` + `PYTHONPATH` = repo root, `PYTHONIOENCODING=utf-8`, `timeout=120`, `check=True`, and the in-process `sys.modules` untouched (pin it: identity of `sys.modules["app.services.price_service"]` and of `app.services.price_service` equal before/after). Then verify `tests/test_shopify_discovery_l13.py::TestFetchShopifyPrice::test_fetch_uses_catalog_and_matches` passes UNDER THE GUARD in CI order (`tests/test_platform_router.py tests/test_shopify_discovery_l13.py` in one process) — at base it fails under a guard (real libcurl GET to shopalmoayyed.com). Also re-check `tests/test_w49_extraction_catch_redaction.py`'s `_price_service_globals` workaround still passes (leave it in place; note it can be simplified later).

## D. #186 — W4-3's file before W4-4's breaks three flag-ON nodes
Reproduce: `pytest tests/test_prescoring_showable_guard.py tests/test_partial_response_no_fabricated_scores.py` -> 3 failed / 139 passed at base (the three node ids in the issue). Root-cause: bisect to the W4-3 test(s) that leave the state (use the sentinel of §E on `scoring_service._scoring_service` and the singleton's `compute_scores` binding; or `--deselect` halving), name the exact leaked state (the issue's mechanism is UNVERIFIED — measure it). Fix hermetically at the SOURCE (the leaking test restores what it changed) and, if W4-4's spy patches a binding the orchestrator does not call, make it patch the one it does. Verify BOTH orders pass (pair run each way) and each file alone.

## E. Hermeticity sentinels — `tests/_hermeticity.py` + a conftest autouse fixture
Snapshot a SMALL set of known-fragile state before each test and compare after monkeypatch's undo (the conftest autouse fixture is set up FIRST and torn down LAST, so monkeypatch's restores have already run): (1) identity of `sys.modules.get("app.services.price_service")` and of `getattr(app.services, "price_service", None)` when loaded; (2) `serper_service.SERPER_API_KEY` value when loaded; (3) `scoring_service._scoring_service` identity and the identity of `compute_scores` resolved through it, when loaded; (4) the SET of `sys.modules` keys starting with `app.` (no deletions). On any difference: `pytest.fail("hermeticity: <test> leaked <item>: <before> -> <after>")` at TEARDOWN (an ERROR attributed to the leaking test, not to a later victim). The RED for #183/#185 is this sentinel ERRORing on the leaking tests at base; the green is those errors gone with the sentinel still armed. Keep the sentinel cheap (a few attribute reads; no imports it does not find already loaded).

## F. Pins — `tests/test_hermeticity_pins.py`
- Guard: each of the four curl_cffi points and `socket.getaddrinfo`/`connect` raise `NetworkBlocked` for a public host; loopback works (a `socket.socketpair()`; a local listen+connect; a loopback `curl_cffi.requests.Session().request` reaches `perform` — allowed — against a local `http.server` on 127.0.0.1); `allow_network` lifts the guard for that test (prove with a marker-carrying test that calls `socket.getaddrinfo("localhost", 80)` — no: with a NON-loopback getaddrinfo that must be blocked WITHOUT the marker and allowed WITH it; the allowed call may itself fail offline — assert only that `NetworkBlocked` is not raised); the report file is written with the expected shape (pytester subprocess run of a tiny test module that attempts one lookup, with a conftest that imports `tests._netguard`).
- LIVE=1 lifts: pytester subprocess with `LIVE=1` in env -> summary says the guard is not installed.
- Ratchet: exit 0 (same set), 0 (fewer, prints the shrink), 1 (a new id, prints it), 2 (missing report); `--write` regenerates the baseline sorted.
- Sentinel: unit-test the compare function with fake snapshots (each of the four items); one pytester run proving a leaking test ERRORs at teardown with the attributing message.
- #183/#185/#186: the fixed files pass alone, in CI order with their neighbours, and the sentinel stays silent over them.

## G. Measurement (red phase; the numbers decide the fix scope)
Run the FULL free tier in CI (alphabetical) order UNDER THE GUARD (the reference plugin via `-p qaren_netguard` at red time, the conftest guard at green time): `tests/ -m "not (live_unit or live_db or integration)" --ignore=tests/test_integration.py --timeout=60 -p no:cacheprovider -p no:randomly`, deselecting `tests/.pre_impl_failures.txt`, chunked into contiguous alphabetical chunks (write the chunk list) with `QAREN_NETGUARD_REPORT` per chunk; record: (a) the attempting node ids and their targets (this becomes `tests/.network_attempt_baseline.txt` at green after the fixes); (b) every FAILED/ERROR node not in `.pre_impl_failures.txt`; re-run each such node WITHOUT the guard in isolation to classify it as (i) a hidden live dependency (passes only with network), (ii) an order-dependence (passes alone with the guard), (iii) a real red at base (fails alone without the guard too). Report the three lists with counts. Session 66 measured 376 attempts from 111 nodes over a 321-file subset — expect the same order of magnitude.

## H. Gates (green)
(a) `tests/test_hermeticity_pins.py` green; the three fixed files green alone and in the CI-order pairs/triples named above (both orders for #186); `tests/test_ci_gates.py` green (it may pin the workflow file — read it).
(b) Mutation from byte snapshots: remove each guard patch point -> its pin red; remove the marker lift -> its pin red; remove the sentinel compare for each item -> the synthetic-leak pin red; revert each of the three fixes -> the reproduction red again.
(c) FULL free tier under the conftest guard in CI order (chunked as in §G): pass/fail set == base's set minus the fixed nodes (list any exception); the attempting-node set written to the baseline by `--write`; the ratchet exits 0 against it.
(d) `ruff --select E9,F63,F7,F82` + `py_compile` on every edited module (incl. `tests/conftest.py`, `tests/_netguard.py`, `tests/_hermeticity.py`, `scripts/netguard_ratchet.py`); CRLF hygiene; `git diff --stat -- app/` empty.
(e) pr_text: the guard contract, the marker, the LIVE lift, the ratchet, the measured offender count and the follow-up campaign, the #183 audit table, the #186 root cause, the four issue numbers ("Closes #183, #184, #185, #186").

## I. Assumptions
- Block-and-count (not fail) in this unit: it lands hermeticity for CI today without turning ~111 pre-existing offenders into a red wall; the ratchet stops growth; the campaign fixes the tail.
- A conftest-level guard beats `pytest-socket`: the repo needs the curl_cffi points, which no package covers, and no new dependency is allowed here.
- Subprocess for the cycle proof beats snapshot/restore of `sys.modules`: a fresh interpreter is the honest proof and cannot orphan bindings.
- Sentinels at teardown beat victim-side workarounds: the leak is attributed where it happens; `_price_service_globals` in the W4-9 file stays as belt-and-braces.
