export const meta = {
  name: 's68-red-hermetic',
  description: 'Session 68 unit 2 RED phase on Opus 5.5: test hermeticity - reproduce #183 #185 #186 in CI order, measure the full free tier under the curl_cffi-aware network guard (#184 / netguard 04c), root-cause #186, audit every importlib.reload in tests/, draft the pin file',
  phases: [
    { title: 'Red', detail: 'reproductions at 61585c58, full free-tier egress measurement under the guard, #186 root cause, reload audit, pin draft', model: 'claude-opus-5-5' },
  ],
}
const MODEL = 'claude-opus-5-5'
const DIR = 'C:/Users/SynAckITPC/Documents/AI/sc-hermetic'
const SPEC = DIR + '/.qa-s68/HERMETICITY_SPEC.md'
const MYSP = 'C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/b1819d0c-d9c5-466a-ba0d-b82774c624db/scratchpad'
const COMMON_PATH = MYSP + '/s68-common.txt'
const GUARD = MYSP + '/netguard/qaren_netguard.py'

const ISSUES = `ISSUE #183 (open): tests/test_hotfix_shopping_query_clean.py::TestSearchProductPricesIntegration (3 tests) does monkeypatch.setenv('SERPER_API_KEY', 'test-key') then importlib.reload(serper_service). monkeypatch restores the env var at teardown but the reloaded module global serper_service.SERPER_API_KEY (serper_service.py:46) stays 'test-key' for every later test in the process. In the single-process CI suite that file runs at ~39%; any later test that reaches the Serper path unstubbed then attempts REAL lookups of google.serper.dev (22 getaddrinfo attempts were counted by W4-9's socket guard at ~96%). Repro: run tests/test_hotfix_shopping_query_clean.py then tests/test_w49_extraction_catch_redaction.py (at the #178 head before its hermeticity fix) in one process. Fix: restore the module attribute in the fixture (snapshot before the reload and reassign after, or monkeypatch.setattr instead of a reload), and audit every other importlib.reload( in tests/ for the same pattern.

ISSUE #184 (open): every autouse socket guard in tests/ patches socket.socket.connect/connect_ex/socket.getaddrinfo. curl_cffi (used by price_service.curl_fetch_html, _fetch_shopify_catalog, noon_service) drives native libcurl and never touches the Python socket module, so those guards see nothing. Measured: the two test_r2e_quota_outage_on_the_phone_path_leaks_no_org_id[sync|sse] nodes in tests/test_w49_extraction_catch_redaction.py made 16 REAL fetches per run before #178's fix (4x /products.json on each of alhajisbahrain.com, bh.asgharali.com, en-bh.ajmal.com via _fetch_shopify_catalog; 2 per product on www.noon.com/_svc/catalog/api/v3/search) - the CI log carries four '[NOON] transport error: curl: (92) HTTP/2 stream 1 reset by server' records. The same blindness applies to every earlier 'measured: zero socket attempts' claim in the repo. Fix: the repo-wide guard (conftest-level, opt-out per marker) must also patch curl_cffi.requests.Session.request, AsyncSession.request, curl_cffi.Curl.perform and AsyncCurl.add_handle, and the 04c unit must re-measure the comm set with it. Severity high (tests reach retailers from CI).

ISSUE #185 (open): tests/test_platform_router.py::test_does_not_import_price_service (lines ~317-328) runs del sys.modules[m] for every name containing price_service as an import-cycle proof and never restores the original module. The next import builds a FRESH app.services.price_service module object (and rebinds the package attribute), but every module that bound price_service names BY NAME at its own import (structured_comparison_service.py binds fetch_shopify_price etc., plus test files that 'from app.services.price_service import ...' at collection) keeps the OLD function objects, whose __globals__ is the orphaned old module dict. Any later patch.object(price_service, ...) / monkeypatch.setattr(ps, ...) then patches the new module and silently misses the code that actually runs. Measured victims in the one-process CI suite (platform_router runs at ~364/608): the W4-9 quota_outage nodes (the _fetch_shopify_catalog stub was missed -> 6 real curl_cffi fetches; worked around in #178 by stubbing every reachable module dict), and tests/test_shopify_discovery_l13.py::TestFetchShopifyPrice::test_fetch_uses_catalog_and_matches (position ~488): imports fetch_shopify_price by name and monkeypatches the re-imported module; after platform_router it makes real curl_cffi GETs to https://shopalmoayyed.com/products.json and, under a guard, fails 'assert None is not None' - in unguarded CI it PASSES on the LIVE catalog, a hidden live-network dependency. Fix: run the cycle proof in a subprocess (sys.executable -c ...), or snapshot sys.modules + the package attribute and restore both in try/finally. Then size the exposure: grep patch.object(price_service, / monkeypatch.setattr(ps, against by-name imports of the patched callers.

ISSUE #186 (open): the post-rebase composition run of the W4-3/W4-4/W4-9 unit files failed 3 nodes only when tests/test_prescoring_showable_guard.py (W4-3, #177) ran BEFORE tests/test_partial_response_no_fabricated_scores.py (W4-4, #179). CI runs alphabetically (partial < prescoring), so both PRs were green; the coupling is latent. Repro (pinned venv, free tier): pytest tests/test_prescoring_showable_guard.py tests/test_partial_response_no_fabricated_scores.py -> 3 failed / 139 passed: test_03_flag_on_post_gather_partial_computes_real_scores ('compute_scores was never called on the post-gather stash' - spy list empty), test_03d_flag_on_post_gather_compute_threads_ctx_user_preferences ('expected exactly one compute_scores call, got 0'), test_14_extra_flag_on_compute_failure_degrades_to_null_not_error (scoring_v2 is a full real scoring block instead of None). Each of test_text_error_envelope_no_raw_exception.py, test_text_routes_error_mapping.py, test_platform_router.py, test_w49_extraction_catch_redaction.py before the W4-4 file passes clean, and the W4-4 file passes alone (92/92), so the state comes from the W4-3 file. Likely mechanism (UNVERIFIED): W4-3's tests around tests/test_prescoring_showable_guard.py:766-790 monkeypatch real_service.compute_scores (a spy on the scoring-service SINGLETON from _ss.get_scoring_service()) and scs.get_scoring_service; the W4-4 file's spy (monkeypatch.setattr(ScoringService, 'compute_scores', _spy) on the CLASS) resolves through a different binding, so after W4-3 ran, the post-gather compute path reaches an object the W4-4 spy never wrapped. Look for a cached singleton (scoring_service._scoring_service) or a binding that monkeypatch's undo does not reset. Fix: make both files hermetic against each other, then guard the order.`

const RED_SCHEMA = {
  type: 'object',
  required: ['unit', 'base_sha_confirmed', 'files_written', 'reproductions', 'root_cause_186', 'reload_audit', 'full_tier_measurement', 'attempting_nodes_count', 'hidden_live_dependencies', 'order_dependent_failures', 'real_reds_at_base', 'pin_draft', 'guard_compat_findings', 'measurements', 'spec_disagreements', 'questions_for_fable', 'git_status_final', 'scratch_cleanup'],
  properties: {
    unit: { type: 'string' },
    base_sha_confirmed: { type: 'boolean' },
    files_written: { type: 'array', items: { type: 'string' }, description: '"<path> <sha256>" per file (tests and .qa-s68 artefacts)' },
    reproductions: { type: 'string', description: '#183, #185, #186 each reproduced at 61585c58 in the CI order that exposes it: the exact command and the verbatim summary + failing node lines; for #183 the probe that shows serper_service.SERPER_API_KEY left as test-key; for #185 the id() change of the price_service module and the guarded shopify_l13 failure' },
    root_cause_186: { type: 'string', description: 'the exact W4-3 test(s) that leave the state, the exact leaked binding (measured, not guessed), and the minimal hermetic fix you propose for the green' },
    reload_audit: { type: 'string', description: 'the table over every importlib.reload( in tests/: file :: fixture/test :: module :: globals still changed after the test (measured by diffing vars()) :: verdict' },
    full_tier_measurement: { type: 'string', description: 'the chunk list, per-chunk summary lines with the [netguard] line, total nodes/passed/failed/errors, wall time' },
    attempting_nodes_count: { type: 'integer', description: 'distinct node ids that attempted the network under the guard (the future baseline size)' },
    hidden_live_dependencies: { type: 'array', items: { type: 'string' }, description: 'node ids that FAIL under the guard but PASS without it in isolation, each with its target host' },
    order_dependent_failures: { type: 'array', items: { type: 'string' }, description: 'node ids failing in the chunk run but passing alone under the guard' },
    real_reds_at_base: { type: 'array', items: { type: 'string' }, description: 'node ids failing alone without the guard too and NOT in tests/.pre_impl_failures.txt' },
    pin_draft: { type: 'string', description: 'tests/test_hermeticity_pins.py written per spec section F: its node list and the verbatim run at HEAD (every node fails for the stated reason: the module/function does not exist yet, asserted inside the test body)' },
    guard_compat_findings: { type: 'string', description: 'anything the reference plugin broke or missed while running the whole tier (loopback servers, socketpair, testserver, asyncio, existing per-file guards double-patching) with the fix you propose' },
    measurements: { type: 'array', items: { type: 'string' } },
    spec_disagreements: { type: 'array', items: { type: 'string' } },
    questions_for_fable: { type: 'array', items: { type: 'string' } },
    git_status_final: { type: 'string' },
    scratch_cleanup: { type: 'string' },
  },
}

const prompt = `You are running the RED phase of the TEST-HERMETICITY unit (issues #183, #184 = netguard 04c with curl_cffi, #185, #186) for the myez/Qaren backend. Worktree: ${DIR} (branch test/hermeticity-183-184-185-186 at 61585c58 = origin/main). Spec (authoritative): ${SPEC}. Shared rules: read ${COMMON_PATH} in full FIRST and obey every line. Reference guard implementation (read it; you load it with -p qaren_netguard for every run): ${GUARD}.

THE FOUR ISSUES (verbatim from GitHub, filed by the session-67 orchestrator):
${ISSUES}

CODE TO READ FIRST: tests/conftest.py (every autouse fixture, pytest_collection_modifyitems, the credential neutralisation), tests/_env_safety.py (live_mode_enabled, LIVE_TIER_MARKERS), tests/test_hotfix_shopping_query_clean.py (the three reload tests), tests/test_platform_router.py::test_does_not_import_price_service, tests/test_shopify_discovery_l13.py::TestFetchShopifyPrice, tests/test_w49_extraction_catch_redaction.py (its autouse guard at ~line 121 and _price_service_globals), tests/test_prescoring_showable_guard.py (~700-800, the spy setup) and tests/test_partial_response_no_fabricated_scores.py (~100-120, the class-level spy; the three failing nodes), app/services/scoring_service.py::get_scoring_service (~line 2929), tests/test_ci_gates.py, .github/workflows/ci.yml (the 'Run unit tests' step), pyproject.toml [tool.pytest.ini_options], tests/.pre_impl_failures.txt.

YOUR TASK, in order (this phase WRITES NO FIX; it measures, reproduces and drafts the pins):
0. Confirm HEAD 61585c58 and a clean tree; else STOP.
1. REPRODUCE at HEAD, under the guard, each in the CI order that exposes it, pasting commands and summaries: (a) #186: the pair run -> 3 failed / 139 passed (or whatever you measure); (b) #185: 'tests/test_platform_router.py tests/test_shopify_discovery_l13.py' in one process -> test_fetch_uses_catalog_and_matches fails under the guard (and passes alone); plus a scratch probe test file (written under your scratchpad, run after test_platform_router.py in one process) that records id(sys.modules['app.services.price_service']) before and after and shows the change; (c) #183: 'tests/test_hotfix_shopping_query_clean.py <probe>' where the probe asserts serper_service.SERPER_API_KEY is unchanged from its value at probe-collection time -> fails with 'test-key'.
2. ROOT-CAUSE #186: bisect to the W4-3 test(s) that leave the state (halve with --deselect, or add a scratch teardown probe that compares scoring_service._scoring_service identity and the identity of compute_scores resolved through get_scoring_service() and through the ScoringService class before/after each W4-3 test); name the exact leaked binding with evidence; propose the minimal hermetic fix (which file restores what) - do NOT apply it.
3. RELOAD AUDIT: for every importlib.reload( in tests/ (13 files at base) run the file with a scratch autouse fixture (a -p plugin or a conftest in a scratch copy - never edit tests/conftest.py in this phase) that snapshots vars() of the reloaded module before each test and diffs after teardown; produce the table the spec's section B asks for.
4. FULL FREE-TIER MEASUREMENT under the reference guard, CI (alphabetical) order, chunked into contiguous alphabetical chunks of at most 40 test files each (write the chunk list to ${DIR}/.qa-s68/chunks.txt), each chunk one process with QAREN_NETGUARD_REPORT=${DIR}/.qa-s68/netguard_chunk_<n>.json, --timeout=60, CI's deselects from tests/.pre_impl_failures.txt, --ignore=tests/test_integration.py, -m "not (live_unit or live_db or integration)" -p no:cacheprovider -p no:randomly -q -rfE. Save every chunk's full output under .qa-s68/. Then classify every FAILED/ERROR node not in tests/.pre_impl_failures.txt by re-running it (i) alone WITHOUT the guard (plain pytest, still no LIVE=1) and (ii) alone WITH the guard: hidden live dependency = fails with guard, passes without; order-dependent = passes alone with the guard; real red = fails alone both ways. Aggregate the attempting node ids across chunks into ${DIR}/.qa-s68/attempting_nodes.txt (sorted, unique) with a second file mapping node -> targets. Report counts and the lists. A node that HANGS to the timeout is re-run alone before it counts. Budget: this is the long step; do not skip chunks - if you must stop early, say exactly which chunks ran.
5. GUARD COMPATIBILITY: record anything the reference plugin broke while running the tier (a test that needs a loopback server, socketpair, a per-file guard that conflicts, a curl_cffi path that slipped through) with the fix you propose for the conftest port.
6. DRAFT tests/test_hermeticity_pins.py per spec section F: every pin imports tests._netguard / tests._hermeticity / scripts.netguard_ratchet INSIDE the test body (so collection succeeds) and asserts their presence with a message before using them - at HEAD every node must fail with that message (paste the run). Also write ${DIR}/.qa-s68/ci_order_set.txt (sorted): the pin file, the three fixed files, their CI-order neighbours named in the spec (test_shopify_discovery_l13, test_w49_extraction_catch_redaction, test_partial_response_no_fabricated_scores), tests/test_ci_gates.py, tests/test_conftest_env_safety.py and every tests/*.py that references socket.getaddrinfo or curl_cffi (they may double-patch).
7. Report per the schema. Leave the worktree with ONLY tests/test_hermeticity_pins.py (new) and the gitignored .qa-s68/ files; 'git status --short' verbatim. Never commit. Remove every scratch worktree; confirm no .env copy exists under your scratchpad.`

log('unit 2 RED: hermeticity in ' + DIR)
const red = await agent(prompt, { label: 'red:hermeticity', phase: 'Red', model: MODEL, effort: 'high', schema: RED_SCHEMA })
return { unit: 'hermeticity', red }
