ENVIRONMENT AND HARD RULES (violating any of these fails the task):
- Windows 11; Git Bash and PowerShell available. For Python always set PYTHONIOENCODING=utf-8. Never print non-ASCII test data; write it to files and read the files.
- Use the Write/Edit tools for every source, test and script file (Git Bash heredocs mangle backslashes and backticks; backticks inside a double-quoted bash string EXECUTE as command substitution). Backend files: core.autocrlf=true, the working copy is CRLF and the index LF - the Edit tool preserves line endings; a whole-file diff in 'git diff --stat' is a defect.
- NEVER run git commit, push, checkout, stash, reset, rebase or clean in any existing worktree. You write files; the orchestrator commits. The ONE allowed git write is creating your own DETACHED scratch worktree under your scratchpad folder ('git worktree add --detach <scratchpad>/<name> 61585c58') for a base or prototype run, and removing it afterwards with 'git worktree remove --force <path>'; confirm with 'git worktree list'. Never copy .env anywhere (the unit tests need no credentials; tests/conftest.py neutralises them).
- NEVER use 'git checkout -- <file>' to restore a file: work is UNCOMMITTED and checkout erases it. Before every mutation take a byte copy in Python (shutil.copyfile), restore from that copy, sha256-compare, stop on mismatch. Never run a recursive delete anywhere. Never run _proof/sweep2.py; the corpus root C:/Users/SynAckITPC/Documents/AI/sc-w0-load/_proof is READ-ONLY.
- Touch ONLY your assigned worktree and your own scratchpad folder. Other agents work concurrently in the sibling worktrees sc-w0-4efg, sc-hermetic and sc-auth-198 and in the clone C:/Users/SynAckITPC/Documents/AI/smartcompare - never write there; read-only 'git -C <dir> ...' is allowed.
- Do not install packages; do not edit requirements*.txt, lockfiles or tests/.pre_impl_failures.txt.
- Never run tests marked live_db / live_unit / integration, never set LIVE=1, never touch a real database, Railway, Supabase, Serper, OpenAI or the network. Never call any Railway MCP tool; never read, copy or print an environment value that looks like a key, token, credentialed URL or password.
- ALWAYS use the pinned venv C:/Users/SynAckITPC/Documents/AI/.venv-qaren/Scripts/python.exe (fastapi 0.141.1 / starlette 1.6.0 / pytest 9.1.1 / curl_cffi 0.16.1 = the CI pins), never the global python.
- EVERY pytest run goes through the process-wide network guard, loaded BEFORE conftest: PYTHONPATH must contain C:/Users/SynAckITPC/AppData/Local/Temp/claude/C--Users-SynAckITPC-Documents-AI/b1819d0c-d9c5-466a-ba0d-b82774c624db/scratchpad/netguard (prepend if PYTHONPATH already exists) and the command passes '-p qaren_netguard'. Command shape (bash): PYTHONIOENCODING=utf-8 PYTHONPATH=<netguard dir> <venv python> -m pytest -p qaren_netguard -p no:cacheprovider -p no:randomly --timeout=120 -m "not (live_unit or live_db or integration)" <files> -q ; paste the '[netguard] blocked N network attempt(s)' line with every summary you report. NEVER import app.* in any ad-hoc script before tests/conftest.py has run: a probe that needs app code is written as a pytest test file and run through pytest with the guard.
- Lint: <venv python> -m ruff check --select E9,F63,F7,F82 --no-cache <files> ; syntax: <venv python> -m py_compile <files>.
- The spec file is authoritative; read it in full ONCE at the start and again before reporting. Where it says STOP and report, stop and report. Report honestly: paste real summary lines; never claim a state you did not observe; a claim without a pasted measurement is a defect. Every file you wrote or edited is listed with its final sha256 (Python hashlib over the bytes on disk). Your final text is the return value (raw data for the orchestrator), not prose for a human.
