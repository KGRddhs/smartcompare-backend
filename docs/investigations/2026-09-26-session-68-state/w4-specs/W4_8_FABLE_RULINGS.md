# FABLE REVIEW RULINGS W4-8 (binding, 2026-09-26, session 68) - these OVERRIDE the spec body and the adversarial review where they differ

Verdict: APPROVED WITH THE CORRECTIONS BELOW. Base 61585c58 (re-anchor by symbol).

R1 (Half B is FLAGGED) - the review is right: for a safety list the risk class is newly ALLOWED content, and A_strict as written unblocks 18 of 22 natural intent phrasings including EN "buy silencer" / "silencer for sale". Half B ships behind `ENABLE_BLOCKLIST_PRECISION_V2` (default OFF, per-call reader in content_safety_service; BOTH lists are compiled at import and selected per call, so OFF is byte-identical). The variant = A_strict PLUS explicit intent-phrase entries so that every one of the 18 natural intent phrasings the review measured stays BLOCKED under the flag (EN: buy silencer, silencer for sale, silencer for rifle, 9mm silencer, silencer for glock, tactical knife for self defense, buy tactical knife; AR: the 11 the review lists), and the measured newly-allowed set is ONLY the household / automotive / EDC collisions (car-exhaust and generator silencer, Bosch silencer, Gerber vs Victorinox, water gun / Nerf / air rifle). Pin: a table of the 22 intent phrasings (blocked in both flag states) and the collision strings (blocked OFF, allowed ON). Drop the inert 'firearm silencer' entry. 'silencer suppressor' in tests/test_blocklist_collision_audit.py:91 is rewritten to 'gun silencer suppressor' (the entry that must block).

R2 (Half A: recall over precision when there is no signal) - the rung order becomes: (1) a pharmacy / household VETO fires -> NOT electronics: dose (reuse SUPPLEMENT_DOSE_RE, price_service.py:638), a plural count 'N tablets/tabs', or a context token from a small pinned list ('descaling', 'dishwasher', 'washing machine', 'detergent', 'chlorine', 'effervescent', 'chewable', 'vitamin', 'supplement', 'paracetamol', 'ibuprofen', 'panadol', 'adol', 'brufen', 'pharmacy') - each entry pinned by its own row or dropped; (2) else a hard device spec -> electronics; (3) else a device-vocabulary token -> electronics; (4) else (a bare 'tablet(s)' with no signal either way) -> ELECTRONICS (the spec's 'other' default is REJECTED: it dropped out-of-vocabulary device recall to 7/27 and bought an A2b call per chipless device compare). 'kids' leaves rung 1 (Panadol Kids tablets). A pharmacy VETO with a Supplements chip -> supplements; a pharmacy VETO with NO chip -> `supplements` directly (open question 4: YES - the veto IS the signal; chipless pharmacy pairs reach iHerb/bolo/nasser without a gpt-4o-mini call); pin both. Report device recall on the review's 27 out-of-vocabulary devices (must be 27/27) and pharmacy precision on the review's 25 extra strings.

R3 (corrections ADOPTED) - B3 -> 'drop katim sawt bunduqiya' (test 18 asserts blocklist_match on the AR rows); B4 -> test 17 only; one pin per corroborator alternative or delete the alternative; both L1 hashes stated (whole 413-map and the 360 subset); the head-records fixture carries `l1_queries`; content_blocklist.json edited IN PLACE with the Edit tool (raw UTF-8 Arabic, its own line endings - never json.dump); Windows-form absolute paths in every recipe; Preserve += test_category_dimensions, test_category_payload_completeness, test_explicit_pair_integration_mocked, test_latency_stack_i56, test_logger_no_query_leak, test_prescoring_showable_guard, test_rating_provenance, test_timeout_partial_integration; CI-order += those plus test_image_service_edges, test_m13_26_image_error_envelope, test_w49_extraction_catch_redaction; section 1c uses the prod-catalog counts (electronics 7 Shopify + 1 Algolia, other 13 Shopify under ENABLE_BH_GCC_CATALOG_SOURCES) and test 11 asserts membership, not `== []`; the client paragraph names the value-only renders (Home smart-pick pill via home_routes.py:497, HistoryScreen item.category).

R4 (out of this unit, recorded in pr_text as rows) - bare AR 'musaddas' (handgun) blocking massage gun / glue gun / caulk gun / water pistol / heat gun = PO-CATEGORIES-I18N-09b (variant AB reddens 4 pins :134/:192/:277/:652 - its own unit); the specs cache is NOT keyed on category (7 d L1 / 30 d L2): the canary uses `nocache` or never-seen pairs and a rollback leaves supplements-schema specs cached - stated limit + follow-up W4-8b (category in the specs cache key); the Arabic category tokens (PO-CATEGORIES-I18N-02, W4-14's Part D) = W4-8c with its own flag ENABLE_ARABIC_CATEGORY_TOKENS, after this unit.

R5 (corpus fixture) - YES: commit the 360-row authored corpus as `tests/fixtures/category_corpus_gcc_360.json` (+ the HEAD-records golden) after a scan that it carries no PII or credential-like strings (it is authored query text).

R6 (L2 surface) - measure `filter_shopping_items` over recorded shopping title+snippet rows if any are on disk (the review state folders); else state that only the 521 page titles were measured.

R7 (gates) - flag-OFF equality over the 360 corpus + the 35 tablet rows (the committed HEAD goldens), base -> head -> base2 in a detached scratch worktree, record by record; the flag-ON delta pinned to the exact set the R2 design yields (re-measure; the spec's 'exactly 1 row' will change with R2); the blocklist gate: both lists compiled, OFF == HEAD list byte-for-byte, ON = the R1 table; the 95-file comm set (+ the review's additions) base vs head, `comm -13` empty against the baseline + the 3 guard-caused SSRF nodes; the CI-order set in one invocation, flag unset and ON; ruff + py_compile; the corpus harness is blind (state it).

R8 (files) - app/services/extraction_service.py (the sweep + reader + veto), app/data/content_blocklist.json (a `v2` list alongside the existing entries - the loader selects), app/services/content_safety_service.py (the per-call select), tests + fixtures. Nothing else under app/ = STOP and report.
