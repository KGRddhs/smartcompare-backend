# FABLE REVIEW RULINGS W4-7 (binding, 2026-09-26, session 68) - these OVERRIDE the spec body and the adversarial review where they differ

Verdict: APPROVED WITH THE CORRECTIONS BELOW. Base 61585c58 (re-anchor by symbol). This unit shares `scoring_service.py` with W4-6a and `response_builder.py` / `structured_comparison_service.py` with W4-12: its green is REBASED onto whichever of those merges first and its unit files re-run before its last adversary.

R1 (flags) - THREE own flags ACCEPTED: ENABLE_RELIABILITY_UNCHECKED_ABSENCE (Part A), ENABLE_CONFIDENCE_SINGLE_COMPUTATION (Part B), ENABLE_FACTCHECK_SHOPPING_KEY (Part C); none rides #109 or #106. Readers per call, `confidence_factcheck_wiring_enabled` idiom.

R2 (Part A shape) - ONE-SIDED None (an unchecked product has NO reliability score; the pair-symmetric version erases a fully-verified product's win and manufactures 67.0/67.0 ties), AND the Part A reader returns False unless ENABLE_MISSING_DIM_RENORM is ON (coupled, pinned both ways, the W4-6a T pattern): under the legacy `MISSING_SCORE = 50` path a one-sided None would let unchecked beat measured-bad (the #101 inversion, measured by the review), under renorm the missing dimension is excluded for the pair and never scores. Consequence, stated in pr_text and pinned by A6: zero-bucket vs 8/0/0 -> reliability excluded, the crown falls to the other dimensions (the honest cost of a dimension one side cannot be measured on); W4-6b may revisit once #101 is answered. Activation order: W4-6b's order-symmetric tie-break -> ENABLE_MISSING_DIM_RENORM -> Part A. Add the composition pin with ENABLE_BUNDLE_C_SCORING=true.

R3 (Part B freshness) - the spec's source stands: a REAL cache hit via `_compute_cache_observability`, not `from_cache`.

R4 (Part B count semantics - the review's over-claim risk) - the count is NOT the raw Serper Shopping row count. `_shopping_listing_count` = the rows that survived identity matching for this product (the candidates `extract_price_from_shopping` accepted, i.e. title-matched rows), stashed at the same site; the measurer finds the honest denominator and pins it. AND the price leg can never read `strong`/High beside a PENDING shown price: when the product's shown price is pending (`unavailable` / `amount None`), the leg is capped at `weak` regardless of the count (this answers open question 5 in the conservative direction; Ahmed may loosen it later - record it in DECISIONS). The supplements canary line becomes `<= 1`. The client plural (`Checked across 1 retail sources.`) is a mobile-lane follow-up; state it.

R5 (Part B leak) - ADOPT: pop `_shopping_listing_count` from every product dict in the builder before `result['products'] = product_data` (unconditional pop, byte-identical OFF); B7 asserts on `result['products']`, `overview.products` and the full `json.dumps`; mutation M-B8. A partial (early-buffer) never reaches the stash -> count 0 under Part B ON: state it.

R6 (Part C scope) - route ALL FOUR wrong-key readers through `_shopping_cache_key` under the flag: :5478, :5499 (`collect_retailer_ratings`), :5517 (`_get_verified_rating` - the cache LOOKUP only; `full_name` stays the rating query string), :5718; C7 RED (`review_sentiment_consistent` None -> True on the mismatching shapes) + C7b PIN; the shape table carries all 13 measured shapes (the 3 extra: vision+variant, brand casing, lower-case variant). The `_price_fallback_on_miss` key drift on the PRICE path is OUT of this unit: file it as follow-up W4-7d in pr_text (a parked price recovered for one shape and lost for three - a genuine capture defect, its own flagged unit).

R7 (test corrections, ADOPTED) - relabel A9, A10, A11 (ON half), B7, B8, B9, C4, C6 as RED (or split PIN/RED halves; every PIN green at HEAD in the red report); M-B5 kills B5 only; add M-B8 and, with R6, M-C4 (switch :5478/:5718 but not :5499 -> the sentiment test red); A6 pins the fully-verified row under R2; CI-order set += `tests/test_multiplicity_discriminator_policy.py` and the `_get_price` / `_fetch_product_data` source scanners the review names (test_wall_caps_i57, test_genuine_price_priority, test_discovery_window_i54, test_category_aware_discovery_i55, test_phase1_per_race_timeouts, test_response_builder_phase1_none_guard, test_price_timeout_returns_parked); the Part C.1 helper refactor (unflagged string-identity extraction) is pinned over all 13 shapes.

R8 (placement / docs) - ENABLE_FACTCHECK_HONEST_ABSENCE gets its own window right after #106 (spec); add #108 ENABLE_CITATION_RUBRIC_V2 to the HARD ACTIVATION ORDER docstring in this unit (docstring only, PO-FACTCHECK-CONFIDENCE-10).

R9 (gates) - the equality gate as the spec defines it (records dumped sort_keys with `metadata.timestamp` removed, base -> head -> base2 in a detached scratch worktree, three new flags unset, record-by-record equality) PLUS `git diff --stat 61585c58 -- app/services/price_service.py app/services/fact_check_service.py` EMPTY; the 238-file comm set (+ the 13 files that grep `SmartCompareApp`) base vs head, `comm -13` empty; the CI-order pin set in one alphabetical invocation in every flag state the spec names; ruff + py_compile. Every response-shape claim is checked against the phones on 561d2cba (no new key on the wire; the pop in R5 is what keeps that true).

R10 (files) - app/services/scoring_service.py (Part A + B readers/legs), app/services/response_builder.py (Part B recompute + the pop), app/services/structured_comparison_service.py (Part B stash, Part C readers, the `_shopping_cache_key` helper); tests: the new files the spec names + the CI-order additions. Anything else under app/ = STOP and report.
