# FABLE REVIEW RULINGS W4-12 (binding, 2026-09-26, session 68) - these OVERRIDE the spec body and the adversarial review where they differ

Verdict: APPROVED WITH THE CORRECTIONS BELOW. Base 61585c58 (re-anchor by symbol). Shares response_builder.py / structured_comparison_service.py with W4-7 (rebase whichever lands second).

F1 (the margin flag) - ACCEPT ENABLE_SINGLE_VERDICT_MARGIN, default OFF, the PRE-nudge calibrated gap in `overview.winner.margin`, `scoring_v2.win_margin` and the SSE verdict margin (ties -> 0). K2 ADOPTED: delete section 4.6 reason (c) and the OTA coupling as a REASON; OWN the intra-payload fact that under the flag `scoring_v2.win_margin` != the nudged `overall_score` gap on nudged / tie rows (4/23) and on W4-4's fabricated block - pr_text states it and W4-12b (tie SCORES, phones read `winner_idx` first) is the follow-up that closes it. K12: read the flag ONCE in scs and pass it into the builder (private kwarg). Flipping with the mobile CTA-threshold OTA stays a RECOMMENDATION, not a precondition.

F2 (retailer_quotes) - leave the verbatim third-party snippets unscrubbed (pin B4). The rendered `review_praise` leak IS folded in: `strip_score_internals` over the review_summary highlights/consensus BEFORE `build_review_praise` on BOTH the canonical (rb:1863) and the alias (rb:2009) path, the alias replacement placed AFTER `pd['review_praise'] = _safe_review_praise(pd)` (K7), pins B5 (reviews None / non-dict review_summary untouched, no exception) and B6 (alias praise == canonical praise). Unflagged: removal of text the payload already forbids; 0/8 stored praise lines change (measured).

F3 (scope split) - W4-12b tie scores, W4-12c priority_match revival (dead dict-vs-string compare; its own flag), W4-12d price_tiers raw keys, W4-12e stale youtube_review_signal on the alias, W4-12f the camera 'Identified: {brand} {name}' doubled brand (image_routes.py:376) - one follow-up issue with five rows, filed after merge; none in this unit.

F4 (the unflagged set) - CONFIRMED: the SSE pre-verdict scrub, the alias review_summary scrub (+ F2's praise scrub), the display-name dedup at home_routes:527/:528 and profile_routes:102/:103 (:456 untouched), the loser-only verdict_short guard WITH K10 (mask the LONGER normalised name first; D3 pins both directions), `metadata.verdict_scrubbed` + one `[VERDICT_SCRUB]` WARNING (written where the dict exists - a local after ~rb:1726, K-drift). The published scrub rate excludes W4-4 honest partials (state it).

F5 (product calls) - recorded for DECISIONS_AHMED, not this unit: gpt-4o priority=high spend for a verdict sentence FactualVerdict out-renders 23/23; the backfill of 17 stored score-leaking `overview.winner.reason` values served by the unauthenticated GET /share/{token}.

F7 (corpus) - YES: commit the de-identified copy (product names only, sha noted) as `tests/fixtures/w412_corpus_min.json` so the payload-equality gate is reproducible.

K1 (flag-ON collateral) - `tests/test_prescoring_showable_guard.py` joins the CI-order set. The 22 W4-4/W4-3 nodes that assert `win_margin == 1` / the raw margin and the 1 test_home_routes node pin the margin-flag-OFF / caption-flag-OFF world: the green adds `monkeypatch.delenv` of the NEW flag to exactly those nodes (listed by id in pr_text) and W4-12 adds its own flag-ON twins; any other red in the flag-ON runs fails the gate.

K3-K6, K8-K9, K11 - ADOPTED as written: D6 feeds a line1 (and separately a stripped reason) equal to the winner's normalised name and expects None; D1's flag-ON half names only the loser; D4 carries both line1 and a clean reason; C4 includes the brand-repeating string-shaped case (TOM FORD / TOM FORD OUD WOOD -> recent_winner); D7/F7 relabelled RED, new symbols imported inside test bodies; gate 3's allowed deltas add `metadata.verdict_scrubbed` on the leaky SSE complete/settle_complete frames and caption-ON verdict_short on 19/22 eligible rows; A5 inputs single-spaced, plus a PIN that a double-spaced clean reason gives verdict reason == complete reason (strip_score_internals normalises whitespace); F3 gains the SSE tie case (57.3/57.3 -> verdict == complete == scoring_v2.win_margin == 0) + the 'SSE margin post-nudge' mutation; the four K9 mutation rows; E3 uses a score-leaking winner_reason on the GPT1/scoring0 mismatch.

R-idempotence - double application (scs then builder) is byte-safe only because `strip_score_internals` is idempotent: add a PIN that `strip_score_internals(strip_score_internals(x)) == strip_score_internals(x)` over the unit's fixtures, so a future non-idempotent regex change reddens instead of silently splitting verdict from complete.

R-gates - payload-equality gate as the spec defines it (corpus rebuild + synthetic SSE frames + smart-pick/profile rows; base -> head -> base2 in a detached scratch worktree; head differs ONLY at the enumerated unflagged deltas as amended by K6/F2; each flag ON alone adds only its own fields); the 209-entry comm set base vs head (base measured ZERO failures), `comm -13` empty; the CI-order set (13 files with K1) in three runs (unset / margin ON / caption ON) with the K1 expected-red sets applied through delenv; ruff + py_compile; the corpus harness is irrelevant (no price module moves - state it).

R-files - app/services/response_builder.py (scrub_verdict_prose helper, praise scrub, alias scrub, margin), app/services/structured_comparison_service.py (the pre-verdict call at :4781 + the margin kwarg), app/api/home_routes.py (:527/:528 dedup + the caption flag), app/api/profile_routes.py (:102/:103); tests (the new file, the K1 delenv additions, the fixture). Anything else under app/ = STOP and report.
