# FABLE REVIEW RULINGS W4-14 (binding, 2026-09-26, session 68) - these OVERRIDE the spec body and the adversarial review where they differ

Verdict: APPROVED WITH THE CORRECTIONS BELOW. Base 61585c58 (re-anchor by symbol). ORDER: W4-14 lands AFTER W4-11 (both edit the verdict prompt path; W4-11's render-digest fixture is regenerated by this unit's locale line, with the diff explained in the PR). Client unit: the worktree needs the node_modules JUNCTION (never removed recursively); every client change is proven by the FULL jest suite + `node node_modules/typescript/bin/tsc --noEmit` (version printed), never `npx` alone.

Q1 - Part D (Arabic category tokens) does NOT move into W4-8 and is NOT in this unit: it becomes W4-8c with its own flag ENABLE_ARABIC_CATEGORY_TOKENS after W4-8 (its 3 wrong rows are W4-8's ambiguity class).
Q2 - PersonalizationChip `dim_key` = follow-up W4-14b. Q3 - `lang` is threaded for the 3 text compare calls only; the ONLY other live client compare route is `/image/identify` (the review corrected the inventory: `/url/compare` and `/text/quick` have no client callers) = W4-14b too; state it. Q4 - `category_switched` disclosure = Ahmed's product call (DECISIONS). Q5 - complete the six Arabic referral-expiry forms WITH correction C2 (the same `{{...}}` set in every AR/EN twin; model EN `_zero`/`_two` on history.hero.count_zero/_two); keep the keys. Q6 - the 51 Arabic labels ship as the proposal; the PR marks them "native review before the OTA" (the OTA is Ahmed's lever). Q7 - CONFIRMED as a hard precondition for flipping ENABLE_ARABIC_VERDICT_OUTPUT, owned by follow-up W4-14c: the Arabic-aware audit of the English-only guards (`strip_score_internals`, `validate_verdict`, `critique_verdict`, `reconcile_winner_prose`, FE `SCORE_INTERNALS_RE` / `BANNED_PATTERN` at ResultsContent.tsx:371 / RunnerUpWinsCard.tsx:90), the English replacement paths (`_QUALITATIVE_WINNER_REASON`, `deterministic_verdict_fields`, `_deterministic_partial_verdict`, the COMPARISON_GENERATION_ERROR fallback), `_PRICE_ADJECTIVE_RE`, and the Arabic `max_tokens` measurement - ALL listed in section 10 as the flip's preconditions. Q8 - delta_text and the factual verdict lines = W4-14d.

Directive (Part B) - the `_ARABIC_OUTPUT_DIRECTIVE` includes the copy-policy Arabic bans and scary terms from `src/i18n/.copy-policy.json` as forbidden words, names `independent_winner_basis` explicitly in its field list, and is pinned NON-EMPTY with its required fragments (C3: mutation M18 = the directive emptied/truncated must redden; test 30 asserts `system != base` as well as `system == base + DIRECTIVE`). C4: test 28 covers `lang='AR'` and `' ar-BH '` -> `output_lang='ar'`. C10: the 18-cell byte-identity matrix gains cells with a `demographics_profile` (ENABLE_COHORT_PERSONALIZATION on) - the Arabic users' real path. Persisted Arabic verdicts re-served to English viewers through History/share is a stated limit of the dark flag's activation note.

Client (Part A, unflagged, OTA-gated) - CONFIRMED unflagged: EN renders are byte-identical (72/72 rows; 44/44 snapshots) and only Arabic users see the translation; the build-time `features.ts` flag is not required. Corrections ADOPTED: C1 test 15 strips comments and excludes `src/**/__tests__/` before asserting the locale argument on the 2 code sites; C5 a per-call language-read pin (ar, en, ar across three calls on one imported api module) = M19; C6 the M2 kill list = the 4 measured suites; C7 client Preserve = 28 suites / 269 tests / 6 snapshots (or name the 2 missing suites) plus `src/utils/__tests__/formatNumber.test.ts` and `currencyDisplay.test.ts`; the limited-data row is backend-unreachable (`data_insufficient` has 0 emitters) - reframe; test 20 pins `compareTextPair` (0 callers) - replace with a `streamComparison` REST-path pin; the afterEach rationale corrected; the eslint-ceiling sentence removed.

Part C (backend fence test) - it must run in the BACKEND CI job (it is the only thing keeping EN catalog labels equal to the backend labels once the 51 EN keys exist): a plain `tests/test_*.py` reading `SmartCompareApp/src/i18n/{en,ar}.json`; add it to the `grep -rl SmartCompareApp tests/` mobile-gate rule in pr_text.

Comm sets (C8) - backend: the spec's regex PLUS `structured_comparison_service` (the 104 files that import it) - the FULL union, run base vs head under the guard, `comm -13` empty against the baseline + the guard-caused SSRF nodes; marker count corrected (13 files carry live markers); client: the grep set PLUS `src/**/__tests__` (sentry.test.ts references streamComparison), then the FULL jest suite (the arbiter) with the HEAD baseline 320 passed / 3 skipped / 3150 tests / 44 snapshots, no `-u`. Backend Preserve += the 6 missing `generate_comparison` drivers (test_comparison_quality_v2, test_diagnostics_flag_gated, test_extraction_pros_cons_diagnostic, test_guidance_insights, test_m18_openai_tpm_sizing, test_w49_extraction_catch_redaction). Anchors: text_routes.py:835.

Gates - the prompt-capture byte-identity gate (18 + demographics cells, base at 61585c58 in a detached scratch worktree -> head flags OFF equal on every cell -> head flag ON None/en equal, ar == base + DIRECTIVE exactly -> base2); the client EN-identity gate + full jest + tsc by path; the comm sets above; the CI-order pin sets (backend and client) per section 9.5 as amended; ruff + py_compile; eslint by path with 0 errors.

Files - client: src/utils/dimensionLabel.ts (new), the DimensionBars / RunnerUpWinsCard routing, src/i18n/en.json + ar.json (51 keys + the referral plural forms; NO other key moves), src/services/api.ts (lang on the streamComparison REST + SSE requests when i18next.language is Arabic, omit-when-unset), tests; backend: app/api/text_routes.py (lang field/query + normalize), app/services/structured_comparison_service.py (the conditional kwarg threading), app/services/extraction_service.py (the reader, the directive, the generate_comparison suffix), tests + the fence test + the regenerated W4-11 render fixture. Anything else = STOP and report.
