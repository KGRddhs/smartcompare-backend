# FABLE RED-GATE RULINGS - W4-13 measurement truth (2026-09-26, session 68; binding for the green, the adversary and the fixer; they sit ON TOP of .qa-s68/specs/W4_13_FABLE_RULINGS.md and the spec's review section)

RED GATE: PASSED at ac887e2d. 95 reds all on assertions in every ambient flag state, 49 pins green, 56 kills (8 at HEAD + 48 on the prototype) each restored sha-verified, prototype satisfies every row (144/144 in all four flag states), the C7 ledger equality gate base -> head -> base2 sha 331d21a9... identical record by record (42/42), CI-order set 20 files 612 passed on the prototype in all four states, Preserve 34 files 786 passed both states, sqlfluff clean on 042 + rollback. The seven spec_disagreements are ACCEPTED as measured (Sentry function is `_scrub_request_region`; C1's coalesce + probe-#1 exclusion re-measured 11,429 tagged / 295 excluded / organic 1,808 rows 82.08 % p50 20,937 ms; M3 vs M3c split; the client_gone twin message; `_is_synthetic_request` lives in database_service with `_search_log_synthetic_kwargs` in text_routes; the 119-file comm set; Preserve 786/34). The prototype design (.qa-s68/proto_W4-13.patch) is ACCEPTED as the design - re-derive it, do not copy bytes.

R1 (Q1 - T1 error_message hygiene, IN SCOPE) - a search_logs row is a persistence surface and W4-9's rule ("never ship str(e)") applies to it: under flag 1 (ENABLE_SEARCH_LOG_TRUTH) every failure row's error_message passes through the SAME codeless floor text_routes already applies to the response (reuse that helper - do not write a second allowlist): a result WITH a code logs its message unchanged (constants by construction); a CODELESS result whose message is not an allowlisted sentence logs INTERNAL_ERROR_FRIENDLY_MESSAGE. This covers T1 (POST/GET failure), T4 (stream error event, already constant), T8 (url failure/exception) and T9 (/text/quick) uniformly. Flag OFF: today's `result.get('error')` byte-identical. Pin: a codeless failure whose error text is 'boom sk-live-000' -> row error_message == INTERNAL_ERROR_FRIENDLY_MESSAGE under flag 1 and == the raw text under flag OFF (that OFF pin documents the pre-existing leak, never fix it unflagged - it is the flag's reason); mutation M30b: bypass the floor -> red.

R2 (Q2 - C3 ordering) - CONFIRMED: the existing rows whose kwargs change (T1/T4/T6/T7) move AFTER their refund ONLY under flag 1; flag OFF keeps HEAD's log -> refund order byte-identically (the ledger proves it). State in pr_text that flag 1 also reorders the fire_and_forget labels on those paths with no response/status/SSE change.

R3 (Q3 - camera cost fallback) - CONFIRMED: rung 1 top-level total_cost (finite, >= 0), rung 2 metadata.total_cost as-is, rung 3 vision_cost alone (the vision call was paid; never 0.0 when the vision cost is known, never a double count). Pin all three rungs; mutation: drop rung 3 -> red.

R4 (Q4 - T8 url rows) - CONFIRMED both: pass user_id (the route has get_optional_user since W2-1; anonymous -> None like every other site), and `_url_log_query` drops query, fragment AND userinfo (`user:pass@`) while keeping scheme, host, an explicit port and the path. Pin the userinfo drop and the port keep (mutation: keep userinfo -> red).

R5 (Q5 - 042 census) - CONFIRMED the operator-instruction variant, NO DO block: the Supabase SQL editor already wraps one pasted multi-statement script in ONE transaction (the CLAUDE.md migrations gotcha), so "census + body + AFTER check in ONE run" IS the same-transaction guarantee; a RAISE-on-mismatch block would add an unverified `$$` body and change the apply-pack style 037/040/041 established. The header must say so in one sentence and give the exact three numbers to paste back (BEFORE tagged-window count, excluded probe-#1 count, AFTER = BEFORE - excluded), plus the 11,429 / 295 / 1,808 expectations from the 2026-09-02 pull as the sanity anchor (live counts will be >= those because of rows written since the pull; the header states that too).

R6 (Q6 - CI-order set) - CONFIRMED at 20 files (LC_ALL=C sorted), run in ONE process per flag state (unset / flag 1 / flag 2 + token / both + token) with CI's deselects; it is also the ship_checks set.

R7 (gates for the green, all binding) - unit files (3) in the four flag states; the mutation table of the red (its 48 prototype mutants + M30b + R3 + R4 rows) re-run on the GREEN bytes from byte snapshots, every restore sha-verified; the 119-file comm set base (ac887e2d, detached scratch worktree, flags + token unset) vs head under the conftest guard, `comm -13` empty against .qa-s68/specs/W4_13_comm_base_failed.txt + the 4 guard-caused SSRF nodes + the memo timing flake (3 isolated re-runs before it counts); the 34-file Preserve set both states; the C7 ledger base -> head -> base2 record-by-record equal with flags + token unset, and the ON ledger recorded (status/body/SSE identical to OFF in all 42 scenarios - that is a HARD requirement: flag 1 and flag 2 may never change a client-visible byte); ruff E9,F63,F7,F82 + py_compile on every changed .py; sqlfluff on 042 + rollback; `git diff --stat -- app/` touches ONLY the ruled files (image_routes, text_routes, url_routes, analytics_service, database_service, sentry_service) + scripts/eval_runner.py + the two migration files + tests. Run WITHOUT -p qaren_netguard once the hermeticity unit is merged; until then use it (state which).

R8 (pr_text must carry) - the flag rows for both flags (default OFF, read per call, `.strip().lower()`), the SEARCH_LOG_SYNTHETIC_TOKEN knob (least privilege, rotation = set a new value on Railway + the eval_runner env), the APPLY_PACK row for 042 (apply AFTER merge, BEFORE flag 2; flag 2 OFF first on rollback), the activation order (flag 1 alone -> 042 -> flag 2 + token -> the 1-minute canary: rows written after the flip > 0), the KPI series change under T5/T3 (abandoned streams and moderation/timeout terminals count as failures - measured at the flip), the CD-wave-diffs-02 caveat (W4-13b), the NULL window statement, Q5 anon_id as Ahmed's row, the follow-ups W4-13b (stream meters success:False terminals), W4-13c (the other three prod-HTTP harnesses' synthetic header), PO-RECORDED-MEASURED-01c (readers without .range()), the moderation-refusal total_cost limit (D2), and the pre-existing str(e)-in-error_message leak that R1 closes only under flag 1.


# FABLE POST-GREEN RULINGS - W4-13 (2026-09-26, after reading the green report; binding for the fix round that follows the adversary)

R9 (R1 scope, two corrections of the green's literal reading) - the floor exists to keep EXCEPTION and PAYLOAD-derived text out of search_logs; it is not a rewrite of route-owned constants. (a) The url `<2 products` failure row logs its own literal `Could not extract both products` VERBATIM (the message is a code constant in url_extraction_service.py:596; no exception text can reach it): revert the red's test 28 [few_products] expectation to that literal; the site passes its literal to the row builder without the floor (or the builder floors only result/exception-derived messages - either shape, pinned: a codeless url failure whose message is the literal logs the literal; one whose message is anything else logs INTERNAL_ERROR_FRIENDLY_MESSAGE; mutation: floor the literal -> red). (b) T7, the camera EXCEPTION row, DOES take the floor under flag 1 - "every failure row" included it; an exception message is exactly the class R1 targets (R-METER measured an OpenAI key tail in a camera str(e)); under flag 1 the row logs the constant `camera_exception` (mirroring `url_compare_exception` / `quick_compare_exception`), flag OFF unchanged (today's str(e) - the pre-existing leak the flag closes); pin both states, mutation: str(e) under flag 1 -> red. T3's no-op floor on coded terminals and T9's floor stay as built.


# FABLE POST-PIPELINE RULINGS R10-R13 - W4-13 (2026-09-26 ~14:10, after adversary r2 returned SOUND with three minors and six prove-nothing rows; binding for fix round 3)

R9(a)/(b) were applied in fix round 1 (verified in the round-1 fixer report and the round-2 ledger: url.few logs its literal, camera.raise logs `camera_exception`). What remains is that the HARD flag-OFF identity is guarded at one site only by the SCRATCH ledger, plus four unpinned predicates. Three of the six prove-nothing rows and the 042 sentence are fixed here; X26 is an accepted equivalent.

R10 (the C7 ledger becomes a COMMITTED pin) - add `tests/test_w4_13_flag_off_ledger.py` + `tests/fixtures/w4_13_flag_off_ledger.json`: the 42 OFF scenarios of scratchpad/w413/test_w413_ledger.py (fixed X-Request-ID, duration_ms normalised, flags + token unset) recorded once as the fixture and asserted RECORD BY RECORD (not by sha alone) with both flags and the token delenv'd; the fixture header (a `_meta` key) names the generator `tests/fixtures/_gen_w4_13_flag_off_ledger.py` (lazy app.* import, run through a pytest probe like W4-6a's), the base sha 88cb78ae-equivalent at which it was recorded and the rule 'flag OFF is byte-identical to the pre-unit kwargs at every log_search site'. Mutation X20 (the camera success-site `_truth and` guard dropped) must redden THIS committed test; also pin that the fixture carries exactly 42 scenarios. The ON ledger stays scratch (it changes by design).

R11 (the unpinned predicates) - (a) X11b: the camera T7 exception row joins `_C3_SITES` (raising row builder -> both camera refunds still fire); (b) X7: a stream terminal WITHOUT a `success` key under flag 1 keeps HEAD's success row (T3's `is False` is deliberate - pin it); (c) X14: a top-level `total_cost` of exactly 0.0 is a valid cost and never falls through to `metadata.total_cost` (pin with metadata 0.02 -> logged 0.0); (d) X15: `metadata.partial` truthy-but-not-True (1, 'yes') is NOT a partial -> no `partial:` note (pin). Each with its mutation from a byte snapshot. X26 (T4 floor on `error_payload`) is an EQUIVALENT mutant because the W4-9 SSE floor already mutated that dict in place - state it in pr_text and keep the row floor (defence in depth).

R12 (042 header truth under READ COMMITTED) - replace the 'no row lands in between' guarantee: each statement takes its own snapshot; `now()` is fixed at transaction start, so a probe-string row committed between the census SELECT and the UPDATE is TAGGED but NOT COUNTED, and the operator's AFTER may exceed BEFORE - EXCLUDED by exactly that many rows; instruct: pause the eval runner (the only producer of the 13 probe strings) for the apply, or accept a small positive difference and re-run the census afterwards. The header pin asserts the corrected sentence and the pause instruction; mutation: the old guarantee restored -> red.

R13 (gates) - the four flag states over the unit files (now four: + the ledger test); the ledger test joins .qa-s68/ci_order_set.txt (21 files) and the CI-order set runs in ONE process per state; the R10/R11/R12 mutations from byte snapshots (sha-verified); the 119-file comm set HEAD run (base unchanged); the 34-file Preserve set; ruff + py_compile; sqlfluff on 042 + rollback; `git diff --stat -- app/` unchanged in file set (the ruled six + scripts/eval_runner.py). Worktree HEAD is still ac887e2d - do not rebase (the orchestrator does; main is 88cb78ae with no overlapping file). Run with -p qaren_netguard (this worktree predates the conftest guard).
