# FABLE RED-GATE RULINGS - W4-12 display contract (2026-09-26, session 68; binding for the green, the adversary and the fixer; they sit ON TOP of .qa-s68/specs/W4_12_FABLE_RULINGS.md and the spec's review section)

RED GATE: PASSED at ac887e2d. 34 reds all on assertions in all three flag states (autouse delenv of both flags, so the exported state cannot leak in), 32 pins green, 9 HEAD kills + 35 of 36 prototype mutants killed (the one survivor is an EQUIVALENT mutant, ruled below), prototype 66/66 in all three states, gate 3 (128 records) base == base2 and head differs ONLY at the enumerated deltas, K1 collateral measured exactly (22 margin-ON nodes + 1 caption-ON node), CI-order set 13 files 385 passed, Preserve 71 files 1361 passed, comm set 210 = the spec's 209 + the new file. The measured spec_disagreements are ACCEPTED (A5b is RED not PIN; the section-7 rows that do not redden as written are corrected by the red's table). The prototype design (.qa-s68/proto_W4-12.patch, 168+/30- over the four ruled app files) is ACCEPTED as the design - re-derive it, do not copy bytes.

R1 (Q1 - the equivalent K9 mutant) - DROP the mutation row "alias scrub before :2009 -> B6". Under F2 the praise is built from the scrubbed summary inside `_safe_review_praise` on BOTH paths, so the alias placement can no longer move the praise; K7's placement stays as the code-order CONVENTION (alias summary rebuilt after the praise line, so a reader sees praise derive from the same input on both surfaces) but is not load-bearing and pr_text says so. B4 (alias rebuilt summary-only) and B6 stay.

R2 (Q2 - what `verdict_scrubbed` counts) - CONFIRMED the section-4.1 definition: the key is telemetry for "the fallback reason was served", so it is written ONLY when `strip_score_internals` EMPTIES `winner_reason` (the 17 corpus rows); a partial strip leaves GPT's prose in place and is NOT counted (E4 pins it on both the sync and the SSE path). The gate-3 allowed-delta list is amended accordingly: NO `verdict_scrubbed` on the LEAKY fixture's SSE complete/settle_complete frames (they are byte-identical to base); the key appears on exactly the fully-emptied rows. The `[VERDICT_SCRUB]` WARNING fires once per build on the SAME condition (one log line = one counted event; pin A6/E1 as written).

R3 (Q3 - the added tests) - ACCEPTED as part of the unit's list: A5b (RED, whitespace normalisation before the verdict yield), B7 (RED, praise built from the scrubbed summary), D3b (RED, K10 longest-first masking), E4 (PIN, partial strip not counted), F8 (RED, K12 read-once - flips the env after the verdict frame and expects complete to keep the frame's margin).

R4 (Q4 - K12 kwarg) - KEEP `_single_margin: Optional[bool] = None` on `build_comparison_response` (None = read the env per call; scs reads the flag ONCE per stream and routes it). Document it in the builder docstring as PRIVATE (the scs is its only caller; tests may pass it). F7's keyword half + F8 pin it.

R5 (K1 collateral, the green's ONLY edits to existing tests) - add `monkeypatch.delenv("ENABLE_SINGLE_VERDICT_MARGIN", raising=False)` to exactly the 22 nodes the red listed (tests/test_partial_response_no_fabricated_scores.py test_05a/05b/05c x6 each + test_15 x2; tests/test_prescoring_showable_guard.py test_10b[unset|false]) and `monkeypatch.delenv("ENABLE_SMART_PICK_VERDICT_CAPTION", raising=False)` to tests/test_home_routes.py::TestHomeSmartPick::test_extension_fields_present_when_data_available - via the file's existing fixture or a one-line addition per node, no other change to those files; list the 23 ids in pr_text. Any OTHER red in a flag-ON run of the CI-order set or the Preserve set fails the gate.

R6 (gates for the green, all binding) - unit file in the three states (unset / margin ON / caption ON); the red's mutation table (9 HEAD rows + the 35 killing prototype rows, R1's row dropped) re-run on the GREEN bytes from byte snapshots with sha-verified restores; gate 3 = the red's recorder (scratchpad/w412red/gate/) base (detached scratch worktree at ac887e2d) -> head -> base2, base == base2 record by record, head differs ONLY at the enumerated deltas as amended by R2, margin ON alone adds only margin fields, caption ON alone adds only verdict_short on the eligible smart-pick rows; the 210-entry comm set base vs head under the guard, `comm -13` EMPTY (base measured ZERO failures, so any base failure is re-run alone 3x and explained); the 13-file CI-order set in ONE process per flag state with R5 applied; the 71-file Preserve set unset; ruff E9,F63,F7,F82 + py_compile on the four app files; `git diff --stat -- app/` touches ONLY response_builder.py, structured_comparison_service.py, home_routes.py, profile_routes.py (the pre-existing F401 on home_routes is not this unit's). The corpus harness is irrelevant (no price module moves - state it). Run WITHOUT -p qaren_netguard once the hermeticity unit is merged; until then use it (state which).

R7 (pr_text must carry) - the two flag rows (default OFF, read per call `.strip().lower()`; margin flag read once per stream in scs), the unflagged set and what changes for callers with both flags OFF (SSE verdict frame scrubbed and equal to complete; alias review_summary + praise scrubbed on both surfaces; smart-pick/recent display names deduped; loser-only verdict_short guarded; `metadata.verdict_scrubbed` additive), the OWNED intra-payload fact under the margin flag (scoring_v2.win_margin != the nudged overall gap on nudged/tie rows; W4-12b closes it), the published scrub rate excluding W4-4 honest partials, the 23 K1 ids, the F3 follow-up issue (W4-12b..f) text, the F5 product calls for DECISIONS_AHMED, and the rebase note (shares response_builder/scs with W4-7; whichever lands second rebases and re-runs the union of both CI-order sets).


# FABLE POST-ADVERSARY RULINGS - W4-12 (2026-09-26, after adversary r0 SOUND with four reproduced minors; binding for the fix round)

The pipeline is SOUND on bytes and every gate reproduced. Three of the four minors are design gaps I will not merge as documented, because two of them sit on the #99 class (naming the loser) and one corrupts the very telemetry the PR publishes. Fix round, then re-adversary.

R8 (count from the ORIGINAL text; never count our own fallback) - `scrub_verdict_prose` decides `counted` from the INCOMING `winner_reason` alone: counted iff the incoming text is non-empty AND `strip_score_internals` empties it AND the incoming text is neither the qualitative fallback for the reconciled winner (`_QUALITATIVE_WINNER_REASON` rendered for `product_names[winner_index]`) nor the reconcile's deterministic replacement. The write-back of the fallback happens after that decision and is never itself re-counted or re-logged, so on SSE the builder's second call over a frame the scs already scrubbed counts nothing and logs nothing (one `[VERDICT_SCRUB]` line per request, pinned with a winner whose display name matches a score pattern - the adversary's probes p1/p2/p3 become the pins: sync mismatch + clean reason -> not counted, 0 logs; SSE agree + fully leaking reason -> counted once, exactly 1 log; SSE mismatch + fully leaking reason -> NOT counted (the reconcile replaced it before the scrub), 0 logs = E3's SSE twin, which also pins the call ORDER, mutant N4). The payload text is unchanged by this ruling. Record as follow-up W4-12g: `strip_score_internals` has a false positive on product names that look like score fragments ("5-Point Harness"), pre-existing at base and out of this unit.

R9 (the fallback names the RECONCILED winner - the #99 class, mutant N5) - pin it: `winner_index=1` with a fully leaking reason -> `overview.winner.reason`, `recommendation` and the BC `comparison.winner_reason` all render product 1's display name and never product 0's; the same pin on the SSE verdict frame. Mutation N5 (fallback from `product_names[0]`) must redden in the CI-order set, not only in the recorder.

R10 (key-present rule, mutants N2/N3) - pin that a `comparison` dict WITHOUT `key_tradeoff` / `winner_declaration` keys leaves the builder without those keys (the helper never creates a key it did not find), and one with both keys leaking gets both emptied/rewritten as designed.

R11 (loser guard: whole tokens, and a caption that names BOTH is kept) - `_names_only_the_loser` matches the normalised names on TOKEN boundaries (a loser name `air` no longer matches inside `airy`), keeps the K10 longest-first masking, and returns False whenever the winner's normalised name also matches the declaration - including the case where the winner's normalised name equals or is a prefix of the loser's (winner `Galaxy S25+` / loser `Galaxy S25` / declaration `Galaxy S25 Plus` -> KEPT). Pins: the Galaxy case kept, the `Airy cushioning wins` case kept, D1/D3/D3b unchanged; mutation: plain substring -> red on the Airy pin; drop the both-match clause -> red on the Galaxy pin. The `Sauvage Elixir` / `Sauvage` / `The Elixir concentration of Sauvage` case stays a documented conservative drop (the string names only the loser; the card falls back to the reason_key copy) - state it in pr_text with the two measured rows it joins.

R12 (gates for the fix round) - unit file in the three states; the R8-R11 pins + their mutations from byte snapshots (sha-verified); the 13-file CI-order set in ONE process per state (N2/N3/N4/N5 must now redden there); gate 3 base -> head -> base2 unchanged in shape (the R8 change moves no payload text, so the 48/39/19 deltas must stay identical record by record; if any record moves, STOP and report); the 71-file Preserve set; ruff + py_compile; `git diff --stat -- app/` still only the four ruled files. Run with -p qaren_netguard as before (the conftest guard is not merged yet).


# FABLE RULINGS R13-R15 - W4-12 (2026-09-26, after the fix-round re-adversary returned SOUND with three reproduced minors; binding for fix round 2)

R13 (punctuation-distinct names stay distinct - the #99 class on the S2x / S2x+ pair) - the token form must never make two DIFFERENT product names identical. Normalise `+` (and the fullwidth `＋`) to the token `plus` BEFORE the punctuation drop, keep everything else in R11 (token boundaries, K10 longest-first masking, the both-match clause). With that the four Galaxy cases resolve by the existing rules and are ALL pinned: winner `Galaxy S25+` / loser `Galaxy S25` / caption `Galaxy S25 Plus` -> KEPT (the longer winner form masks it); winner `Galaxy S25+` / loser `Galaxy S25` / caption `Galaxy S25` -> DROPPED; winner `Galaxy S25` / loser `Galaxy S25+` / caption `Galaxy S25+` -> DROPPED (the longer loser form masks it, nothing of the winner remains); winner `Galaxy S25` / loser `Galaxy S25+` / caption `Samsung Galaxy S25+ wins` -> DROPPED. Safety net: if the two normalised forms still collide after this, the guard falls back to comparing the whitespace-normalised display names (so a collision can never turn the guard OFF silently); pin one synthetic collision. Mutations: drop the `+ -> plus` mapping -> the third and fourth cases red; drop the collision fallback -> the synthetic-collision pin red.

R14 (R8's exclusion is keyed on the RECONCILED winner - mutant X1) - pin the p1 shape with the Graco pair REVERSED (Graco at index 1 is the reconciled winner; GPT picked 0 with a clean reason; reconcile off and on): verdict_scrubbed absent, 0 [VERDICT_SCRUB] lines, the served reason is Graco's fallback. Mutation X1 (`_own_texts` built from `product_names[0]`) must redden in the CI-order set.

R15 (pr_text truth) - the code order is tautology guard -> truncation -> loser guard on the TRUNCATED verdict_short, and that is the ORDER I keep (the guard must judge the text the user sees; spec 4.4's guard-then-truncate is superseded, stated as a deviation with the measured 40-char max declaration vs the 140 limit). Fix the flags-row sentence to match the design section. No code change.

Gates (R12 stands): unit file three states; the new pins + mutations from byte snapshots (sha-verified); the 13-file CI-order set ONE process per state; gate 3 base -> head -> base2 with the SAME 48/39/19 deltas record by record (R13 changes no corpus row - the corpus has no punctuation-variant pair - if any record moves, STOP and report); the 71-file Preserve set; ruff + py_compile; `git diff --stat -- app/` only the four ruled files.


# FABLE RULINGS R16-R18 - W4-12 (2026-09-26, after the fix-round-2 re-adversary returned SOUND with four reproduced minors; binding for fix round 3 - home_routes + the unit test file + pr_text only)

The fixer's deviation on R13's predicted red set is ACCEPTED (an equality-only safety net would have been weaker; the mutant is killed by other rows). Two of the adversary's minors weaken R11's UNFLAGGED Home-copy guard on common pairs and are fixed here; two are hygiene.

R16 (the collision trigger fires only on a NORMALISATION collision) - a collision is: a winner form and a loser form whose TOKEN forms are equal while their whitespace-normalised, punctuation-KEPT forms DIFFER (`WH-1000XM5` vs `WH 1000XM5`). Two products that genuinely share a name form (Ninja `Air Fryer` vs Philips `Air Fryer`: token forms equal AND raw forms equal) are NOT a collision and stay on R11's token path, where the both-match clause and the differing display names decide. Pins: the Ninja/Philips pair with captions `Philips' Air Fryer is quieter`, `Philips(TM) Air Fryer` (use the trademark sign) and `Philips-Air Fryer wins` -> all DROPPED (loser-only), as fix round 1 had them; the synthetic WH-1000XM5 collision pins stay. Mutation: the broad `loser_forms & winner_forms` trigger -> the Philips rows red.

R17 (the plus token is a NAME plus, not prose) - map `+` / U+FF0B to the token `plus` only when it is glued to the preceding word character (`S25+`, `S25＋`); a free-standing ` + ` in prose stays punctuation. Pins: winner `iPad` / loser `iPad Plus` / caption `iPad + keyboard bundle wins` -> KEPT (it does not name the loser); the four Galaxy rows and the fullwidth twins unchanged. Mutation: map every `+` -> the iPad row red. Also pin the LEADING whole-token boundary: loser `Air` / winner `Air Max 90` / caption `Repair kit wins the day` -> KEPT (`air` at the tail of `repair` is not a token match); mutation A2 (lookbehind removed) -> red.

R18 (hygiene) - the unit test file carries the two fullwidth plus characters as `＋` escapes, not literals (0 non-ASCII bytes, as fix round 1 had; the report must state the measured byte count); pr_text limit 8 is rewritten to the R16 definition and gains the R17 prose-plus sentence and the `Repair` boundary case.

Gates (R12 stands): unit file in the three states; the new pins + mutations (broad trigger, every-plus, A2) from byte snapshots with sha-verified restores, the earlier rounds' rows re-run on the final bytes; the 13-file CI-order set ONE process per state; gate 3 base -> head -> base2 with the SAME 48/39/19 deltas record by record (STOP if any record moves); the 71-file Preserve set; ruff + py_compile; `git diff --stat -- app/` only the four ruled files, and response_builder / scs / profile_routes keep their shas (87c7ffb2 / c7a817ef / fb812777).


# FABLE RULINGS R19-R20 - W4-12 (2026-09-26 ~14:40, after the fix-round-3 re-adversary returned SOUND with three minors; binding for fix round 4)

The third adversary found the last #99-class hole in the UNFLAGGED loser guard: a cross-brand pair whose names differ only by punctuation (winner Ninja `Air Fryer`, loser Philips `Air-Fryer`) with the caption `Philips Air Fryer wins` is KEPT, because the bare stem `air fryer` - shared by both products - is credited to the winner. The root cause is that shared stems count as evidence at all. One rule replaces the R11/R13/R16 special cases:

R19 (shared forms are evidence for NEITHER side) - build each side's evidence from its DISTINGUISHING forms only: a form whose token form (`_name_tokens`) equals a token form on the other side is SHARED and is excluded from both evidence sets (bare stems shared by two brands, identical model names). Matching stays R11 (token boundaries, K10 longest-first masking) over the distinguishing forms; the both-match clause is then about distinguishing forms only. The R13/R16 raw-form fallback runs ONLY when NO distinguishing form remains on either side (identical names, or names differing only by punctuation such as `WH-1000XM5` vs `WH 1000XM5`): compare the whitespace-normalised punctuation-kept forms as R13 does; identical raw forms = no evidence = KEPT. Every existing pin must stay green: the four Galaxy rows, the Airy / Repair boundary rows, the Philips apostrophe / trademark / hyphen rows, the WH collision rows, the identical-names row, D1 / D3 / D3b. New pins: (a) the adversary's case (W Ninja `Air Fryer` / L Philips `Air-Fryer` / caption `Philips Air Fryer wins` -> DROPPED; and the mirror W Philips `Air-Fryer` / L Ninja `Air Fryer` / caption `Ninja Air Fryer wins` -> DROPPED); (b) the same pair with caption `Air Fryer wins on crisp` (names NEITHER distinguishing form) -> KEPT; (c) punctuation-only names (`!!!` vs `???`, the adversary's O2 probe) -> the guard stays OFF (KEPT) - pinned so the deviation the round-3 fixer disclosed is no longer unpinned. Mutations: shared forms credited to the winner (today's behaviour) -> (a) red; shared forms credited to the loser only -> (b) red. `_names_collide` may be folded into the new 'no distinguishing form remains' predicate; the R16 Philips rows and the R13 WH rows are the pins that prove the fold.

R20 (pr_text truth) - correct the three numbers the adversary measured: the green table is 46 rows (9 HEAD + 34 prototype + 3 R5), the five superseded fix-round-1 rows were NOT re-run (their round-2 re-anchored twins were), the base comm run covered 207 files (head 208); state the R19 rule in limit 8 and in residual_risk instead of the WH-only variant; keep every other number as measured this round.

Gates (R12 stands): unit file three states; the new pins + the two R19 mutations + every earlier guard row re-run on the final bytes from byte snapshots (sha-verified); the 13-file CI-order set ONE process per state; gate 3 base -> head -> base2 with the SAME 48/39/19 deltas record by record (the corpus has no shared-stem cross-brand pair - STOP if any record moves); the 71-file Preserve set; ruff + py_compile; only home_routes.py, the unit test file and pr_text change (response_builder 87c7ffb2 / scs c7a817ef / profile_routes fb812777 / the three R5 files / the fixture keep their shas).
