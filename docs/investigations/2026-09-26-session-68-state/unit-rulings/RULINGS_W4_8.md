# FABLE RED-GATE RULINGS - W4-8 category truth (2026-09-26, session 68; binding for the green, the adversary and the fixer; they sit ON TOP of .qa-s68/specs/W4_8_FABLE_RULINGS.md and the spec's review section)

RED GATE: PASSED at 04acb757. 156 reds all on assertions in all four flag states (byte-equal FAILED sets), 288 pins green, 76 kills (71 design mutants on the prototype with 0 survivors + 5 HEAD-level), prototype 473/473 in all four states, flag-OFF equality gate records == the 61585c58 golden (corpus hash b76fcef9..., all-strings cac3460c...) with the ON delta exactly one corpus row (C:supplements:05) and the blocklist ON widen [] over 413+22+6 strings, CI-order set 32 files 964 passed at HEAD / 1405 on the prototype in both states, comm set 104 files, base-failed = the 3 guard-caused SSRF nodes. The seven spec_disagreements are ACCEPTED as measured: rungs 2/3 are output-inert under R2 so the veto-only implementation stands (no M2/M3 rows); 'vitamin'/'supplement' are unreachable context tokens and are dropped; EN 'hunting rifle' is allowed at HEAD (21 of 22 blocked) and is pinned allowed in both states; spec tests 5/9/13 are superseded as the red states. The prototype design (.qa-s68/proto_W4-8.patch, 3 files, 135+/4-) is ACCEPTED as the design - re-derive it, do not copy bytes.

R1 (Q1 - household veto) - CONFIRMED as prototyped: a household VETO makes the tablet hit NOT electronics; the classifier then falls to its normal 'other' path and a CHIPLESS household pair pays ONE A2b gpt-4o-mini call (pinned by test_resolver_household_no_chip_escalates_to_a2b_flag_on). A fixed 'other' without A2b is REJECTED: dishwasher/descaling tablets are grocery-or-other and the escalation is the component that knows. State the cost in pr_text (one mini call per chipless household pair, none for pharmacy pairs, none with a chip).

R2 (Q2 - the [2-6]G carve-out) - ACCEPTED with a STATED LIMIT: the reused SUPPLEMENT_DOSE_RE hit whose text fullmatches `[2-6]\s*g` (case-insensitive - the classifier lowercases) is a network generation, not a dose, so 'Galaxy Tab S9 FE 5G tablet' / 'Lenovo Tab M10 4G tablet' stay electronics; the price is that a genuine 2-6 g tablet dose ('glucose 4g tablets') with NO other veto signal stays electronics under A. Pin both directions as the red did ('Glucose 10g tablets' vetoes); pr_text states the limit; follow-up row W4-8e (a dose that is followed by a pharmacy token wins over the carve-out) if it ever measures on real traffic.

R3 (Q3 - the AR/EN intent parity gap) - CONFIRMED as measured: under v2 the AR intents 'buy opium' (BI:11) and 'hunting rifle' (BI:20) stay BLOCKED through their own entries (a safety list must never get MORE permissive in Arabic under a precision flag), while the EN twins 'buy opium' / 'hunting rifle' are ALLOWED in BOTH states because they are allowed at HEAD and widening EN under a narrowing flag would break the '0 newly blocked over 413' gate. Pin all four rows in both states. Record follow-up W4-8d for DECISIONS_AHMED: whether a later v3 adds EN intent phrases ('hunting rifle', 'buy opium', 'rifle for sale') - a safety call, not this unit's.

R4 (Q4 - 'silencer suppressor') - NOT allowed: add the explicit v2 intent entry 'silencer suppressor' (weapon accessory intent; 'surge suppressor' is a real electronics product, so never bare 'suppressor'). Blocked in BOTH states (OFF blocks it through bare 'silencer'), so the corpus 'newly blocked' set stays []. The audit pin 'gun silencer suppressor' stays; add the 'silencer suppressor' row to the R1 intent table and a mutation (drop the entry -> red).

R5 (Q5 - the six no-signal pharmacy strings) - add SIX generic context tokens, never brand names: household class (-> not electronics, 'other' + A2b) = 'denture', 'purification'; pharmacy class (-> supplements) = 'laxative', 'antacid', 'flu', 'vinegar'. Each token gets its own load-bearing row (a string that ONLY that token catches) and a drop-mutant; the six measured strings then classify as: Steradent/Polident 'denture tablets' -> other (+A2b), Aquatabs 'water purification tablets' -> other (+A2b), Dulcolax 'laxative tablets' -> supplements, Imodium/Motilium (if the string carries 'antacid'/'laxative' - measure; a brand-only string stays electronics), 'apple cider vinegar tablets' -> supplements, Beechams 'cold and flu tablets' -> supplements. Brand-only pharmacy queries with no generic token stay electronics under A - a STATED LIMIT with the measured 25-string precision restated after these tokens.

R6 (Q6 - the v2 JSON shape) - ACCEPTED: a top-level "v2": {"notes": ..., "categories": {cat: {lang: [full replacement list]}}} in app/data/content_blocklist.json, edited in place (raw UTF-8 Arabic, CRLF kept); the loader compiles BOTH at construction and `_patterns()` selects per call; test_blocklist_v1_lists_unchanged pins v1 byte-for-byte; add a pin that every v2 category/lang list is non-empty and that v2.notes names the flag.

R7 (Q7 - the Half B reader) - NAME it `content_safety_service.blocklist_precision_v2_enabled()` and pin it by name with the standard `.strip().lower()` rows (' true ', 'TRUE', 'On', '1', 'false', '0', '', unset) exactly like `extraction_service.category_token_fix_enabled()`; both read per call.

R8 (gates for the green, all binding) - the three unit files (two new + the edited audit file) in FOUR states (unset / A / B / both); the red's 71-row design table + the R4/R5/R7 rows re-run on the GREEN bytes from byte snapshots (sha-verified restores; content_blocklist.json restored byte-for-byte incl. CRLF); the flag-OFF equality gate on the green: records == tests/fixtures/category_corpus_gcc_360_head_records.json (b76fcef9...) in unset / 'false' / B-on, the all-strings hash cac3460c..., ON delta exactly the one corpus row + the R5 rows over the 35 tablet rows (re-measure and pin the exact ON set - it changes with R5), blocklist ON widen [] over 413+22+6 strings, L2 (521 titles) unsafe_off == unsafe_on == 0; the 104-file comm set base (04acb757, detached scratch worktree) vs head under the guard, `comm -13` empty against the 3 SSRF nodes; the 32-file CI-order set in ONE process per flag state; ruff + py_compile + json.load on the blocklist; `git diff --stat -- app/` touches ONLY app/services/extraction_service.py, app/services/content_safety_service.py, app/data/content_blocklist.json. The corpus (_proof) harness is blind - state it. Run with -p qaren_netguard (this worktree predates the conftest guard).

R9 (pr_text must carry) - both flag rows (default OFF, read per call), the R2 rung order as built (veto-only; rungs 2/3 inert and why), the stated limits (R2 carve-out, R5 brand-only strings, the EN parity gap R3), the A2b cost (R1), the v2 shape + both L1 hashes, device recall 27/27 and the re-measured pharmacy precision, the follow-ups W4-8b (category in the specs cache key), W4-8c (Arabic category tokens, own flag), W4-8d (EN intent v3, DECISIONS_AHMED), W4-8e (dose-then-pharmacy-token over the carve-out), the canary (nocache or never-seen pairs; a rollback leaves supplements-schema specs cached 7 d/30 d), and the activation note (A alone first, watch supplement-source engagement on chipless pharmacy pairs; B alone second, watch the blocked-query rate on Arabic traffic).
