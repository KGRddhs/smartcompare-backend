# FABLE RED-GATE RULINGS W0-4e / W0-4f / W0-4g (2026-09-26, session 68; binding for the green, the adversary and the fixer)

RED GATE: PASSED. 43 right-reason reds (42 in tests/test_retro_w0_4_efg.py + the F1 allowlist edit), 48 pins green at HEAD in both flag states, the four r1 mutants rebuilt and shown to SURVIVE the existing pins then KILLED by the new ones, the design proven satisfiable on a scratch worktree (289/289 green in both flag states; the 15-row design-mutant matrix all killed). The prototype patch (.qa-s68/proto_w04efg.patch) is the ACCEPTED design shape - re-derive it, read it for shape, never copy it blindly.

R1 (Q1, `waiting` / `peak_waiting` semantics) - `waiting` means BLOCKED ON THE SEMAPHORE. In run_parse_offloaded: increment `waiting` (and its peak) only when `sem.locked()` is true immediately before `await sem.acquire()` (single-threaded loop: no interleaving between the check and acquire's fast path); decrement in the finally around the acquire when it was incremented. Consequence: after ONE uncontended parse the stats read {workers: <PRICE_PARSE_MAX_WORKERS>, queued: 0, jobs_total: 1, waiting: 0, running: 0, peak_waiting: 0, peak_running: 1}; G3 (3 jobs on a size-1 pool) still reads peak_waiting == 2, running peak 1. The green MAY edit tests G2 and G6's expected dicts for exactly the peak_waiting 1 -> 0 change (and G4 must construct a genuinely contended semaphore, which it does); no other test expectation moves. The canary reading: peak_waiting >= 1 means queueing happened; record that in pr_text and in the CLAUDE.md correction for activation step (2).

R2 (Q2, import form) - the NEW code in url_extraction_service.extract_from_url resolves price_service through the PACKAGE ATTRIBUTE exactly as scs does (`from app.services import price_service as _w04_ps` inside the flag-ON branch; `_w04_ps.PRICE_FETCH_MAX_BYTES`, `_w04_ps.run_parse_offloaded`), so production and tests cannot diverge under the #185 class; the existing extract_with_ai import (`from app.services.price_service import run_parse_offloaded`) is NOT this unit's hunk - leave it, name it in pr_text as a one-line nit for the hermeticity follow-up. The new test file's call-time module resolution (importlib.import_module) is ACCEPTED.

R3 (Q3, the three tests/test_security_hardening.py SSRF nodes red under the guard) - ENVIRONMENT, not this unit: they need real DNS for example.com and fail identically at base under the guard; the hermeticity unit (sc-hermetic, running now) stubs their resolver test-side. For THIS unit's gates they are the accepted exceptions: list the three node ids, show they fail at base under the guard (one re-run at base in the detached scratch worktree, that file only) and that they pass without the guard, and move on. Nothing else may be excused this way without the same two measurements.

R4 (Q4, the 153 blocked pre-existing attempts) - #184 class, OUT of scope here; report the count in the comm/CI-order evidence and nothing more.

R5 (Q5) - YES: pr_text records FIVE killed hardening mutants: the four r1 rows plus N_render_cap_flag_true_only_literal (the render-leg cap gated on the literal 'true', killed by the 8 firecrawl/scrapedo H1 rows).

R6 (spec disagreements) - ALL ACCEPTED as measured: 157 nodes in tests/test_retro_w0_4.py (185 was the three-file total); F5's [2999999] and [3000000] rows are boundary PINs, [3000001] and [3500000] the reds; E4 is a signature-check red for both cap values; the CI-order set is the measured 32 files (.qa-s68/ci_order_set.txt), a strict superset of the spec's 29.

R7 (mutation table for the green) = the 15-row design matrix P01-P15 from the red report + the five hardening mutants (R5) + these additions: the semaphore-locked check dropped (every job counts as waiting -> G2/G6 red under R1); `waiting` never decremented on a cancelled acquire (G4 red); `cap=False` honoured flag OFF (E3 red - flag OFF must return the whole body regardless, i.e. the kwarg is never read OFF); the backfill's `re.findall` pattern changed inside the offloaded block (E1/E6 red); the ues cap applied before the flag read (F5b red). Every row from byte snapshots with sha256-verified restores.

R8 (gates, as spec section 5 with these bindings) - (a) the six unit files green with the flag UNSET and =true (289 nodes + any R1 edits); (b) R7; (c) comm HEAD over the recorded 362-file .qa-s68/comm_set.txt in chunks of at most 40 files, --timeout=60, CI's deselects; every failure not in tests/.pre_impl_failures.txt is re-run alone and at base (detached scratch worktree at 61585c58, that file only) before it counts; the three R3 nodes are the only pre-accepted exceptions; hung nodes re-run alone; (d) the corpus gate over the READ-ONLY root C:/Users/SynAckITPC/Documents/AI/sc-w0-load/_proof: `--flags ENABLE_PRICE_PARSE_OFFLOAD --out <scratch>/off.json` must print OVERALL 9504e5a94a218969764c8cbfdb43243da7bfcd8e0950bb5fb9148b9a9258ce99 and results a1b3460c28579fab605287e1b5a05dd9cb8a3b07782573dfb5aac005104c59d8; if the OFF digest differs, STOP and report (search docs/investigations/2026-09-24-session-67-state/harvest_rw04.json for the flags list R-W04 used before concluding anything); then `--flags-on ENABLE_PRICE_PARSE_OFFLOAD --compare <scratch>/off.json` -> equal=True, DIFFERING RECORDS 0 of 1656; record both digests and wall times; never run _proof/sweep2.py; (e) ruff E9,F63,F7,F82 + py_compile on app/main.py, price_service.py, structured_comparison_service.py, url_extraction_service.py and both test files; CRLF hygiene; scripts/ unchanged (no harness code change is expected - if one seems needed, STOP and report); (f) the 32-file CI-order set in ONE process WITH tests/test_retro_w0_4_efg.py: expected = the red's base run (847 passed) plus the new file green, minus the F1 red, with the three R3 nodes as the only failures.

R9 (files this unit may touch) - app/services/price_service.py (curl_fetch_html cap kwarg; _PRICE_PARSE_STATS; price_parse_pool_stats; the INFO line in _get_price_parse_pool; run_parse_offloaded counters), app/services/structured_comparison_service.py (_lazy_bh_pdp_backfill only), app/services/url_extraction_service.py (extract_from_url only), app/main.py (price_parse_pool_snapshot + the health_check merge; `import sys` if absent), tests/test_retro_w0_4_efg.py (R1 edits only), tests/test_retro_w0_4.py (the allowlist edit already made). Nothing else.

R10 (pr_text) - the three follow-ups' designs and effects under the EXISTING flag; flag OFF byte-identical with the corpus digests and wall times; the /health key contract (absent until the pool exists; `price_parse_pool: {workers, queued, waiting, running, jobs_total, peak_waiting, peak_running}`; `waiting` = blocked on the semaphore; `queued` = executor queue depth, 0 by construction); the `[PRICE-PARSE] pool built workers=N` line; the five killed hardening mutants + the 15-row design matrix; the stated limits (fetch_page uncapped; the harness observes neither the cap nor the new sites; five over-cap corpus files unresolved; stats approximate under several loops; the two production-equivalent mutants; the extract_with_ai import-form nit; the R3 SSRF nodes belong to the hermeticity unit); the CLAUDE.md corrections for the docs PR (the W0-4 row gains W0-4e/f/g; activation step (2) watches /health.price_parse_pool.queued|waiting|peak_waiting with peak_waiting >= 1 meaning queueing happened); the 32-file CI-order set and the 362-file comm set as the evidence base.


# FABLE POST-ADVERSARY RULINGS W0-4e/f/g (2026-09-26, after the r0/r1/r2 adversaries; binding for fix round 3)

The pipeline ended SOUND on bytes with one escalated design fact: offloading the backfill's re.findall to the price-parse pool does NOT free the event loop (CPython's sre holds the GIL for the whole findall; measured 0.200 s offloaded vs 0.202 s inline on 40 MB; 0.147 vs 0.153 s on 30 MB). Spec section 7 assumption 2 is REFUTED. A cosmetic offload does not ship.

R11 (W0-4e2 - the scan yields, on the loop, no pool) - under the flag `_lazy_bh_pdp_backfill` scans the UNCAPPED search page on the event loop in slices, yielding between slices, so the loop serves other tasks between chunks: module constants in structured_comparison_service `_W04_SCAN_CHUNK = 1_000_000` and `_W04_SCAN_OVERLAP = 65_536` (chars); the SAME pattern the base uses (compile it once at module level or keep the literal - the bytes the pattern matches must not change), `finditer` over `html[start : min(start + CHUNK + OVERLAP, n)]` for `start` in `range(0, n, CHUNK)`, accepting a match ONLY when its absolute start `start + m.start()` is `< start + CHUNK` (a match beginning in the overlap tail belongs to the next slice - this is the dedupe rule; order is preserved by construction), collecting `m.group(1)`; `await asyncio.sleep(0)` after every slice except the last is NOT required to be skipped - one `sleep(0)` per slice is fine and simpler; the `_accept` loop then runs over the collected list exactly as base. The result equals `re.findall(pattern, html)` for every match shorter than OVERLAP (an href longer than 65,536 chars is not a URL - stated limit). REMOVE the `_w04_scan_block` pool offload for this site; `run_parse_offloaded` and the pool are untouched (the other nine sites keep their offload - soups are not GIL-bound the same way and were measured to help). Flag OFF: the base single `re.findall(...)` inline over the whole page, byte-identical.
Pins (replace the two E2 pool-placement rows, which pinned the refuted design): (E2a) identity - parametrized fixture pages (about 5 MB) with hrefs placed at absolute offsets CHUNK-1, CHUNK, CHUNK+1, one href SPANNING the first boundary (starts at CHUNK-40), the same href repeated on both sides of a boundary (both occurrences kept, as findall would), 300 hrefs spread over the page, and a page shorter than one chunk: the flag-ON list == `re.findall(pattern, page)` == the flag-OFF list; (E2b) yielding - with `scs.asyncio.sleep` (or the module attribute the block calls) replaced by a recorder that awaits the real `asyncio.sleep(0)`, a flag-ON backfill over a 5 MB page records >= ceil(n / CHUNK) sleep(0) calls and a flag-OFF backfill records 0; (E2c) the existing E1/E3/E5/E6/E7 rows stay (E1 and E6 keep proving the cap exemption). Mutation rows: OVERLAP set to 0 (the spanning href is lost -> E2a red); the dedupe rule dropped (a match in the overlap region is collected twice -> E2a red); the `sleep(0)` removed (E2b red); `findall` over the whole page inside the loop with a single sleep at the end (E2b still >= 1 but E2a fine - so ALSO pin that the number of sleeps is >= ceil(n/CHUNK), which that mutant fails); CHUNK changed to 2_000_000 (identity holds - record as equivalent, not a defect). Report in pr_text the measured max loop gap with the chunked scan vs inline on the 30-40 MB probe page (a measurement, not a pin - timing pins flake).
The corpus gate is NOT re-run for this round if the only app/ change is inside `_lazy_bh_pdp_backfill` (the harness never enters it - stated in the spec); the adversary confirms that by reading the diff. Everything else in R8 re-runs: the six unit files in both flag states, ruff + py_compile, the 32-file CI-order set in one process, and the comm chunks whose files reference `_lazy_bh_pdp_backfill` or `structured_comparison_service` (at least chunks 04-08; state which ran).

R12 (pr_text and docs corrections, from the r2 adversary) - (a) the /health contract: the `price_parse_pool` key is absent until the pool is FIRST built; after an ON -> OFF flip inside one process the pool, the key and its counters persist until a restart (stale but harmless) - say so in the contract and in the CLAUDE.md correction; (b) `queued`: the fixer's corrected semantics stand and SUPERSEDE R10's literal "0 by construction": transiently non-zero on a semaphore hand-off (a work item sits in the executor queue until a pool thread dequeues it; measured 0-3 of 60 samples), a value that PERSISTS across polls means orphaned parses (a cancelled await whose worker is still busy), pinned by test_w04g_cancelled_running_parse_releases_its_slot_and_shows_as_queued; (c) the two wording nits: exactly TWO production-equivalent mutants are recorded-not-pinned (N_sem_size_check_removed, N_pool_size_read_every_call), and the queued transient is "0-3 of 60 samples, timing-dependent" not "1-3 of 60"; (d) W0-4e's section describes the chunked yielding scan, the GIL measurement that motivated it, and the OVERLAP stated limit; W0-4e2 leaves the follow-ups list (it is in).

R13 - R1-R10 otherwise stand. The fixer confirms the seven files carry the r2 fixer's recorded shas before editing (main.py c1c94bd6..., price_service 70f9585b..., scs f91f2db7..., ues acf0ee2b..., test_retro_w0_4 5e044841..., test_retro_w0_4_efg 85ed3de2...). Every report carries final sha256s; the re-adversary re-runs the unit files FIRST.


# FABLE RULINGS R14-R16 - W0-4efg (2026-09-26, after the r4 re-adversary returned DEFECTIVE on one major; binding for fix round 5)

The r4 adversary is right: R11's purpose - the loop serves other tasks between BOUNDED chunks - had no pin. Its X1 mutant (`html[_w04_start:]`, the slice end dropped) kept identity and the sleep(0) count while the first slice scanned the whole page (40 MB: max loop gap 1.3-3.7 s, worse than inline). Test-only round; NO app/ byte may change (the design is correct as built; the current shas are the r4 fixer's: scs 223e1701..., efg df01e0de..., main c1c94bd6..., price_service 70f9585b..., ues acf0ee2b..., test_retro_w0_4 5e044841...).

R14 (pin the per-slice bound, deterministically, no timing) - the E2 recorder (`_ReProxy.finditer`) records `len(string)` and the slice offsets for every scan call under the flag; the pin asserts over the 5 MB fixtures that (a) every slice length is <= `_W04_SCAN_CHUNK + _W04_SCAN_OVERLAP`, (b) the slice starts are exactly `range(0, n, CHUNK)` and each end is `min(start + CHUNK + OVERLAP, n)` (the slices COVER [0, n) with the ruled overlap), and (c) the number of scan calls equals ceil(n / CHUNK). Mutations from byte snapshots (sha-verified restores): X1 `html[_w04_start:]` -> red; a slice of `CHUNK` alone (no overlap) is already X-OVERLAP=0's row; a `range(0, n, CHUNK*2)` step -> red on (b)/(c).

R15 (pin the OVERLAP guarantee) - (a) one E2a case with an in-domain href of ~4,000 chars (a long query string) spanning a slice boundary: ON list == findall == OFF list; mutation `_W04_SCAN_OVERLAP = 100` -> red; (b) pin the constant `_W04_SCAN_OVERLAP == 65_536` and `_W04_SCAN_CHUNK == 1_000_000` by value, so the pr_text limit cannot drift silently (a value pin is acceptable here because pr_text and CLAUDE.md quote the numbers as a contract). Keep CHUNK=2_000_000 as the recorded equivalent for the identity rows; it now reddens the value pin, which is the intent (record it as 'kills the value pin only').

R16 (pr_text measurement honesty) - replace the loop-gap table with numbers measured through the real `_lazy_bh_pdp_backfill` on the r4 adversary's REALISTIC-markup page (anchors + spans, one /product/ href per ~5,000 units; re-derive its generator under scratchpad, do not copy numbers): flag OFF inline vs flag ON chunked at 30 MB and 40 MB, 3 reps each, 5 ms heartbeat, idle floor stated; drop the phrase 'at the heartbeat floor' and the '~5-10 ms per turn' figure; state that the earlier table used an 'x'-filler fixture. Fix the 'first seven rows' wording (six unmarked rows + CHUNK=2M outside the table). Keep the chained-href corner as the stated limit + follow-up (measured: R11 differs from findall on 1,533 of 3,000 chained pages, a resume guard 885, a continuation form 0 - the continuation form is the follow-up's candidate, W0-4e3).

Gates for this round: the six unit files both flag states; the R14/R15 pins + their mutations; the 32-file CI-order set in ONE process (the three R3 SSRF nodes remain the only failures); comm chunk 07 (holds the efg file) re-run; ruff + py_compile on the test file; `git diff -- app/` byte-identical to the r4 start (sha-verified). The corpus gate stays not re-run (no app/ change).
