# FABLE RED-GATE RULINGS - W4-6a rubric truth (2026-09-26, session 68; binding for the green, the adversary and the fixer; they sit ON TOP of .qa-s68/specs/W4_6A_FABLE_RULINGS.md R1-R13 and the spec's review section; numbering continues at R14)

RED GATE: PASSED at 04acb757. 39 reds all on assertions in all 8 exported states (the autouse fixture clears the env), 21 pins green (each a named mutation killer), 25 kills on the prototype (M5 unreachable by construction - dropped; M16 killed by the hand-built test only - accepted), prototype 60 passed / 1 xfailed in all 8 states, E1 four goldens byte-unchanged, E2 492 digests base == head == base2 == prototype-OFF, E5 re-measured (OFF tradeoffs differ from base on 0/169), CI-order set 73 files 1,888 passed at HEAD / 1,948 on the prototype in both orders, comm set 244 files, base-failed = 4 baselined ids. The eight spec_disagreements are ACCEPTED as measured (VT leaves the same 3 N/A rows as V; CB->CBT moves 32/144 badges; E5 as written is obsolete; M4's new anchor record; the M16 pin's reach). The prototype design (.qa-s68/proto_W4-6a.patch, scoring_service.py only, 145+/2-) is ACCEPTED as the design - re-derive it, do not copy bytes.

R14 (Q1 - the tradeoff guard rides EITHER flag) - the review's C3 was right and R3 was too narrow: G + G' + the zero-margin rule are gated by `_value_dim_partial_signal_enabled() or _tie_is_not_missing_enabled()` (the T reader stays coupled to renorm, so under ENABLE_MISSING_DIM_RENORM the guard is active iff V is ON). Measured reason: under V alone the prototype raised void-named loser strengths from 2 to 11 records and margin-0 'stays competitive' claims from 66 to 77 (R01/R01b gained 'stays competitive on feature.' at 0.0). Pins: under V alone AND under RV the 169-record sweep has 0 void-named loser_wins and 0 claims with margin < 1.0 (the T/VT rows already pin it); mutation: guard gated on T alone -> the V-alone pin red. Flag OFF stays base byte-for-byte (E2/E5-OFF).

R15 (Q2 - zero-margin semantics = (b), eligibility before the pick) - a dimension whose loser-minus-winner margin is < 1.0 is INELIGIBLE before `_loser_strongest_dim` picks (exactly like G's void skip and G''s > 5 skip); the pick is then the loser's strongest ELIGIBLE dim; nothing eligible -> None (no pair). Reading (a) is REJECTED because it drops a genuine second-best strength when the loser's top-scoring dim happens to be a near-tie. Pin: a record whose loser's top-score dim has margin 0.5 while another dim has margin 3.0 -> the pair names the second dim; mutation (a) -> red; the 'nothing eligible -> None' row stays.

R16 (Q3 - T reverses F4.2's always-render fallback, and that is intended) - under the guard the fabricated runner-up strength is gone, so a sweep pair (the winner leads every eligible dim) ships `tradeoffs: []` and `key_tradeoff: ''`; the F4.2 / W4-10 non-empty-caption pins are FLAG-OFF pins and stay as they are, but the seven nodes that redden when a developer exports T (tests/test_scoring_service.py::TestTradeoffPairs::test_sweep_with_scores_falls_back_to_loser_strongest, ::test_sweep_fallback_winner_idx_1, and the five tests/test_tradeoffs_dedup_parity.py caption/tradeoff pins the red lists) get `monkeypatch.delenv("ENABLE_TIE_IS_NOT_MISSING", raising=False)` AND `monkeypatch.delenv("ENABLE_VALUE_DIM_PARTIAL_SIGNAL", raising=False)` (R14 makes V reach them too) - the K1 pattern, listed by id in pr_text, no other edit to those files; the unit adds its own flag-ON twins (sweep pair under T -> [] / ''). PRODUCT NOTE for pr_text + DECISIONS_AHMED: before flipping either flag, confirm the RunnerUpWinsCard renders its empty state for `tradeoffs: []` (the phone contract was 'always a caption' since F4.2) - a canary item, not this unit's.

R17 (Q4) - CONFIRMED with R14: G reads the two readers as ruled; under renorm the coupled T reader is False, so G is active iff V is ON; test 15 (renorm + T == renorm, tradeoffs included) stays; add the R+V twin: renorm + V never names a void and never ships a margin < 1.0 pair.

R18 (Q5 - the flag-ON golden) - the GREEN generates `tests/fixtures/rubric_truth_flag_on_golden.json` from ITS head through the committed generator `tests/fixtures/_gen_rubric_truth_flag_on_golden.py` (run through a pytest probe, lazy app.* import) and commits it with a `_meta` header (sha, states, the `_grid()` dependency on CATEGORY_SPEC_SCHEMAS); the red's prototype copy (.qa-s68/red_artifacts/proto_rubric_truth_flag_on_golden.json) is reference only; the adversary regenerates from the green bytes and diffs record by record (the expected difference from the prototype copy = exactly R14/R15's effect under V and VT, enumerated).

R19 (gates for the green, all binding) - the unit file in all 8 states (OFF/V/T/VT/R/RV/RT/RVT); the red's mutation table (M1-M4, M6-M17 + the R14/R15 rows) on the GREEN bytes from byte snapshots (sha-verified); E1 the four goldens byte-unchanged; E2 base (04acb757 detached scratch worktree) -> head -> base2 over the 164 records x 3 states record by record; E3 the committed flag-ON golden; E4 tests 6 and 15 + the R17 twin; E5 (re-stated): OFF tradeoffs == base on 169/169; under T and VT the measured set of moved records is enumerated in pr_text; the 244-file comm set base and head in the SAME worktree (C8) under the guard, `comm -13` empty against the 4 baselined ids; the 73-file CI-order set in ONE process per flag state (8 states) with R16's delenv edits applied; the 28-file Preserve set OFF; ruff + py_compile; `git diff --stat -- app/` = app/services/scoring_service.py ONLY. The corpus harness is blind (state it). Run with -p qaren_netguard (this worktree predates the conftest guard).

R20 (pr_text must carry) - both flag rows (default OFF, per call, T coupled to renorm), what moves under T (54/144 grid rows, 11 margins, 6 badges, 0 winner flips, display 78 -> 84 on R02; 32/144 badges with ENABLE_CATEGORY_VALUE_BADGE), what moves under V (0 arithmetic; the emitted missing_data only), the guard's effect (void-named strengths 2 -> 0, margin < 1 claims 66 -> 0 under any flag), the R16 product note, the validate_verdict shape change under T (68/169 records, softened counts move, confidence_adjustment unchanged) as a canary item, the R11 records (home_routes dead compare = W4-12c; excluded_dims vs missing_data under renorm = renorm's own design), the craft-tie xfail (W4-6c = DECISIONS), the activation order (V first alone, then T; each with the flag-ON golden as the canary reference; renorm + T is inert by design), and the 7 delenv ids.
