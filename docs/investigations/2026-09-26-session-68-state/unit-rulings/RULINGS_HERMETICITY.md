# FABLE RED-GATE RULINGS - TEST HERMETICITY (#183 #184 #185 #186) (2026-09-26, session 68; binding for the green, the adversary and the fixer)

RED GATE: PASSED. All three issues reproduced at 61585c58 in the exposing CI order; #186 root-caused (measured, not the issue's guess); the full free tier (18,922 nodes, 17 chunks) ran under the reference guard with 4 failures, all hidden live DNS dependencies; 681 blocked attempts from 196 nodes in 52 files; 43-node pin draft red for the stated reason only. The red's prototypes of the three fixes are ACCEPTED as the design (re-derive them, do not copy bytes).

R1 (#186) - fix at the source: in tests/test_prescoring_showable_guard.py::_run_sync replace `real_service = _ss.get_scoring_service()` with a PRIVATE `real_service = _ss.ScoringService()` (scs.get_scoring_service is already monkeypatched to return it); the singleton is never touched, W4-4 changes nothing. Pins: both orders pass (one subprocess pair each way) and `vars(scoring_service._scoring_service)` carries no `compute_scores` key after the W4-3 sync nodes. Record the mechanism in pr_text: pytest 9.1.1 monkeypatch.setattr on an INSTANCE snapshots the bound method and `undo()` pins it into the instance dict, shadowing the class - a repo-wide hazard for every `monkeypatch.setattr(<singleton instance>, ...)`; grep the tests for other instance-target setattrs on singletons and LIST them in pr_text (do not fix them here unless one trips the sentinel).

R2 (#185) - the subprocess cycle proof exactly as the spec's section C; in-process identity pin (sys.modules entry + package attribute unchanged); CI-order pins platform_router -> shopify_l13 and platform_router -> w49 (subprocess). Leave `_price_service_globals` in the W4-9 file as is; note it can be simplified later.

R3 (#183) - `monkeypatch.setattr(serper_service, "SERPER_API_KEY", "test-key")` replaces setenv + reload in the three tests (measured sufficient: `_resolve_serper_keys` reads the module attribute, `_shopping_primary_countries` reads the env per call); the #183 probe becomes a pin (hotfix -> w49 subprocess pair + the sentinel silent over the file).

R4 (the five identity-only reload leakers: test_backend_cleanup/app.main, test_fan_out_budget_env, test_m18_openai_tpm_sizing, test_observability, test_supplement_branch_genuine) - NOT this unit. They move no value and trip none of the four sentinel items; a proper fix is per-file (private module instances via spec_from_file_location, as W0-3 did). pr_text carries the audit table verbatim and a ready-to-file issue text "tests: five importlib.reload sites leak module IDENTITY (the reload audit of session 68)" - the orchestrator files it via REST after merge.

R5 (the 4 SSRF hidden live dependencies: tests/test_rate_limiting_complete.py::TestUrlDetectSsrf::test_detect_allows_valid_url, tests/test_security_hardening.py::TestSSRFProtection::test_valid_external_url_passes, ::test_valid_http_url_passes, ::TestSecurityIntegration::test_ssrf_protection_integrated) - STUB THE RESOLVER test-side in this unit, never allow_network: the rule under test is "resolves to a public address -> allowed", so a fixture that makes the validator's resolver return a public IP (e.g. 93.184.216.34 for example.com / amazon.ae) exercises exactly that rule offline. Find the resolver the validator actually calls (url_validator's getaddrinfo path AND W0-1's off-loop resolver, whichever the route hits) and stub at that seam; pin that each of the four passes under the guard with 0 attempts.

R6 (IP literals) - ACCEPTED: the guard allows socket.getaddrinfo when `ipaddress.ip_address(host)` parses (no DNS happens; connect/connect_ex/create_connection to a non-loopback literal stay blocked). Pin: getaddrinfo('10.0.0.1', 80) returns; getaddrinfo('192.0.2.1', 443) returns; connect to ('192.0.2.1', 443) raises NetworkBlocked; getaddrinfo('example.com', 443) raises. The 8 SSRF private-IP tests then exercise their real rule (verify one of them fails when the private-IP rule is mutated - a one-off measurement for pr_text, restore sha-verified).

R7 (RFC 2606 sentinels) - keep BLOCKING them; the report classifies every target: `sentinel` (host ends with `.invalid` or is `neutralized.*`), `ip_literal`, `egress` (everything else). tests/.network_attempt_baseline.txt records `<nodeid> <classes comma-joined>` (sorted, generated by `--write`). The ratchet FAILS (exit 1) on a NEW node id whose classes include `egress`; a new sentinel-only or ip_literal-only node prints a WARNING line and exits 0 (the follow-up campaign tightens it). Collection/session-time attempts (the 1 create_connection to neutralized.supabase.invalid at import) are reported under the pseudo-node `<collection>` and never fail the ratchet.

R8 (contracts) - ACCEPTED as drafted: `tests._hermeticity.snapshot(modules=None)` / `compare(before, after) -> list[str]`; both tests._netguard and tests._hermeticity loadable as pytest plugins; sentinel item (1) = "app.services.price_service missing from sys.modules at teardown OR its identity changed OR the package attribute identity changed" (the victim-rebind attribution is moot after R2 but the rule stands); item (3) = `vars(singleton).get('compute_scores')` presence/identity (NOT `__func__`), plus the singleton identity; the LIVE=1 summary line contains `[netguard]` and `not installed`; the terminal line `[netguard] blocked N attempt(s) from M node(s)`; the marker `allow_network` registered in pyproject with a one-line reason; live-tier markers imply allow.

R9 (subprocess pins) - at most FOUR subprocess pytest pins over real files: #186 both orders (2), #185 platform_router -> l13 (1), #183 hotfix -> w49 (1); everything else in-process (the sentinel armed in conftest IS the permanent pin over the fixed files). The report-shape, LIVE=1 and sentinel-teardown pins use tiny synthetic test modules in tmp_path (cheap).

R10 (guard-compat proposals) - (1) per-node attribution via a pytest_runtest_protocol hookwrapper: ACCEPTED; (2) per-file guards under-count our report: DOCUMENT in pr_text, no change; (3) = R6; (4) = R7; (5) tests/test_conftest_env_safety.py::test_sentinel_hosts_really_do_not_resolve_here: give it `@pytest.mark.allow_network` (resolving a `.invalid` name IS its point; RFC 2606 makes the NXDOMAIN deterministic) - one-line edit; (6) curl_cffi stream=True executor path: ACCEPTED - guarded_perform allows when the ContextVar is set OR the handle's EFFECTIVE_URL (curl getinfo) is loopback; pin with a loopback `stream=True` GET against the local http.server; a public streamed GET stays blocked; (7) decode bytes hosts in the report: ACCEPTED; (8) noted for pr_text.

R11 (files this unit may touch) - tests/conftest.py, tests/_netguard.py (new), tests/_hermeticity.py (new), tests/test_hermeticity_pins.py, tests/test_hotfix_shopping_query_clean.py, tests/test_platform_router.py, tests/test_prescoring_showable_guard.py, tests/test_rate_limiting_complete.py, tests/test_security_hardening.py, tests/test_conftest_env_safety.py, pyproject.toml (marker only), scripts/netguard_ratchet.py (new), tests/.network_attempt_baseline.txt (new, generated), .github/workflows/ci.yml (QAREN_NETGUARD_REPORT on the test step + a plain following step "Network-attempt ratchet"), tests/test_ci_gates.py ONLY if it pins the workflow file and needs the new step listed. NOTHING under app/. tests/.pre_impl_failures.txt untouched.

R12 (gates for the green) - (a) tests/test_hermeticity_pins.py green (43 -> all green) and every file in R11 green alone; (b) the four subprocess pins green; (c) the CI-order set .qa-s68/ci_order_set.txt (76 files) in ONE process under the conftest guard: 0 failed except deselected baseline nodes; (d) the FULL tier under the CONFTEST guard in the same 17 chunks (.qa-s68/chunks.txt) with QAREN_NETGUARD_REPORT per chunk: 18,922 run, 0 failed, 0 errors (the 4 SSRF nodes now green), attempting nodes <= 196 minus the fixed ones (state the new count and the delta by node); the merged baseline written by `netguard_ratchet.py --write` from the chunk reports; the ratchet exits 0 against it; (e) the single-process #185 exposure run (.qa-s68/exposure_185_single_process.txt's file list) 0 failed and 0 curl_cffi attempts from shopify_l13; (f) mutation from byte snapshots: remove each guard patch point -> its pin red; remove the IP-literal allowance -> R6 pin red; remove the marker lift -> its pin red; remove the LIVE lift -> its pin red; remove each sentinel item -> its synthetic-leak pin red; revert each of the three fixes -> its reproduction pin red; revert each SSRF stub -> that node red under the guard; ratchet: drop the class rule -> the sentinel-only WARNING pin red; (g) ruff E9,F63,F7,F82 + py_compile on every edited .py; sqlfluff n/a; the ci.yml edit validated by `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`; CRLF hygiene; `git diff --stat -- app/` EMPTY.

R13 (running this unit's tests) - once tests/_netguard.py is wired into conftest, run this unit's pytest WITHOUT `-p qaren_netguard` (the conftest guard is the process-wide guard; two stacked guards under-count the report); every other rule in s68-common.txt stands. Until then (and for any base run in a scratch worktree) keep `-p qaren_netguard`.

R14 (pr_text) - the guard contract and its patch points, the loopback/IP-literal/marker/LIVE rules, the report + baseline + ratchet with the class rule, the measured numbers (18,922 / 4 / 681 / 196 -> the post-fix numbers), the 196-node follow-up campaign by class (69 api.openai.com, 106 sentinel, 28 curl_cffi retailer, 8 SSRF literals, 6 example.com) with the top files, the #186 mechanism + the instance-setattr hazard list, the #183 reload audit table + the R4 issue text, the #185 exposure measurement, the per-file-guard under-count note, "Closes #183, #184, #185, #186".


# FABLE POST-ADVERSARY RULINGS - HERMETICITY (2026-09-26, after the r0/r1/r2 adversaries; binding for fix round 3)

The pipeline ended SOUND on bytes; Fable's diff review APPROVES the design as built. Three items are ruled before the commit.

R15 (sentinel item 3, the lazy-singleton gap - the r2 adversary's defect 1, reproduced) - `tests/_hermeticity.compare` must also cover the transition where `scoring_service._scoring_service` is None at setup and EXISTS at teardown: a singleton CREATED during a test must carry NO instance-level `compute_scores` (i.e. `after["scoring_shadow"]` unset); if it does, that is the #186 class at its source and the test is the leaker. Pins: (a) an in-process compare pin with fake modules where the singleton is None before and created + shadowed after -> a leak string naming the shadow; (b) the same transition created WITHOUT a shadow -> no leak; (c) the existing pre-existing-singleton pins stay. Mutation: revert the new branch -> (a) red. Because the sentinel WIDENS, gate R12(d) (the full tier in the 17 chunks under the conftest guard) re-runs in this round: any test that trips the widened item 3 is a REAL leaker and is fixed at its source (R1's pattern: a private instance) - report it; 0 expected after the #186 fix.

R16 (unpinned resets) - pin `_STATE["node"] = None` in `pytest_runtest_protocol`'s finally: an attempt made after the last test (a session-finish hook in a synthetic conftest) is charged to `<collection>`, never to the last test (mutation A4 red). Tighten `test_ci_runs_the_network_attempt_ratchet` to assert the SAME report file token (`netguard_report.json`) appears in BOTH the test step's env and the ratchet step's command, so a path mismatch between the steps reddens locally instead of only failing CI with exit 2. The `lifted` reset (N8) stays an accepted equivalent (the next protocol re-sets it) - state it.

R17 (accepted as documented limits, no code) - late-thread misattribution (an attempt from a thread that outlives its test is charged to the next test; stamping by origin thread would hide new egress behind pool workers - the fixer's reasoning stands); order-dependent attribution can only shrink; per-file guards under-count the report; libcurl environment proxies are invisible to the proxy check; the Windows proactor connect patch is exercised locally only (CI is Linux/selector). Each is in pr_text's Stated limits; the 90 egress-class baseline nodes are the follow-up campaign; the five identity-only reload sites are the R4 follow-up issue (its text stays in pr_text).

Gates for this round: unit files (11, one process, conftest guard only); the four subprocess pins; the 76-file CI-order set in one process; the #185 single-process exposure run; the FULL 17-chunk tier (R15) with the merged report ratcheted against the committed baseline (exit 0; `--write` must reproduce the committed file byte-identically below the header, or the baseline is regenerated and the delta explained); ruff + py_compile; yaml.safe_load on ci.yml; `git diff --stat -- app/` EMPTY.


# FABLE RULINGS R18-R21 - HERMETICITY (2026-09-26, after the r4 re-adversary returned SOUND with five reproduced minors; binding for fix round 5 - TEST-ONLY, no tier re-run)

Current bytes (the r4 fixer's, verified by the r4 adversary): tests/test_hermeticity_pins.py 99d4cb54..., tests/_hermeticity.py eac6a96b..., tests/_netguard.py aa75b294..., scripts/netguard_ratchet.py 7d5e8991..., tests/.network_attempt_baseline.txt 9e01e319..., conftest b74c2ac9..., ci.yml 4a85abc6..., pyproject c2d43f3f..., the five fixed test files unchanged. The sentinel does not widen this round, so the 17-chunk tier is NOT re-run.

R18 (N8 is NOT equivalent - pin the `lifted` reset) - the adversary is right and R16's premise was wrong: after the LAST test no protocol re-sets `lifted`, so an attempt from a session-finish hook after an `allow_network` (or live-tier) last test would pass UNBLOCKED and unrecorded. Pin it with the adversary's synthetic run (last test marked allow_network + a `pytest_sessionfinish` getaddrinfo in the synthetic conftest): the attempt is BLOCKED and charged to `<collection>`; mutation N8 (`_STATE["lifted"] = False` dropped from the finally) -> red. Correct the `_netguard.py` comment and the pr_text Report bullet (N8 was never equivalent; it is now pinned).

R19 (CI pin shape, the last three survivors) - `test_ci_runs_the_network_attempt_ratchet` additionally asserts on the ratchet step: no `env:` key and no `shell:` key (AD2/AD5), and the report path token in its `run:` is not single-quoted (AD1: `'$RUNNER_TEMP/...'` never expands). Mutations AD1/AD2/AD5 -> red. Narrow the pr_text sentence to exactly what the pin checks (the same report file name in both steps, the test step's env path and the ratchet's argv path both under the runner temp dir, no `if:`/`env:`/`shell:`/`continue-on-error`, argv shape without `|| true`, step order) - CI's own exit 2 remains the arbiter for everything else.

R20 (subprocess pins vs CI `--timeout=60`) - mark the FOUR subprocess pins (#183 hotfix->w49, #185 platform_router->l13->w49, #186 both orders) with `@pytest.mark.timeout(300)` (pytest-timeout's per-test marker overrides the CLI value); state the measured 38-56 s under load in pr_text as the reason.

R21 (accepted in writing) - (a) the committed baseline KEEPS `tests/test_genuine_price_priority.py::TestFlagOffByteIdentity::test_flag_off_timeout_returns_parked_converted` although one `--write` run omits it: the node attempts in 3 of 5 file-alone runs and in the chunk re-run, so dropping it would make CI's ratchet fail intermittently on a spurious NEW egress node; pr_text states this and the ratchet's shrink line is the expected output when it does not attempt. (b) the Stated-limits sentence becomes 'a baseline node that does not attempt only shrinks the set' (the universal 'only ever shrinks' is withdrawn: a timing-dependent node ABSENT from the baseline would appear as NEW and fail the ratchet - that is the ratchet doing its job).

Gates for this round: the 11 unit files in one process; the pin file alone; the R18/R19 pins + mutations N8/AD1/AD2/AD5 from byte snapshots (sha-verified restores); the 76-file CI-order set in ONE process WITHOUT -p qaren_netguard (`--timeout=60` exactly as CI, to prove R20); ruff + py_compile; yaml.safe_load on ci.yml (unchanged); `git diff --stat -- app/` EMPTY; every non-test file keeps its sha (only tests/test_hermeticity_pins.py and the one comment line in tests/_netguard.py may change).


# FABLE RULING R22 - HERMETICITY (2026-09-26 ~13:45, after PR #207's CI came back RED; binding for fix round 6)

PR #207 (commit 41728973 on main 88cb78ae) failed `backend-tests` at COLLECTION: `tests/test_prompt_caching.py:13` runs `tiktoken.encoding_for_model("gpt-4o-mini")` at module level, which on a COLD runner downloads `o200k_base.tiktoken` from `openaipublic.blob.core.windows.net`; under the guard that is `NetworkBlocked` -> `ERROR collecting` -> the job dies before a single test runs (log: scratchpad/ci_log_207_108388136631.txt; 19,316 items / 1 error). The local full-tier measurement could not see it because this box's tiktoken cache (`data-gym-cache` under TEMP) was WARM - a measurement gap the unit must close: cache-backed imports are invisible on a warm box; the cold CI runner is the truth. Two more sites are the same class: `tests/test_exemplar_injection_content.py:53-57` (module-level `encoding_for_model("gpt-4o")` inside a broad `except Exception` -> on a cold runner it silently degrades) and `tests/test_specs_no_fabrication_guard.py:191-192` (in-test `encoding_for_model` after `importorskip` -> a cold runner FAILS with ConnectionError, not a skip). `app/` never imports tiktoken (only `scripts/audit_exemplar_token_cost.py`), so there is no production dependency.

R22a (CI warms the cache BEFORE pytest, explicitly) - `.github/workflows/ci.yml` backend-tests job: set `TIKTOKEN_CACHE_DIR: ${{ runner.temp }}/tiktoken` at the JOB env (every step sees it) and add a step `Warm tiktoken cache` immediately BEFORE `Run unit tests` that runs `python -c "import tiktoken; tiktoken.encoding_for_model('gpt-4o-mini'); tiktoken.encoding_for_model('gpt-4o')"` (both resolve to o200k_base; the step has network, pytest does not). Pin (in tests/test_hermeticity_pins.py, yaml-parsed like the ratchet pin): the warm step exists, precedes the test step, names both models, and the job env carries TIKTOKEN_CACHE_DIR; mutations: warm step removed -> red; warm step after the test step -> red; TIKTOKEN_CACHE_DIR removed -> red.

R22b (one shared loader, never a raw fetch inside pytest) - new `tests/_tokenizer.py` with `encoding_for_model_or_skip(model, *, module_level=False)`: tries `tiktoken.encoding_for_model(model)`; on `tests._netguard.NetworkBlocked`, `requests.exceptions.ConnectionError`, `OSError` it calls `pytest.fail("tiktoken encoding for <model> is not cached and the network is guarded - the CI 'Warm tiktoken cache' step did not run")` when `GITHUB_ACTIONS` is set, else `pytest.skip("tiktoken encoding not cached; warm it once with: python -c \"import tiktoken; tiktoken.encoding_for_model('gpt-4o-mini')\" (network) or set TIKTOKEN_CACHE_DIR", allow_module_level=module_level)`. The three sites use it: test_prompt_caching (module level, `module_level=True`), test_exemplar_injection_content (replace the try/except; keep whatever `_ENC is None` behaviour its tests had ONLY if the file relied on it - measure and state), test_specs_no_fabrication_guard (replace importorskip + the call). Pins: with a fake tiktoken whose `encoding_for_model` raises NetworkBlocked -> under `GITHUB_ACTIONS=true` the helper FAILS with the message, without it SKIPS; a static pin (AST) that no module under tests/ calls `tiktoken.encoding_for_model` / `tiktoken.get_encoding` outside `tests/_tokenizer.py`; mutation: a module-level call re-added in a scratch test module -> the static pin red.

R22c (reproduce the CI failure locally, then prove the fix) - with `TIKTOKEN_CACHE_DIR` pointed at an EMPTY scratch directory under the conftest guard: BEFORE the fix, `pytest --collect-only -q tests/` must reproduce `1 error` on test_prompt_caching (paste); AFTER the fix, `--collect-only` over the whole tree collects with 0 errors, and the three files run: test_prompt_caching skips at module level (locally) / and with `GITHUB_ACTIONS=true` fails with the R22b message (pin); then with the cache WARMED into that scratch dir (one explicit `python -c` warm, network allowed OUTSIDE pytest) the three files pass. Grep the tests for other cache-backed loaders (`nltk.download`, `from_pretrained`, `hf_hub`, `spacy.load`, `torch.hub`) and report (expected none).

R22d (pr_text) - a new section 'CI cold-runner finding (#207 round 1)': the failure, the three sites, why the local tier missed it, the warm step, the helper, the local empty-cache reproduction, and the standing rule: any test-side loader that reads a download cache is warmed in CI by an explicit step and goes through a helper that fails loudly in CI and skips loudly elsewhere.

Gates: the 11 unit files + the three tiktoken files in one process (warm scratch cache); the pin file alone; the R22 pins + mutations from byte snapshots (sha-verified); `pytest --collect-only -q tests/` under the guard with an EMPTY TIKTOKEN_CACHE_DIR -> 0 errors (the CI reproduction); the 76-file CI-order set at --timeout=60 without the plugin; ruff + py_compile; yaml.safe_load; `git diff --stat -- app/` EMPTY. The worktree is now on branch commit 41728973 (rebased onto 88cb78ae) - work on TOP of it as uncommitted changes (never amend; the orchestrator squashes).
