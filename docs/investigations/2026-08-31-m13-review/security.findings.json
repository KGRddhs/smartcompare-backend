{
  "lane": "security",
  "tree": "C:/Users/SynAckITPC/Documents/AI/sc-review-base @ main daa1a42",
  "surface_enumerated": "83 HTTP surfaces: 78 @router.<verb> decorators across 15 files in app/api/ (admin 19, auth 20, url 7, text 8, legal 4, referrals 4, history 3, home 3, profile 3, feedback 2, share 2, image 1, usage 1, version 1) + 3 app-level routes in main.py (/, /health, /favicon.ico) + the implicit /openapi.json + the /admin StaticFiles mount. Plus: 6-file middleware stack, 26 outbound-fetch sites across 11 service modules, 4 secret-handling surfaces (extraction/openai/sentry/.githooks), the search-descriptor store (95 hosts), and both dependency locks.",
  "tools_run": {
    "pip_audit": "pip-audit -r requirements.txt --strict -> 'No known vulnerabilities found' (clean)",
    "npm_audit": "cd SmartCompareApp && npm audit --audit-level=high --package-lock-only -> 35 vulns: 1 critical (tar), 20 high, 13 moderate, 1 low",
    "runtime_proof": "TestClient proof that slowapi default_limits are inert without SlowAPIMiddleware: undecorated route returned [200,200,200,200,200]; decorated route returned [200,200,429,429,429]"
  },
  "findings": [
    {
      "id": "security-01",
      "title": "slowapi default_limits are dead config - 21 routes have NO rate limit at all",
      "file": "app/main.py",
      "line": 110,
      "severity": "P1",
      "category": "rate-limiting",
      "claim": "`limiter` is registered only as `app.state.limiter` and `SlowAPIMiddleware` is never added, so the `default_limits=[DAILY_LIMIT, ANON_LIMIT]` configured at app/middleware/rate_limiter.py:29 apply to nothing and the 21 routes without an explicit `@limiter.limit(...)` decorator are entirely unthrottled.",
      "evidence": "app/main.py:110 sets `app.state.limiter = limiter` and adds ErrorHandler/SecurityHeaders/RequestID middleware only - `grep -rn 'SlowAPIMiddleware' app/ tests/` returns zero hits. slowapi/middleware.py `SlowAPIMiddleware.dispatch` is the ONLY consumer of default limits (`_should_exempt` returns early for routes in `limiter._route_limits`, i.e. decorated ones). Verified by running a TestClient repro: an undecorated route under `default_limits=['2/minute']` returned 200 five times in a row; the decorated twin returned 429 on the third. Enumerated the 21 undecorated routes by script: auth_routes.py:548 POST /logout, :563 GET /me, :615 GET /verify, :628 PUT /profile, :638 PUT /email, :654 PUT /password, :711 GET /preferences, :758 PUT /preferences, :1018 GET /cohort-profile; text_routes.py:695 DELETE /cache, :730 GET /parse; url_routes.py:46 GET /retailers; usage_routes.py:9 GET /status; version_routes.py:19 GET /version; legal_routes.py:17,18,34,35; referral_routes.py:231 GET /status, :248 GET /invite/{share_token}, :275 POST /invite/{share_token}/quiz.",
      "failure_scenario": "PUT /api/v1/auth/password (auth_routes.py:654) and PUT /api/v1/auth/email (:638) both take `current_password` and are verified against Supabase. With a stolen/borrowed access token (a shared device, a leaked token, an XSS in a webview) an attacker can submit unlimited `current_password` guesses at full network speed - there is no per-route limit, no default limit, and `check_account_locked` is only consulted on POST /login, never here. The same hole makes POST /api/v1/referrals/invite/{token}/quiz an unauthenticated, unbounded work amplifier.",
      "fix_sketch": "Either `app.add_middleware(SlowAPIMiddleware)` in main.py so `default_limits` actually apply, or decorate the 21 routes individually. The credential-checking ones (PUT /email, PUT /password) need a tight explicit limit (e.g. 5/minute) plus the same `check_account_locked`/`track_failed_login` treatment POST /login gets.",
      "known_item": false
    },
    {
      "id": "security-02",
      "title": "Rate-limit key and audit ip_address are the Railway edge-proxy IP, not the client",
      "file": "app/middleware/rate_limiter.py",
      "line": 27,
      "severity": "P1",
      "category": "rate-limiting",
      "claim": "`get_remote_address` returns `request.client.host` (the immediate TCP peer) and nothing in the repo enables or trusts proxy headers, so behind Railway's edge every request shares ONE rate-limit bucket and every `admin_audit_log.ip_address` row records the proxy, not the actor.",
      "evidence": "app/middleware/rate_limiter.py:26-30 builds `Limiter(key_func=get_remote_address, storage_uri='memory://')`; slowapi's `get_remote_address` is literally `return request.client.host` (inspected in the installed package) - it never reads `X-Forwarded-For`. `grep -rni 'x-forwarded|forwarded_allow|proxy-headers|proxy_headers|cf-connecting' app/ scripts/ Procfile railway.json .github/` returns ZERO hits. Procfile:1 and railway.json `startCommand` are both bare `uvicorn app.main:app --host 0.0.0.0 --port $PORT`, and uvicorn's ProxyHeadersMiddleware only rewrites `client` for peers in `--forwarded-allow-ips` (default 127.0.0.1), which Railway's edge is not. The only consumers of a real client IP are auth_routes.py:437,465,488,506 which pass `request.client.host` as `ip_address` into `log_audit_event`.",
      "failure_scenario": "Two concrete consequences. (a) Availability: POST /auth/login is `5/minute` - if all callers hash to one key, five failed logins anywhere in the world lock the login endpoint for every user for the rest of that minute, and 10/minute on /text/compare caps the entire product at 10 comparisons/minute. (b) Forensics: the `brute_force_lockout` / `login_failed` audit rows that `admin_routes.py:194 /audit-log` exists to serve all carry the same proxy IP, so the abuse-investigation surface built in Bundle A/D cannot attribute anything.",
      "fix_sketch": "Run uvicorn with `--proxy-headers --forwarded-allow-ips='*'` (safe only because Railway terminates at its own edge), or supply a `key_func` that reads the left-most untrusted-but-useful `X-Forwarded-For` entry, and use the same helper for the `ip_address` audit field. Add a test asserting two requests with different XFF get different limiter keys.",
      "known_item": false
    },
    {
      "id": "security-03",
      "title": "GET /api/v1/auth/verify returns the caller's JWT in the response body, breaking the invariant recorded in auth_service",
      "file": "app/api/auth_routes.py",
      "line": 624,
      "severity": "P1",
      "category": "secret-exposure",
      "claim": "`verify_auth` returns `\"user\": current_user` wholesale, and `verify_token` puts the raw `access_token` into that dict, so every /auth/verify response echoes a live Supabase JWT into a response body - the exact dict the code comments forbid handling loosely.",
      "evidence": "app/api/auth_routes.py:615-625: `@router.get('/verify')` -> `return {'success': True, 'valid': True, 'user': current_user}`. app/services/auth_service.py:255-260 returns `{'id':..., 'email':..., 'access_token': access_token}`. The docstring at auth_service.py:239-243 states verbatim: 'Security note: do NOT log the full `current_user` dict at any call site (it now contains a secret). Existing log statements use only `current_user['id']` - verified safe at audit time.' The audit that added `access_token` (H4, 2026-05-22) checked log statements but not response bodies. Every other endpoint that returns a user (GET /me, :570-581) builds an explicit whitelist dict; /verify is the one that does not.",
      "failure_scenario": "A user calls /auth/verify on app start (CLAUDE.md: 'Useful for checking auth status on app startup'). The JWT now appears in: the mobile client's own response logging, any APM/response-capture, Sentry's Starlette integration if the request later errors (response bodies are not covered by `_before_send`'s header/query scrubbing), and any intermediary that logs bodies. A token that should exist only in expo-secure-store and the Authorization header is duplicated into a payload nobody treats as secret.",
      "fix_sketch": "Return an explicit whitelist, mirroring GET /me: `{'success': True, 'valid': True, 'user': {'id': current_user['id'], 'email': current_user.get('email')}}`. Add a regression test asserting `'access_token' not in json.dumps(response.json())` for /verify (and sweep every route that returns a `current_user`-derived object).",
      "known_item": false
    },
    {
      "id": "security-04",
      "title": "Magento GraphQL adapter POSTs to a URL taken verbatim from a remote configs.json (unvalidated SSRF sink)",
      "file": "app/services/magento_graphql_service.py",
      "line": 1039,
      "severity": "P1",
      "category": "ssrf",
      "claim": "Shape-A resolution fetches `https://{host}/configs.json`, reads `commerce-endpoint` out of the remote JSON with no scheme/host/IP validation, caches it for 24h, and then POSTs the GraphQL query - plus the harvested `x-api-key` header - to whatever URL that remote document named.",
      "evidence": "app/services/magento_graphql_service.py:428-445 `_harvest_shape_a_config` GETs `f'https://{host}/configs.json'`. :391-425 `_parse_configs` does `endpoint = flat.get('commerce-endpoint')` and returns it in the cfg dict with no validation beyond a truthiness check at :414. :1027-1042 builds the header dict (`x-api-key`, `Magento-*`) and calls `_post_graphql(cfg['endpoint'], _SHAPE_A_QUERY, {...}, headers)`. :507-527 `_post_graphql` calls `curl_requests.post(url, ..., allow_redirects=True)` directly. `grep -rn 'validate_external_url' app/services/magento_graphql_service.py` returns ZERO hits - the SSRF validator is imported by url_routes, price_service, shopify_pdp_service and url_extraction_service, never here.",
      "failure_scenario": "A registry host in `_MAGENTO_STORES` is compromised (or a hostile operator publishes a poisoned config) and serves `{\"data\":[{\"key\":\"commerce-endpoint\",\"value\":\"http://169.254.169.254/latest/meta-data/\"}, ...]}`. Every subsequent Shape-A price lookup for 24h POSTs from inside the Railway container to that address, carrying our request headers, and the response body is JSON-parsed and fed into the price cascade. `http://<service>.railway.internal:<port>/` works the same way. Gated by `ENABLE_MAGENTO_GQL_ADAPTER` (default OFF), which is why this is latent rather than live - it is on the activation runbook's step (1).",
      "fix_sketch": "Before caching a harvested cfg, require `urlparse(endpoint).scheme in ('https',)` AND `_host_on_domain(endpoint, host)` (or an explicit per-store allowlist of endpoint hosts, since Alshaya endpoints are stable), AND `validate_external_url(endpoint)`. Same treatment for `base_endpoint` and for the jomashop persisted-query endpoint at :469-495. Pin it with a test that a poisoned configs.json yields None, not a POST.",
      "known_item": false
    },
    {
      "id": "security-05",
      "title": "The Codex HIGH-5 SSRF hardening was never propagated to the M5-M12 adapters - 17 fetch sites follow redirects unvalidated",
      "file": "app/services/salla_service.py",
      "line": 98,
      "severity": "P2",
      "category": "ssrf",
      "claim": "The price HTML path (`curl_fetch_html_same_site`) and the sitemap builder (`_default_fetch`) were explicitly hardened to validate the initial URL and EVERY redirect hop, but the newer adapters all call curl_cffi with `allow_redirects=True` and no per-hop check, so a 30x from any registry host walks the fetch off-domain and into private space.",
      "evidence": "The hardened pattern is documented at app/services/price_service.py:13672-13700 ('Redirect-validating same-site HTML fetch (Codex HIGH-5 SSRF fix)') and re-stated at app/services/sitemap_discovery_service.py:699-717. The unhardened sites, all `allow_redirects=True` with no `validate_external_url` and no `_host_on_domain`: salla_service.py:98 (storefront GET `https://{domain}/`) and :217 (search API); magento_graphql_service.py:445, :491, :525, :654; occ_service.py:289; rest_json_service.py:268, :326, :411; yotpo_service.py:266; judgeme/algolia_service.py:246,:262; unbxd_service.py:197; woocommerce_service.py:294; noon_service.py:147; and price_service.py:13638 (`curl_fetch_html`, still live), :14022, :14065, :15039, :15101. `grep -rn 'validate_external_url' app/services/` shows the validator reaches only price_service, shopify_pdp_service, url_extraction_service and sitemap_discovery_service.",
      "failure_scenario": "reefperfumes.com (a live Salla registry row) is compromised and its root responds `302 Location: http://10.x.x.x:8000/admin/`. `_resolve_store_id` (salla_service.py:88-108) follows it and hands the internal response body to `_extract_store_id`. For price_service.py:13638 `curl_fetch_html`, the URL is not even registry-pinned: structured_comparison_service.py:2321 feeds it a link taken from a Serper organic result, so the fetch target is influenced by the user's own query text.",
      "fix_sketch": "Give the adapters the same helper: a shared `fetch_json_same_site(url, domain)` that mirrors `curl_fetch_html_same_site` (allow_redirects=False, per-hop `validate_external_url` + `_host_on_domain`, hop cap, body cap). Retire the bare `curl_fetch_html` or route its two remaining callers through the same-site variant bound to the URL's own registrable domain.",
      "known_item": false
    },
    {
      "id": "security-06",
      "title": "/openapi.json is still public in production - only the docs UIs were disabled",
      "file": "app/main.py",
      "line": 79,
      "severity": "P2",
      "category": "information-disclosure",
      "claim": "`docs_url` and `redoc_url` are set to None under `RAILWAY_ENVIRONMENT`, but `openapi_url` is left at its FastAPI default, so the complete machine-readable API schema - every admin route, every parameter, every model - is served unauthenticated at /openapi.json in prod.",
      "evidence": "app/main.py:79-80: `docs_url=None if os.getenv('RAILWAY_ENVIRONMENT') else '/docs'`, `redoc_url=...` - no `openapi_url` argument. Verified against the installed fastapi 0.115.0: `FastAPI(docs_url=None, redoc_url=None).routes` -> `['/openapi.json']`. CLAUDE.md's security section claims '**Swagger** disabled in prod' and the App-Store routine treats that as done.",
      "failure_scenario": "`curl https://web-production-58776.up.railway.app/openapi.json` returns the full contract including the 19 `/api/v1/admin/*` endpoints, the `X-Admin-Key` header name, the `/api/v1/text/cache` DELETE and `/api/v1/text/parse` debug routes, and every request-model field name and constraint - handing an attacker the exact enumeration this review had to derive by reading source.",
      "fix_sketch": "`openapi_url=None if os.getenv('RAILWAY_ENVIRONMENT') else '/openapi.json'` on the FastAPI constructor. Add a test that asserts a 404 on /openapi.json when RAILWAY_ENVIRONMENT is set.",
      "known_item": false
    },
    {
      "id": "security-07",
      "title": "The last 10 characters of OPENAI_API_KEY are written to logs at INFO on every cold client init",
      "file": "app/services/extraction_service.py",
      "line": 45,
      "severity": "P2",
      "category": "secret-exposure",
      "claim": "`get_client()` logs `f'Initializing OpenAI client with key ending in: ...{api_key[-10:]}'` at INFO level, putting a 10-character slice of a live credential into Railway logs and any log sink downstream.",
      "evidence": "app/services/extraction_service.py:41-51: the lazy initializer reads `os.getenv('OPENAI_API_KEY')` then `logger.info(f'Initializing OpenAI client with key ending in: ...{api_key[-10:] if api_key else \"NONE\"}')` BEFORE constructing `AsyncOpenAI`. logging_config.py routes this to structured JSON on stdout, which Railway retains. Nothing redacts it: the Sentry scrubbers in sentry_service.py only run on Sentry events, not on stdout.",
      "failure_scenario": "Anyone with Railway log access (or a future log-shipping integration, or a support screenshot) gets a 10-char suffix of the production OpenAI key. That is a meaningful head start on a partially-leaked key and it materially weakens the key-rotation posture CLAUDE.md documents elsewhere ('Full keys ONLY in gitignored .env'). It fires on every process cold start.",
      "fix_sketch": "Log presence and length only - `logger.info('OpenAI client initialized (key present=%s, len=%d)', bool(api_key), len(api_key or ''))` - or drop the line entirely; the `[models] ...` startup line already identifies the deployment.",
      "known_item": false
    },
    {
      "id": "security-08",
      "title": "Sentry's OpenAI-key scrubber only matches sk-proj- keys; classic sk- and sk-svcacct- keys pass through unredacted",
      "file": "app/services/sentry_service.py",
      "line": 17,
      "severity": "P2",
      "category": "secret-exposure",
      "claim": "`_SENSITIVE_PATTERNS` matches `sk-proj-[A-Za-z0-9_-]+` specifically, and no other pattern in the list can catch a non-project OpenAI key, so any key not in the `sk-proj-` format reaches Sentry in exception strings and breadcrumb messages verbatim.",
      "evidence": "app/services/sentry_service.py:15-21 - the five patterns are a JWT triple-segment regex, `sk-proj-...`, `fc-[a-f0-9]{20,}`, `[a-f0-9]{32,}` (lowercase hex only), and `Bearer\\s+...`. A classic `sk-<48 mixed-case base62>` or `sk-svcacct-...` key matches none of them: it is not `sk-proj-`, it is not all-lowercase-hex, and it is not preceded by `Bearer` when it appears inside an OpenAI SDK error string. `_scrub_string` (:93-97) applies only that list; the key-name denylist at :90 only helps for dict-valued fields, not for free text.",
      "failure_scenario": "An OpenAI SDK error (`AuthenticationError`, a 401 body echo, or a `str(e)` from openai_service) that embeds the key hits `capture_exception` in ErrorHandlerMiddleware (error_handler.py:152-154). `_before_send` scrubs `exception.values[].value` through `_scrub_string`, which leaves the key intact, and it is stored in Sentry. Note the repo's own comment at sentry_service.py:9-14 says the pattern list is 'defense-in-depth against future provider keys' - this is the gap it was meant to close.",
      "fix_sketch": "Broaden to `sk-(proj-|svcacct-)?[A-Za-z0-9_-]{20,}` and add the shapes the repo actually uses that are currently uncovered: Upstash REST tokens, `GOCSPX-`, Railway/EAS tokens. Pin each with a test in tests/ that feeds a fake key of each shape through `_scrub_string`.",
      "known_item": false
    },
    {
      "id": "security-09",
      "title": "The pre-commit credential guard misses the repo's highest-value secret shapes, including the Supabase service-role JWT",
      "file": ".githooks/pre-commit",
      "line": 58,
      "severity": "P2",
      "category": "secrets-handling",
      "claim": "The staged-diff secrets grep covers only `sk-`, `AKIA`, `xox[baprs]-` and PEM private keys - it does not cover `SUPABASE_SERVICE_KEY` (an `eyJ...` JWT that bypasses RLS on every user table), the Serper/Scrape.do/Zyte hex keys, `fc-` Firecrawl keys, Upstash REST tokens, or `GOCSPX-` Google client secrets.",
      "evidence": ".githooks/pre-commit:47-59 - the guard is `grep -qE '(\\bsk-[A-Za-z0-9_-]{20,}|AKIA[0-9A-Z]{16}|xox[baprs]-[A-Za-z0-9-]{10,}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----)'`. Every credential named in CLAUDE.md's 'Environment Variables (Railway) -> Required' block except OPENAI_API_KEY falls outside that set. Note the contrast: sentry_service.py:16 DOES carry a JWT pattern, so the shape is known to the codebase - the hook just never got it. Secondary gap on the same guard: :61 `grep -qxE '\\.env(\\..*)?'` is full-line anchored, so it blocks a root `.env` but not `SmartCompareApp/.env` or `backend/.env` (the .gitignore catches those, so this is defence-in-depth only).",
      "failure_scenario": "An agent or a human pastes a Supabase service-role key into a runbook, a `docs/investigations/*.md` evidence dump, or a debug script and commits it. The hook passes silently. That key grants full RLS-bypassing read/write on users, comparisons, referral_invites and admin_audit_log - the single worst secret in the system - and once pushed it is in history forever. The repo has already had a near-miss of exactly this class: .gitignore lines 55-59 record that `.qa-bh-sourcing/_f3_nasser_main.js` carried a scraped live token and was fail-closed by gitignore.",
      "fix_sketch": "Add `eyJ[A-Za-z0-9_-]{20,}\\.eyJ[A-Za-z0-9_-]{20,}\\.`, `fc-[a-f0-9]{20,}`, `GOCSPX-[A-Za-z0-9_-]{20,}`, and a `(SUPABASE_SERVICE_KEY|SERPER_API_KEY|SCRAPEDO_API_TOKEN|ZYTE_API_KEY|UPSTASH_REDIS_TOKEN|ADMIN_API_KEY)\\s*[:=]\\s*['\\\"]?[A-Za-z0-9_.-]{16,}` name-anchored rule to the alternation; change :61 to `grep -qE '(^|/)\\.env(\\..*)?$'`.",
      "known_item": false
    },
    {
      "id": "security-10",
      "title": "Search-descriptor url_template is never host- or scheme-checked, and it is derived from remote homepage HTML",
      "file": "app/services/search_descriptor_service.py",
      "line": 236,
      "severity": "P2",
      "category": "ssrf",
      "claim": "`format_search_url` substitutes `{q}` into a stored `url_template` after checking only kind/robots_allowed/'{q}' presence - there is no check that the template's host belongs to the registry row it is keyed on - while `search_form_template` builds that template by `urljoin`-ing a search-`<form action>` read out of the store's own homepage, so remote content decides where the resulting URL points.",
      "evidence": "app/services/search_descriptor_service.py:132-143 `usable` checks `kind in ('platform_api','onsite_html')`, `robots_allowed`, and `'{q}' in url_template` - nothing about the host. :228-238 `format_search_url` then does `descriptor.url_template.replace('{q}', quote_plus(q))`. :380-389 `search_form_template` does `url = urljoin(base_url, action or '/')` and accepts it as long as `parsed.scheme and parsed.netloc` - an absolute `action=\"https://third-party.example/s\"` passes. scripts/resolve_search_descriptors.py:232-239 persists whatever `probe_search_descriptor` returned with no host assertion. The consumer is app/services/price_service.py:9091-9142 `_descriptor_search_url` -> `build_retailer_url`, whose output is surfaced as `price.url` / `rating_source.url` and opened by the app's `openRatingSource()`. I audited the shipped store (data/search_descriptors.json, 95 hosts: 43 platform_api, 42 sitemap, 6 none, 4 onsite_html) and confirmed 0 off-host templates today - so this is an unenforced invariant, not a live break.",
      "failure_scenario": "Third-party hosted search is common (Searchanise, Algolia proxies), so a legitimate re-run of `resolve_search_descriptors.py --live` can persist an off-host template without anyone noticing; a compromised or hostile storefront can do it deliberately by serving `<form role=search action=\"https://attacker.tld/collect\">`. With `ENABLE_SEARCH_DESCRIPTOR=true` (already SEEDED per CLAUDE.md, runbook step 6 is 'a pure flag-flip'), `build_retailer_url` then hands users an attacker-controlled link labelled as the retailer, and the same URL is a server-side fetch target for any caller that fetches what build_retailer_url returns. `http://169.254.169.254/?q=` also satisfies the scheme+netloc test.",
      "fix_sketch": "Add a host assertion to `SearchDescriptor.usable` (or, better, at parse time in `parse_search_descriptor`): the template's `urlparse().hostname` must equal or be a subdomain of the registry row's domain, and the scheme must be https. Reject at resolution time in `search_form_template` too, so an off-host action is recorded as `kind='none'` rather than persisted. Pin with a test that a template pointing at another host is not `usable`.",
      "known_item": false
    },
    {
      "id": "security-11",
      "title": "Paid-work endpoints are unauthenticated and outside the freemium gate; the 'internal' KPI endpoint has the loosest limit of all",
      "file": "app/api/text_routes.py",
      "line": 636,
      "severity": "P2",
      "category": "authn-authz",
      "claim": "The usage gate is applied only `if user and user.get('id')`, so anonymous callers get unmetered OpenAI+Serper work on /text/compare, /text/compare/stream, /text/quick, /text/prices/{product}, /text/price-kpi and /image/identify - and /text/price-kpi, documented as an 'Internal/measurement surface', is unauthenticated at 30/minute, three times the limit of /compare.",
      "evidence": "app/api/text_routes.py:155-157 `if user and user.get('id'): usage_check = await check_usage_allowed(...)` - the anonymous branch does nothing (same shape at :303 and :440). :636-692 `price_kpi` is `@limiter.limit('30/minute')` with NO auth dependency and calls `parse_product_query(q)` (an OpenAI chat completion, extraction_service.py:1302) plus `service._get_price(...)` (the full Serper price cascade). :562-592 `quick_compare` has no auth dependency and no usage check at all. :595-633 `get_gcc_prices` is 20/minute, no auth. app/api/image_routes.py:57-65 `identify_and_compare` takes `get_optional_user` and never calls `check_usage_allowed`, then runs GPT-4o-mini vision + a full comparison. `grep check_usage_allowed app/api/` shows the gate exists ONLY in text_routes.",
      "failure_scenario": "An unauthenticated script hitting `GET /api/v1/text/price-kpi?q=<random>&nocache=true` runs one OpenAI parse plus the escalation-heavy price cascade per call (CLAUDE.md: '~10-15 [Serper] credits each post-B.0'). At 30/min that is ~450 Serper credits and 30 OpenAI calls per minute from one caller with no account - and because `SERPER_LIFETIME_LIMIT` ships UNSET the spend gate is INERT (CLAUDE.md, Serper spend controls), so nothing stops it before the key is drained. The freemium tier this project sells (3 lifetime / 10 per month) is bypassed simply by not sending an Authorization header.",
      "fix_sketch": "Put /text/price-kpi behind `Depends(verify_admin_key)` like its sibling debug routes /text/cache and /text/parse. For the public compare surfaces, extend the usage gate to anonymous callers keyed on `X-Device-Fingerprint` (the header already exists and is regex-validated at auth_routes.py:22/366) or require auth. Add the gate to /image/identify and /text/quick.",
      "known_item": false
    },
    {
      "id": "security-12",
      "title": "POST /text/compare body fields carry no max_length, unlike their GET twins",
      "file": "app/api/text_routes.py",
      "line": 46,
      "severity": "P2",
      "category": "input-validation",
      "claim": "`TextCompareRequest.query`, `.product_a`, `.product_b`, `.region` and `.selected_category` are bare `Optional[str]`/`str` with no `Field(max_length=...)`, while the GET variants of the same parameters are capped at 500 / 80 / 20 - so the POST path accepts an unbounded string where the GET path rejects it.",
      "evidence": "app/api/text_routes.py:46-53 - `query: Optional[str] = None`, `product_a: Optional[str] = None`, `product_b: Optional[str] = None`, `region: str = 'bahrain'`, `selected_category: Optional[str] = None`. Contrast :246-249 (GET /compare) `q: Optional[str] = Query(None, max_length=500)`, `product_a: ... max_length=80`, and :641 `region: str = Query('bahrain', max_length=20)`. Same gap on `QuickCompareRequest` at :69-73 (`product1: str`, `product2: str`, no caps). Downstream, `parse_product_query` truncates at 500 (extraction_service.py:1312) but `structured_comparison_service` passes `explicit_pair` values through a separate path sanitized at only 80 (:3009, :3501), and the raw string is logged in full at text_routes.py:137 and written to `search_logs.query`.",
      "failure_scenario": "`POST /api/v1/text/compare` with a 5 MB `query` value: the string is logged in full at :137 (`logger.info(f'Text comparison request: {body.query}')`), written to `search_logs` by `log_search`, and carried through the orchestrator. FastAPI applies no default body-size limit, so this is an unauthenticated log-flood / row-bloat primitive, and the asymmetry means a validation fix applied to the GET route gives false confidence about the POST one.",
      "fix_sketch": "Mirror the GET constraints on the model: `query: Optional[str] = Field(None, max_length=500)`, `product_a/product_b: Optional[str] = Field(None, max_length=80)`, `region: str = Field('bahrain', max_length=20)`, `selected_category: Optional[str] = Field(None, max_length=32)`, and the same on `QuickCompareRequest`. Truncate or hash the query in the :137 log line.",
      "known_item": false
    },
    {
      "id": "security-13",
      "title": "Raw exception text is returned to the client on the camera comparison path",
      "file": "app/api/image_routes.py",
      "line": 267,
      "severity": "P2",
      "category": "error-leakage",
      "claim": "`identify_and_compare` catches every exception from the comparison and returns `{'error': str(e), ...}` at HTTP 200, bypassing the unified error envelope that exists precisely to keep internal detail out of responses.",
      "evidence": "app/api/image_routes.py:247-271 - `except Exception as e:` then `return {'success': False, 'action': 'comparison_failed', 'error': str(e), 'products': products, ...}`. Compare app/middleware/error_handler.py:131-162 `ErrorHandlerMiddleware`, which deliberately replaces any unhandled exception with the constant string 'Internal server error'. The route is `get_optional_user`, so an anonymous caller receives this. The same `str(e)` is also written to `search_logs.error_message` at :258, which is fine.",
      "failure_scenario": "A Supabase/httpx/asyncpg failure inside `compare_from_text` surfaces its message to an anonymous HTTP client - these messages routinely embed hostnames, table names, row counts, Postgres error codes, and upstream URLs. An attacker uploads two images repeatedly and reads the differences in exception text to map the backend, which is exactly the reconnaissance the unified envelope was introduced to prevent.",
      "fix_sketch": "Return a constant user-facing message plus the `request_id` (already on `request.state.request_id`) and keep `str(e)` in the log/Sentry path only, matching `_surface_comparison_failure` in text_routes.py:89-112.",
      "known_item": false
    },
    {
      "id": "security-14",
      "title": "npm audit --audit-level=high is NOT clean (1 critical, 20 high) and the CI job that would catch it is continue-on-error",
      "file": ".github/workflows/ci.yml",
      "line": 185,
      "severity": "P2",
      "category": "dependencies",
      "claim": "`npm audit --audit-level=high --package-lock-only` in SmartCompareApp/ reports 35 advisories (1 critical, 20 high), including a HIGH on `axios` - a direct runtime dependency shipped in the app bundle - while the CI `dependency-audit` job is `continue-on-error: true`, so its 'green' carries no signal and CLAUDE.md's App-Store production gate ('npm audit --audit-level=high clean') is currently failing.",
      "evidence": "Raw run (this tree, 2026-08-31): `35 vulnerabilities (1 low, 13 moderate, 20 high, 1 critical)`. Critical: `tar` (node-tar PAX size override / parser confusion), transitive. HIGH with `isDirect=true`: `axios` ('Axios Node HTTP adapter can use an inherited proxy after interceptor config cloning'; 'Axios form serializer') pinned at `^1.13.4` in SmartCompareApp/package.json:20, and `expo` (~54.0.33). The remaining highs are Expo/Metro build tooling (metro, metro-config, image-size, postcss, shell-quote, js-yaml, markdown-it, linkify-it, brace-expansion, nanoid, undici, ws, form-data, fast-uri). .github/workflows/ci.yml:183-187: `# NON-BLOCKING until the current high/critical advisory count is recorded and triaged` + `continue-on-error: true`. By contrast `pip-audit -r requirements.txt --strict` is CLEAN on this tree ('No known vulnerabilities found').",
      "failure_scenario": "CLAUDE.md's 'Routine before App Store production submission' lists `npm audit --audit-level=high` clean as a tick-box, and the 2026-08-25 handoff records `dependency-audit` as 'green'. Both are true only because the step cannot fail. axios is the one advisory that ships to devices rather than staying in the build toolchain, and it is the HTTP client every backend call in `api.ts` goes through.",
      "fix_sketch": "Bump axios to a patched 1.x and re-run; then either flip `continue-on-error` off or convert the step into a 'no NEW advisories vs a checked-in baseline' gate so the count cannot silently grow. Record the accepted build-tooling advisories in that baseline with a reason.",
      "known_item": true
    },
    {
      "id": "security-15",
      "title": "Referral invite endpoints are unauthenticated, unrate-limited, and their path/query params are unvalidated",
      "file": "app/api/referral_routes.py",
      "line": 248,
      "severity": "P2",
      "category": "rate-limiting",
      "claim": "`GET /invite/{share_token}` and `POST /invite/{share_token}/quiz` use `get_optional_user` (so anonymous is fine by design), carry NO `@limiter.limit` decorator, and declare `share_token: str` and `ref: str` with no pattern or length bound - unlike the analogous share_routes token which is regex-pinned.",
      "evidence": "app/api/referral_routes.py:248-253 - `async def resolve_invite(share_token: str, ref: str, user: Optional[dict] = Depends(get_optional_user))`; :275-280 same shape for the quiz POST. Neither has `@limiter.limit`, and per finding security-01 `default_limits` never apply. Contrast app/api/share_routes.py:56 which pins the token as `Path(..., pattern=r'^[A-Za-z0-9_-]{18,30}$')`. The quiz handler calls `service.run_invitee_quiz(...)` (referral_service) which resolves the invite and re-scores a stored comparison per request.",
      "failure_scenario": "With `ENABLE_REFERRAL_SYSTEM=true` (flipped in Railway per CLAUDE.md), an anonymous client can hammer `POST /api/v1/referrals/invite/<anything>/quiz` at full speed - each call is a DB round-trip plus a re-score - with no limit and no auth. The unbounded `share_token`/`ref` also means arbitrary-length strings reach the PostgREST filter values.",
      "fix_sketch": "Add `@limiter.limit('20/minute')` (and `10/minute` on the quiz POST) and pin the params: `share_token: str = Path(..., pattern=r'^[A-Za-z0-9_-]{18,30}$')`, `ref: str = Query(..., pattern=r'^QR-[A-HJ-NP-Z2-9]{6}$')` - the invite-code alphabet is already defined at auth_routes.py:15.",
      "known_item": false
    },
    {
      "id": "security-16",
      "title": "Anonymous feedback/events writes use the service-role client with a client-supplied, unvalidated comparison_id",
      "file": "app/services/feedback_service.py",
      "line": 113,
      "severity": "P2",
      "category": "authz",
      "claim": "`save_feedback` and `track_events_batch` write through `get_supabase_client()` (the deprecated alias that returns the RLS-bypassing admin client) and attach whatever `comparison_id` the caller sent, with no ownership check and no UUID/length validation on the field.",
      "evidence": "app/services/feedback_service.py:113 `client = get_supabase_client()` then :126 `client.table('comparison_feedback').insert(record)`; :145 and :174 the same for `user_events`. app/services/database_service.py:56-59 - `get_supabase_client()` is documented DEPRECATED and 'Returns admin client'. The value comes straight from app/api/feedback_routes.py:72 `comparison_id: Optional[str] = None` (no `Field(pattern=...)`, no max_length) and :91 the same on `EventItem`, both reached through `get_optional_user` so no auth is required at all.",
      "failure_scenario": "An anonymous caller POSTs 50 events per request at 60 req/min, each pinned to another user's `comparison_id` harvested from a share link. The rows land under the service role (RLS cannot stop them) and poison `comparison_feedback` / `user_events` - the tables that feed the /admin/cohort dashboards, the pain-workflow derivation, and `save_comparison_and_track_cohort`. Nothing in the pipeline can distinguish the forged rows afterwards.",
      "fix_sketch": "Validate `comparison_id` as a UUID at the Pydantic layer, and when the caller is authenticated write through `get_user_supabase_client(access_token)` so RLS applies. For anonymous writes, either drop `comparison_id` or verify the id resolves to a row with a live share_token before persisting.",
      "known_item": false
    },
    {
      "id": "security-17",
      "title": "The admin static mount accepts the admin key over HTTP Basic with no rate limit, so key-guessing is unbounded",
      "file": "app/main.py",
      "line": 195,
      "severity": "P3",
      "category": "rate-limiting",
      "claim": "`_AdminAuthenticatedStaticFiles` validates the same `ADMIN_API_KEY` that `/api/v1/admin/*` protects, but the mount is an ASGI app outside the router layer, so none of slowapi's decorators apply and (per security-01) no default limit exists either - guessing attempts against /admin/*.html are unthrottled while the JSON endpoints are 30/minute.",
      "evidence": "app/main.py:165-219 - the StaticFiles subclass compares `X-Admin-Key` (:191) or the HTTP Basic password (:195-204) against `os.getenv('ADMIN_API_KEY')` with `hmac.compare_digest`, then `app.mount('/admin', ...)`. There is no limiter involvement anywhere in the class, and the mount is registered directly on `app`. app/api/admin_routes.py:43 shows the JSON side does carry `@limiter.limit('30/minute')`.",
      "failure_scenario": "An attacker points a Basic-auth brute-forcer at `https://<host>/admin/costs.html`. If ADMIN_API_KEY is anything short of high-entropy (it is an operator-chosen env var, not a generated one), the attempt rate is bounded only by network throughput. A success yields the service-role-backed analytics surface AND the working key for all 19 /api/v1/admin/* endpoints, including `/audit-log` (raw security events with user ids).",
      "fix_sketch": "Wrap the mount in a small ASGI throttle keyed on the peer/XFF, or move the static gate behind a router-level dependency so `@limiter.limit` applies. Independently, generate ADMIN_API_KEY as 32+ random bytes and document the rotation like the Serper playbook.",
      "known_item": false
    },
    {
      "id": "security-18",
      "title": "Prompt-injection detection only logs; the documented injection_attempt audit event is never emitted, and the raw query is logged",
      "file": "app/services/extraction_service.py",
      "line": 1313,
      "severity": "P3",
      "category": "monitoring",
      "claim": "`check_injection_patterns` is called at exactly one site and its only effect is `logger.warning(f'Injection pattern detected in query: {query[:100]}')` - the request proceeds unchanged and no `injection_attempt` row is written, despite CLAUDE.md listing that event type as part of the audit surface.",
      "evidence": "app/services/extraction_service.py:1312-1315: `sanitized_query = sanitize_prompt_input(query, max_length=500)` then `if check_injection_patterns(query): logger.warning(...)` - no branch, no raise, no audit call. `grep -rn 'check_injection_patterns' app/` returns exactly two hits: the import at extraction_service.py:20 and this call. app/services/audit_service.py exposes `log_audit_event` (whose docstring at :26 lists `injection_attempt` as a valid event_type) and `log_content_blocked` - neither is called from any injection site. CLAUDE.md states audit_service fires 'security events to admin_audit_log (login/lockout/usage_limit/injection_attempt)'.",
      "failure_scenario": "The /admin/audit-log endpoint will never return an `injection_attempt` row, so an operator reviewing the audit surface concludes there have been zero attempts when the detector may be firing constantly. Secondarily, the raw first 100 chars of the offending query are written to stdout logs, so the attack payload lands in logs rather than in the queryable audit table.",
      "fix_sketch": "`fire_and_forget(log_audit_event(event_type='injection_attempt', user_id=..., endpoint=..., details={'query_hash': sha256(query)[:16]}), label='audit.injection_attempt')` at the detection site, and log a hash rather than the payload. Decide explicitly whether detection should also block - today it is detect-and-continue with no record.",
      "known_item": false
    },
    {
      "id": "security-19",
      "title": "CORS defaults ship localhost origins with allow_credentials=True, and CORS_ORIGINS='*' would be accepted",
      "file": "app/main.py",
      "line": 86,
      "severity": "P3",
      "category": "cors",
      "claim": "`_DEFAULT_ORIGINS` includes three `http://localhost:*` entries and is used verbatim in production whenever `CORS_ORIGINS` is unset, with `allow_credentials=True`; and `_get_allowed_origins` performs no validation, so setting `CORS_ORIGINS=*` would be honoured.",
      "evidence": "app/main.py:86-107 - `_DEFAULT_ORIGINS = ['https://web-production-58776.up.railway.app', 'http://localhost:8000', 'http://localhost:19006', 'http://localhost:8081']`; `_get_allowed_origins()` splits `CORS_ORIGINS` on commas with no allowlist or wildcard rejection; `app.add_middleware(CORSMiddleware, allow_origins=ALLOWED_ORIGINS, allow_credentials=True, ...)`. CLAUDE.md lists `CORS_ORIGINS` as Optional, so the default path is the live path unless someone set it.",
      "failure_scenario": "Any page a victim can be induced to load from `http://localhost:8081` or `http://localhost:19006` (a locally running dev server, a malicious Electron/desktop app, a compromised local tool) gets credentialed cross-origin access to the production API. And a future operator setting `CORS_ORIGINS=*` to 'fix' a CORS error silently converts the API to origin-reflection-with-credentials, since Starlette's CORSMiddleware pairs `allow_origin_regex`-free `['*']` with credentials by echoing the request Origin.",
      "fix_sketch": "Split the defaults: production defaults to the Railway origin only; the localhost entries are added only when `RAILWAY_ENVIRONMENT` is unset. Reject `*` in `_get_allowed_origins` when `allow_credentials=True` (raise at startup rather than degrade silently).",
      "known_item": false
    },
    {
      "id": "security-20",
      "title": "verify_admin_key 500s instead of 403 on a non-ASCII X-Admin-Key",
      "file": "app/api/admin_routes.py",
      "line": 37,
      "severity": "P3",
      "category": "error-handling",
      "claim": "`hmac.compare_digest(x_admin_key, expected)` on two `str` arguments raises `TypeError` when either contains a non-ASCII codepoint, so a header like `X-Admin-Key: \\u00e9` produces an unhandled exception rather than the intended 403.",
      "evidence": "app/api/admin_routes.py:34-39: `def verify_admin_key(x_admin_key: str = Header(...)): expected = os.getenv('ADMIN_API_KEY', ''); if not expected or not hmac.compare_digest(x_admin_key, expected): raise HTTPException(403, ...)`. CPython's `hmac.compare_digest` requires both str operands to be ASCII-only and raises `TypeError: comparing strings with non-ASCII characters is not supported`. The same construct in app/main.py:191/:200 (the static mount) has the identical exposure; app/api/history_routes.py:150/:188 compares UUID strings so it is safe.",
      "failure_scenario": "`curl -H 'X-Admin-Key: \\u00e9' https://<host>/api/v1/admin/costs` returns a 500 with `code: SERVER_ERROR` and fires `sentry_sdk.capture_exception` (error_handler.py:152), turning a trivially-sendable header into a Sentry-noise generator and a 403-vs-500 oracle. Not a bypass - the failure is closed - but it is an unauthenticated path to a 500.",
      "fix_sketch": "Compare bytes: `hmac.compare_digest(x_admin_key.encode('utf-8'), expected.encode('utf-8'))`, in admin_routes.py and both sites in main.py.",
      "known_item": false
    },
    {
      "id": "security-21",
      "title": ".claude/settings.local.json is committed with a blanket Bash allowlist every clone inherits",
      "file": ".claude/settings.local.json",
      "line": 4,
      "severity": "P3",
      "category": "supply-chain",
      "claim": "The per-machine agent settings file is tracked in git and grants unconditional `Bash(python:*)`, `Bash(curl:*)`, `Bash(pip install:*)`, `Bash(npm install:*)` and `Bash(git push:*)`, so any clone or worktree of this repo starts with those approvals already in place.",
      "evidence": "`.claude/settings.local.json` is present in the pinned tree (it is not in .gitignore, whose agent-scratch entries are `.qa-*/`, `.agents/`, `.codex/`). Its `permissions.allow` array contains `\"Bash(python:*)\"`, `\"Bash(curl:*)\"`, `\"Bash(pip install:*)\"`, `\"Bash(npm install:*)\"`, `\"Bash(git push:*)\"` and `\"Bash(git commit:*)\"` alongside Supabase MCP write tools (`mcp__plugin_supabase_supabase__apply_migration`, `execute_sql`).",
      "failure_scenario": "`Bash(python:*)` plus `Bash(curl:*)` is a full arbitrary-execution grant - an agent following a poisoned instruction found in scraped page content, a fixture, or a doc can run any command without a prompt, and `Bash(git push:*)` lets the result reach main. The whole point of the `.local.json` suffix is that it stays per-machine; committing it makes one operator's accumulated trust decisions the default for everyone who clones.",
      "fix_sketch": "`git rm --cached .claude/settings.local.json` and add `.claude/settings.local.json` to .gitignore (keep the shared `.claude/settings.json`). Re-grant per machine, and narrow `Bash(python:*)`/`Bash(curl:*)` to the specific invocations that were actually needed.",
      "known_item": false
    },
    {
      "id": "security-22",
      "title": "Admin /costs runs an unbounded month-wide select, so the number is silently truncated and the endpoint is a memory risk",
      "file": "app/api/admin_routes.py",
      "line": 110,
      "severity": "P3",
      "category": "availability",
      "claim": "`api_costs` selects every `comparisons.metadata` row since the start of the month with no `.limit()` and no pagination, then sums in Python - PostgREST silently caps the response, so the reported OpenAI cost is wrong once the month exceeds that cap.",
      "evidence": "app/api/admin_routes.py:107-113: `supabase.table('comparisons').select('metadata').gte('created_at', month_start).execute()` then `for row in (result.data or []): openai_cost += meta.get('total_cost', 0)`. No `.limit()`, no `.range()`. The count query immediately below (:122) correctly uses `count='exact'` and does not fetch rows, which makes the mismatch visible: `comparisons_this_month` will keep climbing while `openai.cost_usd` plateaus.",
      "failure_scenario": "Once a month exceeds PostgREST's default max-rows, `summary['openai']['cost_usd']` and therefore `avg_cost_per_comparison` and `estimated_monthly_total` are all understated, with no error and no warning - the cost dashboard that CLAUDE.md points at for spend decisions quietly reports a floor instead of a total. Before that cap, pulling every metadata JSONB blob for a month into the dyno is also a needless memory spike on a 30/minute endpoint.",
      "fix_sketch": "Do the sum in Postgres - an RPC or a view returning `sum((metadata->>'total_cost')::numeric)` for the window - or page with `.range()` until exhausted. Assert in a test that the returned cost is consistent with `comparisons_this_month` at row counts above the PostgREST cap.",
      "known_item": false
    }
  ]
}
