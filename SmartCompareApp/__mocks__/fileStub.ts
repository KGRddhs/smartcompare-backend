// Stub for binary asset requires (TTF, OTF, WOFF, PNG, JPG) under Jest.
// React Native bundler resolves these to numeric resource handles at build
// time; in tests we just need a value that won't crash imports.
export default 0;
