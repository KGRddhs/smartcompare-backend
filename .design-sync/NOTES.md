# design-sync notes — Qaren Design System

Project: **Qaren Design System** (`c57eb43f-cabf-47bc-b980-ff4956512b26`) on claude.ai/design.

## This is a hand-authored project — the converter does NOT apply

`SmartCompareApp/` is **React Native + Expo**. It does not render in a browser, and its
components import directly from `react-native`. So this design-system project is **not** a
compiled-`dist/` bundle produced by the `/design-sync` converter (`package-build.mjs`, the
storybook/package shapes). Instead it holds **hand-authored web JSX references** that mirror
the RN screens:

- `ui_kits/mobile/<Screen>.jsx` — a plain-React (web, inline-style) reimplementation that
  reads tokens from `window.qarenTokens` and exports `window.Qaren<Screen>`.
- `ui_kits/mobile/<screen>.html` — a preview card (babel-standalone) that loads the `.jsx`
  inside `ios-frame.jsx` and carries the `<!-- @dsCard … -->` marker the pane indexes.

**A re-sync = diff the RN source-of-truth against the matching `.jsx` reference and integrate
by hand.** Do NOT point the converter at `SmartCompareApp`.

## Page → source-of-truth map (extend as pages are synced)

| Reference | RN source of truth |
|---|---|
| `ui_kits/mobile/ResultsScreen.jsx` | `SmartCompareApp/src/components/results/ResultsContent.tsx` + `ResultsAccordion.tsx` + `RunnerUpWinsCard.tsx` + `CategoryProfile.tsx` + `DimensionBars.tsx` + `ConfidencePills.tsx` + `FactualVerdict.tsx` + `PersonalizationChip.tsx` + `TopMatchBadge.tsx`; copy in `src/i18n/en.json` `results.*` |

### Results is CATEGORY-DRIVEN (all 9 categories)

`ResultsScreen.jsx` is ONE generic shell + a `CATEGORIES` config covering all 9 Qaren
categories (electronics, grocery, supplements, makeup, skincare, haircare, fragrances,
fashion, other). This mirrors the app exactly: the layout is fixed, the **data** is
per-category. When the per-category structure changes, update it from these backend
sources (NOT invented):

| What | Backend source |
|---|---|
| 6 dims per category + display labels | `app/services/scoring_service.py` `CATEGORY_DIMENSIONS`, `DIMENSION_DISPLAY_NAMES` (bars show the top-4 hero dims) |
| Specs schema + ordered "At a glance" fields | `app/services/extraction_service.py` `CATEGORY_SPEC_SCHEMAS`, `build_category_profile` (At a glance = the populated subset, in schema order) |
| Price-comparability basis (like-for-like unit) | `app/services/price_service.py` `CATEGORY_FAIRNESS` (electronics→GB, fragrances/haircare→ml, supplements→count, grocery/makeup/skincare→weight·vol, **fashion/other→None** → no basis caption) |

Each category has its OWN preview card: `results.html` (Electronics) + `results-<category>.html`,
all loading the shared `ResultsScreen.jsx` via `<QarenResultsScreen category="<key>"/>`. Add a
new category only if `CATEGORY_SPEC_SCHEMAS` gains one (the schema keyset is frozen via
`canonicalize_category()`). Preview cards are regenerated by `.design-sync/staging/_gen_previews.mjs`
(local-only; not uploaded).

## Authoring conventions for the `.jsx` references

- React is a UMD global — use `React.useState`, not a bare `useState` import.
- Style with inline `style={{…}}` + `window.qarenTokens` colors (`C_res.accent`,
  `accentDark`, `accentLight`, `warning`, `text.{primary,secondary,placeholder}`,
  `border.{light,medium}`, `bg.{primary,secondary}`). All exist in the token block.
- Use the **real shipped copy** from `src/i18n/en.json` (e.g. "Why this fits you",
  "Where the runner-up wins", "What we know", "Dig deeper", "At a glance",
  "Pricing lands in an upcoming update."). Copy must pass `src/i18n/.copy-policy.json`.
- Hard invariants to preserve in any results edit: ratings are NEVER AI-generated (stars
  only with a real rating); reviews are PARAPHRASED praise (no verbatim quotes, source
  domains, or `[N]` citations); the runner-up block stays **gray** (emerald = winner signal).

## History

- **2026-06-18** — First `/design-sync` of the Results page. Integrated the post-Bundle-E
  drift into `ui_kits/mobile/ResultsScreen.jsx` AND made it category-driven for all 9
  categories. Changes vs the old Bundle-E reference:
    - "Where the runner-up wins" card (Task #24) — runner-up name + tradeoff + winning dims, gray.
    - "At a glance" (CategoryProfile) as the FIRST "Dig deeper" section.
    - Reviews: source-quote `ReviewLine` → per-product paraphrased `ReviewPraise` (no quotes/domains/[N]).
    - Specs: header row (product names) + emerald per-row winner cells.
    - Verdict: `FactualVerdict` two-line (line1 factual/no-evaluative-words, line2 conditional italic) + `PersonalizationChip` chip.
    - Dimension bars: ONE compact "A · B" legend (was per-row); per-dim emerald winner side.
    - Product card: name/brand/variant split + price-pending state.
    - **NEW — all 9 categories**: each with its own dims / At a glance fields / Specs schema /
      fairness basis, driven from the backend schemas. 9 preview cards
      (`results.html` Electronics + `results-<category>.html`).
  JSX validated via `@babel/parser` (classic-script + jsx). Scope = Results-only (per user);
  other `ui_kits/mobile/*.jsx` references were NOT touched and may be stale vs their RN screens.
    - Added `ui_kits/mobile/results-structure.html` — a "Results — Category Structure" reference
      card documenting, per category, the 6 dims (hero-4 flagged), the Specs/At-a-glance field
      order, and the fairness basis — straight from the backend schemas.
    - Manual correctness pass (Codex was blocked on auth — see below): fixed electronics
      At-a-glance label "Chipset" → "Processor" (the real `processor` schema field), and aligned
      the structure card's makeup/other dim order to weight order. Verified all 9 categories'
      hero dims, spec field order/labels, and fairness units against the backend; no banned
      evaluative words in any verdict line; runner-up gray; reviews paraphrased; pending copy exact.
    - **Codex review DONE** (`codex:codex-rescue`, after re-auth). 5 findings, gated against the
      real code: 4 REAL field-order/tie-break bugs FIXED + uploaded — (1) skincare At-a-glance
      `Active ingredient`↔`Skin type` order; (2) haircare `Volume` before `Scent` (At-a-glance +
      Specs); (3) fragrances Specs `Volume` before `Concentration`; (4) supplements Dosage bar
      `84/84` tie → made `DimensionBar` resolve ties to the overall winner (`winnerIndex={data.winner}`,
      mirroring `DimensionBars.tsx`). 1 FALSE POSITIVE rejected: "ios-frame.jsx missing" — it EXISTS
      in the project (`ui_kits/mobile/ios-frame.jsx`); Codex only saw the staging dir, which holds
      only the changed files. Lesson reaffirmed ([[feedback-verify-llm-reviewer-findings]]): gate
      every reviewer finding against the real tree — a "must-fix" was a sandbox blind spot.
      Independently re-verified all 9 categories' At-a-glance + Specs are in ascending schema order.

- **2026-06-22** — Re-sync of the Results reference to the **fragrance-content-quality**
  clean contract (branch `feature/frag-content-quality-fe`; the FE wave is already committed
  there). RN source-of-truth re-read: `ResultsContent.tsx` / `ResultsAccordion.tsx` /
  `RunnerUpWinsCard.tsx` / `DimensionBars.tsx` / `CategoryProfile.tsx` / `FactualVerdict.tsx`
  / `_deltaText.ts` + `en.json` `results.*`. Integrated into
  `ui_kits/mobile/ResultsScreen.jsx` (staging = the canonical, currently-uploaded copy;
  the older `docs/claude-design-handoff/ui_kits/mobile/ResultsScreen.jsx` is the pre-2026-06-18
  Bundle-E shell and was deliberately left untouched, as in the 2026-06-18 sync). Surgical —
  only what DRIFTED changed; the 2026-06-18 sync already satisfied most of the new contract:
    - **Point 1 — verdict prose qualitative (NO score internals): ALREADY CORRECT.** Audited
      every category's `verdict.line1/line2` with the shipped `SCORE_INTERNALS_RE` +
      FactualVerdict `BANNED_WORDS` — zero hits. The verdict lines use factual price/rating/spec
      deltas ("27 BHD more", "0.2★ higher", "longer wear"), which ARE the FactualVerdict contract
      and are NOT score internals. Documented the two-guard contract in the header.
    - **Point 2 — dimension delta captions qualitative (no "+Npt"): ALREADY CORRECT.** All
      dimension labels + runner-up dim phrases are qualitative; ran `POINT_MATH_RE` / `safeDelta`
      logic over them — clean. Documented the `safeDelta` swap-to-label guard.
    - **Point 3 — drop silent one-sided Specs rows (E3): DRIFTED → ADDED.** The `DetailsAccordion`
      Specs body + `specRowCount` now `.filter(missing !== 1)` (mirrors `ResultsAccordion`), with
      an `EM_DASH` constant. Added a demonstration one-sided electronics row (Water resistance:
      `IP68` vs `—`) — the filter drops exactly it (smoke: 8→7), both-value rows survive,
      both-`—` rows would survive.
    - **Point 4 — partial-result "still settling" affordance (F2): DRIFTED → ADDED.** A `partial`
      flag on a category renders the EXACT `results.partial.note` copy
      ("Prices are still settling — tap to refresh in a moment.") under the "Why this fits you"
      eyebrow. Set on `fragrances` (fragrance compares routinely hit the 30s hard-cap → partial).
      Smoke-verified it shows for fragrances, absent for electronics.
    - **Point 5 — fragrance contract: ALREADY CORRECT.** `fragrances` At-a-glance + Specs render
      `scent_family`, top/heart/base notes, `longevity`, `sillage`, `concentration` in ascending
      `CATEGORY_SPEC_SCHEMAS["fragrances"]` order (re-verified against `extraction_service.py`),
      with confirmed BHD prices; the price-pending state stays built-in via `ProductCard
      price={null}` (documented + the suppression rule added to the header invariants).
  Invariants preserved: ratings never AI-generated, reviews paraphrased (no quotes/domains/[N]),
  runner-up gray. JSX validated with `@babel/parser` `{sourceType:'script', plugins:['jsx']}` —
  PARSE OK. Local-only validators used (`_audit_contract.mjs` = guard sweep, `_validate.mjs` =
  parse + behavior smoke); left untracked (not uploaded). Previews regenerated by
  `_gen_previews.mjs` (byte-identical — they only load the shared JSX). **claude.ai/design upload
  PENDING** — that needs the design tool/auth (a separate step); only the LOCAL refs were updated.
